---
title: "CF-VLA: Efficient Coarse-to-Fine Action Generation for Vision-Language-Action Policies"
title_zh: CF-VLA：视觉-语言-动作策略的高效粗到细动作生成
authors: "Fan Du, Feng Yan, Jianxiong Wu, Xinrun Xu, Weiye Zhang, Weinong Wang, Yu Guo, Bin Qian, Zhihai He"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24622v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于动作生成的视觉-语言-动作策略
tldr: CF-VLA 针对基于流匹配的视觉-语言-动作（VLA）策略推理效率低的问题，提出了一种从粗到精的两阶段动作生成框架。该方法通过粗略阶段将高斯噪声转化为结构化初始动作，再通过精细阶段进行单步修正，在极低推理步数下实现了高性能。实验表明，其在 CALVIN 和 LIBERO 等基准测试中显著降低了延迟并提升了机器人操作成功率。
selection_source: fresh_fetch
motivation: 现有的基于流匹配的 VLA 策略从无信息的高斯噪声中恢复动作结构需要多步推理，导致实时性与生成质量之间存在矛盾。
method: 提出一种两阶段生成方式，先通过粗略阶段学习条件后验以构建结构化起点，再通过精细阶段进行单步局部误差修正。
result: "在低推理步数下，该方法将动作采样延迟降低了 75.4%，并在真实机器人实验中达到了 83.0% 的最高平均成功率。"
conclusion: 结构化的从粗到精生成机制能够有效平衡 VLA 策略的推理效率与动作生成质量。
---

## 摘要
基于流（Flow-based）的视觉-语言-动作（VLA）策略在动作生成方面具有强大的表达能力，但存在一个根本性的效率问题：需要多步推理才能从无信息的高斯噪声中恢复动作结构，导致在实时约束下效率与质量之间的权衡较差。我们通过重新思考生成式动作建模中起始点的作用来解决这一问题。我们没有缩短采样轨迹，而是提出了 CF-VLA，这是一种从粗到细的两阶段方案，将动作生成重构为一个构建动作感知起始点的粗略初始化步骤，随后是一个纠正残差误差的单步局部细化。具体而言，粗阶段学习端点速度的条件后验，将高斯噪声转换为结构化初始化，而细阶段则从该初始化开始执行固定时间的细化。为了稳定训练，我们引入了一种逐步策略，首先学习受控的粗略预测器，然后进行联合优化。在 CALVIN 和 LIBERO 上的实验表明，我们的方法在低 NFE（函数评估次数）机制下建立了强大的效率-性能前沿：它始终优于现有的 NFE=2 方法，在多个指标上达到或超过了 NFE=10 的 $π_{0.5}$ 基线，将动作采样延迟降低了 75.4%，并实现了 83.0% 的最佳平均真实机器人成功率，比 MIP 高出 19.5 个百分点，比 $π_{0.5}$ 高出 4.0 个百分点。这些结果表明，结构化的、从粗到细的生成能够同时实现强大的性能和高效的推理。我们的代码可在 https://github.com/EmbodiedAI-RoboTron/CF-VLA 获取。

## Abstract
Flow-based vision-language-action (VLA) policies offer strong expressivity for action generation, but suffer from a fundamental inefficiency: multi-step inference is required to recover action structure from uninformative Gaussian noise, leading to a poor efficiency-quality trade-off under real-time constraints. We address this issue by rethinking the role of the starting point in generative action modeling. Instead of shortening the sampling trajectory, we propose CF-VLA, a coarse-to-fine two-stage formulation that restructures action generation into a coarse initialization step that constructs an action-aware starting point, followed by a single-step local refinement that corrects residual errors. Concretely, the coarse stage learns a conditional posterior over endpoint velocity to transform Gaussian noise into a structured initialization, while the fine stage performs a fixed-time refinement from this initialization. To stabilize training, we introduce a stepwise strategy that first learns a controlled coarse predictor and then performs joint optimization. Experiments on CALVIN and LIBERO show that our method establishes a strong efficiency-performance frontier under low-NFE (Number of Function Evaluations) regimes: it consistently outperforms existing NFE=2 methods, matches or surpasses the NFE=10 $π_{0.5}$ baseline on several metrics, reduces action sampling latency by 75.4\%, and achieves the best average real-robot success rate of 83.0\%, outperforming MIP by 19.5 points and $π_{0.5}$ by 4.0 points. These results suggest that structured, coarse-to-fine generation enables both strong performance and efficient inference. Our code is available at https://github.com/EmbodiedAI-RoboTron/CF-VLA.

---

## 论文详细总结（自动生成）

### 论文总结：CF-VLA —— 高效的从粗到精视觉-语言-动作策略生成框架

#### 1. 论文的核心问题与整体含义
基于流匹配（Flow-matching）的视觉-语言-动作（VLA）策略（如 $\pi_{0.5}$）虽然在处理连续、多峰值的动作分布上表现出色，但面临严重的**推理效率瓶颈**。标准流模型从无信息的纯高斯噪声开始，需要多次迭代（高 NFE）才能恢复出有意义的动作结构，这在实时机器人控制中导致了生成质量与采样延迟之间的矛盾。论文的核心动机在于：**与其缩短采样轨迹，不如重构生成的起点**。

#### 2. 论文提出的方法论
CF-VLA 提出了一种**从粗到精（Coarse-to-Fine）**的两阶段动作生成架构：
*   **核心思想**：将生成过程分解为“全局对齐”和“局部细化”。首先通过一个粗略阶段将高斯噪声转化为靠近动作流形的“动作先验引导（AP-guided）”初始化点，然后通过一个精细阶段进行单步修正。
*   **关键技术细节**：
    *   **粗略阶段（Coarse Stage）**：学习一个关于端点速度的条件后验分布 $q_\theta(u|o, \epsilon_1)$。通过 KL 散度目标函数，将纯高斯噪声映射为结构化的初始化分布，使其质量集中在可行动作附近。
    *   **精细阶段（Fine Stage）**：从粗略阶段生成的起点出发，执行单步（NFE=1）的局部流形恢复，使用 MSE 损失进行监督。
*   **逐步训练策略（Stepwise Training）**：
    *   **第一阶段（稳定化）**：在受控的代理输入下训练端点表示，避免初期粗略输出不稳定导致细化分支崩溃。
    *   **第二阶段（联合优化）**：对粗略后验预测和细化分支进行全耦合训练，使两者相互增强。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：评估多任务泛化能力，涵盖 Spatial、Object、Goal、Long 四个套件。
    *   **CALVIN**：评估长程指令遵循能力（D→D 设置），关注连续完成 1-5 条指令的成功率。
    *   **真实机器人**：在 Franka 机器人上执行 5 类任务（抓取、放置、擦桌子、倒水、双臂折叠毛巾）。
*   **对比方法（Baselines）**：
    *   **低 NFE 方法**：MIP（NFE=2）、$\pi_{0.5}$ (NFE=2)、UniAct 等。
    *   **高 NFE/自回归方法**：$\pi_{0.5}$ (NFE=10)、OpenVLA、RT-2、Cosmos Policy 等。
*   **评估指标**：成功率、平均序列长度（CALVIN）、动作采样延迟（Latency）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：采用 PyTorch FSDP（完全分片数据并行），全局 Batch Size 为 16，学习率为 $5 \times 10^{-5}$。
*   **时长**：在 LIBERO 上训练了 60,000 步。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流仿真基准和 5 类真实机器人任务，每项仿真任务进行 10 次试验，真实机器人任务每项进行 20-40 次试验。
*   **消融实验**：非常充分。作者分别验证了“无第一阶段稳定化”、“无第二阶段联合优化”、“无方差建模”以及“无细化步骤”对性能的影响。
*   **客观性**：通过在相同的回放过滤数据上重新训练 $\pi_{0.5}$ 和 MIP 等基线模型，确保了对比的公平性。此外，还验证了该方法在不同模型骨干（如 PaliGemma 2）上的可迁移性。

#### 6. 论文的主要结论与发现
*   **效率飞跃**：在 NFE=2 的情况下，CF-VLA 将动作采样延迟降低了 **75.4%**（从 29.17ms 降至 7.81ms）。
*   **性能领先**：在低推理步数下，CF-VLA 显著优于 MIP 和低步数版的 $\pi_{0.5}$。在 CALVIN 长程任务中，其 5 指令成功率比 NFE=2 的 $\pi_{0.5}$ 高出 4.9 个百分点。
*   **真实机器人表现**：实现了 **83.0%** 的平均成功率，在擦桌子、倒水等接触密集型任务中表现尤为突出，证明了其在实时闭环控制中的鲁棒性。

#### 7. 优点
*   **即插即用**：该框架不依赖特定的 VLM 骨干，可无缝集成到任何基于流的 VLA 策略中。
*   **结构化创新**：通过重构起始点而非单纯缩短轨迹，解决了流模型在极低步数下“全局运输”与“局部修正”相互竞争的问题。
*   **训练稳定**：提出的两阶段逐步训练策略有效解决了粗细阶段耦合时的优化不稳定问题。

#### 8. 不足与局限
*   **性能上限**：虽然在低 NFE 下表现优异，但与 NFE=100 或集成复杂世界模型（如 Cosmos Policy）的方法相比，在某些极复杂任务上仍存在微小差距。
*   **特定失败模式**：在真实机器人实验中，双臂协作时的释放时机控制以及视觉遮挡（如倒水时手部遮挡相机）仍是导致失败的主要原因。
*   **超参数敏感性**：粗略阶段的方差（noise_var）和损失权重（$\lambda$）需要针对不同场景进行一定程度的调优以达到最佳平衡。

（完）
