---
title: "SpaAct: Spatially-Activated Transition Learning with Curriculum Adaptation for Vision-Language Navigation"
title_zh: SpaAct：面向视觉语言导航的带课程自适应的空间激活转移学习
authors: "Pengna Li, Kangyi Wu, Shaoqing Xu, Fang Li, Hanbing Li, Lin Zhao, Kailin Lyu, Long Chen, Zhi-Xin Yang, Nanning Zheng"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27620v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有动作推理能力的视觉语言导航智能体
tldr: 针对视觉语言导航（VLN）任务，本文提出SpaAct框架，旨在通过激活视觉语言模型（VLM）的动态空间感知能力来提升导航性能。该框架引入了动作回顾和未来帧选择两个轻量级任务，分别强化后向动作推理和前向状态预测，并结合TriPA课程学习策略由易到难地训练模型。实验证明，该方法在VLN-CE基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在导航中缺乏动态空间感知，需要同时具备理解动作原因（后向推理）和预测动作结果（前向预测）的能力。
method: 提出了SpaAct框架，包含动作回顾与未来帧选择两个空间激活任务，并设计了TriPA三因子渐进式课程学习方法来优化训练过程。
result: 在标准的VLN-CE基准测试中，SpaAct显著提升了基于VLM的导航性能，并取得了当前最先进的性能表现。
conclusion: 通过结合空间激活任务与循序渐进的课程学习，SpaAct有效地增强了VLM在复杂3D环境中的动态空间推理和长程导航能力。
---

## 摘要
视觉语言导航 (VLN) 旨在使具身智能体能够遵循自然语言指令，并在未见的 3D 环境中导航至目标位置。我们认为，将视觉语言模型 (VLM) 适配到 VLN 需要赋予它们两种互补的能力以获取此类意识，即后向动作推理（为什么）和前向转移预测（如何）。基于这一见解，我们提出了 SpaAct，这是一个简单而有效的训练框架，旨在激活 VLM 中的动态空间意识。具体而言，SpaAct 引入了两个空间激活任务：动作回顾 (Action Retrospection)，要求模型从视觉转移中推断执行的动作序列；以及未来帧选择 (Future Frame Selection)，迫使模型在给定历史和动作的条件下预测视觉转移。这两个目标为后向动作推理和前向转移预测提供了轻量级监督，鼓励模型以一种对 VLM 友好的方式构建动态空间意识。为了进一步稳定适配过程，我们设计了 TriPA，一种三因子渐进式自适应课程学习方法，该方法将训练样本按由易到难的顺序组织，使模型能够逐渐习得从基础运动到长程推理的导航技能。在标准 VLN-CE 基准测试上的实验表明，SpaAct 持续提升了基于 VLM 的导航性能，并达到了当前最先进的水平。我们将发布代码和模型以支持未来的研究。

## Abstract
Vision-and-Language Navigation (VLN) aims to enable an embodied agent to follow natural-language instructions and navigate to a target location in unseen 3D environments. We argue that adapting VLMs to VLN requires endowing them with two complementary capabilities for acquiring such awareness, namely backward action reasoning (why) and forward transition prediction~(how). Based on this insight, we propose SpaAct, a simple yet effective training framework that activates the dynamic spatial awareness in VLMs. Specifically, SpaAct introduces two spatial activation tasks: Action Retrospection, which asks the model to infer the executed action sequence from visual transitions, and Future Frame Selection, which forces the model to predict the visual transitions conditioned on history and action. These two objectives provide lightweight supervision on both backward action reasoning and forward transition prediction, encouraging the model to build dynamic spatial awareness in a VLM-friendly way. To further stabilize adaptation, we design TriPA, a Tri-factor Progressive Adaptive curriculum learning method that organizes training samples from easy to hard, allowing the model to gradually acquire navigation skills from basic locomotion to long-horizon reasoning. Experiments on standard VLN-CE benchmarks show that SpaAct consistently improves VLM-based navigation and achieves state-of-the-art performance. We will release the code and models to support future research.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SpaAct** 的视觉语言导航（VLN）训练框架，旨在通过激活视觉语言模型（VLM）的动态空间感知能力来提升其在复杂 3D 环境中的导航表现。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于 VLM 的导航方法虽然具备强大的语义理解能力（知道“是什么”），但缺乏**动态空间感知能力**（不知道动作如何导致视觉变化，即“怎么去”）。
*   **研究背景**：直接将观察结果映射到动作的端到端学习容易导致“浅层模式匹配”，而非真正的空间推理。
*   **核心动机**：作者认为，要让 VLM 胜任导航任务，必须赋予其两种互补能力：**后向动作推理**（理解视觉变化的原因）和**前向转移预测**（预判动作的结果）。

### 2. 方法论
SpaAct 框架由两个核心空间激活任务和一个课程学习策略组成：

*   **动作回顾（Action Retrospection, AR）**：
    *   **核心思想**：给定同一轨迹中的两个连续观察图像，要求模型推断导致这一视觉变化的动作序列。
    *   **技术细节**：将几何回归任务转化为 VLM 擅长的语言描述任务，通过预测动作 Token（如 `MOVE_FORWARD`, `TURN_LEFT`）来隐式学习 3D 一致性和逆向动力学。
*   **未来帧选择（Future Frame Selection, FFS）**：
    *   **核心思想**：给定历史观察、当前观察和预定动作，从四个候选图像中选择正确的下一帧。
    *   **技术细节**：采用判别式而非生成式方法，降低计算开销。通过引入“同轨迹硬负样本”（时间接近但动作不符）来增强模型对细微几何变化的辨别力。
*   **TriPA 课程学习（Tri-factor Progressive Adaptive Curriculum）**：
    *   **核心思想**：根据样本难度由易到难组织训练，稳定 VLM 的适配过程。
    *   **难度因子**：综合考虑**轨迹复杂度**（路径长度）、**指令复杂度**（字数）和**运动复杂度**（转向动作密度）。
    *   **公式**：$d_i = \alpha s_{traj} + \beta s_{instr} + \gamma s_{turn}$。训练分为四个阶段，逐步增加高难度样本的采样权重。

### 3. 实验设计
*   **数据集与场景**：在 **R2R-CE** 和 **RxR-CE**（连续环境下的视觉语言导航）基准测试上进行验证，这些数据集基于 Matterport3D 的高保真扫描环境。
*   **Benchmark 与指标**：主要评估指标包括成功率（SR）、成功加权路径长度（SPL）、导航误差（NE）和动态时间规整（nDTW）。
*   **对比方法**：对比了包括 JanusVLN、NaVILA、StreamVLN、CMA、VLN$\circlearrowright$BERT 等在内的多种 SOTA 方法。
*   **真实世界部署**：在 Unitree Go2 四足机器人上进行了零样本（Zero-shot）迁移实验。

### 4. 资源与算力
*   **算力设备**：论文提到在部署阶段使用了远程服务器，配备了 **NVIDIA H20 GPU** 用于实时推理。
*   **训练细节**：
    *   第一阶段：使用 0.69M AR/FFS 样本和 2.4M 导航样本进行联合训练。
    *   第二阶段：引入 DAgger 数据和 ScaleVLN 合成数据（约 10.5M 样本）进一步微调。
*   **说明**：文中未明确给出具体的训练总时长或训练时使用的 GPU 总数。

### 5. 实验数量与充分性
*   **实验规模**：作者在两个主流 VLN-CE 数据集上进行了详尽测试，并提供了两阶段训练的结果对比。
*   **消融实验**：针对 AR、FFS 和 TriPA 分别做了消融实验，验证了每个组件的独立贡献及其互补性。
*   **充分性评价**：实验设计较为充分，涵盖了从仿真环境到真实机器人的跨度。通过损失函数曲线分析和不同指令长度下的性能分析，客观地展示了方法的稳定性。

### 6. 主要结论与发现
*   **性能提升**：SpaAct 在 R2R-CE 和 RxR-CE 上均取得了 SOTA 性能。在不使用额外数据的情况下，SpaAct-stage1 显著优于基准模型。
*   **空间感知的重要性**：AR 和 FFS 任务能有效激活 VLM 的空间推理能力，使其在处理长指令和复杂转向点时表现更稳健。
*   **课程学习的价值**：TriPA 策略显著降低了训练初期的梯度不稳定，使模型能够更好地从基础运动过渡到长程推理。

### 7. 优点
*   **轻量且兼容**：AR 和 FFS 任务设计巧妙，完全兼容 VLM 原有的语言预测范式，无需引入复杂的几何回归模块。
*   **推理高效**：空间激活任务仅在训练阶段使用，推理时保持了原有 VLM 的轻量化架构。
*   **泛化能力强**：在仿真环境训练后，能直接部署到真实物理环境（Unitree Go2），展现了良好的零样本迁移能力。

### 8. 不足与局限
*   **Sim-to-Real 差距**：尽管有成功案例，但作者承认仿真环境与现实世界在相机配置、机器人动力学方面仍存在差异。
*   **负样本依赖**：FFS 任务的有效性高度依赖于负样本的质量，若负样本区分度不足，可能限制模型学习上限。
*   **应用限制**：目前主要针对低级动作控制（Low-level actions），对于更高层级的逻辑规划（如复杂任务拆解）探讨较少。

（完）
