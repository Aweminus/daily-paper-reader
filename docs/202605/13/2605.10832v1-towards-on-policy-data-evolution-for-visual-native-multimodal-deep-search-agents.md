---
title: Towards On-Policy Data Evolution for Visual-Native Multimodal Deep Search Agents
title_zh: 迈向原生视觉多模态深度搜索智能体的同策略数据演化
authors: "Shijue Huang, Hangyu Guo, Chenxin Li, Junting Lu, Xinyu Geng, Zhaochen Su, Zhenyu Li, Shuang Chen, Hongru Wang, Yi R. Fung"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10832v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有视觉推理能力的多模态深度搜索智能体
tldr: 针对多模态深度搜索智能体中视觉证据不可复用和训练数据静态的问题，本文提出了一种视觉原生智能体框架。该框架引入“图像库引用协议”实现中间视觉证据的持续利用，并结合“同策略数据演化（ODE）”机制，通过闭环生成与当前模型能力匹配的训练数据。实验表明，该方法显著提升了模型在复杂搜索任务中的表现，在多个基准测试中超越了Gemini-2.5 Pro等强力模型。
selection_source: fresh_fetch
motivation: 现有多模态智能体无法复用搜索过程中的中间视觉证据，且静态训练数据难以适应模型不断进化的能力。
method: 提出图像库引用协议以实现视觉证据的跨工具复用，并利用同策略数据演化（ODE）闭环生成针对性的SFT和RL训练数据。
result: ODE显著提升了Qwen3-VL系列模型的性能，在8项基准测试中平均得分大幅增长，甚至在特定规模下超越了Gemini-2.5 Pro。
conclusion: 视觉证据的复用与基于模型反馈的动态数据演化是构建高性能多模态搜索智能体的关键。
---

## 摘要
多模态深度搜索要求智能体通过在不断演变的文本和视觉上下文中链接搜索、工具使用和视觉推理，来解决开放世界问题。当前系统受限于两个瓶颈：首先，现有的工具使用框架将搜索、浏览或转换返回的图像视为瞬时输出，导致中间视觉证据无法被后续工具再次利用；其次，训练数据通常由固定的策划方案构建，无法追踪目标智能体不断演进的能力。为了应对这些挑战，我们首先引入了一种以图像库引用协议为核心的原生视觉智能体框架，该协议将每个工具返回的图像注册为可寻址的引用，使中间视觉证据可供后续工具重复使用。在此框架之上，同策略数据演化（ODE）运行一个闭环数据生成器，该生成器根据正在训练的策略的采样轨迹进行跨轮次的自我优化。这种逐轮优化使得每一轮的数据都能针对当前策略仍需学习的内容。该框架同时支持多样化的监督微调数据和策略感知的强化学习数据策划，覆盖了目标智能体的完整训练生命周期。在 8 个多模态深度搜索基准测试中，ODE 将 Qwen3-VL-8B 智能体的平均表现从 24.9% 提升至 39.0%，在标准智能体工作流设置下超过了 Gemini-2.5 Pro (37.9%)。在 30B 参数规模下，ODE 将平均得分从 30.6% 提高到 41.5%。进一步的分析验证了图像库复用的有效性，特别是在需要迭代视觉优化的复杂任务上；同时，与静态合成相比，基于采样反馈的演化产生了更具事实依据的 SFT 轨迹和更匹配策略的 RL 任务。

## Abstract
Multimodal deep search requires an agent to solve open-world problems by chaining search, tool use, and visual reasoning over evolving textual and visual context. Two bottlenecks limit current systems. First, existing tool-use harnesses treat images returned by search, browsing, or transformation as transient outputs, so intermediate visual evidence cannot be re-consumed by later tools. Second, training data is usually built by fixed curation recipes that cannot track the target agent's evolving capability. To address these challenges, we first introduce a visual-native agent harness centered on an image bank reference protocol, which registers every tool-returned image as an addressable reference and makes intermediate visual evidence reusable by later tools. On top of this harness, On-policy Data Evolution (ODE) runs a closed-loop data generator that refines itself across rounds from rollouts of the policy being trained. This per-round refinement makes each round's data target what the current policy still needs to learn. The same framework supports both diverse supervised fine-tuning data and policy-aware reinforcement learning data curation, covering the full training lifecycle of the target agent. Across 8 multimodal deep search benchmarks, ODE improves the Qwen3-VL-8B agent from 24.9% to 39.0% on average, surpassing Gemini-2.5 Pro in standard agent-workflow setting (37.9%). At 30B, ODE raises the average score from 30.6% to 41.5%. Further analyses validate the effectiveness of image-bank reuse, especially on complex tasks requiring iterative visual refinement, while rollout-feedback evolution yields more grounded SFT traces and better policy-matched RL tasks than static synthesis.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards On-Policy Data Evolution for Visual-Native Multimodal Deep Search Agents》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于**多模态深度搜索（Multimodal Deep Search）**领域，即智能体需要通过链式调用搜索、工具使用和视觉推理来解决开放世界问题。
*   **研究动机**：现有的多模态智能体存在两个核心瓶颈：
    1.  **视觉证据不可复用**：现有的工具链将搜索或转换生成的图像视为临时输出，后续工具无法直接调用这些中间视觉证据。
    2.  **训练数据静态化**：现有的训练数据多基于固定配方生成，无法根据智能体在训练过程中不断进化的能力进行动态调整，导致数据与模型能力脱节。
*   **整体含义**：本文旨在通过“视觉原生”的架构设计和“同策略”的数据演化机制，构建能够高效处理复杂视觉证据并持续自我进化的深度搜索智能体。

### 2. 论文提出的方法论
论文提出了一个协同设计的框架，包含两个核心组件：

*   **视觉原生智能体框架（Visual-Native Agent Harness）**：
    *   **核心思想**：引入**图像库引用协议（Image Bank Reference Protocol）**。
    *   **技术细节**：将原始图像和所有工具返回的图像注册为可寻址的引用（如 `<image:N>`）。这使得智能体可以像处理文本一样，在后续步骤中对中间生成的图像进行缩放（Zoom-in）、旋转、视觉搜索或 Python 处理，实现了视觉证据的跨工具链式传递。
*   **同策略数据演化（ODE, On-policy Data Evolution）**：
    *   **核心思想**：将数据构建视为一个闭环的自适应优化过程。
    *   **算法流程**：
        1.  **前向策划（Forward Curation）**：通过种子提议、网页探索、图组织（构建多模态证据图）和任务策划四个阶段生成候选任务。
        2.  **后向优化（Backward Optimization）**：利用当前策略模型进行采样（Rollout），通过基于**准则（Rubric）**的轨迹分析（评估信息复杂度、视觉依赖性、捷径泄露等），诊断生成器的不足。
        3.  **配置更新**：根据诊断反馈更新生成器的配置（Evolvable Config），使下一轮生成的数据更贴合模型的“学习前沿”。该机制同时支持 SFT（生成高质量教师轨迹）和 RL（生成具有挑战性的可验证任务）。

### 3. 实验设计
*   **数据集/场景**：涵盖了 8 个极具挑战性的多模态基准测试，包括：
    *   深度搜索类：MMSearch, MMSearch+, VDR, MM-BrowseComp (MMBC)。
    *   复杂推理类：HLE-VL, BC-VL。
    *   事实性与知识类：SimpleVQA, FVQA。
*   **对比方法**：
    *   **闭源模型**：GPT-5, Claude-4/3.7 Sonnet, Gemini-2.5 Pro/Flash。
    *   **开源基座**：Qwen3-VL-8B/30B。
    *   **专用智能体**：MMSearch-R1, WebWatcher。
*   **实验设置**：分为“直接推理”、“标准智能体工作流”和“本文提出的 ODE 框架”三种模式进行对比。

### 4. 资源与算力
*   **硬件**：论文明确提到所有训练实验均在 **NVIDIA H20 GPU** 上进行。
*   **训练细节**：
    *   SFT 阶段：最大序列长度 64k，Batch Size 64，学习率 2e-5，训练 2 个 epoch。
    *   RL 阶段：采用 **GRPO** 算法，Batch Size 96，学习率 2e-6，每个 Prompt 采样 6 个响应。
*   **数据规模**：最终生成了 8,855 条 SFT 示例和针对不同规模模型的各 4,000 条 RL 任务。

### 5. 实验数量与充分性
*   **实验组数**：论文在 8 个基准测试上进行了主实验，并针对 8B 和 30B 两种规模的模型进行了验证。
*   **消融实验**：
    *   针对“视觉原生框架”的消融：验证了图像复用对性能的贡献。
    *   针对“数据演化”的消融：对比了 ODE 与静态合成（Static Synthesis）的效果。
    *   针对“演化机制”的分析：展示了 SFT 和 RL 模式下数据分布的动态变化。
*   **充分性与公平性**：实验覆盖了从感知到推理、从搜索到计算的多种维度。通过在相同硬件和相同基座模型（Qwen3-VL）上对比不同训练策略，确保了实验的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：ODE 显著提升了模型能力。Qwen3-VL-8B 的平均得分从 24.9% 提升至 39.0%，超越了 Gemini-2.5 Pro（37.9%）；30B 模型则达到了 41.5%。
*   **视觉复用的价值**：在需要迭代视觉精细化处理的任务（如 MMBC, HLE-VL）中，图像库引用协议带来了显著的增益。
*   **演化的必要性**：相比静态数据，ODE 生成的数据在视觉密度、工具链多样性和难度匹配度上表现更优，能有效解决 RL 训练中的“任务过易”或“捷径泄露”问题。

### 7. 优点
*   **架构创新**：图像库引用协议解决了多模态智能体长期存在的“视觉遗忘”问题，使视觉操作具有连贯性。
*   **闭环进化**：ODE 框架将数据生成与模型反馈挂钩，模拟了“因材施教”的过程，极大地提高了合成数据的利用效率。
*   **多维度评估**：引入了详尽的 Rubric（准则）系统，不仅关注结果正确性，更关注工具使用的逻辑性和多样性。

### 8. 不足与局限
*   **生成成本**：ODE 依赖强力模型（如 GPT-5.2）作为生成器和裁判，且需要多轮迭代，数据构建的算力成本较高。
*   **模型偏差风险**：由于训练数据和评估准则高度依赖上位模型的判断，可能会引入上位模型的偏见或特定的行为模式。
*   **实时性挑战**：复杂的工具链和多轮视觉处理会显著增加推理延迟，论文未深入讨论在实时搜索场景下的响应速度优化。

（完）
