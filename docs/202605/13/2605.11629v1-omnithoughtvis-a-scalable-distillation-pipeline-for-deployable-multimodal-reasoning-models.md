---
title: "OmniThoughtVis: A Scalable Distillation Pipeline for Deployable Multimodal Reasoning Models"
title_zh: OmniThoughtVis：一种面向可部署多模态推理模型的可扩展蒸馏流水线
authors: "Yuanhao Yue, Chengyu Wang, Yuanjie Lyu, Lei Shen, Jun Huang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11629v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言任务中多模态思维链推理的蒸馏
tldr: 针对多模态大模型在实际部署中面临的资源限制和推理能力不足问题，本文提出了 OmniThoughtVis 蒸馏框架。该框架通过从大规模教师模型中提取结构化思维链（CoT）轨迹，并结合难度、质量和语义标签进行自动化筛选，构建了包含 180 万样本的高质量数据集。实验证明，该方法显著提升了 2B 至 8B 规模小模型的推理性能，使其在多个基准测试中达到甚至超越了更大规模的基准模型。
selection_source: fresh_fetch
motivation: 现有小型多模态模型因缺乏大规模、高质量的思维链监督数据，导致其推理能力受限且难以在资源受限环境下高效部署。
method: 开发了一种可扩展的蒸馏流水线，通过生成结构化思维链轨迹并结合难度感知选择与多样性采样，构建了 180 万规模的精选语料库。
result: 蒸馏后的 Qwen3-VL 系列模型在九个基准测试中均有显著提升，其中 4B 模型在 MathVerse 上进步了 16.8 分，性能甚至超越了 8B 基准模型。
conclusion: 该研究证明了通过大规模推理能力蒸馏可以有效提升轻量级多模态模型的性能，为构建高性能且易于部署的模型提供了实用方案。
---

## 摘要
近期的多模态大语言模型（MLLMs）在视觉语言任务上展现出强大的思维链（CoT）推理能力，但它们在现实系统中的直接部署往往受限于延迟和资源约束。在实践中，在线服务更倾向于使用较小的 MLLM，但由于缺乏大规模、高质量的多模态 CoT 监督，它们的推理性能受到了瓶颈限制。在本文中，我们提出了 OmniThoughtVis，这是一种可扩展的数据策展与蒸馏流水线，用于将多模态推理能力从高容量教师模型迁移到更小的、面向部署的 MLLM。从多样化的开源种子池出发，我们的流水线生成结构化的 CoT 轨迹，并对推理难度、答案质量和语义任务标签进行联合标注。为了在大规模情况下保持数据质量，我们结合了基于规则的过滤、难度感知选择和基于标签的多样性采样，最终形成了一个包含 180 万个样本的精选语料库，支持为下游训练构建可控子集。我们使用 OmniThoughtVis 蒸馏了参数量从 2B 到 8B 的 Qwen3-VL 模型，并在九个多模态推理基准测试上对其进行了评估。结果显示，蒸馏后的模型在不同模型规模上均表现出一致的提升，其中 4B 模型在 MathVerse 上提升高达 +16.8 分，在 MMMU-Pro 上提升 +5.6 分。值得注意的是，蒸馏后的 4B 模型在多项任务上达到或超过了未蒸馏的 8B 基准模型，凸显了可扩展推理蒸馏对于面向部署的 MLLM 的实用价值。

## Abstract
Recent multimodal large language models (MLLMs) have shown strong chain-of-thought (CoT) reasoning ability on vision-language tasks, but their direct deployment in real-world systems is often limited by latency and resource constraints. In practice, smaller MLLMs are preferred for online serving, yet their reasoning performance is bottlenecked by the lack of large-scale, high-quality multimodal CoT supervision. In this paper, we present OmniThoughtVis, a scalable data curation and distillation pipeline for transferring multimodal reasoning capabilities from high-capacity teacher models to smaller, deployment-oriented MLLMs. Starting from a diverse open-source seed pool, our pipeline generates structured CoT traces and performs joint annotation of reasoning difficulty, answer quality, and semantic task tags. To maintain data quality at scale, we combine rule-based filtering, difficulty-aware selection, and tag-based diversity sampling, resulting in a curated corpus of 1.8M samples that supports controllable subset construction for downstream training. We use OmniThoughtVis to distill Qwen3-VL models from 2B to 8B parameters and evaluate them on nine multimodal reasoning benchmarks. The resulting distilled models show consistent gains across model scales, including improvements of up to +16.8 points on MathVerse and +5.6 points on MMMU-Pro for the 4B model. Notably, the distilled 4B model matches or surpasses the undistilled 8B baseline on several tasks, highlighting the practical value of scalable reasoning distillation for deployment-oriented MLLMs.

---

## 论文详细总结（自动生成）

以下是对论文《OmniThoughtVis: A Scalable Distillation Pipeline for Deployable Multimodal Reasoning Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然大型多模态模型（MLLMs）具备强大的思维链（CoT）推理能力，但其高延迟和高资源消耗限制了在实际生产环境中的部署。
*   **核心矛盾**：工业界更倾向于部署轻量化模型（如 2B-8B），但这些小模型由于缺乏大规模、高质量的多模态 CoT 监督数据，在处理复杂推理任务时表现不佳，往往只能进行浅层模式匹配。
*   **研究目标**：开发一套可扩展的流水线，将高能力教师模型的推理过程（CoT）有效地蒸馏到面向部署的小型模型中。

### 2. 方法论：OmniThoughtVis 流水线
该流水线包含四个关键阶段，旨在将原始图像-指令对转化为高质量的推理数据：
*   **种子数据采样**：从 FineVision（包含 3.5M 样本的开源聚合池）出发，通过分层采样和类别上限控制（每类 20K），确保任务多样性。
*   **带约束的蒸馏生成**：使用 **Qwen-VL-Max** 作为教师模型，强制要求输出 XML 风格的结构化格式（`<think>` 标签内为推理过程，`<answer>` 标签内为最终答案），便于后续自动化解析和过滤。
*   **联合标注与语义打标**：使用 **Qwen3-VL-Flash** 对生成的样本进行三维度标注：
    *   **难度 (1-5)**：评估任务的认知复杂度。
    *   **质量 (1-5)**：评估答案的准确性和完整性。
    *   **语义标签**：生成任务标签（如计数、空间推理、数学等）。
*   **质量控制与子集构建**：
    *   **规则过滤**：剔除格式错误、过短/过长（>4000 token）或存在严重重复的样本。
    *   **难度感知选择**：实验发现“难度 ≥ 4”是比“质量”更有效的筛选信号。
    *   **多样性采样**：利用标签嵌入进行 DBSCAN 聚类，并通过最远点采样（FPS）减少冗余，最终构建出 1.8M 的精选语料库。

### 3. 实验设计
*   **学生模型**：Qwen3-VL 系列（2B, 4B, 8B）。
*   **基准测试 (Benchmarks)**：涵盖 9 个主流多模态推理榜单：
    *   **通用理解**：MMBench, MMStar, AI2D。
    *   **学科/专家推理**：MMMU, MMMU-Pro (Standard & Vision)。
    *   **数学推理**：MathVista, MathVerse, MathVision。
*   **对比方法**：主要对比未经过 OmniThoughtVis 蒸馏的原始 Qwen3-VL-Instruct 基准模型。

### 4. 资源与算力
*   **训练配置**：Batch size 128，序列长度 3072，使用 AdamW 优化器，线性预热 3% 后接余弦退火。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量。但提到在 0.5M 样本子集上进行了 4 个 epoch 的训练，并进行了 20 组超参数网格搜索。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 种不同参数规模的模型上验证了蒸馏效果。
    *   进行了 20 组超参数搜索（学习率 × 训练轮数）。
    *   针对数据过滤策略（质量 vs 难度）进行了消融实验。
    *   分析了训练过程中不同任务（通用理解 vs 复杂推理）的性能演变曲线。
*   **充分性评价**：实验设计较为全面，覆盖了从感知到逻辑推理的多种任务。通过对比不同规模模型的性能，客观地展示了蒸馏对小模型的“越级”提升作用。

### 6. 主要结论与发现
*   **性能显著提升**：蒸馏后的模型在所有规模上均有进步。4B 模型在 MathVerse 上提升了 **16.8 分**，在 MMMU-Pro 上提升了 **5.6 分**。
*   **小模型逆袭**：蒸馏后的 **4B 模型**在 AI2D 和 MMMU 等多个榜单上达到甚至超过了未蒸馏的 **8B 基准模型**，证明了高质量 CoT 数据能显著优化质量-效率权衡。
*   **难度是关键信号**：在筛选数据时，选择高难度样本（Difficulty ≥ 4）比单纯追求高评分质量（Quality ≥ 5）更能提升模型的推理能力。
*   **饱和速度差异**：通用视觉理解任务在训练早期即达到饱和，而复杂的推理任务（如数学）则需要更长的训练时间才能持续提升。

### 7. 优点（亮点）
*   **实用性强**：直接针对工业部署痛点，提供了一套从数据生产到模型训练的完整工程化方案。
*   **结构化监督**：通过 XML 标签强制分离思考与回答，不仅提高了数据可解释性，也便于自动化评估。
*   **可控性高**：引入语义标签和难度标注，允许开发者根据需求（如特定领域增强或计算预算限制）灵活构建训练子集。

### 8. 不足与局限
*   **语言与模态局限**：目前主要集中在英文和视觉中心任务，未涵盖多语言或其他模态（如视频、音频）。
*   **模型偏见风险**：教师模型、标注模型和学生模型均属于 Qwen 家族，实验结果可能存在一定的“家族内部偏见”，在其他架构模型上的泛化性有待验证。
*   **自动化标注误差**：虽然使用了 LLM-as-a-judge，但自动评分和标签生成仍可能存在噪声，尤其是对于极高难度的抽象推理样本。

（完）
