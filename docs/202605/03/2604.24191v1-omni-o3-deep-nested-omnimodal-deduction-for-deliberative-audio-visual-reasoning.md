---
title: "Omni-o3: Deep Nested Omnimodal Deduction for Deliberative Audio-Visual Reasoning"
title_zh: Omni-o3：用于审慎式视听推理的深度嵌套全模态演绎
authors: "Zhicheng Zhang, Wentao Gu, Weicheng Wang, Yongjie Zhu, Wenyu Qin, Meng Wang, Pengfei Wan, Jufeng Yang"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24191v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于审慎视听推理的深度嵌套演绎策略
tldr: 针对全模态理解中跨模态交互搜索空间巨大且推理路径孤立的问题，本文提出Omni-o3框架。该框架引入深度嵌套演绎策略，将推理建模为动态递归搜索，通过共享推理前缀提高探索效率。通过两阶段训练（冷启动SFT和探索性强化学习），Omni-o3在11个音视频推理基准测试中表现优异，显著提升了复杂多模态任务的逻辑推理能力。
selection_source: fresh_fetch
motivation: 现有的推理范式因推理路径孤立，导致在复杂音视频任务中探索效率低下且容易产生累积错误。
method: 提出深度嵌套演绎策略，将推理视为包含扩展、选择、模拟和回溯的动态递归搜索，并采用两阶段训练和多步奖励模型进行优化。
result: 实验表明Omni-o3在11个涵盖音视频、视觉和音频中心的基准测试中均达到了具有竞争力的领先性能。
conclusion: Omni-o3通过递归搜索机制和强化学习，成功解锁了高效且深入的全模态逻辑推理能力，为复杂多模态理解提供了新方案。
---

## 摘要
全模态理解涉及跨模态交互的巨大且高度冗余的搜索空间，需要专注且审慎的推理。目前的推理范式依赖于顺序的逐步生成或并行的逐样本展开，导致推理轨迹相互孤立。这种无法共享有前景的中间路径的局限性严重限制了探索效率，并在复杂的视听任务中导致复合误差。为了突破这一瓶颈，我们引入了 Omni-o3，这是一个由深度嵌套演绎策略驱动的新型框架。通过将推理建模为动态递归搜索，Omni-o3 在本质上实现了跨分支的推理前缀共享，从而能够迭代执行四种原子认知动作：扩展、选择、模拟和反向传播。为了增强该框架，我们提出了一种稳健的两阶段训练范式：(1) 在从 350 万个多样化全模态样本中提取的 10.1 万条高质量长链轨迹上进行冷启动有监督微调，以实现必要的递归搜索模式；(2) 在 1.8 万个复杂多轮样本上进行基于嵌套组展开的探索性强化学习，并由一种新型多步奖励模型显式引导，以激发深度嵌套推理。广泛的实验表明，Omni-o3 在 11 个基准测试中均取得了具有竞争力的性能，解锁了在综合视听、以视觉为中心以及以音频为中心的推理任务中的先进能力。

## Abstract
Omnimodal understanding entails a massive, highly redundant search space of cross-modal interactions, demanding focused and deliberative reasoning. Current reasoning paradigms rely on either sequential step-by-step generation or parallel sample-by-sample rollouts, leading to isolated reasoning trajectories. This inability to share promising intermediate paths severely limits exploration efficiency and causes compounding errors in complex audio-visual tasks. To break this bottleneck, we introduce Omni-o3, a novel framework driven by a deep nested deduction policy. By formulating reasoning as a dynamic recursive search, Omni-o3 inherently shares reasoning prefixes across branches, enabling the iterative execution of four atomic cognitive actions: expansion, selection, simulation, and backpropagation. To empower this framework, we propose a robust two-stage training paradigm: (1) cold-start supervised fine-tuning on 101K high-quality, long-chain trajectories distilled from 3.5M diverse omnimodal samples, enabling necessary recursive search patterns; and (2) nested group rollout-driven exploratory reinforcement learning on 18K complex multi-turn samples, explicitly guided by a novel multi-step reward model to stimulate deep nested reasoning. Extensive experiments demonstrate that Omni-o3 achieves competitive performance across 11 benchmarks, unlocking advanced capabilities in comprehensive audio-visual, visual-centric, and audio-centric reasoning tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Omni-o3: Deep Nested Omnimodal Deduction for Deliberative Audio-Visual Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：全模态（音视频+文本）理解面临巨大的搜索空间和高度冗余的跨模态交互。现有的推理范式主要分为**顺序生成**（如标准 CoT）和**并行采样**（如多个 Rollout），但它们都存在推理轨迹孤立的问题，无法共享中间推理路径。
*   **核心挑战**：在处理长视频或复杂音频时，音视频 Token 与文本 Token 的比例往往超过 100:1。现有方法在每个推理分支中重复编码庞大的音视频上下文，导致计算效率极低且容易产生累积误差。
*   **整体含义**：本文旨在通过一种“深度嵌套演绎”策略，将推理过程从浅层的语言思考提升为审慎的全模态推理，实现计算资源的有效分配和推理路径的动态优化。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将推理过程建模为**动态递归树搜索（Deep Nested Deduction）**。通过共享推理前缀（即“根”节点），模型只需计算一次沉重的音视频编码，随后在多个“叶”分支中进行轻量化的逻辑探索。
*   **关键技术细节**：
    *   **原子认知动作**：模型在树搜索中执行四种动作：**选择**（寻找最有希望的分支）、**扩展**（提出新想法）、**模拟**（调用内在技能如音频转录、空间定位）、**反向传播**（更新节点价值）。
    *   **两阶段训练范式**：
        1.  **冷启动 SFT**：在 10.1万条高质量长链轨迹上微调，使模型掌握嵌套推理的结构化语法（如 `# Turn` 和 `## Action` 标签）并激活内在的全模态技能。
        2.  **探索性强化学习（RL）**：在 1.8万个复杂样本上进行嵌套组展开（Nested Group Rollout），利用**多步奖励模型（Multi-step RM）**引导。奖励函数结合了结果准确性（Outcome）、过程逻辑（Process）和格式规范（Format）。
*   **数据引擎**：利用蒙特卡洛树搜索（MCTS）驱动的自动化引擎，从 350万个原始样本中蒸馏出高质量的多轮推理轨迹，解决了审慎式音视频数据稀缺的问题。

### 3. 实验设计
*   **数据集与场景**：
    *   **音视频综合能力**：Video-MME, WorldSense, DailyOmni, Video-Holmes, IntentBench, AGAVQA 等。
    *   **视觉中心能力**：Video-MMMU, Vript, TVG, FineVD 等。
    *   **音频中心能力**：MMAU, MMSU, QualiSpeech, NISQA 等。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-5 (预测), Gemini 1.5/2.5 Pro。
    *   **开源视频/全模态模型**：Qwen2.5/3-VL, Qwen2.5/3-Omni, VideoChat-R1, Omni-R1, AVATAR 等。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 H100/A100）、显卡数量、训练总时长或能耗数据。这在当前的学术论文中较为遗憾，限制了对其训练成本的评估。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 11 个主流基准测试，涉及 20 多个子任务，实验覆盖面非常广泛。
*   **充分性与客观性**：
    *   实验不仅对比了最终的准确率，还通过定性分析展示了模型的“自我纠错”过程（Aha moment）。
    *   对比对象包含了当时最先进的闭源和开源模型，评估维度从感知（Perception）跨越到推理（Reasoning），实验设计较为客观、公平。
    *   **消融实验**：虽然在主文中对 SFT 和 RL 的阶段性作用做了说明，但缺乏对树搜索深度、分支因子等超参数的详细消融分析。

### 6. 主要结论与发现
*   **性能领先**：Omni-o3 在多个音视频基准测试中达到了 SOTA。例如在 Video-MME 上达到 72.6，在 IntentBench 上达到 68.4，显著超过了 Qwen3-Omni。
*   **超越闭源模型**：在需要深度推理的任务（如 Video-Holmes）中，Omni-o3 的表现（53.0）甚至超过了 GPT-4o（42.0）。
*   **推理深度**：实验证明，通过嵌套演绎，模型能够处理长达 20,000 Token 的长上下文轨迹，并能通过多轮交互纠正初始的错误判断。

### 7. 优点（亮点）
*   **架构创新**：引入树搜索机制解决了多模态推理中的计算冗余问题，前缀共享机制极具启发性。
*   **数据质量**：通过 MCTS 驱动的数据引擎生成了高质量、可追溯的推理数据，为社区提供了宝贵的资源。
*   **系统 2 推理**：成功将 LLM 的“慢思考”（System 2）引入全模态领域，使模型具备了动态评估、回溯和自我验证的能力。

### 8. 不足与局限
*   **幻觉风险**：尽管引入了验证机制，但在极高噪声的音视频环境下，模型仍可能产生事实不符的幻觉。
*   **实时性限制**：由于采用了复杂的树搜索和多轮演绎，该模型在实时交互场景（如实时对话机器人）中的延迟可能较高，文中未对此进行优化探讨。
*   **算力透明度**：缺乏训练资源和推理开销的具体数据，不便于其他研究者复现或评估部署成本。
*   **应用限制**：目前主要集中在离线视频/音频分析，对于具身智能（Embodied AI）中的实时反馈能力尚待验证。

（完）
