---
title: "What to Ignore, What to React: Visually Robust RL Fine-Tuning of VLA Models"
title_zh: 忽略什么，响应什么：VLA 模型的视觉鲁棒强化学习微调
authors: "Yuanfang Peng, Jingjing Fu, Chuheng Zhang, Li Zhao, Jiang Bian, Mingyu Liu, Ling Zhang, Jun Zhang, Rui Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13105v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言动作模型的强化学习微调以增强鲁棒性
tldr: 针对视觉语言动作（VLA）模型在强化学习微调中面临的视觉偏移挑战，本文提出 PAIR-VLA 框架。该方法通过在 PPO 优化中引入成对视觉变体的辅助目标，即对任务无关变化保持动作一致性，对任务相关变化保持动作敏感性。实验证明，该方法在 ManiSkill3 基准测试中显著提升了 OpenVLA 和 π0.5 模型的视觉鲁棒性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的强化学习微调方法难以区分视觉变化是否影响任务执行，导致模型在面对部署时的视觉偏移时表现不佳。
method: 提出 PAIR-VLA 框架，通过引入动作不变性（针对无关干扰）和动作敏感性（针对目标变化）两个辅助目标来指导策略学习。
result: "在 ManiSkill3 上的多种视觉偏移测试中，该方法使 π0.5 和 OpenVLA 的成功率分别平均提升了 16.62% 和 9.10%。"
conclusion: 通过在行为层面引入视觉变体的引导，可以显著增强 VLA 模型对分布外视觉偏移的鲁棒性和跨场景泛化能力。
---

## 摘要
强化学习（RL）微调在机器人操作的视觉-语言-动作（VLA）模型中展现出巨大潜力，但部署时的视觉偏移（visual shifts）带来了实际挑战。一个关键难点在于，标准的任务奖励仅监督任务是否成功，但在视觉变化是任务无关的还是改变了操作所需的行为方面，提供的指导有限。我们提出了 PAIR-VLA（视觉鲁棒 VLA 的成对动作不变性与敏感性），这是一个 RL 微调框架，通过在 PPO 优化过程中对成对的视觉变体添加两个辅助目标来解决这一难题：一个是不变性项，用于减少任务保持对（例如，不同的干扰物）之间动作分布的差异；另一个是敏感性目标，用于鼓励任务改变对（例如，目标物体处于不同姿态）之间产生可区分的动作分布。这些目标共同将视觉变体从单纯的观测多样性转变为 RL 微调期间策略响应的行为级指导。我们在 ManiSkill3 上针对两种代表性的 VLA 架构（OpenVLA 和 $π_{0.5}$）进行了评估，涵盖了多种分布外（OOD）视觉偏移，包括未见过的干扰物、纹理变化、目标物体姿态变化、视角偏移和光照变化。我们的方法相比标准 PPO 取得了一致的提升，在 $π_{0.5}$ 上平均提升了 16.62%，在 OpenVLA 上平均提升了 9.10%。值得注意的是，消融实验进一步展示了跨视觉偏移的泛化能力：从干扰物和纹理变体中学到的不变性指导可以迁移到目标姿态和光照偏移中，而对目标姿态变体添加敏感性指导则进一步提高了对干扰偏移（nuisance shifts）的鲁棒性，凸显了行为级 RL 指导更广泛的可迁移性。

## Abstract
Reinforcement learning (RL) fine-tuning has shown promise for Vision-Language-Action (VLA) models in robotic manipulation, but deployment-time visual shifts pose practical challenges. A key difficulty is that standard task rewards supervise task success, but offer limited guidance on whether a visual change is task-irrelevant or changes the behavior required for manipulation. We propose PAIR-VLA (Paired Action Invariance & Sensitivity for Visually Robust VLA), an RL fine-tuning framework to address this difficulty by adding two auxiliary objectives over paired visual variants during PPO optimization: an invariance term that reduces the discrepancy between action distributions for a task-preserving pair (e.g., different distractors), and a sensitivity objective that encourages separable action distributions for a task-altering pair (e.g., target object in a different pose). Together, these objectives turn visual variants from mere observation diversity into behavior-level guidance on policy responses during RL fine-tuning. We evaluate on ManiSkill3 across two representative VLA architectures, OpenVLA and $π_{0.5}$, under diverse out-of-distribution visual shifts including unseen distractors, texture changes, target object pose variation, viewpoint shifts, and lighting changes. Our method consistently improves over standard PPO, achieving average improvements of 16.62% on $π_{0.5}$ and 9.10% on OpenVLA. Notably, ablations further show generalization across visual shifts: invariance guidance learned from distractor and texture variants transfers to target-pose and lighting shifts, while adding sensitivity guidance on target-pose variants further improves robustness to nuisance shifts, highlighting the broader transferability of behavior-level RL guidance.

---

## 论文详细总结（自动生成）

### 论文总结：PAIR-VLA —— 视觉鲁棒的 VLA 模型强化学习微调

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在通用机器人操作中表现出色，但在部署时极易受到**视觉偏移（Visual Shifts）**的影响，如光照变化、背景纹理或干扰物的出现。
*   **研究动机**：现有的强化学习（RL）微调方法（如 PPO）主要依赖任务成功奖励，但这无法告诉模型哪些视觉变化是“无关痛痒”的（应忽略），哪些是“改变任务状态”的（应响应）。
*   **核心问题**：如何通过 RL 微调，使 VLA 模型在行为层面区分视觉变化的性质，从而提高其在分布外（OOD）场景下的鲁棒性。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PAIR-VLA** 框架，其核心思想是在 PPO 优化中引入**成对视觉变体**的辅助目标，将视觉多样性转化为行为级的指导。
*   **核心思想**：通过构建“任务保持（Task-preserving）”和“任务改变（Task-altering）”两种视觉视图，强制模型在前者下保持动作一致，在后者下产生动作差异。
*   **关键技术细节**：
    *   **任务保持视图（Invariance）**：通过前景-背景合成技术，移除干扰物并更换背景纹理，但保留目标物体和机器人状态。目标是最小化原始视图与该视图之间的动作分布 KL 散度（$L_{inv}$）。
    *   **任务改变视图（Sensitivity）**：通过扰动目标物体的姿态（平移或旋转）重新渲染。目标是最大化原始视图与该视图之间的动作分布 KL 散度（$L_{sens}$），并使用阈值 $c$ 防止梯度爆炸。
*   **总损失函数**：$L(\theta) = L_{PPO}(\theta) + \alpha L_{inv}(\theta) + \beta L_{sens}(\theta)$。
*   **部署优势**：辅助目标仅在训练时使用，推理时无需额外模块，不增加计算开销。

#### 3. 实验设计
*   **数据集/场景**：使用 **ManiSkill3** 模拟器，专注于带有干扰物的“抓取并放置（Pick-and-Place）”任务。
*   **Benchmark 与 OOD 设置**：设计了四种严苛的分布外场景：
    1.  **未见纹理**：使用训练集之外的桌面纹理。
    2.  **未见光照**：改变环境光、主灯光等参数。
    3.  **未见姿态**：目标物体初始位置和旋转超出训练分布。
    4.  **未见杂乱度**：增加干扰物数量（最多 8 个），且包含未见过的物体类别。
*   **对比方法**：以标准的 **PPO** 微调为基准，分别在自回归架构（**OpenVLA**）和流匹配架构（**$\pi_{0.5}$**）上进行验证。

#### 4. 资源与算力
*   **算力资源**：每个实验均在 **8 张 NVIDIA H100 GPU** 上运行。
*   **训练时长**：
    *   **OpenVLA**：训练过程约需 **1 天**。
    *   **$\pi_{0.5}$**：训练过程约需 **3 天**。
*   **微调技术**：OpenVLA 使用 LoRA 降低开销；$\pi_{0.5}$ 对 VLM 骨干使用 LoRA 并全量微调动作专家。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括主实验（4 种 OOD 场景）、训练效率曲线对比、消融实验（验证 $L_{inv}$ 和 $L_{sens}$ 的各自贡献）、超参数敏感性分析（$\alpha$ 的影响）以及相机视角泛化实验。
*   **充分性与公平性**：实验涵盖了两种主流的 VLA 架构，证明了方法的通用性。每组实验均运行了 3 个独立随机种子并报告了标准差，对比基准 PPO 使用了相同的超参数设置，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能大幅提升**：PAIR-VLA 在 $\pi_{0.5}$ 上平均提升了 **16.62%** 的成功率，在 OpenVLA 上提升了 **9.10%**。
*   **训练效率更高**：在达到相同成功率的情况下，PAIR-VLA 的收敛速度比标准 PPO 快约 **3 倍**。
*   **泛化能力强**：即使辅助目标中未包含光照变化，模型也能通过学习到的“行为级不变性”泛化到未见过的光照条件。
*   **消融结论**：不变性目标（$L_{inv}$）提供了主要的鲁棒性增益，而敏感性目标（$L_{sens}$）在联合使用时能进一步增强性能。

#### 7. 优点（亮点）
*   **行为级引导**：不同于传统的图像增强（仅增加观测多样性），该方法直接规范了模型对视觉变化的“反应方式”。
*   **架构无关性**：成功应用于自回归和流匹配两种截然不同的 VLA 架构。
*   **零推理成本**：通过训练时的辅助损失实现鲁棒性，不改变模型结构，适合实时机器人部署。
*   **显著的泛化性**：展示了从简单视觉扰动到复杂 OOD 场景（如视角、光照）的知识迁移。

#### 8. 不足与局限
*   **模拟器依赖**：任务视图的构建依赖于模拟器提供的物体掩码（Mask）和姿态 API。虽然论文提到可以用 SAM 等模型替代，但在真实世界中的掩码精度和渲染一致性仍是挑战。
*   **Sim-to-Real 验证缺失**：所有实验均在 ManiSkill3 模拟器中完成，尚未在真实物理机器人上验证其迁移效果。
*   **任务复杂度**：目前主要集中在单一的抓取放置任务，对于更复杂、多阶段的长程任务（Long-horizon tasks）的适用性有待进一步验证。

（完）
