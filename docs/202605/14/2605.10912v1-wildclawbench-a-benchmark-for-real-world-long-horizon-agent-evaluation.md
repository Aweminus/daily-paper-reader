---
title: "WildClawBench: A Benchmark for Real-World, Long-Horizon Agent Evaluation"
title_zh: WildClawBench：一个用于真实世界、长程智能体评估的基准测试
authors: "Shuangrui Ding, Xuanlang Dai, Long Xing, Shengyuan Ding, Ziyu Liu, Yang JingYi, Penghui Yang, Zhixiong Zhang, Xilin Wei, Xinyu Fang, Yubo Ma, Haodong Duan, Jing Shao, Jiaqi Wang, Dahua Lin, Kai Chen, Yuhang Zang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10912v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 针对视觉语言模型代理在真实任务中的评估基准
tldr: WildClawBench是一个针对真实世界、长程任务的智能体评估基准，包含60个双语多模态任务。与以往依赖合成沙盒或模拟API的基准不同，它在原生运行时环境（如Docker容器）中通过真实CLI工具链进行评估。研究发现，即使是目前最顶尖的模型在处理这些平均耗时8分钟、涉及20多次工具调用的复杂任务时，表现也远未达到理想水平，揭示了长程智能体评估的挑战性。
selection_source: fresh_fetch
motivation: 现有的智能体基准多依赖合成环境和短程任务，无法真实反映智能体在实际部署环境中的长程工作能力。
method: 提出了包含60个真实任务的WildClawBench，利用Docker容器和真实CLI工具链，结合规则、环境状态审计及LLM评委进行混合评分。
result: "在19个前沿模型中，表现最好的Claude Opus 4.7仅达到62.2%的成功率，且不同工具链对模型表现影响显著。"
conclusion: 长程、原生运行时的智能体评估对当前前沿模型仍是巨大挑战，该基准为未来智能体研究提供了更具挑战性的评估工具。
---

## 摘要
大语言模型和视觉语言模型正越来越多地驱动智能体，通过命令行界面（CLI）框架代表用户执行操作。然而，大多数智能体基准测试仍依赖于合成沙盒、短程任务、模拟服务 API 和最终答案检查，这使得智能体是否能在其部署的运行时环境中完成现实的长程工作仍是一个悬而未决的问题。本研究提出了 WildClawBench，这是一个原生运行时的基准测试，包含 60 个由人类编写、双语且多模态的任务，涵盖六个主题类别。每个任务平均耗时约 8 分钟（实际时间），涉及超过 20 次工具调用，并在托管实际 CLI 智能体框架（如 OpenClaw、Claude Code、Codex 或 Hermes Agent）的可复现 Docker 容器中运行，且能够访问真实工具而非模拟服务。评分采用混合模式，结合了确定性的基于规则的检查、环境状态副作用审计，以及用于语义验证的 LLM/VLM 裁判。在 19 个前沿模型的测试中，表现最好的 Claude Opus 4.7 在 OpenClaw 框架下的总分仅为 62.2%，而其他所有模型均低于 60%，且仅更换框架就能使单个模型的得分波动高达 18 分。这些结果表明，对于当前的前沿模型而言，长程、原生运行时的智能体评估仍然是一个远未解决的任务。我们发布了任务、代码和容器化工具，以支持可复现的评估。

## Abstract
Large language and vision-language models increasingly power agents that act on a user's behalf through command-line interface (CLI) harnesses. However, most agent benchmarks still rely on synthetic sandboxes, short-horizon tasks, mock-service APIs, and final-answer checks, leaving open whether agents can complete realistic long-horizon work in the runtimes where they are deployed. This work presents WildClawBench, a native-runtime benchmark of 60 human-authored, bilingual, multimodal tasks spanning six thematic categories. Each task averages roughly 8 minutes of wall-clock time and over 20 tool calls, and runs inside a reproducible Docker container hosting an actual CLI agent harness (OpenClaw, Claude Code, Codex, or Hermes Agent) with access to real tools rather than mock services. Grading is hybrid, combining deterministic rule-based checks, environment-state auditing of side effects, and an LLM/VLM judge for semantic verification. Across 19 frontier models, the best, Claude Opus 4.7, reaches only 62.2% overall under OpenClaw, while every other model stays below 60%, and switching harness alone shifts a single model by up to 18 points. These results show that long-horizon, native-runtime agent evaluation remains a far-from-resolved task for current frontier models. We release the tasks, code, and containerized tooling to support reproducible evaluation.

---

## 论文详细总结（自动生成）

### WildClawBench：真实世界长程智能体评估基准论文总结

#### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）和视觉语言模型（VLM）的发展，智能体（Agent）已开始在真实命令行界面（CLI）中执行复杂任务。然而，现有的智能体基准测试存在四大缺陷：
*   **环境脱节**：多依赖合成沙盒而非真实运行时环境。
*   **任务过短**：通常在 1 分钟内完成，缺乏长程规划。
*   **工具虚构**：使用模拟 API 而非真实的系统工具。
*   **评估单一**：仅检查最终答案，忽略了执行过程中的副作用和轨迹审计。

**研究动机**：填补真实部署环境与评估基准之间的鸿沟，验证 Agent 是否能在长达数分钟、涉及数十次真实工具调用的复杂场景中保持可靠性。

#### 2. 论文提出的方法论
论文提出了 **WildClawBench**，其核心思想是在**原生运行时（Native-Runtime）**中进行长程评估。
*   **核心架构**：基于 Docker 容器提供隔离且可复现的环境，直接托管真实的 CLI 智能体框架（如 OpenClaw, Claude Code 等）。
*   **任务设计**：包含 60 个由人类编写的任务，涵盖双语（中英）和多模态输入。
*   **混合评分机制（Hybrid Grading）**：
    1.  **确定性规则检查**：验证文件是否存在、格式是否正确、数值是否精确。
    2.  **环境状态审计**：检查执行过程中的副作用（如邮件是否发出、日历是否更新、系统命令是否安全）。
    3.  **LLM/VLM 裁判**：针对报告质量、图像美观度等主观语义进行评分。
*   **防泄露机制**：评分资源仅在 Agent 进程退出后才挂载到容器中，防止 Agent 在执行过程中通过搜索文件系统获取答案。

#### 3. 实验设计
*   **数据集/场景**：60 个任务分为六大类：生产力流（10）、代码智能（12）、社交互动（6）、搜索与检索（11）、创意合成（11）、安全对齐（10）。
*   **对比方法（模型）**：评估了 19 个前沿模型，包括 6 个闭源模型（如 Claude Opus 4.7, GPT 5.5）和 13 个开源模型（如 DeepSeek V4 Pro, Qwen 3.5）。
*   **对比框架（Harnesses）**：在 OpenClaw（默认）、Claude Code、Codex 和 Hermes Agent 四种真实框架下进行交叉测试。
*   **基准指标**：成功率（Score）、运行时间（Time）、API 成本（Cost）以及工具调用次数。

#### 4. 资源与算力
*   **算力消耗**：论文**未明确说明**训练这些模型所使用的具体算力（如 GPU 型号或数量），因为该研究侧重于对现有前沿模型的**评估**而非训练。
*   **推理成本与时长**：详细记录了评估过程中的资源消耗。平均每个任务耗时约 **8.5 分钟**，平均工具调用次数超过 **20 次**。API 成本从每个任务 $0.1 到 $1.67 不等。

#### 5. 实验数量与充分性
*   **实验规模**：对 19 个模型进行了详尽测试，并针对 4 个代表性模型进行了 3 次独立重复实验以验证方差。
*   **消融与分析实验**：
    *   **框架对比**：验证了同一模型在不同 CLI 框架下的表现差异。
    *   **思考模式对比**：测试了 GPT 5.4 在不同“思考强度”下的表现。
    *   **时间预算实验**：分析了任务成功率随执行时间限制的变化。
    *   **技能增强实验**：评估了添加外部工具（Skills）对性能的提升。
*   **公平性与客观性**：通过统一的 OpenRouter 接口访问模型，固定系统提示词和工具 Schema，并引入人类专家与 GPT 评分的一致性研究（偏差控制在 3% 以内），确保了评估的客观性。

#### 6. 论文的主要结论与发现
*   **性能远未饱和**：最强模型 Claude Opus 4.7 的总分仅为 62.2%，大多数模型低于 60%，说明长程任务对当前 AI 仍是巨大挑战。
*   **框架影响显著**：仅更换运行框架，同一模型的得分波动可达 18 分，证明框架的控制循环和工具管理策略至关重要。
*   **多模态瓶颈**：大多数模型在多模态任务上的表现显著弱于纯文本任务。
*   **推理能力不等于 Agent 能力**：增加模型的内部思考时间（Thinking Mode）有时反而会导致超时失败，因为 Agent 需要在思考与环境交互之间取得平衡。
*   **安全风险**：在安全对齐任务中，模型仍容易受到隐藏在文件中的提示词注入（Prompt Injection）攻击。

#### 7. 优点
*   **高度真实性**：使用真实 Docker 环境和 CLI 工具，而非简化的模拟器。
*   **长程视角**：任务平均耗时和工具调用次数远超现有基准，能有效暴露模型在复杂规划中的脆弱性。
*   **双语与多模态**：全面覆盖了中英双语及视听觉输入，符合现代 Agent 的应用趋势。
*   **可复现性强**：提供了完整的容器化工具链和详细的轨迹日志。

#### 8. 不足与局限
*   **单轮指令限制**：目前所有任务均为单轮初始指令，未涵盖用户在执行过程中提供反馈或修正的多轮交互场景。
*   **任务规模有限**：虽然 60 个任务质量很高，但相对于真实世界的复杂性，样本量仍显不足，且部分垂直领域（如生物、金融）覆盖较少。
*   **静态环境**：虽然是原生运行时，但环境状态在任务开始前是预设的，缺乏动态变化的外部干扰。

（完）
