---
title: "EARL: Towards a Unified Analysis-Guided Reinforcement Learning Framework for Egocentric Interaction Reasoning and Pixel Grounding"
title_zh: EARL：一个用于第一视角交互推理与像素级定位的统一分析引导强化学习框架
authors: "Yuejiao Su, Xinshen Zhang, Zhen Ye, Lei Yao, Lap-Pui Chau, Yi Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14742v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于第一视角交互推理和定位的强化学习框架
tldr: 针对第一视角交互推理和像素级定位难题，本文提出EARL框架。该框架采用两阶段解析：先进行粗粒度交互解释生成结构化描述，再结合分析引导特征合成器（AFS）进行细粒度查询响应。通过多面奖励函数和GRPO强化学习优化，EARL在Ego-IRGBench上取得了显著的性能提升，并展现出强大的跨场景迁移能力。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理第一视角交互时，难以实现准确的语义推理和精细的像素级定位。
method: 提出一种两阶段解析框架，利用结构化语义先验引导查询推理，并结合GRPO强化学习算法优化文本、边界框和掩码等异构输出。
result: "在Ego-IRGBench基准测试中，EARL实现了65.48%的cIoU，比之前的强化学习方法提高了8.37%，且在OOD场景下表现出良好的泛化性。"
conclusion: EARL通过将分析引导与强化学习相结合，有效提升了第一视角下复杂交互的理解与精细化定位能力。
---

## 摘要
从第一视角视觉理解人与环境的交互对于辅助机器人和具身智能体至关重要，然而现有的多模态大语言模型（MLLMs）在准确的交互推理和细粒度像素级定位方面仍面临困难。为此，本文提出了 EARL，这是一个第一视角分析引导的强化学习框架，它显式地将粗粒度的交互语义转化为面向查询的回答与定位。具体而言，EARL 采用了一个两阶段解析框架，包括粗粒度解释和细粒度响应。第一阶段从整体上解释第一视角交互并生成结构化的文本描述。第二阶段根据用户查询生成文本回答和像素级掩码。为了衔接这两个阶段，我们提取了一个全局交互描述符作为语义先验，并通过一种新型的分析引导特征合成器（AFS）将其整合，以进行面向查询的推理。为了优化包括文本回答、边界框和定位掩码在内的异构输出，我们设计了一个多维奖励函数，并使用 GRPO 训练响应阶段。在 Ego-IRGBench 上的实验表明，EARL 在像素级定位方面达到了 65.48% 的 cIoU，比之前的基于强化学习的方法提高了 8.37%，同时在 EgoHOS 上的分布外（OOD）定位结果表明其对未见过的第一视角定位场景具有很强的迁移能力。

## Abstract
Understanding human--environment interactions from egocentric vision is essential for assistive robotics and embodied intelligent agents, yet existing multimodal large language models (MLLMs) still struggle with accurate interaction reasoning and fine-grained pixel grounding. To this end, this paper introduces EARL, an Egocentric Analysis-guided Reinforcement Learning framework that explicitly transfers coarse interaction semantics to query-oriented answering and grounding. Specifically, EARL adopts a two-stage parsing framework including coarse-grained interpretation and fine-grained response. The first stage holistically interprets egocentric interactions and generates a structured textual description. The second stage produces the textual answer and pixel-level mask in response to the user query. To bridge the two stages, we extract a global interaction descriptor as a semantic prior, which is integrated via a novel Analysis-guided Feature Synthesizer (AFS) for query-oriented reasoning. To optimize heterogeneous outputs, including textual answers, bounding boxes, and grounding masks, we design a multi-faceted reward function and train the response stage with GRPO. Experiments on Ego-IRGBench show that EARL achieves 65.48% cIoU for pixel grounding, outperforming previous RL-based methods by 8.37%, while OOD grounding results on EgoHOS indicate strong transferability to unseen egocentric grounding scenarios.

---

## 论文详细总结（自动生成）

### EARL：一个用于第一视角交互推理与像素级定位的统一分析引导强化学习框架

#### 1. 核心问题与整体含义
*   **研究动机**：在第一视角（Egocentric/FPV）视觉中，理解人与环境、物体的交互是具身智能和辅助机器人的核心。然而，现有的多模态大语言模型（MLLMs）在处理此类任务时面临三大挑战：交互语义解析不准、细粒度像素定位（Grounding）不可靠、跨场景泛化能力弱。
*   **核心问题**：如何将宏观的交互语义分析（如“正在做什么”）与微观的查询响应（如“那个杯子在哪里”）有机结合，并利用强化学习提升模型的推理与定位精度？
*   **整体含义**：本文提出了 EARL 框架，通过“粗粒度分析引导细粒度响应”的两阶段策略，并结合强化学习（RL）优化，实现了交互描述、文本问答和像素级定位的统一。

#### 2. 方法论
*   **核心思想**：采用“由粗到细”的解析架构。第一阶段进行全局交互解释，提取语义先验；第二阶段在先验引导下进行面向查询的推理与定位，并使用 GRPO 算法进行对齐优化。
*   **关键技术细节**：
    *   **两阶段解析**：
        1.  **粗粒度解释**：生成结构化文本描述 $T_{ana}$，并提取解码器的隐藏层嵌入作为“全局交互描述符” $F_{ana}$。
        2.  **细粒度响应**：根据用户查询生成文本答案 $T_{ans}$ 和边界框 $B$，最后由 SAM2 生成掩码 $M$。
    *   **分析引导特征合成器 (AFS)**：采用“选择与融合”范式，通过自注意力机制将 $F_{ana}$ 中的语义先验注入到查询相关的多模态特征中，避免直接融合带来的噪声。
    *   **GRPO 强化学习优化**：使用组相对策略优化（Group Relative Policy Optimization），无需额外的 Critic 网络，通过组内样本的相对得分估计优势函数。
*   **奖励函数设计**：
    *   **格式奖励 ($R_f$)**：确保输出符合特定的 XML 标签结构。
    *   **回答相关性奖励 ($R_a$)**：结合精确匹配和 Levenshtein 距离衡量文本相似度。
    *   **定位准确性奖励 ($R_g$)**：计算预测掩码与真值的 IoU。

#### 3. 实验设计
*   **数据集与场景**：
    *   **Ego-IRGBench (域内)**：包含 20,504 张图像和超过 160 万个查询对，涵盖复杂的交互描述、问答和掩码标注。
    *   **EgoHOS (分布外/OOD)**：包含 11,743 张图像，用于测试模型在未见场景下的手部和物体分割泛化能力。
*   **对比方法**：
    *   通用 MLLM：Qwen2.5-VL, InternVL2.5, Qwen3-VL。
    *   第一视角专用模型：EgoThinker, ANNEXE。
    *   像素级定位模型：Sa2VA, UniPixel。
    *   基于 RL 的分割模型：Seg-R1, Seg-Zero。

#### 4. 资源与算力
*   **硬件环境**：使用了 **4 台 NVIDIA A800 80GB GPU**。
*   **训练时长**：
    *   第一阶段（SFT）：未详细说明具体时长，但属于标准微调。
    *   第二阶段（RL）：1000 步训练约耗时 10 小时。
    *   **总时长**：完整流水线约 **13 小时**。
*   **模型规模**：分析器使用 Qwen2.5-VL-3B，响应器使用 Qwen2.5-VL-7B，掩码生成使用 SAM2-Large。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Ego-IRGBench 的测试集和验证集上进行了全面的性能对比。
    *   在 EgoHOS 上进行了 5 种不同类别的 OOD 定位实验。
    *   **消融实验**：对比了有无 RL、不同特征融合机制（MLP, Concat, CAF, AFS）的效果。
    *   **超参数研究**：探讨了三种奖励权重 ($\lambda_f, \lambda_a, \lambda_g$) 对最终性能的影响。
*   **充分性评价**：实验设计客观且充分。通过引入 OOD 测试验证了泛化性，通过消融实验证明了 AFS 模块和 RL 策略的必要性，对比基准涵盖了 2024-2025 年最新的 SOTA 模型。

#### 6. 主要结论与发现
*   **性能突破**：EARL 在 Ego-IRGBench 上的定位精度（cIoU）达到 **65.48%**，比之前最好的 RL 方法提升了 **8.37%**。
*   **RL 的作用**：强化学习显著增强了 MLLM 的空间推理能力，使其生成的边界框和掩码更加精准。
*   **语义引导的价值**：AFS 模块能有效将全局交互背景转化为局部定位的线索，显著优于传统的特征拼接方法。
*   **泛化性**：即使在未见过的 EgoHOS 数据集上，EARL 依然保持了领先的定位性能，证明了其鲁棒性。

#### 7. 优点
*   **统一框架**：首次在第一视角下实现了分析、问答与像素级定位的深度耦合。
*   **高效优化**：采用 GRPO 算法，在保证性能的同时降低了 RL 训练的算力门槛。
*   **设计精巧**：AFS 模块解决了两阶段任务间的语义鸿沟问题，奖励函数设计全面。

#### 8. 不足与局限
*   **极端场景挑战**：对于极小物体或严重遮挡的交互物体，定位精度仍有提升空间。
*   **OOD 评估不全**：由于现有 OOD 数据集（如 EgoHOS）缺乏文本描述标注，泛化性测试目前仅局限于定位任务，未能全面评估推理能力的泛化。
*   **视角局限**：该模型专门针对第一视角设计，在第三视角（他者视角）下的表现尚未验证。

（完）
