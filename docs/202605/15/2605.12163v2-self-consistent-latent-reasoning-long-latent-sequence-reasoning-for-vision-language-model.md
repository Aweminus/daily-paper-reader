---
title: "Self-Consistent Latent Reasoning: Long Latent Sequence Reasoning for Vision-Language Model"
title_zh: 自一致潜空间推理：视觉语言模型的长潜序列推理
authors: "Chenfeng Wang, Wei He, Xuhan Zhu, Chunpeng Zhou, Qizhen Li, Song Yan, Yufei Zheng, Chengjun Yu, Fan Lu, Wei Zhai, Yang Cao, Pengfei Yu, Zheng-Jun Zha"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12163v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言模型的视觉潜空间推理
tldr: 本研究针对视觉语言模型中长链潜空间推理性能下降的问题，揭示了“信息增益坍缩”这一核心原因，即自回归生成导致后续token难以引入新信息。为此提出了SCOLAR框架，通过轻量级detransformer利用LLM全序列隐藏状态一次性生成辅助视觉token，并结合三阶段SFT和ALPO强化学习。该方法将有效潜空间推理长度提升了30倍以上，在多个基准测试中显著超越开源基座模型，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 发现现有视觉潜空间推理随序列增长性能反而下降，源于自回归生成导致的信息增益坍缩及高倍池化监督信号的失效。
method: 提出SCOLAR框架，利用detransformer单次生成独立锚定于原始视觉空间的辅助token，并配合三阶段微调与强化学习优化。
result: "在真实推理基准上比基座模型提升14.12%，并将可接受的潜空间思维链长度扩展了30倍以上，达到开源模型领先水平。"
conclusion: SCOLAR有效解决了长序列视觉推理中的信息退化问题，证明了长潜空间序列在增强多模态模型推理能力方面的巨大潜力。
---

## 摘要
在语言推理中，更长的思维链（Chain of Thought）始终能带来更好的性能，这自然地暗示了视觉潜空间推理可能同样受益于更长的潜序列。然而，我们发现了一个反直觉的现象：现有视觉潜空间推理方法的性能随着潜序列变长而系统性地下降。我们揭示了其根本原因：信息增益坍缩（Information Gain Collapse）——自回归生成使得每一步都高度依赖于先前的输出，导致后续的 token 几乎无法引入新信息。我们进一步发现，作为监督目标的重度池化（$\geq 128\times$）图像嵌入所提供的信号并不比无意义的占位符更多。基于这些见解，我们提出了 SCOLAR（自一致潜空间推理），它引入了一个轻量级的 detransformer，利用大语言模型（LLM）的全序列隐藏状态一次性生成辅助视觉 token，且每个 token 都独立地锚定在原始视觉空间中。结合三阶段监督微调（SFT）和 ALPO 强化学习，SCOLAR 将可接受的潜空间思维链长度扩展了 30 倍以上，在真实世界推理基准测试中达到了开源模型的最先进水平（比基准模型提升了 14.12%），并展示了强大的分布外泛化能力。

## Abstract
In language reasoning, longer chains of thought consistently yield better performance, which naturally suggests that visual latent reasoning may likewise benefit from longer latent sequences. However, we discover a counterintuitive phenomenon: the performance of existing latent visual reasoning methods systematically degrades as the latent sequence grows longer. We reveal the root cause: Information Gain Collapse -- autoregressive generation makes each step highly dependent on prior outputs, so subsequent tokens can barely introduce new information. We further identify that heavily pooled ($\geq 128\times$) image embeddings used as supervision targets provide no more signal than meaningless placeholders. Motivated by these insights, we propose SCOLAR (Self-COnsistent LAtent Reasoning), which introduces a lightweight detransformer that leverages the LLM's full-sequence hidden states to generate auxiliary visual tokens in a single shot, with each token independently anchored to the original visual space. Combined with three-stage SFT and ALPO reinforcement learning, SCOLAR extends acceptable latent CoT length by over $30\times$, achieves state-of-the-art among open-source models on real-world reasoning benchmarks (+14.12% over backbone), and demonstrates strong out-of-distribution generalization.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Self-Consistent Latent Reasoning: Long Latent Sequence Reasoning for Vision-Language Model》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：在纯文本大模型中，增加思维链（CoT）长度能显著提升推理能力。然而，在视觉语言模型（VLM）的潜空间推理（Latent Reasoning）中，研究者发现了一个**反直觉现象**：随着潜序列长度增加，模型性能反而系统性下降。
*   **核心问题**：论文诊断出该现象的根源在于**“信息增益坍缩（Information Gain Collapse）”**。
    *   **自回归依赖**：现有的潜空间推理多采用自回归生成，导致后续 token 高度依赖前序输出，原始视觉信息在传递中不断衰减。
    *   **监督信号失效**：常用的重度池化（$\ge 128\times$）图像嵌入作为监督目标时，其包含的语义信息极低，几乎等同于无意义的占位符。
*   **整体含义**：本研究旨在打破自回归限制，通过一种“自一致”的生成范式，使视觉潜空间推理能够真正受益于长序列扩展。

### 2. 方法论：SCOLAR 框架
SCOLAR（Self-COnsistent LA tent Reasoning）的核心思想是**“单次生成、独立锚定”**。

*   **核心架构**：
    *   **Detransformer**：在 LLM 内部引入一个轻量级的两层 Transformer 分支。它不采用自回归方式，而是利用 LLM 的全序列隐藏状态（Hidden States），通过一次前向传播（Single-shot）生成完整的辅助视觉 token 序列。
    *   **独立锚定**：生成的每个辅助 token 直接对应原始图像的高分辨率空间区域，而非依赖前一个潜 token，从而彻底消除信息衰减。
    *   **门控融合**：通过公式 $A = V + g(A_0)$ 将生成的辅助特征 $A_0$ 与原始视觉特征 $V$ 融合，其中 $g$ 为门控函数，确保视觉编辑的强度受控。
*   **训练流程（三阶段 SFT）**：
    1.  **Detransformer 预训练**：仅训练 Detransformer 进行特征重建，确保其能从 LLM 隐藏状态中解码出视觉语义。
    2.  **触发 Token 学习**：通过加权交叉熵损失，训练模型在需要时自主输出 `<auxiliary>` 触发标识。
    3.  **联合推理与教师强制退火**：在训练中逐渐从使用“真实辅助图像”过渡到使用“模型自生成潜变量”，解决训练与推理的分布不一致问题。
*   **ALPO 强化学习**：
    *   提出**辅助潜变量策略优化（ALPO）**。由于潜变量是连续的，无法直接应用 GRPO 等离散策略梯度算法。ALPO 通过在 Rollout 阶段缓存连续嵌入，并在 Actor 更新时将其作为固定上下文，仅对离散文本 token 计算梯度，从而实现对潜空间推理路径的间接优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **真实世界感知与推理**：V*Bench（视觉搜索）、HRBench4K/8K（高分辨率感知）、MME-RealWorld-Lite（综合推理）。
    *   **分布外（OOD）泛化**：VisualPuzzles（抽象视觉逻辑谜题，训练集未包含）。
*   **对比方法**：
    *   **开源基座**：Qwen2.5-VL-7B。
    *   **视觉推理模型**：DeepEyes（基于裁剪）、LVR、Monet（基于潜空间）。
    *   **闭源模型**：GPT-4o、GPT-5.2、Gemini-3.1Pro。
*   **关键实验**：
    *   **长度缩放实验**：测试潜序列从 3 到 1000+ 长度时的准确率曲线。
    *   **无意义填充实验**：将生成的潜 token 替换为随机噪声，验证其是否携带真实语义。
    *   **信息增益分析**：利用正交投影残差量化每个 token 贡献的新信息。

### 4. 资源与算力
论文明确列出了训练所需的算力资源（使用 **NVIDIA H200 GPU**）：
*   **SFT 阶段 1**：每轮（Epoch）约 12 GPU-hours。
*   **SFT 阶段 2 & 3**：每阶段每轮约 50 GPU-hours。
*   **RL (ALPO) 阶段**：每轮约 **800 GPU-hours**。
*   **硬件环境**：使用了 8 台 GPU 服务器进行分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括 4 个主流基准测试、1 组 OOD 泛化测试、以及针对核心假设的 3 组诊断性实验。
*   **消融实验**：共设计了 9 组消融对比，涵盖了辅助 token 的必要性、Detransformer 预训练的作用、SFT 各阶段的影响以及 ALPO 算法的有效性。
*   **客观性与公平性**：通过“无意义填充”和“信息增益量化”等硬核指标，从底层数学逻辑上证明了方法的有效性，而非仅仅依赖最终的 Benchmark 分数，实验设计非常严谨且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：SCOLAR-7B 在 MME-RealWorld-Lite 上比基座模型提升了 **14.12%**，在 V*Bench 上以 83.77% 的高分超越了 GPT-4o（67.50%）。
*   **长度可扩展性**：成功将有效的潜空间 CoT 长度扩展了 **30 倍以上**。在其他模型性能随长度增加而崩溃时，SCOLAR 的性能随长度增加而持续提升。
*   **语义真实性**：证明了 SCOLAR 生成的潜 token 确实携带了与空间位置对应的视觉细节，而非仅仅是增加了计算量的“占位符”。

### 7. 优点：亮点设计
*   **诊断深刻**：不仅提出了新方法，还深入分析了现有方法失败的底层原因（信息增益坍缩），这为该领域提供了重要的理论见解。
*   **架构优雅**：Detransformer 的 Single-shot 设计既解决了信息衰减问题，又比自回归生成更具推理效率。
*   **强化学习创新**：ALPO 算法巧妙地解决了连续潜空间与离散策略梯度之间的矛盾，为 VLM 的 RL 训练提供了新思路。

### 8. 不足与局限
*   **KV Cache 重计算**：目前在注入辅助视觉 token 后，需要丢弃 Phase-1 的 KV Cache 并重新计算全序列，这增加了推理延迟。
*   **监督信号依赖**：Detransformer 的训练仍依赖于成对的“原始图-辅助图”数据，获取高质量的几何对齐数据成本较高。
*   **隐式优化风险**：ALPO 对 Detransformer 的优化是隐式的（通过文本奖励间接引导），未来可能需要更直接的端到端梯度回传机制。

（完）
