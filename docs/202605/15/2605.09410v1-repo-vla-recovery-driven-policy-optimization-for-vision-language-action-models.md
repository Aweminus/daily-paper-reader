---
title: "RePO-VLA: Recovery-Driven Policy Optimization for Vision-Language-Action Models"
title_zh: RePO-VLA：面向视觉-语言-动作模型的恢复驱动策略优化
authors: "Weijia Liufu, Xiaoyu Guo, Ruiyi Chen, Jingzhi Liu, Kaidong Zhang, Xiwen Liang, Jianqi Lin, Dawei Sun, Yuze Wang, Rongtao Xu, Bingqian Lin, Bowen Yang, Tongtong Cao, Bowen Peng, Dongyu Zhang, Guangrun Wang, Min Wang, Liang Lin, Xiaodan Liang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09410v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的策略优化
tldr: 针对视觉-语言-动作（VLA）模型在长程复杂任务中鲁棒性不足的问题，本文提出RePO-VLA框架。该框架通过回收失败轨迹并将其转化为恢复性数据，利用恢复感知初始化（RAI）和进度感知语义价值函数（PAS-VF）来区分成功、恢复和失败动作。通过价值条件优化（VCR），模型学会从不利状态回归成功路径。实验表明，该方法在模拟和真实双臂机器人任务中显著提升了对抗环境下的成功率。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要依赖成功示例的模仿学习，缺乏对执行偏差和失败状态的有效监督，导致在复杂任务中极易崩溃。
method: 提出一种恢复驱动的策略优化框架，结合RAI初始化、PAS-VF价值函数评估以及VCR价值条件细化，将失败过程转化为学习恢复能力的教学数据。
result: "在模拟和真实双臂机器人任务中，RePO-VLA将对抗环境下的平均成功率从20%大幅提升至75%以上。"
conclusion: RePO-VLA通过充分利用失败和恢复轨迹，有效增强了VLA模型的鲁棒性和自我纠错能力，为长程机器人操控提供了更可靠的方案。
---

## 摘要
视觉-语言-动作（VLA）模型在长时程、接触密集的操纵任务中表现脆弱，原因在于仅基于成功的模仿学习对执行偏移（execution drift）提供的监督有限，而失败的尝试（rollouts）往往被丢弃。我们提出了 RePO-VLA，一种恢复驱动的策略优化框架，为成功、恢复和失败轨迹分配不同的角色。RePO-VLA 首先采用恢复感知初始化（RAI），通过切分恢复片段并重置历史，使纠正动作依赖于当前的不利状态而非先前的失败过程。接着，它学习进度感知语义价值函数（PAS-VF），将时空轨迹特征与指令及成功参考对齐。生成的标签通过可靠性衰减挽救有用的失败前缀，而低价值标签则标记偏移和终端崩溃，从而教授常规、失败与纠正动作之间的差异。其数据引擎将不利状态转化为规划器生成或人工采集的纠正轨迹，教授向成功流形（success manifold）的恢复。价值条件细化（VCR）训练策略偏好高进度动作。部署时，固定的高价值（v=1.0）引导动作偏向学习到的成功流形，无需在线故障检测器或启发式重试。我们引入了具有标准化错误注入和恢复导向评估的 FRBench。在模拟和真实的双臂任务中，RePO-VLA 显著提升了鲁棒性，将对抗成功率从平均 20% 提高到 75%，在规模化真实世界试验中最高达 80%。

## Abstract
Vision-Language-Action (VLA) models remain brittle in long-horizon, contact-rich manipulation because success-only imitation provides little supervision for execution drift, while failed rollouts are often discarded. We introduce RePO-VLA, a recovery-driven policy optimization framework that assigns distinct roles to success, recovery, and failure trajectories. RePO-VLA first applies Recovery-Aware Initialization (RAI), slicing recovery segments and resetting history so corrective actions depend on the current adverse state rather than the preceding failure. It then learns a Progress-Aware Semantic Value Function (PAS-VF), aligning spatiotemporal trajectory features with instructions and successful references. The resulting labels salvage useful failure prefixes via reliability decay, while low-value labels mark drift and terminal breakdowns, teaching differences among nominal, failed, and corrective actions. The data engine turns adverse states into planner-generated or human-collected corrective rollouts, teaching recovery to the success manifold. Value-Conditioned Refinement (VCR) trains the policy to prefer high-progress actions. At deployment, a fixed high value ($v=1.0$) biases actions toward the learned success manifold without online failure detectors or heuristic retries. We introduce FRBench, with standardized error injection and recovery-focused evaluation. Across simulated and real-world bimanual tasks, RePO-VLA improves robustness, raising adversarial success from 20% to 75% on average and up to 80% in scaled real-world trials.

---

## 论文详细总结（自动生成）

这篇论文提出了 **RePO-VLA**，一个旨在提升视觉-语言-动作（VLA）模型在复杂机器人操纵任务中鲁棒性的框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLA 模型在长程、接触密集的任务中表现脆弱。微小的执行偏差（如抓取偏移、物体滑动）往往会导致任务彻底失败。
*   **研究动机**：
    *   **监督信号不足**：目前的训练主要依赖“仅成功”的演示（Supervised Fine-Tuning, SFT），模型从未见过失败状态，因此不知道如何从偏差中恢复。
    *   **数据浪费**：大量的失败尝试（Failed Rollouts）通常被直接丢弃，但这些数据的前半段往往包含有用的运动先验。
    *   **因果困惑**：直接模仿恢复轨迹可能导致模型错误地认为“先发生错误”是“执行正确动作”的前提。

### 2. 方法论：RePO-VLA 框架
RePO-VLA 采用两阶段课程学习方案，核心思想是**结构化地利用成功、失败和恢复轨迹**。

*   **阶段 I：恢复感知初始化 (RAI)**
    *   **轨迹切分与历史重置 (TSHR)**：将恢复轨迹中的纠正片段切分出来，并重置其观察历史缓冲区。这确保了模型学习的是“基于当前不利状态进行纠正”，而不是依赖于导致错误的特定历史序列，从而消除因果困惑。
*   **进度感知语义价值函数 (PAS-VF)**
    *   **语义对齐**：利用冻结的时空编码器（V-JEPA）和轻量化适配器，将机器人动作视频与语言指令映射到统一流形空间。
    *   **密集标签生成**：通过计算当前轨迹与成功参考簇的余弦相似度来估计任务进度。
    *   **可靠性衰减 (Reliability Decay)**：对于纯失败轨迹，利用公式 $v_t = V(\tau_{fail}) \cdot (1 - t/T)^\alpha$ 对标签进行衰减。这保留了失败轨迹早期的有用动作，同时抑制了接近崩溃时的错误动作。
*   **阶段 II：价值条件细化 (VCR)**
    *   **价值条件训练**：在策略中引入一个价值 Token（Value Token）。训练时，成功和恢复动作标记为高价值（$v=1.0$），错误动作标记为低价值（$v=0.0$）。
    *   **推理引导**：部署时，无需在线故障检测，只需将价值 Token 固定为 $1.0$，引导模型始终向“成功流形”靠拢。

### 3. 实验设计
*   **数据集与场景**：
    *   **FRBench-Sim**：基于 RoboTwin 仿真环境，包含 46 个任务，共 23,453 个双臂操纵回合。
    *   **FRBench-Real**：在真实世界中使用双臂机器人执行倒水、烹饪、整理桌面和折叠毛巾等任务。
*   **错误注入分类 (E1-E4)**：
    *   E1：提前闭合夹爪；E2：抓取滑动；E3：位置偏移；E4：姿态不匹配。
*   **对比方法**：对比了 RDT-1B、GO-1、$\pi_0$ 和 $\pi_{0.5}$ 等最先进的 VLA 基准模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：实验基于 $\pi_{0.5}$（一种基于流匹配的 VLA 模型）进行微调，并对比了 1B 参数规模的 RDT 模型，暗示实验涉及中大规模的算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 46 个任务和超过 2.3 万个回合，数据量较大。
    *   真实世界实验针对 4 个典型任务进行了 10-30 轮的重复测试。
*   **充分性与公平性**：
    *   **消融实验**：详细测试了历史重置、价值引导和衰减率 $\alpha$ 的影响，证明了各组件的必要性。
    *   **数据缩放研究**：展示了随着恢复数据量从 1x 增加到 4x，成功率稳步提升，验证了方法的扩展性。
    *   **评估协议**：引入了阶段性评估协议，确保恢复成功率是基于验证过的“不利状态”计算的，避免了偶然成功的干扰。

### 6. 主要结论与发现
*   **性能飞跃**：在对抗性干扰下，RePO-VLA 将平均成功率从基准的 **20% 提升至 75%**。
*   **无需外部检测器**：通过价值条件化，模型能够自主识别并纠正执行偏差，无需手动编写重试规则。
*   **失败数据的价值**：证明了失败轨迹通过合理的语义对齐和衰减处理，可以转化为有效的训练信号。

### 7. 优点与亮点
*   **数据引擎创新**：提出了一种自动化的错误注入与恢复数据生成流程，解决了恢复数据获取难的问题。
*   **解决因果困惑**：通过历史重置（History Reset）巧妙解决了模仿学习中常见的“为了纠正而先犯错”的逻辑陷阱。
*   **统一框架**：将正常执行与故障恢复集成在同一个策略网络中，推理逻辑简洁高效。

### 8. 不足与局限
*   **分类限制**：目前的恢复能力主要针对预定义的错误分类（E1-E4），对于完全未见过的物理崩溃（Out-of-taxonomy）泛化能力可能受限。
*   **真实数据成本**：虽然仿真数据丰富，但高质量的真实世界恢复数据仍依赖人工遥操作采集，成本较高。
*   **物理介质覆盖**：目前的 FRBench 尚未覆盖流体或高度易碎/变形的物体，物理交互的复杂性仍有提升空间。

（完）
