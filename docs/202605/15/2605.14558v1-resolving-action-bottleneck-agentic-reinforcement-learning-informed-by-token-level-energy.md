---
title: "Resolving Action Bottleneck: Agentic Reinforcement Learning Informed by Token-Level Energy"
title_zh: 解决动作瓶颈：基于 Token 级能量启发的智能体强化学习
authors: "Langzhou He, Junyou Zhu, Yue Zhou, Zhengyao Gu, Junhua Liu, Wei-Chieh Huang, Henry Peng Zou, David Wipf, Philip S. Yu, Qitian Wu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14558v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 带有推理轨迹的智能体强化学习
tldr: 针对智能体强化学习中推理与动作序列权重分配不均的问题，本文揭示了“动作瓶颈”现象，即奖励信号主要集中在动作 token 而非推理 token。为此提出了 ActFocus 方法，通过降低推理 token 权重并根据不确定性重分配动作 token 权重，在不增加额外计算开销的情况下，显著提升了 PPO 和 GRPO 在多轮交互任务中的性能。
selection_source: fresh_fetch
motivation: 现有的强化学习方法对推理和动作 token 进行均匀信用分配，导致训练信号在关键动作上的严重错配。
method: 提出 ActFocus 方法，通过降低推理 token 的梯度权重，并利用能量模型对高不确定性的动作 token 进行权重重分配。
result: 在多种环境和模型规模下，ActFocus 相比 PPO 和 GRPO 分别实现了最高 65.2 和 63.7 个百分点的性能提升。
conclusion: 识别并解决动作瓶颈问题是提升智能体强化学习效率的关键，ActFocus 证明了非均匀信用分配的优越性。
---

## 摘要
智能体强化学习利用多轮轨迹训练大语言模型，这些轨迹交织了长推理链与短促的面向环境的动作。常见的策略梯度方法（如 PPO 和 GRPO）对轨迹中的每个 token 一视同仁，导致了均匀的信用分配。在本文中，我们批判性地证明了这种均匀的信用分配在很大程度上错配了 token 级的训练信号。从基于能量的模型视角出发，我们表明 token 级的训练信号（通过其与给定提示词下不同采样轨迹的奖励方差之间的相关性来量化）急剧集中在动作 token 而非推理 token 上，尽管动作 token 仅占轨迹的一小部分。我们将这一现象称为“动作瓶颈”。受此启发，我们提出了一种极其简单的 token 重加权方法 ActFocus，该方法降低了推理 token 的梯度权重，并结合了一种额外的基于能量的再分配机制，进一步增加了具有更高不确定性的动作 token 的权重。在四种环境和不同模型规模下，ActFocus 始终优于 PPO 和 GRPO，在不增加任何额外运行时间或内存成本的情况下，最终步的增益分别高达 65.2 和 63.7 个百分点。

## Abstract
Agentic reinforcement learning trains large language models using multi-turn trajectories that interleave long reasoning traces with short environment-facing actions. Common policy-gradient methods, such as PPO and GRPO, treat each token in a trajectory equally, leading to uniform credit assignment. In this paper, we critically demonstrate that such uniform credit assignment largely misallocates token-level training signals. From an energy-based modeling perspective, we show that token-level training signals, quantified by their correlations with reward variance of different rollouts sampled from a given prompt, concentrate sharply on action tokens rather than reasoning tokens, even though action tokens account for only a small fraction of the trajectory. We refer to this phenomenon as the Action Bottleneck. Motivated by this observation, we propose an embarrassingly simple token reweighting approach, ActFocus, that downweights gradients on reasoning tokens, along with an additional energy-based redistribution mechanism that further increases the weights on action tokens with higher uncertainty. Across four environments and different model sizes, ActFocus consistently outperforms PPO and GRPO, yielding final-step gains of up to 65.2 and 63.7 percentage points, respectively, without any additional runtime or memory cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resolving Action Bottleneck: Agentic Reinforcement Learning Informed by Token-Level Energy》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在智能体强化学习（Agentic RL）中，大语言模型（LLM）生成的轨迹通常由长篇幅的**推理（Reasoning）**和短促的**动作（Action）**组成。目前的策略梯度方法（如 PPO 和 GRPO）普遍采用**均匀信用分配（Uniform Credit Assignment）**，即对轨迹中的每个 token 赋予相同的梯度权重。

论文指出，这种做法存在严重的信号错配：
*   **动作瓶颈（Action Bottleneck）**：实验发现，虽然动作 token 仅占轨迹的极小比例（如 4%），但它们与奖励方差（训练信号的质量指标）高度相关，而占据绝大部分篇幅的推理 token 几乎不提供有效的训练信号。
*   **梯度稀释**：均匀分配导致梯度质量被大量冗余的推理 token 稀释，导致训练不稳定或性能低下。

### 2. 论文提出的方法论：ActFocus
论文提出了一种名为 **ActFocus** 的 token 级重加权机制，核心思想是“聚焦动作，抑制冗余”。

*   **核心思想**：通过显式的结构化标签（如 `<think>` 和 `<answer>`）区分推理与动作，降低推理 token 的权重，并根据动作 token 的不确定性进行内部权重再分配。
*   **关键技术细节**：
    1.  **推理降权**：为推理 token 分配一个较小的固定权重 $\alpha$（如 0.1）。
    2.  **动作增强与能量重分配**：利用冻结的参考模型（Reference Model）计算 token 级的**自由能量（Free Energy）** $E_t = -\log \sum_{v \in V} \exp(f_{ref, t, v})$。能量越高，代表模型对该位置的预测越不确定。
    3.  **权重公式**：
        $$w_t = \begin{cases} \alpha, & t \in \text{Reasoning} \\ 1 + \beta \cdot \text{sigmoid}\left(\frac{E_t - \mu_E}{\sigma_E}\right), & t \in \text{Action} \end{cases}$$
        其中 $\beta$ 控制能量调制的强度，$\mu_E$ 和 $\sigma_E$ 是批次内的均值和标准差。
    4.  **目标函数**：将 $w_t$ 引入 PPO 或 GRPO 的代理损失函数中，通过加权平均代替简单平均。

### 3. 实验设计
*   **实验场景**：涵盖了四种不同类型的多轮交互环境：
    *   **Sokoban（推箱子）**：空间规划任务。
    *   **FrozenLake（冰湖）**：随机环境下的导航任务。
    *   **Sudoku（数独）**：逻辑约束满足任务。
    *   **WebShop**：真实的电子商务搜索与购买任务。
*   **基准模型（Backbone）**：Qwen2.5-Instruct 系列（0.5B, 1.5B, 3B）。
*   **对比方法**：标准的 **PPO** 和 **GRPO**（均使用 Uniform 均匀信用分配）。

### 4. 资源与算力
*   **硬件设备**：使用了单节点 **4 × NVIDIA H100 GPU**。
*   **训练时长**：论文未明确给出总训练小时数，但提到在 Sokoban 等环境训练 200 个迭代步，WebShop 训练 100 个迭代步。
*   **计算开销**：由于 ActFocus 使用的是冻结参考模型的单次前向传播（这在标准 RL 训练中通常是现成的，用于计算 KL 散度），因此该方法**几乎没有增加额外的运行时间或内存成本**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个环境、2 种算法（PPO/GRPO）、多个模型规模上进行了交叉验证。
    *   进行了详细的**消融实验**，包括超参数 $\alpha$（推理权重）和 $\beta$（能量强度）的敏感度分析。
    *   对比了不同的信号源（能量 vs. 熵 vs. 策略负对数似然等）。
*   **充分性评价**：实验设计较为充分，涵盖了从简单符号逻辑到复杂网页交互的任务，证明了该现象的普遍性。通过对比不同模型规模，验证了该方法在不同能力水平的模型上均有效。

### 6. 主要结论与发现
*   **性能大幅提升**：ActFocus 在所有配置下均优于基准方法。在数独任务上，相比 PPO 提升了最高 65.2 个百分点；在 WebShop 任务上，显著提升了“严格成功率”。
*   **增强稳定性**：ActFocus 有效缓解了 GRPO 在训练后期的“峰值后退化”（Peak-to-final degradation）现象，使训练过程更加平滑。
*   **响应效率**：使用 ActFocus 训练的模型倾向于生成更短、更高效的推理轨迹，而不是通过增加 token 数量来换取性能。

### 7. 优点
*   **简单且即插即用**：不需要复杂的奖励建模或额外的辅助网络，仅需修改损失函数的权重分配。
*   **理论动机清晰**：通过能量模型量化了“动作瓶颈”现象，为非均匀信用分配提供了实证依据。
*   **零额外开销**：利用现有计算资源实现了显著的性能增益。

### 8. 不足与局限
*   **依赖结构化输出**：该方法依赖于模型能够生成带有明确标签（如 `<think>`）的响应，对于没有显式推理过程的模型可能不适用。
*   **超参数敏感性**：$\alpha$ 和 $\beta$ 的最优值在不同任务间存在差异（如 WebShop 需要保留更多推理权重），可能需要针对新场景进行少量调优。
*   **环境覆盖**：虽然涵盖了四种环境，但尚未在更大规模的参数（如 70B+ 模型）或更复杂的长程规划任务（如软件工程智能体）上进行验证。

（完）
