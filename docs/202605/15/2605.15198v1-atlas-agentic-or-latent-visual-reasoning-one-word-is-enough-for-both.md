---
title: "ATLAS: Agentic or Latent Visual Reasoning? One Word is Enough for Both"
title_zh: ATLAS：代理式还是潜空间视觉推理？一个词足以兼顾两者
authors: "Ziyu Guo, Rain Liu, Xinyan Chen, Pheng-Ann Heng"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15198v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用功能令牌的智能体视觉推理框架
tldr: ATLAS 提出了一种结合代理推理与潜空间推理的新框架。针对现有方法在计算开销、上下文切换延迟及训练难度上的局限，该研究引入了“功能性标记”（functional token），将复杂的视觉操作简化为单个离散词。通过 LA-GRPO 算法优化强化学习过程，ATLAS 在无需架构修改的情况下实现了高效、可解释且泛化性强的视觉推理，为该领域提供了新范式。
selection_source: fresh_fetch
motivation: 现有的视觉推理方法在代理执行效率与潜空间表示的泛化性之间难以兼得，且计算成本高昂。
method: 提出 ATLAS 框架，利用单个离散的功能性标记作为视觉推理单元，并结合 LA-GRPO 算法稳定强化学习训练。
result: 实验表明 ATLAS 在多个挑战性基准测试中取得了优异性能，同时保持了良好的可解释性和训练效率。
conclusion: ATLAS 通过将视觉操作内化为标准标记，成功融合了代理与潜空间推理的优势，为未来视觉推理研究提供了高效的新路径。
---

## 摘要
视觉推理通常与中间视觉状态交织在一起，已成为该领域一个极具前景的方向。一种直接的方法是在推理过程中通过统一模型直接生成图像，但这在计算上开销巨大，且在架构上并非易事。最近的替代方案包括通过代码或工具调用的代理式推理，以及使用可学习隐藏嵌入的潜空间推理。然而，代理式方法会因外部执行产生上下文切换延迟，而潜空间方法缺乏任务泛化能力，且难以进行自回归并行化训练。为了结合两者的优势并减轻其局限性，我们提出了 ATLAS 框架。在该框架中，一个被称为“功能标记”（functional token）的离散“单词”既充当代理操作，又充当潜空间视觉推理单元。每个功能标记都与一个内化的视觉操作相关联，但不需要视觉监督，并且仍然是分词器词表中的标准标记，可以通过下一标记预测（next-token prediction）生成。这种设计避免了冗长的中间视觉内容生成，同时保持了与原生可扩展 SFT 和强化学习（RL）训练的兼容性，无需进行架构或方法论上的修改。为了进一步解决 RL 过程中功能标记稀疏的问题，我们引入了潜空间锚定 GRPO（LA-GRPO），通过使用静态加权的辅助目标锚定功能标记来稳定训练，从而提供更强的梯度更新。大量的实验和分析表明，ATLAS 在具有挑战性的基准测试中实现了卓越的性能，同时保持了清晰的可解释性。我们希望 ATLAS 能提供一种新的范式，启发未来的视觉推理研究。

## Abstract
Visual reasoning, often interleaved with intermediate visual states, has emerged as a promising direction in the field. A straightforward approach is to directly generate images via unified models during reasoning, but this is computationally expensive and architecturally non-trivial. Recent alternatives include agentic reasoning through code or tool calls, and latent reasoning with learnable hidden embeddings. However, agentic methods incur context-switching latency from external execution, while latent methods lack task generalization and are difficult to train with autoregressive parallelization. To combine their strengths while mitigating their limitations, we propose ATLAS, a framework in which a single discrete 'word', termed as a functional token, serves both as an agentic operation and a latent visual reasoning unit. Each functional token is associated with an internalized visual operation, yet requires no visual supervision and remains a standard token in the tokenizer vocabulary, which can be generated via next-token prediction. This design avoids verbose intermediate visual content generation, while preserving compatibility with the vanilla scalable SFT and RL training, without architectural or methodological modifications. To further address the sparsity of functional tokens during RL, we introduce Latent-Anchored GRPO (LA-GRPO), which stabilizes the training by anchoring functional tokens with a statically weighted auxiliary objective, providing stronger gradient updates. Extensive experiments and analyses demonstrate that ATLAS achieves superior performance on challenging benchmarks while maintaining clear interpretability. We hope ATLAS offers a new paradigm inspiring future visual reasoning research.

---

## 论文详细总结（自动生成）

这是一份关于论文《ATLAS: Agentic or Latent Visual Reasoning? One Word is Enough for Both》的结构化技术总结：

### 1. 核心问题与研究动机
*   **研究背景**：在复杂的视觉推理任务（如几何证明、游戏求解）中，模型往往需要中间视觉状态（如画辅助线、局部放大）来辅助决策。
*   **核心问题**：现有的视觉推理范式存在明显缺陷：
    *   **统一模型（Unified Models）**：直接生成像素级图像，计算开销巨大且架构复杂。
    *   **代理式推理（Agentic Reasoning）**：通过代码或工具调用，存在严重的外部执行延迟和冗长的上下文。
    *   **潜空间推理（Latent Reasoning）**：在隐藏层进行推理，缺乏泛化性且破坏了标准自回归模型的并行训练效率。
*   **研究动机**：探索一种既能像代理一样操作、又能像潜空间一样高效，且完全兼容现有自回归架构的视觉推理新范式。

### 2. 方法论
*   **核心思想**：引入“功能标记”（Functional Tokens），将复杂的视觉操作简化为词表中的单个离散单词。模型通过预测这些标记来触发内化的视觉操作，而无需生成实际图像或调用外部工具。
*   **关键技术细节**：
    *   **词表扩展**：在标准词表中加入 5 个功能标记：`<|Manip|>`（图像增强/缩放）、`<|Shape|>`（区域标注）、`<|Line|>`（几何线段）、`<|Arrow|>`（方向指引）、`<|Text|>`（符号标注）。
    *   **两阶段训练流程**：
        1.  **SFT 阶段**：构建 **ATLAS-178K** 数据集，将图像处理代码映射为功能标记序列，让模型学会何时及如何调用这些标记。
        2.  **RL 阶段**：使用 **GRPO**（群体相对策略优化）进行结果导向的强化学习。
    *   **LA-GRPO 算法**：针对功能标记在长序列中极其稀疏（约占 2.3%）导致的“梯度稀释”问题，提出了**潜空间锚定 GRPO（Latent-Anchored GRPO）**。该算法在标准 GRPO 损失基础上，增加了一个静态加权的标记级辅助目标，专门强化对功能标记位置的梯度更新。

### 3. 实验设计
*   **数据集与 Benchmark**：
    *   **V***：评估视觉搜索能力。
    *   **BLINK**：评估多模态感知能力（包含 9 个子任务，如空间关系、计数、多视图等）。
    *   **WeMath**：评估数学与几何推理能力。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude-4-Sonnet, Gemini-2.5-Pro。
    *   **开源 VLM**：Qwen2.5-VL（基座）、LLaVA-OneVision。
    *   **代理式/潜空间模型**：Visual CoT, V-Thinker, LVR, Monet, CoVT 等。

### 4. 资源与算力
*   **模型基础**：基于 **Qwen2.5-VL-7B**。
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练配置**：SFT 阶段冻结视觉编码器，仅更新投影层和语言模型；RL 阶段同样冻结编码器，训练 1 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了全面评估，涵盖了从基础感知到高阶数学推理的多个维度。
*   **消融实验**：针对奖励函数的设计（格式奖励、长度惩罚、标记滥用惩罚）以及 LA-GRPO 的有效性进行了详细的消融研究。
*   **客观性**：对比了多种不同范式的模型（统一、代理、潜空间），并使用了 LLM-as-a-judge（Qwen3-VL-235B）进行自动化评估，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能卓越**：ATLAS 在 BLINK 和 WeMath 上的表现显著优于基座模型，且在多个子项上达到了与闭源模型竞争的水平。
*   **效率极高**：相比于代理式方法（V-Thinker），ATLAS 的推理延迟降低了约 **5 倍**（从 18.83s 降至 3.80s），峰值显存占用降低了约 **44%**。
*   **LA-GRPO 的必要性**：实验证明 LA-GRPO 能有效稳定功能标记的调用，防止模型在强化学习中出现“标记刷分（Reward Hacking）”行为。

### 7. 优点
*   **极简主义**：用“一个词”解决了复杂的视觉操作表达，极大地压缩了推理序列长度。
*   **原生兼容**：无需修改任何模型架构，完全适配标准的自回归并行训练，易于在大规模数据上扩展。
*   **强可解释性**：通过注意力图可视化，证明了功能标记确实能引导模型关注任务相关的视觉区域（如几何图形的特定边）。

### 8. 不足与局限
*   **功能覆盖有限**：目前仅定义了 5 种功能标记，对于更极端或更精细的视觉操作（如复杂的图像编辑）可能覆盖不足。
*   **冷启动依赖**：模型高度依赖第一阶段 SFT 数据的质量，若初始轨迹标注有误，会影响后续 RL 的效果。
*   **黑盒性质**：虽然有注意力图支撑，但模型内部如何将离散标记转化为“视觉理解力”的深层机制仍有待进一步探索。

（完）
