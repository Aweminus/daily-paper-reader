---
title: Reinforcing Multimodal Reasoning Against Visual Degradation
title_zh: 增强多模态推理对抗视觉退化
authors: "Rui Liu, Dian Yu, Haolin Liu, Yucheng Shi, Tong Zheng, Runpeng Dai, Haitao Mi, Pratap Tokekar, Leoweiliang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09262v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 针对多模态大模型推理能力的强化学习微调
tldr: 针对多模态大语言模型（MLLM）在视觉退化（如模糊、压缩）下推理能力脆弱的问题，本文提出ROMA框架。该框架通过强化学习微调，在不增加额外采样开销的情况下，利用双向前向策略、Token级KL惩罚和辅助策略梯度损失，增强了模型对图像干扰的鲁棒性，在保持清晰图像性能的同时显著提升了退化场景下的推理准确率。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习模型在面对真实世界的视觉退化时表现脆弱，且直接在退化图像上进行采样会导致奖励中毒和优化不稳定。
method: 提出ROMA框架，采用双向前向策略评估退化视图，并结合Token级KL惩罚、辅助策略梯度损失及正确性条件正则化来稳定优化。
result: "在Qwen3-VL模型上的实验显示，该方法在保持清晰图像准确率的同时，将已知和未知退化场景下的鲁棒性分别提升了2.4%和2.3%。"
conclusion: ROMA有效地解决了MLLM在视觉干扰下的推理鲁棒性问题，为构建更稳健的多模态推理系统提供了新的强化学习范式。
---

## 摘要
强化学习显著提升了多模态大语言模型（MLLM）的推理能力，但所得策略在面对模糊、压缩伪影和低分辨率扫描等现实视觉退化时表现脆弱。先前视觉和深度强化学习领域的鲁棒性技术依赖于静态数据增强或基于价值的正则化，均难以直接迁移至自回归 MLLM 的无评论器（critic-free）强化学习微调。针对此类损坏增强推理并非易事：在采样（rollout）阶段直接注入退化视图会引发奖励中毒，即感知遮挡触发幻觉轨迹并导致优化失稳。我们提出了 ROMA，一种强化学习微调框架，通过修改优化动态来增强对抗视觉退化的推理能力，同时保持清晰输入下的性能。该框架采用双前向传递策略，利用教师强制（teacher forcing）根据清晰图像轨迹评估损坏视图，避免在退化输入上进行新采样。为实现分布一致性，我们针对最坏情况的增强应用了标记级代理 KL 惩罚；为防止正则化下的策略崩溃，引入了锚定于清晰图像优势（advantages）的辅助策略梯度损失以保留可靠奖励信号；为避免系统性错误不变性，通过正确性条件正则化将约束限制在成功轨迹上。在 Qwen3-VL 4B/8B 及七个多模态推理基准上的实验表明，该方法在已知和未知损坏上分别比 GRPO 提升了 2.4% 和 2.3%，且保持了清晰图像的准确率。

## Abstract
Reinforcement Learning has significantly advanced the reasoning capabilities of Multimodal Large Language Models (MLLMs), yet the resulting policies remain brittle against real-world visual degradations such as blur, compression artifacts, and low-resolution scans. Prior robustness techniques from vision and deep RL rely on static data augmentation or value-based regularization, neither of which transfers cleanly to critic-free RL fine-tuning of autoregressive MLLMs. Reinforcing reasoning against such corruptions is non-trivial: naively injecting degraded views during rollout induces reward poisoning, where perceptual occlusions trigger hallucinated trajectories and destabilize optimization. We propose ROMA, an RL fine-tuning framework that modifies the optimization dynamics to reinforce reasoning against visual degradation while preserving clean-input performance. A dual-forward-pass strategy uses teacher forcing to evaluate corrupted views against clean-image trajectories, avoiding new rollouts on degraded inputs. For distributional consistency, we apply a token-level surrogate KL penalty against the worst-case augmentation; to prevent policy collapse under regularization, an auxiliary policy gradient loss anchored to clean-image advantages preserves a reliable reward signal; and to avoid systematically incorrect invariance, correctness-conditioned regularization restricts enforcement to successful trajectories. On Qwen3-VL 4B/8B across seven multimodal reasoning benchmarks, our method improves robustness by +2.4% on seen and +2.3% on unseen corruptions over GRPO while matching clean accuracy.

---

## 论文详细总结（自动生成）

这篇论文提出了 **ROMA** 框架，旨在增强多模态大语言模型（MLLM）在面对现实世界视觉退化（如模糊、噪声、压缩等）时的推理鲁棒性。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：虽然强化学习（RL）显著提升了 MLLM 的推理能力，但这些模型在非理想视觉输入下表现极其脆弱。
*   **研究动机**：
    *   **架构不匹配**：现代 MLLM 常使用无评论器（critic-free）的算法（如 GRPO），传统的基于价值函数的鲁棒性正则化方法无法直接应用。
    *   **奖励中毒（Reward Poisoning）**：如果在 RL 采样阶段直接使用退化图像，模型会因为看不清而产生幻觉，导致奖励信号错误，进而使优化过程崩溃。
*   **整体含义**：论文旨在探索如何在不牺牲清晰图像性能的前提下，通过 RL 微调使模型具备对抗视觉干扰的稳健推理能力。

### 2. 方法论：ROMA 框架
ROMA 采用**双向前向传递（Dual-forward-pass）**策略，核心思想是“在清晰图像上采样，在退化图像上对齐”。
*   **双向前向策略**：
    1.  **第一遍（采样）**：在清晰图像上进行标准的 RL 采样（Rollout），获取推理轨迹、答案及优势值（Advantages）。
    2.  **第二遍（评估）**：利用“教师强制”（Teacher Forcing），将第一遍生成的轨迹输入到多个退化视图中，计算 Token 级的概率分布，而不进行新的采样。
*   **关键技术细节**：
    *   **正确性条件 Token 级不变性**：计算退化视图与清晰视图之间的 Token 级 KL 散度。关键在于引入了**正确性掩码**，仅对推理正确的轨迹应用此惩罚，防止模型学习错误的推理路径。
    *   **最坏情况多视图优化（Worst-Case Multi-View）**：同时生成多个退化视图，仅针对与清晰视图偏差最大的那个视图进行惩罚，实现对抗式鲁棒性。
    *   **辅助策略梯度（Auxiliary PG）**：在退化视图上计算一个额外的策略梯度损失，但其“优势值”锚定在清晰图像的反馈上，从而提供稳定的学习信号。

### 3. 实验设计
*   **模型基础**：Qwen3-VL-4B 和 8B Instruct 版本。
*   **数据集**：训练使用 MMRL30k；评估涵盖 7 个基准测试：MathVista, WeMath, ChartQA, LogicVista, MMStar, VisualPuzzles, RealWorldQA。
*   **退化协议**：
    *   **已知退化（Seen）**：高斯噪声、模糊、JPEG 压缩、分辨率缩放。
    *   **未知退化（Unseen/OOD）**：运动模糊、椒盐噪声、斑点噪声、海报化、像素化。
*   **对比方法**：Base 模型、标准 GRPO，以及外部模型如 Vision-R1、NoisyRollout、PAPO 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：提到了使用 EasyR1 框架，训练 120 个 step，学习率 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 256，每个输入生成 8 个采样。

### 5. 实验数量与充分性
*   **实验规模**：在 2 种模型规模（4B/8B）和 7 个主流基准上进行了测试，涵盖了数学、逻辑、图表等多种推理任务。
*   **消融实验**：非常充分。分别验证了多视图策略（均值 vs 最坏情况）、辅助 PG 的必要性、以及正确性条件的约束作用。
*   **敏感性分析**：对超参数 $\alpha$（辅助系数）、$K$（视图数量）、$\beta$（惩罚权重）进行了详细的参数搜索。
*   **客观性**：通过区分“已知”和“未知”退化类型，客观地评估了模型的泛化能力（OOD 鲁棒性）。

### 6. 主要结论与发现
*   **鲁棒性显著提升**：在 8B 模型上，ROMA 在已知退化下比 GRPO 提升了 **2.4%**，在未知退化下提升了 **2.3%**。
*   **保持清晰性能**：ROMA 在清晰图像上的表现（68.7%）与标准 GRPO（68.9%）几乎持平，证明了该方法不会导致性能退化。
*   **缓解策略崩溃**：通过锚定清晰图像的优势值，ROMA 成功避免了在严重视觉干扰下的策略坍塌。

### 7. 优点（亮点）
*   **巧妙避开奖励中毒**：通过 Teacher Forcing 机制，既让模型看到了退化图像，又保证了训练轨迹的逻辑严密性。
*   **计算效率高**：不需要在退化图像上进行昂贵的自回归采样，仅增加了一次前向计算。
*   **泛化能力强**：实验证明模型不仅对训练过的干扰有抵抗力，对从未见过的噪声类型也表现出更强的韧性。

### 8. 不足与局限
*   **模态局限**：目前仅针对静态图像，未讨论视频推理中的时序退化问题。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，难以评估在大规模部署时的成本。
*   **任务依赖**：该方法高度依赖于能够自动判定正确性的任务（如数学、选择题），对于开放式、无标准答案的视觉对话任务，其“正确性条件正则化”可能难以实施。

（完）
