---
title: How Mobile World Model Guides GUI Agents?
title_zh: 移动世界模型如何引导 GUI 智能体？
authors: "Weikai Xu, Kun Huang, Yunren Feng, Jiaxing Li, Yuhan Chen, Yuxuan Liu, Zhizheng Jiang, Heng Qu, Pengzhi Gao, Wei Liu, Jian Luan, Xiaolin Hu, Bo An"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10347v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于移动端GUI智能体的视觉语言模型
tldr: 本研究深入探讨了移动端世界模型如何引导GUI智能体，通过训练涵盖增量文本、全文本、扩散图像和可渲染代码四种模态的模型，在多个基准测试中取得SOTA性能。研究揭示了不同模态在分布内外执行中的优劣，证明了世界模型生成的轨迹能有效提升智能体任务表现，并指出世界模型作为先验感知或训练监督比作为事后验证器更为有效。
selection_source: fresh_fetch
motivation: 旨在解决移动GUI智能体在复杂交互中难以预测动作后果的问题，并探究何种表征形式及引导方式能最有效地提升智能体性能。
method: 通过过滤和标注数据，训练了基于增量文本、全文本、扩散图像和可渲染代码四种模态的移动世界模型，并在多个下游任务中评估其效用。
result: 模型在MobileWorldBench等基准上达到SOTA，发现可渲染代码适合数据构建，而文本反馈在处理分布外数据时更具鲁棒性。
conclusion: 世界模型在作为先验感知或训练监督时效果最佳，而非通用的事后验证工具，且生成的交互经验对智能体训练具有显著的迁移价值。
---

## 摘要
视觉语言模型的最新进展使移动 GUI 智能体能够感知视觉界面并执行用户指令，但对于长时程和高风险的交互，对动作后果的可靠预测仍然至关重要。现有的移动世界模型提供基于文本或基于图像的未来状态，但目前尚不清楚哪种表示更有用、生成的展开（rollouts）是否可以替代真实环境，以及测试时引导如何帮助不同能力的智能体。为了回答上述问题，我们过滤并标注了移动世界模型数据，随后训练了涵盖四种模态的世界模型：增量文本（delta text）、全文、基于扩散的图像以及可渲染代码。这些模型在 MobileWorldBench 和 Code2WorldBench 上均达到了 SoTA 性能。此外，通过在 AITZ、AndroidControl 和 AndroidWorld 上评估其下游效用，我们得到了三个发现。首先，可渲染代码重构实现了较高的分布内保真度，并为数据构建提供了有效的多模态监督，而基于文本的反馈在在线分布外（OOD）执行中更为鲁棒。其次，世界模型生成的轨迹可以在训练过程中提供可迁移的交互经验，并提高智能体的端到端任务性能，尽管这些数据并未保持原始分布。最后，对于动作熵较低的过度自信移动智能体，后验自我反思带来的收益有限，这表明世界模型作为先验感知或训练监督比作为通用的事后验证器更有效。

## Abstract
Recent advances in vision-language models have enabled mobile GUI agents to perceive visual interfaces and execute user instructions, but reliable prediction of action consequences remains critical for long-horizon and high-risk interactions. Existing mobile world models provide either text-based or image-based future states, yet it remains unclear which representation is useful, whether generated rollouts can replace real environments, and how test-time guidance helps agents of different strengths. To answer the above questions, we filter and annotate mobile world-model data, then train world models across four modalities: delta text, full text, diffusion-based images, and renderable code. These models achieve SoTA performance on both MobileWorldBench and Code2WorldBench. Furthermore, by evaluating their downstream utility on AITZ, AndroidControl, and AndroidWorld, we obtain three findings. First, renderable code reconstruction achieves high in-distribution fidelity and provides effective multimodal supervision for data construction, while text-based feedback is more robust for online out-of-distribution (OOD) execution. Second, world-model-generated trajectories can provide transferable interaction experience in the training process and improve agents' end-to-end task performance, although these data do not preserve the original distribution. Last, for overconfident mobile agents with low action entropy, posterior self-reflection provides limited gains, suggesting that world models are more effective as prior perception or training supervision than as universal post-hoc verifiers.

---

## 论文详细总结（自动生成）

这是一份关于论文《How Mobile World Model Guides GUI Agents?》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着视觉语言模型（VLM）的发展，移动端 GUI 智能体已能执行复杂指令。然而，在长时程和高风险交互中，智能体往往缺乏对动作后果的预判能力，导致容错率低。
*   **核心问题**：现有的移动世界模型（World Models）通常仅提供单一的文本或图像预测，研究者尚不清楚：
    1.  哪种状态表征（文本、图像、代码等）对引导智能体最有效？
    2.  世界模型生成的虚拟轨迹（Rollouts）能否替代真实环境进行训练？
    3.  在测试阶段，世界模型如何有效地辅助不同能力的智能体？

### 2. 方法论
*   **核心思想**：通过构建并对比四种不同模态的移动世界模型，系统性地评估它们在预测精度及下游任务引导中的效用。
*   **关键技术细节**：
    *   **四种模态模型**：
        1.  **增量文本 (Delta Text)**：仅预测动作后界面元素的变化。
        2.  **全文本 (Full Text)**：预测动作后完整的界面描述。
        3.  **扩散图像 (Diffusion Image)**：利用扩散模型生成动作后的视觉截图。
        4.  **可渲染代码 (Renderable Code)**：预测可被渲染成界面的结构化代码（如 HTML/XML）。
    *   **数据处理**：对移动端交互数据进行严格过滤和自动标注，确保动作与状态转移的高质量对应。
    *   **引导策略**：探索了世界模型作为“先验感知”（预测未来辅助决策）、“训练监督”（生成合成数据）和“事后验证器”（反思错误）三种角色。

### 3. 实验设计
*   **评估基准 (Benchmarks)**：
    *   **模型性能评估**：MobileWorldBench 和 Code2WorldBench（用于衡量预测的保真度）。
    *   **下游效用评估**：AITZ、AndroidControl 和 AndroidWorld（涵盖了从简单点击到复杂跨应用操作的场景）。
*   **对比方法**：对比了不同模态的世界模型，以及在有无世界模型引导下的各类基座智能体（Agent）表现。

### 4. 资源与算力
*   **算力说明**：论文中未明确给出具体的 GPU 型号、数量或总训练时长。但考虑到涉及扩散模型（Diffusion）的训练以及大规模 VLM 的微调，推测使用了高性能计算集群（如 A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 4 种模态的横向对比。
    *   在 3 个主流移动端数据集上进行了下游任务验证。
    *   进行了分布内（ID）与分布外（OOD）的鲁棒性测试。
    *   包含针对“动作熵”和“自我反思”机制的消融实验。
*   **充分性评价**：实验设计较为全面且客观。研究不仅关注模型指标（如 BLEU、MSE），更深入探讨了世界模型对 Agent 决策逻辑的影响，具有较强的说服力。

### 6. 主要结论与发现
1.  **模态优劣**：**可渲染代码**在分布内（ID）具有最高的保真度，最适合用于构建合成训练数据；而**文本反馈**在面对未见过的界面（OOD）时表现出更强的鲁棒性。
2.  **训练价值**：世界模型生成的交互轨迹虽然不能完全保持原始分布，但能提供有效的**迁移经验**，显著提升智能体的端到端任务成功率。
3.  **引导机制**：世界模型作为**“先验感知”**（事前预测）或**“训练监督”**的效果远好于“事后验证”。
4.  **智能体特性**：移动智能体通常表现出“过度自信”（动作熵低），这使得事后的自我反思（Self-reflection）收益有限，因此事前引导更为关键。

### 7. 优点
*   **多模态视角**：首次系统性地引入并对比了“可渲染代码”这一模态，填补了现有研究在表征形式上的空白。
*   **实战导向**：不仅停留在预测精度上，还深入研究了如何将世界模型转化为 Agent 的实际性能提升。
*   **深刻洞察**：指出了移动端 Agent “过度自信”的特性，为未来设计更有效的反思机制提供了理论依据。

### 8. 不足与局限
*   **推理延迟**：生成图像或长代码作为预测状态会带来显著的计算开销，可能限制其在实时移动设备上的部署。
*   **OOD 瓶颈**：尽管文本模态相对鲁棒，但在极端陌生的应用界面下，所有模态的预测准确率仍有大幅下降的空间。
*   **依赖基座能力**：世界模型的表现高度依赖于底层 VLM 的理解能力，对于极细微的界面变化捕捉仍不够完美。

（完）
