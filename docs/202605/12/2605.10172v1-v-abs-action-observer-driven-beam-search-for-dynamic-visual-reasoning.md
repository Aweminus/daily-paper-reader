---
title: "V-ABS: Action-Observer Driven Beam Search for Dynamic Visual Reasoning"
title_zh: V-ABS：用于动态视觉推理的动作-观察者驱动束搜索
authors: "Zhiwei Ning, Xuanang Gao, Jiaxi Cao, Gengming Zhang, Shengnan Ma, Wenwen Tong, Hanming Deng, Jie Yang, Wei Liu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10172v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态大模型中动作-观察者驱动的束搜索用于动态视觉推理
tldr: "针对多模态大模型在复杂视觉推理中存在的想象与反馈不一致（IAO偏差）问题，本文提出V-ABS框架。该框架通过“思考者-执行者-观察者”迭代，结合动作-观察驱动的束搜索和基于熵的自适应加权算法，动态平衡先验置信度与观察反馈。实验表明，V-ABS在多个基准测试中显著提升了推理性能，在Qwen3-VL-8B上平均提升达19.7%。"
selection_source: fresh_fetch
motivation: 现有的多模态智能体在工具调用时往往忽略执行反馈，导致模型想象与实际观察结果之间存在偏差，影响推理的稳定性。
method: 提出一种由动作-观察驱动的束搜索框架，并利用基于熵的自适应加权算法动态调整策略先验与观察反馈的权重。
result: "在八个基准测试中达到SOTA水平，在Qwen3-VL-8B基准上平均提升19.7%，且在开源和闭源模型上均表现出一致的增益。"
conclusion: V-ABS通过引入执行反馈循环和自适应加权机制，有效解决了多步视觉推理中的偏差问题，显著增强了多模态模型的推理能力。
---

## 摘要
多模态大语言模型 (MLLMs) 在通用感知方面取得了显著成功，但复杂的多步视觉推理仍然是一个持续的挑战。尽管最近的智能体方法引入了工具使用，但它们往往忽略了关键的执行反馈。因此，它们面临想象-动作-观察者 (IAO) 偏差，即先验想象与观察者反馈之间的不一致，这损害了推理的稳定性和最优性。为了弥补这一差距，我们引入了 V-ABS，这是一个动作-观察者驱动的束搜索框架，通过思考者-执行者-观察者迭代实现深思熟虑的推理。我们还提出了一种基于熵的自适应加权算法，通过动态平衡策略先验与观察反馈之间的置信度分数来减轻 IAO 偏差。此外，我们构建了一个包含超过 8 万个样本的大规模有监督微调 (SFT) 数据集，以引导模型为正确的动作路径分配更高的先验置信度。在八个不同基准测试上的广泛实验表明，V-ABS 达到了最先进的性能，在 Qwen3-VL-8B 基准上平均提升了 19.7%，并在开源和专有模型中均实现了一致的增益。

## Abstract
Multimodal large language models (MLLMs) have achieved remarkable success in general perception, yet complex multi-step visual reasoning remains a persistent challenge. Although recent agentic approaches incorporate tool use, they often neglect critical execution feedback. Consequently, they suffer from the imagination-action-observer (IAO) bias, a misalignment between prior imagination and observer feedback that undermines reasoning stability and optimality. To bridge this gap, we introduce V-ABS, an action-observer driven beam search framework that enables deliberate reasoning through thinker-actor-observer iterations. We also propose an entropy-based adaptive weighting algorithm to mitigate the IAO bias by dynamically balancing the confidence scores between the policy priors and the observational feedback. Moreover, we construct a large-scale supervised fine-tuning (SFT) dataset comprising over 80k samples to guide the model to assign higher prior confidence to correct action paths. Extensive experiments across eight diverse benchmarks show that V-ABS achieves state-of-the-art performance, delivering an average improvement of 19.7% on the Qwen3-VL-8B baseline and consistent gains across both open-source and proprietary models.

---

## 论文详细总结（自动生成）

这是一份关于论文《V-ABS: Action-Observer Driven Beam Search for Dynamic Visual Reasoning》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管多模态大语言模型（MLLM）在感知任务上表现优异，但在处理需要多步交互的复杂视觉推理（如视觉搜索、导航）时仍面临挑战。
*   **核心问题**：作者发现现有智能体方法存在**想象-动作-观察者（IAO）偏差**。即模型在执行动作前的“先验想象”与动作执行后观察到的“实际反馈”之间存在显著不一致（相似度仅为0.38-0.59）。这种偏差导致模型容易陷入错误的推理路径。
*   **整体含义**：论文旨在将推理过程从“开环”转为“闭环”，通过引入执行反馈来修正模型的先验偏差。

### 2. 方法论
V-ABS 框架将视觉推理建模为一个动态的马尔可夫决策过程（MDP），核心包含以下三个模块的迭代：
*   **思考者-执行者-观察者（TAO）循环**：
    *   **思考者 (Thinker)**：根据当前状态预测候选动作的先验得分（$F_{pri}$）。
    *   **执行者 (Actor)**：调用外部工具（如裁剪、缩放、填充）执行动作，更新视觉状态。
    *   **观察者 (Observer)**：对执行后的新视觉状态进行评估，给出观察反馈得分（$F_{obs}$）。
*   **关键技术细节**：
    *   **动作-观察驱动的束搜索 (Beam Search)**：在每一步推理中保留 Top-K 个最优路径，而非单一路径。
    *   **基于熵的自适应加权算法**：计算思考者预测的香农熵。如果思考者不确定性高（熵大），则增加观察者反馈的权重；反之则信任先验。
    *   **加速机制**：当先验置信度极高时，跳过耗时的观察者模块以提升效率。
    *   **SFT 策略**：构建了 80k 规模的数据集，训练模型在思考阶段为正确动作分配更高的概率，从而降低先验不确定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **精细视觉搜索**：V* Benchmark, HR-Bench-4K/8K（评估超高分辨率感知）。
    *   **视觉导航**：VisuoThink (Level 3-5), Frozen Lake (4x4 到 8x8 网格), TIR-Bench Maze。
    *   **视觉逻辑与操作**：Jigsaw（拼图）, Sudoku（数独）。
*   **对比方法**：
    *   **基础模型**：Qwen3-VL-8B, Qwen2.5-VL-7B, InternVL3-8B, GPT-4o。
    *   **SOTA 方法**：ZoomEye, DeepEyes V2, Pixel-Reasoner, Thyme 等。
    *   **搜索策略对比**：标准束搜索 (Beam Search) 和 蒙特卡洛树搜索 (MCTS)。

### 4. 资源与算力
*   **训练算力**：使用了 **16 张 NVIDIA H100-80G GPU**。
*   **训练细节**：有监督微调（SFT）进行了 200 个 step，batch size 为 128，学习率为 $1 \times 10^{-5}$。
*   **推理算力**：推理阶段使用单张 GPU 即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 大类任务、8 个主流基准测试。
*   **消融实验**：非常充分。作者针对搜索深度（D=1到16）、加权组件（仅先验 vs 仅观察 vs 自适应）、SFT 的影响、搜索策略对比、熵阈值（$\delta$）以及提示词模板等均做了详细的对比实验。
*   **公平性**：实验涵盖了开源和闭源模型，并与最新的 SOTA 方法在相同基准下对比，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：V-ABS 在 Qwen3-VL-8B 上实现了平均 **19.7%** 的性能提升，在 InternVL3-8B 上提升了 **13.9%**。
*   **IAO 偏差的缓解**：通过自适应加权，模型能有效识别并修正错误的先验判断。
*   **SFT 与搜索的关系**：对于动作空间受限的任务（如裁剪），训练前的搜索已足够；但对于开放式操作任务（如拼图），SFT 是必不可少的，因为它能显著缩小搜索空间。
*   **超越 SOTA**：V-ABS 仅通过推理时优化和 SFT，就超过了许多使用强化学习（RL）的复杂方法。

### 7. 优点
*   **闭环反馈机制**：通过“观察者”模块解决了 MLLM 常见的幻觉和盲目执行问题。
*   **通用性强**：框架不依赖特定模型架构，在 GPT-4o 等闭源模型上也表现出显著增益。
*   **理论支撑**：引入统计估计理论（逆方差加权原则）来解释熵驱动的加权机制，具有科学性。
*   **效率平衡**：设计了加速机制，在保证精度的同时兼顾了推理成本。

### 8. 不足与局限
*   **推理成本**：尽管有加速机制，但束搜索和多次调用观察者模块仍会增加 API 调用次数和 Token 消耗（相比单次推理）。
*   **启发式依赖**：在某些任务中仍需定义简单的启发式得分（$F_{heur}$），这可能需要一定的领域知识。
*   **工具局限性**：推理能力受限于可用工具的范围（如目前主要集中在裁剪、填充等基础操作）。

（完）
