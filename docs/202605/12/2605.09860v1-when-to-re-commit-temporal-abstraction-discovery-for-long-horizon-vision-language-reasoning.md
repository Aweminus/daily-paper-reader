---
title: "When to Re-Commit: Temporal Abstraction Discovery for Long-Horizon Vision-Language Reasoning"
title_zh: 何时重新承诺：面向长程视觉语言推理的时间抽象发现
authors: "Chen Li, Zhantao Yang, Fangyi Chen, Han Zhang, Anudeepsekhar Bolimera, Marios Savvides"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09860v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 长程视觉语言推理以及用于动作承诺的可学习策略
tldr: 针对长程视觉语言推理任务，本文提出了“承诺深度”概念，即在重新规划前执行的开环动作数量。研究者开发了一种能根据状态自适应预测动作及其执行深度的视觉语言策略。在拼图和推箱子任务中，该方法在成功率和效率上均显著优于固定深度的基准模型及GPT-4o等强力模型，证明了动态调整执行步长在复杂推理中的优越性。
selection_source: fresh_fetch
motivation: 现有的长程推理系统通常采用固定的重规划频率，难以在规划成本与执行误差之间取得最佳平衡。
method: 将承诺深度作为策略的可学习状态变量集成到视觉语言模型中，使其能同时预测动作序列及其执行步长。
result: "该自适应策略在提高解决率的同时减少了约25%的动作执行量，性能超越了GPT-4o和Claude Sonnet等顶级模型。"
conclusion: 理论与实验均证明，状态感知的动态承诺深度在性能上严格优于任何固定深度的策略。
---

## 摘要
长程推理不仅需要决定采取哪些动作，还需要决定在下一次观测之前要投入多深。我们将其形式化为“承诺深度”（commitment depth）：即在两次重规划之间以开环方式执行的基元动作数量。承诺深度在重规划成本与复合执行误差之间引入了权衡，然而大多数现有的长程系统将其固定为人工设计的标量。在这项工作中，我们将承诺深度视为策略本身的一个可学习且基于状态的变量。我们在模型原生的视觉-语言策略中实现了这一方案，该策略能够联合预测执行内容及执行时长。在数字华容道（Sliding Puzzle）和推箱子（Sokoban）任务中，由此产生的自适应策略在帕累托意义上优于所有非退化的固定深度基准，在每回合减少约 25% 基元动作的同时，成功率提升了多达 12.5 个百分点。尽管仅使用了 7B 参数的主干网络，我们的方法在两项任务上均优于 GPT-5.5 和 Claude Sonnet，而所有受测的开源视觉-语言模型在零样本测试中的成功率均为 0%。我们进一步提供了理论分析，证明在标准承诺深度代理指标下，只要局部最优深度随状态而异，基于状态的承诺就严格优于任何固定深度。

## Abstract
Long-horizon reasoning requires deciding not only what actions to take, but how deeply to commit before the next observation. We formalize this as \emph{commitment depth}: the number of primitive actions executed open-loop between replans. Commitment depth induces a trade-off between replanning cost and compounding execution error, yet most existing long-horizon systems fix it as a hand-designed scalar. In this work, we instead treat commitment depth as a learnable, state-conditioned variable of the policy itself. We instantiate this within a model-native vision--language policy that jointly predicts both what to execute and for how long. Across Sliding Puzzle and Sokoban, the resulting adaptive policy Pareto-dominates every non-degenerate fixed-depth baseline, achieving up to 12.5 percentage points higher solve rate while using approximately 25\% fewer primitive actions per episode. Despite using a 7B backbone, our method outperforms GPT-5.5 and Claude Sonnet on both tasks, while every tested open-weight vision--language model achieves 0\% zero-shot success. We further present a theoretical analysis showing that, under the standard commitment-depth surrogate, state-conditioned commitment strictly dominates any fixed depth whenever the locally optimal depth varies across states.

---

## 论文详细总结（自动生成）

这篇论文探讨了长程视觉语言推理中的一个核心决策问题：在重新观察环境并规划之前，模型应该“承诺”执行多少步动作。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：在长程任务（如复杂拼图、机器人操控）中，系统通常采用“规划-执行-观测”的循环。目前主流系统（如动作分块 Action Chunking）通常将执行步长（即**承诺深度 $h$**）固定为一个预设常数。
*   **研究动机**：固定的承诺深度存在权衡：步长太短会导致频繁重规划，增加计算开销；步长太长则会因为开环执行中误差的累积导致偏离目标。论文认为，最优的承诺深度应该是**状态感知（State-conditioned）**的，即在确定性高的状态下多走几步，在不确定性高的状态下谨慎行事。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将承诺深度 $h$ 视为策略的一个可学习变量，由视觉语言模型（VLM）根据当前视觉观测动态预测。
*   **关键技术**：
    *   **统一策略架构**：基于 Qwen2.5-VL-7B 主干网络，设计了两个共享特征的预测头：**深度头（Depth Head）**预测 $h \in \{1, 2, 4, 8\}$；**动作头（Action Head）**自回归生成对应长度的动作序列。
    *   **两阶段训练流程**：
        1.  **有监督微调 (SFT)**：使用专家轨迹生成“反事实宏动作”数据。对于每个状态，模型学习在不同 $h$ 下应执行的正确动作序列。
        2.  **强化学习 (RL)**：采用 **GRPO（群体相对策略优化）** 算法。奖励函数结合了任务成功（稀疏奖励）和基于精确求解器提供的距离缩减（稠密奖励）。
    *   **理论证明**：论文通过数学模型证明，只要不同状态下的局部最优深度不同，自适应深度策略在性能上必然严格优于任何固定深度策略。

### 3. 实验设计
*   **实验场景**：
    *   **数字华容道 (Sliding Puzzle)**：可逆任务，侧重于搜索效率。
    *   **推箱子 (Sokoban)**：不可逆任务（存在死锁），侧重于规划的准确性。
*   **基准测试 (Benchmarks)**：
    *   **固定深度基准**：$h=1, 2, 4, 8$ 的同架构模型。
    *   **前沿闭源模型**：GPT-5.5 (OpenAI 预研版本概念)、Claude 3.5 Sonnet、Gemini 3.1 Pro。
    *   **开源 VLM**：InternVL3、Qwen2.5-VL、Qwen3-VL 等不同参数规模的模型。
*   **对比维度**：解决率（Solve Rate）与每回合平均动作数（Efficiency）的帕累托前沿。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA A100 (80GB)** 显卡。
*   **训练时长**：
    *   SFT 阶段：约 2-3 小时。
    *   RL 阶段：约 30 分钟至 1 小时。
*   **模型规模**：主干网络为 7B 参数。

### 5. 实验数量与充分性
*   **实验规模**：在两个具有挑战性的视觉推理任务上进行了广泛测试。
*   **消融实验**：
    *   测试了仅训练深度头、仅训练动作头以及联合训练的效果。
    *   研究了不同训练预算（$K_{train}$）对策略稳定性的影响。
    *   进行了多随机种子实验以验证鲁棒性。
*   **充分性评价**：实验设计非常严谨，不仅对比了最先进的商业模型，还通过理论分析与实验结果相互印证，客观地展示了自适应深度的优势。

### 6. 主要结论与发现
*   **帕累托主导**：自适应策略在所有任务中均优于固定深度的基准。在解决率提升高达 **12.5%** 的同时，减少了约 **25%** 的动作执行量。
*   **超越巨头**：尽管只有 7B 参数，该模型在特定任务上的表现超过了 GPT-5.5 和 Claude Sonnet，而其他开源模型在零样本（Zero-shot）情况下得分几乎为 0。
*   **机制发现**：模型学会了在接近目标（信号清晰）时增加承诺深度，在远离目标或环境复杂（信号嘈杂）时减小深度。

### 7. 优点（亮点）
*   **问题定义新颖**：首次将“何时重新承诺”形式化为预算约束下的优化问题。
*   **模型原生（Model-native）**：不需要外部规划器或复杂的启发式规则，直接在 VLM 内部实现时间抽象。
*   **高效性**：通过 RL 成功让模型在推理成功率和计算效率之间找到了自动平衡点。

### 8. 不足与局限
*   **任务领域受限**：目前仅在拼图类游戏（Sliding Puzzle, Sokoban）中验证，这些任务具有完美的动力学模型和精确求解器。
*   **依赖求解器**：训练阶段需要精确求解器提供距离信息作为稠密奖励，这在现实世界任务（如开放域机器人操控）中难以获取。
*   **泛化性待验证**：尚未在更广泛的具身智能场景（如 Web 导航或真实机器人）中测试其泛化能力。

（完）
