---
title: Emergent Communication between Heterogeneous Visual Agents through Decentralized Learning
title_zh: 通过去中心化学习在异构视觉智能体之间实现的涌现通信
authors: "Mikako Ochiai, Masatoshi Nagano, Tadahiro Taniguchi"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11695v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉智能体之间的涌现通信，非机器人重点
tldr: 本研究探讨了具有异构视觉表征的智能体在去中心化学习下如何产生涌现通信。研究者提出了“大都会-哈斯廷斯字幕游戏”（MHCG），智能体仅交换离散令牌，并根据各自私有的视觉特征利用MH准则进行局部评估和模型更新。实验表明，即使没有共享的外部目标，智能体也能建立起具有视觉信息量的共享符号系统。研究还揭示了视觉编码器的相似程度如何约束涌现语言的内容、对称性及特异性，为理解异构系统间的协作提供了新视角。
selection_source: carryover_cache
motivation: 探究在感知空间私有且异构的情况下，智能体能否仅通过局部感知证据而非共享目标来建立共同的符号语言。
method: 提出MHCG框架，让智能体通过交换令牌序列并利用基于自身视觉特征的MH接受准则进行去中心化协作学习。
result: 在MS-COCO上的实验显示，MHCG生成的序列在跨智能体对齐和图像检索任务中优于无通信基准，且性能随编码器差异增加而下降。
conclusion: 共享符号可仅凭局部感知评估产生，而智能体间视觉表征的相似性是决定涌现语言质量和对称性的关键因素。
---

## 摘要
符号是共享的，但感知是私有的。我们研究了通过去中心化学习在异构视觉智能体之间产生的涌现通信，探讨当智能体具有不同的视觉表示时，哪些视觉信息可以变得可共享。我们的智能体并非通过共享的外部通信目标来优化消息，而是仅交换离散的标记序列，并利用局部感知证据更新各自的模型。这一设定聚焦于涌现通信中一个尚未得到充分探索的方面，即在没有共享感知访问权限的情况下，是否能产生共同的符号，以及私有视觉空间之间的相似性如何约束所得语言的内容和对称性。我们在 Metropolis-Hastings 字幕游戏（MHCG）中实例化了这一设定。在该游戏中，两个智能体通过交换提议的标记序列来协作形成共享字幕，听者根据其自身的视觉特征，利用 MH 风格的标准来接受或拒绝这些序列。我们比较了三种冻结视觉编码器的配对情况，智能体从随机初始化的文本模块开始。在 MS-COCO 上的实验表明，MHCG 产生了具有视觉信息量的共享标记序列，在跨智能体对齐、视觉特征预测和图文检索方面优于无通信基准；随着编码器不匹配程度的增加，所有跨智能体指标均有所下降。中等程度的编码器异构性会减少共享序列的数量，但保留了每个序列的视觉特异性；而更强的编码器异构性则会导致产生更少、更粗略且更不对称的序列。消融实验表明，听者侧的 MH 接受机制对于避免退化标记的形成至关重要。这些结果表明，仅通过局部感知评估即可产生共享符号，而跨编码器的视觉表示相似性塑造了所得语言的内容和对称性。

## Abstract
Symbols are shared, but perception is private. We study emergent communication between heterogeneous visual agents through decentralized learning, asking what visual information can become shareable when agents have different visual representations. Instead of optimizing messages through a shared external communicative objective, our agents exchange only discrete token sequences and update their own models using local perceptual evidence. This setting focuses on an underexplored aspect of emergent communication, examining whether common symbols can arise without shared perceptual access, and how the similarity between private visual spaces constrains the content and symmetry of the resulting language. We instantiate this setting in the Metropolis-Hastings Captioning Game (MHCG), where two agents collaboratively form shared captions by exchanging proposed token sequences that a listener accepts or rejects using an MH-style criterion evaluated against its own visual features. We compare three pairings of frozen visual encoders, with agents starting from randomly initialized text modules. Experiments on MS-COCO show that MHCG produces visually informative shared token sequences that outperform a no-communication baseline in cross-agent alignment, visual-feature prediction, and image-text retrieval; all cross-agent metrics decline as encoder mismatch increases. Moderate encoder heterogeneity reduces the number of shared sequences while preserving per-sequence visual specificity, whereas stronger encoder heterogeneity yields fewer, coarser, and more asymmetric sequences. Ablations show that listener-side MH acceptance is critical for avoiding degenerate token formation. These results suggest that shared symbols can arise from local perceptual evaluation alone, with visual representational similarity across encoders shaping both the content and symmetry of the resulting language.

---

## 论文详细总结（自动生成）

这是一份关于论文《Emergent Communication between Heterogeneous Visual Agents through Decentralized Learning》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多智能体系统中，通信需要协调公共符号，但每个智能体的感知（视觉表征）通常是私有的且可能存在异构性（如不同的架构、训练历史或传感器）。
*   **核心问题**：当智能体的视觉表征不同，且无法访问彼此的感知数据、无法共享梯度或目标函数时，它们能否仅通过局部感知证据的交换产生共享的符号？这种异构性如何影响涌现语言的内容、特异性和对称性？
*   **背景**：传统的涌现通信（EC）多依赖于共享的外部任务奖励（如指代游戏），这掩盖了符号如何在私有感知空间之间起到接口作用的本质。

### 2. 论文提出的方法论
*   **核心思想**：基于**集体预测编码（CPC）**理论，将符号视为智能体推断出的共享潜变量。通过**大都会-哈斯廷斯字幕游戏（MHCG）**实现去中心化推理。
*   **关键技术细节**：
    *   **架构**：每个智能体由冻结的视觉编码器（如 DINOv2, MAE）和可训练的文本模块（类 BLIP 架构）组成。
    *   **MHCG 流程**：
        1.  **提议**：说话者（Speaker）根据其观察到的图像生成一个标记序列（Caption）提议 $c^*$。
        2.  **评估**：听者（Listener）根据其自身的视觉特征，利用 **ProbVLM** 估计提议序列的似然值。
        3.  **接受/拒绝**：听者计算 MH 风格的似然比 $r = \min(1, \frac{p(z_{Li,v} | c^*)}{p(z_{Li,v} | c)})$，决定是接受提议还是保留自己生成的字幕。
        4.  **更新**：被接受的序列作为本地训练目标，通过语言建模（LLM）、图文对比（ITC）和匹配（ITM）损失更新文本模块。
*   **去中心化特性**：智能体之间仅交换离散标记，不共享参数、梯度或全局奖励。

### 3. 实验设计
*   **数据集**：使用 **MS-COCO**。为了模拟私有感知，两个智能体观察同一张图像的独立增强版本（随机裁剪、翻转）。
*   **编码器配对（Benchmark）**：
    *   **HOMO**：同构（DINOv2-B/14 + DINOv2-B/14）。
    *   **HETERO 1**：中等异构（DINOv2-B/14 + DINOv2-S/14）。
    *   **HETERO 2**：强异构（DINOv2-B/14 + MAE-B/16）。
*   **对比方法（Baseline）**：
    *   **NoCom**：无通信基准。
    *   **AllAccept**：无条件接受所有提议。
    *   **Random accept**：以相同概率随机接受。
    *   **ITM-based**：基于判别式匹配分数进行接受。
*   **评估指标**：表征相似性分析（RSA）、跨智能体图文检索（i2t/t2i）、视觉特征预测（$\Delta R^2$）。

### 4. 资源与算力
*   **硬件**：使用了单节点 3 块 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：完成一个 40 epoch 的训练任务大约需要 **9.7 到 23.6 小时**（中位数为 11 小时）。
*   **算力消耗**：标称每个运行任务消耗约 29-71 A100 GPU 小时。

### 5. 实验数量与充分性
*   **实验规模**：每个配置运行了 **3 个随机种子**（Seed 1, 2, 4；Seed 3 因基准方法崩溃被排除）。
*   **充分性评价**：
    *   **客观性**：通过固定测量空间（如 DINOv2-B CLS 空间）评估视觉特异性，避免了评估指标向特定智能体倾斜。
    *   **全面性**：涵盖了从同构到强异构的梯度对比，并进行了详尽的消融实验来验证 MH 接受准则的必要性。
    *   **局限性**：种子数量较少（n=3），在某些统计显著性判断上（如视觉半径差异）略显单薄。

### 6. 主要结论与发现
*   **共享符号的产生**：即使在完全去中心化且视觉异构的情况下，MHCG 也能产生具有视觉信息量的共享标记序列。
*   **异构性的影响**：
    *   随着编码器不匹配程度增加，所有跨智能体通信指标均下降。
    *   **中等异构**：减少了共享序列的数量（词汇量缩减），但保留了单个序列的视觉特异性（精准度）。
    *   **强异构**：导致产生更少、更粗略且具有明显**方向性偏差**（向更强能力的编码器对齐）的序列。
*   **机制验证**：听者侧的 MH 似然比过滤是防止标记退化和支持视觉信息转移的关键。

### 7. 优点
*   **理论严谨**：将涌现通信建立在贝叶斯推理和 CPC 框架之上，而非简单的启发式奖励。
*   **隐私与解耦**：实现了一种无需共享内部参数或梯度的协作学习模式，具有隐私保护的潜力。
*   **深入的表征分析**：利用 RSA 和偏相关分析揭示了涌现语言如何“偏袒”特定智能体的私有视觉几何结构。

### 8. 不足与局限
*   **感知固定**：视觉编码器是冻结的。在现实中，语言的涌现可能会反过来重塑感知表征（感知学习），本文未涉及。
*   **规模限制**：词汇量（100）和序列长度（5）较小，尚不清楚在更复杂的语言空间中去中心化协调的难度。
*   **泛化性**：仅在 MS-COCO 自然图像上测试，未在其他模态或更极端的异构场景（如视觉 vs 触觉）中验证。
*   **自一致性假设**：MH 准则中的简化依赖于解码器提议与编码器后验的自一致性，这在训练初期较弱。

（完）
