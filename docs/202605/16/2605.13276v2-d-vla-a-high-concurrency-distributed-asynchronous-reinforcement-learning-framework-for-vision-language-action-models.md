---
title: "D-VLA: A High-Concurrency Distributed Asynchronous Reinforcement Learning Framework for Vision-Language-Action Models"
title_zh: D-VLA：面向视觉-语言-动作模型的高并发分布式异步强化学习框架
authors: "Yucheng Guo, Yongjian Guo, Zhong Guan, Wen Huang, Haoran Sun, Haodong Yue, Xiaolong Xiang, Shuai Di, Zhen Sun, Luqiao Wang, Junwu Xiong, Yicheng Gong"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13276v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的分布式强化学习框架
tldr: 针对具身智能中大模型强化学习面临的仿真与深度学习资源冲突问题，本文提出了D-VLA分布式异步框架。该框架通过平面解耦、四线程异步流水线及双池显存管理等技术，实现了采样、推理与训练的高效并行。实验证明，D-VLA在处理十亿级参数模型时显著提升了吞吐量和采样效率，并具备良好的扩展性，为通用具身智能体提供了高效的训练支撑。
selection_source: fresh_fetch
motivation: 解决大规模具身基础模型在分布式强化学习中，物理仿真与深度学习计算之间的资源竞争及系统瓶颈。
method: 提出平面解耦机制隔离数据与权重，并设计四线程异步流水线与双池显存管理模型以优化并行效率。
result: 在LIBERO等基准测试中，D-VLA在吞吐量和采样效率上显著优于主流框架，并实现了优异的线性加速比。
conclusion: D-VLA为高性能通用具身智能体的训练提供了一个高并发、低延迟且具备极强扩展性的稳健系统框架。
---

## 摘要
具身智能（Embodied AI）的快速演进使得视觉-语言-动作（VLA）模型在多模态感知和任务执行方面表现卓越。然而，在大规模分布式环境中将强化学习（RL）应用于这些海量模型面临着严重的系统瓶颈，这主要是由于高保真物理仿真与深度学习对显存（VRAM）/带宽的密集需求之间的资源冲突所致。这种冲突通常导致整体吞吐量受限于执行阶段的低效。为了应对这些挑战，我们提出了 D-VLA，这是一个面向大规模具身基础模型的高并发、低延迟分布式强化学习框架。D-VLA 引入了“平面解耦”（Plane Decoupling），在物理上将高频训练数据与低频权重控制隔离，以消除仿真与优化之间的干扰。我们进一步设计了一个四线程异步“泳道”（Swimlane）流水线，实现了采样、推理、梯度计算和参数分发的完全并行重叠。此外，双池显存管理模型和拓扑感知复制解决了内存碎片问题并优化了通信效率。在 LIBERO 等基准测试上的实验表明，对于十亿参数规模的 VLA 模型，D-VLA 在吞吐量和采样效率方面显著优于主流强化学习框架。在万亿参数的可扩展性测试中，我们的框架保持了卓越的稳定性和线性加速比，为高性能通用具身智能体提供了一个稳健的系统。

## Abstract
The rapid evolution of Embodied AI has enabled Vision-Language-Action (VLA) models to excel in multimodal perception and task execution. However, applying Reinforcement Learning (RL) to these massive models in large-scale distributed environments faces severe systemic bottlenecks, primarily due to the resource conflict between high-fidelity physical simulation and the intensive VRAM/bandwidth demands of deep learning. This conflict often leaves overall throughput constrained by execution-phase inefficiencies. To address these challenges, we propose D-VLA, a high-concurrency, low-latency distributed RL framework for large-scale embodied foundation models. D-VLA introduces "Plane Decoupling," physically isolating high-frequency training data from low-frequency weight control to eliminate interference between simulation and optimization. We further design a four-thread asynchronous "Swimlane" pipeline, enabling full parallel overlap of sampling, inference, gradient computation, and parameter distribution. Additionally, a dual-pool VRAM management model and topology-aware replication resolve memory fragmentation and optimize communication efficiency. Experiments on benchmarks like LIBERO show that D-VLA significantly outperforms mainstream RL frameworks in throughput and sampling efficiency for billion-parameter VLA models. In trillion-parameter scalability tests, our framework maintains exceptional stability and linear speedup, providing a robust system for high-performance general-purpose embodied agents.

---

## 论文详细总结（自动生成）

### D-VLA：面向视觉-语言-动作模型的高并发分布式异步强化学习框架总结

#### 1. 核心问题与研究动机
*   **研究背景**：具身智能（Embodied AI）正从依赖专家数据的监督微调（SFT）转向通过强化学习（RL）进行自主探索，以提升泛化能力。
*   **核心问题**：在大规模分布式环境下，训练视觉-语言-动作（VLA）模型面临严重的系统瓶颈。主要矛盾在于：**高保真物理仿真**（产生高频、碎片化的资源占用）与**大规模深度学习**（需要极高的显存带宽和吞吐量）之间的资源竞争。
*   **瓶颈表现**：现有框架在仿真逻辑与学习逻辑耦合时，容易产生显存碎片、通信延迟高以及由于“木桶效应”导致的硬件利用率低下。

#### 2. 方法论：核心思想与关键技术
D-VLA 提出了“平面解耦”的设计哲学，旨在从底层架构上消除仿真与优化的干扰：
*   **平面解耦（Plane Decoupling）**：物理隔离“训练数据平面”（高频、大流量的交互数据）与“权重控制平面”（低频、高确定性的参数同步）。
*   **四线程“泳道”流水线（Swimlane Pipeline）**：设计了四个并行线程——数据采样、异步权重接收、梯度训练执行、参数分发。这使得采样、推理、计算和通信能够完全重叠。
*   **双池显存管理（Dual-pool VRAM Management）**：将显存划分为“模型计算池”（Torch管理）和“环境辅助池”（物理引擎专用），防止物理引擎频繁申请释放内存导致的框架崩溃。
*   **拓扑感知复制（Topology-aware Replication）**：在节点内构建完整的“采样-推理”闭环，减少跨节点通信；全局梯度归约结合 FSDP 技术，并由控制平面异步处理。
*   **算法集成**：集成了群体相对策略优化（GRPO）算法，配合微批次训练以适应异步架构。

#### 3. 实验设计
*   **模型架构**：
    *   **$\pi_{0.5}$**：基于扩散（Diffusion）的迭代去噪模型。
    *   **OpenVLA-OFT**：基于 Transformer 的自回归模型，使用参数高效微调（PEFT）。
*   **仿真环境**：使用 **ManiSkill** 物理仿真框架（支持 GPU 加速渲染和物理内核）。
*   **基准测试（Benchmark）**：LIBERO 任务集及 ManiSkill 操纵任务。
*   **对比方法**：
    *   **RLinf-VLA**：支持同置（co-located）、解耦（disaggregated）和混合（hybrid）模式的通用框架。
    *   **RL-VLA³**：现有的三阶段异步分布式架构。

#### 4. 资源与算力
*   **硬件配置**：实验主要在一个 **16-GPU 集群**上进行。
*   **网络互联**：使用了 InfiniBand 网络织物，支持 NCCL（数据平面）和 Gloo（控制平面）后端。
*   **算力细节**：文中提到了万亿参数级别的扩展性测试，但未详细列出训练总时长，重点在于吞吐量（steps/s）和延迟（ms）的测量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从单机多卡到多机多卡的扩展性实验，环境数量从 384 扩展到 3,072 个。
*   **维度覆盖**：对比了不同资源分配比例（如 Rollout 与 Actor 的比例为 1:1 或 3:1）、不同模型架构（扩散 vs Transformer）以及不同部署策略。
*   **充分性评价**：实验设计较为充分，通过消融实验展示了资源配比对吞吐量的影响，并验证了异步机制在不损失收敛性能（Success Rate）的前提下提升了效率，结果具有客观性。

#### 6. 主要结论与发现
*   **吞吐量大幅提升**：在 $\pi_{0.5}$ 模型上，D-VLA 相比基准框架实现了最高 **86.26%** 的吞吐量增长；在 OpenVLA-OFT 上提升了 **44.44%**。
*   **延迟优化**：通过流水线掩盖了推理延迟，总步骤时间（Step Time）相比传统解耦方案减少了约 **50%**。
*   **线性扩展性**：在多节点扩展中表现出良好的稳定性，能够有效利用硬件并行潜力，直到达到 GPU 显存带宽或计算单元的物理饱和点。
*   **训练稳定性**：异步架构并未破坏 GRPO 算法的收敛性，成功率曲线与同步框架保持一致。

#### 7. 优点与亮点
*   **架构创新**：通过“平面解耦”和“泳道”模型，从系统工程角度解决了具身智能训练中特有的异构资源冲突问题。
*   **显存优化**：双池管理模型有效解决了物理引擎与深度学习框架共存时的显存碎片痛点。
*   **灵活性**：支持同置、分离和混合等多种部署策略，能够根据硬件拓扑动态调整。

#### 8. 不足与局限
*   **时间对齐依赖**：异步效率高度依赖于各组件（Rollout 与 Actor）之间的时间对齐。如果资源配比不当（如 Actor 训练太慢），系统会退化为准同步模式，导致性能下降。
*   **硬件依赖**：高性能表现依赖于高速互联（如 InfiniBand），在低带宽环境下的表现未做深入探讨。
*   **动态调整自动化**：目前资源配比（如 1:1 或 3:1）似乎仍需人工设定，未来需要更智能的动态负载均衡机制。

（完）
