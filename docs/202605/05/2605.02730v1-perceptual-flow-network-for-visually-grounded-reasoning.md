---
title: Perceptual Flow Network for Visually Grounded Reasoning
title_zh: 面向视觉落地推理的感知流网络
authors: "Yangfu Li, Yuning Gong, Hongjian Zhan, Teng Li, Yuanhuiyi Lyu, Tianyi Chen, Qi Liu, Ziyuan Huang, Zhihang Zhong, Dandan Zheng, Yue Lu"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02730v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大型视觉语言模型中的可解释视觉推理
tldr: "针对大型视觉语言模型（LVLM）中存在的语言偏见和幻觉问题，本文提出了感知流网络（PFlowNet）。该方法通过解耦感知与推理，建立自条件生成过程，并利用变分强化学习结合多维奖励与邻域几何塑造，使模型能够生成更具推理导向且视觉可靠的感知行为。PFlowNet在V* Bench和MME-RealWorld-lite等基准测试中刷新了SOTA记录，显著提升了视觉定位推理能力。"
selection_source: fresh_fetch
motivation: 现有的视觉专家监督过于强调几何精度而忽视了推理效用，导致模型在复杂视觉推理任务中容易产生幻觉。
method: 提出PFlowNet，通过解耦感知与推理并引入变分强化学习，将多维奖励与几何约束相结合以优化视觉感知轨迹。
result: "在V* Bench（90.6%）和MME-RealWorld-lite（67.0%）等多个基准测试中取得了新的SOTA性能。"
conclusion: PFlowNet证明了通过优化推理导向的感知行为，可以有效缓解LVLM的幻觉问题并提升视觉定位推理的准确性。
---

## 摘要
尽管大视觉语言模型（LVLMs）取得了成功，但通用的优化目标（如标准极大似然估计 MLE）未能约束视觉轨迹，导致了语言偏见和幻觉。为了缓解这一问题，现有方法引入了来自视觉专家的几何先验作为额外监督。然而，我们观察到这种监督通常是次优的：它偏向于几何精度，而提供的推理效用有限。为了弥补这一差距，我们提出了感知流网络（PFlowNet），它避开了与专家先验的僵化对齐，实现了可解释且更有效的视觉推理。具体而言，PFlowNet 将感知与推理相解耦，以建立一个自条件生成过程。在此基础上，它通过变分强化学习将多维奖励与邻域几何塑造相结合，从而在保持视觉可靠性的同时，促进了面向推理的感知行为。PFlowNet 提供了可证明的性能保证和具有竞争力的实证结果，特别是在 V* Bench (90.6%) 和 MME-RealWorld-lite (67.0%) 上创下了新的 SOTA 记录。

## Abstract
Despite the success of Large-Vision Language Models (LVLMs), general optimization objectives (e.g., standard MLE) fail to constrain visual trajectories, leading to language bias and hallucination. To mitigate this, current methods introduce geometric priors from visual experts as additional supervision. However, we observe that such supervision is typically suboptimal: it is biased toward geometric precision and offers limited reasoning utility. To bridge this gap, we propose Perceptual Flow Network (PFlowNet), which eschews rigid alignment with the expert priors and achieves interpretable yet more effective visual reasoning. Specifically, PFlowNet decouples perception from reasoning to establish a self-conditioned generation process. Based on this, it integrates multi-dimensional rewards with vicinal geometric shaping via variational reinforcement learning, thereby facilitating reasoning-oriented perceptual behaviors while preserving visual reliability. PFlowNet delivers a provable performance guarantee and competitive empirical results, particularly setting new SOTA records on V* Bench (90.6%) and MME-RealWorld-lite (67.0%).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PFlowNet（感知流网络）** 的新型框架，旨在解决大型视觉语言模型（LVLM）在复杂视觉推理任务中存在的语言偏见和幻觉问题。

以下是对该论文的结构化总结：

### 1. 研究动机与核心问题
*   **核心问题**：现有的 LVLM 优化目标（如 MLE）无法有效约束视觉注意力轨迹。虽然目前有研究引入视觉专家（如检测模型）的几何先验进行强化学习（RLVR），但作者发现**几何精度并不等同于推理效用**。
*   **研究动机**：实验表明，过于精确的专家标注（如紧贴目标的框）反而会因缺乏上下文（隧道视野效应）而降低推理准确率。因此，需要一种能够平衡“视觉可靠性”与“推理导效用”的感知机制。

### 2. 方法论：感知流网络（PFlowNet）
*   **核心思想**：将感知过程从推理中解耦，构建一个结构化的“感知流（Perceptual Flow）”，并通过变分强化学习（Variational RFT）进行优化。
*   **关键技术细节**：
    *   **感知流结构**：包含“规划状态（Planning State）”（分析查询并确定候选区域）和“感知状态（Perceptual States）”（一系列感兴趣区域 RoI 及其描述性标题）。
    *   **变分强化学习（RFT）**：采用子轨迹平衡（Sub-TB）目标，提供密集的中间监督，允许模型探索多样化的感知行为。
    *   **多维奖励函数**：
        *   **质量奖励（Quality）**：通过对比学习（正向：RoI 内特征；负向：RoI 外特征）确保标题与视觉区域严格对应。
        *   **效用奖励（Efficacy）**：衡量感知流对得出正确答案的信息增益。
    *   **邻域几何塑造（Vicinal Geometric Shaping）**：不强制与专家先验死板对齐，而是在专家先验的邻域内进行约束，既保证视觉可靠性又允许探索。

### 3. 实验设计
*   **数据集与基准测试**：共使用了 **15 个基准测试**，涵盖：
    *   **通用 VQA**：MMBench, MME-RealWorld-Lite, HallusionBench, AI2D, ChartQA, MathVision 等。
    *   **细粒度视觉理解**：V* Bench, HR-Bench (4K/8K), TreeBench, ScreenSpot (GUI 定位) 等。
*   **对比方法**：
    *   **通用模型**：GPT-4o, Qwen2.5-VL, InternVL3, Qwen3-VL 等。
    *   **视觉落地推理模型**：TreeVGR, Pixel-Reasoner, DeepEyes, Thyme 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 张 NVIDIA H200 GPU**。
*   **训练细节**：
    *   **冷启动阶段（SFT）**：在 45k 样本上训练 3 个 epoch。
    *   **强化学习阶段（RFT）**：在 42k 样本上训练 5 个 epoch。
    *   **推理框架**：基于 vLLM 和 TRL 框架构建。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的跨基准测试（15 个），并针对不同参数规模（4B, 8B, 32B）的 Qwen3-VL 变体进行了验证。
*   **消融实验**：详细探讨了训练策略（SFT vs RFT）、奖励设计（质量 vs 效用）、几何塑造参数（半径 $\epsilon$ 和强度 $\lambda$）的影响。
*   **公平性**：所有对比均在相同硬件和推理框架下进行，并严格执行了数据去重，确保无数据泄露。实验设计非常充分且客观。

### 6. 主要结论与发现
*   **性能突破**：PFlowNet 在 V* Bench (90.6%) 和 MME-RealWorld-lite (67.0%) 上刷新了 SOTA 记录。
*   **推理效用**：证明了感知流不仅是定位工具，更是关键的语义锚点，能显著引导模型的推理轨迹。
*   **测试时缩放（Test-time Scaling）**：相比于传统的 RLVR 方法，PFlowNet 在增加采样预算（Pass@k）时表现出更强的性能增长潜力，克服了模式崩溃问题。

### 7. 优点与亮点
*   **解耦设计**：通过感知与推理的解耦，提高了模型的可解释性。
*   **变分框架**：引入变分推理解决了专家先验偏见问题，使模型能自主发现更有利于推理的视觉证据。
*   **效率平衡**：相比于需要多轮工具调用的 Agent 框架，PFlowNet 的结构化感知流在保持高性能的同时，显著降低了推理延迟和上下文长度。
*   **理论保证**：提供了关于全变分（TV）距离界的理论证明，确保了方法的严谨性。

### 8. 不足与局限
*   **缺乏自适应性**：目前模型对所有问题都采用固定的感知流格式，对于简单问题可能会引入不必要的计算开销。
*   **计数任务挑战**：在极细粒度的计数任务中，模型可能会因为倾向于合并相邻区域而产生误差。
*   **规划状态监督不足**：规划状态（Planning State）目前缺乏直接的显式监督，主要依赖强化学习的被动优化，在处理分布外（OOD）场景时可能失效。

（完）
