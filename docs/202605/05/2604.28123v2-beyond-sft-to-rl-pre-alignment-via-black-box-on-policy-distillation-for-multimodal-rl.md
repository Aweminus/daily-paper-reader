---
title: "Beyond SFT-to-RL: Pre-alignment via Black-Box On-Policy Distillation for Multimodal RL"
title_zh: 超越 SFT 到 RL：通过黑盒同策略蒸馏实现多模态强化学习的预对齐
authors: "Sudong Wang, Weiquan Huang, Xiaomin Yu, Zuhao Yang, Hehai Lin, Keming Wu, Chaojun Xiao, Chen Chen, Wenxuan Wang, Beier Zhu, Yunjian Zhang, Chengwei Qin"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28123v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 强化学习中多模态推理的分布对齐
tldr: 针对多模态大模型在SFT到RL阶段存在的分布偏移问题，本文提出PRISM框架。通过在SFT和RLVR之间引入预对齐阶段，利用黑盒在线策略蒸馏（OPD）和混合专家（MoE）判别器，分别纠正感知和推理偏差。结合高质量的Gemini 3 Flash数据，PRISM显著提升了模型在多模态推理任务中的表现，为多模态强化学习提供了更稳健的初始化。
selection_source: fresh_fetch
motivation: 传统的SFT到RL流程在多模态场景下会产生严重的分布偏移，导致感知和推理错误在后续强化学习中不断累积。
method: 提出PRISM框架，通过黑盒在线策略蒸馏和包含感知与推理专家的MoE判别器，在RL前进行显式的分布对齐。
result: 在Qwen3-VL模型上，PRISM使4B和8B版本的平均准确率分别比SFT-to-RLVR基线提升了4.4和6.0个百分点。
conclusion: 预对齐阶段能有效缓解多模态模型的分布漂移，为下游强化学习提供更高质量的策略起点。
---

## 摘要
大型多模态模型（LMMs）的标准后训练方案通常是在精选的演示数据上进行有监督微调（SFT），随后进行带有可验证奖励的强化学习（RLVR）。然而，SFT 会引入分布偏移，既无法保留模型的原始能力，也无法忠实地匹配监督分布。这一问题在多模态推理中被进一步放大，其中感知错误和推理失败遵循不同的偏移模式，并在随后的强化学习过程中复合叠加。我们提出了 PRISM，这是一个三阶段流水线，通过在 SFT 和 RLVR 之间插入一个显式的分布对齐阶段来缓解这种偏移。基于同策略蒸馏（OPD）的原理，PRISM 将对齐建模为策略与具有专门感知和推理专家的混合专家（MoE）判别器之间的黑盒响应级对抗博弈，在无需访问教师模型 Logits 的情况下，提供解耦的纠正信号，引导策略向监督分布靠拢。虽然 126 万条公开演示数据足以进行广泛的 SFT 初始化，但分布对齐需要更高保真度的监督；因此，我们从 Gemini 3 Flash 中精选了 11.3 万条额外的演示数据，其特点是在最难的未解决问题上具有密集的视觉定位和分步推理。在 Qwen3-VL 上的实验表明，PRISM 在多种强化学习算法（GRPO、DAPO、GSPO）和多样化的多模态基准测试中一致地提升了下游 RLVR 的性能，在 4B 和 8B 模型上，平均准确率分别比 SFT-to-RLVR 基线提高了 4.4 和 6.0 个百分点。我们的代码、数据和模型权重已在 https://github.com/XIAO4579/PRISM 公开。

## Abstract
The standard post-training recipe for large multimodal models (LMMs) applies supervised fine-tuning (SFT) on curated demonstrations followed by reinforcement learning with verifiable rewards (RLVR). However, SFT introduces distributional drift that neither preserves the model's original capabilities nor faithfully matches the supervision distribution. This problem is further amplified in multimodal reasoning, where perception errors and reasoning failures follow distinct drift patterns that compound during subsequent RL. We introduce PRISM, a three-stage pipeline that mitigates this drift by inserting an explicit distribution-alignment stage between SFT and RLVR. Building on the principle of on-policy distillation (OPD), PRISM casts alignment as a black-box, response-level adversarial game between the policy and a Mixture-of-Experts (MoE) discriminator with dedicated perception and reasoning experts, providing disentangled corrective signals that steer the policy toward the supervision distribution without requiring access to teacher logits. While 1.26M public demonstrations suffice for broad SFT initialization, distribution alignment demands higher-fidelity supervision; we therefore curate 113K additional demonstrations from Gemini 3 Flash, featuring dense visual grounding and step-by-step reasoning on the hardest unsolved problems. Experiments on Qwen3-VL show that PRISM consistently improves downstream RLVR performance across multiple RL algorithms (GRPO, DAPO, GSPO) and diverse multimodal benchmarks, improving average accuracy by +4.4 and +6.0 points over the SFT-to-RLVR baseline on 4B and 8B, respectively. Our code, data, and model checkpoints are publicly available at https://github.com/XIAO4579/PRISM.

---

## 论文详细总结（自动生成）

### 论文总结：PRISM —— 通过黑盒同策略蒸馏实现多模态强化学习的预对齐

#### 1. 核心问题与整体含义
*   **研究背景**：大型多模态模型（LMM）的后训练通常遵循“有监督微调（SFT）→ 强化学习（RLVR）”的路径。
*   **核心痛点**：SFT 阶段会引入**分布漂移（Distributional Drift）**。模型在模仿外部演示数据时，往往只学会了表面模式（如格式），却偏离了其原始的分布优势。在多模态推理中，这种漂移表现为“感知错误”与“推理失败”的复合叠加，严重限制了后续强化学习的效果。
*   **研究动机**：探索如何在 SFT 和 RL 之间插入一个显式的“预对齐”阶段，修复分布偏差，为强化学习提供一个更高质量、更稳健的策略起点。

#### 2. 方法论
PRISM 提出了一个三阶段流水线，其核心是**黑盒对抗同策略蒸馏（Adversarial On-Policy Distillation）**：
*   **第一阶段：冷启动 SFT**。在 1.37M 高质量数据（含 113K 精选自 Gemini 3 Flash 的复杂推理数据）上进行微调，建立初步能力。
*   **第二阶段：分布对齐（核心）**。
    *   **MoE 判别器**：构建一个混合专家判别器，包含**感知专家（Perception Expert）**和**推理专家（Reasoning Expert）**。前者负责检查视觉定位的准确性，后者负责检查逻辑推导的一致性。
    *   **对抗博弈**：判别器学习区分“监督池中的参考响应”与“策略模型生成的响应”；策略模型则通过 GRPO 算法进行优化，目标是生成让判别器给出高分的响应。
    *   **黑盒特性**：该方法不需要访问教师模型的 Logits（概率分布），仅通过响应文本进行对齐，具有极强的通用性。
*   **第三阶段：可验证奖励强化学习（RLVR）**。在对齐后的模型基础上，利用确定性的规则（如答案正确性）进行最终的性能压榨。

#### 3. 实验设计
*   **模型基础**：选用 Qwen3-VL-4B 和 Qwen3-VL-8B 作为基座模型。
*   **数据集/Benchmark**：
    *   **数学推理**：MathVista, MathVerse, MathVision, WeMath。
    *   **通用理解**：MMMU, MMMU-Pro, HallusionBench。
*   **对比方法**：
    *   基准模型（Instruct）。
    *   标准 SFT 后的模型。
    *   传统的 SFT → RLVR 流程（涵盖 GRPO, DAPO, GSPO 三种主流 RL 算法）。
    *   PRISM 增强后的三阶段流程。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA H100-SXM5-80GB** GPU。
*   **训练时长/配置**：
    *   SFT 阶段：1 epoch。
    *   对齐阶段：500 steps。
    *   RLVR 阶段：1500 steps。
    *   软件框架：使用 LlamaFactory 进行 SFT，使用 veRL + vLLM 进行对齐和 RL 训练。

#### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同参数规模（4B/8B）的模型上进行了验证，并适配了三种不同的 RL 算法，证明了方法的普适性。
*   **消融实验**：非常充分。作者对比了“单模型判别器 vs MoE 判别器”、“多模态判别器 vs 纯文本判别器”，并验证了 SFT 数据规模对最终结果的影响。
*   **客观性**：通过训练动力学曲线（Reward Gap）和结构化代理指标（如推理步数分布、视觉描述项分布）多维度证明了分布对齐的有效性，而非简单的过拟合。

#### 6. 主要结论与发现
*   **性能提升显著**：PRISM + GRPO 在 4B 和 8B 模型上分别比传统的 SFT → RLVR 基线提升了 **4.4** 和 **6.0** 个平均分。
*   **对齐阶段的本质**：对齐阶段本身并不显著提升答案准确率，但它通过修复分布，为 RL 阶段提供了极佳的初始化，从而大幅提升了 RL 的上限。
*   **强模型更受益**：实验发现 8B 模型受 SFT 漂移的影响比 4B 更严重，而 PRISM 对强模型的修复效果也更明显。
*   **Token 效率**：PRISM 训练出的模型在推理时更加简洁高效，在获得更高准确率的同时，生成的 Token 数量反而更少。

#### 7. 优点
*   **解耦反馈信号**：通过 MoE 判别器将感知和推理反馈分开，解决了多模态任务中错误信号混杂的问题。
*   **黑盒对齐**：不依赖教师模型 Logits，使得利用闭源强模型（如 Gemini/GPT）进行蒸馏变得更加可行。
*   **高质量数据贡献**：开源了 113K 包含密集视觉定位和分步推理的高质量数据集。

#### 8. 不足与局限
*   **计算成本增加**：引入中间对齐阶段和 MoE 判别器增加了训练的时间和显存开销。
*   **结构化依赖**：目前的方法依赖于响应中显式的 XML 标签（caption/think/answer）来拆分专家反馈，对于不遵循此格式的任务可能需要重新设计。
*   **评估指标局限**：对于“分布对齐”的程度，主要通过响应结构等间接指标观察，缺乏更直接的数学度量手段。

（完）
