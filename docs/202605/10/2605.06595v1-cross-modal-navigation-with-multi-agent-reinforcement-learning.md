---
title: Cross-Modal Navigation with Multi-Agent Reinforcement Learning
title_zh: 基于多智能体强化学习的跨模态导航
authors: "Shuo Liu, Xinzichen Li, Christopher Amato"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06595v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于导航的多模态强化学习
tldr: 针对具身导航中多模态数据对齐难及单体模型表示复杂的问题，本文提出CRONA框架。该框架利用多智能体强化学习（MARL），让轻量化的模态专用智能体通过辅助信念和集中式评论家进行协作。实验证明，该方法在视听导航任务中显著优于单智能体基准，揭示了异构协作在复杂环境中的高效性。
selection_source: fresh_fetch
motivation: 传统的单体多模态导航模型面临高质量数据获取难、对齐复杂以及策略空间过大导致的训练挑战。
method: 提出CRONA框架，利用轻量化模态专用智能体进行协作，并结合辅助信念与全局状态的集中式评论家优化决策。
result: 实验表明多智能体方法在视听导航任务的性能和效率上均优于单智能体基准，尤其在复杂环境中表现突出。
conclusion: 异构模态智能体间的协作是实现高效跨模态导航的关键，且复杂环境导航需要更强的多模态感知与模型容量。
---

## 摘要
鲁棒的具身导航依赖于互补的感官线索。然而，在实践中往往难以获得高质量且良好对齐的多模态数据。训练单一模型也具有挑战性，因为丰富的多模态输入会产生复杂的表示，并显著扩大策略空间。轻量级模态专用智能体之间的跨模态协作提供了一种可扩展的范式。它能够实现灵活部署和并行执行，同时保留每种模态的优势。在本文中，我们提出了 CRONA，一个用于跨模态导航的多智能体强化学习（MARL）框架。CRONA 通过利用与控制相关的辅助信念和具有全局状态的集中式多模态评论家（critic）来改进协作。在视觉-听觉导航任务上的实验表明，多智能体方法相比单智能体基准显著提高了性能和效率。我们发现，在显著线索下的短程导航中，有限模态的同质协作已足够；具有互补模态的智能体之间的异质协作通常是高效且有效的；而在大型复杂环境中的导航则需要更丰富的多模态感知和更大的模型容量。

## Abstract
Robust embodied navigation relies on complementary sensory cues. However, high-quality and well-aligned multi-modal data is often difficult to obtain in practice. Training a monolithic model is also challenging as rich multi-modal inputs induce complex representations and substantially enlarge the policy space. Cross-modal collaboration among lightweight modality-specialized agents offers a scalable paradigm. It enables flexible deployment and parallel execution, while preserving the strength of each modality. In this paper, we propose \textbf{CRONA}, a Multi-Agent Reinforcement Learning (MARL) framework for \textbf{Cro}ss-Modal \textbf{Na}vigation. CRONA improves collaboration by leveraging control-relevant auxiliary beliefs and a centralized multi-modal critic with global state. Experiments on visual-acoustic navigation tasks show that multi-agent methods significantly improve performance and efficiency over single-agent baselines. We find that homogeneous collaboration with limited modalities is sufficient for short-range navigation under salient cues; heterogeneous collaboration among agents with complementary modalities is generally efficient and effective; and navigation in large, complex environments requires both richer multi-modal perception and increased model capacity.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **CRONA** 的多智能体强化学习框架，旨在解决具身导航（Embodied Navigation）中多模态数据对齐难、单体模型复杂度高的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的具身导航通常采用单体（Monolithic）模型处理多模态输入（如视觉、听觉、语言）。然而，不同模态的数据在维度、噪声水平和时间结构上差异巨大，导致模型难以对齐信号，且容易出现“模态竞争”现象（即强模态主导更新，弱模态被忽略）。
*   **核心问题**：如何利用轻量级的模态专用智能体（如一个负责看，一个负责听）通过协作来降低学习难度，提高导航的鲁棒性和效率？
*   **整体含义**：论文将多模态导航任务转化为多智能体协作任务，通过去中心化执行和中心化训练，实现了比单体模型更高效的跨模态感知。

### 2. 方法论：核心思想与关键技术
CRONA 采用 **CTDE（中心化训练，去中心化执行）** 范式，核心组件包括：
*   **辅助信念预测器（Auxiliary Belief Predictor）**：针对音频等高噪声信号，设计了一个预测器来估计目标的类别和相对位置。通过预测瞬时目标位置并进行指数移动平均（EMA）平滑，为策略提供控制相关的特征。
*   **基于注意力的历史编码器（Attention-Based History Encoder）**：使用 Transformer 块处理短时历史缓存（观察、动作、位姿），捕捉空间和时间上的依赖关系。
*   **带全局信息的中心化评论家（Centralized Critic with Global State）**：在训练阶段，Critic 可以访问全局状态（如所有智能体位姿、目标真实位置、任务完成状态），利用联合历史和辅助信念来估计状态价值函数 $V$，从而减少方差并加速收敛。
*   **优化目标**：结合了 PPO 的剪切代理目标、价值函数损失以及辅助信念预测的监督损失。

### 3. 实验设计
*   **数据集与场景**：基于 **Matterport3D** 真实室内场景，利用 Habitat 和 libsora 模拟视觉和音频。
*   **Benchmark 场景**：设计了 5 个难度递增的场景：
    1.  **Studio**（单房间，单一目标）
    2.  **Corridor**（长走廊，依赖音频定位）
    3.  **Apartment**（多房间，视觉显著目标）
    4.  **Ranch**（大型复杂布局，跨模态互补）
    5.  **Maze**（最大最复杂，多目标导航）
*   **对比方法（Baselines）**：
    *   **Single-Agent**：输入所有模态的单体大模型。
    *   **VLA-Collab**：两个视觉-语言智能体协作（同质）。
    *   **ALA-Collab**：两个音频-语言智能体协作（同质）。
    *   **AVLA-Collab**：两个全模态（音视语）智能体协作（同质）。
    *   **CRONA**：一个视觉智能体与一个音频智能体协作（异质/跨模态）。

### 4. 资源与算力
论文在附录 G 中明确列出了计算资源：
*   **硬件**：
    *   本地工作站：1x NVIDIA RTX 5090 (32GB VRAM), AMD Ryzen 9 9950X。
    *   云集群：2x NVIDIA A100-SXM4 (80GB VRAM each), AMD EPYC 7513。
*   **训练时长**（针对 500k 步训练）：
    *   简单场景（Studio）：约 8-10 小时。
    *   中等场景（Apartment/Ranch）：约 20-25 小时。
    *   复杂场景（Corridor/Maze）：约 30-48 小时。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同特性的场景上进行了测试，每组实验运行 5 次并取平均值（附带 90% 置信区间）。
*   **消融实验**：非常充分。涵盖了模型容量（Embedding Size）、视觉分辨率（4x4 到 32x32）、辅助信念组件（类别 vs 位置预测）以及中心化 Critic 中全局状态的作用。
*   **客观性**：通过设置相同的参数量和环境步数，确保了单智能体与多智能体、同质与异质协作之间的公平比较。

### 6. 主要结论与发现
*   **多智能体优于单智能体**：在所有场景下，协作导航的成功率和效率均显著高于单体模型。
*   **五种模态主导模式**：识别出不同场景下模态的作用不同（无明显主导、视觉主导、音频主导、跨模态互补、全模态主导）。
*   **跨模态协作的优势**：在目标具有清晰且模态特异性线索时（如 Ranch 场景），异质协作（CRONA）比全模态同质协作更有效且参数利用率更高。
*   **复杂环境需求**：在 Maze 等极端复杂场景下，全模态输入和更大的模型容量依然是必需的。

### 7. 优点（亮点）
*   **范式创新**：将复杂的单体多模态对齐问题简化为多个轻量级专才智能体的协作问题。
*   **鲁棒性强**：实验证明 CRONA 在低分辨率视觉输入下表现极其稳健，音频智能体能有效弥补视觉的不足。
*   **理论支撑**：附录中提供了关于状态增强 Critic 无偏估计的数学证明。
*   **实用性**：去中心化执行意味着智能体在部署时无需实时通信，降低了带宽需求。

### 8. 不足与局限
*   **模态种类有限**：目前仅验证了视觉和音频，尚未扩展到 LiDAR、触觉或雷达等更多模态。
*   **任务维度**：实验主要在 2D 导航平面进行，未充分探讨 3D 空间（如无人机飞行）中的跨模态协作。
*   **信念预测依赖**：辅助信念预测器需要目标的 Ground Truth 类别和位置进行训练，这在某些完全未知的环境中可能难以获得。
*   **通信缺失**：论文假设完全无通信，但在极复杂任务中，引入有限的通信机制可能会进一步提升性能。

（完）
