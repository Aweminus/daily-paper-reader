---
title: "What You Think is What You See: Driving Exploration in VLM Agents via Visual-Linguistic Curiosity"
title_zh: 所思即所见：通过视觉-语言好奇心驱动 VLM 智能体的探索
authors: "Haoxi Li, Qinglin Hou, Jianfei Ma, Jinxiang Lai, Tao Han, Sikai Bai, Jingcai Guo, Jie Zhang, Song Guo"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03782v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 具有好奇心的VLM智能体强化学习
tldr: 针对视觉语言模型（VLM）智能体在稀疏奖励环境下缺乏主动探索能力的问题，本文提出了GLANCE框架。该框架通过将智能体的语言世界模型与演化目标网络的视觉表示对齐，利用语言预测与视觉现实之间的差异作为内在好奇心信号，引导智能体探索未知区域。实验证明，这种“所思即所见”的对齐机制能显著提升智能体在复杂任务中的泛化与解决能力。
selection_source: fresh_fetch
motivation: 现有的VLM智能体主要依赖被动推理，在稀疏奖励任务中缺乏主动发现并探索未知环境的内在动力。
method: 提出GLANCE框架，利用语言预测与视觉表征之间的差异作为强化学习的内在好奇心奖励，驱动智能体主动探索。
result: 在多项智能体任务中的实验表明，GLANCE能有效增强智能体的探索效率，并显著提升其在复杂或稀疏奖励环境下的任务成功率。
conclusion: 将智能体的内部语言世界模型与外部视觉现实相对齐，是解决复杂智能体任务并实现稳健泛化的关键。
---

## 摘要
为了在部分可观测的视觉环境中导航，近期的 VLM 智能体越来越多地通过显式的思维链（CoT）推理将世界建模能力内化到其策略中，使其能够在行动前对未来进行心理模拟。然而，在稀疏奖励任务中，仅依靠对已访问状态的被动推理是不够的，因为它缺乏主动揭示实现鲁棒泛化所需的“已知未知”的认识论驱动力。我们提出疑问：VLM 智能体能否通过好奇心驱动的探索，主动寻找能够挑战并完善其内部世界模型的信号？在这项工作中，我们提出了 GLANCE，这是一个统一的框架，通过将智能体的语言世界模型锚定在不断演进的目标网络的稳定视觉表示中，从而架起了推理与探索之间的桥梁。关键在于，GLANCE 利用语言预测与视觉现实之间的差异作为强化学习中的内在好奇心信号，引导智能体主动探索其内部模型不确定的区域。在一系列智能体任务上的广泛实验证明了 GLANCE 的有效性，并表明将“智能体所思”与“智能体所见”对齐是解决复杂或稀疏智能体任务的关键。

## Abstract
To navigate partially observable visual environments, recent VLM agents increasingly internalize world modeling capabilities into their policies via explicit CoT reasoning, enabling them to mentally simulate futures before acting. However, relying solely on passive reasoning over visited states is insufficient for sparse-reward tasks, as it lacks the epistemic drive to actively uncover the ``known unknown'' required for robust generalization. We ask: Can VLM agents actively find signals that challenge and refine their internal world model through curiosity-driven exploration? In this work, we propose GLANCE, a unified framework that bridges reasoning and exploration by grounding the agent's linguistic world model into the stable visual representations of an evolving target network. Crucially, GLANCE leverages the discrepancy between linguistic prediction and visual reality as an intrinsic curiosity signal within reinforcement learning, steering the agent to actively explore areas where its internal model is uncertain. Extensive experiments across a series of agentic tasks show the effectiveness of GLANCE, and demonstrate that aligning ``what the agent thinks'' with ``what the agent sees'' is key to solving complex or sparse agentic tasks.

---

## 论文详细总结（自动生成）

这篇论文提出了 **GLANCE** 框架，旨在通过“视觉-语言好奇心”驱动视觉语言模型（VLM）智能体在复杂环境中的主动探索。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：当前的 VLM 智能体（如基于强化学习的智能体）通常通过思维链（CoT）推理内化“世界模型”，以预测环境动态。
*   **核心问题**：现有的 VLM 智能体主要处于**被动利用（Passive Exploitation）**阶段。在奖励稀疏的任务中，智能体往往只能对已访问的状态进行推理，缺乏主动寻找“已知未知（Known Unknowns）”的动力。例如，智能体可能学会了完美描述一个“死胡同”，却不知道应该去探索另一条路径。
*   **核心含义**：论文提出“所思即所见（What You Think is What You See）”的原则，认为有效的探索应由智能体的**语言假设与视觉现实之间的不一致性**来驱动。

### 2. 方法论（GLANCE 框架）
GLANCE（Grounding Linguistic Alignment for Curiosity Exploration）通过将语言推理与视觉表征对齐来构建内在动力。
*   **核心思想**：将智能体对未来的“语言预测”映射到视觉特征空间，并与实际观察到的“视觉现实”进行对比。两者的差异（预测误差）即为**内在好奇心奖励**。
*   **关键技术细节**：
    1.  **在线 VLM 智能体**：包含视觉编码器和 LLM 主干。在推理过程中，模型生成 `<Pred>` 标记，其隐藏状态 $h_{t+1}$ 被视为对未来的语义压缩。
    2.  **动量目标网络（Momentum Target Network）**：一个缓慢更新的视觉编码器，用于提供稳定的视觉表征 $y_{t+1}$ 作为回归目标。
    3.  **跨模态对齐投影器（Projector）**：一个轻量级的 MLP，将语言空间的 $h_{t+1}$ 映射到视觉空间。
    4.  **内在奖励公式**：使用预测值与目标值之间的均方误差（MSE）作为内在奖励 $r_i = \beta \cdot L_{explore}$。
    5.  **课程探索与复原（Curriculum Exploration with Rejuvenation）**：为了防止“好奇心枯竭”（即投影器过快拟合导致奖励消失），当预测误差趋于平稳时，系统会随机重新初始化投影器参数，强制智能体重新审视环境中的细微差异。

### 3. 实验设计
*   **实验场景（5 类任务）**：
    *   **认知网格谜题**：Sokoban（推箱子）、FrozenLake。
    *   **具身 3D 控制**：Navigation（室内导航）、PrimitiveSkill（机械臂操作）。
    *   **生成式推理**：SVG Reconstruction（根据图像生成 SVG 代码）。
*   **Benchmark 与基准方法**：
    *   **主干模型**：Qwen2.5-VL-3B。
    *   **对比方法**：VAGEN（目前最强的 VLM-RL 基准）、Turn-level PPO、GRPO、Vanilla PPO。
    *   **零样本对比**：GPT-4o、Claude 3.7/4.5 Sonnet、Gemini 2.5 Pro 等闭源模型。

### 4. 资源与算力
*   **文中提及的细节**：
    *   训练批次大小（Batch Size）为 128。
    *   学习率：RL Actor 为 $1 \times 10^{-6}$，Critic 为 $1 \times 10^{-5}$。
    *   **未明确说明的信息**：论文未明确列出具体的 GPU 型号（如 A100 或 H100）以及总训练时长。不过，论文在“进一步分析”中对比了每步训练的时间开销，指出 GLANCE 引入的额外计算负担非常小。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个截然不同的领域进行了测试，涵盖了 2D、3D 及代码生成任务。
*   **消融实验**：针对动量更新（EMA）、投影器复原机制、内在奖励权重 $\beta$ 的敏感性、以及是否存在外部推理监督（LLM-as-a-judge）等多个维度做了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅对比了开源 RL 框架，还与当前最顶尖的闭源模型进行了零样本性能对比。通过可视化“预测误差-成功率”的锯齿状曲线，有力地证明了课程复原机制的有效性。

### 6. 主要结论与发现
*   **性能提升**：GLANCE 在所有任务上均优于仅依赖外部奖励的基准方法。在稀疏奖励的 Sokoban 任务中，成功率从 0.79 提升至 0.85。
*   **好奇心驱动的价值**：即使在没有外部推理奖励（由 LLM 评判）的情况下，GLANCE 依然能通过内在好奇心维持较高的推理质量。
*   **解决停滞**：通过定期重置投影器，智能体能够打破性能瓶颈，持续在环境中发现新的“未知”细节。

### 7. 优点（亮点）
*   **跨模态统一性**：巧妙地将 VLM 的 CoT 推理能力转化为探索动力，实现了“想”与“看”的闭环。
*   **轻量化设计**：不需要额外的复杂世界模型插件，直接利用 VLM 内部状态，计算开销低。
*   **课程机制**：提出的“投影器复原”有效解决了强化学习中常见的内在奖励消失问题。

### 8. 不足与局限
*   **主干冻结风险**：为了防止语言漂移和降低算力需求，实验中冻结了 LLM 主干。这可能限制了智能体根据视觉反馈彻底重塑其逻辑推理规则的能力。
*   **投影器结构简单**：目前仅使用 2 层 MLP，对于极其复杂的时空关联任务，可能需要更强大的跨模态注意力机制。
*   **环境泛化性**：虽然任务种类多，但实验主要集中在模拟器环境，尚未在真实世界的物理机器人上进行验证。

（完）
