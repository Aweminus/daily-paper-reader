---
title: "FineState-Bench: Benchmarking State-Conditioned Grounding for Fine-grained GUI State Setting"
title_zh: FineState-Bench：针对细粒度 GUI 状态设置的状态条件定位基准测试
authors: "Fengxian Ji, Jingpu Yang, Zirui Song, Yuanxi Wang, Zhexuan Cui, Yuke Li, Qian Jiang, Xiuying Chen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27974v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于视觉语言模型的GUI智能体
tldr: 针对大型视觉语言模型在细粒度GUI状态设置中表现不佳的问题，本文提出了FineState-Bench基准测试。该基准包含跨桌面、Web和移动端的2209个实例，涵盖23种UI组件，旨在评估模型将指令映射到特定UI控件并达到精确目标状态的能力。研究还引入了四阶段诊断指标FineState-Metrics和视觉诊断助手VDA，揭示了当前模型在视觉定位和状态理解方面的显著不足。
selection_source: fresh_fetch
motivation: 现有的GUI评估缺乏对细粒度状态定义的精确覆盖，且过度依赖最终任务成功率，难以诊断模型失败的具体原因。
method: 提出了包含2209个跨平台实例的FineState-Bench，并配套四阶段诊断指标及可生成定位提示的视觉诊断助手VDA。
result: "实验显示当前模型在精确目标状态设置上的成功率较低（平均仅22.8%），但通过VDA提供定位提示可显著提升性能。"
conclusion: 视觉定位能力的提升是优化细粒度GUI交互的关键，目前LVLM在实现可靠的状态条件交互方面仍有巨大提升空间。
---

## 摘要
尽管大型视觉语言模型（LVLMs）取得了快速进展，但细粒度的、基于状态条件的 GUI 交互仍然具有挑战性。当前的评估覆盖范围有限，目标状态定义不精确，且过度依赖最终任务的成功，从而掩盖了智能体失败的具体位置和原因。为了填补这一空白，我们推出了 FineState-Bench，这是一个评估智能体能否将指令正确定位到预期的 UI 控件并达到精确目标状态的基准。FineState-Bench 包含跨桌面、Web 和移动平台的 2,209 个实例，涵盖 4 个交互系列和 23 种 UI 组件类型，每个实例都明确指定了用于细粒度状态设置的精确目标状态。我们进一步提出了 FineState-Metrics，这是一个包含阶段性成功率的四阶段诊断流水线：定位成功率 (SR@Loc)、交互成功率 (SR@Int)、定位时的精确状态成功率 (ES-SR@Loc) 和交互时的精确状态成功率 (ES-SR@Int)，以及一个即插即用的视觉诊断助手 (VDA)，它通过生成描述和边界框定位提示，通过有无提示的对照实验来诊断视觉定位的原因。在 FineState-Bench 上，精确目标状态的成功率仍然较低：ES-SR@Int 在 Web 上达到峰值 32.8%，跨平台平均值为 22.8%。借助 VDA 定位提示，Gemini-2.5-Flash 的 ES-SR@Int 提升了 14.9 个百分点，这表明改进视觉定位仍有巨大的提升空间，但整体准确率对于可靠的细粒度状态条件交互而言仍然不足。

## Abstract
Despite the rapid progress of large vision-language models (LVLMs), fine-grained, state-conditioned GUI interaction remains challenging. Current evaluations offer limited coverage, imprecise target-state definitions, and an overreliance on final-task success, obscuring where and why agents fail. To address this gap, we introduce \textbf{FineState-Bench}, a benchmark that evaluates whether an agent can correctly ground an instruction to the intended UI control and reach the exact target state. FineState-Bench comprises 2,209 instances across desktop, web, and mobile platforms, spanning four interaction families and 23 UI component types, with each instance explicitly specifying an exact target state for fine-grained state setting. We further propose \textit{FineState-Metrics}, a four-stage diagnostic pipeline with stage-wise success rates: Localization Success Rate (SR@Loc), Interaction Success Rate (SR@Int), Exact State Success Rate at Locate (ES-SR@Loc), and Exact State Success Rate at Interact (ES-SR@Int), and a plug-and-play \textit{Visual Diagnostic Assistant} (VDA) that generates a Description and a bounding-box Localization Hint to diagnose visual grounding reason via controlled w/ vs.\ w/o comparisons. On FineState-Bench, exact goal-state success remains low: ES-SR@Int peaks at 32.8\% on Web and 22.8\% on average across platforms. With VDA localization hints, Gemini-2.5-Flash gains +14.9 ES-SR@Int points, suggesting substantial headroom from improved visual grounding, yet overall accuracy is still insufficient for reliable fine-grained state-conditioned interaction \href{https://github.com/FengxianJi/FineState-Bench}{Github.}

---

## 论文详细总结（自动生成）

以下是对论文《FineState-Bench: Benchmarking State-Conditioned Grounding for Fine-grained GUI State Setting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于：**当前的 GUI 智能体在处理“细粒度、状态条件化”的交互任务时表现不佳，且缺乏有效的评估手段。**
*   **研究动机**：现有的基准测试（如 ScreenSpot, AITW）多关注端到端任务完成率或粗粒度的点击定位，忽略了如“将滑动条精确调至 77%”或“在日期选择器中选中特定日期”等需要精确状态控制的场景。
*   **痛点**：现有指标（如成功率）过于笼统，无法区分智能体失败是因为“没找到控件”、“点错了位置”还是“无法理解目标状态”，导致研究者难以定位性能瓶颈。

### 2. 方法论：核心思想与关键技术
论文提出了一个名为 **FineState** 的完整评估框架，包含数据集、指标体系和诊断工具：
*   **FineState-Bench 数据集**：
    *   涵盖桌面、Web、移动端三大平台，共 2,209 个实例。
    *   **双区域标注（Dual-region Annotation）**：为每个控件标注了“定位框”（Locate Box，覆盖整个组件）和“交互框”（Interact Box，触发状态改变的核心区域，如滑动条的滑块）。
    *   **精确目标状态**：每个任务都有明确的数值或类别目标状态（如 `s_goal`）。
*   **FineState-Metrics 诊断指标**：
    *   **SR@Loc**：定位成功率，看预测点是否落在定位框内。
    *   **SR@Int**：交互成功率，看预测点是否落在交互核心区域。
    *   **ES-SR@Loc/Int**：精确状态成功率，在满足定位/交互的前提下，最终状态是否与目标完全一致。
*   **视觉诊断助手 (VDA)**：
    *   一个即插即用的工具（基于 GPT-4o），为智能体提供“结构化描述”和“定位提示（Bounding Box）”。通过对比“有提示”和“无提示”的表现，量化视觉定位误差对失败的影响。

### 3. 实验设计
*   **场景与平台**：Desktop (810), Web (701), Mobile (698)。
*   **交互分类**：分为数值调节、开关/选项选择、特定数据选择（日期/颜色）、内容组织（拖拽/缩放）四大类，共 23 种 UI 组件。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude-3.5-Sonnet, Gemini-2.5-Flash。
    *   **开源 GUI 专用模型**：OS-Atlas-7B, CogAgent-9B, UGround-7B, Jedi-7B, ShowUI-2B。
*   **协议**：单步、基于点的交互协议，评估智能体预测的点击/释放坐标。

### 4. 资源与算力
*   **训练算力**：论文主要侧重于**基准测试和评估**，而非从头训练模型，因此**未明确说明训练所需的 GPU 算力、型号或时长**。
*   **推理成本**：实验涉及对 8 个模型在 2,209 个实例上的推理，并使用了 GPT-4o 作为 VDA 的后端，这涉及到显著的 API 调用成本和推理时间。

### 5. 实验数量与充分性
*   **实验规模**：进行了 2,209 个实例的全面测试，涵盖了 23 种不同的 UI 组件，样本量充足。
*   **消融实验**：针对 VDA 的不同组件（仅描述、仅定位提示、两者结合）进行了消融研究，验证了定位提示是性能提升的主因。
*   **客观性与公平性**：
    *   采用了双人标注（Double-annotation）研究，验证了标注的可靠性（目标状态一致性达 97.5%）。
    *   所有模型均在相同的单步协议下测试，确保了对比的公平性。
    *   通过 VDA 这种受控实验方法，客观地分离了“视觉感知”与“交互决策”的错误。

### 6. 主要结论与发现
*   **性能低下**：即使是表现最好的模型（UGround-7B），平均精确状态成功率（ES-SR@Int）也仅为 22.8%，说明细粒度控制对当前 LVLM 仍是巨大挑战。
*   **瓶颈定位**：实验发现 **SR@Loc（找到控件）显著高于 SR@Int（点对位置）**。这表明智能体通常能找到正确的控件，但无法精确点击到触发状态改变的核心区域。
*   **视觉定位是关键**：引入 VDA 定位提示后，Gemini-2.5-Flash 的成功率提升了 14.9%，证明改进视觉定位精度是提升 GUI 智能体能力的最直接路径。
*   **平台差异**：模型在 Web 端的表现普遍优于桌面端，反映了不同平台 UI 复杂度的差异。

### 7. 优点（亮点）
*   **诊断性极强**：通过四阶段指标和双框标注，第一次清晰地揭示了 GUI 交互失败的具体环节（是没找对地方，还是点得不够准）。
*   **标注精细**：不仅有点击点，还有目标状态的精确定义，解决了以往基准测试中“模糊成功”的问题。
*   **跨平台覆盖**：同时覆盖移动、Web 和桌面，具有很强的通用性。

### 8. 不足与局限
*   **静态局限性**：该基准测试基于静态截图（Single-step），无法评估智能体在动态交互中的多步推理或自我纠错能力。
*   **VDA 依赖性**：诊断工具 VDA 依赖于强大的闭源模型（GPT-4o），对于完全开源环境下的诊断可能存在成本或访问限制。
*   **应用限制**：未涵盖所有专业软件或无障碍接口（Accessibility）的变体，可能存在一定的分布偏差。

（完）
