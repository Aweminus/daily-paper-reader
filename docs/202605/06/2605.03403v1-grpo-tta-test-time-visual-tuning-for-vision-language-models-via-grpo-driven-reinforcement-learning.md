---
title: "GRPO-TTA: Test-Time Visual Tuning for Vision-Language Models via GRPO-Driven Reinforcement Learning"
title_zh: GRPO-TTA：通过 GRPO 驱动的强化学习实现视觉语言模型的测试时视觉微调
authors: "Yujun Li, Hongyuan Zhang, Yuan Yuan"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03403v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于GRPO驱动的强化学习用于视觉语言模型微调
tldr: 本研究探讨了组相对策略优化（GRPO）在视觉语言模型测试时自适应（TTA）中的潜力，提出了GRPO-TTA方法。该方法将类别预测重构为组策略优化问题，通过从CLIP相似度分布中采样Top-K候选类别构建输出组，在无标注情况下实现概率驱动的优化。结合专门设计的对齐和分散奖励函数，GRPO-TTA在多个基准测试中显著优于现有TTA方法，特别是在处理自然分布偏移时表现出色。
selection_source: fresh_fetch
motivation: 旨在探索在大型语言模型中表现优异的GRPO算法是否能有效提升视觉语言模型在测试阶段的自适应能力。
method: 将类别预测重构为组策略优化问题，通过采样Top-K候选类别构建输出组，并利用对齐和分散奖励函数指导视觉编码器的微调。
result: 实验证明GRPO-TTA在多个基准测试上均优于现有方法，尤其在应对自然分布偏移时展现出更显著的性能提升。
conclusion: GRPO-TTA证明了强化学习在无监督测试时自适应中的有效性，为增强视觉语言模型的鲁棒性提供了新思路。
---

## 摘要
组相对策略优化（GRPO）近期在大语言模型和视觉语言模型的后训练中表现出强大的性能。这引发了一个问题：GRPO 是否也能显著提升视觉语言模型的测试时自适应（TTA）能力。在本文中，我们提出了用于测试时自适应的组相对策略优化（GRPO-TTA），通过将特定类别的提示预测重新表述为组级策略优化问题，将 GRPO 适配到 TTA 场景中。具体而言，我们通过从 CLIP 相似度分布中采样前 K 个候选类别来构建输出组，从而在无需访问真实标签的情况下实现概率驱动的优化。此外，我们设计了专门针对测试时自适应的奖励函数，包括对齐奖励和分散奖励，以引导有效的视觉编码器微调。在多个基准测试上的广泛实验表明，GRPO-TTA 一致优于现有的测试时自适应方法，在自然分布偏移下性能提升尤为显著。

## Abstract
Group Relative Policy Optimization (GRPO) has recently shown strong performance in post-training large language models and vision-language models. It raises a question of whether the GRPO also significantly promotes the test-time adaptation (TTA) of vision language models. In this paper, we propose Group Relative Policy Optimization for Test-Time Adaptation (GRPO-TTA), which adapts GRPO to the TTA setting by reformulating class-specific prompt prediction as a group-wise policy optimization problem. Specifically, we construct output groups by sampling top-K class candidates from CLIP similarity distributions, enabling probability-driven optimization without access to ground-truth labels. Moreover, we design reward functions tailored to test-time adaptation, including alignment rewards and dispersion rewards, to guide effective visual encoder tuning. Extensive experiments across diverse benchmarks demonstrate that GRPO-TTA consistently outperforms existing test-time adaptation methods, with notably larger performance gains under natural distribution shifts.

---

## 论文详细总结（自动生成）

这篇论文探讨了如何将大语言模型（LLM）中表现优异的**组相对策略优化（GRPO）**算法引入到视觉语言模型（VLM）的**测试时自适应（TTA）**任务中，旨在提升模型在面对分布偏移（Distribution Shift）时的鲁棒性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（如 CLIP）在面对下游任务的分布偏移（如自然环境变化、跨域数据）时，零样本（Zero-shot）性能会显著下降。
*   **研究动机**：
    *   现有的 TTA 方法（如 TPT、DiffTPT）通常依赖于熵最小化或一致性约束，容易陷入预测崩溃或计算开销过大。
    *   GRPO 算法在 DeepSeek-R1 等模型中证明了其在无需复杂价值模型的情况下进行策略优化的能力。
    *   **科学假设**：能否将 GRPO 这种“组内相对比较”的强化学习机制，转化为一种无需标签的测试时在线优化策略，以更有效地微调视觉编码器？

### 2. 核心方法论
论文提出了 **GRPO-TTA** 框架，其核心思想是将类别预测任务重构为一个强化学习策略优化问题：
*   **输出组构建（Top-K Sampling）**：由于 TTA 阶段没有真实标签，作者通过 CLIP 原始的相似度分布，采样概率最高的 Top-K 个类别作为“输出组”。这模拟了 LLM 中的多样本生成过程。
*   **策略定义**：将模型对这 K 个类别的预测概率分布视为策略 $\pi_\theta$。
*   **双重奖励函数设计**：
    *   **对齐奖励（Alignment Reward）**：利用 CLIPScore（图像与文本特征的余弦相似度）作为信号，鼓励模型增强图像与语义相关文本的对齐。
    *   **分散奖励（Dispersion Reward）**：计算相似度得分的离散程度，防止模型对所有候选类别给出均等评分（预测崩溃）或过度自信。
*   **优化流程**：对单个测试样本进行多次数据增强，筛选出低熵（高置信度）的视图，计算组内相对优势（Advantage），通过 GRPO 的代理损失函数更新视觉编码器的投影层（Projector）。

### 3. 实验设计
*   **数据集与场景**：
    *   **跨域泛化（10个数据集）**：包括 Aircraft, Caltech101, StanfordCars, DTD, EuroSAT, Flowers102, Food101, Pets, SUN397, UCF101。
    *   **自然分布偏移（ImageNet 及其变体）**：ImageNet-A, ImageNet-V2, ImageNet-R, ImageNet-Sketch。
*   **Benchmark 与对比方法**：
    *   基准：Zero-shot CLIP, CoOp, CoCoOp。
    *   TTA 竞争对手：TPT, DiffTPT, RLCF（另一种基于强化学习的方法）, CLIPTTA, DOTA, DPE 等。
*   **实验设置**：使用 ViT-B/16 作为骨干网络，采用单样本在线处理模式（Batch size = 1）。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **耗时**：
    *   在 ImageNet 上完成自适应约需 **3小时40分钟**。
    *   相比之下，TPT 需 >10小时，DiffTPT 需 >20小时，而高效方法 DOTA 仅需 32分钟。
    *   结论：GRPO-TTA 在精度和速度之间取得了较好的平衡，属于中等计算开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 15 个不同的识别基准测试，实验量较大。
*   **消融实验**：
    *   验证了对齐奖励与分散奖励的各自贡献。
    *   探讨了采样因子 $K$（Top-K 数量）、分散奖励权重 $\lambda$ 以及更新步数对性能的影响。
*   **评价**：实验设计较为全面，对比了从早期的提示微调到最新的视觉微调等多种路径，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：GRPO-TTA 在 15 个基准测试中几乎全面超越现有方法。在自然分布偏移下，平均精度提升了 1.21% ~ 11.19%；在跨域任务中平均提升 0.65% ~ 5.53%。
*   **强化学习的有效性**：证明了 GRPO 的组相对优势估计比传统的 RLHF（如 PPO 变体）在 TTA 场景下更稳定且高效。
*   **视觉微调潜力**：仅通过微调视觉编码器的投影层，即可获得比微调文本提示（Prompt Tuning）更好的泛化效果。

### 7. 亮点
*   **首创性**：首次将 GRPO 算法成功适配到视觉语言模型的测试时自适应领域。
*   **无需标签的奖励机制**：巧妙地利用 CLIP 相似度和分布离散度构建奖励，解决了 TTA 缺乏监督信号的核心痛点。
*   **鲁棒性强**：在极端分布偏移（如 ImageNet-A）下表现出极强的纠偏能力。

### 8. 不足与局限
*   **实时性挑战**：虽然比 TPT 快，但 3.6 小时的 ImageNet 处理时间对于某些实时推理场景（如自动驾驶、实时监控）仍然过慢。
*   **架构依赖**：目前主要针对 ViT 架构进行验证，对于 CNN 架构或其他多模态架构的普适性有待进一步探讨。
*   **超参数敏感性**：Top-K 的选择（实验中 $K=4$ 最佳）和分散奖励权重 $\lambda$ 对结果有一定影响，不同数据集可能需要微调。

（完）
