---
title: "PRTS: A Primitive Reasoning and Tasking System via Contrastive Representations"
title_zh: PRTS：一种通过对比表示实现的元推理与任务系统
authors: "Yang Zhang, Jiangyuan Zhao, Chenyou Fan, Fangzheng Yan, Tian Li, Haitong Tang, Sen Fu, Xuan'er Wu, Qizhen Weng, Weinan Zhang, Xiu Li, Chi Zhang, Chenjia Bai, Xuelong Li"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27472v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过目标条件强化学习构建的VLA基础模型
tldr: PRTS是一个基于对比表征的视觉-语言-动作（VLA）基础模型。针对现有模型忽视任务进度和目标达成性的问题，它将预训练重新表述为目标条件强化学习。通过对比学习构建统一嵌入空间，量化评估从当前状态到达语言目标的物理可行性。在167B token数据上预训练后，PRTS在多个基准测试和真实任务中达到SOTA，显著提升了长程规划和复杂操作的成功率。
selection_source: fresh_fetch
motivation: 现有VLA模型多采用行为克隆，缺乏对任务时间进度和目标达成可能性的理解。
method: 采用对比强化学习将语言指令视为目标，通过统一嵌入空间评估状态-动作与目标的匹配度，并结合角色感知因果掩码集成到VLM中。
result: 在LIBERO、SimplerEnv及14项真实世界复杂任务中取得SOTA性能，尤其在长程任务和零样本指令设置下表现优异。
conclusion: 将目标可达性感知注入机器人基础策略，能显著提升其执行成功率和长程规划能力。
---

## 摘要
视觉-语言-动作（VLA）模型通过强大的视觉-语言先验推动了机器人控制的发展。然而，现有的 VLA 模型主要将预训练框架化为有监督的行为克隆，忽略了机器人学习作为目标达成过程的本质，而这一过程需要理解任务的时间进度。我们提出了 PRTS（元推理与任务系统），这是一种通过目标条件强化学习（Goal-Conditioned Reinforcement Learning）重新构建预训练过程的 VLA 基础模型。通过将语言指令视为目标并采用对比强化学习，PRTS 学习了一个统一的嵌入空间。在该空间中，状态-动作嵌入与目标嵌入的内积近似于对数折扣目标占用率（log-discounted goal occupancy），即从当前状态-动作出发达成语言指定目标的概率，从而在静态语义匹配之外定量评估物理可行性。PRTS 直接从离线轨迹中提取这种密集的目标可达性监督，无需奖励标注，并通过角色感知因果掩码（role-aware causal mask）将其整合到 VLM 主干网络中，相比于传统的行为克隆，其产生的额外开销微乎其微。这种范式赋予了高层推理系统内在的目标可达性感知能力，桥接了语义推理与任务的时间进度，并进一步有益于目标条件下的动作预测。在包含 167B token 的多样化操作和具身推理数据上进行预训练后，PRTS 在 LIBERO、LIBERO-Pro、LIBERO-Plus、SimplerEnv 以及包含 14 个复杂任务的真实世界测试集上达到了最先进的性能。在长时程（long-horizon）、高接触（contact-rich）和零样本新指令设置下，PRTS 的提升尤为显著，这证实了注入目标可达性感知能显著提高通用机器人基础策略的执行成功率和长时程规划能力。

## Abstract
Vision-Language-Action (VLA) models advance robotic control via strong visual-linguistic priors. However, existing VLAs predominantly frame pretraining as supervised behavior cloning, overlooking the fundamental nature of robot learning as a goal-reaching process that requires understanding temporal task progress. We present \textbf{PRTS} (\textbf{P}rimitive \textbf{R}easoning and \textbf{T}asking \textbf{S}ystem), a VLA foundation model that reformulates pretraining through Goal-Conditioned Reinforcement Learning. By treating language instructions as goals and employing contrastive reinforcement learning, PRTS learns a unified embedding space where the inner product of state-action and goal embeddings approximates the log-discounted goal occupancy, the probability of reaching the language-specified goal from the current state-action, quantitatively assessing physical feasibility beyond static semantic matching. PRTS draws this dense goal-reachability supervision directly from offline trajectories without reward annotations, and folds it into the VLM backbone via a role-aware causal mask, incurring negligible overhead over vanilla behavior cloning. This paradigm endows the high-level reasoning system with intrinsic goal reachability awareness, bridging semantic reasoning and temporal task progress, and further benefits goal-conditioned action prediction. Pretrained on 167B tokens of diverse manipulation and embodied-reasoning data, PRTS reaches state-of-the-art performance on LIBERO, LIBERO-Pro, LIBERO-Plus, SimplerEnv, and a real-world suite of 14 complex tasks, with particularly substantial gains on long-horizon, contact-rich, and zero-shot novel-instruction settings, confirming that injecting goal-reachability awareness significantly improves both execution success and long-horizon planning of general-purpose robotic foundation policies.

---

## 论文详细总结（自动生成）

以下是对论文《PRTS: A Primitive Reasoning and Tasking System via Contrastive Representations》的结构化深度总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA, RT-2）大多将预训练视为**有监督的行为克隆（Behavior Cloning, BC）**。这种方式将机器人学习简化为状态到动作的静态映射，忽略了机器人任务本质上是一个“目标达成”过程。
*   **研究背景**：传统的 BC 范式缺乏对任务时间进度（Temporal Progress）和物理可行性（Physical Feasibility）的理解。模型不知道当前动作对实现长程目标的贡献程度，导致在复杂、长时程（Long-horizon）任务中表现不佳。

### 2. 方法论
PRTS 提出将 VLA 预训练重新表述为**目标条件强化学习（Goal-Conditioned RL, GCRL）**，核心思想如下：
*   **对比强化学习（Contrastive RL）**：将语言指令视为“目标（Goal）”，通过对比学习构建一个统一的嵌入空间。
*   **目标可达性度量**：在嵌入空间中，状态-动作嵌入 $\phi(s, a)$ 与目标嵌入 $\psi(g)$ 的内积被训练为近似**对数折扣目标占用率（log-discounted goal occupancy）**。这量化了从当前状态出发达成目标的概率，从而赋予模型“物理可行性”的感知。
*   **密集监督信号**：直接从离线轨迹中提取监督信号，无需人工标注奖励函数。通过对比正样本（同一轨迹中的后续状态）和负样本（其他轨迹的状态）来学习。
*   **角色感知因果掩码（Role-aware Causal Mask）**：在 VLM（视觉语言模型）主干网络中引入特殊的掩码机制，使模型能同时处理语义推理、目标评估和动作预测，且计算开销与传统 BC 相比几乎可以忽略。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用了 LIBERO 评测套件（包括 LIBERO-Spatial, LIBERO-Object, LIBERO-Goal, LIBERO-Long），以及 LIBERO-Pro 和 LIBERO-Plus 扩展版。
    *   **模拟器评测**：SimplerEnv（用于评估在不同环境下的泛化能力）。
    *   **真实世界**：包含 14 个复杂的真实操作任务，涵盖高接触、长时程和零样本新指令场景。
*   **Benchmark 与对比方法**：对比了当前最先进的 VLA 模型，如 OpenVLA、Octo、RT-2 等。

### 4. 资源与算力
*   **数据规模**：在包含 **167B token** 的大规模多样化操作和具身推理数据上进行预训练。
*   **算力细节**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及训练总时长。但考虑到 167B token 的处理量和 VLA 基础模型的定位，通常需要大规模 GPU 集群（如数百块 H100/A100）进行数周的训练。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常全面，涵盖了从基础仿真到复杂真实场景的跨度。
*   **消融实验**：进行了针对目标可达性感知、对比损失函数设计以及掩码机制的消融研究。
*   **充分性评价**：实验不仅关注成功率，还深入分析了模型在长时程规划和零样本泛化上的表现。使用了多个标准 Benchmark 和真实机器人验证，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能提升**：PRTS 在所有测试的 Benchmark 上均达到了 SOTA（最先进）水平。
*   **长程任务优势**：通过注入目标可达性感知，模型在长时程任务中的成功率显著高于纯 BC 模型，证明了理解“任务进度”的重要性。
*   **零样本能力**：在面对从未见过的语言指令时，PRTS 表现出更强的语义-物理映射能力，能够更好地理解新指令背后的物理意图。
*   **推理与执行的桥接**：证明了将高层语义推理与底层物理可行性评估集成在同一个 Transformer 架构中是行之有效的。

### 7. 优点
*   **范式创新**：打破了 VLA 纯行为克隆的局限，引入了强化学习中的可达性概念，且无需繁琐的奖励工程。
*   **高效集成**：通过角色感知掩码将 GCRL 融入 VLM，在不显著增加推理延迟的前提下提升了性能。
*   **实用性强**：在真实世界的高接触、复杂任务中表现稳健，证明了其作为机器人基础模型的潜力。

### 8. 不足与局限
*   **计算资源需求**：虽然推理开销小，但 167B token 的预训练需要极高的算力支撑，普通研究机构难以复现。
*   **离线数据依赖**：模型性能高度依赖于离线轨迹的质量和多样性，对于极度罕见或分布外（OOD）的物理交互，可能仍存在预测偏差。
*   **动态环境适应**：虽然在静态目标达成上表现优异，但在极高动态、需要瞬时反应的环境下的表现尚需进一步验证。

（完）
