---
title: "PRISM: Pre-alignment via Black-box On-policy Distillation for Multimodal Reinforcement Learning"
title_zh: PRISM：通过黑盒同策略蒸馏实现多模态强化学习的预对齐
authors: "Sudong Wang, Weiquan Huang, Xiaomin Yu, Zuhao Yang, Hehai Lin, Keming Wu, Chaojun Xiao, Chen Chen, Wenxuan Wang, Beier Zhu, Yunjian Zhang, Chengwei Qin"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28123v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 多模态强化学习与可验证奖励及推理
tldr: 针对大模型多模态强化学习中SFT导致的分布偏移问题，本文提出PRISM框架。通过在SFT与RLVR之间引入基于黑盒在线策略蒸馏（OPD）的预对齐阶段，利用混合专家（MoE）判别器提供感知与推理的解耦修正信号。结合高质量的Gemini 3 Flash蒸馏数据，PRISM显著提升了Qwen3-VL在多模态基准测试中的强化学习表现。
selection_source: fresh_fetch
motivation: 传统的SFT阶段会引入分布偏移，且多模态推理中的感知与推理错误会进一步放大该问题，影响后续强化学习的效果。
method: 提出三阶段流水线，在SFT后引入基于MoE判别器的黑盒在线策略蒸馏，通过解耦的感知与推理专家信号引导模型对齐。
result: 在Qwen3-VL模型上，PRISM使4B和8B版本的平均准确率分别比传统SFT-to-RLVR基准提升了4.4和6.0个百分点。
conclusion: PRISM通过显式的预对齐阶段有效缓解了分布偏移，为提升多模态大模型的强化学习性能提供了新路径。
---

## 摘要
大型多模态模型（LMMs）的标准后训练方案通常是在精选演示数据上进行监督微调（SFT），随后进行带有可验证奖励的强化学习（RLVR）。然而，SFT 会引入分布偏移，既无法保留模型的原始能力，也无法忠实地匹配监督分布。这一问题在多模态推理中被进一步放大，其中感知错误和推理失败遵循不同的偏移模式，并在随后的强化学习过程中产生复合影响。我们提出了 PRISM，这是一个三阶段流水线，通过在 SFT 和 RLVR 之间插入一个显式的分布对齐阶段来缓解这种偏移。基于同策略蒸馏（OPD）原理，PRISM 将对齐建模为策略与具有专门感知和推理专家的混合专家（MoE）判别器之间的黑盒、响应级对抗博弈，在无需访问教师模型 Logits 的情况下提供解耦的纠正信号，引导策略趋向监督分布。虽然 126 万条公开演示数据足以进行广泛的 SFT 初始化，但分布对齐需要更高保真度的监督；因此，我们从 Gemini 3 Flash 中策划了 11.3 万条额外的演示数据，其特点是在最难的未解决问题上具有密集的视觉定位和分步推理。在 Qwen3-VL 上的实验表明，PRISM 在多种强化学习算法（GRPO、DAPO、GSPO）和多样化的多模态基准测试中一致地提高了下游 RLVR 性能，在 4B 和 8B 模型上分别比 SFT-to-RLVR 基准提高了 4.4 和 6.0 个百分点的平均准确率。我们的代码、数据和模型权重已在 https://github.com/XIAO4579/PRISM 公开。

## Abstract
The standard post-training recipe for large multimodal models (LMMs) applies supervised fine-tuning (SFT) on curated demonstrations followed by reinforcement learning with verifiable rewards (RLVR). However, SFT introduces distributional drift that neither preserves the model's original capabilities nor faithfully matches the supervision distribution. This problem is further amplified in multimodal reasoning, where perception errors and reasoning failures follow distinct drift patterns that compound during subsequent RL. We introduce PRISM, a three-stage pipeline that mitigates this drift by inserting an explicit distribution-alignment stage between SFT and RLVR. Building on the principle of on-policy distillation (OPD), PRISM casts alignment as a black-box, response-level adversarial game between the policy and a Mixture-of-Experts (MoE) discriminator with dedicated perception and reasoning experts, providing disentangled corrective signals that steer the policy toward the supervision distribution without requiring access to teacher logits. While 1.26M public demonstrations suffice for broad SFT initialization, distribution alignment demands higher-fidelity supervision; we therefore curate 113K additional demonstrations from Gemini 3 Flash, featuring dense visual grounding and step-by-step reasoning on the hardest unsolved problems. Experiments on Qwen3-VL show that PRISM consistently improves downstream RLVR performance across multiple RL algorithms (GRPO, DAPO, GSPO) and diverse multimodal benchmarks, improving average accuracy by +4.4 and +6.0 points over the SFT-to-RLVR baseline on 4B and 8B, respectively. Our code, data, and model checkpoints are publicly available at https://github.com/XIAO4579/PRISM.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PRISM** 的三阶段后训练框架，旨在解决多模态大模型（LMM）在强化学习（RL）之前，由于监督微调（SFT）阶段引入的“分布偏移”问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：传统的“SFT → RLVR（可验证奖励强化学习）”流程存在缺陷。SFT 阶段虽然能赋予模型基础能力，但往往会导致模型偏离其原始分布，产生“分布偏移（Distributional Drift）”。
*   **多模态挑战**：在多模态推理中，这种偏移是异质的——视觉感知错误和逻辑推理失败具有不同的模式。如果直接从偏移后的 SFT 模型开始 RL，这些错误会相互复合，限制 RL 的最终性能。
*   **研究目标**：在 SFT 和 RLVR 之间插入一个显式的“预对齐”阶段，通过纠正分布偏移，为后续强化学习提供更好的初始化状态。

### 2. 方法论：PRISM 框架
PRISM 包含三个阶段：**冷启动 SFT**、**分布对齐（核心创新）**、**结果导向 RLVR**。

*   **核心思想**：采用**黑盒同策略蒸馏（Black-box On-policy Distillation, OPD）**。模型学习自己生成的样本（On-policy），并通过一个判别器与高质量监督数据进行对比，从而向目标分布靠拢。
*   **关键技术细节**：
    *   **混合专家（MoE）判别器**：为了处理异质偏移，设计了包含两个专家的判别器：**感知专家（Perception Expert）**检查视觉定位，**推理专家（Reasoning Expert）**检查逻辑一致性。
    *   **对抗博弈**：将对齐建模为策略模型与 MoE 判别器之间的极小极大（Minimax）博弈。判别器学习区分策略生成结果与监督池数据，策略模型则通过策略梯度（如 GRPO）优化，以获得更高的判别器奖励。
    *   **黑盒公式**：不需要访问教师模型的 Logits（概率分布），仅通过响应级别的对比即可实现蒸馏，适用于闭源模型作为教师的情况。
    *   **奖励函数**：$r(x, y) = \alpha \cdot D_v(x, c) + (1 - \alpha) \cdot D_r(x, t)$，其中 $c$ 是视觉描述，$t$ 是推理链。

### 3. 实验设计
*   **基准测试（Benchmarks）**：
    *   **数学推理**：MathVista, MathVerse, MathVision, WeMath。
    *   **通用多模态理解**：MMMU, MMMU-Pro, HallusionBench。
*   **对比方法**：
    *   基础模型（Instruct 版本）。
    *   仅 SFT 模型。
    *   标准的 SFT → RLVR 流程（使用 GRPO, DAPO, GSPO 等不同算法）。
*   **实验模型**：Qwen3-VL-4B 和 Qwen3-VL-8B。
*   **数据来源**：使用了 126 万条公开数据进行 SFT，并从 Gemini 3 Flash 蒸馏了 11.3 万条高质量推理数据（包含密集视觉定位和分步推理）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100-SXM5-80GB GPU**。
*   **训练细节**：
    *   SFT 阶段：1 个 Epoch。
    *   PRISM 对齐阶段：500 个步数（Steps）。
    *   RLVR 阶段：1500 个步数。
*   **框架**：基于 LlamaFactory 进行 SFT，基于 veRL 框架进行强化学习，使用 vLLM 作为推理引擎。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4B 和 8B 两个模型尺度，验证了 3 种主流 RL 算法（GRPO, DAPO, GSPO），并在 7 个权威基准上进行了测试。
*   **消融实验**：非常充分。作者分别验证了 MoE 判别器设计的必要性（对比单模型判别器）、三阶段流程的必要性（对比跳过对齐阶段）、视觉信息的重要性（对比纯文本判别器）以及 SFT 数据规模的影响。
*   **客观性**：通过跟踪训练动态（奖励差距收敛图）和结构化代理指标（推理步数、描述项分布）来量化分布对齐的效果，实验设计严谨且公平。

### 6. 主要结论与发现
*   **性能提升**：PRISM 显著提升了下游 RLVR 的表现。在 4B 和 8B 模型上，平均准确率分别比 SFT → RLVR 基准提升了 **4.4** 和 **6.0** 个百分点。
*   **对齐的作用**：对齐阶段本身并不显著提高答案准确率，但它通过修复分布偏移，为 RL 提供了更优的起点。
*   **模型规模效应**：SFT 带来的负面偏移在更强大的模型（如 8B）上更明显，而 PRISM 的修复作用也随之更显著。
*   **效率**：PRISM 训练出的模型在推理时更加简洁高效，在获得更高准确率的同时使用了更少的 Token。

### 7. 优点（亮点）
*   **解耦反馈**：通过 MoE 判别器将感知和推理信号分开，解决了多模态任务中错误来源复杂的痛点。
*   **算法无关性**：证明了该预对齐阶段对多种 RL 算法均有增益，具有很强的通用性。
*   **高质量数据**：策划并开源了 11.3 万条高质量多模态推理数据，对社区有重要贡献。

### 8. 不足与局限
*   **计算开销**：引入额外的对齐阶段和 MoE 判别器增加了训练的时间和显存成本。
*   **格式依赖**：目前的 MoE 设计依赖于显式的“视觉描述+推理链”结构化输出格式，对于不符合此格式的任务可能难以直接应用。
*   **评估指标**：分布对齐的分析主要依赖于结构化代理指标（如步数、项数），缺乏更直接的、模型无关的分布距离度量。

（完）
