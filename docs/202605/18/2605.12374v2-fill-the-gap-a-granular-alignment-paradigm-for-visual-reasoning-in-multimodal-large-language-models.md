---
title: "Fill the GAP: A Granular Alignment Paradigm for Visual Reasoning in Multimodal Large Language Models"
title_zh: 填补 GAP：多模态大语言模型中视觉推理的细粒度对齐范式
authors: "Yanting Miao, Yutao Sun, Dexin Wang, Mengyu Zhou, Pascal Poupart, Lei Lv, Qi Zhao, Li Wang, Hao Li, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12374v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 多模态大语言模型视觉推理的细粒度对齐范式
tldr: 针对多模态大模型在视觉潜空间推理中存在的特征空间不匹配问题，本文提出了GAP（粒度对齐范式）。该方法通过特征级、上下文级和容量引导三个维度的对齐，解决了解码器输出与输入嵌入之间的分布差异。在Qwen2.5-VL 7B上的实验证明，GAP显著提升了模型的感知与推理能力，并验证了生成的潜变量包含有效的任务相关视觉信号，而非仅是增加占位符。
selection_source: fresh_fetch
motivation: 现有视觉潜空间推理方法因解码器隐藏状态与输入嵌入之间的特征空间不匹配，导致模型反馈不可靠且性能不稳定。
method: 提出GAP范式，通过PCA对齐潜头进行特征对齐、利用辅助视觉监督进行上下文对齐，并根据模型能力进行选择性监督。
result: 在Qwen2.5-VL 7B上实现了最佳的感知与推理综合性能，且探测实验证实生成的潜变量提供了关键的任务相关视觉信号。
conclusion: 通过多层次的粒度对齐，可以有效弥合多模态大模型内部特征空间的鸿沟，从而显著增强其视觉潜空间推理的稳定性与深度。
---

## 摘要
视觉潜空间推理（Visual latent reasoning）允许多模态大语言模型（MLLM）将中间视觉证据生成为连续标记（continuous tokens），从而避免使用外部工具或图像生成器。然而，现有方法通常遵循“输出即输入”的潜空间范式，且收益不稳定。我们发现了导致这种不稳定性的一种特征空间失配证据：主流的视觉潜空间模型基于 pre-norm MLLM 构建，并将解码器隐藏状态重用为预测的潜空间输入，尽管这些状态的范数范围（norm regime）与模型训练时所消耗的输入嵌入（input embeddings）显著不同。这种失配可能导致直接的潜空间反馈变得不可靠。基于这一诊断，我们提出了 GAP，一种用于视觉潜空间建模的细粒度对齐范式（Granular Alignment Paradigm）。GAP 在三个层面上对齐视觉潜空间推理：特征级对齐通过轻量级的 PCA 对齐潜空间头将解码器输出映射为与输入兼容的视觉潜变量；上下文级对齐通过可检查的辅助视觉监督来锚定潜空间目标；能力引导的对齐则有选择地将潜空间监督分配给基础 MLLM 难以处理的样本。在 Qwen2.5-VL 7B 上，所得模型在所有监督变体中实现了最佳的平均综合感知和推理性能。推理阶段的干预探测进一步表明，生成的潜变量提供了与任务相关的视觉信号，而不仅仅是增加了标记槽位。

## Abstract
Visual latent reasoning lets a multimodal large language model (MLLM) create intermediate visual evidence as continuous tokens, avoiding external tools or image generators. However, existing methods usually follow an output-as-input latent paradigm and yield unstable gains. We identify evidence for a feature-space mismatch that can contribute to this instability: dominant visual-latent models build on pre-norm MLLMs and reuse decoder hidden states as predicted latent inputs, even though these states occupy a substantially different norm regime from the input embeddings the model was trained to consume~\citep{xie2025mhc,li2026siamesenorm,team2026attention}. This mismatch can make direct latent feedback unreliable. Motivated by this diagnosis, we propose \textbf{GAP}, a \textbf{G}ranular \textbf{A}lignment \textbf{P}aradigm for visual latent modeling. GAP aligns visual latent reasoning at three levels: feature-level alignment maps decoder outputs into input-compatible visual latents through a lightweight PCA-aligned latent head; context-level alignment grounds latent targets with inspectable auxiliary visual supervision; and capacity-guided alignment assigns latent supervision selectively to examples where the base MLLM struggles. On Qwen2.5-VL 7B, the resulting model achieves the best mean aggregate perception and reasoning performance among our supervised variants. Inference-time intervention probing further suggests that generated latents provide task-relevant visual signal beyond merely adding token slots.

---

## 论文详细总结（自动生成）

### 论文总结：Fill the GAP: A Granular Alignment Paradigm for Visual Reasoning in Multimodal Large Language Models

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理复杂视觉任务（如图表分析、几何数学）时，常因缺乏局部视觉证据而失败。现有的“视觉潜空间推理”（Visual Latent Reasoning）通过生成连续的潜变量标记（Latent Tokens）来提供中间证据，无需调用外部工具。
**核心痛点：** 现有方法通常直接将解码器的输出隐藏状态作为下一步的输入，但在主流的 Pre-norm Transformer 架构中，输出状态的范数（Norm）远大于输入嵌入（如文中指出文本状态大 546 倍，视觉状态大 8.7 倍）。这种**特征空间不匹配（Feature-space Mismatch）**导致反馈循环不稳定，限制了性能增益。

#### 2. 方法论：GAP 粒度对齐范式
论文提出了 GAP 框架，从三个维度解决对齐问题：
*   **特征级对齐（Feature-level）：** 引入**PCA 对齐潜空间头**。不直接重用隐藏状态，而是通过 PCA 将解码器输出映射到辅助图像视觉嵌入的主子空间中，确保生成的潜变量在分布和范数上与模型训练时的输入保持一致。
*   **上下文级对齐（Context-level）：** 采用结构化监督格式：`<think>`（推理意图）+ `<latent>`（视觉潜变量）+ `<parser>`（对潜变量内容的文本描述）。这增强了潜变量的可解释性和训练稳定性。
*   **能力引导对齐（Model-level）：** 提出**难度感知监督**。通过对基础模型进行多次采样，仅对模型无法解决的“困难”样本施加潜空间推理监督，避免在简单任务上引入不必要的噪声。

#### 3. 实验设计
*   **基础模型：** Qwen2.5-VL 7B。
*   **数据集：** 构建了包含 49K 高质量样本的训练集（涵盖视觉 CoT、图表、几何、计数等）。
*   **Benchmark：**
    *   **感知任务：** HRBench4K, MMStar, MME-RealWorld-Lite。
    *   **推理任务：** MathVista, WeMath。
*   **对比方法：** 零样本基座模型、Dense Caption SFT（纯文本增强）、Monet-7B、LVR 等现有潜空间推理模型。

#### 4. 资源与算力
*   **算力设备：** 使用了 **32 台 NVIDIA B200 GPU**。
*   **训练时长：** 核心训练过程耗时约 **3 小时 55 分钟**。
*   **数据规模：** 49,309 个精心筛选的多模态样本。

#### 5. 实验数量与充分性
论文进行了多维度的详尽实验：
*   **主实验：** 在 5 个主流感知和推理基准上验证了 GAP 的优越性。
*   **消融实验：** 针对 PCA 组件数量、难度感知策略、数据质量进行了独立验证。
*   **干预探测：** 通过在推理时注入噪声或禁用潜变量，证明了生成的标记确实携带了任务相关的视觉信号，而非简单的占位符。
*   **Token 预算实验：** 测试了从 0 到 144 个潜变量标记对性能的影响。
**评价：** 实验设计客观、公平，通过控制变量法（Matched-recipe）排除了数据量差异带来的干扰，论证充分。

#### 6. 主要结论与发现
*   **特征对齐至关重要：** 仅通过简单的范数缩放就能提升性能，而 GAP 的 PCA 映射进一步稳定了训练。
*   **综合性能最优：** GAP 在感知和推理的综合指标上均优于现有的潜空间推理方法（如 Monet 和 LVR）。
*   **潜变量有效性：** 实验证实潜变量中包含有效的视觉特征，能够修正基座模型在几何推理等任务中的视觉误判。
*   **边际效应：** 潜变量标记并非越多越好，36 个标记在性能与成本之间达到了最佳平衡。

#### 7. 优点（亮点）
*   **诊断精准：** 首次从 Pre-norm 架构的残差积累角度解释了潜空间推理不稳定的根本原因。
*   **轻量高效：** PCA 投影头参数量极小，且无需外部图像生成器，保持了单模型推理的简洁性。
*   **可解释性：** 通过 `<parser>` 标签将不可见的连续向量与可见的文本描述挂钩，便于分析模型行为。

#### 8. 不足与局限
*   **模型依赖性：** 主要在 Qwen 系列模型上验证，对于 Post-norm 架构或其他架构的普适性有待进一步测试。
*   **推理开销：** 引入潜变量标记会增加推理延迟（约 1.6 倍），在实时性要求极高的场景下受限。
*   **数据构建成本：** 训练依赖于高质量的辅助图像和教师模型（如 Qwen3-VL-235B）生成的推理链，构建成本较高。

（完）
