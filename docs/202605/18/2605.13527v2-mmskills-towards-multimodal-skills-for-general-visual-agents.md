---
title: "MMSkills: Towards Multimodal Skills for General Visual Agents"
title_zh: MMSkills：迈向通用视觉智能体的多模态技能
authors: "Kangning Zhang, Shuai Shao, Qingyao Li, Jianghao Lin, Lingyue Fu, Shijian Wang, Wenxiang Jiao, Yuan Lu, Weiwen Liu, Weinan Zhang, Yong Yu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13527v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通用视觉智能体的多模态程序性知识
tldr: 本研究提出了MMSkills框架，旨在为通用视觉智能体提供多模态程序性知识。该框架将文本步骤、运行时状态卡片和多视角关键帧封装为可重用的技能包，并通过自动化的生成器从公开交互轨迹中提取这些知识。在推理阶段，智能体通过分支加载机制高效利用多模态证据。实验表明，MMSkills显著提升了前沿及小型多模态智能体在GUI和游戏基准测试中的表现，证明了外部多模态知识对模型先验的补充作用。
selection_source: fresh_fetch
motivation: 现有的智能体技能包主要依赖文本或代码，难以满足视觉智能体在复杂环境中对状态识别和视觉进度判断的多模态需求。
method: 开发了MMSkills框架，包含一种结合视觉关键帧的技能表示法、一个自动化的轨迹到技能生成器，以及一种分支加载的推理机制。
result: 在GUI和游戏等多个视觉智能体基准测试中，MMSkills持续提升了不同规模多模态模型的任务执行能力。
conclusion: 外部多模态程序性知识是提升视觉智能体决策能力的有效补充，能够显著增强模型对环境状态的理解和操作精度。
---

## 摘要
可重用技能已成为提升智能体能力的核心基础，然而大多数现有的技能包主要将可重用行为编码为文本提示、可执行代码或学习到的例程。然而，对于视觉智能体而言，过程性知识本质上是多模态的：重用不仅取决于要执行什么操作，还取决于识别相关状态、解释进度或失败的视觉证据，以及决定下一步该做什么。我们将这一需求形式化为多模态过程性知识，并解决了三个实际挑战：(I) 多模态技能包应包含什么；(II) 如何从公共交互经验中获取此类技能包；以及 (III) 智能体如何在推理时参考多模态证据，而不会产生过多的图像上下文或过度锚定到参考截图。我们提出了 MMSkills，这是一个用于表示、生成和使用可重用多模态过程以进行运行时视觉决策的框架。每个 MMSkill 都是一个紧凑的、以状态为条件的包，它将文本过程与运行时状态卡和多视图关键帧相结合。为了构建这些包，我们开发了一个基于智能体的“轨迹到技能”生成器，通过工作流分组、过程归纳、视觉定位和元技能引导的审计，将公共非评估轨迹转化为可重用的多模态技能。为了使用这些技能，我们引入了一个分支加载的多模态技能智能体：在临时分支中检查选定的状态卡和关键帧，将其与实时环境对齐，并提炼为针对主智能体的结构化指导。在 GUI 和基于游戏的视觉智能体基准测试上的实验表明，MMSkills 持续提升了前沿和较小规模多模态智能体的性能，这表明外部多模态过程性知识是对模型内部先验的补充。

## Abstract
Reusable skills have become a core substrate for improving agent capabilities, yet most existing skill packages encode reusable behavior primarily as textual prompts, executable code, or learned routines. For visual agents, however, procedural knowledge is inherently multimodal: reuse depends not only on what operation to perform, but also on recognizing the relevant state, interpreting visual evidence of progress or failure, and deciding what to do next. We formalize this requirement as multimodal procedural knowledge and address three practical challenges: (I) what a multimodal skill package should contain; (II) where such packages can be derived from public interaction experience; and (III) how agents can consult multimodal evidence at inference time without excessive image context or over-anchoring to reference screenshots. We introduce MMSkills, a framework for representing, generating, and using reusable multimodal procedures for runtime visual decision making. Each MMSkill is a compact, state-conditioned package that couples a textual procedure with runtime state cards and multi-view keyframes. To construct these packages, we develop an agentic trajectory-to-skill Generator that transforms public non-evaluation trajectories into reusable multimodal skills through workflow grouping, procedure induction, visual grounding, and meta-skill-guided auditing. To use them, we introduce a branch-loaded multimodal skill agent: selected state cards and keyframes are inspected in a temporary branch, aligned with the live environment, and distilled into structured guidance for the main agent. Experiments across GUI and game-based visual-agent benchmarks show that MMSkills consistently improve both frontier and smaller multimodal agents, suggesting that external multimodal procedural knowledge complements model-internal priors.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **MMSkills**，一个旨在为通用视觉智能体（Visual Agents）提供多模态程序性知识的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的智能体技能通常以文本提示或代码形式存在，这对于依赖视觉证据的智能体（如操作 GUI 或玩游戏的智能体）是不够的。视觉任务的成功不仅取决于“做什么”，还取决于“如何识别当前状态”以及“如何通过视觉证据判断进度或失败”。
*   **核心概念**：论文提出了**多模态程序性知识（Multimodal Procedural Knowledge）**，强调技能应将文本步骤与视觉状态识别、验证线索紧密结合，解决文本技能描述模糊和原始演示（Demonstration）过于冗长且难以泛化的问题。

### 2. 方法论
MMSkills 框架由三个核心部分组成：
*   **多模态技能包表示**：每个技能包含四个组件 $M = (D, P, S, K)$。
    *   $D$：简短描述。
    *   $P$：可重用的文本操作流程。
    *   $S$（状态卡片）：定义了何时使用/不使用该技能、视觉线索和验证标准。
    *   $K$（多视角关键帧）：提供全局上下文（全帧）、局部细节（聚焦裁剪）以及操作前后的对比图。
*   **技能生成器（Generator）**：一个自动化的“轨迹到技能”流水线，包含五个阶段：
    1.  任务嵌入与聚类。
    2.  聚类级别的技能规划。
    3.  技能合并与去重。
    4.  文本草案编写。
    5.  视觉定位（Grounding）与审计（使用元技能引导 LLM 提取关键帧）。
*   **分支加载机制（Branch-loading）**：为了避免主智能体上下文过载或被参考截图误导，推理时会开启一个“临时分支”：
    *   **阶段 1（门控视图选择）**：根据当前观察选择相关的状态卡片和视图类型。
    *   **阶段 2（分支规划）**：将选定的视觉证据与当前环境对齐，返回结构化的指导（如子目标、负面约束、验证点），主智能体仅接收提炼后的文本指导。

### 3. 实验设计
*   **数据集/场景**：
    *   **GUI 任务**：OSWorld (Ubuntu), macOSWorld。
    *   **游戏任务**：VAB-Minecraft, Super Mario Bros (LMGame-Bench)。
*   **对比方法**：
    *   **No-skill**：无外部技能辅助。
    *   **Text-only skill**：仅提供文本形式的技能。
    *   **MMSkills**：完整的多模态技能框架。
*   **评估模型**：涵盖了前沿模型（Gemini 3.1 Pro, Kimi-K2.6）和较小规模模型（Gemini 3 Flash, Qwen3-VL-8B, GLM-5V 等）。

### 4. 资源与算力
*   **算力说明**：论文未明确给出生成技能包的具体 GPU 训练时长。由于生成器是基于 LLM API 的智能体流水线（Agentic Pipeline），其成本主要体现在 API 调用上。
*   **运行环境**：GUI 实验主要在 AWS（亚马逊云）上运行，使用官方基准测试镜像。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的基准测试上进行了测试，涵盖了从操作系统操作到开放世界游戏的多种视觉环境。
*   **消融实验**：非常充分。作者分别验证了“状态卡片”和“视觉关键帧”的必要性，并对比了“直接加载”与“分支加载”的效果。
*   **行为分析**：除了成功率，还深入分析了动作步数、重复动作比例、动作原语分布等，证明了 MMSkills 能显著减少无效尝试和重复操作。

### 6. 主要结论与发现
*   **性能提升**：MMSkills 在所有模型和基准测试上均优于无技能和纯文本技能。例如，在 OSWorld 上，Gemini 3 Flash 的成功率从 36.65% 提升至 47.97%。
*   **小模型增益更大**：对于较弱的模型（如 Qwen3-VL-8B），MMSkills 带来的提升尤为显著（OSWorld 成功率从 10.78% 提升至 25.40%），说明外部知识能有效弥补模型内部先验的不足。
*   **效率优化**：使用 MMSkills 的智能体通常能以更短的路径完成任务，且表现出更强的“完成意识”（更准确地判断何时该输出 DONE）。

### 7. 优点
*   **创新的表示法**：通过“状态卡片”将视觉证据与决策逻辑解耦，比单纯的截图挂载更具可解释性和重用性。
*   **分支加载机制**：巧妙地解决了多模态信息带来的上下文压力和视觉锚定偏差问题。
*   **自动化构建**：能够从公开的非测试轨迹中自动提取知识，具备良好的扩展性。

### 8. 不足与局限
*   **依赖源轨迹质量**：技能的覆盖范围和准确性高度依赖于输入的公共交互轨迹。
*   **推理成本**：分支加载机制虽然保护了主上下文，但引入了额外的 LLM 调用次数，增加了推理延迟和成本。
*   **视觉定位误差**：如果生成阶段的视觉定位（Grounding）出现错误，可能会给智能体提供错误的参考信息。
*   **安全性与隐私**：技能包中包含截图，在处理敏感环境时需要严格的隐私过滤机制。

（完）
