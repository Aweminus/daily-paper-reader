---
title: "GuidedVLA: Specifying Task-Relevant Factors via Plug-and-Play Action Attention Specialization"
title_zh: GuidedVLA：通过即插即用的动作注意力专门化指定任务相关因素
authors: "Xiaosong Jia, Bowen Yang, Zuhao Ge, Xian Nie, Yuchen Zhou, Cunxin Fan, Yufeng Li, Yilin Chai, Chao Jing, Zijian Liang, Qingwen Bu, Haidong Cao, Chao Wu, Qifeng Li, Zhenjie Yang, Chenhe Zhang, Hongyang Li, Zuxuan Wu, Junchi Yan, Yu-Gang Jiang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12369v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于通用机器人学习的视觉-语言-动作 (VLA) 模型
tldr: 针对现有视觉-语言-动作（VLA）模型在端到端训练中容易过拟合环境噪声和视觉捷径的问题，本文提出了GuidedVLA框架。该框架通过将动作解码器视为功能组件的集合，利用辅助信号监督特定的注意力头，使其专注于物体定位、空间几何和时间技能逻辑等任务相关因素。实验证明，GuidedVLA在仿真和真实机器人任务中均显著提升了泛化能力和成功率，为构建鲁棒的通用机器人模型提供了新思路。
selection_source: fresh_fetch
motivation: 现有的VLA模型依赖端到端隐式学习，容易受到视觉捷径和环境噪声干扰，导致泛化能力不足。
method: 通过辅助信号监督特定的注意力头，使动作解码器能够显式地捕捉物体定位、空间几何和时间逻辑等关键因素。
result: 在仿真和真实机器人实验中，GuidedVLA在域内和跨域设置下的表现均显著优于现有的强基准模型。
conclusion: 显式引导动作解码器学习解耦的任务相关特征是提升VLA模型鲁棒性和通用性的有效方向。
---

## 摘要
视觉-语言-动作 (VLA) 模型旨在通过将动作作为一种模态对齐到强大的视觉-语言模型 (VLM) 中，来实现通用机器人学习。现有的 VLA 依赖端到端监督，隐式地使动作解码过程学习任务相关特征。然而，在缺乏显式引导的情况下，这些模型往往会过拟合于伪相关性，例如视觉捷径或环境噪声，从而限制了它们的泛化能力。在本文中，我们介绍了 GuidedVLA，这是一个旨在手动引导动作生成以关注任务相关因素的框架。我们的核心见解是将动作解码器视为功能组件的集合，而非单一的学习器。单个注意力头由手动定义的辅助信号监督，以捕获不同的因素。作为初步研究，我们使用三个专门的头来实例化这一范式：物体定位、空间几何和时间技能逻辑。通过仿真和真实机器人实验，GuidedVLA 在域内和跨域设置下，相比强大的 VLA 基准模型均提高了成功率。最后，我们表明这些专门化因素的质量与任务性能呈正相关，并且我们的机制产生了解耦的高质量特征。我们的结果表明，显式引导动作解码器学习是构建更鲁棒、更通用的 VLA 模型的一个有前景的方向。

## Abstract
Vision-Language-Action (VLA) models aim for general robot learning by aligning action as a modality within powerful Vision-Language Models (VLMs). Existing VLAs rely on end-to-end supervision to implicitly enable the action decoding process to learn task-relevant features. However, without explicit guidance, these models often overfit to spurious correlations, such as visual shortcuts or environmental noise, limiting their generalization. In this paper, we introduce GuidedVLA, a framework designed to manually guide the action generation to focus on task-relevant factors. Our core insight is to treat the action decoder not as a monolithic learner, but as an assembly of functional components. Individual attention heads are supervised by manually defined auxiliary signals to capture distinct factors. As an initial study, we instantiate this paradigm with three specialized heads: object grounding, spatial geometry, and temporal skill logic. Across simulation and real-robot experiments, GuidedVLA improves success rates in both in-domain and out-of-domain settings compared to strong VLA baselines. Finally, we show that the quality of these specialized factors correlates positively with task performance and that our mechanism yields decoupled, high-quality features. Our results suggest that explicitly guiding action-decoder learning is a promising direction for building more robust and general VLA models.

---

## 论文详细总结（自动生成）

这是一份关于论文《GuidedVLA: Specifying Task-Relevant Factors via Plug-and-Play Action Attention Specialization》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型（如 $\pi_0$、OpenVLA）主要依赖端到端监督，期望模型能隐式学习任务相关特征。然而，这种“黑盒”训练容易导致模型学习到**视觉捷径（Visual Shortcuts）**或**因果混淆（Causal Confusion）**，例如过拟合背景纹理或相机伪影，而非理解任务逻辑。
*   **核心问题**：如何引导 VLA 的动作解码器显式地提取对机器人操控至关重要的任务相关因素（如物体位置、空间几何、时间逻辑），从而提升模型在复杂环境和分布外（OOD）场景下的鲁棒性？

### 2. 论文提出的方法论
*   **核心思想**：将动作解码器视为**功能组件的集合**，而非单一的学习器。通过“注意力头专门化（Attention Head Specialization）”，手动指定并监督不同的注意力头去捕捉特定的任务因素。
*   **关键技术细节**：
    *   **三大专门化头**：
        1.  **物体头 (Object Head)**：通过辅助损失（$L_{object}$）监督注意力图，强制动作 Token 关注任务相关的物体掩码（由 Grounding SAM/SAM2 自动标注）。
        2.  **技能头 (Skill Head)**：通过辅助分类损失（$L_{skill}$）对齐内部特征与当前子技能阶段（如“抓取”或“放置”），增强时间逻辑。
        3.  **深度头 (Depth Head)**：不使用损失函数，而是通过架构约束，让特定头只处理来自预训练深度模型（Depth Anything 3）的特征，注入 3D 空间感知。
    *   **即插即用适配器 (ControlNet-style Adapter)**：借鉴 ControlNet 的设计，引入零初始化的残差分支。这使得模型在训练初期能保持预训练 VLA 的行为，随后逐渐注入专门化的偏置，实现无损增强。
    *   **自动化标注流水线**：利用 Qwen3-VL 和 SAM2 构建了高效的标注工具，将标注速度提升了约 11 倍。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：**LIBERO-Plus**（包含 7 种维度的鲁棒性扰动，如光照、背景、布局等）和 **RoboTwin 2.0**（8 种复杂的双臂操控任务）。
    *   **真实机器人**：在 **ALOHA**（家庭任务：捡蔬菜、叠碗、清理桌面）和 **PSI-Bot**（实验室任务：放置烧杯、嵌套烧杯、加热实验）两个平台上进行验证。
*   **对比方法 (Baselines)**：
    *   基础模型：$\pi_0$、OpenVLA。
    *   先进变体：DreamVLA（预测动态区域/深度）、AdaMoE（任务专家混合）、UniVLA、VLA-Adapter 等。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **训练**：使用了 **8 张 NVIDIA H200 GPU** 进行 LIBERO-Plus 实验，**4 张 H200 GPU** 进行 RoboTwin 2.0 实验。
    *   **推理**：在单张 **NVIDIA RTX 4090 GPU** 上进行实时部署。
*   **训练时长**：RoboTwin 实验中每个模型训练约 30k 步（Batch Size 为 16 或 64）。
*   **模型规模**：基于 $\pi_0$ 架构（包含 Gemma-2B 视觉语言骨干和 Gemma-300M 动作专家头）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的实验，包括：
    *   LIBERO-Plus 的 4 个子套件、7 种扰动维度的全面测试。
    *   RoboTwin 2.0 的 8 个任务、100 次 Rollout/任务。
    *   真实机器人 6 个任务，每种设置 20 次试验（总计数百次实机测试）。
*   **充分性与公平性**：实验设计非常详尽，包含了**消融实验**（验证每个头的作用）、**敏感性分析**（定量证明因素质量与成功率的正相关性）、**特征解耦分析**（通过 t-SNE 证明专门化头确实学习到了不重叠的特征）。所有对比实验均在相同的硬件和超参数设置下进行，具有较高的客观性。

### 6. 主要结论与发现
*   **性能提升**：GuidedVLA 在所有基准测试中均显著优于基准模型。在 LIBERO-Plus 上平均成功率从 68.2% 提升至 75.4%；在 RoboTwin 2.0 上从 77.38% 提升至 90.63%。
*   **因素相关性**：实验定量证明，物体定位精度、技能识别准确率和几何感知能力的提升，会单调地带动任务成功率的增长。
*   **解耦优势**：相比于将所有辅助任务混合训练（Mixture），专门化的注意力头能产生更清晰、互不干扰的特征表示，性能更优。
*   **泛化能力**：显式引导显著增强了模型在光照变化、背景切换和物体位置偏移下的泛化表现。

### 7. 优点（亮点）
*   **架构优雅**：通过注意力头的自然解耦特性实现专门化，无需大幅改动 VLA 骨干网络。
*   **即插即用**：ControlNet 风格的适配器保证了预训练知识的保留，且易于扩展到其他任务因素。
*   **实用性强**：配套的自动化标注流水线解决了辅助监督数据获取难的问题。
*   **分析深入**：论文不仅展示了结果，还深入探讨了“为什么有效”，并提供了丰富的可视化证据。

### 8. 不足与局限
*   **因素依赖性**：目前模型依赖于预定义的三个因素（物体、技能、几何），对于某些特定任务，可能需要手动寻找新的关键因素。
*   **自动化瓶颈**：虽然标注已大幅自动化，但在处理极其复杂的连续任务时，自动技能切分仍可能存在误差，需要少量人工干预。
*   **算力门槛**：尽管推理可在 4090 上完成，但训练过程仍需要 H200 等高性能计算资源。
*   **动态环境**：对于极高速运动或高度动态变化的场景，该框架的实时响应和因素提取能力尚待进一步验证。

（完）
