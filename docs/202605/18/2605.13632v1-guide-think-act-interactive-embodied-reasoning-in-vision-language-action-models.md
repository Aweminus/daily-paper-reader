---
title: "Guide, Think, Act: Interactive Embodied Reasoning in Vision-Language-Action Models"
title_zh: 引导、思考、行动：视觉-语言-动作模型中的交互式具身推理
authors: "Yiran Ling, Qing Lian, Jinghang Li, Qing Jiang, Tianming Zhang, Xiaoke Jiang, Chuanxiu Liu, Jie Liu, Lei Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13632v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于具身推理的交互式视觉-语言-动作框架
tldr: 针对现有具身智能模型在分布外场景下鲁棒性差且难以纠错的问题，本文提出GTA-VLA交互式框架。该框架允许用户通过点、框、轨迹等空间先验引导机器人，并将其融入空间-视觉思维链推理中。通过结合外部引导、内部任务规划与轻量级动作执行头，GTA-VLA实现了人类意图与自主决策的对齐，显著增强了机器人在复杂及歧义环境下的任务恢复能力。
selection_source: fresh_fetch
motivation: 现有VLA模型采用直接的感知到动作映射，在面对分布外偏移或视觉歧义时表现脆弱且缺乏有效的交互纠错机制。
method: 提出一种“引导-思考-行动”框架，通过接收用户空间引导并生成集成了外部先验与内部规划的空间-视觉思维链来驱动动作执行。
result: "在SimplerEnv WidowX基准测试中取得81.2%的成功率，并在视觉偏移和空间歧义场景下通过单次交互显著提升了任务成功率。"
conclusion: 将人类空间引导与具身推理相结合，为提升VLA模型的鲁棒性、可解释性以及在现实复杂环境中的部署能力提供了有效路径。
---

## 摘要
在本文中，我们提出了 GTA-VLA（引导、思考、行动），这是一个交互式视觉-语言-动作（VLA）框架，通过允许用户使用显式视觉提示引导机器人策略，实现了空间可控的具身推理。现有的 VLA 模型学习从多模态观测到机器人动作的直接“感知到动作”（Sense-to-Act）映射。虽然在训练分布内有效，但这种紧耦合的策略在分布外（OOD）偏移下非常脆弱，且在发生故障时难以纠正。尽管最近的具身思维链（CoT）方法暴露了中间推理过程，但它们仍缺乏整合人类空间引导的机制，限制了其解决视觉歧义或从错误中恢复的能力。为了填补这一空白，我们的框架允许用户选择性地使用空间先验（如可操作点、框和轨迹）来引导策略，随后的推理过程可以直接以此为条件。基于这些输入，模型生成统一的空间-视觉思维链，将外部引导与内部任务规划相结合，使人类视觉意图与自主决策保持一致。为了实际部署，我们进一步将推理模块与轻量级反应式动作头相结合，以实现高效的动作执行。大量实验证明了我们方法的有效性。在域内 SimplerEnv WidowX 基准测试中，我们的框架实现了 81.2% 的最先进成功率。在 OOD 视觉偏移和空间歧义下，单次视觉交互相比现有方法显著提高了任务成功率，突显了交互式推理在具身控制故障恢复中的价值。项目详情见：https://signalispupupu.github.io/GTA-VLA_ProjPage/

## Abstract
In this paper, we propose GTA-VLA(Guide, Think, Act), an interactive Vision-Language-Action (VLA) framework that enables spatially steerable embodied reasoning by allowing users to guide robot policies with explicit visual cues. Existing VLA models learn a direct "Sense-to-Act" mapping from multimodal observations to robot actions. While effective within the training distribution, such tightly coupled policies are brittle under out-of-domain (OOD) shifts and difficult to correct when failures occur. Although recent embodied Chain-of-Thought (CoT) approaches expose intermediate reasoning, they still lack a mechanism for incorporating human spatial guidance, limiting their ability to resolve visual ambiguities or recover from mistakes. To address this gap, our framework allows users to optionally guide the policy with spatial priors, such as affordance points, boxes, and traces, which the subsequent reasoning process can directly condition on. Based on these inputs, the model generates a unified spatial-visual Chain-of-Thought that integrates external guidance with internal task planning, aligning human visual intent with autonomous decision-making. For practical deployment, we further couple the reasoning module with a lightweight reactive action head for efficient action execution. Extensive experiments demonstrate the effectiveness of our approach. On the in-domain SimplerEnv WidowX benchmark, our framework achieves a state-of-the-art 81.2% success rate. Under OOD visual shifts and spatial ambiguities, a single visual interaction substantially improves task success over existing methods, highlighting the value of interactive reasoning for failure recovery in embodied control. Details of the project can be found here: https://signalispupupu.github.io/GTA-VLA_ProjPage/

---

## 论文详细总结（自动生成）

以下是对论文《Guide, Think, Act: Interactive Embodied Reasoning in Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多采用直接的“感知到动作”（Sense-to-Act）映射。这种紧耦合的策略在面对分布外（OOD）场景、视觉歧义或语义偏移时表现脆弱，且由于缺乏中间推理过程，人类难以在机器人出错时进行干预或纠正。
*   **研究背景**：虽然具身思维链（CoT）技术提升了模型的可解释性，但现有的 CoT 过程是封闭的，无法整合人类的实时空间引导（如指点、划框）。
*   **整体含义**：本文提出了 **GTA-VLA** 框架，旨在通过“引导（Guide）、思考（Think）、行动（Act）”三个阶段，将人类的空间先验与机器人的自主推理相结合，使机器人不仅能自主工作，还能在复杂环境下接受人类的精准引导。

### 2. 方法论：核心思想与关键技术
GTA-VLA 将具身任务分解为三个异步执行的阶段：
*   **引导（Guide）阶段**：引入可选的空间先验 $P_{spatial}$（包括**点**、**框**、**轨迹**）。这些先验可以由人类提供，也可以由外部感知模型生成，作为模型推理的约束条件。
*   **思考（Think）阶段**：VLM 主干（基于 Qwen3-VL-2B）生成结构化的空间-视觉思维链 $C$。
    *   **任务 CoT ($C_{task}$)**：语义分解任务。
    *   **视觉 CoT ($C_{vision}$)**：在图像空间中定位目标区域和可操作点（Affordance）。
    *   **机器人 CoT ($C_{robot}$)**：预测末端执行器的粗略 2D 运动轨迹。
*   **行动（Act）阶段**：采用**异步设计**。
    *   **慢速推理模块**：VLM 以较低频率运行，更新潜在推理状态 $H_{reasoning}$。
    *   **快速动作模块**：轻量级流量匹配（Flow-Matching）动作头以高频率（如 10Hz）运行，结合当前观测和最新的推理状态生成连续动作块。
*   **数据流水线**：构建了 **Interact-306K** 数据集，通过自动化流水线从现有机器人数据中提取关键帧、目标跟踪和运动轨迹，生成伪标签用于训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：用于评估多任务泛化和长程推理。
    *   **SimplerEnv (WidowX/Google Robot)**：用于评估真实到仿真的迁移能力。
    *   **SimplerEnv-Plus**（本文提出）：专门针对 OOD 鲁棒性，包含视觉偏移（光照、视角）、机器人状态偏移、语言偏移和对象偏移（未见物体、干扰项）。
*   **对比方法**：包括 OpenVLA、$\pi_0$、GR00T-N1、X-VLA、ThinkAct、MolmoAct 等 SOTA 模型。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **预训练**：使用了 **48 台 NVIDIA H800 GPU**。
*   **微调**：使用了 **16 台 NVIDIA H800 GPU**。
*   **推理/部署**：在 NVIDIA L20 GPU 上进行仿真评估；实机部署使用双 GPU 工作站（包含 **2 块 NVIDIA RTX 5090**），其中 VLM 分支和动作头分支各占一块 GPU。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了大规模仿真基准测试、系统性的 OOD 鲁棒性测试、消融实验以及真实世界机器人部署。
*   **充分性**：
    *   **消融实验**：分别验证了任务、视觉、机器人三个 CoT 字段的必要性，以及结构化 CoT 优于自由文本 CoT 的原因。
    *   **交互实验**：测试了点、框、轨迹三种引导方式在解决歧义时的有效性。
    *   **客观性**：通过在 SimplerEnv-Plus 中引入随机扰动和未见物体，客观地评估了模型在极端情况下的表现。

### 6. 主要结论与发现
*   **性能领先**：在 SimplerEnv 基准上达到 81.2% 的成功率，超越了 $\pi_0$ 和 X-VLA 等强基线。
*   **OOD 鲁棒性**：在视觉和物体偏移场景下，GTA-VLA 的表现显著优于直接映射模型，证明了中间推理的防御作用。
*   **交互纠错**：在语言无法区分目标（如多个相同物体）时，简单的空间点引导能将成功率从 37.5% 提升至 54.2%，人类干预可恢复约 20% 的失败案例。
*   **异步优势**：解耦推理与控制解决了 VLM 自回归生成的延迟问题，保证了实机控制的响应速度。

### 7. 优点
*   **可控性强**：通过空间先验接口，实现了人类意图与机器人动作的精准对齐。
*   **架构优雅**：异步“慢思考、快行动”的设计平衡了高层逻辑推理与底层实时控制的需求。
*   **数据高效**：开发了自动化标注流水线，无需人工收集交互轨迹即可训练交互能力。
*   **解释性好**：结构化的 CoT 让用户能清晰看到机器人“想去哪”和“怎么去”。

### 8. 不足与局限
*   **空间维度限制**：目前的推理和引导主要基于 **2D 图像空间**，在处理复杂的 3D 几何关系或深度信息敏感的任务时可能存在局限。
*   **伪标签依赖**：训练数据依赖于自动化标注工具（如 Dino-X, SAM），标注噪声可能会影响推理的准确性。
*   **低层控制瓶颈**：人类引导主要解决“找对目标”的问题，但无法纠正由于机械臂奇异点、夹爪力度不足等引起的底层执行错误。

（完）
