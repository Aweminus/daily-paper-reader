---
title: "SpatialForge: Bootstrapping 3D-Aware Spatial Reasoning from Open-World 2D Images"
title_zh: SpatialForge：从开放世界 2D 图像中引导 3D 感知空间推理
authors: "Zishan Liu, Ruoxi Zang, Yanglin Zhang, Wei Liu, Yin Zhang, Jian Yao, Jiayin Zheng, Zhengzhe Liu"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11462v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉语言模型中的空间推理
tldr: 针对大型视觉语言模型（VLM）在空间推理（如深度排序、坐标定位）方面的短板，本文提出了 SpatialForge 自动合成管线。该方法通过将海量互联网 2D 图像转化为包含深度、布局和视点相关推理的结构化监督信号，构建了拥有 1000 万个空间问答对的 SpatialForge-10M 数据集。实验证明，利用该数据集训练能显著提升 VLM 的 3D 空间感知与推理能力。
selection_source: fresh_fetch
motivation: 现有的空间推理数据集受限于场景数量，规模远小于互联网 2D 图像，导致 VLM 在处理几何任务时表现不佳。
method: 提出一种可扩展的合成管线，将空间推理分解为感知与关系，通过自动验证从 2D 图像中提取深度、布局等结构化监督信号。
result: 构建了包含 1000 万条数据的 SpatialForge-10M，在多个空间推理基准测试中显著提升了标准 VLM 的性能。
conclusion: 证明了通过大规模 2D 图像合成 3D 空间监督信号是提升模型空间推理能力的有效途径。
---

## 摘要
大型视觉语言模型（VLM）的最新进展展示了卓越的语义理解能力，但这些模型在空间推理方面始终面临挑战，经常在深度排序和精确坐标定位等基础几何任务上失败。最近的研究尝试从以场景为中心的数据集（如多视图扫描或室内视频）中引入空间监督，但受限于底层场景数量的不足。因此，此类数据的规模和多样性仍显著小于互联网规模的 2D 图像集合。为了解决这一局限性，我们提出了 SpatialForge，这是一个可扩展的数据合成流水线，可将野外（in-the-wild）2D 图像转化为空间推理监督信号。我们的方法将空间推理分解为感知和关系，并构建了涵盖深度、布局和视点依赖推理的结构化监督信号，同时通过自动验证确保数据质量。基于该流水线，我们构建了 SpatialForge-10M，这是一个包含 1000 万个空间问答对的大规模数据集。在多个空间推理基准测试上的广泛实验表明，在 SpatialForge-10M 上进行训练显著提升了标准 VLM 的空间推理能力，突显了通过扩展 2D 数据来实现 3D 感知空间推理的有效性。

## Abstract
Recent advancements in Large Vision-Language Models (VLMs) have demonstrated exceptional semantic understanding, yet these models consistently struggle with spatial reasoning, often failing at fundamental geometric tasks such as depth ordering and precise coordinate grounding. Recent efforts introduce spatial supervision from scene-centric datasets (e.g., multi-view scans or indoor video), but are constrained by the limited number of underlying scenes. As a result, the scale and diversity of such data remain significantly smaller than those of web-scale 2D image collections. To address this limitation, we propose SpatialForge, a scalable data synthesis pipeline that transforms in-the-wild 2D images into spatial reasoning supervision. Our approach decomposes spatial reasoning into perception and relation, and constructs structured supervision signals covering depth, layout, and viewpoint-dependent reasoning, with automatic verification to ensure data quality. Based on this pipeline, we build SpatialForge-10M, a large-scale dataset containing 10 million spatial QA pairs. Extensive experiments across multiple spatial reasoning benchmarks demonstrate that training on SpatialForge-10M significantly improves the spatial reasoning ability of standard VLMs, highlighting the effectiveness of scaling 2D data for 3D-aware spatial reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《SpatialForge: Bootstrapping 3D-Aware Spatial Reasoning from Open-World 2D Images》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大型视觉语言模型（VLM）在语义理解上表现出色，但在空间推理（如深度排序、精确坐标定位、视点变换）方面存在显著短板。
*   **核心瓶颈**：现有的空间监督数据主要依赖于场景中心的数据集（如 ScanNet 的 3D 扫描或室内视频），这些数据规模小、多样性差（多局限于室内），难以支撑模型在开放世界中的泛化。
*   **研究目标**：探索如何直接从海量的互联网 2D 图像中提取结构化的 3D 感知空间监督信号，以低成本、高扩展性的方式提升 VLM 的空间推理能力。

### 2. 方法论
SpatialForge 提出了一套自动化的数据合成管线，将空间推理分解为**感知（Perception）**与**关系（Relation）**两个层级：
*   **核心思想**：利用现有的专家模型（检测、深度估计、VLM）对 2D 图像进行多维度标注，并将其转化为结构化的问答对（QA）。
*   **关键技术流程**：
    1.  **图像过滤**：使用 CLIP 过滤非自然场景（如截图、文档），确保物理真实性。
    2.  **数据预处理**：
        *   **分级描述**：利用强 VLM 生成全局描述，并提取物体查询词，再通过开放词汇检测器（Grounding DINO）定位。
        *   **深度估计**：使用单目深度估计模型（Depth Anything V2）获取物体级的相对深度。
        *   **人体朝向估计**：识别场景中人的朝向（面向/背向相机），为视点变换推理提供基础。
    3.  **任务生成**：
        *   **空间感知**：包括定位（Grounding）、指代（Referring）和计数（Counting）。
        *   **空间关系**：包括远近排序（Near-Far）、左右布局（Left-Right）以及视点依赖推理（Perspective）。
    4.  **质量检查**：引入更强大的 VLM 作为“裁判”，对生成的 QA 对进行一致性验证，剔除错误样本。

### 3. 实验设计
*   **数据集**：构建了 **SpatialForge-10M**，包含来自 Objects365、OpenImages 和 Pixmo 的 200 万张图像，共 1000 万个 QA 对。
*   **评估基准（Benchmarks）**：
    *   **CV-Bench**：评估 2D/3D 空间位置关系。
    *   **SPAR-Bench**：评估深度预测和空间想象。
    *   **SpaCE10**：评估组合空间智能。
    *   **OmniSpatial & MindCube**：评估视点变换和心理旋转能力。
*   **对比方法**：包括闭源模型（GPT-4o, Gemini-2.0-Flash）、开源通用模型（LLaVA-OneVision, Qwen2.5-VL）以及专门的空间模型（SpatialBot, SpatialLadder 等）。

### 4. 资源与算力
*   **算力投入**：使用了 **32 台 NVIDIA H200 GPU**。
*   **训练时长**：总训练时间约为 **24 小时**。
*   **基础模型**：以 Qwen3-VL-2B-Instruct 为底座进行全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流空间推理基准上进行了全面测试，涵盖了从基础感知到高阶认知（如心理旋转）的多个维度。
*   **消融实验**：系统对比了“仅感知数据”、“仅关系数据”、“感知+基础关系”以及“全量数据（含视点推理）”的效果，验证了各组件的互补性。
*   **客观性**：实验对比了多种参数规模的模型（2B 到 7B），并与当前最强的闭源模型进行了横向对比，结果显示该方法在特定空间任务上甚至超越了 GPT-4o。

### 6. 主要结论与发现
*   **2D 数据可引导 3D 能力**：大规模、高质量的 2D 图像合成数据能够有效弥补 VLM 在 3D 空间感知上的缺失，无需专门的 3D 编码器或传感器输入。
*   **感知与关系的协同**：准确的物体定位（感知）是进行复杂空间关系推理的基础，两者结合能显著提升模型性能。
*   **视点推理的重要性**：引入人体朝向和视点变换数据，使模型能够处理非相机视角的空间逻辑，显著提升了在 OmniSpatial 等高难度基准上的表现。

### 7. 优点
*   **高扩展性**：摆脱了对昂贵 3D 扫描数据的依赖，能够利用无限的互联网图像资源。
*   **任务全面**：涵盖了从简单的框选到复杂的视点切换等 6 类空间任务。
*   **即插即用**：作为纯数据驱动的方法，不需要修改 VLM 的模型架构，具有很强的通用性。

### 8. 不足与局限
*   **精度限制**：基于单目深度估计生成的监督信号是近似的，在处理遮挡严重或复杂几何结构时可能存在误差。
*   **误差累积**：多阶段管线（检测->深度->描述）中任何一环的错误都可能传播到最终的 QA 对中。
*   **缺乏物理交互**：目前主要关注静态几何关系，尚未涉及动态的物理交互或时间序列的空间推理。
*   **感知权衡**：在强化空间推理能力的同时，模型在某些纯 2D 感知任务上可能会出现轻微的性能波动（容量分配问题）。

（完）
