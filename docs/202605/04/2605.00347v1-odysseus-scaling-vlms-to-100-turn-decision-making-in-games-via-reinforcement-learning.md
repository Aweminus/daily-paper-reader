---
title: "Odysseus: Scaling VLMs to 100+ Turn Decision-Making in Games via Reinforcement Learning"
title_zh: Odysseus：通过强化学习将视觉语言模型（VLM）扩展至游戏中 100 轮以上的决策任务
authors: "Chengshuai Shi, Wenzhe Li, Xinran Liang, Yizhou Lu, Wenjia Yang, Ruirong Feng, Seth Karten, Ziran Yang, Zihan Ding, Gabriel Sarch, Danqi Chen, Karthik Narasimhan, Chi Jin"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00347v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 基于强化学习的视觉语言模型长程决策训练
tldr: 本研究针对视觉语言模型（VLM）在长程决策任务中的局限性，提出了Odysseus训练框架。通过在《超级马里奥乐园》等需要100步以上交互的环境中应用强化学习，研究者改进了PPO算法并引入轻量级评论器，显著提升了训练稳定性和采样效率。实验证明，预训练VLM提供的动作先验能大幅减少人工设计需求，使模型在游戏进度上达到前沿模型的3倍以上，并具备良好的泛化能力，为开发具身智能代理提供了新路径。
selection_source: fresh_fetch
motivation: 现有的VLM决策方法大多依赖大规模人类轨迹微调或仅适用于短程任务，难以应对需要上百步交互的复杂长程决策场景。
method: 提出了一种改进的PPO算法，结合轻量级回合级评论器，并利用预训练VLM的动作先验来优化强化学习过程。
result: Odysseus框架在多个游戏关卡中表现优异，平均游戏进度至少是现有前沿模型的3倍，且在跨游戏泛化中表现出良好的鲁棒性。
conclusion: 研究确定了在长程多模态设置下使强化学习稳定有效的关键要素，为将VLM开发为高效的具身智能代理提供了实践指导。
---

## 摘要
鉴于视觉语言模型（VLM）能力的快速增长，将其扩展到视频游戏等交互式决策任务已成为一个极具前景的前沿领域。然而，现有方法要么依赖于对人类轨迹的大规模监督微调（SFT），要么仅在相对短时域的设置（通常约为 20-30 轮）中应用强化学习（RL）。在这项工作中，我们研究了基于 RL 的 VLM 训练，用于《超级马里奥乐园》（Super Mario Land）中的长时域决策，这是一个需要 100 轮以上交互并涉及感知、推理和动作协调的视觉具身环境。我们首先对关键算法组件进行了系统研究，并提出了一种带有轻量级轮次级评论家（turn-level critic）的 PPO 改进变体，与 GRPO 和 Reinforce++ 等无评论家方法相比，该方法显著提高了训练稳定性和样本效率。我们进一步表明，与从头开始训练的经典深度 RL 相比，预训练的 VLM 提供了强大的动作先验，显著提高了 RL 训练期间的样本效率，并减少了对动作工程等人工设计选择的需求。基于这些见解，我们推出了 Odysseus，这是一个用于 VLM 智能体的开放训练框架，在游戏的多个关卡中取得了显著收益，平均游戏进度至少是前沿模型的 3 倍。此外，训练后的模型在游戏内和跨游戏泛化设置下均表现出持续的改进，同时保持了通用领域的能力。总之，我们的结果确定了使 RL 在长时域、多模态设置中稳定且有效的关键要素，并为将 VLM 开发为具身智能体提供了实践指导。

## Abstract
Given the rapidly growing capabilities of vision-language models (VLMs), extending them to interactive decision-making tasks such as video games has emerged as a promising frontier. However, existing approaches either rely on large-scale supervised fine-tuning (SFT) on human trajectories or apply reinforcement learning (RL) only in relatively short-horizon settings (typically around 20--30 turns). In this work, we study RL-based training of VLMs for long-horizon decision-making in Super Mario Land, a visually grounded environment requiring 100+ turns of interaction with coordinated perception, reasoning, and action. We begin with a systematic investigation of key algorithmic components and propose an adapted variant of PPO with a lightweight turn-level critic, which substantially improves training stability and sample efficiency over critic-free methods such as GRPO and Reinforce++. We further show that pretrained VLMs provide strong action priors, significantly improving sample efficiency during RL training and reducing the need for manual design choices such as action engineering, compared to classical deep RL trained from scratch. Building on these insights, we introduce Odysseus, an open training framework for VLM agents, achieving substantial gains across multiple levels of the game and at least 3 times average game progresses than frontier models. Moreover, the trained models exhibit consistent improvements under both in-game and cross-game generalization settings, while maintaining general-domain capabilities. Overall, our results identify key ingredients for making RL stable and effective in long-horizon, multi-modal settings, and provide practical guidance for developing VLMs as embodied agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Odysseus: Scaling VLMs to 100+ Turn Decision-Making in Games via Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何将视觉语言模型（VLM）有效地扩展到需要长程决策（Long-horizon, 100步以上交互）的具身任务中。
*   **研究背景**：现有的VLM智能体训练方法主要依赖大规模人类轨迹的监督微调（SFT），或者仅在极短时域（20-30步）内应用强化学习（RL）。在复杂的视觉环境（如视频游戏）中，VLM面临感知、推理与动作协调的长期挑战。
*   **研究动机**：探索RL在长程任务中训练VLM的稳定性、样本效率以及预训练先验知识的作用。

### 2. 方法论：核心思想与关键技术
论文提出了 **Odysseus** 训练框架，其核心组件包括：
*   **改进的PPO算法**：
    *   **轻量级回合级评论器（Lightweight Turn-level Critic）**：放弃使用昂贵的VLM作为评论器，转而使用轻量级的CNN。这显著降低了显存和计算开销，同时能有效处理视觉状态的价值估计。
    *   **正优势过滤（Positive-advantage Filtering）**：在训练中过滤掉优势值为负的样本（或将其剪裁为零），实验证明这能显著增强长程任务中的优化稳定性。
*   **Odysseus 训练流水线**：
    *   **轻量级监督初始化（SFT）**：利用GPT-o3生成的少量（约5000帧）高质量感知与推理数据进行微调，旨在注入领域知识和增强感知，而非直接学习动作。
    *   **多任务RL与自动课程学习（Auto-Curriculum）**：在多个游戏关卡同时训练，通过“逆轨迹长度加权”动态调整各关卡的采样比例，防止模型在简单关卡过拟合而忽略困难关卡。
*   **交互协议**：采用结构化的思维链（CoT），包含 `<perception>`（感知）、`<reasoning>`（推理）和 `<answer>`（动作）三个标签。

### 3. 实验设计
*   **实验场景**：主要使用 Game Boy 游戏《超级马里奥乐园》（Super Mario Land），该游戏需要精确的视觉定位和超过100步的连续操作。
*   **Benchmark 与对比方法**：
    *   **前沿模型**：GPT-5.4、GLM-4.6V、Gemini-3-Flash、Claude-Sonnet-4.6 等。
    *   **RL算法对比**：GRPO（结果奖励与过程奖励版）、Reinforce++、标准PPO。
    *   **基准对比**：从头开始训练的经典深度RL（CNN-based PPO）。
*   **泛化性测试**：游戏内未见状态、游戏内未见关卡、跨游戏（《超级马里奥兄弟》）以及通用多模态基准（MMMU, MathVision等）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的GPU型号、数量或总训练时长。
*   **训练规模**：提到进行了“数千万次交互样本”的稳定训练。由于采用了轻量级CNN评论器，作者强调该方法比传统的双VLM架构（Actor-Critic均使用大模型）更具计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对RL算法组件（PPO vs GRPO）进行了深入的消融实验。
    *   对比了VLM-based RL与经典RL的样本效率。
    *   在10个不同的游戏关卡上进行了性能评估。
    *   进行了跨游戏和通用能力的全面测试。
*   **充分性评价**：实验设计较为**充分且客观**。通过多轮独立运行（256次运行取平均）和标准差标注，保证了结果的可信度。特别是对“正优势过滤”和“CNN评论器”的消融，有力支撑了其算法改进的有效性。

### 6. 主要结论与发现
*   **算法有效性**：带有轻量级评论器的PPO在长程任务中远优于无评论器的GRPO和Reinforce++。
*   **性能飞跃**：Odysseus 的平均游戏进度是 GPT-5.4 的 5 倍，是 GLM-4.6V 的 3 倍，且远超基座模型。
*   **VLM先验优势**：相比从头训练的经典RL，VLM-based RL的样本效率高出约2倍，且无需复杂的动作空间工程。
*   **能力保持**：在显著提升游戏决策能力的同时，模型在通用多模态任务（如MMMU）上的表现并未下降，证明了该框架不会导致严重的灾难性遗忘。

### 7. 优点
*   **稳定性**：成功解决了VLM在100步以上长程任务中RL训练易崩溃的问题。
*   **高效性**：通过CNN评论器和正优势过滤，实现了高性能与低计算成本的平衡。
*   **泛化强**：展示了从一个游戏到另一个相似游戏的零样本/微调迁移潜力。
*   **实用性**：提供了一个开放的框架，降低了开发具身智能体对大规模专家数据的依赖。

### 8. 不足与局限
*   **环境局限**：目前主要集中在 2D 平台类游戏，尚未在更复杂的 3D 开放世界或现实机器人任务中验证。
*   **动作空间**：虽然减少了工程量，但仍依赖于离散的动作组合，对于需要极高连续控制精度的任务可能需要进一步调整。
*   **负优势样本的争议**：虽然正优势过滤在本文中有效，但在RL理论界，如何处理负样本仍有争议，该结论在其他领域的普适性有待验证。

（完）
