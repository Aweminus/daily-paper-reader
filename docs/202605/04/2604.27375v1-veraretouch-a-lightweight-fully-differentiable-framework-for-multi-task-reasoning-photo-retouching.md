---
title: "VeraRetouch: A Lightweight Fully Differentiable Framework for Multi-Task Reasoning Photo Retouching"
title_zh: VeraRetouch：一种用于多任务推理照片修图的轻量级全可微框架
authors: "Yihong Guo, Youwei Lyu, Jiajun Tang, Yizhuo Zhou, Hongliang Wang, Jinwei Chen, Changqing Zou, Qingnan Fan"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27375v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: VLM作为推理和规划的核心智能体
tldr: 针对现有推理修图模型依赖不可微外部工具、参数冗余且泛化性差的问题，本文提出VeraRetouch。该框架采用0.5B轻量级视觉语言模型作为核心，结合全可微修图渲染器，实现了端到端的像素级优化。通过引入首个百万级专业修图数据集AetherRetouch-1M+和DAPO-AE强化学习策略，模型在保持轻量化、支持移动端部署的同时，在多项基准测试中达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有推理修图方法因依赖不可微外部软件导致优化受阻，且模型参数冗余、泛化能力有限。
method: 提出由0.5B VLM驱动的轻量级框架，结合全可微渲染器和DAPO-AE强化学习策略，并构建了百万级数据集。
result: 在多个基准测试中取得SOTA性能，且模型体积小，支持在移动设备上高效运行。
conclusion: VeraRetouch通过全可微设计和大规模数据驱动，为轻量化、高性能的智能推理修图提供了有效解决方案。
---

## 摘要
推理照片修图（Reasoning photo retouching）已引起广泛关注，它要求模型分析图像缺陷、给出推理过程并执行精确的修图增强。然而，现有方法通常依赖于不可微的外部软件，这造成了优化障碍，并面临参数冗余度高和泛化能力有限的问题。为了应对这些挑战，我们提出了 VeraRetouch，这是一个用于多任务照片修图的轻量级全可微框架。我们采用一个 0.5B 的视觉语言模型（VLM）作为核心智能体，根据指令和场景语义制定修图方案。此外，我们开发了一个全可微的修图渲染器（Retouch Renderer）来替代外部工具，通过光照、全局色彩和特定颜色调整的解耦控制潜变量，实现了直接的端到端像素级训练。为了克服数据稀缺问题，我们推出了 AetherRetouch-1M+，这是首个通过新型逆退化工作流构建的百万级专业修图数据集。此外，我们提出了 DAPO-AE，这是一种增强自主审美认知的强化学习后训练策略。大量实验表明，VeraRetouch 在多个基准测试中达到了最先进的性能，同时保持了显著更小的模型体积，从而支持移动端部署。我们的代码和模型已在 https://github.com/OpenVeraTeam/VeraRetouch 公开。

## Abstract
Reasoning photo retouching has gained significant traction, requiring models to analyze image defects, give reasoning processes, and execute precise retouching enhancements. However, existing approaches often rely on non-differentiable external software, creating optimization barriers and suffering from high parameter redundancy and limited generalization. To address these challenges, we propose VeraRetouch, a lightweight and fully differentiable framework for multi-task photo retouching. We employ a 0.5B Vision-Language Model (VLM) as the central intelligence to formulate retouching plans based on instructions and scene semantics. Furthermore, we develop a fully differentiable Retouch Renderer that replaces external tools, enabling direct end-to-end pixel-level training through decoupled control latents for lighting, global color, and specific color adjustments. To overcome data scarcity, we introduce AetherRetouch-1M+, the first million-scale dataset for professional retouching, constructed via a new inverse degradation workflow. Furthermore, we propose DAPO-AE, a reinforcement learning post-training strategy that enhances autonomous aesthetic cognition. Extensive experiments demonstrate that VeraRetouch achieves state-of-the-art performance across multiple benchmarks while maintaining a significantly smaller footprint, enabling mobile deployment. Our code and models are publicly available at https://github.com/OpenVeraTeam/VeraRetouch.

---

## 论文详细总结（自动生成）

### 论文总结：VeraRetouch - 轻量级全可微多任务推理照片修图框架

#### 1. 核心问题与整体含义（研究动机和背景）
推理照片修图（Reasoning Photo Retouching）要求模型不仅能增强图像，还能理解用户指令、分析图像缺陷并给出逻辑化的修图方案。
*   **现有挑战**：
    1.  **优化障碍**：现有方法（如 JarvisArt, MonetGPT）依赖 Lightroom 等外部不可微工具，无法进行端到端的像素级梯度回传。
    2.  **参数冗余**：通常使用巨大的多模态模型，推理速度慢，难以在移动端部署。
    3.  **数据稀缺**：高质量的专业修图成对数据（原图-修图后）规模较小（如 MIT-Adobe FiveK 仅 5000 张），限制了模型的泛化能力。
*   **研究目标**：开发一个轻量级、全可微、支持多任务（自动、风格化、参数化修图）且可部署于移动端的推理修图框架。

#### 2. 方法论
VeraRetouch 核心由 **0.5B VLM 智能体**、**全可微修图渲染器** 和 **强化学习策略** 组成：
*   **核心架构**：
    *   **VLM 脑（0.5B FastVLM）**：负责分析图像语义和用户指令，生成结构化的推理链（内容分析 -> 问题诊断 -> 修图计划）。
    *   **全可微修图渲染器（Retouch Renderer）**：基于轻量级 MLP 实现，替代外部软件。它通过三个解耦的潜变量控制：**光照（Lighting）**、**全局色彩（Global Color）**和**特定颜色调整（Specific Color）**。
    *   **修图适配器（Retouch Adaptor）**：将 VLM 生成的特殊 Token 特征对齐到渲染器的潜变量空间。
*   **关键技术细节**：
    *   **AetherRetouch-1M+ 数据集**：通过“逆向退化”工作流构建。利用现有的专家修图对作为参考，将高质量摄影作品逆向生成具有真实缺陷的“原图”，从而构建了首个百万级数据集。
    *   **DAPO-AE 强化学习**：提出解耦剪枝与动态采样策略优化（DAPO-AE），通过格式奖励、图像相似度奖励和审美奖励（LAION/Q-Align 分数）进一步提升模型的审美认知。
    *   **端到端训练流程**：分为领域对齐预训练、推理监督微调（RSFT）和强化学习后训练（RPT）三个阶段。

#### 3. 实验设计
*   **任务场景**：
    1.  **Auto-Retouch**：仅输入图像，自主修图。
    2.  **Style-Retouch**：根据风格化指令（如“浪漫甜粉风”）修图。
    3.  **Param-Retouch**：根据具体参数指令（如“曝光+2”）修图。
*   **数据集与 Benchmark**：
    *   测试集：MIT-Adobe FiveK-Bench、PPR10K-Bench、自定义的 Aether-Bench（包含自动、风格、参数三个子集）。
*   **对比方法**：
    *   传统/监督方法：RSFNet, AdaInt, NamedCurves。
    *   扩散模型：Flux.1 Kontext, Qwen-Image。
    *   VLM 代理方法：MonetGPT, JarvisArt, Nano Banana。
*   **评价指标**：PSNR, SSIM, LPIPS（保真度）；LAION, Q-Align, LIQE（审美质量）；DISTS, GMSD, TD（纹理保持度）。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H20 GPU**。
*   **训练时长**：
    *   渲染器/编码器训练：200k 步。
    *   领域对齐预训练：600k 步。
    *   监督微调（SFT）：500k 步。
    *   强化学习（DAPO-AE）：13k 步。
*   **部署测试**：在 Macbook Air (M4) 和 iPhone 16 Pro 上进行了推理延迟测试，证明了移动端可行性。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖 5 个以上主流基准）和定性视觉展示。
*   **消融实验**：非常充分，涵盖了：
    *   **潜变量预测 vs. 参数预测**：证明了全可微渲染器的优越性。
    *   **数据规模缩放**：验证了百万级数据对性能的持续提升。
    *   **DAPO-AE 有效性**：通过用户调研证明 RL 显著提升了审美偏好。
    *   **解耦能力验证**：通过 Mask 实验证明了光照、全局色、局部色三个维度的独立控制能力。
*   **客观性**：引入了 38 人的用户盲测（User Study），从审美、指令对齐、纹理一致性三个维度评分，结果具有统计学意义。

#### 6. 主要结论与发现
*   **性能领先**：VeraRetouch 在所有基准测试中均达到 SOTA，尤其在参数化修图（PSNR > 30dB）和审美评分上表现突出。
*   **轻量高效**：仅需 0.5B 参数，推理速度比基于扩散模型或大型 Agent 的方法快 2.5 倍以上。
*   **全可微优势**：直接在像素空间优化比通过 API 预测离散参数更精确，避开了离散化带来的精度损失。
*   **泛化性**：通过百万级数据训练，模型能处理从未见过的复杂场景和多样化语言指令。

#### 7. 优点
*   **全流程可微**：解决了 VLM 与修图工具之间梯度断裂的痛点，实现了真正的端到端优化。
*   **工业价值高**：模型极小（0.5B），推理快，且支持 4K 超高清图像处理，具备极强的移动端落地潜力。
*   **数据贡献**：开源了首个百万级专业修图数据集 AetherRetouch-1M+，填补了领域空白。
*   **可解释性**：通过结构化推理链，用户可以清晰看到模型“为什么要这么修”。

#### 8. 不足与局限
*   **局部修图限制**：目前模型主要侧重于全局和特定颜色的调整，对于复杂的局部区域修图（如特定物体的精细遮罩处理）能力仍有限。
*   **视频一致性**：虽然初步探索了视频修图，但主要依赖关键帧潜变量的应用，缺乏专门针对视频时序连续性的深度建模。
*   **参数分布偏差**：在处理真实专家参数时，由于专家习惯的复杂相关性，模型在某些极端专业风格上的拟合仍有提升空间。

（完）
