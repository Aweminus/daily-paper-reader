---
title: "LaST-R1: Reinforcing Robotic Manipulation via Adaptive Physical Latent Reasoning"
title_zh: LaST-R1：通过自适应物理潜空间推理强化机器人操纵
authors: "Hao Chen, Jiaming Liu, Zhonghao Yan, Nuowei Han, Renrui Zhang, Chenyang Gu, Jialin Gao, Ziyu Guo, Siyuan Qian, Yinxi Wang, Peng Jia, Shanghang Zhang, Pheng-Ann Heng"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.28192v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: VLA 模型中“行动前潜在推理”的强化学习后训练框架
tldr: "针对机器人基础模型在动态环境中缺乏适应性的问题，本文提出LaST-R1强化学习框架。该框架通过LAPO算法将潜在思维链（CoT）推理嵌入强化学习优化循环，实现“先推理后行动”的策略。此外，引入自适应推理机制以动态调整推理长度。实验表明，LaST-R1在LIBERO基准测试中达到99.9%的成功率，并在真实世界复杂任务中显著优于现有SFT方法，展现了极强的泛化能力。"
selection_source: fresh_fetch
motivation: 现有的潜在推理视觉-语言-动作模型多局限于静态模仿学习，难以应对动态环境中的复杂物理交互和泛化需求。
method: 提出LaST-R1框架，利用LAPO算法联合优化潜在推理与动作生成，并引入自适应思维链机制动态调整推理深度。
result: "在LIBERO基准上实现99.9%的成功率，且在真实世界单/双臂任务中比SOTA监督微调方法平均提升了22.5%。"
conclusion: LaST-R1证明了通过强化学习优化潜在推理过程能显著提升机器人操作的鲁棒性、适应性及跨场景泛化性能。
---

## 摘要
机器人基础模型需要对复杂的视觉场景进行推理，以便在动态环境中执行自适应动作。尽管近期关于潜空间推理视觉-语言-动作（VLA）模型的研究已展示出捕捉细粒度物理动力学的能力，但它们仍主要局限于静态模仿学习，严重限制了其自适应能力和泛化性。在本文中，我们提出了 LaST-R1，这是一种新型强化学习（RL）后训练框架，旨在有效利用“先潜空间推理后行动”的策略。具体而言，我们提出了潜空间到动作策略优化（LAPO），这是一种核心强化学习算法，可联合优化潜空间推理过程和动作生成。通过将潜空间思维链（CoT）推理显式地直接嵌入到强化学习优化循环中，LAPO 激发了深刻的物理世界建模，进而驱动了在交互式环境中的鲁棒执行。此外，引入了一种自适应潜空间 CoT 机制，允许策略根据不同的环境状态动态调节其推理视野。实验表明，LaST-R1 在仅进行一次监督预热的情况下，在 LIBERO 基准测试中实现了接近完美的 99.9% 平均成功率，与之前的最先进（SOTA）方法相比，显著提高了收敛速度和性能。在真实世界部署中，LaST-R1 在包括单臂和双臂设置在内的四个复杂任务中，比 SOTA 监督微调方法平均提升了高达 22.5%。最后，LaST-R1 在模拟和真实世界环境中均表现出强大的泛化能力。

## Abstract
Robotic foundation models require reasoning over complex visual scenes to execute adaptive actions in dynamic environments. While recent studies on latent-reasoning Vision-Language-Action (VLA) models have demonstrated the capability to capture fine-grained physical dynamics, they remain predominantly confined to static imitation learning, severely limiting their adaptability and generalization. In this paper, we present LaST-R1, a novel reinforcement learning (RL) post-training framework designed to effectively harness "latent reasoning-before-acting" policies. Specifically, we propose Latent-to-Action Policy Optimization (LAPO), a core RL algorithm that jointly optimizes the latent reasoning process and the action generation. By explicitly embedding latent Chain-of-Thought (CoT) reasoning directly within the RL optimization loop, LAPO stimulates profound physical world modeling, which in turn drives robust execution in interactive environments. Furthermore, an adaptive latent CoT mechanism is introduced, allowing the policy to dynamically modulate its reasoning horizon based on diverse environment states. Experiments show that LaST-R1 achieves a near-perfect 99.9% average success rate on the LIBERO benchmark with only one-shot supervised warm-up, significantly improving convergence speed and performance over prior state-of-the-art (SOTA) methods. In real-world deployments, LaST-R1 yields up to a 22.5% average improvement over SOTA supervised fine-tuning approach across four complex tasks, including both single-arm and dual-arm settings. Finally, LaST-R1 demonstrates strong generalization across simulated and real-world environments.

---

## 论文详细总结（自动生成）

### 论文总结：LaST-R1：通过自适应物理潜空间推理强化机器人操纵

#### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型虽然开始引入思维链（CoT）推理，但大多局限于静态的**模仿学习（SFT）**。这种方式依赖高质量专家数据，难以应对动态环境中的误差累积，且泛化能力有限。
*   **核心挑战**：如何在强化学习（RL）框架下，不仅优化机器人的动作执行，还同时优化其内部的“物理推理”过程，使模型在行动前能更准确地建模物理动力学。
*   **整体含义**：本文提出了 **LaST-R1**，这是一个专门为“先推理后行动”策略设计的强化学习后训练框架，通过在潜空间（Latent Space）进行物理推理并结合环境反馈，显著提升了机器人的操作精度和泛化性。

#### 2. 方法论
*   **核心思想**：将潜空间推理嵌入 RL 优化循环，使环境奖励（Reward）能够直接塑造模型的推理逻辑和动作生成。
*   **关键技术细节**：
    *   **LaST-R1 VLA 架构**：基于 Qwen3-VL-4B 主干，集成 SigLIP2 视觉编码器。模型先自回归生成 $N_z$ 个潜空间推理嵌入（Latent Embeddings），再并行解码动作序列。
    *   **潜空间表示**：利用 **DINOv3** 视觉基础模型的 `<CLS>` 标记作为潜空间目标，这些目标包含丰富的语义和空间先验，且通过离线计算避免了推理延迟。
    *   **LAPO 算法（Latent-to-Action Policy Optimization）**：这是本文的核心 RL 算法。它将潜空间嵌入视为隐式决策变量，通过统一的步级似然比（Step-level Likelihood Ratio）联合优化推理和动作。
        *   对于离散动作，使用标准的对数概率。
        *   对于连续潜空间嵌入，通过各向同性高斯分布近似似然比，使奖励信号能拉近当前推理与“成功路径”推理的距离。
    *   **自适应潜空间 CoT 机制**：引入 `<latent_end>` 标记，允许模型根据任务复杂度动态决定推理长度（如简单任务早退出，复杂任务深推理），平衡了认知能力与计算效率。

#### 3. 实验设计
*   **模拟环境**：使用 **LIBERO** 基准测试，涵盖四个子集：LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务），共 40 个任务。
*   **真实世界场景**：使用 Franka Research 3 机械臂（单臂及双臂），执行四个复杂任务：插入六角块、拉开包拉链、海绵擦拭花瓶、打开瓶盖。
*   **对比方法**：
    *   **SFT 基准**：OpenVLA、π0、π0.5、OpenVLA-OFT 等。
    *   **RL 基准**：GRAPE (DPO)、VLA-RL (PPO)、πRL (PPO)、SimpleVLA-RL (GRPO) 等。
*   **实验设置**：采用“单样本（One-shot）预热 + 在线 RL”的极低数据模式。

#### 4. 资源与算力
*   **算力设备**：
    *   **训练**：使用了 **8 张 NVIDIA H20 GPU**。
    *   **推理/部署**：真实世界部署使用单张 NVIDIA RTX 4090 GPU。
*   **训练细节**：
    *   预训练阶段使用了 40 万条轨迹（2800 万帧数据）。
    *   LIBERO 模拟训练每个子集约 10K 次迭代。
    *   RL 阶段使用 verl 框架结合 Ray 和 FSDP（全分片数据并行）进行分布式训练。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在模拟器中进行了数千次 Rollouts，并在 50 个预留测试场景中评估成功率。
    *   真实世界中每个任务进行 20 次测试，并包含三种泛化场景（未见物体、背景变化、光照变化）。
    *   **消融实验**：针对潜空间表示方法（DINOv3 vs 卷积 vs Q-Former）、推理长度（1-8 标记）、损失函数权重（$\lambda_1, \lambda_2, \lambda_3$）以及自适应机制进行了详尽测试。
*   **充分性评价**：实验设计非常充分，不仅涵盖了标准基准，还深入探讨了 OOD（分布外）泛化能力，对比了多种 SOTA 模型，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能突破**：LaST-R1 在 LIBERO 上达到了 **99.9%** 的平均成功率，尤其在长程任务（LIBERO-Long）上表现远超前人。
*   **数据效率**：仅需 1 条专家轨迹进行预热，即可通过 RL 达到极高水平。
*   **推理价值**：实验证明，优化潜空间推理过程能显著加快 RL 的收敛速度，并使模型在面对未见过的场景时表现出更强的鲁棒性。
*   **自适应性**：模型学会了在简单状态下减少推理步骤，在复杂状态下增加推理，实现了效率与性能的平衡。

#### 7. 优点
*   **创新性强**：首次将潜空间 CoT 推理与 RL 后训练深度结合，提出了 LAPO 这一新颖的联合优化算法。
*   **物理扎根**：通过 DINOv3 引导潜空间推理，使模型的“思考”具有明确的物理含义，而非盲目的标记生成。
*   **泛化性优异**：在真实世界中面对光照、背景和物体的剧烈变化，性能下降极小，展现了基础模型的潜力。

#### 8. 不足与局限
*   **推理长度限制**：虽然是自适应的，但最大推理长度仍被硬编码为 8，且退出位置是预设的候选点，尚未实现完全自由的动态推理。
*   **硬件覆盖**：目前主要在 Franka 机械臂上验证，尚未在灵巧手或全尺寸人形机器人等更复杂的构型上进行大规模测试。
*   **安全保障**：论文提到 VLA 模型在物理环境中存在不可预测行为的风险，但在 RL 训练过程中缺乏显式的安全约束机制（Safety Guardrails）。

（完）
