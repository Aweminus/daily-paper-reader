---
title: "OpenSearch-VL: An Open Recipe for Frontier Multimodal Search Agents"
title_zh: OpenSearch-VL：前沿多模态搜索智能体的开源方案
authors: "Shuang Chen, Kaituo Feng, Hangting Chen, Wenxuan Huang, Dasen Dai, Quanxin Shou, Yunlong Lin, Xiangyu Yue, Shenghua Gao, Tianyu Pang"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05185v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 利用智能体强化学习训练多模态搜索智能体
tldr: 针对多模态搜索智能体难以复现的问题，本文推出了OpenSearch-VL，一套完整的开源训练方案。该方案通过维基百科路径采样等技术构建了高质量的SFT和RL数据集，并设计了包含搜索与图像处理的多元工具环境。此外，提出了一种致命感知的GRPO算法以应对工具调用失败。实验表明，该模型在多个基准测试中表现优异，性能可媲美闭源商业模型，极大地推动了多模态深度搜索的研究。
selection_source: fresh_fetch
motivation: 现有的顶尖多模态搜索智能体由于缺乏高质量开源数据、透明的合成管线和详细训练配方，导致其难以被学术界复现。
method: 提出了一套包含高质量数据构建管线、多元化工具环境以及致命感知GRPO强化学习算法的完整开源训练方案。
result: OpenSearch-VL在七个基准测试中平均提升超过10分，在多项任务上达到了与商业闭源模型相当的水平。
conclusion: 本研究通过全面开源数据、代码和模型，为开发高性能、可复现的多模态深度搜索智能体提供了重要的技术路径。
---

## 摘要
深度搜索已成为前沿多模态智能体的一项关键能力，使模型能够通过主动搜索、证据验证和多步推理来解决复杂问题。尽管取得了快速进展，但顶尖的多模态搜索智能体仍然难以复现，这在很大程度上归因于缺乏开源的高质量训练数据、透明的轨迹合成流水线或详细的训练方案。为此，我们推出了 OpenSearch-VL，这是一个通过智能体强化学习训练前沿多模态深度搜索智能体的全开源方案。首先，我们策划了一个专门的流水线，通过维基百科路径采样、模糊实体重写和源锚点视觉定位来构建高质量训练数据，这些方法共同减少了捷径学习和单步检索崩溃。基于该流水线，我们策划了两个训练数据集：用于有监督微调（SFT）的 SearchVL-SFT-36k 和用于强化学习（RL）的 SearchVL-RL-8k。此外，我们设计了一个多样化的工具环境，统一了文本搜索、图像搜索、OCR、裁剪、锐化、超分辨率和透视校正，使智能体能够将主动感知与外部知识获取相结合。最后，我们提出了一种多轮致命错误感知的 GRPO 训练算法，该算法通过屏蔽失败后的 token 来处理级联工具故障，同时通过单侧优势钳位保留失败前有用的推理。基于该方案，OpenSearch-VL 实现了显著的性能提升，在七个基准测试中平均提高了 10 个百分点以上，并在多项任务上取得了与闭源商业模型相当的结果。我们将发布所有数据、代码和模型，以支持多模态深度搜索智能体的开源研究。

## Abstract
Deep search has become a crucial capability for frontier multimodal agents, enabling models to solve complex questions through active search, evidence verification, and multi-step reasoning. Despite rapid progress, top-tier multimodal search agents remain difficult to reproduce, largely due to the absence of open high-quality training data, transparent trajectory synthesis pipelines, or detailed training recipes. To this end, we introduce OpenSearch-VL, a fully open-source recipe for training frontier multimodal deep search agents with agentic reinforcement learning. First, we curated a dedicated pipeline to construct high-quality training data through Wikipedia path sampling, fuzzy entity rewriting, and source-anchor visual grounding, which jointly reduce shortcuts and one-step retrieval collapse. Based on this pipeline, we curate two training datasets, SearchVL-SFT-36k for SFT and SearchVL-RL-8k for RL. Besides, we design a diverse tool environment that unifies text search, image search, OCR, cropping, sharpening, super-resolution, and perspective correction, enabling agents to combine active perception with external knowledge acquisition. Finally, we propose a multi-turn fatal-aware GRPO training algorithm that handles cascading tool failures by masking post-failure tokens while preserving useful pre-failure reasoning through one-sided advantage clamping. Built on this recipe, OpenSearch-VL delivers substantial performance gains, with over 10-point average improvements across seven benchmarks, and achieves results comparable to proprietary commercial models on several tasks. We will release all data, code, and models to support open research on multimodal deep search agents.

---

## 论文详细总结（自动生成）

以下是对论文《OpenSearch-VL: An Open Recipe for Frontier Multimodal Search Agents》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：多模态深度搜索（Multimodal Deep Search）是前沿智能体的核心能力，要求模型能主动检索证据、验证事实并进行多步推理。
*   **核心痛点**：尽管商业模型（如 GPT-5, Gemini 2.5）表现强劲，但其训练数据、合成管线和训练配方均不透明，导致学术界难以复现高性能的多模态搜索智能体。
*   **论文目标**：提供一套全开源的“配方”（Open Recipe），包括高质量数据构建管线、多样化工具环境和改进的强化学习算法，旨在降低复现门槛并推动开源社区的研究。

### 2. 方法论
*   **核心思想**：通过模拟人类“先修复感知、再检索知识”的行为逻辑，结合改进的群体相对策略优化（GRPO）算法，训练具备长程推理和工具调用能力的智能体。
*   **关键技术细节**：
    *   **数据管线**：从维基百科超链接图出发，进行多跳路径采样。通过**模糊实体重写**（将具体名称改为属性描述）和**源锚点视觉定位**（将搜索起点锚定在图像中），强制模型必须通过多步搜索而非记忆来回答问题，有效防止了“检索捷径”。
    *   **工具环境**：构建了包含检索类（文本/图像搜索）和感知增强类（OCR、裁剪、锐化、超分、透视校正）的综合工具箱，支持智能体处理模糊、倾斜等低质量输入。
    *   **Fatal-aware GRPO 算法**：
        *   **致命步检测**：识别导致轨迹崩溃的连续工具错误。
        *   **致命感知掩码**：在优化时屏蔽失败步之后的噪声 Token。
        *   **单侧优势钳位（One-sided Advantage Clamping）**：即使轨迹最终失败，只要前期推理步骤优于群体平均水平，就保留其正向梯度，避免因后期崩溃而惩罚前期的正确逻辑。

### 3. 实验设计
*   **数据集**：
    *   **SearchVL-SFT-36k**：用于有监督微调的 3.6 万条高质量专家轨迹。
    *   **SearchVL-RL-8k**：用于强化学习训练的 8000 个任务提示词。
*   **Benchmark**：在 7 个知识密集型基准上进行评估，包括 SimpleVQA、VDR、MMSearch、LiveVQA、BrowseComp-VL、FVQA 和 InfoSeek。
*   **对比方法**：
    *   **直接推理**：GPT-4o, GPT-5, Gemini-2.5-Pro, Claude-3.7/4, Qwen3-VL。
    *   **RAG 工作流**：将检索结果直接喂给模型。
    *   **智能体工作流**：对比了 SenseNova-MARS, WebWatcher, DeepEyes-v2 等现有开源 SOTA 智能体。

### 4. 资源与算力
*   **硬件型号**：Nvidia H20 GPU。
*   **SFT 阶段**：使用 256 张 H20。8B 模型耗时约 2 天，30B-A3B 模型耗时约 4 天。
*   **RL 阶段**：使用 64 张 H20。进行约 200 个优化步，耗时约 10 天。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 8B 到 32B 不同参数规模的模型，并在 7 个不同侧重点的基准上验证，实验覆盖面广。
*   **消融实验**：非常充分。分别针对数据管线的四个关键组件（锚点定位、重写、过滤、增强）以及 RL 算法的三个核心设计（不同掩码策略、钳位机制）进行了对比测试。
*   **客观性**：采用了统一的 GPT-4o 作为评判员（LLM-as-a-Judge），并遵循了 VDR-Bench 的标准评估协议，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：OpenSearch-VL 在 7 个基准上平均提升超过 10 分。OpenSearch-VL-32B 达到了 63.7 的平均分，显著优于基座模型（48.0）并媲美顶级商业模型。
*   **RL 的必要性**：强化学习显著提升了模型在长程工具调用中的鲁棒性，Fatal-aware GRPO 相比传统 GRPO 在处理复杂任务时表现更稳健。
*   **感知与搜索的协同**：引入图像修复工具（如超分、锐化）能有效提升模型在处理真实世界低质量图像时的搜索成功率。

### 7. 优点
*   **全栈开源**：不仅开源模型，还开源了极其关键的数据合成管线和 RL 训练代码，对社区贡献巨大。
*   **算法创新**：提出的单侧优势钳位机制巧妙解决了强化学习中“信用分配”难题，尤其适用于多步工具调用场景。
*   **数据质量高**：通过维基百科路径采样构建的数据集逻辑严密，有效解决了模型“偷懒”不搜索的问题。

### 8. 不足与局限
*   **外部 API 依赖**：模型运行依赖 Serper 搜索和 PaddleX OCR 等外部服务，API 的变动或网络延迟可能影响复现的数值一致性。
*   **奖励模型成本**：RL 阶段依赖 GPT-4o/5.4 进行打分，对于完全没有商业 API 预算的研究者来说，复现成本依然较高。
*   **视觉反馈细粒度不足**：目前的奖励机制主要基于最终答案和搜索查询质量，对中间视觉操作（如裁剪位置是否精确）的直接反馈较少。

（完）
