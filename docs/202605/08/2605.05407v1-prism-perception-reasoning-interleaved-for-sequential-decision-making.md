---
title: "PRISM: Perception Reasoning Interleaved for Sequential Decision Making"
title_zh: PRISM：感知与推理交织的序列决策框架
authors: "Mohamed Salim Aissi, Clemence Grislain, Clement Romac, Laure Soulier, Mohamed Chetouani, Olivier Sigaud, Nicolas Thome"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05407v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于VLM的具身智能体用于序列决策
tldr: 针对具身智能体在多模态环境中感知与决策脱节的问题，本文提出PRISM框架。该框架通过动态问答（DQA）机制将VLM感知与LLM决策紧密耦合，由LLM主动询问并引导VLM提取任务关键信息，从而生成精准的场景理解。在ALFWorld和R2R基准测试中，PRISM显著优于现有模型，证明了交互式感知在复杂决策任务中的有效性且无需人工干预。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在具身任务中往往忽略关键信息，导致感知、推理与决策之间存在明显的断层。
method: 提出一种动态问答流水线，通过LLM对VLM描述的批判与主动提问，实现目标导向的闭环感知交互。
result: 在ALFWorld和R2R基准测试中，PRISM的表现显著超越了当前最先进的基于图像的模型。
conclusion: 紧密耦合感知与决策的交互式机制能有效提升具身智能体在复杂环境下的任务执行能力。
---

## 摘要
将基于大语言模型（LLM）的具身智能体从纯文本环境扩展到复杂的多模态设置仍然是一个重大挑战。最近的研究指出，独立视觉语言模型（VLM）中存在感知-推理-决策鸿沟，这些模型往往会忽略任务关键信息。在本文中，我们介绍了 PRISM，这是一个通过动态问答（DQA）流水线将感知（VLM）与决策（LLM）紧密耦合的框架。LLM 不再是被动地接受 VLM 的描述，而是对其进行评判，通过面向目标的问题对 VLM 进行探测，并合成简洁的图像描述。这种闭环交互产生了一种敏锐的、任务驱动的场景理解。我们在 ALFWorld 和 Room-to-Room (R2R) 基准测试上评估了 PRISM。结果表明：(1) PRISM 显著优于最先进的基于图像的模型；(2) 我们的交互式面向目标感知流水线带来了系统性且实质性的提升；(3) PRISM 是全自动的，无需手工设计问题或答案。

## Abstract
Scaling LLM-based embodied agents from text-only environments to complex multimodal settings remains a major challenge. Recent work identifies a perception-reasoning-decision gap in standalone Vision-Language Models (VLMs), which often overlook task-critical information. In this paper, we introduce PRISM, a framework that tightly couples perception (VLM) and decision (LLM) through a dynamic question-answer (DQA) pipeline. Instead of passively accepting the VLM's description, the LLM critiques it, probes the VLM with goal-oriented questions, and synthesizes a compact image description. This closed-loop interaction yields a sharp, task-driven understanding of the scene. We evaluate PRISM on the ALFWorld and Room-to-Room (R2R) benchmarks. We show that: (1) PRISM significantly outperforms state-of-the-art image-based models, (2) our Interactive goal-oriented perception pipeline yields systematic and substantial gains, and (3) PRISM is fully automatic, eliminating the need for handcrafted questions or answers.

---

## 论文详细总结（自动生成）

### 论文总结：PRISM —— 感知与推理交织的序列决策框架

#### 1. 核心问题与研究动机
*   **核心问题**：现有的具身智能体（Embodied Agents）在处理视觉环境时，感知模块（VLM）与决策模块（LLM）往往是松散耦合的。VLM 生成的场景描述通常是通用的、任务无关的，容易忽略对完成特定目标至关重要的微小视觉细节，导致“感知-推理-决策”之间存在断层。
*   **研究动机**：借鉴人类感知与决策深度交织的特性（即感官输入会优先处理与当前目标相关的信息），作者旨在建立一种动态交互机制，让决策模块能够主动引导感知模块提取关键信息，从而缩小视觉智能体与拥有“完美文本描述”的智能体之间的性能差距。

#### 2. 方法论
PRISM 的核心思想是通过**动态问答（DQA）流水线**实现感知与推理的闭环交互。
*   **交互式目标导向感知**：
    1.  **初始描述**：VLM 首先生成一个通用的场景描述 $d_t^i$。
    2.  **动态提问**：LLM（推理模块）对比初始描述与任务目标 $g$，识别信息缺口，并向 VLM 提出针对性的问题 $Q_t$（例如：“你看到桌子上有鸡蛋吗？”）。
    3.  **视觉回传**：VLM 针对这些具体问题重新观察图像并给出回答 $\Lambda_t$。
    4.  **语义合成**：LLM 将初始描述、问题和答案合成为一个简洁、任务驱动的最终描述 $d_t^f$。这一步避免了简单拼接导致的冗长和噪声。
*   **序列决策**：
    *   使用带有 **LoRA 适配器**的 LLM 作为策略网络，根据 $d_t^f$、任务目标和历史记录生成动作 $a_t$。
*   **训练策略**：
    *   采用两阶段训练：首先通过**行为克隆（BC）**进行预训练，然后在视觉环境中通过**近端策略优化（PPO）**算法进行在线强化学习（RL）微调。

#### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：一个合成环境，要求智能体在家庭场景中执行多步物体操纵任务（如清洁、加热物体）。
    *   **Room-to-Room (R2R)**：一个基于真实室内全景图的视觉语言导航（VLN）基准。
*   **对比方法**：
    *   **有文本环境访问权限（Oracle）**：ReAct, Reflexion, DEPS, EMMA 等（这些模型能获取模拟器提供的完美文本信息）。
    *   **无文本环境访问权限（视觉基准）**：MiniGPT-4, RL4VLM, Idefics2, VIPER, LangNav, NavGPT, DiscussNav 等。

#### 4. 资源与算力
*   **算力资源**：实验使用了 **IDRIS HPC** 资源（由 GENCI 分配）。
*   **硬件细节**：在推理延迟测试中，使用了单张 **NVIDIA H100 GPU**。
*   **训练细节**：BC 阶段学习率为 $10^{-4}$，RL 阶段为 $5 \times 10^{-6}$。
*   **延迟说明**：PRISM 的单步推理延迟约为 6.35 秒，比不带 DQA 的原始感知（2.14 秒）高出约 3 倍，但通过减少无效动作步骤（平均每集减少 18% 的步骤）补偿了部分时间开销。

#### 5. 实验数量与充分性
*   **实验规模**：在 ALFWorld 的 6 类任务上进行了全面评估，并在 R2R 导航任务上验证了通用性。
*   **消融实验**：
    *   对比了单体 VLM 与模块化架构。
    *   对比了不同的信息融合策略（简单拼接 vs. LLM 合成）。
    *   对比了自动生成问题与人工标注（Oracle）问题的效果。
    *   验证了不同 VLM 后端（Idefics2, InternVL）的稳定性。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注成功率，还深入分析了描述的准确性（使用 GPT-4 作为裁判）、幻觉率以及强化学习的训练曲线，证明了 DQA 机制的系统性增益。

#### 6. 主要结论与发现
*   **性能领先**：PRISM 在无文本辅助的方法中表现最优，在 ALFWorld 上的平均成功率达到 80%，显著缩小了与拥有“上帝视角”文本描述模型的差距。
*   **感知增强**：简单的“目标感知感知”（将目标直接喂给 VLM）反而会增加幻觉，而 PRISM 的 DQA 闭环机制能显著提升描述的准确性和任务相关性。
*   **合成优于拼接**：LLM 对问答对进行语义合成，比 DiscussNav 等方法采用的简单文本拼接更有效，能显著降低输入噪声。

#### 7. 优点
*   **全自动闭环**：无需人工预定义问题或依赖 Oracle 答案，实现了真正的端到端自主感知。
*   **任务驱动**：通过 LLM 的批判性思维，强制 VLM 关注任务关键细节，有效解决了 VLM 观察不细致的问题。
*   **架构通用**：可以灵活更换底层的 VLM 或 LLM 模型，且在导航和物体操纵两个迥异的领域均表现出色。

#### 8. 不足与局限
*   **推理延迟**：由于需要多次调用 VLM 和 LLM 进行交互（提问、回答、合成），单步决策的时间成本较高，可能限制其在实时性要求极高的机器人场景中的应用。
*   **环境先验依赖**：虽然不依赖人工问题，但 LLM 生成问题的质量仍受限于其内在的常识，对于某些具有特殊物理规则的虚拟环境，可能无法提出最有效的问题。
*   **算力需求**：训练和运行这种多模块交互框架对显存和计算能力有较高要求。

（完）
