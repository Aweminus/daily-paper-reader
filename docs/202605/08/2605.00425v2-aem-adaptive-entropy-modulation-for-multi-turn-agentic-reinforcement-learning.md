---
title: "AEM: Adaptive Entropy Modulation for Multi-Turn Agentic Reinforcement Learning"
title_zh: AEM：面向多轮智能体强化学习的自适应熵调制
authors: "Haotian Zhao, Songlin Zhou, Yuxin Zhang, Stephen S. -T. Yau, Wenyu Zhang, Lun Tian, Tianshu Zhu, Yifeng Huang, Yucheng Zeng, Jingnan Gu, Daxiang Dong, Jianmin Wu"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00425v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 针对多轮任务中LLM智能体的智能体强化学习，非机器人重点
tldr: 针对大语言模型智能体在多轮强化学习中面临的稀疏奖励和信用分配难题，本文提出了一种无需额外监督的自适应熵调制方法（AEM）。该方法将熵动力学分析从 Token 级别提升至响应级别，通过响应级不确定性代理来动态缩放优势函数，从而优化探索与利用的平衡。在 ALFWorld、WebShop 和 SWE-bench 等多个基准测试中，AEM 显著提升了不同规模模型的性能，为长轨迹任务提供了高效的信用分配方案。
selection_source: fresh_fetch
motivation: 现有的强化学习智能体在长轨迹任务中难以处理稀疏奖励，且引入中间监督信号会增加复杂性并限制泛化能力。
method: 提出 AEM 方法，通过在响应级别分析熵动力学并利用不确定性代理动态调整优势函数，实现从探索到利用的自然过渡。
result: "在 ALFWorld、WebShop 及 SWE-bench-Verified 等任务上，AEM 持续优于强基线模型，在软件工程 RL 框架中实现了 1.4% 的性能提升。"
conclusion: AEM 提供了一种高效且无需额外监督的信用分配方案，通过响应级熵调制有效提升了多轮智能体强化学习的训练效率和效果。
---

## 摘要
强化学习 (RL) 显著提升了大语言模型 (LLM) 智能体与环境交互及解决多轮任务的能力。然而，有效的智能体强化学习仍面临挑战：仅基于结果的稀疏奖励在长交互轨迹中为单个步骤分配信用（credit assignment）时提供的指导有限。现有方法通常引入密集的中间监督，例如过程奖励模型或辅助自监督信号，这增加了监督和调优的复杂性，并可能限制跨任务和领域的泛化能力。我们提出了 AEM，一种无需监督的信用分配方法，它在强化学习训练过程中自适应地调制熵动力学，以改善探索与利用之间的权衡。我们的分析将熵动力学从 token 级别提升到响应（response）级别，使不确定性估计与 LLM 智能体的有效动作粒度保持一致，并降低了对 token 级别采样噪声的敏感性。我们进一步证明，自然梯度更新下的熵漂移受采样响应的优势（advantage）与其相对惊奇度（surprisal）之间相互作用的支配。受此结果启发，AEM 推导出一个实用的响应级不确定性代理指标，并利用其重新缩放优势值，通过利用正负样本之间不断演变的平衡，实现从探索到利用的自然过渡。在 ALFWorld、WebShop 和 SWE-bench-Verified 上进行的广泛实验（模型规模从 1.5B 到 32B）表明，AEM 持续改进了强大的强化学习基线，在集成到最先进的软件工程强化学习训练框架中时，获得了 +1.4% 的提升。

## Abstract
Reinforcement learning (RL) has substantially improved the ability of large language model (LLM) agents to interact with environments and solve multi-turn tasks. However, effective agentic RL remains challenging: sparse outcome-only rewards provide limited guidance for assigning credit to individual steps within long interaction trajectories. Existing approaches often introduce dense intermediate supervision, such as process reward models or auxiliary self-supervised signals, which increases supervision and tuning complexity and may limit generalization across tasks and domains. We present AEM, a supervision-free credit assignment method that adaptively modulates entropy dynamics during RL training to improve the exploration-exploitation trade-off. Our analysis lifts entropy dynamics from the token level to the response level, aligning uncertainty estimation with the effective action granularity of LLM agents and reducing sensitivity to token-level sampling noise. We further show that entropy drift under natural-gradient updates is governed by the interaction between the sampled-response advantage and its relative surprisal. Motivated by this result, AEM derives a practical response-level uncertainty proxy and uses it to rescale advantages, leveraging the evolving balance between positive and negative samples to naturally transition from exploration to exploitation. Extensive experiments on ALFWorld, WebShop, and SWE-bench-Verified with models ranging from 1.5B to 32B demonstrate that AEM consistently improves strong RL baselines, including a +1.4\% gain when integrated into a state-of-the-art software-engineering RL training framework.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AEM（Adaptive Entropy Modulation，自适应熵调制）** 的方法，旨在解决大语言模型（LLM）智能体在多轮强化学习（RL）任务中面临的信用分配难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：在多轮智能体任务（如软件工程、网页导航）中，奖励通常是稀疏且仅基于结果的（Outcome-only）。这意味着智能体在执行了长达数十步的操作后才得到一个反馈，导致难以确定中间哪一步操作对最终成败起到了关键作用（即“信用分配”难题）。
*   **现有方案局限**：
    *   **密集监督（如 PRM）**：需要额外的人工标注或模型，成本高且泛化性差。
    *   **结构化方法（如 Tree-GRPO）**：计算开销巨大。
    *   **自监督方法**：容易出现上下文不一致或对结构假设依赖过强。
*   **研究动机**：利用策略自身的**熵（Entropy）**作为内在信号。高熵代表探索性决策，低熵代表自信决策。通过在“响应级别”调制熵动力学，实现无需额外监督的信用分配。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将熵的分析从 Token 级别提升到**响应（Response）级别**。因为在智能体交互中，一个完整的回复（Action）才是改变环境状态的有效单位。
*   **关键技术细节**：
    *   **理论基础**：论文证明了在自然梯度更新下，策略熵的漂移（Drift）是由采样响应的**优势值（Advantage）**与其**相对惊奇度（Relative Surprisal）**的乘积决定的。
    *   **不确定性代理指标**：由于全局熵难以计算，AEM 使用长度归一化的 Token 熵均值作为响应级不确定性的代理指标。
    *   **调制机制**：
        1.  计算组内（Group）所有响应的熵均值。
        2.  通过 Min-Max 归一化和指数映射生成调制系数 $\alpha$。
        3.  **自适应缩放**：用 $\alpha$ 重新缩放原始优势值。
    *   **探索-利用转换**：
        *   **训练早期**：负样本多，AEM 放大高熵响应的优势，维持探索，防止熵过快塌陷。
        *   **训练后期**：正样本增加，AEM 强化低熵（高置信度）响应，促进收敛和利用。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：文本驱动的具身决策任务（6类家庭任务）。
    *   **WebShop**：网页购物导航与搜索任务。
    *   **SWE-bench-Verified**：真实的软件工程问题修复任务。
*   **模型规模**：Qwen2.5 (1.5B, 7B) 以及 Qwen3-32B。
*   **对比基线**：
    *   **闭源模型**：GPT-5.2-Pro, Gemini-3-Pro。
    *   **RL 算法**：PPO, GRPO, GSPO, DAPO。
    *   **特定框架**：DeepSWE（针对软件工程的最优 RL 框架）。

### 4. 资源与算力
*   **算力配置**：
    *   **Qwen2.5-1.5B**：使用 4 张 NVIDIA A800 GPU。
    *   **Qwen2.5-7B**：使用 8 张 NVIDIA A800 GPU。
    *   **Qwen3-32B**：使用 64 张 NVIDIA H200 GPU。
*   **训练时长**：ALFWorld 和 WebShop 训练 150 步；SWE-bench 训练 250 步。
*   **开销分析**：AEM 仅增加约 **1.1%** 的额外计算耗时，因为它复用了对数概率计算过程中的中间值，无需额外的模型前向传播。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从轻量级（1.5B）到中量级（32B）的模型，并在三个完全不同领域的基准上验证。
*   **消融实验**：非常充分。作者测试了“随机打乱系数”、“反向调制”、“不同归一化方式（Batch vs. Trajectory vs. Group）”等变体，证明了 AEM 机制的有效性并非来自随机噪声，而是来自精确的熵-优势对齐。
*   **客观性**：所有实验均采用 3 个随机种子取平均值，并提供了详细的训练曲线（熵变化与成功率对比），结果具有统计学意义。

### 6. 主要结论与发现
*   **性能提升**：AEM 在所有基线之上均有提升。在 ALFWorld 上，GRPO+AEM 相比纯 GRPO 提升了 **8.8%**；在 SWE-bench 上，在已有的 SOTA 框架 DeepSWE 基础上进一步提升了 **1.4%**。
*   **熵动力学优化**：AEM 有效缓解了 RL 训练早期的“策略熵骤降”现象，使模型在后期能实现更彻底的收敛。
*   **通用性**：作为一个“插件式”方法，它能无缝集成到 PPO、GRPO、DAPO 等多种主流 RL 算法中。

### 7. 优点：亮点总结
*   **无需监督（Supervision-free）**：不需要昂贵的中间过程标注或额外的奖励模型。
*   **轻量高效**：计算开销几乎可以忽略不计，适合大规模扩展。
*   **理论支撑强**：从信息几何和 Fisher-Rao 度量的角度推导了熵漂移公式，为启发式操作提供了严谨的数学基础。
*   **自适应性**：能够根据训练阶段自动平衡探索与利用，无需手动调节复杂的调度表。

### 8. 不足与局限
*   **代理指标的启发性**：虽然理论上使用的是响应熵，但实践中用的是 Token 熵的均值，这是一种启发式近似，在某些极端分布下可能不够精确。
*   **组内多样性依赖**：AEM 依赖于同一 Prompt 下采样出的响应组（Group）具有一定的多样性，如果采样多样性极低，调制效果会减弱。
*   **任务覆盖**：虽然涵盖了具身、网页和代码，但尚未在纯数学推理（如 MATH 基准）上进行大规模验证。

（完）
