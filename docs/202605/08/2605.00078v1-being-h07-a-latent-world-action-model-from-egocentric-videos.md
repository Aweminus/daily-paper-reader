---
title: "Being-H0.7: A Latent World-Action Model from Egocentric Videos"
title_zh: Being-H0.7：一种基于第一视角视频的潜空间世界-动作模型
authors: "Hao Luo, Wanpeng Zhang, Yicheng Feng, Sipeng Zheng, Haiweng Xu, Chaoyi Xu, Ziheng Xi, Yuhui Fu, Zongqing Lu"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00078v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于VLA策略中具备未来感知推理能力的潜空间世界动作模型
tldr: Being-H0.7 提出了一种潜空间世界动作模型，旨在解决传统 VLA 模型缺乏动力学理解及视频预测模型推理开销大的问题。该模型通过在感知与动作间引入可学习的潜查询，并采用双分支设计在训练时对齐当前与未来信息，使模型在推理时无需生成未来帧即可具备预见性推理能力。实验证明其在多个仿真和真实任务中达到了 SOTA 性能，兼顾了世界模型的预测优势与 VLA 的高效性。
selection_source: fresh_fetch
motivation: 传统的 VLA 模型往往只学习简单的映射而忽略了动力学规律，而基于像素预测的世界模型推理成本过高且包含冗余视觉细节。
method: 引入潜查询接口，通过训练时的双分支设计将未来观测信息注入到当前的潜推理空间中，实现无需视觉回放的未来感知推理。
result: 在六个仿真基准测试和多种真实世界任务中，该模型取得了领先或相当的性能表现，展现了极高的部署效率。
conclusion: Being-H0.7 证明了在潜空间进行未来感知推理可以显著提升机器人控制策略的性能，且无需承担视频生成的计算负担。
---

## 摘要
视觉-语言-动作模型（VLA）通过将多模态观测和语言指令直接映射到动作，推动了通用机器人控制的发展，但稀疏的动作监督往往会导致模型学习到捷径映射，而非对动力学、接触和任务进度的表征。最近的世界-动作模型通过视频展开（video rollouts）引入了未来预测，但像素空间的预测对于控制而言是一种昂贵且间接的基础，因为它可能会建模与动作生成无关的视觉细节，并带来巨大的训练或推理开销。我们提出了 Being-H0.7，这是一种潜空间世界-动作模型，它在不生成未来帧的情况下，将具备未来感知能力的推理引入到 VLA 风格的策略中。Being-H0.7 在感知和动作之间插入了可学习的潜查询（latent queries）作为紧凑的推理接口，并采用一种基于未来信息的双分支设计进行训练：一个可部署的先验分支从当前上下文中推断潜状态，而一个仅用于训练的后验分支则用来自未来观测的嵌入替换这些查询。通过在潜推理空间联合对齐这两个分支，使得先验分支能够仅凭当前观测就推理出具备未来感知能力且对动作有用的结构。在推理时，Being-H0.7 丢弃后验分支，且不进行视频展开。在六个仿真基准测试和多种真实世界任务中的实验表明，Being-H0.7 达到了最先进或相当的性能，结合了世界模型的预测优势与直接 VLA 策略的高效性和可部署性。

## Abstract
Visual-Language-Action models (VLAs) have advanced generalist robot control by mapping multimodal observations and language instructions directly to actions, but sparse action supervision often encourages shortcut mappings rather than representations of dynamics, contact, and task progress. Recent world-action models introduce future prediction through video rollouts, yet pixel-space prediction is a costly and indirect substrate for control, as it may model visual details irrelevant to action generation and introduces substantial training or inference overhead. We present Being-H0.7, a latent world-action model that brings future-aware reasoning into VLA-style policies without generating future frames. Being-H0.7 inserts learnable latent queries between perception and action as a compact reasoning interface, and trains them with a future-informed dual-branch design: a deployable prior branch infers latent states from the current context, while a training-only posterior branch replaces the queries with embeddings from future observations. Jointly aligning the two branches at the latent reasoning space leads the prior branch to reason future-aware, action-useful structure from current observations alone. At inference, Being-H0.7 discards the posterior branch and performs no visual rollout. Experiments across six simulation benchmarks and diverse real-world tasks show that Being-H0.7 achieves state-of-the-art or comparable performance, combining the predictive benefits of world models with the efficiency and deployability of direct VLA policies.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Being-H0.7** 的潜空间世界-动作模型（Latent World-Action Model），旨在提升机器人通用策略的预见性推理能力，同时保持极高的部署效率。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **现有 VLA 的局限性**：传统的视觉-语言-动作模型（VLA）直接将观测映射到动作，由于动作监督信号稀疏，模型容易学习到“捷径”映射（如简单的视觉匹配），而缺乏对物理动力学、物体接触和任务进度的深刻理解。
*   **现有世界模型（WAM）的局限性**：虽然通过视频生成（像素预测）引入了未来感知，但像素空间预测成本极高，且包含大量与控制无关的冗余细节（如光影、背景），导致推理延迟大，难以处理动态任务。
*   **核心动机**：能否在不生成未来像素的前提下，让模型在“潜空间”内进行具备未来感知能力的推理？即让模型在行动前先思考“未来会发生什么”，而非“下一帧图像长什么样”。

### 2. 方法论
*   **潜推理空间（Latent Reasoning Space）**：在多模态上下文（指令、观测历史、状态）和动作 Token 之间插入一组可学习的“潜查询”（Latent Queries）。这些查询作为推理接口，在 Transformer 层间传递时聚合任务相关信息。
*   **双分支对齐设计（Dual-Branch Design）**：
    *   **先验分支（Prior Branch）**：可部署分支，仅利用当前上下文和潜查询推断潜状态。
    *   **后验分支（Posterior Branch）**：仅训练时使用，将潜查询替换为从“未来观测”中提取的嵌入（Future Embeddings）。
    *   **联合对齐（Joint Alignment）**：通过损失函数强制先验分支的隐藏状态向后验分支对齐，使模型学会在仅有当前信息时也能模拟出拥有“未来视角”的推理结果。
*   **防坍缩正则化**：引入范数保持（Norm Preservation）和谱多样性（Spectral Diversity/Rank）约束，防止潜空间状态在对齐过程中出现数值萎缩或维度坍缩。
*   **高效实现**：采用混合 Transformer（MoT）架构，通过双分支注意力掩码（Dual-Branch Attention Mask）在单次前向传播中同时处理两个分支，共享上下文计算。

### 3. 实验设计
*   **仿真基准测试**：涵盖了 6 个主流基准，包括 **LIBERO**（桌面操作）、**RoboCasa**（厨房长程任务）、**GR1**（双臂人形）、**LIBERO-plus**（鲁棒性）、**RoboTwin 2.0**（双臂随机化场景）和 **CALVIN**（长程指令序列）。
*   **真实世界评估**：在 **PND Adam-U**（上身人形）、**Unitree G1**（全尺寸人形）和 **Franka FR3**（单臂）三个平台上进行了 12 项挑战性任务，分为动态场景、物理推理、运动推理、长程执行和泛化五个维度。
*   **对比方法**：对比了 $\pi_0$、$\pi_{0.5}$、GR1、Fast-WAM、Cosmos-Policy、LingBot-VA 等多种 SOTA VLA 和世界模型。

### 4. 资源与算力
*   **模型规模**：Being-H0.7 约为 **3B** 参数量（基于 InternVL3.5 和 Qwen3 构建）。
*   **算力说明**：文中**未明确给出**具体的 GPU 型号、数量或总训练时长。但提到其在混合的人类和机器人操作数据上进行了大规模预训练，并使用了序列打包（Sequence Packing）技术来维持约 128 的全局 Batch Size。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验覆盖了从简单桌面到复杂厨房、从单臂到双臂人形的极广范围；真实世界实验包含 12 个任务，每个任务进行 20 次盲测（Blind Trials），总计数百次真实机器人测试。
*   **充分性与客观性**：
    *   **多维度评估**：不仅测试了成功率，还测试了推理延迟（3-4ms/step）和显存占用。
    *   **消融与可视化**：通过将潜状态输入视频生成器，可视化了模型内部确实捕捉到了未来的物理演化（如球的滚动轨迹），证明了方法的有效性。
    *   **公平性**：采用统一的黑盒推理服务器和盲测协议，确保了不同模型间对比的公正性。

### 6. 主要结论与发现
*   **性能领先**：Being-H0.7 在所有 6 个仿真基准上均达到或接近 SOTA，尤其在长程任务（CALVIN）和复杂环境（RoboCasa）中表现突出。
*   **动态任务优势**：在真实世界中，该模型在“动态场景”和“运动推理”任务（如接住快速滚动的球、向移动容器倒水）中显著优于传统 VLA，证明了潜空间未来感知的价值。
*   **部署高效**：推理时无需生成像素，延迟极低，配合 UAC（通用异步分块）机制，能够支持高频、平滑的机器人控制。

### 7. 优点（亮点）
*   **架构优雅**：巧妙地将“世界模型”的预测能力转化为“潜空间推理”，避免了昂贵的视频生成开销。
*   **训练高效**：双分支掩码设计使得训练开销仅略高于普通 VLA，而非翻倍。
*   **跨具身能力**：在不同形态的机器人（单臂、双臂、人形）上表现出极强的通用性和泛化能力。

### 8. 不足与局限
*   **对未来观测质量的依赖**：训练阶段需要高质量的未来帧嵌入，如果预训练视频数据质量不佳，可能会限制潜空间的学习效果。
*   **物理细节的抽象程度**：虽然潜空间能捕捉宏观动力学，但对于极细微的物理交互（如精密装配中的微小力反馈），潜空间表征是否足够精细仍有待进一步验证。
*   **算力细节缺失**：论文未披露训练资源消耗，使得其他研究者难以评估其复现成本。

（完）
