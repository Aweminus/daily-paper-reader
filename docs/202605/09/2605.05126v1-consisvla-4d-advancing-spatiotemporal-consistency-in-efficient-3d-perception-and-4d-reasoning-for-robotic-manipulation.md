---
title: "ConsisVLA-4D: Advancing Spatiotemporal Consistency in Efficient 3D-Perception and 4D-Reasoning for Robotic Manipulation"
title_zh: ConsisVLA-4D：提升机器人操作中高效 3D 感知与 4D 推理的时空一致性
authors: "Wei Li, Jizhihui Liu, Li Yixing, Junwen Tong, Rui Shao, Liqiang Nie"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.05126v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于4D推理和机器人操作的视觉-语言-动作（VLA）模型
tldr: ConsisVLA-4D是一个旨在提升机器人操作中时空一致性的视觉-语言-动作（VLA）框架。针对现有模型在3D感知和4D推理中计算开销大、时空一致性差的问题，该研究提出了CV-Aligner、CO-Fuser和CS-Thinker三个核心模块，分别实现跨视图语义对齐、跨对象几何一致性和跨场景时空推理。实验表明，该模型在LIBERO基准和真实场景中性能显著优于OpenVLA，且推理速度提升超过2倍。
selection_source: fresh_fetch
motivation: 现有VLA模型在3D感知上依赖额外传感器且计算开销大，且视觉推理缺乏与指令及场景的对齐，导致时空一致性不足。
method: 提出包含CV-Aligner、CO-Fuser和CS-Thinker的统一框架，通过语义对齐、几何融合和时空推理模块增强感知与决策。
result: "在LIBERO和真实世界任务中，性能分别提升21.6%和41.5%，推理速度达到OpenVLA的2.3倍以上。"
conclusion: ConsisVLA-4D通过高效的时空一致性设计，显著提升了机器人操作的感知精度、推理能力和执行效率。
---

## 摘要
现有的视觉-语言-动作（VLA）模型主要侧重于将 2D 观测映射到动作，但在时空感知与推理方面存在显著局限：1）空间表示往往依赖额外传感器，引入了巨大的计算开销；2）视觉推理通常局限于未来帧预测，缺乏与基于指令的场景的对齐，从而损害了时空一致性。为了应对这些挑战，我们提出了 ConsisVLA-4D，这是一个统一且高效的框架，旨在增强 3D 感知和 4D 推理中的时空一致性。具体而言，我们设计了：1）CV-Aligner，通过过滤指令相关区域并对齐多视角下的物体身份，确保跨视角的物体语义一致性；2）CO-Fuser，利用紧凑的潜表示消除跨视角物体间的空间关系歧义，从而保证跨物体的空间几何一致性。在此基础上，我们引入了 3）CS-Thinker，以在动作展开过程中实现跨场景的时空一致性。它从 CV-Aligner 的物体语义 token 中学习局部动力学的隐式知识，并从 CO-Fuser 的几何 token 中学习全局深度，从而增强了场景变化下的高效视觉推理。大量实验表明，得益于其高效的时空一致性设计，ConsisVLA-4D 在 LIBERO 基准测试和真实世界平台上，相比 OpenVLA 分别实现了 21.6% 和 41.5% 的性能提升，以及 2.3 倍和 2.4 倍的推理加速。ConsisVLA-4D 已开源。

## Abstract
Current Vision-Language-Action (VLA) models primarily focus on mapping 2D observations to actions, but exhibit notable limitations in spatiotemporal perception and reasoning: 1) spatial representations often rely on additional sensors, introducing substantial computational overhead; 2) visual reasoning is typically limited to future-frame prediction, lacking alignment with the instruction-grounded scene and thus compromising spatiotemporal consistency. To address these challenges, we propose ConsisVLA-4D, a unified and efficient framework that enhances spatiotemporal consistency in 3D perception and 4D reasoning. Specifically, we design: 1) CV-Aligner, which ensures cross-view object semantic consistency by filtering instruction-relevant regions and aligning object identities across multiple viewpoints; 2) CO-Fuser, which guarantees cross-object spatial geometric consistency by eliminating spatial relation ambiguities between objects across views using compact latent representations. Building upon these, we introduce 3) CS-Thinker to achieve cross-scene spatiotemporal consistency as actions unfold. It learns implicit knowledge of local dynamics from object-semantic tokens of CV-Aligner and global depth from geometric tokens of CO-Fuser, thereby enhancing efficient visual reasoning under scene variations. Extensive experiments demonstrate that, benefiting from its efficient spatiotemporal consistency design, ConsisVLA-4D achieves 21.6% and 41.5% performance improvements, along with 2.3-fold and 2.4-fold inference speedups compared to OpenVLA on the LIBERO benchmark and real-world platforms, respectively.ConsisVLA-4D is open-sourced and publicly available at

---

## 论文详细总结（自动生成）

### 论文总结：ConsisVLA-4D

#### 1. 核心问题与研究动机
*   **核心问题**：当前的视觉-语言-动作（VLA）模型在机器人操作中面临两大瓶颈：
    1.  **空间感知不足**：现有的 2D 到 3D 映射往往无法捕捉复杂的 3D 结构，而依赖额外传感器（如点云、深度相机）的方法计算开销巨大。
    2.  **时空推理缺失**：视觉推理通常仅限于预测未来帧，缺乏与指令及动态场景的深度对齐，导致动作生成在时间维度上不稳定。
*   **研究动机**：模仿人类通过双目视觉和运动维持空间一致性并预测未来状态的能力，旨在构建一个既能高效生成 3D 表示，又能通过 4D 推理增强动作预测稳定性的统一框架。

#### 2. 方法论
ConsisVLA-4D 包含三个核心集成模块，旨在实现从 3D 感知到 4D 推理的跨越：
*   **CV-Aligner（跨视角对齐器）**：
    *   **显式语义物体选择**：利用 SigLIP 提取语义，通过 FiLM 调制增强指令相关性，并使用 Top-K 策略过滤掉 7/8 的冗余视觉 token。
    *   **单融合（Single-Fusion）**：将过滤后的物体 token 与 VGGT 提取的 3D 特征融合，确保不同视角下物体身份的一致性。
*   **CO-Fuser（跨物体融合器）**：
    *   **组融合（Group-Fusion）**：在 DINOv2 和 VGGT 编码器层间进行深度融合，利用余弦衰减权重平衡几何先验与学习特征。
    *   **隐式几何关系聚合**：通过块级因果自注意力机制（BC-Attn）消除单视角下的几何歧义，生成紧凑的全局空间关系表示。
*   **CS-Thinker（跨场景思考器）**：
    *   **4D 推理与时空一致性注意力（SC-Attn）**：在训练阶段学习未来动态物体（局部）和全局深度（全局）的隐式知识。
    *   **高效推理**：推理时无需显式生成图像，而是利用预学习的隐式知识引导动作 token 的并行解码，确保动作在动态场景中的连贯性。

#### 3. 实验设计
*   **仿真环境**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件。
    *   **ManiSkill2**：测试 Pick-Cube、Stack-Cube 等空间感知任务。
    *   **RoboTwin 2.0**：基于 ALOHA 的双臂协作任务。
*   **真实世界场景**：
    *   **平台**：AgileX Cobot Magic（移动 ALOHA）和 Galaxea R1 Lite。
    *   **任务**：微波炉操作、剥香蕉、抽屉整理、折叠 T 恤（均为长程、双臂任务）。
*   **对比方法**：包括 OpenVLA（基准）、OpenVLA-OFT（高效版）、$\pi_0$、SpatialVLA（空间增强型）、CoT-VLA（推理增强型）等 SOTA 模型。

#### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA A800 GPU**。
*   **训练细节**：训练步数为 80K 步，单臂任务 Batch Size 为 64，双臂任务为 32。
*   **推理算力**：真实世界部署在单张 **RTX 5090 GPU** 上。
*   **效率提升**：相比 OpenVLA，训练成本降低了约 26%-30%，推理延迟降低了约 56%。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 3 个主流基准测试，涉及数十种任务和数千次试验。
    *   真实世界实验在两个不同硬件平台上进行了 4 类长程任务测试，每项任务进行 15-30 次试验。
    *   **消融实验**：针对 CV-Aligner、CO-Fuser、CS-Thinker 各组件以及 Token 稀疏化比例进行了详尽的对比。
*   **充分性评价**：实验设计非常充分，不仅对比了成功率，还深入分析了推理速度、吞吐量和显存占用。通过 Sim-to-Real 的一致性表现，证明了实验结果的客观性和公平性。

#### 6. 主要结论与发现
*   **性能突破**：在 LIBERO 上平均成功率达到 98.1%，远超 OpenVLA（76.5%）和 SpatialVLA（78.1%）。
*   **效率优势**：通过将视觉输入压缩至原来的 1/8，模型在增加 3D/4D 推理模块的情况下，推理速度反而提升了 2.3 倍以上。
*   **时空一致性的价值**：消融实验证明，跨视角语义对齐和跨场景推理对长程任务（如剥香蕉）至关重要，能显著减少执行过程中的动作漂移。

#### 7. 优点
*   **高效性**：创新的 Token 过滤机制解决了 VLA 模型处理多视角高分辨率输入时的计算瓶颈。
*   **统一框架**：将 3D 感知与 4D 推理无缝集成，无需依赖昂贵的实时深度传感器。
*   **泛化能力**：在不同机器人平台（Galaxea 和 AgileX）上表现出极强的鲁棒性和 sim-to-real 迁移能力。

#### 8. 不足与局限
*   **依赖预训练模型**：框架高度依赖 SigLIP、DINOv2 和 VGGT 等冻结的视觉编码器，若这些基础模型存在感知偏差，可能会影响下游任务。
*   **训练复杂性**：虽然推理高效，但训练阶段涉及多个损失函数（动作、动态物体、全局深度）的联合优化，超参数调节可能较为复杂。
*   **极端环境测试缺失**：实验主要集中在室内桌面操作，对于光照剧烈变化或极度拥挤的动态室外环境，其表现尚待验证。

（完）
