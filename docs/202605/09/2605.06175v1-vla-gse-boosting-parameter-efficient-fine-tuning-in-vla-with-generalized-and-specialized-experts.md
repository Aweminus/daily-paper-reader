---
title: "VLA-GSE: Boosting Parameter-Efficient Fine-Tuning in VLA with Generalized and Specialized Experts"
title_zh: VLA-GSE：利用通用与专用专家提升 VLA 的参数高效微调
authors: "Yuhua Jiang, Junjie Lu, Xinyao Qin, Xiaoyu Chen, Kaixin Wang, Feifei Gao, Li Zhao"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06175v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉-语言-动作（VLA）模型在控制任务中的参数高效微调
tldr: "针对视觉-语言-动作（VLA）模型在机器人控制微调中易过拟合及遗忘预训练知识的问题，本文提出了VLA-GSE框架。该方法通过对冻结的主干网络进行谱分解，将其初始化为共享的通用专家和路由的专用专家，在仅更新2.51%参数的情况下，显著提升了模型对控制任务的适应能力。实验表明，VLA-GSE在LIBERO-Plus基准测试中表现优于全量微调，并能有效保留多模态理解能力，在真实世界操作中展现出极强的鲁棒性。"
selection_source: fresh_fetch
motivation: 全量微调VLA模型易导致灾难性遗忘，而现有参数高效微调方法在复杂的机器人控制任务中适应性不足。
method: 通过谱分解将冻结的权重分解为通用专家和专用专家，在固定参数预算下利用专家混合机制增强模型的表达能力。
result: "在LIBERO-Plus上达到81.2%的平均零样本成功率，仅用2.51%的参数量即超越了全量微调和LoRA等强基线。"
conclusion: VLA-GSE证明了通过合理的专家分解与参数高效微调，可以在提升机器人控制性能的同时完美兼顾预训练知识的保留。
---

## 摘要
视觉-语言-动作（VLA）模型从预训练的视觉-语言主干网络中继承了丰富的视觉语义先验，但将其适配到机器人控制任务中仍然具有挑战性。全量微调（FFT）容易在下游机器人数据上产生过拟合，并导致预训练视觉-语言能力的灾难性遗忘。参数高效微调（PEFT）能更好地保留预训练知识，但现有的 PEFT 方法在有效适配机器人控制任务方面仍存在困难。为了填补这一空白，我们提出了 VLA-GSE，这是一种参数高效的 VLA 微调框架，在保留 PEFT 知识保留优势的同时，提高了控制适配能力。具体而言，VLA-GSE（通用与专用专家）通过对冻结的主干网络进行谱分解进行初始化，将主要奇异分量分配给通用专家（共享专家），将不相交的残差分量分配给专用专家（路由专家）。这种分解在固定的可训练参数预算下提升了适配能力。在相当的参数预算下，VLA-GSE 仅更新了全量模型参数的 2.51%，且始终优于强大的 FFT 和 PEFT 基准模型。它在 LIBERO-Plus 上实现了 81.2% 的平均零样本成功率，在多模态理解基准测试中保留了与 LoRA 相当的预训练 VLM 能力，并提升了在多种分布偏移下的现实世界操纵成功率。代码可在以下网址获取：https://github.com/YuhuaJiang2002/VLA-GSE。

## Abstract
Vision-language-action (VLA) models inherit rich visual-semantic priors from pre-trained vision-language backbones, but adapting them to robotic control remains challenging. Full fine-tuning (FFT) is prone to overfitting on downstream robotic data and catastrophic forgetting of pretrained vision-language capabilities. Parameter-efficient fine-tuning (PEFT) better preserves pre-trained knowledge, yet existing PEFT methods still struggle to adapt effectively to robot control tasks. To address this gap, we propose VLA-GSE, a parameter-efficient VLA fine-tuning framework that improves control adaptation while retaining PEFT's knowledge preservation advantage. Specifically, VLA-GSE (Generalized and Specialized Experts) is initialized by spectrally decomposing the frozen backbone, assigning leading singular components to generalized experts (shared experts) and disjoint residual components to specialized experts (routed experts). This decomposition improves adaptation capacity under a fixed trainable-parameter budget. Under a comparable parameter budget, VLA-GSE updates only 2.51% of the full model parameters and consistently outperforms strong FFT and PEFT baselines. It achieves 81.2% average zero-shot success on LIBERO-Plus, preserves pre-trained VLM capability comparably to LoRA on multimodal understanding benchmarks, and improves real-world manipulation success under multiple distribution shifts. Code is available at: https://github.com/YuhuaJiang2002/VLA-GSE

---

## 论文详细总结（自动生成）

这篇论文提出了 **VLA-GSE**，一种针对视觉-语言-动作（VLA）模型的高效微调框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何让预训练的视觉-语言模型（VLM）在适配到机器人控制任务（VLA）时，既能获得**精确的控制能力**，又能**避免灾难性遗忘**（保留原有的视觉语义知识）。
*   **研究背景**：
    *   **全量微调（FFT）**：表达能力强，但极易在有限的机器人数据上过拟合，且会破坏 VLM 原有的多模态理解能力。
    *   **现有 PEFT（如 LoRA）**：虽然能保留知识，但在需要高精度动作反馈的具身智能任务中，往往表现出“适配不足”，导致控制精度低。
*   **整体含义**：VLA-GSE 通过一种“通用+专用”的专家分解机制，在极低参数量（2.51%）下实现了超越全量微调的控制性能。

### 2. 论文提出的方法论
核心思想是将主干网络的权重通过**谱分解（SVD）**初始化为两类专家：
*   **通用专家（Generalized Expert）**：
    *   **初始化**：提取冻结权重矩阵 $W_0$ 中最大的前 $r_g$ 个奇异值分量。
    *   **作用**：作为始终开启的共享路径，捕捉基础的、通用的视觉语义特征。
*   **专用专家（Specialized Experts）**：
    *   **初始化**：将剩余的显著奇异值分量均匀分配给多个不相交的专家。
    *   **作用**：采用稀疏路由（Top-k 机制），根据输入动态选择专家，用于处理特定任务或复杂的控制行为。
*   **关键技术细节**：
    *   **梯度缩放平衡（Gradient Scale Balancing）**：由于不同专家的奇异值量级不同，导致优化速度不一。论文推导并应用了“迹逆缩放规则”（Trace-inverse scaling），使各专家梯度量级在初始化时保持平衡。
    *   **主干权重调整（Backbone Weight Adjustment）**：为了消除初始化时新增专家带来的权重偏移，通过减去期望残差来调整冻结权重，确保模型在初始时刻的输出与原始 VLM 一致。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **LIBERO-Plus** 基准测试，包含 7 个维度的扰动（相机、机器人、语言、光照、背景、噪声、布局），全面评估零样本泛化能力。
    *   **现实世界**：在 AgileX PiPER 机器人上进行 4 类任务（倒水、放勺子、放胡萝卜、取笔），涵盖 4 种分布偏移。
    *   **多模态理解**：在 MMMU、MMStar、OCRBench 等 8 个通用 VLM 榜单上测试知识保留情况。
*   **对比方法**：
    *   **强基线**：全量微调（FFT）、OpenVLA、$\pi_0$、$\pi_{0.5}$ 等。
    *   **PEFT 变体**：LoRA、rsLoRA、DoRA、PiSSA、MoLoRA、AdaMoLE、HydraLoRA、GOAT 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练配置**：
    *   主干网络：Qwen3-VL-4B-Instruct。
    *   训练步数：80,000 步。
    *   Batch Size：总计 128。
    *   参数量：总参数 4.5B，仅微调 **114M（2.51%）**。
*   **推理效率**：在 RTX 5080 上测试了推理延迟，VLA-GSE 略高于 FFT，但显著快于基于扩散或流匹配的模型（如 $\pi_0$）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 LIBERO 的所有 4 个子集和 7 种泛化维度。
    *   现实世界进行了 240 次试验（60 次/任务）。
    *   进行了详尽的**消融实验**，验证了 SVD 初始化、通用/专用专家设计、辅助损失和梯度缩放的必要性。
*   **充分性评价**：实验设计非常充分且客观。通过与近 10 种 PEFT 方法在相同参数预算下的对比，有力证明了该结构在机器人控制领域的优越性。

### 6. 主要结论与发现
*   **性能超越 FFT**：在 LIBERO-Plus 上，VLA-GSE 的零样本成功率（81.2%）比全量微调高出 6.3%。
*   **知识保留极佳**：在通用多模态测试中，VLA-GSE 的得分与原始 VLM 几乎持平，而全量微调后的模型得分大幅下降（如 MMMU 从 53.2 降至 35.6）。
*   **鲁棒性强**：在光照变化、背景切换等现实干扰下，VLA-GSE 的表现显著优于现有 SOTA 模型。

### 7. 优点（亮点）
*   **理论支撑**：不仅提出了结构，还通过数学推导解决了 SVD 初始化带来的梯度不平衡问题。
*   **参数极其高效**：仅用 2.5% 的参数就解决了 VLA 领域长期存在的“性能 vs 知识保留”的矛盾。
*   **即插即用**：该方法基于 SVD 分解，可以很容易地推广到其他预训练主干网络。

### 8. 不足与局限
*   **应用领域局限**：目前仅在机器人操作任务中验证，尚未在医疗影像、金融预测等其他需要高精度适配的领域测试。
*   **推理开销**：虽然是稀疏路由，但相比于简单的 LoRA，多专家结构和路由机制仍会带来微小的额外计算延迟。
*   **超参数敏感性**：专家数量、奇异值分配比例等超参数可能需要针对不同规模的模型进行微调。

（完）
