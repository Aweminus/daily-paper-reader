---
title: "GazeVLM: Active Vision via Internal Attention Control for Multimodal Reasoning"
title_zh: GazeVLM：通过内部注意力控制实现多模态推理的主动视觉
authors: "Brown Ebouky, Gabriele Carrino, Niccolo Avogaro, Christoph Studer, Andrea Bartezzaghi, Mattia Rigotti"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07817v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: VLM 多模态推理中的主动视觉
tldr: "针对现代多模态模型被动处理视觉信息导致的推理稀释问题，本文提出GazeVLM。该模型借鉴人类主动视觉机制，通过生成特殊的<LOOK>标记来动态控制内部注意力掩码，实现对任务相关细节的“注视”聚焦与全局感知的无缝切换。这种内化的元认知控制无需外部裁剪工具或增加视觉标记，显著提升了模型在高分辨率场景下的空间推理能力。"
selection_source: fresh_fetch
motivation: 现代多模态模型缺乏人类主动视觉中的目标驱动注意力，导致在处理大规模视觉上下文时容易产生空间推理稀释和幻觉。
method: "通过引入<LOOK>标记自主控制因果注意力掩码，动态抑制无关视觉特征以模拟人类的注视点聚焦，并利用GRPO算法进行强化学习训练。"
result: "4B参数的GazeVLM在HRBench-4k/8k基准上表现优异，分别超过同类SOTA模型约4%以及基于代理的图像推理流水线5%以上。"
conclusion: 将注意力控制内化到推理循环中，使模型能在不增加计算负担的情况下实现高效的高分辨率多模态推理。
---

## 摘要
人类的视觉推理受主动视觉支配，这是一个由元认知控制驱动自上而下的目标导向注意力的过程，在保持对全局场景的外围感知的同时，动态地将中央凹焦点引导至与任务相关的细节。相比之下，现代视觉语言模型（VLM）被动地处理视觉信息，依赖于海量标记上下文的静态累积，这稀释了空间推理能力并诱发了语言幻觉。在此，我们提出以下范式转变：GazeVLM，一种将这种对其注意力资源部署的元认知监督直接内化到推理循环中的多模态架构。通过赋予 VLM 自主生成注视标记（<LOOK>）的能力，GazeVLM 对其自身的因果注意力掩码建立了一种自上而下的控制机制。该模型动态地决定其聚焦意图，触发一种持续的抑制偏差，从而削弱无关的视觉特征，实现空间选择性注意力并模拟中央凹注视。一旦局部推理结束，偏差就会解除，无缝恢复全局视野。这种架构使模型能够在全局空间感知和局部聚焦推理之间流利切换，而无需依赖裁剪工具等外部智能体装置，也不会因局部视觉补丁产生的额外视觉标记而膨胀上下文窗口。我们的 4B 参数 GazeVLM 采用定制的群体相对策略优化（GRPO）程序进行训练，该程序奖励有效的视觉定位，在 HRBench-4k 和 HRBench-8k 上表现出强大的高分辨率多模态推理性能，比同参数级别的最先进 VLM 高出近 4%，比围绕图像思考构建的智能体多模态流水线高出 5% 以上。

## Abstract
Human visual reasoning is governed by active vision, a process where metacognitive control drives top-down goal-directed attention, dynamically routing foveal focus toward task-relevant details while maintaining peripheral awareness of the global scene. In contrast, modern Vision-Language Models (VLMs) process visual information passively, relying on the static accumulation of massive token contexts that dilute spatial reasoning and induce linguistic hallucinations. Here we propose the following paradigm shift: GazeVLM, a multimodal architecture that internalizes this metacognitive oversight over its deployment of attention resources directly into the reasoning loop. By empowering the VLM to autonomously generate gaze tokens ($\texttt{<LOOK>}$), GazeVLM establishes a top-down control mechanism over its own causal attention mask. The model dynamically dictates its focal intent, triggering a continuous suppression bias that dampens irrelevant visual features, implementing spatial selective attention and simulating foveal fixation. Once local reasoning concludes, the bias lifts, seamlessly restoring the global view. This architecture enables the model to fluidly transition between global spatial awareness and localized focal reasoning without relying on external agentic contraptions like cropping tools, or inflating the context window with additional visual tokens derived from localized visual patches. Trained with a bespoke Group Relative Policy Optimization (GRPO) procedure that rewards valid grounding, our 4B-parameter GazeVLM delivers strong high-resolution multimodal reasoning performance, surpassing state-of-the-art VLMs in its parameter class by nearly 4% and agentic multimodal pipelines built around thinking with images by more than 5% on HRBench-4k and HRBench-8k.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《GazeVLM: Active Vision via Internal Attention Control for Multimodal Reasoning》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代多模态大模型（VLM）在处理高分辨率图像时，通常采用“被动视觉”模式，即一次性接收海量视觉标记（Tokens）。这种方式会导致空间推理能力的稀释，并容易引发语言幻觉，因为模型难以在庞大的上下文窗口中精准定位关键细节。
*   **研究动机**：借鉴人类的“主动视觉”机制。人类在观察时，会通过元认知控制将注意力（中央凹焦点）动态引导至任务相关的局部细节，同时保持对全局场景的感知。
*   **整体含义**：GazeVLM 旨在将这种注意力控制机制“内化”到模型的推理循环中，使其能够自主决定“看哪里”以及“如何看”，从而在不增加计算负担的前提下提升高分辨率推理能力。

### 2. 论文提出的方法论
*   **核心思想**：引入一种名为 `<LOOK>` 的特殊标记，赋予模型自主控制其内部因果注意力掩码（Causal Attention Mask）的能力。
*   **关键技术细节**：
    *   **`<LOOK>` 标记与坐标生成**：模型在推理过程中可以自主生成 `<LOOK> [x1, y1, x2, y2]` 序列。
    *   **内部注意力控制（Internal Attention Control）**：一旦触发 `<LOOK>`，模型会对注意力掩码施加一种“抑制偏差”（Suppression Bias）。具体而言，它会削弱（或屏蔽）指定边界框之外的视觉特征权重，模拟人类的中央凹注视。
    *   **无缝切换**：这种控制是动态的。当局部推理结束，模型可以解除偏差，恢复全局视野。
    *   **无需额外 Token**：与传统的“裁剪-放大”方法（如 LLaVA-UHD）不同，GazeVLM 不需要为局部区域生成新的视觉 Token，因此不会膨胀上下文窗口。
*   **算法流程**：
    1.  模型接收全局图像和问题。
    2.  模型生成推理链，并在需要细节时输出 `<LOOK>` 标记及坐标。
    3.  系统根据坐标修改注意力掩码，使后续推理聚焦于该区域。
    4.  利用 **GRPO（群体相对策略优化）** 强化学习算法进行训练，奖励函数基于回答准确性和视觉定位（Grounding）的精准度。

### 3. 实验设计
*   **数据集/场景**：主要针对高分辨率多模态推理任务。
*   **Benchmark（基准）**：使用了 **HRBench-4k** 和 **HRBench-8k**，这两个基准专门设计用于评估模型在超高分辨率下的细节捕捉和空间推理能力。
*   **对比方法**：
    *   同参数级别的 SOTA（先进）多模态模型（如 4B 参数规模的模型）。
    *   基于代理（Agentic）的流水线：即那些通过外部工具进行图像裁剪和二次处理的模型。

### 4. 资源与算力
*   **模型规模**：4B（40亿）参数。
*   **算力说明**：论文摘要中未明确给出具体的 GPU 型号、数量或总训练时长。但提到使用了定制的 GRPO 程序，这通常意味着需要支持大规模并行采样和强化学习训练的计算集群。

### 5. 实验数量与充分性
*   **实验覆盖**：论文在 HRBench 的两个版本（4k 和 8k）上进行了测试，验证了模型在不同分辨率下的鲁棒性。
*   **对比充分性**：对比了直接推理模型和复杂的 Agent 架构，证明了“内化注意力”比“外部工具”更高效。
*   **客观性**：通过 GRPO 强化学习进行对齐，并使用客观的奖励机制（Grounding Reward），减少了人工标注的主观偏差。

### 6. 主要结论与发现
*   **性能卓越**：4B 参数的 GazeVLM 在 HRBench-4k/8k 上比同类 SOTA 模型高出约 **4%**。
*   **超越 Agent 架构**：相比于依赖外部裁剪工具的智能体流水线，GazeVLM 的性能高出 **5% 以上**。
*   **效率优势**：通过修改注意力掩码而非增加 Token，模型在保持高精度的同时，显著降低了长上下文带来的计算开销。

### 7. 优点
*   **生物启发性**：成功将人类视觉的“中央凹-外围”机制转化为神经网络的注意力掩码控制。
*   **架构优雅**：无需修改视觉编码器或增加额外的视觉处理模块，仅通过元认知标记实现功能。
*   **解决稀释问题**：有效缓解了 VLM 在处理大规模视觉输入时的“注意力稀释”和幻觉问题。

### 8. 不足与局限
*   **训练复杂性**：强化学习（GRPO）的训练过程通常比监督微调（SFT）更不稳定且耗时。
*   **依赖基础模型**：其性能上限仍受限于底层视觉编码器提取特征的能力，如果原始特征中完全丢失了微小细节，仅靠注意力掩码可能无法完全挽回。
*   **应用限制**：目前主要证明了在高分辨率推理上的有效性，在常规低分辨率或纯文本任务中的收益尚待进一步验证。

（完）
