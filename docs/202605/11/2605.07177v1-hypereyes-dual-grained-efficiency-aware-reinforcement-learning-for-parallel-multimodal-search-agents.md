---
title: "HyperEyes: Dual-Grained Efficiency-Aware Reinforcement Learning for Parallel Multimodal Search Agents"
title_zh: HyperEyes：面向并行多模态搜索智能体的双粒度效率感知强化学习
authors: "Guankai Li, Jiabin Chen, Yi Xu, Xichen Zhang, Yuan Lu"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07177v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于并行多模态搜索智能体的强化学习，不涉及机器人
tldr: 针对现有模型在多模态搜索中串行处理效率低的问题，本文提出HyperEyes框架。该框架通过将视觉定位与检索融合为原子操作，实现多实体并发搜索。采用双粒度效率感知强化学习，结合宏观层面的TRACE奖励机制和微观层面的在线蒸馏，在提升搜索准确率的同时显著降低推理成本。实验表明，HyperEyes在保持高精度的同时，大幅减少了工具调用轮数。
selection_source: fresh_fetch
motivation: 现有模型顺序处理目标实体导致交互冗余，需要一种能并发执行独立子检索任务的并行搜索机制以提升效率。
method: 提出双粒度效率感知强化学习，利用TRACE轨迹奖励抑制多余调用，并通过在线蒸馏提供细粒度纠错信号。
result: "HyperEyes-30B在六个基准测试中准确率提升9.9%，且工具调用轮数平均减少了5.3倍。"
conclusion: 并行化搜索策略与效率感知的强化学习框架能有效平衡多模态智能体的推理效率与任务执行能力。
---

## 摘要
现有的多模态搜索智能体按顺序处理目标实体，每个实体发出一次工具调用，每当查询分解为独立的子检索时，就会累积冗余的交互轮次。我们认为，有效的多模态智能体应该搜索得“更广”而非“更长”：即在单轮交互中并发调度多个落地的查询。为此，我们提出了 HyperEyes，这是一种并行多模态搜索智能体，它将视觉落地与检索融合为单个原子动作，在实现跨多个实体并发搜索的同时，将推理效率视为首要训练目标。HyperEyes 的训练分为两个阶段。在冷启动监督阶段，我们开发了一个适用于并行的多模态数据合成流水线，涵盖了视觉多实体和文本多约束查询，并通过渐进式拒绝采样策划面向效率的轨迹。在此基础上，我们的核心贡献——双粒度效率感知强化学习框架，在两个层面上运行。在宏观层面，我们提出了 TRACE（工具使用参考自适应成本效率），这是一种轨迹级奖励，其参考标准在训练过程中单调收紧，以抑制多余的工具调用，同时不限制真正的多跳搜索。在微观层面，我们采用同策略蒸馏，在失败的展开中注入来自外部教师的密集 Token 级纠正信号，从而缓解稀疏结果奖励带来的信用分配缺陷。由于现有基准测试仅将准确率作为唯一指标而忽略了推理成本，我们引入了 IMEB，这是一个包含 300 个实例的人工策划基准，用于联合评估搜索能力和效率。在六个基准测试中，HyperEyes-30B 的准确率比最强的同类开源智能体高出 9.9%，且平均工具调用轮次减少了 5.3 倍。

## Abstract
Existing multimodal search agents process target entities sequentially, issuing one tool call per entity and accumulating redundant interaction rounds whenever a query decomposes into independent sub-retrievals. We argue that effective multimodal agents should search wider rather than longer: dispatching multiple grounded queries concurrently within a round. To this end, we present HyperEyes, a parallel multimodal search agent that fuses visual grounding and retrieval into a single atomic action, enabling concurrent search across multiple entities while treating inference efficiency as a first-class training objective. HyperEyes is trained in two stages. For cold-start supervision, we develop a Parallel-Amenable Data Synthesis Pipeline covering visual multi-entity and textual multi-constraint queries, curating efficiency-oriented trajectories via Progressive Rejection Sampling. Building on this, our central contribution, a Dual-Grained Efficiency-Aware Reinforcement Learning framework, operates at two levels. At the macro level, we propose TRACE (Tool-use Reference-Adaptive Cost Efficiency), a trajectory-level reward whose reference is monotonically tightened during training to suppress superfluous tool calls without restricting genuine multi-hop search. At the micro level, we adapt On-Policy Distillation to inject dense token-level corrective signals from an external teacher on failed rollouts, mitigating the credit-assignment deficiency of sparse outcome rewards. Since existing benchmarks evaluate accuracy as the sole metric, omitting inference cost, we introduce IMEB, a human-curated benchmark of 300 instances that jointly evaluates search capability and efficiency. Across six benchmarks, HyperEyes-30B surpasses the strongest comparable open-source agent by 9.9% in accuracy with 5.3x fewer tool-call rounds on average.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **HyperEyes**，一个旨在提升多模态搜索智能体（Multimodal Search Agents）效率的并行化框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：现有的多模态搜索智能体在处理复杂查询（如涉及多个实体的图像）时，通常采用“串行”模式，即每轮交互仅处理一个实体或执行一次工具调用。这种方式导致了严重的交互冗余、高延迟以及上下文窗口的快速消耗。
*   **核心问题**：如何让智能体在搜索时“更宽”而非“更长”？即在单轮交互中并发处理多个独立的子检索任务，同时在训练中显式地优化推理效率，而不仅仅是准确率。

### 2. 方法论
HyperEyes 提出了一个两阶段训练框架和一种新型动作空间：
*   **统一落地搜索（Unified Grounded Search, UGS）**：将视觉定位（Grounding）与检索融合为原子操作。智能体可以在单轮内预测多个实体的边界框并同时发起并行检索，避免了传统“先裁剪再搜索”的脆弱依赖。
*   **并行友好型数据合成流水线**：
    *   **视觉多实体合成**：通过 Mosaic 增强技术将不同类别的图像组合，生成需要同时识别多个实体才能回答的问题。
    *   **渐进式拒绝采样（PRS）**：在 SFT 阶段，通过严格限制轮次预算，仅保留最短且成功的轨迹，从而蒸馏出高效、无冗余的并行调度行为。
*   **双粒度效率感知强化学习（RL）**：
    *   **宏观层面（TRACE 奖励）**：提出“工具使用参考自适应成本效率”奖励。它设定一个动态收紧的工具调用轮次参考值，奖励高效轨迹，惩罚冗余调用。
    *   **微观层面（同策略蒸馏, OPD）**：针对失败的尝试，引入强力教师模型（如 235B 版本）提供 Token 级的密集纠错信号，解决稀疏奖励下的信用分配难题。

### 3. 实验设计
*   **数据集与场景**：涵盖了 6 个多模态搜索基准测试，包括 MMSearch、FVQA、LiveVQA、BrowseComp-VL (BCVL)、MMSearch-Plus。
*   **新基准测试（IMEB）**：论文引入了 **Image Multi-Entity Benchmark**，包含 300 个由人类策划的实例，专门用于联合评估准确率与搜索效率。
*   **对比方法**：
    *   **开源智能体**：DeepEyes-V2, MMSearch-R1, WebWatcher, VDR, REDSearch。
    *   **闭源模型**：Kimi-K2.5, Claude-Opus-4.6, Gemini-3.1-Pro。
    *   **基座模型**：Qwen3-VL (30B/235B)。

### 4. 资源与算力
*   **硬件环境**：使用了 8 个节点，共计 **64 张 NVIDIA H20 (141GB) GPU**。
*   **训练时长**：
    *   **SFT 阶段**：30B 模型约 10 小时，235B 模型约 20 小时。
    *   **RL 阶段**：30B 模型约 48 小时，235B 模型约 72 小时。
*   **模型规模**：主要实验基于 Qwen3-VL 的 30B 和 235B 版本。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流基准上进行了全面测试，并针对数据过滤、奖励机制（TRACE）、蒸馏策略（OPD）以及动作空间（UGS）进行了详细的消融实验。
*   **客观性与公平性**：
    *   通过 3 个独立随机种子验证了 RL 训练的稳定性。
    *   提出了 **CAS（成本感知评分）** 指标，综合衡量准确率、Token 消耗和工具调用轮次，比单一准确率指标更公平地评价智能体性能。
    *   实验设计充分，对比了不同规模的模型以及当前最先进的商业模型。

### 6. 主要结论与发现
*   **性能领先**：HyperEyes-30B 在准确率上比最强的同规模开源智能体（VDR）高出 **9.9%**，同时工具调用轮次平均减少了 **5.3 倍**。
*   **逼近商业模型**：HyperEyes-235B 的表现已接近 Gemini-3.1-Pro，且在效率指标上显著优于 Claude 和 Kimi。
*   **效率与准确率互补**：实验发现，盲目增加工具调用并不一定提高准确率，反而可能引入噪声。通过 TRACE 抑制冗余调用，不仅提升了效率，还增强了模型对干扰信息的鲁棒性。

### 7. 优点（亮点）
*   **效率优先**：首次将推理效率作为强化学习的一等公民目标，解决了智能体“过度搜索”的问题。
*   **动作空间创新**：UGS 动作空间优雅地解决了多模态任务中的并行化难题。
*   **高质量数据**：PRS 采样技术有效地从嘈杂的原始轨迹中提取出“零冗余”的专家示例。
*   **开源贡献**：发布了 IMEB 基准和完整的训练流水线，对社区具有很高的参考价值。

### 8. 不足与局限
*   **教师依赖**：OPD 蒸馏依赖于同系列更强大的教师模型，这限制了该方法在最顶级规模模型（无更强教师可用时）上的直接应用。
*   **模态限制**：目前仅专注于静态图像和文本，尚未扩展到视频或音频等动态多模态环境。
*   **闭源差距**：尽管表现优异，但在处理极高复杂度的推理任务时，与顶级闭源模型（如 Gemini-3.1-Pro）仍存在微小差距，这可能受限于基座模型的参数量和预训练数据。

（完）
