---
title: "LiteGUI: Distilling Compact GUI Agents with Reinforcement Learning"
title_zh: LiteGUI：通过强化学习蒸馏紧凑型 GUI 智能体
authors: "Yubin Wu, Zicheng Cai, Liping Ning, Hua Wang, Zhi Chen, Yaohua Tang, Hao Chen"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07505v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 使用强化学习训练的非机器人视觉语言GUI智能体
tldr: LiteGUI 针对端侧轻量级 GUI 智能体性能受限及 SFT 易过拟合的问题，提出了一种无需 SFT 的训练范式。该方法通过引导式在线蒸馏结合动态检索减少幻觉，并引入多解双层 GRPO 框架，在宏观规划与微观执行层面进行对齐，显著提升了 2B/3B 规模模型的长程任务处理能力，在多个基准测试中达到领先水平。
selection_source: fresh_fetch
motivation: 传统的监督微调（SFT）在小规模 GUI 智能体上容易导致过拟合和策略僵化，难以满足高效跨平台交互的需求。
method: 提出引导式在线蒸馏技术，并结合多解双层 GRPO 框架，通过宏观子任务规划与微观执行匹配实现强化学习对齐。
result: 在多个基准测试中，LiteGUI 在轻量级模型中取得了 SOTA 性能，甚至能与参数量大得多的模型相媲美。
conclusion: 该研究证明了通过结构化在线蒸馏和多解双层探索，可以充分释放小规模智能体的潜力，突破传统模仿学习的性能瓶颈。
---

## 摘要
开发轻量级、端侧视觉语言 GUI 智能体对于实现高效的跨平台自动化交互至关重要。然而，目前的端侧智能体受限于模型容量，性能仍亟待进一步提升。针对小规模模型的传统监督微调（SFT）往往会导致过拟合、灾难性遗忘和策略僵化，因此无法充分解决这些挑战。在这项工作中，我们提出了一种新型的免 SFT 训练范式，显著增强了小规模模型的性能。我们首先通过引导式同策略蒸馏（Guided On-policy Distillation），首次将广义知识蒸馏系统性地整合到 GUI 智能体领域。通过结合专家参考轨迹与动态检索机制，我们的方法减少了幻觉，并缓解了多解 GUI 任务中固有的认知失调。在此基础上，我们进一步引入了一个多解双层 GRPO 框架，该框架将宏观层面的子任务规划与微观层面的执行匹配联合对齐，从而改善了长程 GUI 智能体场景中的探索。此外，我们构建了一个自动化数据生成流水线，用于合成具有丰富多解标注的 GUI 任务轨迹。大量实验表明，我们的方法在轻量级模型中实现了最先进的性能，同时在所有基准测试中与规模大得多的模型相比仍具竞争力。消融研究进一步证明，结构化的同策略蒸馏和多解双层探索可以充分释放 2B/3B 规模智能体的潜力，超越了传统模仿学习的性能极限。

## Abstract
Developing lightweight, on-device vision-language GUI agents is essential for efficient cross-platform automated interaction. However, current on-device agents are constrained by limited model capacity, and further performance improvements remain urgently needed. Traditional Supervised Fine-Tuning (SFT) for small-scale models often leads to overfitting, catastrophic forgetting and policy rigidity, and thus fails to fully address these challenges. In this work, we propose a novel SFT-free training paradigm that significantly enhances the performance of small-scale models. We first present the initial systematic integration of generalized knowledge distillation into the GUI agent domain via Guided On-policy Distillation. By incorporating oracle reference trajectories together with a dynamic retrieval mechanism, our method reduces hallucinations and mitigates the cognitive misalignment inherent in multi-solution GUI tasks. Building on this foundation, we further introduce a Multi-solution Dual-level GRPO framework that jointly aligns macro-level subtask planning with micro-level execution matching, thereby improving exploration in long-horizon GUI agent scenarios. In addition, we construct an automated data generation pipeline to synthesize GUI task trajectories with rich multi-solution annotations. Extensive experiments show that our method achieves state-of-the-art performance among lightweight models while remaining competitive with substantially larger-scale models across all benchmarks. Ablation studies further demonstrate that structured on-policy distillation and multi-solution dual-level exploration can fully unlock the capabilities of 2B/3B scale agents, surpassing the performance limits of conventional imitation learning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LiteGUI** 的新型训练范式，旨在提升轻量级端侧视觉 GUI 智能体（Agent）的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的端侧 GUI 智能体（通常为 2B-3B 参数规模）受限于模型容量，在处理复杂、长程任务时表现不佳。
*   **研究动机**：传统的**监督微调（SFT）**在小模型上容易导致过拟合、灾难性遗忘以及策略僵化（无法从错误中恢复）。
*   **整体含义**：论文提出了一种**免 SFT（SFT-free）**的训练范式，通过结合“引导式在线蒸馏”和“强化学习（RL）”，在不依赖静态模仿学习的情况下，充分释放小规模视觉语言模型的潜力。

### 2. 方法论：核心思想与关键技术
LiteGUI 采用两阶段训练流程，并配合自动化数据生成流水线：

*   **自动化数据流水线 (ATG)**：
    *   利用大模型（Qwen3-VL-32B）作为教师，通过“生成-验证-修正”闭环合成轨迹。
    *   引入**多解标注**：针对同一 GUI 状态，标注多个可行的正确动作，形成多路径专家数据集。
*   **第一阶段：引导式同策略蒸馏 (Guided-OPD)**：
    *   **核心思想**：让学生模型生成动作，教师模型在给定“特权信息”（参考轨迹）的情况下对学生生成的序列进行评估。
    *   **关键技术**：**最匹配参考检索 (Most-Matched-GT)**。系统动态推断学生的探索意图，从多解池中检索最接近学生当前输出的正确轨迹作为教师的引导，减少了因单一参考答案导致的认知失调和幻觉。
*   **第二阶段：多解双层 GRPO (MD-GRPO)**：
    *   **宏观层面（规划奖励）**：使用 VLM Judge 评估子任务规划（Subtask Planning）的合理性，确保模型理解任务进度。
    *   **微观层面（执行奖励）**：采用**多解动作匹配**，只要学生动作匹配多解池中的任一正确动作即可获得奖励，缓解了单一标签导致的“假阴性”惩罚。
    *   **算法**：基于 GRPO（群体相对策略优化），无需价值模型，通过组内样本相对质量进行优化。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **ScreenSpot-Pro**：评估 GUI 定位精度（Grounding Accuracy）。
    *   **OS-World**：评估在真实操作系统环境下的任务成功率（Success Rate）。
    *   **Lite-Bench（新提出）**：包含 160 个任务，涵盖文件系统、网页和终端，强调纯视觉交互。
*   **对比方法**：
    *   **通用模型**：GPT-4o, Claude 3.5 Sonnet, Qwen2.5/3-VL 系列。
    *   **专用 GUI 模型**：CogAgent, UI-TARS, ShowUI, JEDI, UI-R1 等。
*   **模型骨干**：以 Qwen3-VL-2B 和 Qwen3-VL-30B-A3B 为基础进行实验。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA A100 GPU 集群。
*   **具体配置**：
    *   **2B 模型蒸馏**：1 节点（8 张 A100），Batch Size 32。
    *   **A3B 模型蒸馏**：2 节点（16 张 A100）。
    *   **GRPO 阶段**：2 节点（16 张 A100），每条提示词生成 16 个候选样本（G=16）。
*   **训练时长**：文中未明确给出总小时数，但提到了蒸馏阶段为 2 个 Epoch，GRPO 阶段为 1 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流且具挑战性的基准上进行了全面测试。
    *   **消融实验**：详细对比了 SFT、标准 OPD、不同引导策略（Single/Multi/Most-Matched）以及双层奖励对性能的影响（见表 4）。
*   **充分性与公平性**：实验涵盖了从 2B 到 30B 的不同参数规模，对比对象既包括闭源商业模型也包括开源 SOTA 模型。通过多次运行取平均值（如 ScreenSpot-Pro）来确保结果的客观性。

### 6. 主要结论与发现
*   **性能突破**：LiteGUI-2B 在轻量级模型中达到 SOTA，甚至超越了参数量大得多的 UI-TARS-72B 和 JEDI-7B。
*   **免 SFT 的优越性**：实验证明，对于小模型，直接进行在线蒸馏和 RL 比先进行 SFT 效果更好，因为 SFT 会导致严重的“能力退化”和“过拟合”。
*   **多解的重要性**：在 GUI 任务中，允许模型探索多种正确路径（Multi-solution）是提升泛化能力和鲁棒性的关键。

### 7. 优点（亮点）
*   **训练范式创新**：首次在 GUI 领域系统性地引入了“引导式在线蒸馏”，并证明了免 SFT 路径的可行性。
*   **双层奖励机制**：将宏观规划与微观执行结合，有效解决了长程任务中的探索瓶颈。
*   **高质量资源贡献**：开源了包含 30K 轨迹的 Lite-Dataset 和新的 Lite-Bench，填补了高质量 GUI 数据的空白。

### 8. 不足与局限
*   **标注成本**：多解标注（Multi-solution annotation）虽然效果好，但比单路径标注更耗费人力和时间。
*   **Judge 模型依赖**：RL 阶段依赖于强大的 VLM（如 Qwen3-32B）作为裁判，这增加了训练成本，且裁判模型本身可能存在评估偏差。
*   **解空间的有限性**：标注的多解池仍然是真实解空间的有限近似，可能无法覆盖所有合理的交互方式。
*   **推理开销**：虽然模型是轻量级的，但为了维持长程记忆，历史窗口的维护仍会带来一定的内存压力。

（完）
