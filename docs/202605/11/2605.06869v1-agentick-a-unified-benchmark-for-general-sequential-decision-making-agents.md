---
title: "Agentick: A Unified Benchmark for General Sequential Decision-Making Agents"
title_zh: Agentick：通用序列决策智能体的统一基准
authors: "Roger Creus Castanyer, Pablo Samuel Castro, Glen Berseth"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06869v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 强化学习、大语言模型和视觉语言模型智能体在序列决策中的基准测试
tldr: Agentick是一个统一的通用序列决策智能体基准测试，旨在公平评估强化学习（RL）、大语言模型（LLM）、视觉语言模型（VLM）及人类智能体。它包含37个程序化生成的任务，涵盖6大能力类别和5种观察模态。研究发现，目前没有任何单一方法能主导所有任务，LLM在推理增强下表现显著提升，而RL在规划和多智能体任务中更具优势，该基准为通用自主智能体研究提供了重要基础设施。
selection_source: fresh_fetch
motivation: 现有的AI智能体研究在强化学习和大模型范式之间缺乏统一的评估标准，难以在相同环境下公平比较不同方法的优劣。
method: 提出了Agentick基准，包含37个跨越不同难度和模态的任务，并提供Gymnasium接口、参考策略、SFT数据集及可组合的智能体框架。
result: 实验表明GPT-5 mini在综合得分上领先，但PPO在规划和多智能体任务中表现更佳，且推理增强显著提升了LLM的性能。
conclusion: Agentick揭示了当前各类智能体范式在复杂序列决策中仍存在巨大挑战，为未来基础模型的强化学习后训练提供了理想的实验场。
---

## 摘要
AI 智能体研究涵盖了广泛的领域：从从零开始学习的强化学习（RL）智能体到利用预训练知识的基础模型智能体，然而目前尚无统一的基准能够对这些方法进行公平比较。我们提出了 Agentick，这是一个专为序列决策智能体设计的基准，旨在在统一平台上评估 RL、LLM、VLM、混合型以及人类智能体，并助力对序列决策基本挑战的研究。Agentick 提供了跨越 6 个能力类别、4 个难度级别和 5 种观测模态的 37 个程序化生成任务，所有任务均通过单一的 Gymnasium 兼容接口公开。该基准附带了编程 API、所有任务的专家（oracle）参考策略、预构建的 SFT 数据集、可组合的智能体框架（harness）以及实时排行榜。一项涵盖 27 种配置和超过 90,000 个回合的评估显示，没有任何一种方法占据绝对主导地位：GPT-5 mini 以 0.309 的专家归一化得分在总体上领先，而 PPO 在规划和多智能体任务中占据优势；推理框架将 LLM 的性能提升了 3 到 10 倍；且 ASCII 观测结果始终优于自然语言。这些发现强调了所有智能体范式仍存在巨大的改进空间。Agentick 的能力分解和多模态设计提供了推动通用自主智能体进步所需的实证基础设施，它既可以作为评估框架，也可以作为在真实序列环境中对基础模型进行 RL 后训练的训练场。

## Abstract
AI agent research spans a wide spectrum: from RL agents that learn from scratch to foundation model agents that leverage pre-trained knowledge, yet no unified benchmark enables fair comparison across these approaches. We present Agentick, a benchmark for sequential decision-making agents designed to evaluate RL, LLM, VLM, hybrid, and human agents on common ground and to power research on the fundamental challenges of sequential decision-making. Agentick provides 37 procedurally generated tasks across six capability categories, four difficulty levels, and five observation modalities, all exposed through a single Gymnasium-compatible interface. The benchmark ships with a Coding API, oracle reference policies for all tasks, pre-built SFT datasets, a composable agent harness, and a live leaderboard. An evaluation spanning 27 configurations and over 90,000 episodes reveals that no single approach dominates: GPT-5 mini leads overall at 0.309 oracle-normalized score while PPO dominates planning and multi-agent tasks; the reasoning harness multiplies LLM performance by 3-10x; and ASCII observations consistently outperform natural language. These findings highlight the substantial room for improvement that remains across all agent paradigms. Agentick's capability-decomposed, multi-modal design provides the empirical infrastructure needed to drive progress toward general autonomous agents, both as an evaluation framework and as a training ground for RL post-training of foundation models in truly sequential environments.

---

## 论文详细总结（自动生成）

以下是对论文《Agentick: A Unified Benchmark for General Sequential Decision-Making Agents》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前 AI 智能体研究存在范式割裂。强化学习（RL）智能体擅长从交互中学习但采样效率低；基础模型（FM/LLM/VLM）智能体拥有广泛先验但缺乏精确的序列控制能力。
*   **研究动机**：目前缺乏一个统一的基准测试，能够让不同范式的智能体（RL、LLM、VLM、混合型及人类）在相同的任务、相同的观测模态和相同的评估标准下进行公平竞争。
*   **整体含义**：Agentick 旨在填补这一空白，通过提供一个高度可控、多模态、能力分解的实验场，推动通用自主智能体（General Autonomous Agents）的研究。

### 2. 方法论：Agentick 基准设计
*   **核心思想**：基于四个原则设计：范式通用性、能力分解、训练优先、难度可控。
*   **关键技术细节**：
    *   **37 个任务与 6 大类别**：涵盖导航（Navigation）、规划（Planning）、推理（Reasoning）、记忆（Memory）、泛化（Generalization）和多智能体（Multi-Agent）。
    *   **五种同步观测模态**：ASCII 文本网格、自然语言描述、结构化字典、512×512 等轴像素渲染、原始 Numpy 状态数组。这确保了不同架构的智能体不会因观测格式受限。
    *   **专家归一化得分 (ONS)**：
        $$ONS = \frac{\text{agent\_return} - \text{random\_baseline}}{\text{oracle\_return} - \text{random\_baseline}}$$
        该指标将智能体表现与随机基准和专家策略（Oracle）对齐，便于跨任务比较。
    *   **编程 API 与专家策略**：提供 `AgentickAPI`，支持空间查询、路径规划等，用于构建专家策略并生成大规模 SFT 数据集（120K-500K 回合）。
    *   **可组合智能体框架 (Harness)**：解耦了模型后端与推理策略（如 Markovian 简单策略与 Markovian Reasoner 思维链策略）。

### 3. 实验设计
*   **对比方法**：
    *   **前沿 LLM**：GPT-5 mini, Gemini 3.1 Flash Lite, Claude Haiku 4.5。
    *   **从零训练的 RL**：使用 PPO 算法，基于像素观测训练（2M 步和 500k 步配置）。
    *   **开源 LLM**：Qwen3.5 系列（0.8B, 2B, 4B）及 Qwen3-4B。
*   **Benchmark 场景**：37 个程序化生成的任务，每个任务设有 Easy, Medium, Hard, Expert 四个难度等级。
*   **评估协议**：每个“任务-难度”组合使用 25 个确定性种子，总计每位智能体需运行 3,700 个回合。

### 4. 资源与算力
*   **RL 训练**：PPO 在 37 个任务上训练，每个任务 2M 步约需 4 小时（单张 NVIDIA A100 80GB），总计约 **150 GPU 小时**。
*   **LLM 评估**：
    *   **前沿模型**：通过商业 API 评估，费用约 $290（GPT-5 mini 占比最高）。
    *   **开源模型**：使用 vLLM 在 2 张 A100 上运行，总计约 **200 GPU 小时**。
*   **数据生成**：500K 专家回合数据集在 32 核 CPU 机器上约 8 小时完成。

### 5. 实验数量与充分性
*   **实验规模**：累计进行了超过 **90,000 个回合**的评估，涵盖 27 种不同的智能体配置。
*   **充分性与公平性**：
    *   **充分性**：通过 4 个难度梯度和 37 个任务全面覆盖了序列决策的多个维度。
    *   **公平性**：所有智能体在相同的种子（Seed）下运行；为 LLM 提供了最适合其处理的 ASCII 模态，为 RL 提供了标准的像素预处理，确保了比较的客观性。

### 6. 主要结论与发现
*   **无绝对主导者**：GPT-5 mini 综合得分最高（0.309 ONS），但在规划和多智能体任务中，从零训练的 PPO 显著优于所有 LLM。
*   **推理框架的巨大作用**：引入简单的思维链（CoT）推理框架，使 LLM 的性能提升了 **3 到 10 倍**。
*   **观测模态影响**：对于 LLM 而言，**ASCII 网格表现始终优于自然语言描述**，表明紧凑的令牌表示更有利于空间推理。
*   **推理任务是瓶颈**：所有智能体（包括前沿 LLM）在逻辑推理类任务（如程序合成、图着色）上表现极差，接近零分。

### 7. 优点与亮点
*   **统一接口**：通过 Gymnasium 接口实现了 RL 与 LLM 的无缝切换评估。
*   **训练友好型设计**：不仅是评估工具，还提供了大规模专家轨迹数据集，可直接用于基础模型的 RL 后训练（RLVR）。
*   **能力画像**：通过雷达图分解能力，能够清晰识别特定模型的短板（如 GPT-5 mini 强于导航但弱于推理）。
*   **程序化生成**：保证了任务的无限多样性和评估的不可作弊性。

### 8. 不足与局限
*   **环境抽象**：任务局限于 2D 离散网格世界，无法直接推广到连续控制或实时高动态环境。
*   **模型覆盖**：受预算限制，未测试各家厂商的最强旗舰模型（如 GPT-5 Pro 或 Claude Opus），目前数据仅代表“经济型”前沿模型。
*   **多智能体局限**：目前的多智能体任务中，对手是脚本化的，缺乏对智能体间协同演化（Co-adaptation）的深入探讨。
*   **能力缺失**：尚未涵盖持续学习（Continual Learning）等更高级的序列决策挑战。

（完）
