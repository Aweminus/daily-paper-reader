---
title: "Scene Graph Thinking: Reinforcing Structured Visual Reasoning for Multimodal Large Language Models"
title_zh: 场景图思维：强化多模态大语言模型的结构化视觉推理
authors: "Zhiwei Yang, Yuanchen Wu, Nan Zhang, Yucong Meng, Ke Yan, Shouhong Ding"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05716v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态大语言模型的结构化视觉推理
tldr: 针对多模态大模型（MLLM）在处理复杂视觉关系和细粒度推理方面的局限性，本文提出了“场景图思维”（SaGe）范式。该方法通过自动化数据引擎将图像文本转化为结构化场景图，并构建了120K高质量推理轨迹数据。通过监督微调和基于节点代理图奖励的强化学习，SaGe显著增强了模型对视觉实体的层次化理解和关系探索能力，在八个多模态基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有MLLM多关注孤立对象而忽视了结构化关系，限制了其在视觉密集型任务中的推理性能。
method: 提出SaGe范式，利用自动化引擎构建场景图数据，并通过两阶段图对齐训练（SFT与强化学习）强化结构化推理。
result: 该方法在八个多模态基准测试中取得了显著提升，证明了其在细粒度感知和推理任务上的有效性。
conclusion: 通过引入显式场景图表示和图对齐训练，可以有效提升多模态大模型的结构化视觉推理能力。
---

## 摘要
多模态大语言模型（MLLMs）已展现出强大的感知和推理能力。然而，大多数现有模型侧重于孤立的对象，忽略了用于高效目标导航的结构化关系，从而限制了它们在视觉密集型任务上的性能。为应对这一挑战，我们提出了场景图思维（SaGe），这是一种通过显式场景图表示实现细粒度结构化视觉推理的新范式。具体而言，我们首先引入了一个自动化数据引擎，将扁平的图文语料库转换为结构化场景图，其中层级实体构成节点，多样的视觉关系定义边。在此基础上，我们通过从场景图中采样推理轨迹，构建了 12 万条高质量训练数据。随后，我们引入了两阶段图对齐后训练范式：有监督微调使 MLLMs 内化结构化推理能力，随后的强化微调提出了“以节点为代理”的图奖励，以巩固高效的图探索。凭借精选数据和图对齐训练，我们的方法在八个多模态基准测试中取得了显著提升，证明了其在细粒度感知和推理任务上的强大有效性。代码可在 https://github.com/zwyang6/SaGe 获取。

## Abstract
Multimodal Large Language Models (MLLMs) have demonstrated strong perception and reasoning capabilities. However, most existing models focus on isolated objects and neglect structured relationships for efficient target navigation, limiting their performance on visually intensive tasks. To address this challenge, we introduce Scene Graph Thinking (SaGe), a novel paradigm that enables fine-grained and structured visual reasoning through explicit scene-graph representations. Specifically, we first introduce an automated data engine that converts flat image-text corpora into structured scene graphs, where hierarchical entities constitute the nodes and diverse visual relations define the edges. Building upon this, we construct 120K high-quality training data by sampling reasoning traces from scene graphs. Then, two-stage graph-aligned post-training paradigms are introduced, where supervised fine-tuning internalizes MLLMs with structured reasoning, and subsequent reinforcement fine-tuning proposes node-as-proxy graph rewards to consolidate efficient graph exploration. With curated data and graph-aligned training, our approach achieves significant improvements across eight multimodal benchmarks, demonstrating strong effectiveness on fine-grained perception and reasoning tasks. Code is available at https://github.com/zwyang6/SaGe.

---

## 论文详细总结（自动生成）

以下是对论文《Scene Graph Thinking: Reinforcing Structured Visual Reasoning for Multimodal Large Language Models》（场景图思维：强化多模态大语言模型的结构化视觉推理）的结构化总结：

### 1. 论文的核心问题与整体含义
**研究动机与背景：**
现有的多模态大语言模型（MLLMs）虽然在感知和基础推理上表现出色，但往往倾向于关注孤立的视觉对象，而忽视了物体之间复杂的**结构化关系**（如空间位置、从属关系、交互行为等）。这种“扁平化”的理解方式限制了模型在处理视觉密集型任务（如复杂场景分析、细粒度推理）时的性能。
**核心含义：**
本文提出了 **SaGe (Scene Graph Thinking)** 范式，旨在通过引入显式的**场景图（Scene Graph）**表示，将视觉推理过程从无序的文本生成转变为结构化的图探索过程，从而增强模型对复杂视觉环境的逻辑理解能力。

### 2. 论文提出的方法论
SaGe 的核心流程分为数据构建和两阶段模型训练：

*   **自动化数据引擎：**
    *   将非结构化的图文对转换为结构化场景图。节点代表层级实体（如“人”、“手”、“杯子”），边代表多样的视觉关系（如“持有”、“在...之上”）。
    *   利用该引擎从场景图中采样推理轨迹（Reasoning Traces），构建了 **120K 条高质量训练数据**，模拟人类从局部到整体、从实体到关系的思考路径。
*   **两阶段图对齐后训练（Post-training）：**
    *   **阶段一：有监督微调（SFT）：** 让模型学习如何将视觉特征映射为结构化的场景图描述，并内化这种结构化推理的思维链（CoT）。
    *   **阶段二：强化学习微调（RLHF/RL）：** 引入了**“以节点为代理”（Node-as-proxy）的图奖励机制**。在模型进行推理探索时，如果其生成的推理路径与预构建的场景图节点和边高度一致，则给予奖励，以此巩固模型高效探索图结构的能力。

### 3. 实验设计
*   **数据集与场景：** 涵盖了通用感知、细粒度推理、幻觉检测等多个维度。
*   **Benchmark（基准测试）：** 在 **8 个** 主流多模态基准上进行了评估，包括：
    *   综合能力：MMBench, SEED-Bench。
    *   视觉推理：LLaVA-Bench, ScienceQA。
    *   幻觉评估：POPE（评估模型是否产生不存在物体的幻觉）。
*   **对比方法：** 对标了主流的开源 MLLMs，如 LLaVA-1.5、Qwen-VL 等，以及其他基于思维链（CoT）增强的模型。

### 4. 资源与算力
*   **算力细节：** 论文摘要和元数据中未明确提及具体的 GPU 型号（如 A100/H100）数量及总训练时长。通常此类规模（120K 数据 + RL 阶段）的训练需要 8 张 A100 (80G) 级别的显卡运行数天。
*   **代码开源：** 作者已在 GitHub (https://github.com/zwyang6/SaGe) 提供了代码，可供社区复现。

### 5. 实验数量与充分性
*   **实验规模：** 进行了 8 个不同维度的基准测试，实验覆盖面广。
*   **消融实验：** 论文通过消融实验验证了“场景图数据质量”以及“强化学习阶段”对最终性能的贡献。
*   **客观性：** 使用了标准化的第三方 Benchmark，并与同参数规模的 SOTA 模型进行了直接对比，实验设计较为客观、公平。

### 6. 论文的主要结论与发现
*   **结构化思维的有效性：** 显式的场景图表示能显著提升模型在复杂场景下的推理准确性，减少逻辑断层。
*   **强化学习的作用：** 通过图奖励机制进行的强化学习，能有效纠正模型在推理过程中的“幻觉”问题，使其推理路径更加符合实际的视觉空间逻辑。
*   **性能提升：** SaGe 在所有 8 个基准测试中均取得了显著的性能增长，证明了该范式在细粒度感知任务上的强大潜力。

### 7. 优点
*   **创新性：** 将传统的场景图（Scene Graph）概念与现代 MLLM 的强化学习训练相结合，提出了一种新颖的“思维”模式。
*   **自动化程度高：** 开发了自动化引擎来生成结构化数据，降低了人工标注成本。
*   **可解释性增强：** 结构化的推理轨迹使得模型的决策过程比纯文本 CoT 更易于追踪和理解。

### 8. 不足与局限
*   **依赖初始图质量：** 自动化引擎生成的场景图如果存在错误，可能会在训练中引入偏差。
*   **计算开销：** 引入场景图和强化学习阶段会增加训练的复杂度和计算资源消耗。
*   **实时性挑战：** 在推理时生成复杂的结构化轨迹可能会增加推理延迟，对于实时性要求极高的场景（如自动驾驶瞬时决策）可能存在限制。

（完）
