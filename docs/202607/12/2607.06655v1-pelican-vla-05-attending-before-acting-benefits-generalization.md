---
title: "Pelican-VLA 0.5: Attending Before Acting Benefits Generalization"
title_zh: Pelican-VLA 0.5：先关注后行动有利于提升泛化能力
authors: "Zeyuan Ding, Wenhai Liu, Yang Xu, Jiayu Hu, Yinda Chen, Yi Zhang, Yong Dai, Jian Tang, Xiaozhu Ju"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06655v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 带有推理槽的VLA模型用于动作预测
tldr: Pelican-VLA 0.5 是一款集成了视觉语言理解、未来帧生成和动作预测的统一 VLA 模型。该研究通过在感知与动作层之间引入可学习的“推理槽”（Reasoning Slots），利用信息瓶颈机制路由任务相关的视觉信息。实验证明，该模型在无需额外标注或微调的情况下，能自动聚焦于指令相关的物体和接触区域，在未知场景和机器人形态上展现出卓越的泛化能力，显著优于现有开源基准。
selection_source: fresh_fetch
motivation: 旨在解决现有 VLA 模型在处理未知场景和机器人形态时泛化性差，且难以自动识别任务关键视觉区域的问题。
method: 在感知与动作路径间插入可学习的推理槽，通过紧凑的瓶颈结构强制模型在生成动作前提取关键视觉特征。
result: 模型实现了注意力级别的泛化，能精准锁定任务相关物体，且在跨场景和跨机器人实验中表现出比同类模型更强的鲁棒性。
conclusion: 研究表明，在动作执行前引入推理引导的注意力机制（Attending Before Acting）是提升 VLA 模型泛化性能的关键。
---

## 摘要
在本报告中，我们介绍了 Pelican-VLA 0.5，这是一个在单一架构中集成了视觉语言理解、未来帧生成和动作预测的统一 VLA 模型。Pelican-VLA 0.5 实现了注意力层级的泛化：在没有物体标注、分割掩码、注意力监督或特定任务微调的情况下，其动作路径已经能够聚焦于与指令相关的物体和接触区域。这种行为在未见场景和未见机器人实体中依然存在，并且显著强于其他开源 VLA 基准模型。我们验证了这种能力源于插入在感知和动作之间的可学习推理槽位（Reasoning Slots）：通过将任务相关的视觉信息路由经过一个紧凑的瓶颈结构，槽位接口在预训练期间诱导出了以操作为中心的注意力，并在包括 MoT 风格架构在内的不同策略结构中保持有效。

## Abstract
In this report, we present Pelican-VLA 0.5, a unified VLA model that integrates vision-language understanding, future-frame generation, and action prediction within a single architecture. Pelican-VLA 0.5 achieves attention-level generalization: without object annotations, segmentation masks, attention supervision, or task-specific fine-tuning, its action pathway already focuses on the instruction-relevant object and contact region. This behavior persists across unseen scenes and unseen robot embodiments, and is substantially stronger than in other open-source VLA baselines. We verify that this ability originates from the learnable Reasoning Slots inserted between perception and action: by routing task-relevant visual information through a compact bottleneck, the slot interface induces manipulation-centric attention during pre-training and remains effective across different policy structures, including a MoT-style architecture.

---

## 论文详细总结（自动生成）

这篇论文由北京人形机器人创新中心（X-Humanoid）发布，介绍了一个名为 **Pelican-VLA 0.5** 的统一视觉-语言-动作（VLA）模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 VLA 模型在面对新物体、新场景和新机器人形态时泛化能力较弱，且往往需要针对特定任务进行大量微调。
*   **研究动机**：作者发现现有模型的“动作路径”注意力过于分散（常关注背景或机械臂本身），而非聚焦于任务相关的物体。
*   **整体含义**：论文提出“先关注后行动”（Attending Before Acting）的理念，认为在感知与动作之间建立一个紧凑的“推理瓶颈”，有助于模型学习到可跨场景迁移的内部表示，从而提升泛化性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在 Transformer 骨干网络中引入一组可学习的**推理槽（Reasoning Slots）**，强制所有感知信息必须通过这个有限的“瓶颈”才能传递给动作生成模块。
*   **关键技术细节**：
    *   **统一架构**：基于 Qwen3-VL 4B 骨干网络，将视觉语言理解、未来帧预测和动作生成集成在单一 Token 流中。
    *   **四段式输入序列**：包括 Prefix（视觉语言 Token）、Middle（用于动力学建模的 Cosmos 潜变量）、**Slots（32 个推理槽）**、Suffix（机器人状态与加噪动作）。
    *   **瓶颈机制**：
        *   **课程注意力掩码**：训练初期允许直接关注，后期强制动作路径只能通过推理槽读取感知信息。
        *   **正交正则化**：通过损失函数确保不同槽位捕捉互补的信息，防止表示坍缩。
        *   **槽位门控生成**：利用槽位信息调制未来帧的预测，增强其动作相关性。
    *   **训练目标**：结合了流匹配（Flow-matching）动作损失、未来帧生成损失、槽位-语言对比学习损失（InfoNCE）以及正交正则化损失。

### 3. 实验设计
*   **数据集**：使用了超过 6000 小时的异构机器人操作数据（包括 AgiBot World Alpha, InternData-A1, Galaxea 等），实际预训练使用了约 2400 小时的数据。
*   **Benchmark**：
    *   **仿真环境**：RoboTwin 2.0（包括 Clean 和 Randomized 两个子集）。
    *   **真实世界**：在“天工”（TienKung）人形机器人上进行桌面清理任务。
*   **对比方法**：对比了 $\pi_0$、$\pi_{0.5}$、X-VLA、StarVLA、Qwen-VLA、Hy-VLA 等多个开源 VLA 基准模型。

### 4. 资源与算力
*   **模型规模**：骨干网络为 Qwen3-VL 4B。
*   **训练细节**：使用 bfloat16 精度，AdamW 优化器，峰值学习率 $5 \times 10^{-5}$。
*   **算力说明**：文中提到每张 GPU 的 Batch Size 为 6，预训练进行了 0.4 个 Epoch（约 2400 小时数据量）。**但论文未明确说明具体的 GPU 型号（如 H100/A100）及总卡数。**

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 RoboTwin 上进行了 10 余个基准模型的性能对比。
    *   进行了详细的**消融实验**，验证了推理槽、对比损失、生成损失各自的作用。
    *   进行了**零样本（Zero-shot）泛化实验**，测试了未见过的物体、场景和机器人形态。
    *   提供了**注意力可视化分析**，对比了不同架构（如 MoT 风格）下的注意力分布。
*   **充分性评价**：实验设计较为全面，尤其是通过 IoU 定量分析注意力演变过程，以及对比微调前后的表示相似性，论证了“表示-动作间隙”的存在，逻辑严密且客观。

### 6. 主要结论与发现
*   **注意力级别泛化**：Pelican-VLA 0.5 在无监督情况下，预训练后即可自动聚焦于指令相关物体，这种能力在跨形态机器人上依然有效。
*   **推理槽的决定性作用**：消融实验证明，物体中心注意力的产生主要归功于推理槽的瓶颈设计，而非单纯的数据量或辅助损失。
*   **表示与动作的脱节**：模型在零样本下能“看对”物体并尝试接近，但由于数据规模和动作表示（关节空间）的限制，最终执行成功率仍有待提高。
*   **微调的本质**：微调并不会改变注意力模式，而是强化了从“已有表示”到“精确动作”的映射。

### 7. 优点
*   **创新的瓶颈设计**：通过推理槽成功诱导出了以物体为中心的注意力，提升了模型的可解释性和泛化潜力。
*   **推理效率**：推理槽支持缓存机制（Cached Inference），在动作去噪循环中无需重复计算庞大的视觉 Token，显著降低了计算开销。
*   **统一性**：将理解、预测和控制整合，证明了多任务联合训练对机器人表示学习的增益。

### 8. 不足与局限
*   **零样本执行成功率低**：虽然注意力实现了泛化，但在完全未见的场景下，精确操作（如稳定抓取）的成功率依然有限。
*   **动作表示限制**：采用关节位置（Joint-position）作为动作空间，这在跨机器人形态迁移时比末端执行器位姿（EEF Pose）更具挑战性。
*   **数据噪声敏感性**：作者观察到语言条件注意力在训练后期有所退化，推测与训练数据中语言-动作不匹配的噪声有关。

（完）
