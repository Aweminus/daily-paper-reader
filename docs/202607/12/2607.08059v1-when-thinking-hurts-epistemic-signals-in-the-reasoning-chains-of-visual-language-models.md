---
title: "When Thinking Hurts: Epistemic Signals in the Reasoning Chains of Visual Language Models"
title_zh: 当思考变得痛苦：视觉语言模型推理链中的认知信号
authors: Mayank Singal
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08059v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 对思考模式VLM中推理链和熵的实证表征
tldr: 本研究探讨了视觉语言模型（VLM）在“思考模式”下的不确定性量化。通过在POPE、VQAv2和HallusionBench等数据集上测试Qwen3、GLM-4.1V和InternVL3三种模型，研究发现推理链的熵（chain entropy）在预测模型可靠性方面普遍优于传统的答案熵。研究揭示了不同模型在思考过程中的三种行为模式，并证明了利用推理链信号可显著提升模型在复杂任务中的准确率。
selection_source: fresh_fetch
motivation: 传统VLM不确定性量化仅关注答案分布，忽略了推理链中蕴含的丰富认知信号。
method: 对比分析了三种主流思考型VLM在多个视觉问答任务中推理链熵与答案熵的预测性能。
result: 推理链熵在所有测试模型中均表现出更强的预测力，尤其是在答案熵失效的情况下优势更明显。
conclusion: 推理链是比最终答案更可靠的不确定性指标，通过结构化弃答机制可大幅提升模型输出的准确性。
---

## 摘要
视觉语言模型（VLM）的不确定性量化通常针对答案标记分布。我们首次对思考模式 VLM 中答案熵的行为进行了三类实证表征。通过在相同的 POPE 对抗样本上运行四个模型，我们发现了三种定性上截然不同的模式：Qwen3-VL-8B-Thinking 表现出完全崩溃（答案熵 AUROC = 0.492）；GLM-4.1V-9B-Thinking 未表现出崩溃（0.716）；而 InternVL3-8B 表现出选择性思考（仅在 50% 的样本上生成推理链，全样本答案熵为 0.675，仅思考样本为 0.602）。在所有三种思考模式模型中，在生成推理链的子集上，推理链熵的表现均优于答案熵（分别为 0.647、0.759、0.608 对比 0.492、0.716、0.602），这表明只要存在推理链，链信号就是更可靠的预测指标。这一结论在 Qwen 和 GLM 上表现强劲，但在 InternVL3 上仅具有微弱且统计上不可靠的优势（n_FP = 17）。一项包含 300 个样本的 VQAv2 初步实验证实，在 VQAv2 问题上，推理链熵（0.680）优于答案熵（0.595），且在自由回答中差距最大（0.733 对比 0.467）。在更难的推理任务（HallusionBench）中，两个 Qwen 模型均显示出中等强度的信号（约 0.64），这与在困难问题上不完全的预承诺相一致。此外，我们还记录了影响 12-22% 查询的结构化弃权现象，且对缺失对象查询具有不对称性；同时，一种实用的弃权门控在 62.7% 的覆盖率下将准确率从 71.0% 提高到 93.8%，且无需额外的推理成本。

## Abstract
Uncertainty quantification for visual language models (VLMs) conventionally targets the answer token distribution. We provide the first three-family empirical characterisation of answer entropy behaviour in thinking-mode VLMs. Running four models on identical POPE adversarial samples, we find three qualitatively distinct patterns: Qwen3-VL-8B-Thinking shows complete collapse (ans H AUROC = 0.492); GLM-4.1V-9B-Thinking shows no collapse (0.716); and InternVL3-8B shows selective thinking (chains on only 50% of samples, ans H = 0.675 full / 0.602 thinking-only). Across all three thinking-mode models, thinking chain entropy outperforms answer entropy on the subset where chains are generated (0.647, 0.759, 0.608 vs. 0.492, 0.716, 0.602 respectively), suggesting chain signals are the more reliable predictor whenever chains are present. This holds strongly for Qwen and GLM, but with only marginal and statistically unreliable advantage for InternVL3 (n_FP = 17). A 300-sample VQAv2 pilot confirms chain entropy (0.680) outperforms answer entropy (0.595) on VQAv2 questions, with the gap largest for free-form answers (0.733 vs. 0.467). On harder reasoning tasks (HallusionBench) both Qwen models show moderate signal (approx. 0.64), consistent with incomplete pre-commitment on difficult questions. We additionally document structured abstention affecting 12-22% of queries with asymmetry toward absent-object queries, and a practical abstention gate raising accuracy from 71.0% to 93.8% at 62.7% coverage with no additional inference cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《When Thinking Hurts: Epistemic Signals in the Reasoning Chains of Visual Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着视觉语言模型（VLM）引入“思考模式”（Thinking-mode，即在输出答案前生成 `<think>...</think>` 推理链），传统的不确定性量化方法（如基于答案 token 分布的熵）面临失效风险。
*   **核心问题**：推理链作为一种强先验，会使模型在输出最终答案前就已完成“预承诺”（Pre-commitment），导致答案 token 的分布坍塌，从而破坏了传统不确定性指标的可靠性。
*   **研究动机**：探索在思考模式下，推理链本身是否蕴含比最终答案更丰富的认知信号（Epistemic Signals），以实现更精准的幻觉检测和不确定性量化。

### 2. 论文提出的方法论
论文提出利用推理链生成过程中的内部信号作为不确定性的度量指标，核心思想是“思考过程的挣扎程度反映了模型的不确定性”。
*   **核心指标**：
    *   **推理链熵 ($H_{think}$)**：计算 `<think>` 块内所有 token 的 Shannon 熵的平均值。公式为 $H_t = -\sum p_t(v) \log p_t(v)$。
    *   **推理链长度 ($L$)**：推理链包含的 token 总数。
    *   **答案熵 ($H_{ans}$)**：作为基准对比，计算最终答案 token 的平均熵。
*   **关键技术细节**：这些信号均来自于单次贪婪解码（Greedy Decoding）的转发过程，无需多次采样或额外的探测模型，具有**零额外推理成本**的优势。
*   **弃权门控（Abstention Gate）**：基于推理链长度或熵设定阈值，自动拒绝那些模型表现出高度不确定性（推理链过长或熵过高）的样本。

### 3. 实验设计
*   **模型选择**：涵盖了三个主流模型家族：Qwen3-VL-8B-Thinking、GLM-4.1V-9B-Thinking、InternVL3-8B，并使用 Qwen3-VL-8B-Instruct 作为非思考模式的对照组。
*   **数据集/场景**：
    *   **POPE Adversarial**：用于检测物体幻觉（1,000 个样本）。
    *   **HallusionBench**：针对视觉幻觉和复杂推理的进阶诊断（951 个样本）。
    *   **VQAv2 (Pilot)**：测试开放式问答场景下的泛化能力（300 个样本）。
*   **对比基准**：主要对比 $H_{ans}$（答案熵）与 $H_{think}$（推理链熵）在区分真阳性（TP）和假阳性（FP）时的 AUROC 表现。

### 4. 资源与算力
*   **硬件**：所有实验均在单张 **NVIDIA A100-SXM4-40 GB GPU** 上运行。
*   **精度**：模型以 **bfloat16** 精度加载。
*   **时长**：文中未明确说明总训练或推理时长，但强调了该方法是基于单次前向传播的，因此推理开销极低。

### 5. 实验数量与充分性
*   **实验规模**：总计处理了约 2,000-3,000 个基准测试样本。
*   **充分性**：实验设计较为全面，不仅跨越了不同的模型家族，还通过消融实验（Instruct vs Thinking 版本对比）直接证明了“思考模式”是导致答案熵坍塌的根本原因。
*   **客观性**：通过对答案方向（Yes/No）进行条件化处理，排除了模型偏好对熵值的影响，确保了评估的公平性。

### 6. 论文的主要结论与发现
*   **答案熵坍塌**：在 Qwen3-Thinking 模型中，答案熵的 AUROC 降至 0.492（等同于随机猜测），证明了思考模式确实会破坏传统的认知信号。
*   **推理链信号更优**：在所有测试的思考型模型中，$H_{think}$ 的表现均优于 $H_{ans}$。在 Qwen 和 GLM 上优势显著，在 InternVL3 上则表现为“思考链的存在与否”本身就是一个强信号。
*   **认知层次结构**：模型状态遵循“正确提交 < 错误提交 < 弃权”的熵值/长度递增规律，证明这些信号真实反映了模型的认知状态。
*   **实用价值**：通过简单的弃权门控，在 POPE 任务上将准确率从 71.0% 提升至 93.8%（覆盖率为 62.7%）。

### 7. 优点
*   **首创性**：首次系统性地表征了思考模式 VLM 中的答案熵坍塌现象。
*   **高效性**：提出的推理链信号是“免费”的副产品，无需像传统方法那样进行多次采样（Self-consistency）或训练额外的探针。
*   **跨模型验证**：结论在多个不同架构和训练方案的模型家族中得到了验证，具有较强的普适性。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 8B-9B 规模的模型，尚未验证更大规模（如 32B 或 72B）模型是否存在相同的坍塌模式。
*   **解码策略单一**：所有结果均基于贪婪解码，尚未探讨随机采样（Temperature > 0）是否能缓解答案熵坍塌。
*   **任务类型有限**：虽然进行了 VQAv2 试点实验，但主要分析仍集中在二元分类任务（Yes/No）上，对于更复杂的长文本生成任务的适用性有待进一步研究。
*   **InternVL3 深度不足**：相比 Qwen 和 GLM，对 InternVL3 的详细指标分析（如弃权门控）不够深入。

（完）
