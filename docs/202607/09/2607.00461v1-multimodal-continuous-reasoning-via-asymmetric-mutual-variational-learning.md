---
title: Multimodal Continuous Reasoning via Asymmetric Mutual Variational Learning
title_zh: 通过非对称互变分学习实现多模态连续推理
authors: "Shijie Li, Yilin Gao, Siyuan Yang, Tieyuan Chen, Chaofan Gan, Zhihao He, Zicheng Zhao, Yuyu Guo, Weiyao Lin, Hang Yu"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00461v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 针对复杂视觉任务的连续潜空间推理
tldr: 针对多模态大模型在连续潜空间推理中存在的“答案泄露”和训练-推理不匹配问题，本文提出了非对称互变分学习（AMVL）框架。该框架通过双向KL散度校准目标，在训练先验匹配后验的同时，利用反向KL散度约束后验以避免过度依赖答案信息。实验证明，AMVL在BLINK等复杂推理基准上显著优于离散和潜空间推理基准，大幅提升了模型在复杂视觉推理任务中的表现和潜空间稳定性。
selection_source: fresh_fetch
motivation: 现有的多模态连续潜空间推理在训练时容易利用答案捷径，导致推理阶段的先验分布与训练阶段的后验分布严重失配。
method: 提出AMVL框架，通过前向KL散度训练先验并引入反向KL散度正则化后验，实现双向校准以减少先验污染。
result: 在BLINK基准测试中平均得分提升了10.83分，部分任务提升高达32.00分，显著超越了现有的离散和潜空间推理模型。
conclusion: AMVL通过双向变分学习有效解决了连续推理中的答案泄露问题，为多模态大模型提供了更稳定、高效的潜空间推理能力。
---

## 摘要
多模态大语言模型 (MLLMs) 通常受限于语言空间瓶颈，迫使复杂的视觉推理转化为离散标记，这可能会丢失感知的细微差别。一种有前景的替代方案是连续潜空间推理，其目标是发现连接多模态查询和最终答案的隐式推理路径。然而，这引入了严重的训练-推理不匹配：以真实答案为条件的训练时后验可能会利用依赖于答案的捷径。标准的变分训练随后会迫使推理时的先验去模仿一个能够访问测试时不可用信息的后验，从而导致性能不佳。为了解决这个问题，我们提出了非对称互变分学习 (AMVL)，这是一个通过双向校准目标解决这种不匹配的框架。前向 KL 散度训练目标无关的先验以匹配后验，而一种新颖的反向 KL 散度同时对后验进行正则化，防止其坍缩到推理不兼容的区域并缓解这种“答案泄漏”。我们提供了理论分析，将这种泄漏形式化为先验污染，并证明了我们的双 KL 目标减少了这种污染。我们在潜空间集成的 MLLM 中实例化了 AMVL，并表明它始终优于强大的离散和潜空间推理基线，在复杂的 BLINK 基准测试中平均得分提高了 +10.83，在单个推理任务中获得了高达 +32.00 的提升，分析证实了潜空间稳定性的提高。

## Abstract
Multimodal Large Language Models (MLLMs) are often constrained by a language-space bottleneck, forcing complex visual reasoning into discrete tokens which can lose perceptual nuance. A promising alternative is continuous latent reasoning, where the goal is to discover implicit reasoning pathways that bridge the multimodal query and the final answer. However, this introduces a severe train-inference mismatch: a training-time posterior, conditioned on the ground-truth answer, can exploit answer-dependent shortcuts. Standard variational training then forces the inference-time prior to mimic a posterior that has access to information unavailable at test time, leading to poor performance. To address this, we propose Asymmetric Mutual Variational Learning (AMVL), a framework that resolves this mismatch via a bidirectional calibration objective. A forward KL divergence trains the target-agnostic prior to match the posterior, while a novel reverse KL divergence simultaneously regularizes the posterior, preventing it from collapsing into inference-incompatible regions and mitigating this ``answer leakage''. We provide theoretical analysis formalizing this leakage as prior contamination and prove that our dual-KL objective reduces it. We instantiate AMVL in a latent-integrated MLLM and show that it consistently outperforms strong discrete and latent-reasoning baselines, improving the average score on the complex BLINK benchmark by +10.83 and achieving gains of up to +32.00 on individual reasoning tasks, with analyses confirming improved latent-space stability.

---

## 论文详细总结（自动生成）

### 论文总结：通过非对称互变分学习实现多模态连续推理 (AMVL)

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理复杂视觉推理时，通常受限于**“语言空间瓶颈”**：即必须将高维、连续的视觉感知转化为离散的文本标记（Tokens），这会导致感知细节的丢失和推理漂移。
虽然**连续潜空间推理**（Latent Reasoning）能缓解这一问题，但它面临严重的**“训练-推理不匹配”**：
*   **训练阶段**：后验分布（Posterior）可以接触到真实答案，容易利用答案作为捷径（答案泄露），构建出“作弊”的推理路径。
*   **推理阶段**：先验分布（Prior）无法接触答案。如果仅使用标准变分推断（ELBO），先验会被迫模仿那个“作弊”的后验，导致测试时性能低下。

#### 2. 方法论：核心思想与关键技术
论文提出了**非对称互变分学习（AMVL）**框架，通过双向校准目标解决上述不匹配问题：
*   **核心思想**：建立先验和后验之间的非对称互学习机制。不仅让先验去匹配后验，还通过反向约束限制后验，防止其向“答案泄露”的区域坍缩。
*   **关键技术细节**：
    *   **前向 KL 对齐 ($L_{fwd}$)**：训练目标无关的先验分布去匹配后验推断的潜状态，确保推理时的先验具备表达能力。
    *   **反向 KL 正则化 ($L_{rev}$)**：引入反向 KL 散度作为正则项，约束后验分布不要偏离先验太远，从而抑制后验利用答案走捷径。
    *   **架构集成**：在 MLLM（如 Qwen2.5-VL）中插入 $k$ 个连续潜变量槽位（Latent Slots），使用轻量级的“LLM原生变分头”来参数化对角高斯分布。
    *   **非对称调度**：通过停止梯度（stop-gradient）操作确保两个 KL 项分别更新不同的分支，并采用延迟退火策略平衡训练。

#### 3. 实验设计
*   **数据集/场景**：
    *   **细粒度感知**：V*、HRBench4K、HRBench8K（评估高分辨率处理和密集特征提取）。
    *   **复杂视觉推理**：BLINK 基准测试（涵盖 14 个感知重任务，如拼图、空间关系等）。
    *   **分布外（OOD）泛化**：VisualPuzzles（评估抽象推理的迁移能力）。
*   **对比方法**：
    *   **离散推理基线**：Vision-R1, PAPO（文本 CoT）；PixelReasoner, DeepEyes（视觉工具增强）。
    *   **连续潜空间推理基线**：LVR, Mull-Tokens, Monet。

#### 4. 资源与算力
*   **硬件**：使用了 **16 台 NVIDIA A100 GPU**。
*   **训练时长**：主模型训练大约耗时 **20 小时**。
*   **基础模型**：基于 Qwen2.5-VL-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了广泛的实验，包括在 4 个主流基准上的性能对比，以及针对 VisualPuzzles 的 OOD 测试。
*   **消融实验**：非常充分。涵盖了损失函数项（前向 vs 反向 KL）、潜变量配置（槽位数量 $k$ 和维度 $d$）、变分头架构、损失权重、停止梯度设计以及推理采样温度等多个维度的测试。
*   **客观性**：通过理论证明（Proposition 1-2）与实验数据相互印证，并使用了相同的基座模型进行公平对比，实验设计严谨、客观。

#### 6. 主要结论与发现
*   **性能领先**：AMVL 在所有基准上均优于离散和连续推理基线。在 BLINK 上平均提升 **+10.83** 分，在 Jigsaw（拼图）任务上提升高达 **+32.00** 分。
*   **解决泄露**：理论和实验均证明，双向 KL 校准能有效减少先验污染，使潜空间在推理阶段更加稳定。
*   **感知增强**：连续潜空间推理在处理空间逻辑和细粒度视觉搜索方面显著优于依赖文本描述的方法。

#### 7. 优点：亮点总结
*   **理论完备**：不仅提出了算法，还通过数学分析形式化了“答案泄露”为先验污染，并证明了双向 KL 的有效性。
*   **无需人工标注**：不同于以往依赖人工设计监督信号（如重建损失）的方法，AMVL 实现了端到端的潜空间发现。
*   **高效集成**：变分头设计轻量且与 LLM 架构原生兼容，易于扩展到其他多模态模型。

#### 8. 不足与局限
*   **模型规模限制**：目前的实验主要集中在 **7B 参数规模**，尚未在大规模模型（如 70B+）上验证其涌现能力。
*   **潜在风险**：增强的推理能力可能被误用于生成更高级的虚假信息或自动化恶意活动。
*   **计算开销**：虽然推理时高效，但训练阶段需要维护先验和后验两个分支，且双向 KL 的优化比标准 SFT 略复杂。

（完）
