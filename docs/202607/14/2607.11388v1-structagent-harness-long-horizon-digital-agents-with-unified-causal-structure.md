---
title: "StructAgent: Harness Long-horizon Digital Agents with Unified Causal Structure"
title_zh: StructAgent：通过统一因果结构强化长程数字智能体
authors: "Wenyi Wu, Sibo Zhu, Kun Zhou, Aayush Salvi, Zixuan Song, Biwei Huang"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11388v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于计算机操作数字智能体的视觉语言模型
tldr: 针对长程数字化任务中交互历史冗长且难以验证的问题，本文提出 StructAgent 框架。该框架通过统一的因果结构对智能体的状态和工作流进行显式建模，引入可验证的状态转换机制，支持进度检查点、证据驱动的任务完成及针对性故障恢复。实验表明，该方法显著提升了多种模型在 OSWorld 和 Minecraft 任务中的成功率，刷新了开源模型的最优性能。
selection_source: fresh_fetch
motivation: 现有的数字化智能体依赖原始交互历史，导致长程任务进度难以解释、验证和恢复，限制了执行的可靠性。
method: 提出以状态为中心的 StructAgent 框架，利用统一的因果结构维护紧凑可验证的任务进度，并通过验证器支持的状态转换规范工作流。
result: "在 OSWorld-Verified 基准测试中，StructAgent 将 Qwen3.5-27B 的成功率从 31.6% 提升至 62.2%，并以 78.9% 的成绩创下开源模型新纪录。"
conclusion: 显式结构化智能体状态与工作流是实现可靠长程任务执行的关键，且该设计在桌面环境和游戏场景中具有良好的通用性。
---

## 摘要
大语言模型（LLM）和视觉语言模型（VLM）的最新进展使得用于计算机操作的数字智能体能力日益增强。然而，现实世界的任务通常是长程的（long-horizon），且涉及不断演变的上下文，其中包含累积的观察结果、中间编辑、失败的尝试以及部分完成的执行。现有的智能体通常基于原始交互历史进行操作，这使得任务进度难以解释、验证和恢复，最终限制了可靠的长程执行。在本文中，我们认为应对这一挑战需要围绕任务进度的统一因果表示，显式地构建智能体的状态和工作流。我们提出了 StructAgent，这是一个以状态为中心的框架，它引入了一个统一状态来维护简洁且可验证的任务进度，并设计了一个结构化工作流，通过基于验证器的状态转换来调节进度。基于这一设计，StructAgent 进一步实现了显式的进度检查点（checkpointing）、证据驱动的任务完成、针对性的故障恢复以及工具支持的执行，同时确保所有进度更新都基于验证。广泛的实验表明，StructAgent 在长程计算机使用任务上持续提升了多种 LLM 和 VLM 基座模型的性能。在 OSWorld-Verified 基准测试中，它将 Qwen3.5-9B 的成功率从 27.0% 提升至 46.9%，将 Qwen3.5-27B 从 31.6% 提升至 62.2%，并凭借 MiniMax-M3 达到了 78.9% 的开源新 SOTA。此外，该框架还可以从桌面环境泛化到 Minecraft，证明了我们设计的通用性。

## Abstract
Recent advances in large language models (LLMs) and vision-language models (VLMs) have enabled increasingly capable digital agents for computer use. However, real-world tasks are often long-horizon and involve evolving contexts containing accumulated observations, intermediate edits, failed attempts, and partially completed executions. Existing agents typically operate over raw interaction history, making task progress difficult to interpret, verify, and recover, which ultimately limits reliable long-horizon execution. In this paper, we argue that addressing this challenge requires explicitly structuring both the agent's state and workflow around a unified causal representation of task progress. We present \textbf{StructAgent}, a state-centered framework that introduces a unified state for maintaining compact, verifiable task progress and a structured workflow that regulates progress through verifier-backed state transitions. Building on this design, StructAgent further enables explicit progress checkpointing, evidence-driven task completion, targeted failure recovery, and tool-supported execution, while ensuring that all progress updates remain grounded in verification. Extensive experiments demonstrate that StructAgent consistently improves a wide range of LLM and VLM backbones on long-horizon computer-use tasks. On OSWorld-Verified, it improves Qwen3.5-9B from 27.0\% to 46.9\% success rate and Qwen3.5-27B from 31.6\% to 62.2\%, while achieving a new open-source state of the art of 78.9\% with MiniMax-M3. Moreover, the same framework generalizes beyond desktop environments to Minecraft, demonstrating the generality of our design.

---

## 论文详细总结（自动生成）

以下是对论文《StructAgent: Harness Long-horizon Digital Agents with Unified Causal Structure》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
数字智能体（Digital Agents）在处理现实世界任务时，面临的主要挑战是**长程执行（Long-horizon execution）的不可靠性**。
*   **背景**：现有的智能体通常直接在原始交互历史（Raw Interaction History）上操作。
*   **核心问题**：随着任务步骤增加，上下文会积累大量冗余信息、失败尝试和中间编辑。这导致智能体难以准确判断任务进度，难以检测异常状态，且在发生错误时难以恢复。
*   **研究动机**：作者认为需要将智能体的状态和工作流显式地组织起来，建立一个统一的、最小充分的因果结构，使任务进度变得可解释、可验证且可审计。

### 2. 方法论：核心思想与技术细节
StructAgent 提出了一个以状态为中心的框架，由**统一状态**和**结构化工作流**组成：

*   **统一状态（Unified State, $s_t$）**：将冗长的历史压缩为三个核心组件：
    1.  **$s_{req}$（当前需求）**：记录当前子目标的要求及验证标准。
    2.  **$s_{val}$（有用数值）**：存储执行过程中发现的关键信息（如文件路径、URL、提取的字段）。
    3.  **$s_{ver}$（验证证据）**：存储由验证器收集的、支持当前进度的证据。
*   **结构化工作流（Structured Workflow）**：采用“规划-执行-验证-更新”的闭环：
    *   **Planner（规划器）**：读取统一状态，决定下一个子目标。
    *   **Actor（执行器）**：尝试完成子目标并产生执行轨迹。
    *   **Verifier（验证器）**：通过结构化探测（如文件检查、URL 匹配、UI 状态分析）判断子目标是否达成。
    *   **Update（状态转换）**：**核心规则**是只有经过验证器确认的决策才能更新状态（Verified, Invalidated, 或 Pending），确保进度具有因果透明性。
*   **高级功能**：基于此结构，实现了进度检查点（Checkpointing）、针对性故障恢复（根据证据进行重试或重规划）以及工具支持的执行。

### 3. 实验设计
论文在三个不同环境的基准测试中验证了 StructAgent 的有效性：
*   **OSWorld-Verified**：针对真实桌面应用（Office, Chrome, VS Code 等）的长程任务。这是 OSWorld 的验证子集，评价更严格。
*   **Mind2Web**：测试网页操作的泛化能力，涵盖信息检索、服务、购物等类别。
*   **Minecraft**：跨领域泛化研究，将桌面环境的证据源替换为游戏内的库存（Inventory）检查。
*   **对比方法**：
    *   **单模型基座**：Qwen3.5-9B/27B, MiniMax-M3。
    *   **现有框架**：Agent S3, OS-Symphony, VLAA-GUI。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号或数量），因为该研究侧重于**推理框架（Inference Framework）**和 Prompt 工程，而非模型预训练。
*   **模型调用**：实验使用了 Qwen3.5 系列和 MiniMax-M3 作为开源背书模型，并使用 GPT-4o 作为 Minecraft 实验的规划器。

### 5. 实验数量与充分性
*   **实验规模**：在 OSWorld-Verified 上进行了大规模测试，涵盖 10 个领域（Chrome, GIMP, Calc 等）；在 Mind2Web 上测试了 5 个网站类别。
*   **消融实验**：
    *   对比了“结构化验证”与“仅视觉验证”的准确率（Recall 从 0.67 提升至 0.81）。
    *   分析了不同领域（如 VS Code vs. Calc）中证据来源的分布。
    *   进行了详细的失败案例审计（分析了 83 个失败轨迹）。
*   **客观性**：实验采用了受控协议（Matched Runner Settings），在相同的模型和预算下对比不同框架，确保了比较的公平性。

### 6. 主要结论与发现
*   **性能提升**：StructAgent 显著提升了开源模型的表现。Qwen3.5-27B 的成功率从 31.6% 提升至 62.2%；MiniMax-M3 达到了 **78.9%**，创下开源模型新纪录。
*   **验证的重要性**：结构化证据（文件、URL、Shell 输出）在验证任务完成度方面比单纯的视觉观察更可靠，尤其是在处理不可见状态（如文件内容）时。
*   **泛化性**：相同的状态中心化设计在 Minecraft 中也取得了 76% 的成功率，证明了框架在不同证据源下的通用性。
*   **瓶颈发现**：尽管状态管理得到了优化，但跨应用协作和复杂的逻辑推理（尤其是小模型）仍然是长程任务的主要瓶颈。

### 7. 优点
*   **因果透明**：通过验证器强制执行状态转换，避免了智能体“盲目自信”地宣布任务完成。
*   **紧凑的状态表示**：解决了长程任务中上下文爆炸和噪声干扰的问题。
*   **强大的恢复能力**：能够根据验证失败的具体证据进行针对性修复，而非简单的循环重试。
*   **跨领域一致性**：设计理念不局限于 GUI，可扩展至游戏和网页环境。

### 8. 不足与局限
*   **验证器依赖**：框架的有效性高度依赖于验证器的质量。如果验证器误判（如接受了弱证据），会导致错误的进度提交。
*   **动态环境挑战**：在 Mind2Web 的实验中发现，对于高度动态、具有 site-specific 流程的网页，仅靠状态跟踪不足以解决动作对齐（Action Grounding）的问题。
*   **计算开销**：引入额外的验证步骤和探测动作可能会增加任务执行的总步数和推理成本。
*   **人工干预**：虽然支持故障恢复，但在极端复杂的跨应用逻辑中，仍可能需要人类介入。

（完）
