---
title: Entropy Pacing Policy Optimization for Multi-Task Agentic Reinforcement Learning
title_zh: 针对多任务智能体强化学习的熵步调策略优化
authors: "Zetian Hu, Shunyu Liu, Junjie Zhang, Yongcheng Jing, Ting-En Lin, Yongbin Li, Dacheng Tao"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07178v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于 LLM 任务的多任务智能体强化学习
tldr: 针对多任务大模型智能体强化学习中不同任务探索步调不一致导致的训练不稳定问题，本文提出了熵步调策略优化（EPPO）。该方法识别出简单任务过早收敛与困难任务持续探索之间的冲突，通过引入基于任务熵的动态裁剪机制，自适应调整更新幅度，从而协调多任务间的熵演化。实验证明，EPPO 在多任务智能体基准测试中显著优于现有方法。
selection_source: fresh_fetch
motivation: 多任务强化学习中存在任务间探索与利用步调不匹配的现象，导致熵值交叉和频繁波动，破坏了训练稳定性。
method: 提出 EPPO 算法，利用基于任务熵的动态自适应边界替换固定裁剪阈值，从而协调不同任务的更新步调。
result: 在多任务智能体基准测试上的实验结果表明，EPPO 的性能显著优于包括 GRPO 在内的现有基准方法。
conclusion: 通过动态协调任务间的熵演化，EPPO 有效解决了多任务智能体学习中的步调失衡问题，提升了通用智能体的训练效果。
---

## 摘要
强化学习（RL）最近的突破凸显了其在复杂智能体大语言模型（LLM）任务中的潜力。然而，现有的研究主要集中在单任务设置上，而实际部署则需要能够同时解决多个任务的通用智能体。在这项工作中，我们发现了多任务智能体强化学习中一个关键但尚未得到充分探索的现象：不同任务可能表现出探索与利用步调不匹配。具体而言，较简单的任务可能会过早收敛到低熵策略，从而阻碍较难任务的学习；而较难的任务反过来又可能将较简单的任务推回高熵探索状态。这种反复的相互作用导致了任务间的熵交叉和频繁的熵峰值。受此观察启发，我们为多任务智能体大语言模型引入了熵步调策略优化（EPPO），通过协调各任务间的熵来稳定多任务优化过程。EPPO 的核心是一种按任务划分的动态裁剪机制，它将群体相对策略优化（GRPO）中的固定裁剪阈值替换为感知任务熵的自适应边界，收紧过度自信任务的更新，同时放宽未充分探索任务的更新。在多任务智能体基准测试上的实验表明，所提出的 EPPO 取得了优于同类方法的结果。

## Abstract
Recent breakthroughs of Reinforcement Learning (RL) have highlighted its potential for complex agentic Large Language Model (LLM) tasks. However, existing efforts largely focus on single-task settings, whereas real-world deployment necessitates a generalist agent capable of solving multiple tasks simultaneously. In this work, we identify a critical yet underexplored phenomenon in multi-task agentic RL: different tasks can exhibit exploration-exploitation pace mismatch. Specifically, easier tasks may converge early to low-entropy policies that hinder learning on harder tasks, while harder tasks can, in turn, push easier tasks back toward high-entropy exploration. This back-and-forth interaction creates inter-task entropy crossovers and frequent entropy spikes. Inspired by this observation, we introduce Entropy Pacing Policy Optimization (EPPO) for multi-task agentic LLMs, which coordinates entropy across tasks to stabilize multi-task optimization. At the core of EPPO is a task-wise dynamic clipping mechanism that replaces the fixed clipping threshold in Group Relative Policy Optimization (GRPO) with a task entropy-aware adaptive bound, tightening updates for over-confident tasks while relaxing them for under-explored ones. Experiments on the multi-task agentic benchmarks demonstrate that the proposed EPPO yields results superior to its counterparts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Entropy Pacing Policy Optimization for Multi-Task Agentic Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**多任务智能体强化学习（Multi-Task Agentic RL）**中的训练稳定性问题。
*   **研究动机**：现有的 LLM 智能体训练多集中在单任务，而现实应用需要通用智能体。在多任务联合训练中，作者发现了一个关键现象——**“步调不匹配”（Pace Mismatch）**。
*   **核心矛盾**：简单任务会迅速收敛到低熵状态（过度自信），而困难任务仍需高熵探索。这种不一致会导致任务间的“熵交叉”（Entropy Crossover）和频繁的“熵峰值”（Entropy Spikes），即已收敛的任务会被未收敛任务的梯度扰动，从而破坏整体训练的稳定性。

### 2. 方法论：熵步调策略优化（EPPO）
EPPO 的核心思想是**将 GRPO 的固定裁剪（Clipping）阈值改为按任务动态调整的自适应边界**，以协调不同任务的探索进度。其技术流程包含三个模块：

*   **熵追踪器（Entropy Tracker）**：利用指数移动平均（EMA）实时追踪每个任务 $i$ 的策略熵 $H_i(t)$，并计算“熵坍缩率” $R_i(t)$（当前熵与初始熵的比值），使不同规模任务的进度具有可比性。
*   **进度步调器（Progress Pacer）**：
    *   计算各任务坍缩率的 $z$-score（标准化得分），衡量该任务相对于整体的进度。
    *   通过 `tanh` 函数将 $z$-score 映射为裁剪缩放因子。
    *   **逻辑**：对于“过度自信”（熵减过快）的任务，收紧裁剪范围（减小 $\epsilon$），限制其更新幅度；对于“探索不足”的任务，放宽裁剪范围，加速其学习。
*   **稳定性趋势约束（Stability Trend Constraint）**：当检测到某个任务的熵异常上升（出现波动）时，自动下调其裁剪阈值，以抑制干扰引起的振荡，防止策略发生剧烈偏移。

### 3. 实验设计
*   **数据集/场景**：使用了 **AgentBench** 中的五个多轮交互任务：操作系统（OS）、数据库（DB）、知识图谱（KG）、ALFWorld（文字游戏）和 WebShop（在线购物）。
*   **基准测试（Benchmark）**：
    *   **API 模型**：Claude-Sonnet-4, GPT-5, o3-mini 等。
    *   **开源模型**：DeepSeek-V3/R1, Qwen2.5 系列, AgentLM 等。
    *   **RL 基准方法**：**AgentRL**（主要对比对象）、DCPO（动态裁剪）、BAPO（平衡策略优化）。
*   **实验设置**：以 Qwen2.5-3B-Instruct 为基础模型，对比了五任务联合训练和三任务联合训练的效果。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   框架：Ray + SGLang + FSDP。
    *   参数：训练 700 个 step，每个 batch 包含 128 个 prompt，每个 prompt 进行 8 次采样（rollouts）。
    *   精度：Actor 模型使用 float32，参考模型使用 bfloat16。
*   **时长**：文中未明确给出总训练小时数，但提到了同步周期为每 25 步一次。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从 0.5B 到 8B 不同参数规模的底座模型（Qwen2.5-0.5B/3B, Qwen3-8B）。
    *   进行了五任务和三任务的对比实验。
    *   **消融实验**：分别验证了“动态裁剪”和“趋势约束”的有效性。
    *   **泛化性测试**：在 MATH500、GSM8K 等通用推理基准上测试了智能体训练后的能力迁移。
*   **评价**：实验设计较为充分，通过熵曲线的可视化直观展示了 EPPO 如何消除熵交叉，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：EPPO 在五任务平均成功率上达到 54.3%，显著优于 AgentRL 的 51.6%，尤其在 WebShop 和 DB 任务上提升明显。
*   **训练稳定性**：EPPO 成功将各任务的熵演化组织成稳定的“带状结构”，大幅减少了任务间的排名翻转（Crossover Count）和后期熵突发。
*   **推理能力保持**：在提升智能体交互能力的同时，EPPO 在数学推理（GSM8K, MATH）等任务上也表现出比 AgentRL 更好的性能保持或提升。

### 7. 优点
*   **针对性强**：精准捕捉到了多任务 RL 中由于任务难度差异导致的“步调不匹配”这一痛点。
*   **即插即用**：作为一种裁剪机制的改进，它可以轻松集成到现有的 GRPO 或 PPO 框架中，不需要额外的 Critic 网络。
*   **理论支撑**：附录中提供了关于裁剪边界统一性和稳定性约束的理论分析。

### 8. 不足与局限
*   **任务组合固定**：实验主要基于 AgentBench 的特定组合，尚未验证在更大规模（如数十个任务）或任务分布极度极端情况下的表现。
*   **信号单一**：目前仅依赖“策略熵”作为步调信号，未来可能需要结合奖励趋势或值函数估计等更多维度的指标。
*   **算力限制**：作者在附录中提到由于硬件（H20）和内存限制，无法使用更大的 Batch Size，这可能限制了模型性能的进一步挖掘。

（完）
