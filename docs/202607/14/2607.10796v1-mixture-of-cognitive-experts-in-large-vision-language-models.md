---
title: Mixture of Cognitive Experts in Large Vision-Language Models
title_zh: 大视觉语言模型中的认知专家混合
authors: "Robert Wijaya, Ngai-Man Cheung"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10796v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 大型视觉语言模型中的视觉和文本输入推理
tldr: 针对大视觉语言模型在整合多专家编码器时缺乏可解释性和一致性的问题，本文提出了一种受布鲁姆分类法启发的证据驱动多模态推理框架。该框架通过两阶段认知言语化过程，将专家输出转化为原子证据摘要和分阶段推理轨迹，并利用推理轨迹模块进行定量分析。研究表明，该方法显著提升了模型的感知与推理能力，并为不同查询的认知处理过程提供了细粒度的可解释性。
selection_source: fresh_fetch
motivation: 现有的LVLM在处理不同认知难度的视觉语言问题时，往往采用统一的感知推理路径，缺乏对多专家模型输出的有效整合与可解释性。
method: 提出一种基于布鲁姆分类法的分层推理协议，通过两阶段言语化生成原子证据摘要和推理轨迹，并引入推理轨迹模块进行定量分析。
result: 该框架在感知和推理任务上均表现出显著提升，并能通过推理轨迹定量揭示不同查询所需的认知层级和证据使用路径。
conclusion: 通过将认知分类法引入多专家LVLM，不仅增强了模型的性能，还为理解复杂视觉推理过程提供了高度可解释的量化手段。
---

## 摘要
大视觉语言模型 (LVLM) 需要对视觉和文本输入进行强大的推理。最近的研究表明，认知要素，特别是多样化的表征和元认知，与更好的性能相关。许多所需的感知功能已经由专门的领域特定计算机视觉模型提供，这些模型作为感知子系统，用于检测物体、定位、推断状态、恢复空间布局和读取文本。关键挑战在于将这些多编码器专家整合到一个可信、可解释且连贯的表征中，以提高可验证性并减少幻觉。这具有挑战性，因为视觉语言问题跨越了不同的认知层面，但大多数 LVLM 流水线无论每个查询的需求如何，都采用相同的感知-推理路由。我们提出了一种证据驱动的多模态推理框架，该框架利用受布鲁姆 (Bloom) 启发的分类法作为分层推理协议。两阶段认知语言化首先通过将专家输出分解为简短、原子的证据陈述，生成字面证据摘要 (Literal Evidence Summary)。随后执行布鲁姆语言化 (Bloom Verbalization)，将这些证据项转化为阶段性的推理轨迹，并由一个轻量级的推理轨迹模块 (Reasoning Trace Module) 对轨迹进行定量分析，使证据使用和推理进展显式化。通过这种整合，我们观察到感知和推理能力的若干提升。此外，轨迹模块提供了定量证据，表明不同的查询会诱发不同的认知切入点和证据使用轨迹，从而实现细粒度分析。

## Abstract
Large Vision Language Models (LVLMs) require strong reasoning over both visual and textual input. Recent work suggests that cognitive elements, especially diverse representations and metacognition, correlate with better performance. Many of the needed perceptual functions are already provided by specialized domain-specific computer vision models, which act as the perceptual subsystem for detecting objects, localizing them, inferring states, recovering spatial layout, and reading text. The key challenge is to integrate these multi-encoder experts into a trustworthy, interpretable, and coherent representation that improves verifiability and reduces hallucinations. This is difficult because vision-language questions span different cognitive levels, yet most LVLM pipelines apply the same perception-reasoning routing regardless of the demand of each query. We propose an evidence-driven multimodal reasoning framework that utilizes a Bloom-inspired taxonomy as a hierarchical reasoning protocol. The two-stage cognitive verbalization first produces a Literal Evidence Summary by decomposing expert outputs into short, atomic evidence statements. It then performs Bloom Verbalization to turn these evidence items into a staged reasoning trace, and a lightweight Reasoning Trace Module quantitatively analyzes the trace to make evidence usage and reasoning progression explicit. Through this integration, we observed several improvements in perception and reasoning abilities. Moreover, the trace module provides quantitative evidence that different queries induce different cognitive entry levels and evidence-use trajectories that enable fine-grained analysis.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **MoCE (Mixture of Cognitive Experts)** 的框架，旨在通过引入认知科学中的布鲁姆分类法（Bloom's Taxonomy），提升大视觉语言模型（LVLM）在整合多专家视觉编码器时的推理能力、可解释性和可验证性。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管现有的 LVLM 可以通过集成专门的视觉专家（如 OCR、目标检测、分割模型）来增强感知，但如何将这些异构输出转化为**可信、连贯且可验证**的推理过程仍是巨大挑战。
*   **核心问题**：目前的 LVLM 往往对所有问题采用统一的“感知-推理”路径，忽略了不同问题在认知深度上的差异（例如，“图中有什么”与“为什么这个场景很危险”所需的认知层级不同）。
*   **整体含义**：论文试图通过模拟人类认知的层级结构，将视觉专家的感知结果转化为原子证据，并按照从低级到高级的认知阶段进行推理，从而减少幻觉并提高推理的透明度。

### 2. 方法论：核心思想与关键技术
该框架主要包含以下四个关键组件：
*   **视觉专家池与路由（Router）**：利用 LLM 指导的路由器，根据查询内容从专家池（如 DETR、SAM、Pix2Struct 等）中选择最相关的 K 个专家。
*   **两阶段认知言语化（Two-stage Cognitive Verbalization）**：
    1.  **字面证据摘要（Literal Evidence Summary）**：将专家输出的原始数据（如坐标、掩码、文本行）转化为**原子化、非推理**的短句，并存储在统一的证据库中，每个证据都有唯一的 ID 和类型（TEXT, ENTITY, REGION）。
    2.  **布鲁姆言语化（Bloom Verbalization）**：按照布鲁姆分类法（列举 LIST、解释 EXPLAIN、举例 ILLUSTRATE、比较 COMPARE、假设 HYPOTHESIZE、最终答案 FINAL ANS）生成分阶段的推理草案，并在每一步显式引用证据 ID。
*   **推理轨迹模块（Reasoning Trace Module）**：一个轻量级的确定性模块，用于解析布鲁姆言语化的输出，定量分析推理的起始层级、证据使用频率和推理路径。
*   **适配器（Adapter）**：采用类似 MoVA 的架构，将路由后的专家特征融合进 LLM 可理解的视觉表征中。

### 3. 实验设计
*   **数据集/场景**：涵盖了视觉问答、通用感知和推理的五大主流基准：
    *   **TextVQA**（文本视觉问答）
    *   **MMBench**（多模态综合能力）
    *   **MMVet**（集成能力评估）
    *   **POPE**（物体幻觉评估）
    *   **SEED**（生成式理解）
*   **对比方法（Baselines）**：对比了 InstructBLIP、Qwen-VL、LLaVA-1.5 和 LLaVA-NeXT 等主流模型。
*   **消融实验**：专门对比了“仅使用视觉专家”与“视觉专家 + 布鲁姆言语化”的效果，以验证认知层级推理的有效性。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到该框架在推理阶段是**无需训练（Training-free）**的，主要依赖于预训练的视觉专家和 LLM（如 Vicuna-7B）的推理能力。适配器部分参考了 MoVA 的权重。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同的基准测试上进行了全面评估，并针对 MMBench 的二级能力（如逻辑推理、空间关系、细粒度感知）进行了深入拆解分析。
*   **充分性**：实验设计较为充分。除了性能对比，还通过“布鲁姆轨迹诊断”对 5000 个样本进行了定量分析，展示了模型如何根据问题难度选择不同的认知起点。
*   **客观性**：通过引入 POPE 评估幻觉，以及在 SEED 上测试不同场景的鲁棒性，证明了该方法在减少错误关联方面的客观优势。

### 6. 主要结论与发现
*   **性能提升**：MoCE 在所有基准测试中均表现优异，尤其在 MMBench 和 MMVet 上提升显著，证明了认知启发式架构比单纯增加参数量更有效。
*   **认知层级分布**：通过轨迹模块发现，约 83.8% 的查询从最低层级（LIST）开始，说明当前基准测试中的大多数问题仍侧重于基础感知。
*   **证据驱动**：显式的证据引用显著增强了模型对细粒度物体和文本的定位与理解能力，有效缓解了幻觉问题。

### 7. 优点：亮点与创新
*   **认知科学结合**：成功将教育心理学中的布鲁姆分类法转化为可操作的 AI 推理协议。
*   **高可解释性**：通过“证据 ID 引用”和“分阶段推理”，用户可以清晰看到模型是基于哪些视觉事实得出的结论。
*   **模块化与灵活性**：框架不依赖特定 LLM，且可以根据需要轻松扩展或更换视觉专家。

### 8. 不足与局限
*   **推理延迟**：由于需要多阶段言语化和多次 LLM 调用（或生成更长的 Token），端到端延迟增加了约 30%（从 10.24s 增至 13.40s）。
*   **专家依赖**：推理质量高度依赖于底层视觉专家的准确性；如果专家输出错误证据，后续的布鲁姆推理可能会产生“一本正经胡说八道”的现象。
*   **计算开销**：生成更长的推理轨迹（Token 数增加约 20%）会带来额外的计算 GFLOPs 消耗。

（完）
