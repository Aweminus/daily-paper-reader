---
title: Cognitive Dual-Process Planning for Autonomous Driving with Structured Scene Knowledge and Verifiable Reasoning-Action Consistency
title_zh: 基于结构化场景知识与可验证推理-动作一致性的自动驾驶认知双过程规划
authors: "Zhongyao Yang, Haoyu Li, Yu Yan, Zhuangxuan Yu, Jiangfeng Nan, Jinrui Nan"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19194v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于VLM的自动驾驶规划与决策
tldr: 本研究针对自动驾驶中视觉语言模型（VLM）规划存在的推理成本高、简单场景冗余及推理与动作不一致等挑战，提出了一种认知双过程规划框架。该框架引入结构化思维链（S-CoT）表示场景知识，利用自动数据引擎生成监督信号，并通过视觉仲裁器在快速元动作预测与慢速结构化推理间自适应切换。此外，通过规则验证器确保推理与动作的一致性。实验表明，该方法在提升规划准确率和逻辑一致性的同时，显著降低了推理延迟。
selection_source: fresh_fetch
motivation: 旨在解决自动驾驶中VLM推理成本高昂、简单场景下冗余计算以及生成的推理逻辑与最终驾驶动作不一致的问题。
method: 提出一种包含结构化思维链（S-CoT）的双过程框架，利用视觉仲裁器实现快慢路径切换，并结合规则验证器进行一致性检查与强化学习优化。
result: "在NAVSIM测试中达到80.14%的规划准确率和97.20%的逻辑一致性得分，且相比全慢速推理模式降低了17.39%的平均延迟。"
conclusion: 研究证明了通过显式场景知识、自适应推理路由和规则化验证，可以有效提升自动驾驶高层规划决策的效率与可靠性。
---

## 摘要
自动驾驶的高层规划是一项知识密集型的工程决策任务，需要准确的场景理解、及时的推理以及内部一致的动作选择。视觉语言模型（VLM）可以使中间推理过程显式化，但其在部署规划器中的应用受到昂贵的结构化监督、常规场景中不必要的推理以及生成的理由与驾驶动作之间可能存在的不一致性的限制。我们提出了一种认知双过程规划框架，该框架在机器可解析的结构化思维链（S-CoT）模式中表示与规划相关的场景知识。一个自动化数据引擎集成了感知基础模型、关键路径过滤和专家 VLM，在无需人工标注单个理由的情况下生成 S-CoT 监督。一个轻量级的视觉仲裁器（Arbiter）在语言解码之前，根据多级视觉编码器特征估计场景复杂度，并将每个输入路由到快速元动作预测或慢速结构化推理。对于慢速路径输出，一个基于确定性规则的验证器检查解析后的 S-CoT 字段是否与最终元动作一致，并为群体相对策略优化（GRPO）提供可验证的奖励。在 195 个场景的人工审核中，生成的标注达到了 91.8% 的 CoT 准确率和 98.5% 的逻辑一致性得分（LCS）。在 574 个经过人工验证的 NAVSIM 测试样本上，该规划器实现了 80.14% 的规划准确率和 97.20% 的 LCS，同时相对于对每个场景应用慢速推理，平均延迟降低了 17.39%。对外部长尾子集的评估进一步确定了路由和规划性能下降的条件。总之，这些结果表明了如何通过自适应推理和基于规则的验证来运用显式场景知识，以支持高层 VLM 规划决策。

## Abstract
High-level planning for autonomous driving is a knowledge-intensive engineering decision task that requires accurate scene understanding, timely inference, and internally consistent action selection. Vision-language models (VLMs) can make intermediate reasoning explicit, but their use in deployed planners is constrained by costly structured supervision, unnecessary reasoning in routine scenes, and possible inconsistencies between generated rationales and driving actions. We present a cognitive dual-process planning framework that represents planning-relevant scene knowledge in a machine-parsable structured chain-of-thought (S-CoT) schema. An automated data engine integrates perception foundation models, critical-path filtering, and an expert VLM to generate S-CoT supervision without manual annotation of individual rationales. A lightweight visual Arbiter estimates scene complexity from multilevel vision-encoder features before language decoding and routes each input to either fast meta-action prediction or slow structured reasoning. For slow-path outputs, a deterministic rule-based validator checks whether the parsed S-CoT fields are consistent with the final meta-action and provides verifiable rewards for Group Relative Policy Optimization (GRPO). In a 195-scene manual audit, the generated annotations achieve 91.8\% CoT accuracy and a 98.5\% Logical Consistency Score (LCS). On 574 manually verified NAVSIM test samples, the planner achieves 80.14\% planning accuracy and 97.20\% LCS while reducing average latency by 17.39\% relative to applying slow reasoning to every scene. Evaluation on external long-tail subsets further identifies conditions under which routing and planning performance degrade. Together, these results show how explicit scene knowledge can be operationalized through adaptive reasoning and rule-based verification to support high-level VLM planning decisions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Cognitive Dual-Process Planning for Autonomous Driving with Structured Scene Knowledge and Verifiable Reasoning-Action Consistency》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
自动驾驶的高层规划需要兼顾**准确性、实时性与逻辑一致性**。虽然视觉语言模型（VLM）通过思维链（CoT）提供了可解释的推理过程，但面临三大挑战：
*   **推理成本高昂**：对所有场景（包括简单的直行）都进行全量推理会导致严重的计算延迟。
*   **监督数据获取难**：高质量、结构化的“推理-动作”对标注成本极高。
*   **逻辑不一致（Hallucination）**：模型生成的推理理由（如“看到红灯”）可能与最终输出的动作（如“加速通过”）相矛盾。
本研究旨在通过模拟人类认知的“双过程理论”（快思考与慢思考），构建一个既高效又能保证逻辑自洽的规划框架。

### 2. 方法论：核心思想与关键技术
该框架主要包含以下四个核心组件：
*   **结构化思维链（S-CoT）**：将场景知识定义为机器可解析的结构化字段（如：交通灯状态、障碍物距离、车道关联等），使推理过程从纯文本转变为可验证的逻辑结构。
*   **自动化数据引擎**：利用感知基础模型（如 Grounding DINO, SAM）提取目标，结合专家 VLM（如 GPT-4o）生成 S-CoT 伪标签，无需人工逐条标注。
*   **认知双过程路由（Visual Arbiter）**：
    *   **快路径（Fast Path）**：直接从视觉特征预测元动作（Meta-action）。
    *   **慢路径（Slow Path）**：生成完整的 S-CoT 推理后再输出动作。
    *   **仲裁器（Arbiter）**：一个轻量级模块，在解码前根据视觉特征评估场景复杂度，决定调用哪条路径。
*   **可验证的一致性优化（GRPO + Rule Validator）**：
    *   引入**规则验证器**，根据确定性规则检查 S-CoT 字段与动作是否匹配（例如：若检测到前方有近距离障碍物，动作必须是减速或停止）。
    *   使用**群体相对策略优化（GRPO）**算法，将验证器的反馈作为奖励信号，强化模型推理与动作的一致性。

### 3. 实验设计
*   **数据集与 Benchmark**：主要基于 **NAVSIM**（源自 nuPlan）进行评估。
*   **评估指标**：
    *   **规划准确率（Planning Accuracy）**：元动作预测的准确性。
    *   **逻辑一致性得分（LCS）**：推理理由与动作的一致程度。
    *   **延迟（Latency）**：端到端推理时间。
*   **对比方法**：对比了全慢速推理（Always Slow）、全快速预测（Always Fast）以及不同的路由策略（如基于熵的路由）。
*   **长尾测试**：在外部长尾场景子集上测试模型的泛化能力。

### 4. 资源与算力
*   **模型规模**：实验主要基于 **InternVL2-2B** 轻量级 VLM 展开。
*   **算力细节**：论文中**未明确给出**具体的 GPU 型号、数量及总训练时长。但考虑到使用了 GRPO 强化学习和 2B 规模的模型，通常需要 8x A100/H100 级别的显卡进行高效训练。

### 5. 实验数量与充分性
*   **数据规模**：使用了 195 个场景进行人工审核标注质量，并在 574 个经过人工验证的 NAVSIM 测试样本上进行最终评估。
*   **消融实验**：针对 Arbiter 的路由效果、GRPO 对一致性的提升效果、以及不同视觉特征对复杂性判断的影响做了详细的消融研究。
*   **充分性评价**：实验设计较为科学，涵盖了从数据生成质量到最终部署效率的全流程验证。虽然测试集绝对数量不算巨大（574个），但通过人工验证确保了测试基准的可靠性。

### 6. 主要结论与发现
*   **效率提升**：双过程框架相比全慢速模式，**平均延迟降低了 17.39%**，且未显著损失准确率。
*   **一致性增强**：通过 GRPO 优化，逻辑一致性得分（LCS）达到了 **97.20%**，有效解决了 VLM “胡言乱语”的问题。
*   **规划性能**：在 NAVSIM 测试中达到了 **80.14%** 的规划准确率。
*   **路由有效性**：视觉仲裁器能有效识别复杂场景（如路口、拥堵），并将其分配给慢路径处理。

### 7. 优点
*   **架构优雅**：成功将认知心理学的双过程理论落地到自动驾驶规划中，平衡了性能与功耗。
*   **闭环验证**：通过规则验证器将“不可解释”的神经网络输出转化为“可验证”的逻辑对齐，提升了系统的安全性。
*   **低成本标注**：自动化数据引擎极大降低了构建高质量 CoT 数据集的门槛。

### 8. 不足与局限
*   **测试场景覆盖**：虽然进行了长尾测试，但 574 个样本的测试集规模对于证明自动驾驶的绝对安全性仍显不足。
*   **仲裁器偏差**：在某些极端长尾场景下，仲裁器可能误判场景复杂度，导致该慢思考时选择了快路径，存在安全风险。
*   **依赖专家模型**：数据引擎的质量高度依赖于 GPT-4o 等专家模型的表现，可能继承专家模型的潜在偏差。
*   **实时性挑战**：尽管降低了延迟，但 2B 规模的 VLM 在车载嵌入式芯片上的实时运行（如达到 10Hz 以上）仍面临挑战。

（完）
