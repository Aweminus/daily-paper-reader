---
title: "LEEVLA: Seeing What Matters in Latent Environment Evolution for Vision-Language-Action"
title_zh: LEEVLA：在视觉-语言-动作（VLA）的潜在环境演化中洞察关键信息
authors: "Qi Lyu, Baicheng Liu, Xudong Wang, Jiahua Dong, Lianqing Liu, Zhi Han"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08182v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于动态环境推理的视觉-语言-动作架构
tldr: 针对现有视觉-语言-动作（VLA）模型在复杂动态场景下难以识别任务关键信息的局限，本文提出LEEVLA架构。该架构通过漂移引导动态优先级（DGDP）机制引导模型关注指令相关的显著区域，并利用结构化特征流生成（SFFG）建模潜在环境演化。实验证明，这种显式的任务证据引导和结构化潜在推理显著提升了VLA模型在基准测试中的表现。
selection_source: fresh_fetch
motivation: 现有VLA模型因均匀处理视觉Token且缺乏对任务关键因素的关注，难以应对复杂的动态环境。
method: 提出由DGDP（动态位置优先级与语义漂移引导）和SFFG（原型到外围预测及对比损失）组成的“何处-如何”训练框架。
result: 在多个VLA基准测试上的广泛实验表明，LEEVLA的一致性表现优于现有先进方法。
conclusion: 显式的任务证据引导和结构化的潜在空间推理对于构建可扩展且高效的VLA模型至关重要。
---

## 摘要
视觉-语言-动作（VLA）模型旨在将多模态输入映射到机器人动作。然而，由于统一对待所有视觉 token 并基于人工选择的因素进行推理，大多数现有方法在处理复杂的动态场景时面临挑战，缺乏强调任务关键证据并忽略潜在因素的机制。为解决这一问题，我们提出了 LEEVLA，这是一种用于在潜在环境演化中洞察关键信息的 VLA 架构，它在保留潜在世界表示的结构化演化的同时，显式地引导模型关注信息丰富的区域。为了识别显著且与指令相关的区域，我们引入了漂移引导的动态优先级排序（DGDP），它结合了动态位置优先级排序（DPP）和语义漂移引导（SDG），以在训练期间引导 VLA 智能体关注的位置。在此基础上，我们引入了结构化特征流生成（SFFG），通过原型到外围（P2P）预测来建模这些高优先级特征在潜在空间中应如何演化，并使用互邻域对比（MC）损失来维持邻域间的拓扑一致性。DGDP 和 SFFG 共同构成了一个任务感知的“何处-如何”（where-how）训练框架。在 VLA 基准测试上的广泛实验表明，LEEVLA 始终优于先前的方法，证实了显式的任务证据引导和结构化的潜在推理对于可扩展的 VLA 都至关重要。我们的代码可在 https://github.com/LyuQi127/LEEVLA 获取。

## Abstract
Vision-language-action (VLA) models aim to map multimodal inputs to robot actions. However, most existing approaches struggle to cover complex dynamic scenarios due to treating all visual tokens uniformly and reasoning with human-selected factors, which lack mechanisms to emphasize task-critical evidence and ignore underlying factors. To address this issue, we propose LEEVLA, a VLA architecture for seeing what matters in Latent Environment Evolution that explicitly guides the model toward informative regions while preserving the structured evolution of latent world representations. To identify salient and instruction-relevant regions, we introduce drift-guided dynamic prioritization (DGDP), which combines dynamic position prioritization (DPP) with semantic drift guidance (SDG) to guide the VLA agent where to attend during training. On top of this, we introduce structured feature flow generation (SFFG), which models how these prioritized features should evolve in latent space via prototype-to-periphery (P2P) prediction, and a mutual-neighborhood contrastive (MC) loss to maintain topological consistency among neighborhoods. Together, DGDP and SFFG form a task-aware "where-how" training framework. Extensive experiments on VLA benchmarks show that LEEVLA consistently outperforms prior methods, confirming that explicit task-evidence guidance and structured latent reasoning are both crucial for scalable VLA. Our code is available at https://github.com/LyuQi127/LEEVLA.

---

## 论文详细总结（自动生成）

这篇论文提出了 **LEEVLA**，一种旨在增强视觉-语言-动作（VLA）模型在复杂动态环境下推理能力的架构。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：现有的 VLA 模型（如 OpenVLA, RT-2）通常将所有视觉 token 同等对待，并依赖于人工选择的中间表示（如深度图、分割图）。
*   **核心痛点**：
    1.  **注意力分散**：模型在训练时容易被静态背景或与任务无关的物体干扰，缺乏对任务关键区域的聚焦。
    2.  **推理受限**：依赖外部辅助模型（如深度估计）会增加计算开销，且受限于辅助任务的精度，忽略了潜在空间中未被定义的关键因素。
    3.  **结构破坏**：将视觉特征展平为序列进行预测会破坏物体原有的空间拓扑和语义连续性。
*   **核心目标**：在潜在特征空间中直接进行“结构化演化推理”，让模型自动识别“该看哪里（Where）”以及环境“如何演化（How）”。

### 2. 方法论：核心思想与关键技术
LEEVLA 提出了一个“何处-如何”训练框架，主要包含两大模块：

*   **漂移引导动态优先级（DGDP）—— 解决“看哪里”**：
    *   **动态位置优先级（DPP）**：通过计算相邻时间步特征之间的余弦相似度变化，量化每个区域的动态程度。变化剧烈的区域（如机械臂、目标物体）被赋予更高权重。
    *   **语义漂移引导（SDG）**：计算视觉特征与语言指令 token 的内积，识别语义上向指令目标“漂移”的区域，从而抑制无关的动态背景（如路过的行人或背景干扰）。
*   **结构化特征流生成（SFFG）—— 解决“如何演化”**：
    *   **原型到外围（P2P）预测**：对视觉特征进行聚类，将同一语义单元（如整个盘子）的特征归为一类。预测时按照“从中心原型到边缘外围”的顺序进行，以保持空间语义的连贯性。
    *   **互邻域对比（MC）损失**：构建二阶 K-近邻互邻域图，通过对比学习拉近语义相似特征的距离，过滤噪声连接，维持潜在空间的拓扑一致性。
*   **训练目标**：结合了 L1 动作回归损失、P2P 预测损失和 MC 对比损失，共同优化策略。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **LIBERO**（包含 Spatial, Object, Goal, Long 四个子集）和 **CALVIN**（ABC-D 任务）基准测试。
    *   **真实世界**：使用 UR5 机械臂进行“放置物体”、“按按钮”、“关抽屉”等任务。
*   **对比方法**：
    *   **小规模模型**：Octo, UniACT, Seer, DreamVLA, FLOWER。
    *   **大规模模型**：OpenVLA, π0, OpenVLA-OFT, UniVLA, MemoryVLA。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练时长**：
    *   **LEEVLA-large (7B)**：在 Open X-Embodiment 数据集上预训练，训练步数为 50k 至 150k 步。
    *   **LEEVLA-mini (0.5B)**：在 LIBERO-90 上训练，步数为 20k 至 50k 步。
*   **推理开销**：文中强调 DGDP 和 SFFG 仅在训练阶段使用，**推理时无额外内存和计算开销**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.5B 到 7B 两个参数量级的模型。
*   **消融实验**：在 LIBERO-Goal 上进行了详尽的消融，分别验证了 P2P、MC、DPP、SDG 以及全局因子 $\alpha$ 的有效性。
*   **客观性**：实验采用了 3 个随机种子，并在多个标准基准上进行测试，对比了 2024-2025 年最新的 SOTA 方法，结果具有较强的说服力和公平性。

### 6. 主要结论与发现
*   **性能领先**：LEEVLA 在 LIBERO 和 CALVIN 上的成功率均达到或刷新了 SOTA 记录（如在 LIBERO 平均成功率达到 98.2%）。
*   **潜在推理的价值**：在潜在空间进行未来特征预测（辅助任务）能显著增强模型对环境动力学的理解，从而提升下游动作预测的准确性。
*   **结构化优于扁平化**：P2P 预测机制证明了保持特征空间结构比简单的序列预测更能提升长程任务的稳定性。

### 7. 优点（亮点）
*   **任务感知**：通过 DGDP 机制，模型能够自动“聚焦”于任务相关的动态区域，而非盲目处理所有视觉信息。
*   **零推理成本**：通过辅助训练任务提升性能，而不增加部署时的计算负担，这对实时机器人控制非常友好。
*   **拓扑保持**：引入互邻域对比损失，有效解决了机器人演示数据中常见的噪声和语义碎片化问题。

### 8. 不足与局限
*   **聚类依赖**：SFFG 依赖于特征聚类，如果视觉编码器（如 DINOv2）在某些极端环境下特征提取不佳，聚类质量可能会影响训练效果。
*   **真实世界样本量**：虽然进行了真实世界实验，但每个任务 20 次测试的样本量相对较小，对于极长程或极复杂环境的泛化能力仍有待更大规模验证。
*   **超参数敏感性**：模型涉及多个损失函数的平衡（$\lambda_1, \lambda_2, \lambda_3$），在不同机器人平台上可能需要精细调优。

（完）
