---
title: "Beyond the Eye: Efficient Multimodal Reasoning via Self-Regulated Implicit Visual Tools"
title_zh: 视界之外：通过自我调节的隐式视觉工具实现高效多模态推理
authors: "Xiuwei Chen, Quanlin Chen, Wentao Hu, Zisheng Chen, Kun Xiang, Zehua Ma, Mingyang Zhang, Jianhua Han, Hanhui Li, Hang Xu, Xiaodan Liang"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11106v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过自调节隐式视觉工具进行多模态推理
tldr: 针对多模态大模型在细粒度感知任务中依赖外部工具导致的高延迟问题，本文提出BEE框架。BEE通过隐式视觉工具范式，将工具调用行为融入训练目标，使模型具备自我调节能力。通过CoT指令微调和基于奖励的对齐机制，BEE能自适应平衡内部知识与隐式工具，在保持通用推理能力的同时，显著提升了细粒度感知性能和推理效率。
selection_source: fresh_fetch
motivation: 现有的多模态模型依赖频繁的外部工具调用和重复图像编码，导致了巨大的计算开销和推理延迟。
method: 提出BEE框架，通过两阶段训练（CoT指令微调和自调节奖励对齐）使模型能够隐式调用工具并自发平衡内部知识与工具使用。
result: BEE在细粒度视觉感知任务上取得了最先进的性能，同时显著提高了推理效率并减少了冗余的工具依赖。
conclusion: 该研究证明了通过自我调节的隐式工具机制，可以有效解决多模态推理中的效率与精度平衡问题。
---

## 摘要
近期，多模态大语言模型（MLLMs）在“以图思考”（Thinking with Images, TwI）范式下，通过迭代执行各种视觉工具操作，在细粒度感知任务上取得了显著进展。然而，该范式严重依赖频繁的外部工具调用和重复的图像重编码，导致了巨大的计算开销和推理延迟。为了解决这些问题，我们提出了 Beyond the Eye (BEE)，这是一种以自我调节能力为核心的新型隐式视觉工具范式。BEE 直接将视觉工具调用行为纳入训练目标，并鼓励模型开发自我调节的调用机制。这种设计使模型能够自适应地平衡内部知识与隐式工具，在避免冗余工具使用的同时，大幅降低推理延迟。具体而言，BEE 包含两个阶段的训练过程：(1) 形式化思维链（CoT）指令微调（SFT）。我们构建了具有结构化工具槽位和混合调用状态的 CoT 轨迹。该阶段激活了模型的隐式工具表示和自适应切换能力。(2) 自我调节的奖励驱动对齐。针对认知边界模糊导致的冗余工具使用问题，我们首先引入了净工具增益（NTG）指标来量化这一现象。基于此观察，我们进一步提出了一种自我调节的奖励机制。该机制惩罚无效的工具依赖，并鼓励模型进行知识路由，确保仅在模型内部知识不足时才调用隐式工具。BEE 在细粒度视觉感知方面达到了最先进的性能，同时在通用推理任务中保持了竞争力，并显著提升了推理效率。

## Abstract
Recent multimodal large language models (MLLMs) have made remarkable progress on fine-grained perception tasks under the "Thinking with Images" (TwI) paradigm by iteratively performing various visual tool operations. However, this paradigm relies heavily on frequent external tool calls and repeated image re-encoding, which leads to substantial computational overhead and inference latency. To address these issues, we propose Beyond the Eye (BEE), a novel implicit visual tool paradigm centered on self-regulated capability. BEE directly incorporates visual tool invocation behaviors into the training objective and encourages the model to develop a self-regulated invocation mechanism. This design enables the model to adaptively balance internal knowledge and implicit tools, avoiding redundant tool usage while substantially reducing inference latency. Specifically, BEE involves a two-stage training process: (1) Formalized Chain-of-Thought (CoT) Supervised Fine-tuning (SFT). We construct CoT trajectories with structured tool slots and mixed invocation states. This stage activates the model's implicit tool representations and adaptive switching capability. (2) Self-regulated Reward-Driven Alignment. To address redundant tool usage caused by ambiguous cognitive boundaries, we first introduce the Net Tool Gain (NTG) metric to quantify this phenomenon. Based on this observation, we further propose a self-regulated reward mechanism. This mechanism penalizes ineffective tool dependency and encourages the model to perform knowledge routing, ensuring that implicit tools are invoked only when the model's internal knowledge is insufficient. BEE achieves state-of-the-art performance in fine-grained visual perception while remaining competitive in general reasoning tasks and achieving substantial gains in inference efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：Beyond the Eye (BEE) —— 通过自我调节的隐式视觉工具实现高效多模态推理

#### 1. 核心问题与整体含义（研究动机和背景）
当前多模态大语言模型（MLLM）在处理细粒度视觉任务时，正从“对图思考”（推理仅在文本空间）转向“以图思考”（TwI，通过缩放、裁剪等工具与图像交互）。然而，现有的 TwI 范式存在两大痛点：
*   **高延迟与高开销**：依赖外部工具调用（API）和频繁的图像重编码，导致推理速度极慢。
*   **缺乏能力边界意识**：模型无法判断任务难易，往往在简单任务上也盲目调用工具，造成计算资源浪费和冗余。
**BEE 的核心目标**是构建一种**隐式视觉工具（IvT）**范式，将工具调用内化为模型的隐式表示，并通过自我调节机制实现“按需调用”，在提升精度的同时大幅优化效率。

#### 2. 方法论：核心思想与关键技术
BEE 提出了一个两阶段训练框架，旨在实现工具能力的内化与自发调节：

*   **核心思想**：将视觉工具（如局部放大、旋转、对比度增强）的操作结果转化为隐式语义 Token（Tool Slots），使模型在单次前向传播中完成“规划-调用-推理”的全过程。
*   **关键技术细节**：
    1.  **形式化 CoT 指令微调（Stage 1）**：构建包含隐式工具槽位的思维链（CoT）数据。利用预训练的语义分词器（Semantic Tokenizer）将工具处理后的图像特征离散化为 Token，训练模型在 CoT 轨迹中预测这些 Token。
    2.  **净工具增益（NTG）指标**：论文创新性地提出了 NTG 指标，用于量化工具调用对性能的边际贡献，发现 SFT 后的模型存在严重的工具过度使用问题。
    3.  **自我调节奖励驱动对齐（Stage 2）**：引入 **MC-Reward（自我调节置信度校准奖励）**。该机制基于模型在 Rollout 阶段的平均准确率 $p$ 与预设阈值 $\tau$ 的关系：
        *   当 $p > \tau$（简单任务）时，惩罚工具调用，鼓励直接推理。
        *   当 $p < \tau$（困难任务）时，奖励有效的工具调用。
    4.  **算法流程**：模型首先生成推理计划，自主决定是否触发 `<tool_start>` 标签。若触发，则生成隐式视觉 Token 并结合后续文本进行推理；若不触发，则直接输出答案。

#### 3. 实验设计
*   **数据集与场景**：
    *   **细粒度感知**：V* Bench, HRBench (4K/8K), MME-RealWorld, MME-RealWorld-Lite。
    *   **通用推理与泛化**：LogicVista, VisuLogic, WeMath, POPE（幻觉评估）, SEEDBench 等。
*   **对比方法**：
    *   **闭源模型**：Gemini-2.5-Flash/Pro。
    *   **开源 SOTA**：Qwen3-VL (4B/8B/235B), GLM-4.5V (108B), Kimi-K2.6 (1T)。
    *   **Agentic/工具类模型**：DeepEyes, Thyme, Mini-o3。
    *   **隐式/潜空间模型**：Monet, EVA。

#### 4. 资源与算力
*   **硬件**：使用单节点 **8 张 NVIDIA H800 GPU**。
*   **训练时长**：
    *   **SFT 阶段**：约 **4 小时**（处理 320K 数据）。
    *   **RL 阶段**：约 **49 小时**（处理 55K 数据）。
*   **对比优势**：相比同类模型（如 Thyme 的 RL 阶段需 150 小时），BEE 的训练效率显著更高。

#### 5. 实验数量与充分性
*   **实验规模**：论文在超过 10 个主流 Benchmark 上进行了测试，涵盖了感知、逻辑、数学、幻觉等多个维度。
*   **消融实验**：针对工具槽位格式（替换为原图、unk 字符、纯文本）、Token 长度、阈值 $\tau$ 的选取、感知与推理的解耦等进行了详尽的消融研究。
*   **客观性**：引入了系统级指标（吞吐量、每千次样本成本、延迟方差）的对比，并统一使用 Qwen2.5-VL-235B 作为第三方评测器，确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能卓越**：BEE-8B 在感知任务上的平均得分（75.3）超过了参数量大得多的模型（如 1T 参数的 Kimi-K2.6）。
*   **效率飞跃**：在 8K 高分辨率设置下，BEE 比 Thyme 等外部工具方法**加速达 86.2%**。
*   **成本降低**：部署成本从外部工具方案的 $6.4/1K 样本降低至 **$0.72/1K 样本**。
*   **自适应性**：MC-Reward 成功将简单任务的工具调用率降至接近 0%，而将困难任务的调用率维持在合理水平，实现了真正的“按需分配”。

#### 7. 优点（亮点）
*   **端到端优化**：消除了外部 API 调用带来的系统级延迟和重编码开销。
*   **认知边界意识**：通过强化学习让模型学会“自省”，识别自身能力的局限性，从而优化推理路径。
*   **部署友好**：由于是隐式 Token 设计，BEE 可以像标准多模态模型一样直接部署，无需复杂的沙盒环境。

#### 8. 不足与局限
*   **复杂逻辑推理瓶颈**：在 VisuLogic 等极高难度的逻辑推理任务上，相比超大规模模型仍有差距，有时会跳过必要的推理步骤。
*   **多目标感知遗漏**：在自动驾驶等密集目标场景中，偶尔会漏掉部分视觉元素（如远处的车辆）。
*   **数据依赖**：模型性能高度依赖于第一阶段构建的 CoT 轨迹质量，且目前主要覆盖了 6 种视觉操作，工具多样性仍有扩展空间。

（完）
