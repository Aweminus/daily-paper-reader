---
title: "SIVA-RL: Sensitivity-Invariance Visual Alignment for Multimodal Reinforcement Learning"
title_zh: SIVA-RL：多模态强化学习中的敏感性-不变性视觉对齐
authors: "Cheng Tang, Junzhi Ning, Min Cen, Wei Li, Xinyi Zeng, Pinxian Zeng, Rongbin Li, Qiming Zhu, Yuqiang Li, Junjun He, Yirong Chen, Ming Hu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13931v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态强化学习用于将预测关联至视觉证据
tldr: 针对多模态强化学习中模型可能仅凭文本逻辑而非视觉证据得出正确答案的问题，本文提出SIVA-RL框架。该框架通过PatchSwap构建局部视觉干预，并根据干预后奖励的实际变化（而非干预类型）动态分配监督信号。通过对奖励大幅下降的样本进行敏感性对齐，对变化微小的样本进行不变性对齐，显著提升了模型在视觉推理任务中的视觉接地能力和整体性能。
selection_source: fresh_fetch
motivation: 现有的视觉干预方法错误地假设相同的干预操作在不同样本上会产生一致的效果，导致监督信号不准确，无法有效保证模型的视觉接地。
method: 提出一种基于结果调节的对齐框架，利用PatchSwap生成干预样本，并根据审计策略评估的奖励降幅将样本动态路由至敏感性对齐或不变性对齐路径。
result: "在9个多模态推理基准测试中，SIVA-RL在3B和7B模型上均优于基准，视觉依赖型任务提升达8.79个百分点，整体相对提升最高达14.9%。"
conclusion: SIVA-RL通过解耦干预构建与监督分配，有效增强了多模态大模型在强化学习过程中的视觉对齐能力。
---

## 摘要
具有可验证奖励的强化学习（RLVR）推动了多模态推理的发展，但答案层面的正确性并不能保证视觉语言模型将其预测建立在视觉证据之上。现有的视觉干预方法通过对比原始图像和修改后图像上的策略行为来进行，但它们是根据干预类型而非观察到的效果来分配监督信号。这一假设存在缺陷：相同的算子在不同样本上会产生异质的结果。我们提出了 SIVA-RL，这是一个敏感性-不变性视觉对齐框架，它将基于算子的正则化替换为基于样本和结果的监督。SIVA-RL 通过令牌对齐且受距离约束的图像内 PatchSwap 构建局部干预。随后，一个冻结的审计策略对每个“原始-干预”对进行评分，观察到的奖励下降将转化为软路由权重。奖励大幅下降的样本对驱动敏感性对齐，下降较小的样本对驱动以原始图像为锚点的不变性对齐，而模糊的样本对则被降低权重。这种设计将干预构建与监督分配解耦，并兼容 GRPO 和 DAPO 骨干网络。在涵盖数学、逻辑和视觉依赖任务的九个多模态推理基准测试中，SIVA-RL 在所有设置下均使 3B 和 7B 模型优于匹配的强化学习基准。它在视觉依赖推理上实现了 8.79 个百分点的提升，并在所有四种基于 GRPO 和 DAPO 的配置中实现了高达 14.9% 的相对整体改进。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) drives multimodal reasoning, but answer-level correctness does not guarantee that a vision-language model grounds its predictions in visual evidence. Existing visual-intervention methods contrast policy behavior on original and modified images, yet assign supervision by the type of intervention rather than its observed effect. This assumption fails: identical operators produce heterogeneous outcomes across samples. We propose SIVA-RL, a Sensitivity-Invariance Visual Alignment framework that replaces operator-conditioned regularization with sample-wise, outcome-conditioned supervision. SIVA-RL constructs localized interventions through token-aligned, distance-constrained within-image PatchSwap. A frozen audit policy then scores each clean-intervention pair, and the observed reward drop becomes soft routing weights. Large-drop pairs drive sensitivity alignment, low-drop pairs drive clean-anchored invariance alignment, and ambiguous pairs are down-weighted. This design decouples intervention construction from supervision assignment and is compatible with both GRPO and DAPO backbones. Across nine multimodal reasoning benchmarks spanning mathematical, logical, and vision-dependent tasks, SIVA-RL improves 3B and 7B models over matched RL baselines in every setting. It yields an 8.79 percentage-point gain on vision-dependent reasoning and up to 14.9% relative overall improvement across all four GRPO- and DAPO-based configurations.

---

## 论文详细总结（自动生成）

### SIVA-RL: 多模态强化学习中的敏感性-不变性视觉对齐总结

#### 1. 核心问题与研究动机
*   **核心问题**：在多模态强化学习（RLVR）中，模型即使给出了正确答案，也可能并非基于视觉证据，而是依赖语言先验、答案频率偏差或记忆的推理模板（即“奖励歧义”）。
*   **研究动机**：现有的视觉干预方法（如掩码、扰动）通常假设某种干预操作必然导致某种结果（如破坏性干预应导致性能下降）。然而，作者通过审计发现，**相同的干预算子在不同样本上会产生异质效应**（有些样本奖励大幅下降，有些则保持稳定）。因此，基于算子类型的固定正则化是不准确的，需要一种基于样本实际结果的动态监督机制。

#### 2. 方法论
SIVA-RL 框架将视觉干预监督分解为三个阶段：
*   **局部补丁交换（PatchSwap）构建**：
    *   在图像内部进行令牌对齐（Token-aligned）且受距离约束的补丁交换。
    *   这种方法破坏了局部证据和空间关系，但保留了图像的全局统计特性和风格，比全局掩码更精细。
*   **基于结果的样本级路由（Outcome-Conditioned Routing）**：
    *   使用一个冻结的**审计策略（Audit Policy）**对“原始-干预”图像对进行评分。
    *   计算奖励降幅 $\Delta$。根据 $\Delta$ 的大小将样本动态路由至不同路径：
        *   **大降幅样本**：分配高“敏感性”权重，认为干预破坏了关键证据。
        *   **小降幅样本**：分配“不变性”权重，认为干预未触及核心信息或模型具有鲁棒性。
        *   **模糊样本**：降低权重或跳过。
*   **双向对齐目标（Dual-Direction Alignment）**：
    *   **敏感性对齐（Sensitivity Alignment）**：强制原始图像与干预图像的预测分布保持一个最小距离（Margin），鼓励模型感知视觉变化。
    *   **不变性对齐（Invariance Alignment）**：在一定阈值内约束两者的差异，鼓励模型在非关键变化下保持一致性。
    *   该方法兼容 GRPO 和 DAPO 等主流强化学习算法。

#### 3. 实验设计
*   **模型规模**：Qwen2.5-VL-3B 和 7B。
*   **数据集与基准**：
    *   **通用多模态推理**：Geometry3K, MathVista, We-Math, MMK12, MathVerse。
    *   **视觉依赖型推理**：LogicVista, Super-CLEVR (计数), MMMU-Pro, MathVerse-V (仅视觉版)。
    *   **额外领域**：医疗领域（VQA-RAD, SLAKE, PathVQA）。
*   **对比方法**：匹配的 GRPO/DAPO 基准、PAPO、VPPO、DVRP 以及 GPT-4o、InternVL2.5 等主流模型。

#### 4. 资源与算力
*   **算力配置**：论文附录提到数学阶段和医疗阶段的训练均使用了 **4 张 GPU**。
*   **训练时长**：分为两个阶段，第一阶段（预热）200 步，第二阶段（SIVA 对齐）100 步。
*   **具体型号**：文中未明确说明 GPU 的具体型号（如 A100 或 H100），但提到了使用 `bf16` 精度和 AdamW 优化器。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个核心基准测试，并在 3B 和 7B 两种规模、GRPO 和 DAPO 两种算法框架下进行了交叉验证。
*   **消融实验**：非常充分。包括干预算子对比（随机掩码 vs PatchSwap）、路由策略对比（硬路由 vs 软路由）、超参数敏感性分析（补丁比例、距离约束等）以及训练动态诊断。
*   **客观性**：通过引入“审计策略”和“验证门控”机制，确保了监督信号的可靠性，实验设计考虑了基准模型的公平对比。

#### 6. 主要结论与发现
*   **性能提升**：SIVA-RL 在所有配置下均显著优于基准 RL 方法。在视觉依赖型任务上平均提升 **8.79 个百分点**，整体相对提升最高达 **14.9%**。
*   **视觉接地增强**：实验证明，通过结果调节的对齐，模型减少了对语言先验的依赖，真正提高了对图像细节（如几何形状、物体计数）的感知能力。
*   **异质性验证**：训练过程中的诊断显示，敏感性路由和不变性路由在整个训练期间都保持活跃，证明了双向对齐的必要性。

#### 7. 优点
*   **解耦设计**：将“干预如何构建”与“如何分配监督”解耦，解决了传统方法中算子效应不均的问题。
*   **样本级精度**：通过实际观察到的奖励变化来决定优化方向，比粗暴的算子分类更科学、更符合数据分布。
*   **即插即用**：作为辅助损失项，能够轻松集成到现有的多模态 RL 流程中。

#### 8. 不足与局限
*   **复杂场景失效**：在极端稠密的物体计数（如 Case 5 所示）中，模型仍会出现过数或漏数，说明仅靠答案级的奖励对齐可能不足以处理极细粒度的感知。
*   **计算开销**：引入审计策略需要额外的推理步骤（虽然是冻结的），在训练效率上比单一路径的 RL 略有增加。
*   **医疗领域局限**：作者指出医疗 QA 的提升可能部分源于文本逻辑的增强，而非纯粹的视觉接地，需谨慎解读。

（完）
