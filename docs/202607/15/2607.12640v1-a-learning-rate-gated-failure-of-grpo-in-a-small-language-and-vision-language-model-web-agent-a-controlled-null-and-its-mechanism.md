---
title: "A Learning-Rate-Gated Failure of GRPO in a Small Language and Vision-Language Model Web Agent: A Controlled Null and Its Mechanism"
title_zh: 小型语言与视觉语言模型 Web 智能体中受学习率门控的 GRPO 失效：一项受控的零结果及其机制研究
authors: "Chengguang Gan, Zhixi Cai, Yunhao Liang, Hanjun Wei, Shiwen Ni, Qinghao Zhang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12640v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 针对VLM网络智能体的强化学习(GRPO)
tldr: 本研究探讨了强化学习算法GRPO在4B至8B规模的小型语言及视觉语言模型Web代理中的应用效果。通过对学习率、KL权重等参数的受控实验发现，在模型已掌握的任务上，GRPO无法超越强监督基线，且中高学习率会导致性能下降甚至崩溃。研究揭示了GRPO仅在采样策略优于贪婪策略时有效，并分析了模型退化与注意力层、MLP块及有效秩之间的关联，为理解小模型强化学习的局限性提供了重要见解。
selection_source: fresh_fetch
motivation: 探究GRPO强化学习能否在小型Web代理模型中提升技能，还是仅重塑模型已有的行为。
method: 在不同学习率、初始化和观察模式下，对4B和8B规模的模型进行18次受控实验，并对比监督基线。
result: 在已掌握任务中GRPO未能提升成功率，且中高学习率会导致性能退化，仅在存在“提升空间”的任务中见效。
conclusion: GRPO的有效性取决于初始策略的采样潜力，且小模型的性能退化与特定层权重变化及有效秩密切相关。
---

## 摘要
具有可验证奖励的强化学习，特别是群体相对策略优化（GRPO），目前通常在监督微调检查点上运行，以期产生更强大的智能体。我们探究了它是否能为 4B 到 8B 规模的小型语言和视觉语言模型 Web 智能体增加技能，还是仅仅重塑了监督模型已有的行为。在涵盖学习率、KL 权重、种子、初始化和裁剪等变量的 18 次实验控制网格中，没有任何配置能可靠地提高强监督基线在智能体已基本掌握的任务上的成功率。在文本轨道上，中到高的学习率会使其表现显著变差。这一零结果在配对测试、25 个评估种子、6 个训练种子、方案变更、文本和 Set-of-Marks 截图观测以及将骨干网络扩展到 8B 的情况下均成立；显著的损害主要出现在文本轨道，而在 Set-of-Marks 下仅为名义上的。为了证明该零结果反映的是设定而非流水线故障，我们在奖励可通过采样达到的任务上运行了相同的测试框架、奖励和方案，结果成功率提高了 22 个百分点，且配对区间不包含零。因此，GRPO 仅在存在提升空间时才有帮助，即采样策略的成功率已经高于贪婪策略。随后我们解释了这一失效现象。中等学习率会导致智能体性能退化，而高学习率会导致其崩溃，这两种状态形成了双重分离：嫁接实验将退化状态定位在注意力层和 MLP 块，而崩溃状态无法追溯到任何单一组，且主导权重变化的嵌入层变化在因果上是无效的。在 4B 规模下，后期层的有效秩在两个方向上都与能力相关；在 8B 规模下，两者则发生了分离。这种耦合是小型模型特有的，因此我们将其报告为规模相关的。

## Abstract
Reinforcement learning with verifiable rewards, and Group Relative Policy Optimization (GRPO) in particular, is now run routinely on a supervised checkpoint in the hope of producing a stronger agent. We ask whether it adds skill to a small language and vision-language model web agent at the 4B to 8B scale, or whether it mostly reshapes behavior the supervised model already has. Across a control grid of 18 runs that varies learning rate, KL weight, seed, initialization, and clipping, no configuration credibly improves the success rate of a strong supervised baseline on tasks the agent has largely mastered. On the text track, moderate to high learning rates make it credibly worse. The null holds under paired testing, 25 evaluation seeds, 6 training seeds, changes to the recipe, both text and Set-of-Marks screenshot observations, and scaling the backbone to 8B; the credible harm is a text-track finding and is only nominal under Set-of-Marks. To show that the null reflects the setting and not a broken pipeline, we run the identical harness, reward, and recipe on tasks whose reward is reachable by sampling, and there the success rate rises by 22 points with a paired interval that excludes zero. GRPO therefore helps only when there is headroom to climb, meaning the sampled policy already succeeds more often than the greedy one. We then explain the failure. A middle learning rate degrades the agent and a high one collapses it, and the two regimes form a double dissociation: grafting localizes the degrade regime to the attention and MLP blocks, while the collapse regime cannot be traced to any single group, and the embedding change that dominates the weight movement is causally inert. At 4B, effective rank in the late layers tracks capability in both directions; at 8B the two come apart. This coupling is specific to the smaller model, so we report it as scale-dependent.

---

## 论文详细总结（自动生成）

这是一份关于论文《小型语言与视觉语言模型 Web 智能体中受学习率门控的 GRPO 失效：一项受控的零结果及其机制研究》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在大型推理模型中，强化学习（RL）特别是 GRPO 算法已成为提升性能的标准手段。然而，对于 4B 到 8B 规模的小型 Web 智能体，RL 究竟是赋予了模型“新技能”，还是仅仅“磨利”了监督微调（SFT）模型已有的行为，目前尚不明确。
*   **核心问题**：在小型 Web 智能体上，GRPO 是否能真正超越强监督基线？如果失败了，其背后的物理机制是什么？

### 2. 方法论
*   **核心思想**：通过严密的受控实验（Controlled Null Experiment），在固定任务、奖励和评估框架下，系统性地探索 GRPO 的超参数空间。
*   **关键技术细节**：
    *   **GRPO 算法**：使用群体相对策略优化，通过组内奖励均值中心化来估计优势（Advantage），采用非对称裁剪（Clipping）和 KL 散度锚定。
    *   **因果权重嫁接（Weight Grafting）**：为了定位失效原因，研究者将训练后模型的特定组件（如注意力层、MLP 或嵌入层）替换回初始状态，观察性能恢复情况。
    *   **有效秩（Effective Rank）**：利用线性代数工具测量模型后期层表示的维度，作为衡量模型能力退化的指标。
    *   **上升空间条件（Headroom Condition）**：提出一个简单指标——采样成功率与贪婪解码成功率之差，用于预测 RL 是否会生效。

### 3. 实验设计
*   **场景与数据集**：使用 **MiniWoB** 基准测试，涵盖点击、打字、导航等 Web 交互任务。
*   **模型骨干**：Qwen3-VL（4B 和 8B 版本）。
*   **对比基线**：强监督微调（SFT）模型。
*   **实验分支**：
    *   **控制网格**：18 组实验，涵盖不同学习率（LR）、KL 权重、种子、初始化方式。
    *   **观测轨道**：文本轨道（Text track）和视觉标记轨道（Set-of-Marks）。
    *   **正向控制组**：专门挑选 SFT 模型尚未完全掌握、但通过采样可以触达奖励的任务。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实验规模**：虽然未提及硬件，但作者完成了 18 次网格搜索运行，并进行了 25 个评估种子和 6 个训练种子的鲁棒性验证，显示了相当大的实验工作量。

### 5. 实验数量与充分性
*   **实验数量**：非常充分。除了核心的 18 组网格实验，还包括：
    *   针对 8B 模型的规模扩展实验。
    *   针对视觉模态（SoM）的鲁棒性检查。
    *   针对失效机制的近 2000 个固定状态的探测分析。
*   **客观性与公平性**：研究采用了配对统计测试（McNemar 检验）和 Wilson 置信区间，确保结论不是由于随机噪声产生的。通过“正向控制”排除了训练流水线本身存在故障的可能性。

### 6 主要结论与发现
*   **零结果（Null Result）**：在智能体已基本掌握的任务上，GRPO 无法可靠地超越 SFT 基线。
*   **学习率门控效应**：
    *   **低学习率**：更新太小，表现与 SFT 无异。
    *   **中学习率**：导致模型退化（Degrade），后期层有效秩崩溃。
    *   **高学习率**：导致模型彻底崩溃（Collapse），输出无效。
*   **生效前提**：GRPO 仅在“采样策略优于贪婪策略”时（即存在 Headroom）才能提升性能。
*   **机制发现**：在 4B 模型中，性能退化与后期层的有效秩高度耦合，且这种损害主要发生在注意力层和 MLP 块，而非权重变化最大的嵌入层。在 8B 模型中，这种耦合关系发生了解耦。

### 7. 优点
*   **严谨的科学态度**：不回避“零结果”，而是深入挖掘为什么 RL 没有起作用。
*   **因果诊断**：通过权重嫁接而非仅仅是相关性分析来定位模型失效的物理位置。
*   **实用建议**：为从业者提供了一个廉价的预筛选指标（Headroom 差距），避免在没有提升空间的任务上浪费算力。

### 8. 不足与局限
*   **环境单一**：实验仅限于 MiniWoB 这一确定性较强的基准，在更复杂、动态的 Web 环境（如 WebArena）中结论是否成立尚待验证。
*   **模型局限**：主要基于 Qwen 系列模型，结论可能具有一定的模型家族特异性。
*   **规模效应的复杂性**：研究发现 4B 和 8B 模型在机制上存在差异，这意味着针对更大型模型（如 70B+）的推断需要更加谨慎。

（完）
