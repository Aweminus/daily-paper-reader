---
title: Prompt-Driven Exploration
title_zh: 提示驱动的探索
authors: "Sunshine Jiang, John Marangola, David Zhang, Raghuram Kowdeed, Ruiyang Luo, Nitish Dashora, Richard Li, Pulkit Agrawal, Zhang-Wei Hong"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08837v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: VLM在强化学习探索中对Rollout进行推理以优化提示词
tldr: 针对强化学习中传统动作空间噪声难以实现全局探索的问题，本文提出提示词驱动探索（PDE）。该方法利用视觉语言模型（VLM）对策略执行视频进行诊断，并重写自然语言提示词以诱导更有意义的行为改变。PDE 在提示词层面实现了后验采样框架，有效解决了稀疏奖励下的探索难题。实验表明，PDE 能在零奖励起点下学习成功策略，显著提升了操作与推理任务的样本效率。
selection_source: fresh_fetch
motivation: 传统的动作空间随机扰动难以产生全局性的行为改变，导致强化学习在面临稀疏奖励时难以跳出局部最优。
method: 利用视觉语言模型分析执行视频并重写提示词，在提示词空间实现后验采样以引导策略进行更有效的全局探索。
result: 在操作和推理任务中，PDE 证明了其即使在初始奖励为零的情况下也能成功学习，并大幅提高了学习效率。
conclusion: 将探索空间从动作层级提升到提示词层级，为解决复杂强化学习任务中的探索瓶颈提供了有力的新途径。
---

## 摘要
探索对于强化学习（RL）至关重要，因为策略无法通过重复采样其已偏好的行为来获得提升。标准方法在动作空间中注入随机性，但这种抖动只能产生与原始轨迹接近的展开（rollouts）。摆脱弱策略通常需要动作噪声无法产生的全局扰动。大语言模型（LLMs）和视觉-语言-动作（VLA）模型提供了一条路径：它们将策略建立在自然语言提示（prompt）的基础上，由于展开过程遵循该提示，因此修改提示会引发全局变化。挑战在于寻找能够诱导有用全局变化的提示。对于极少成功的弱策略，奖励过于稀疏，无法据此进行选择。我们的想法是从展开过程本身优化提示：视觉语言模型（VLM）对展开视频进行推理，诊断策略的响应方式，并重写提示以在下次诱导更好的行为。这一过程在提示层面实现了后验采样（posterior sampling）——一种经典的强化学习探索框架：VLM 维护一个关于有用提示的隐式分布，并根据观察到的展开进行更新。我们将这种策略称为提示驱动的探索（PDE）。在操作和推理任务中，PDE 使强化学习即使在零奖励起点下也能学习到成功的策略，并广泛提高了样本效率。我们的网站见 https://xinyunsunshine.github.io/prompt-rl。

## Abstract
Exploration is essential to RL since a policy cannot improve by repeatedly sampling the behaviors it already prefers. Standard methods inject stochasticity in the action space, but such jitter only yields rollouts close to the original. Escaping a weak policy often requires global perturbations that action noise cannot produce. Large language models (LLMs) and vision-language-action (VLA) models offer a pathway: they condition the policy on a natural language prompt, and since the rollout follows from it, modifying the prompt induces global changes. The challenge is finding prompts that induce useful global changes. With a weak policy that rarely succeeds, reward is too sparse to select on. Our idea is to refine prompts from the rollouts themselves: a vision-language model (VLM) reasons over the rollout video, diagnoses how the policy responded, and rewrites the prompt to elicit better behavior next time. This procedure realizes posterior sampling, a classical RL exploration framework, at the level of prompts: the VLM maintains an implicit distribution over useful prompts and updates it from observed rollouts. We call this strategy Prompt-Driven Exploration (PDE). Across manipulation and reasoning tasks, PDE enables RL to learn successful policies even from zero-reward starts, and improves sample efficiency more broadly. Our website is available at https://xinyunsunshine.github.io/prompt-rl.

---

## 论文详细总结（自动生成）

这是一份关于论文《Prompt-Driven Exploration》（提示驱动的探索）的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：强化学习（RL）中的“探索”瓶颈。传统的探索方法（如在动作空间注入随机噪声）只能产生局部的动作抖动，难以诱导全局性的行为改变。在视觉-语言-动作（VLA）模型中，如果初始策略较弱且奖励稀疏，动作噪声几乎无法发现成功路径。
*   **研究动机**：基础模型（LLM/VLA）是受提示词（Prompt）驱动的。改变提示词可以从语义层面直接改变模型的全局策略（例如从“抓取”变为“推”）。
*   **整体含义**：本文提出将探索空间从“动作层级”提升到“提示词层级”，利用视觉语言模型（VLM）作为“后验采样器”，通过分析失败的视频来重写提示词，从而引导策略发现成功的行为。

### 2. 方法论：提示驱动的探索 (PDE)
*   **核心思想**：将提示词优化视为一种后验采样过程。VLM 维护一个关于“有用提示词”的隐式分布，并根据观察到的轨迹反馈不断更新该分布。
*   **关键技术细节**：
    *   **提示词后验更新**：VLM 观察策略在当前提示词下的执行视频，诊断失败原因（如“误触了无关物体”），并生成新的候选提示词（如“避开柜子，直接推微波炉门”）。
    *   **混合采样（Mixture Sampling）**：在数据收集阶段，以一定比例混合使用“原始任务提示词”和“VLM 发现的探索提示词”，并随成功率提升逐渐向原始提示词倾斜。
    *   **混合反向传播（Mixed Backpropagation）**：在 PPO 更新时，同时计算动作在“探索提示词”和“原始提示词”下的对数概率。这确保了通过探索提示词获得的成功经验能够有效地迁移回原始提示词对应的策略上。
*   **算法流程**：
    1. 冻结策略，利用 VLM 进行多轮提示词搜索，建立“成功提示词池”。
    2. 开启 RL 训练，从池中采样提示词生成轨迹。
    3. 使用混合反向传播更新策略参数，使原始提示词也能触发成功的行为。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **机器人操作**：LIBERO（标准任务）、LIBERO-PRO（泛化挑战任务）、ManiSkill（物体放置）。
    *   **真机实验**：使用 Franka FR3 机械臂进行抽屉开启、物体放置等任务。
    *   **LLM 推理**：LiveCodeBench（编程）、AIME 2026（数学竞赛）。
*   **对比方法（Baselines）**：
    *   **噪声类**：Action Noise（标准 PPO）、Parameter Noise（参数空间噪声）。
    *   **奖励/探索类**：RND（随机网络蒸馏）、Robometer（基于图像的密集奖励模型）。
    *   **模型类**：针对不同基座模型（Pi0.5, GR00T, Pi0）进行了测试。

### 4. 资源与算力
*   **算力设备**：论文明确提到每个实验使用了一个节点，包含 **8 张 NVIDIA H200 GPU**。
*   **训练时长**：虽然未详细列出总小时数，但实验涵盖了数百万步（Env Steps）的仿真环境交互，以及 128 次真实的机器人 Rollouts（约 1 小时实机数据）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO-PRO 上测试了 120 个任务，涵盖了 Hard/Medium/Easy 三种难度。
    *   在 ManiSkill 上测试了 25 个物体的泛化性。
    *   进行了详细的**消融实验**：验证了提示词优化的必要性（对比随机改写）、混合反向传播的作用、以及对不同基座模型的适配性。
*   **充分性与公平性**：实验设计较为充分，所有对比方法均在相同的 SFT 权重起点、相同的 PPO 参数和相同的交互预算下进行，确保了对比的客观性。

### 6. 主要结论与发现
*   **突破零奖励困境**：在许多初始成功率为 0% 的任务中，传统方法完全无法学习，而 PDE 通过提示词改写成功“破局”，引导策略获得第一批奖励信号。
*   **显著提升效率**：在 LIBERO-PRO 的困难任务上，PDE 相比 Action Noise 实现了约 60% 的相对性能提升。
*   **跨领域通用性**：PDE 不仅适用于机器人控制，在 LLM 编程任务中也能显著提升早期学习效率（如在 LiveCodeBench 上比标准 RLOO 更快达到 50% 准确率）。
*   **真机有效性**：在仅 1 小时的实机数据下，PDE 训练出的策略成功率是传统 PPO 的两倍以上。

### 7. 优点（亮点）
*   **视角独特**：将 RL 的探索问题转化为提示词空间的搜索问题，充分利用了基础模型（VLM）的语义理解和诊断能力。
*   **即插即用**：不需要训练复杂的辅助奖励模型（如 RND 或密集奖励函数），直接利用现有的语言接口。
*   **迁移性强**：通过混合反向传播巧妙解决了“探索提示词”与“评估提示词”不一致的问题。

### 8. 不足与局限
*   **VLM 依赖性**：该方法高度依赖于一个强大的 VLM（如 GPT-4o 或 Qwen-VL）来提供准确的视频诊断和提示词重写。如果 VLM 诊断错误，可能会引入无效的探索方向。
*   **搜索开销**：在正式 RL 训练前，需要额外的提示词搜索阶段（虽然论文证明这部分开销远小于 RL 训练的交互总量）。
*   **应用限制**：主要适用于“语言条件化”的策略。对于没有语言输入的纯视觉/状态 RL 任务，该方法无法直接应用。

（完）
