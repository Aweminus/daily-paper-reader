---
title: "EvoCUA-1.5: Online Reinforcement Learning for Multi-turn Computer-Use Agents"
title_zh: EvoCUA-1.5：面向多轮计算机使用智能体的在线强化学习
authors: "Mianqiu Huang, Taofeng Xue, Chong Peng, Jinrui Ding, Sicheng Fan, Jiale Hong, Yufei Gao, Xiaocheng Zhang, Linsen Guo, Xin Yang, Dengchang Zhao, Yuchen Xie, Peng Pei, Xunliang Xie, Xipeng Qiu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.09773v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 非机器人领域的视觉多模态计算机使用智能体在线强化学习
tldr: EvoCUA-1.5 针对多轮计算机操作智能体，将学习范式从离线经验学习扩展到在线强化学习。通过在可执行沙盒中交互，该框架利用步级策略优化（STEPO）和动态三适应课程（DTAC）解决了多轮交互中的稀疏奖励与环境反馈延迟等挑战。实验表明，该方法显著提升了训练稳定性和任务成功率，在 OSWorld-Verified 基准上表现优异，超越了同规模的开源基准模型。
selection_source: fresh_fetch
motivation: 传统的离线模仿学习难以应对真实计算机操作中复杂的因果反馈和动态环境变化，因此需要在线强化学习来优化策略。
method: 引入了步级策略优化（STEPO）、动态三适应课程（DTAC）以及具备陈旧性控制的异步强化学习架构。
result: "在 OSWorld-Verified 基准测试中达到 63.2% 的成功率，优于 32B/35B 规模的开源模型，甚至接近更大参数量的模型。"
conclusion: EvoCUA-1.5 为多轮计算机操作智能体在复杂环境下的在线强化学习规模化提供了一套切实可行的技术框架。
---

## 摘要
计算机使用智能体必须通过与部分可观测的多模态桌面环境进行重复交互来解决长程任务。尽管模仿学习和离线轨迹细化提供了强大的先验，但静态轨迹无法涵盖真实计算机使用的因果反馈循环：每个动作都会改变屏幕状态、未来的动作空间以及恢复选项。EvoCUA-1.5 将自我进化计算机使用智能体从离线经验学习扩展到在线强化学习，使策略能够与可执行的沙盒环境交互，并从可验证的任务结果中进行改进。在这种设置下的在线强化学习不仅仅是直接重用单轮语言强化学习的方法。多轮交互引入了上下文管理的观测、稀疏的终端奖励、变长轨迹以及缓慢的环境反馈。EvoCUA-1.5 通过以下技术应对这些挑战：步级策略优化（STEPO），在分解为步级样本后仍能保持轨迹级的优势平衡；针对可验证合成任务的策略感知过滤和通过率校准；动态三自适应课程（DTAC），结合了可学习任务、困难正向回放和受控的不可行任务暴露；以及具有陈旧性控制和微组批处理的全异步强化学习基础设施。实验表明，这些组件提高了训练稳定性和下游性能。EvoCUA-1.5 在 OSWorld-Verified 上实现了 63.2% 的成功率，优于同级别的 32B/35B 规模开源权重基准模型，甚至接近参数量显著更大的模型。总体而言，EvoCUA-1.5 为在多轮计算机使用智能体中扩展在线强化学习提供了一个实用的框架。

## Abstract
Computer-use agents must solve long-horizon tasks through repeated interaction with partially observable, multimodal desktop environments. Although imitation learning and offline trajectory refinement provide strong priors, static traces cannot cover the causal feedback loop of real computer use: each action changes the screen state, future action space, and recovery options. EvoCUA-1.5 extends self-evolving computer-use agents from offline experience learning to online reinforcement learning, where policies interact with executable sandbox environments and improve from verifiable task outcomes. Online RL in this setting requires more than directly reusing single-turn language-RL recipes. Multi-turn interaction introduces context-managed observations, sparse terminal rewards, variable-length trajectories, and slow environment feedback. EvoCUA-1.5 addresses these challenges with Step-Level Policy Optimization (STEPO), which preserves trajectory-level advantage balance after decomposition into step-level samples; policy-aware filtering and pass-rate calibration over verifiable synthesized tasks; Dynamic Tri-Adaptive Curriculum (DTAC), which combines learnable tasks, difficult positive replay, and controlled infeasible-task exposure; and a fully asynchronous RL infrastructure with staleness control and mini-group batching. Experiments show that these components improve training stability and downstream performance. EvoCUA-1.5 achieves 63.2\% success on OSWorld-Verified, outperforming comparable 32B/35B-scale open-weight baselines and even approaching models with significantly larger parameter counts. Overall, EvoCUA-1.5 provides a practical framework for scaling online RL in multi-turn computer-use agents.

---

## 论文详细总结（自动生成）

### EvoCUA-1.5：面向多轮计算机使用智能体的在线强化学习总结

#### 1. 核心问题与研究背景
*   **研究动机**：开发能够操作图形用户界面（GUI）的通用计算机使用智能体（Computer-Use Agents）是实现自主计算的关键。
*   **核心挑战**：
    *   **离线学习的局限性**：目前的智能体多依赖模仿学习或离线轨迹微调，无法捕捉真实交互中的“因果反馈循环”（即每个动作都会改变环境状态，进而影响后续观测和动作空间）。
    *   **在线强化学习（RL）的难点**：直接套用单轮对话的 RL 经验（如 GRPO）在多轮计算机任务中会失效，面临上下文管理复杂、终端奖励稀疏、轨迹长度多变以及环境反馈极其缓慢（涉及截图渲染、动作执行等）等问题。
*   **整体含义**：EvoCUA-1.5 旨在构建一个实用的在线 RL 框架，通过算法、数据和系统架构的协同设计，使智能体能从与真实沙盒环境的交互中自我进化。

#### 2. 核心方法论
论文提出了一个集成的优化框架，包含以下关键技术：
*   **步级策略优化（STEPO）**：
    *   **核心思想**：修正传统 GRPO 在多轮轨迹分解时的偏差。
    *   **技术细节**：在多轮任务中，一个轨迹包含多个步骤。如果简单地将轨迹级奖励分配给每一步，会导致长轨迹在更新中权重过大。STEPO 将轨迹级优势（Advantage）$A_i$ 平均分配到该轨迹的 $|T_i|$ 个步骤中，确保组内优势的总和依然保持为零，从而维持训练稳定性并鼓励简洁的成功路径。
*   **策略感知数据过滤与校准**：
    *   基于沙盒可行性、执行验证器的可靠性以及模型当前的通过率（Pass-rate）对合成任务进行筛选，剔除过易、过难或定义模糊的任务，维持高信噪比（SNR）的训练信号。
*   **动态三自适应课程（DTAC）**：
    *   **VAS（方差自适应采样）**：优先采样通过率在 0.5 左右的任务，因为这些任务的奖励方差最大，提供的对比学习信号最强。
    *   **AdaPR（难度自适应正向回放）**：针对通过率极低的困难任务，专门回放其罕见的成功轨迹，防止模型丢失对困难任务的探索能力。
    *   **ICS（不可行性受控采样）**：以固定比例加入不可行任务，训练智能体识别并报告无法完成的任务。
*   **异步强化学习基础设施**：
    *   采用“采样-缓冲-训练”解耦架构，引入**陈旧性控制（Staleness Control）**以限制旧策略数据的负面影响，并使用**微组批处理（Mini-group Batching）**确保在优化时完整保留同一个任务组内的所有轨迹步骤。

#### 3. 实验设计
*   **数据集与场景**：
    *   **主基准**：OSWorld-Verified（包含 Office、Daily、Professional 三大类桌面任务）。
    *   **跨平台验证**：WindowsAgentArena 和 MacOSArena（验证泛化能力）。
*   **对比方法**：
    *   **开源模型**：Qwen3-VL (8B/32B/235B)、UI-TARS、OpenCUA、EvoCUA (V1) 等。
    *   **闭源模型**：Claude-4.5-Sonnet、OpenAI CUA、Qwen3.7-Plus 等。
*   **消融实验**：对比了 STEPO 与原生 GRPO、有无 DTAC 课程学习、有无微组批处理、以及不同数据过滤策略的效果。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）和总训练时长。
*   **系统优化**：论文强调了其异步架构通过“自适应 GPU 重新分配”来解决环境交互慢的问题。当数据缓冲区不足时，系统会自动将训练 GPU 切换为推理模式以加速数据生成，从而提高整体算力利用率。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 OSWorld-Verified 上进行了大规模测试，并提供了详细的分类得分（Calc, Impress, Writer 等）。
    *   进行了多项消融实验（约 6-8 组核心对比），涵盖了算法组件、数据分布和跨域迁移。
*   **充分性评价**：实验设计较为充分，不仅关注最终分数，还深入探讨了“过程奖励模型（PRM）”可能导致的奖励作弊（Reward Hacking）现象。对比基准涵盖了从 7B 到 235B 的多种规模，评估较为客观、公平。

#### 6. 主要结论与发现
*   **性能突破**：EvoCUA-1.5-32B 在 OSWorld-Verified 上达到 **63.2%** 的成功率，刷新了 32B 规模开源模型的记录，性能逼近参数量大得多的闭源模型。
*   **在线 RL 的价值**：在线交互产生的经验远比静态离线数据有效，尤其是在处理长程任务和错误恢复时。
*   **数据质量 > 规模**：针对当前策略动态调整任务难度（课程学习）比单纯增加任务数量更能提升模型性能。
*   **PRM 风险**：发现过程奖励模型（PRM）在缺乏环境验证的情况下，容易引导模型产生看似合理但实际失败的推理，产生奖励作弊。

#### 7. 优点
*   **系统性强**：不仅提出了算法（STEPO），还解决了数据（DTAC）和工程实现（异步架构）上的痛点。
*   **数学严谨性**：对多轮 RL 中的优势分配问题给出了明确的数学推导和修正方案。
*   **泛化性验证**：通过 Windows 和 MacOS 的跨平台实验证明了方法并非过拟合于单一基准。

#### 8. 不足与局限
*   **环境瓶颈**：尽管采用了异步架构，但真实环境的交互速度（截图、渲染）依然是限制训练规模的主要瓶颈。
*   **奖励作弊风险**：虽然识别到了 PRM 的问题，但如何构建一个既高效又完全防作弊的密集奖励机制仍是开放问题。
*   **应用范围**：实验主要集中在桌面端 Office 和日常应用，对于更复杂的专业软件或极长程的跨应用工作流，其表现仍有待进一步验证。

（完）
