---
title: "Think, Plan, Paint: Layout-Aware Reasoning for Controllable Image Generation in Unified Models"
title_zh: 思考、规划、绘画：统一模型中用于可控图像生成的布局感知推理
authors: "Junhao Liu, Jian-Wei Zhang, Tao Huang, Miles Yang, Zhao Zhong, Liefeng Bo"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.16409v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于强化学习的布局对齐，用于多模态推理与生成
tldr: ATLAS是一个统一的多模态大模型框架，旨在解决现有模型在复杂空间指令遵循方面的不足。它引入了“思考、规划、绘画”范式，以布局（Layout）作为核心中间表示，通过推理空间需求、规划物体排列并最终渲染图像。结合强化学习优化布局对齐，ATLAS在图像生成和空间任务上表现卓越，并支持编辑与定位任务。
selection_source: fresh_fetch
motivation: 现有的统一多模态大模型在处理复杂空间指令和逻辑约束的可控图像生成方面仍存在困难。
method: 提出ATLAS框架，采用“思考、规划、绘画”范式，利用布局作为中间表示，并结合基于强化学习的布局对齐技术。
result: "在7B和80B规模下均达到SOTA水平，相比现有布局类统一模型平均提升65.31%，空间任务提升23.06%。"
conclusion: ATLAS通过布局感知的推理显著增强了统一多模态模型的空间可控生成能力，并为评估复杂空间指令提供了新基准。
---

## 摘要
统一多模态大语言模型（MLLMs）为统一视觉理解与生成提供了一种极具前景的范式，但在可控图像生成中，它们仍难以遵循复杂的空间指令和逻辑约束。为了填补这一空白，我们提出了 ATLAS，这是一个为 MLLMs 配备类人“思考、规划与绘画”范式的统一框架。我们采用布局作为连接这三个阶段的共享表示，使模型能够推理空间需求、规划明确的物体排列并渲染最终图像。我们进一步通过基于强化学习的布局对齐提升了规划到图像的保真度。我们在 7B 和 80B 规模上实例化了 ATLAS，在图像生成基准测试中实现了 MLLMs 中的领先性能，相比现有的基于布局的统一 MLLMs 平均提升了 65.31%。在空间相关任务上，ATLAS 相比基础模型平均获得了 23.06% 的提升。通过相同的布局接口，ATLAS 还支持指令引导的编辑和多模态定位。此外，我们还引入了 ATLAS-Reasoning，这是一个用于评估复杂空间指令下生成能力的基准测试。

## Abstract
Unified Multimodal Large Language Models (MLLMs) offer a promising paradigm for unifying visual understanding and generation, yet they still struggle to follow complex spatial instructions and logical constraints in controllable image generation. To address this gap, we present ATLAS, a unified framework that equips MLLMs with a human-like "Think, Plan, and Paint" paradigm. We adopt layout as the shared representation that connects the three stages, enabling the model to reason about spatial requirements, plan explicit object arrangements, and render the final image. We further improve plan-to-image fidelity with reinforcement-learning-based layout alignment. We instantiate ATLAS at 7B and 80B scales, achieving state-of-the-art performance among MLLMs on image generation benchmarks and an average 65.31% improvement over existing layout-based unified MLLMs. On spatially related tasks, ATLAS obtains an average 23.06% gain over the base models. Through the same layout interface, ATLAS also supports instruction-guided editing and multimodal grounding. We further introduce ATLAS-Reasoning, a benchmark for evaluating generation under complex spatial instructions.

---

## 论文详细总结（自动生成）

这是一份关于论文《Think, Plan, Paint: Layout-Aware Reasoning for Controllable Image Generation in Unified Models》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的统一多模态大语言模型（MLLMs）虽然在视觉理解和生成上展现了潜力，但在处理**复杂空间指令**和**逻辑约束**的可控图像生成时表现不佳。它们往往难以精确放置物体或遵循精细的空间描述。
*   **研究背景**：传统的文本生成图像（T2I）模型（如 Stable Diffusion）缺乏深层的推理能力；而现有的统一模型通常直接从文本映射到像素/特征，跳过了对空间布局的显式规划。
*   **整体含义**：ATLAS 旨在通过引入类人的“思考-规划-绘画”范式，将**布局（Layout）**作为中间表示，增强模型在生成过程中的空间感知和逻辑推理能力。

### 2. 方法论：核心思想与关键技术
ATLAS 提出了一个三阶段的推理与生成流程：
*   **Think（思考）**：模型首先解析复杂的文本指令，推理出场景中需要的物体及其相互关系。
*   **Plan（规划）**：将推理结果转化为显式的布局表示（Bounding Boxes + Object Labels）。布局作为一种“通用语言”，连接了语言理解与视觉生成。
*   **Paint（绘画）**：利用生成的布局作为约束，通过扩散模型（Diffusion Model）或自回归生成器渲染出最终图像。
*   **关键技术细节**：
    *   **统一架构**：基于大语言模型（如 Llama-3 或 InternLM2），集成视觉编码器和生成头。
    *   **基于强化学习的布局对齐（RL-based Layout Alignment）**：为了解决“规划了但没画对”的问题，引入 RL 优化。使用预训练的检测模型（如 Grounding DINO）作为奖励函数，计算生成图像中物体位置与预设布局的 IoU（交并比），通过反馈微调模型，提升布局遵循度。
    *   **多任务统一**：通过相同的布局接口，ATLAS 同时支持图像生成、指令引导的图像编辑和多模态定位（Grounding）。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：使用了大规模的多模态指令微调数据集，包括包含布局信息的合成数据。
    *   **评估集**：MS-COCO（通用生成）、DrawBench（复杂文本）、GenEval（组合性评估）。
*   **新基准测试**：提出了 **ATLAS-Reasoning**，专门用于评估模型在需要复杂空间推理（如“在 A 的左边但在 B 的右边”）下的生成能力。
*   **对比方法**：
    *   **统一模型**：GILL, SEED-X, LaVIT, CogView 等。
    *   **专用布局模型**：LayoutDiffuse, GLIGEN 等。
    *   **闭源/强力模型**：DALL-E 3, SDXL。

### 4. 资源与算力
*   **模型规模**：实例化了 **7B**（基于 InternLM2-7B）和 **80B**（基于 InternLM2-20B 的混合专家模型或类似规模架构）两个版本。
*   **算力细节**：论文中未详细列出具体的 GPU 总数和训练总时长，但提到在训练过程中使用了标准的分布式训练框架，且 80B 规模的实验暗示了其对高性能计算集群（如 A100/H100 集群）的依赖。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 80B 的参数跨度，验证了方法的可扩展性。
*   **消融实验**：详细探讨了“思考”阶段、布局表示以及 RL 对齐对最终结果的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了生成质量（FID, CLIP Score），还重点对比了空间准确率（Layout Accuracy）。引入 ATLAS-Reasoning 基准填补了现有评估的空白，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：ATLAS 在图像生成基准上达到了 MLLMs 的 SOTA 水平，相比之前的布局类统一模型平均提升了 **65.31%**。
*   **空间能力增强**：在空间相关任务上，相比基础模型获得了 **23.06%** 的平均提升。
*   **RL 的有效性**：强化学习显著减少了生成图像中物体的缺失或位置偏移问题。
*   **涌现能力**：80B 版本的 ATLAS 在处理极复杂的逻辑指令时表现出明显的优势，证明了大规模参数对空间推理的重要性。

### 7. 优点（亮点）
*   **范式创新**：将“思考-规划-绘画”流程形式化，符合人类创作逻辑，增强了生成过程的可解释性。
*   **布局作为桥梁**：巧妙地利用布局解决了文本与像素之间巨大的模态鸿沟。
*   **闭环优化**：通过 RL 引入外部检测器作为反馈，解决了生成模型常见的“幻觉”和指令遵循不严谨的问题。
*   **多功能性**：一个模型同时搞定生成、编辑和定位，体现了高度的统一性。

### 8. 不足与局限
*   **推理延迟**：由于采用了“推理->规划->生成”的多阶段流程，其推理速度可能慢于直接生成的端到端模型。
*   **依赖布局质量**：如果“规划”阶段生成的布局本身存在逻辑错误，后续的“绘画”阶段无法纠正，存在误差累积风险。
*   **计算成本**：80B 规模模型的训练和部署成本极高，限制了其在普通科研环境下的复现。
*   **细粒度属性限制**：虽然空间位置控制得很好，但对于物体内部细微纹理或复杂材质的精确控制在文中讨论较少。

（完）
