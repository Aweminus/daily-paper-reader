---
title: "EmoAgent-R1: Towards Multimodal Emotion Understanding with Reinforcement Learning-based Dynamic Agent Specialization"
title_zh: EmoAgent-R1：基于强化学习的动态智能体专业化实现多模态情感理解
authors: "Lihuang Fang, Yuchen Zou, kebin Jin, Jinghui Qin"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21013v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用强化学习优化多模态智能体的推理与专业化
tldr: 针对多模态情感识别（MER）中固定提示词难以应对复杂情感源的问题，本文提出 EmoAgent-R1 框架。该框架通过强化学习实现动态智能体特化，采用冷启动策略赋予模型初步推理与路由能力，并引入渐进式组相对策略优化（P-GRPO）算法，利用 PMI 启发式令牌级调制解决稀疏奖励问题。实验证明该方法在情感推理性能和优化稳定性上均优于现有模型。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在情感识别时通常使用固定提示词，忽略了多模态输入中情感来源的动态性和复杂性。
method: 提出一种基于强化学习的动态智能体特化框架，并设计了 P-GRPO 算法以实现细粒度的令牌级学习信号调制。
result: 在多个多模态情感识别基准测试中，EmoAgent-R1 展现出更强的情感推理能力和更高的优化稳定性。
conclusion: 通过强化学习驱动的动态智能体特化，可以显著提升多模态大模型在复杂情感理解任务中的表现。
---

## 摘要
多模态大语言模型（MLLMs）在多模态情感识别（MER）任务中取得了令人瞩目的性能，并将 MER 提升到了一个新的水平，即结合先进的视频理解能力和自然语言描述的复杂情感理解。然而，现有的基于 MLLM 的方法通常使用固定的提示词来感知情感，忽略了多模态输入中情感来源的动态性和复杂性。为了解决这些问题，我们提出了一种新颖的基于强化学习的动态智能体专业化框架（EmoAgent-R1），通过基于强化学习的动态智能体专业化来优化 MLLM 的情感识别、推理和泛化能力。具体而言，我们首先采用冷启动策略，通过使用合成的以答案为条件的思维链数据和智能体路由数据进行训练，赋予 MLLM 初步的情感识别、推理和智能体路由能力。然后，我们进一步利用强化学习训练 MLLM，使其在包含智能体选择和智能体专业化的两步智能体工作流中感知情感。为了有效地训练 EmoAgent-R1，我们提出了一种新颖的渐进式组相对策略优化（P-GRPO），将基于组的相对优势与受 PMI 启发的渐进式 Token 级调制相结合，将稀疏奖励转化为细粒度的学习信号，从而缓解了 GRPO 中粗粒度的统一信用分配问题。在 MER 基准测试上的广泛实验证明了 EmoAgent-R1 在更强的情感推理性能和更高的优化稳定性方面的优越性。

## Abstract
Multimodal large language models (MLLMs) have achieved impressive performance in multimodal emotion recognition (MER) tasks and lifted MER to a new level that is complex emotion understanding with advanced video understanding abilities and natural language description. However, existing MLLM-based methods often use a fixed prompt to perceive the emotions, ignoring the dynamicity and complexity of the emotion source in the multimodal inputs. To address these issues, we propose a novel Reinforcement Learning-based Dynamic Agent Specialization framework (\textbf{EmoAgent-R1}) to optimize the emotion recognition, reasoning, and generalization abilities of an MLLM with dynamic agent specialization based on reinforcement learning. Specifically, we first adopt a cold start strategy to endow an MLLM with preliminary emotion recognition, reasoning, and agent routing ability by training with synthetic answer-conditioned chain-of-thought data and agent routing data. Then, we further train the MLLM with reinforcement learning to perceive emotions in a two-step agentic workflow with agent selection and agent specialization. To effectively train EmoAgent-R1, we propose a novel Progressive Group-Relative Policy Optimization (P-GRPO) to combine group-based relative advantages with a PMI-inspired progressive token-level modulation to transform sparse rewards into fine-grained learning signals, mitigating the coarse-grained uniform credit assignment issue in GRPO. Extensive experiments on MER benchmarks demonstrate the superiority of our EmoAgent-R1 in stronger emotion reasoning performance and improved optimization stability.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《EmoAgent-R1: Towards Multimodal Emotion Understanding with Reinforcement Learning-based Dynamic Agent Specialization》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在处理多模态情感识别（MER）时，通常采用**静态、单一的提示词（Monolithic Agent）**。这种方式存在“统一性偏差（Uniformity Bias）”，即模型倾向于均匀地对待所有模态和时间段，忽略了情感信号在视频中往往是稀疏、局部且模态依赖的（例如：微表情可能只持续几帧，或者情感仅通过语调而非文字表达）。
*   **研究背景**：为了应对情感源的动态性和复杂性，研究者希望赋予模型“动态路由”和“专家特化”的能力，使其能根据输入内容自动选择最合适的推理策略。

### 2. 方法论：核心思想与关键技术
论文提出了 **EmoAgent-R1** 框架，其核心思想是将情感理解分解为“路由选择”和“特化推理”两个阶段，并通过强化学习进行优化。
*   **动态智能体特化架构（DSA）**：
    *   **路由智能体（Router Agent）**：负责元认知，评估输入的模态优势，从专家库（如视觉专家、讽刺专家等）中选择最优策略。
    *   **特化智能体（Specialized Agent）**：根据路由指令，在特定的归纳偏置下生成思维链（CoT）推理。
*   **两阶段学习流程**：
    1.  **阶段 I：冷启动训练**：利用教师模型（Qwen2.5-VL-72B）生成“答案条件化”的合成 CoT 数据和路由数据，通过监督微调（SFT）让模型具备初步的推理和专家选择能力。
    2.  **阶段 II：强化学习（P-GRPO）**：提出**渐进式组相对策略优化（Progressive Group-Relative Policy Optimization）**。
*   **关键算法 P-GRPO**：
    *   针对标准 GRPO 存在的“粗粒度信用分配”问题（即序列级奖励均匀分配给所有 Token），引入了**PMI（点互信息）启发的 Token 级调制因子**。
    *   该因子根据 Token 在推理路径中的位置和贡献度动态调整梯度权重，使模型能识别出哪些关键推理步骤导致了正确答案，从而在稀疏奖励环境下实现更稳定的收敛。

### 3. 实验设计
*   **数据集/场景**：采用了 **MER-UniBench** 统一基准，涵盖了：
    *   **情感分析**：MOSI, MOSEI, SIMS, SIMS v2。
    *   **基本情感识别**：MER23, MER24, MELD, IEMO。
    *   **细粒度情感理解**：OV-MERD+（开放词汇任务）。
*   **对比方法**：包括通用模型（Video-LLaVA, VideoChat2, mPLUG-Owl 等）和情感领域专用模型（AffectGPT, AffectGPT-R1）。

### 4. 资源与算力
*   **硬件环境**：实验在 **H20 GPU**（显存 96GB）上运行。
*   **训练细节**：
    *   使用 PyTorch 框架。
    *   冷启动阶段：Batch size 为 8，学习率 1e-5。
    *   强化学习阶段：Batch size 为 1。
    *   **未明确说明**：论文未明确给出具体的 GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在 9 个子数据集上进行了全面测试，并提供了详细的消融实验。
*   **充分性评价**：
    *   **多维度验证**：不仅对比了最终性能，还进行了**模型缩放（Scaling）分析**（对比 3B、4B、7B 参数量），证明了该框架在小参数模型上也能超越大参数通用模型。
    *   **消融实验**：分别验证了冷启动策略、P-GRPO 算法以及动态特化架构的必要性。
    *   **客观性**：使用了统一的 MER-UniBench 评估指标（如 EW 相似度、WAF 等），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：EmoAgent-R1 在 MER-UniBench 上取得了 **77.85%** 的平均分，刷新了 SOTA 记录，优于之前的 AffectGPT-R1。
*   **推理能力增强**：在最难的细粒度情感任务（OV-MERD+）中，性能提升显著，证明了 P-GRPO 在处理复杂语义推理时的有效性。
*   **缩放效应**：研究发现，通过强化学习优化的 3B 模型在情感任务上的表现甚至超过了 7B 的指令微调基座模型，说明**结构化推理优化比单纯增加参数量更有效**。

### 7. 优点（亮点）
*   **动态路由机制**：打破了传统“一个提示词走天下”的局限，使模型能像人类专家一样根据视频特征（如视觉主导或文本主导）灵活切换视角。
*   **细粒度奖励分配**：P-GRPO 算法通过 PMI 调制解决了强化学习在长文本推理中“奖励信号稀疏且模糊”的痛点，提高了训练效率。
*   **冷启动策略**：通过事后重标记（Hindsight Relabeling）构建路由语料，巧妙解决了“没有专家选择标签”的难题。

### 8. 不足与局限
*   **计算开销**：两步智能体工作流（先路由、后推理）相比单次推理会增加推理延迟和计算成本。
*   **数据依赖**：冷启动阶段高度依赖高性能教师模型（如 Qwen2.5-72B）生成的合成数据质量，若合成数据存在偏差，可能影响后续 RL 的效果。
*   **模态冲突处理**：虽然提出了专家特化，但在极端模态冲突（如视觉和音频表达完全相反的情感）的情况下，路由智能体是否能始终做出最优判断仍有待更极端的压力测试。

（完）
