---
title: Learning Robust Execution in Robotic Manipulation with Agentic Reinforcement Learning
title_zh: 基于智能体强化学习的机器人操作鲁棒执行学习
authors: "Xiaopeng Zhang, Yueyang Weng, Qi Liu, Yongjin Mu, Yanjie Li"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13818v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的代理强化学习
tldr: 针对机器人操作中因不确定性和长程执行导致的误差累积问题，本文提出了一种智能体强化学习框架。该框架通过引入运行时执行质量评估指标，使策略能够在高层进行决策，在执行偏离时触发恢复机制，将机器人恢复至标称状态。在LIBERO基准测试中，该方法显著提升了标准及干扰环境下的任务成功率，增强了执行鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作（VLA）模型缺乏评估执行稳定性及在偏离预定行为时进行自我恢复的显式机制。
method: 提出了两种互补的运行时执行质量评估指标，并构建了一个通过高层决策选择执行模式以实现状态恢复的智能体强化学习框架。
result: "在LIBERO基准测试中，该方法在标准设置下成功率提升达13.7%，在干扰设置下提升达39.2%。"
conclusion: 通过引入高层推理和恢复机制，该研究显著增强了机器人操作在复杂和干扰环境下的执行鲁棒性。
---

## 摘要
由于不确定性、长时程执行和复合误差，机器人操作面临着根本性的挑战，这些因素很容易使执行过程不稳定并导致任务失败。尽管最近的视觉-语言-动作（VLA）模型表现出强大的泛化能力，但它们通常缺乏评估执行稳定性的显式机制，也缺乏在执行偏离名义行为时进行恢复的机制。在本文中，我们提出了：（1）两个用于在运行时评估执行质量的互补指标，以及（2）一个智能体强化学习框架，该框架通过高层决策而非直接学习底层动作来学习恢复有效执行。在该框架中，智能体策略对最近的执行历史进行推理，并在一组少量的执行模式中进行选择，以调节执行过程。在执行退化的情况下，它会触发适当的恢复机制，将机器人恢复到先前访问过的名义状态，从而使任务得以继续。我们在 LIBERO 基准测试上评估了所提方法，在标准设置下成功率提升了高达 13.7%，在干扰设置下提升了高达 39.2%，证明了执行鲁棒性的显著增强。

## Abstract
Robotic manipulation poses fundamental challenges due to uncertainty, long-horizon execution, and compounding errors, which can easily destabilize execution and lead to task failure. Although recent vision-language-action (VLA) models exhibit strong generalization, they typically lack explicit mechanisms to assess execution stability and to recover when execution deviates from its nominal behavior. In this paper, we propose: (1) two complementary metrics to assess execution quality at runtime, and (2) an agentic reinforcement learning framework that learns to restore effective execution through high-level decision-making rather than directly learning low-level actions. In this framework, an agentic policy reasons over recent execution history and selects among a small set of execution modes to regulate the execution process. Under execution degradation, it triggers appropriate recovery mechanisms to restore the robot to previously visited nominal states, enabling the task to continue. We evaluate the proposed method on the LIBERO benchmark, achieving up to a 13.7% improvement in success rate under standard settings and up to a 39.2% improvement under disturbance settings, demonstrating substantially enhanced execution robustness.

---

## 论文详细总结（自动生成）

### 论文总结：基于智能体强化学习的机器人操作鲁棒执行学习

#### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于机器人操作中的**“执行级失败”（Execution-level failures）**。尽管现有的视觉-语言-动作（VLA）模型在泛化性上表现出色，但在长程任务中，微小的感知误差或动力学扰动会随时间累积，导致执行偏离名义轨迹（Nominal trajectory）。
*   **研究动机**：现有的VLA模型通常是“单体式”执行器，缺乏评估自身执行稳定性及在偏离时自我恢复的机制。
*   **现有方案局限**：单纯扩大数据规模成本极高；引入大型视觉语言模型（VLM）进行推理则会带来高延迟和系统复杂性。
*   **核心目标**：在不重新训练底层策略的前提下，通过一个轻量级的高层“智能体策略”来监控执行质量并在必要时触发恢复机制。

#### 2. 论文提出的方法论
提出了一种**智能体强化学习（Agentic RL）框架**，将执行管理与动作生成分离。
*   **核心思想**：冻结底层策略（如VLA），在其上层构建一个智能体策略 $\pi_{agent}$。该策略观察执行历史，从四个离散模式中选择其一：`EXECUTE`（继续）、`RETRY`（回滚重试）、`REPAIR`（修复并恢复）、`RESET`（重置）。
*   **关键技术细节**：
    *   **局部执行质量 ($q_{local}$)**：通过滑动窗口监测末端执行器的运动有效性（实际位移与指令比）和运动平滑度。
    *   **全局执行质量 ($q_{global}$)**：将当前执行特征与成功参考轨迹库进行对比，检测长程漂移。
    *   **恢复机制**：利用空间算子控制（OSC）将机器人回滚到历史中质量得分最高的标称状态。其中 `RETRY` 回滚较短步数，`REPAIR` 回滚至较长时程内的无接触状态。
*   **算法流程**：智能体策略以固定间隔（K步）运行，基于最近 $L$ 步的本体感受、底层动作和质量得分进行推理，通过 PPO 算法优化决策逻辑。

#### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 基准测试，涵盖四个子集：LIBERO-Spatial（空间关系）、LIBERO-Object（物体操作）、LIBERO-Goal（目标达成）和 LIBERO-Long（长程任务）。
*   **对比方法（Baselines）**：
    *   **OpenVLA**、**$\pi_0$**、**$\pi_{0.5}$**（均为当前领先的VLA模型）。
    *   **Diffusion Policy**（扩散策略）。
*   **测试设置**：
    1.  **标准评估**：名义条件下的任务成功率。
    2.  **鲁棒性评估**：在执行过程中随机注入运动学扰动（连续5步的随机噪声）。

#### 4. 资源与算力
*   **训练细节**：每个任务训练步数高达 1,000,000 次高层决策步。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到智能体策略和评论家（Critic）均采用轻量级的多层感知器（MLP），暗示推理开销较低。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个 LIBERO 子集上进行了广泛测试，对比了 4 种主流底层策略。
*   **充分性分析**：
    *   **消融与分析**：包含了决策分布分析（证明策略能根据失败类型选择不同恢复模式）、执行质量变化分析以及恢复成本分析。
    *   **客观性**：使用了 3 个随机种子并报告了 95% 置信区间。通过“扰动测试”模拟现实不确定性，验证了方法的鲁棒性，实验设计较为全面且公平。

#### 6. 论文的主要结论与发现
*   **性能提升**：在标准设置下，该框架使成功率平均提升了 5.1% 至 13.7%；在扰动设置下，提升幅度高达 **25.7% 至 39.2%**。
*   **协同效应**：对于较弱的策略（如 OpenVLA），提升效果最显著；对于已接近饱和的强策略（如 $\pi_{0.5}$），该方法能保持其性能并增强抗干扰能力。
*   **决策逻辑**：智能体策略学会了“分级恢复”：轻微偏差用 `RETRY`，碰撞干扰用 `REPAIR`，不可恢复状态用 `RESET`。

#### 7. 优点
*   **即插即用**：无需修改或重新训练底层昂贵的 VLA 模型，具有极高的灵活性。
*   **轻量高效**：相比基于 VLM 的推理，该方法基于本体感受和执行指标，延迟极低。
*   **可解释性**：通过定义的执行质量指标，可以直观地观察到机器人何时、为何发生执行退化。

#### 8. 不足与局限
*   **恢复能力上限**：对于极度严重的执行退化或完全超出分布（OOD）的场景，仅靠回滚历史状态可能无法解决问题。
*   **依赖参考轨迹**：全局质量指标 $q_{global}$ 需要预先收集少量成功轨迹作为参考库。
*   **感知局限**：目前的智能体决策主要依赖本体感受，未充分利用视觉反馈来识别复杂的语义级失败（例如物体破碎或环境剧烈变化）。

（完）
