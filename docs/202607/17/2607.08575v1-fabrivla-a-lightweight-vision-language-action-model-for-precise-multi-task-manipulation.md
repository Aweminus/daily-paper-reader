---
title: "FabriVLA: A Lightweight Vision-Language-Action Model for Precise Multi-Task Manipulation"
title_zh: FabriVLA：一种用于精确多任务操控的轻量级视觉-语言-动作模型
authors: "Shiyuan Yang, Borong Zhang, Jizheng Zhang, Zhijia Tao, Junfei Guo, Donglai Ran, Xu Bian, Qingbiao Li"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08575v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于多任务操作的轻量级视觉-语言-动作模型
tldr: "FabriVLA 是一种轻量级视觉-语言-动作（VLA）模型，旨在实现精确的多任务机器人操作。它将 InternVL3.5 视觉语言骨干网络与流匹配动作头相结合，通过门控自注意力和浅层 VLM 层融合增强空间上下文感知。在 Meta-World MT50 基准测试中，该 1B 规模的模型实现了 90.0% 的平均成功率，证明了紧凑型 VLA 无需庞大参数量也能展现出极强的多任务处理能力。"
selection_source: fresh_fetch
motivation: 探索轻量级 VLA 模型是否能在不依赖数十亿参数规模的情况下，实现高精度的多任务机器人操作。
method: 采用 InternVL3.5 骨干网络配合流匹配动作头，并通过门控自注意力和浅层特征融合进行单阶段联合训练。
result: "在 Meta-World MT50 的 50 项多样化操作任务中，FabriVLA 达到了 90.0% 的平均成功率。"
conclusion: 实验证明，基于 1B 规模 VLM 构建的紧凑型模型足以在复杂多任务场景中提供卓越的性能。
---

## 摘要
我们提出了 FabriVLA，这是一种用于精确多任务操控的轻量级视觉-语言-动作（VLA）模型。FabriVLA 将 InternVL3.5 视觉-语言主干网络与流匹配（flow-matching）动作头相结合，该动作头具有跨动作标记（action tokens）的门控自注意力机制，并采用浅层 VLM 层融合以增强空间上下文。该模型通过预训练 VLM 和随机初始化的动作头的单阶段联合优化进行训练。在涵盖 50 个不同操控任务的 Meta-World MT50 基准测试中，FabriVLA 实现了 90.0% 的层级平均成功率，这表明基于 1B 规模 VLM 构建的紧凑型 VLA 无需依赖数十亿参数的 VLA 主干网络即可获得强大的性能。

## Abstract
We present FabriVLA, a lightweight Vision-Language-Action model for Precise Multi-Task Manipulation. FabriVLA combines an InternVL3.5 vision-language backbone with a flow-matching action head featuring gated self-attention across action tokens and shallow VLM layer fusion for enriched spatial context. The model is trained via single stage joint optimization from a pretrained VLM and randomly initialized action head. On the Meta-World MT50 benchmark spanning 50 diverse manipulation tasks, FabriVLA achieves a tier-average success rate of 90.0%, demonstrating that a compact VLA built on a 1B scale VLM can achieve strong performance without relying on multi billion parameter VLA backbones.

---

## 论文详细总结（自动生成）

### FabriVLA：用于精确多任务操控的轻量级视觉-语言-动作模型总结

#### 1. 核心问题与整体含义
随着视觉-语言-动作（VLA）模型在机器人操控领域的兴起，虽然大规模模型（如数百亿参数）表现出色，但其高昂的计算成本和推理延迟限制了实时控制的应用。**FabriVLA 的核心目标是探索轻量级 VLA 的性能边界**，即如何在保持较小参数规模（约 1B）的情况下，通过优化架构设计实现高精度的多任务机器人操控。

#### 2. 方法论：核心思想与关键技术
FabriVLA 采用了一种结合预训练视觉语言模型（VLM）与生成式动作头（Action Head）的架构，核心思想是通过增强空间感知和动作序列的内部依赖来提升精度。

*   **骨干网络（Backbone）：** 采用 **InternVL3.5-1B**，保留前 14 层 Transformer。输入 448×448 图像和文本指令，输出多模态融合特征。
*   **浅层特征融合（Shallow Layer Fusion）：** 为了弥补深层特征在空间细节上的损失，模型将第 14 层（语义丰富）和第 6 层（空间细节丰富）的特征进行拼接并投影，使动作头能同时获取物体类别信息和精确的几何位置信息。
*   **门控自注意力动作头（Gated Self-Attention）：** 动作头基于**流匹配（Flow-matching）**框架。其核心是 8 层 Transformer 块，引入了带可学习门控参数（初始为 0）的自注意力机制。这使得模型在训练初期专注于跨模态对齐，随后逐渐学习动作序列步长间的时序依赖。
*   **流匹配训练：** 使用线性插值将噪声转化为专家动作，通过预测速度场（Velocity Field）进行训练。采样时采用 Beta(2, 2) 分布，使模型更关注去噪难度适中的中间阶段。

#### 3. 实验设计
*   **基准测试（Benchmark）：** 使用 **Meta-World MT50**，包含 50 个任务，分为简单、中等、困难、极难四个等级。
*   **数据集：** 使用 Evo-1 公开的 Meta-World 演示数据集，共 2500 条轨迹（每个任务 50 条）。
*   **对比方法：** 与 TinyVLA、π0 (3.5B)、SmolVLA、Evo-1、LA4VLA 等近年主流轻量级或中型 VLA 模型进行了对比。
*   **评估指标：** 报告层级平均成功率（Tier-average Success Rate）和总回合成功率。

#### 4. 资源与算力
*   **硬件：** 使用了 **5 台 NVIDIA RTX PRO 6000 GPU**。
*   **训练细节：** 训练步数为 100,000 步（在 93,000 步处达到最佳性能）。
*   **优化技术：** 采用 **DeepSpeed ZeRO-2** 和 **FP32 主权重**（Master Weights）进行混合精度训练（BF16），以确保在联合优化过程中 VLM 权重的微小更新不会因量化噪声而丢失。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 50 个任务中的每一个进行了 10 次随机初始状态的测试（共 500 次评估）。
*   **消融实验：** 论文进行了充分的消融研究，包括：
    1.  **浅层融合验证：** 对比了仅使用深层特征与融合浅层特征的效果。
    2.  **动作头模块对比：** 对比了门控自注意力（SA）、标记残差头（TR）和时间卷积（TC）的组合效果。
    3.  **训练动态分析：** 记录了不同训练步数下的性能变化。
*   **客观性：** 实验设置了统一的随机种子，并使用了公开基准，对比方法涵盖了当前该领域的代表性模型，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能卓越：** FabriVLA 在 MT50 上达到了 **90.0% 的层级平均成功率**，超越了许多参数量更大的模型（如 3.5B 的 π0）。
*   **空间特征至关重要：** 引入 VLM 的浅层特征融合使平均成功率提升了 7.1%，证明了底层空间信息对精确操控的关键作用。
*   **门控机制有效：** 门控自注意力是动作头中最有效的组件，相比基础跨注意力模型显著提升了性能。
*   **规模并非唯一：** 证明了 1B 规模的模型通过架构优化，无需大规模机器人数据预训练也能在复杂多任务中表现强劲。

#### 7. 优点
*   **轻量高效：** 参数量不到 1B，适合对实时性有要求的机器人部署。
*   **架构创新：** 门控自注意力提供了平滑的优化路径；浅层融合策略简单且有效地解决了 VLM 深层特征空间分辨率不足的问题。
*   **训练策略稳健：** 采用单阶段联合训练，并利用 DeepSpeed 解决了低精度训练下权重更新丢失的问题。

#### 8. 不足与局限
*   **任务局限性：** 在涉及工具使用（Tool-mediated）和粗略运输（Coarse transport）的任务上表现相对较弱（成功率约 83%-85%）。
*   **环境单一：** 实验主要集中在 Meta-World 仿真环境，缺乏在真实世界物理机器人上的验证。
*   **数据依赖：** 尽管不需要机器人预训练，但仍依赖于高质量的专家演示数据。

（完）
