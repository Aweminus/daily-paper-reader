---
title: "Scene Graph Thinking: Reinforcing Structured Visual Reasoning for Multimodal Large Language Models"
title_zh: 场景图思维：增强多模态大语言模型的结构化视觉推理
authors: "Zhiwei Yang, Yuanchen Wu, Nan Zhang, Yucong Meng, Ke Yan, Shouhong Ding"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05716v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态大语言模型的结构化视觉推理
tldr: 针对多模态大模型（MLLM）在处理复杂视觉关系时忽略结构化信息的局限，本文提出SaGe（Scene Graph Thinking）范式。该方法通过自动化数据引擎将图像文本转化为场景图，并构建120K高质量推理轨迹数据。通过监督微调与引入节点代理奖励的强化学习两阶段训练，显著提升了模型在细粒度感知和结构化推理任务上的表现。
selection_source: fresh_fetch
motivation: 现有MLLM多关注孤立对象而忽视了对高效目标导航至关重要的结构化视觉关系，限制了其在视觉密集型任务中的表现。
method: 提出SaGe范式，利用自动化引擎构建场景图并采样推理轨迹，通过监督微调和基于节点代理奖励的强化学习实现图对齐训练。
result: 该方法在8个多模态基准测试中取得了显著的性能提升，充分证明了其在细粒度感知和复杂推理任务上的有效性。
conclusion: SaGe通过显式场景图表示强化了MLLM的结构化视觉推理能力，为提升多模态模型的细粒度理解提供了有效的训练框架。
---

## 摘要
多模态大语言模型（MLLMs）已展现出强大的感知和推理能力。然而，大多数现有模型关注孤立的对象，而忽略了用于高效目标导航的结构化关系，从而限制了它们在视觉密集型任务上的表现。为了应对这一挑战，我们引入了场景图思维（SaGe），这是一种通过显式场景图表示实现细粒度和结构化视觉推理的新范式。具体而言，我们首先引入了一个自动化数据引擎，将扁平的图文语料库转换为结构化场景图，其中层级实体构成节点，多样的视觉关系定义边。在此基础上，我们通过从场景图中采样推理轨迹，构建了 12 万条高质量训练数据。随后，我们引入了两阶段图对齐后训练范式，其中监督微调使 MLLMs 内化结构化推理，随后的强化微调提出了“节点即代理”的图奖励，以巩固高效的图探索。凭借精心策划的数据和图对齐训练，我们的方法在八个多模态基准测试中取得了显著提升，证明了其在细粒度感知和推理任务上的强大有效性。代码可在 https://github.com/zwyang6/SaGe 获取。

## Abstract
Multimodal Large Language Models (MLLMs) have demonstrated strong perception and reasoning capabilities. However, most existing models focus on isolated objects and neglect structured relationships for efficient target navigation, limiting their performance on visually intensive tasks. To address this challenge, we introduce Scene Graph Thinking (SaGe), a novel paradigm that enables fine-grained and structured visual reasoning through explicit scene-graph representations. Specifically, we first introduce an automated data engine that converts flat image-text corpora into structured scene graphs, where hierarchical entities constitute the nodes and diverse visual relations define the edges. Building upon this, we construct 120K high-quality training data by sampling reasoning traces from scene graphs. Then, two-stage graph-aligned post-training paradigms are introduced, where supervised fine-tuning internalizes MLLMs with structured reasoning, and subsequent reinforcement fine-tuning proposes node-as-proxy graph rewards to consolidate efficient graph exploration. With curated data and graph-aligned training, our approach achieves significant improvements across eight multimodal benchmarks, demonstrating strong effectiveness on fine-grained perception and reasoning tasks. Code is available at https://github.com/zwyang6/SaGe.

---

## 论文详细总结（自动生成）

这是一份关于论文《Scene Graph Thinking: Reinforcing Structured Visual Reasoning for Multimodal Large Language Models》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）虽然在基础感知上表现出色，但往往倾向于关注图像中**孤立的对象**，而忽视了对象之间复杂的**结构化关系**。这种“只见树木不见森林”的局限性导致模型在处理视觉密集型任务（如复杂场景推理、目标导航）时表现欠佳。
*   **研究动机**：为了让模型具备类似人类的“结构化思维”，研究者提出将**场景图（Scene Graph）**引入推理过程。场景图能将图像转化为由实体（节点）和关系（边）组成的拓扑结构，从而引导模型进行更有逻辑的细粒度推理。

### 2. 论文提出的方法论：SaGe 范式
该论文提出了 **SaGe (Scene Graph Thinking)** 范式，包含三个关键步骤：
*   **自动化数据引擎**：
    *   利用现有的视觉模型和 LLM，将非结构化的图像-文本对转化为结构化的场景图。
    *   节点代表层级实体（如：人、衣服、纽扣），边代表多样的视觉关系（如：空间位置、从属关系、动作交互）。
*   **推理轨迹构建**：
    *   从生成的场景图中采样推理路径，构建了 **120K 条高质量的推理轨迹数据**。这些轨迹展示了如何从一个物体联想到另一个物体，最终得出结论。
*   **两阶段图对齐后训练**：
    *   **阶段一：监督微调 (SFT)**：让模型学习并内化场景图的结构化表达，使其输出包含逻辑链条的推理过程。
    *   **阶段二：强化学习 (RL)**：引入**“节点即代理”（Node-as-proxy）**的图奖励机制。在模型进行强化学习探索时，如果其推理路径准确命中了场景图中的关键节点和关系，则给予奖励，从而巩固高效的图搜索能力。

### 3. 实验设计
*   **数据集与场景**：涵盖了通用感知、细粒度理解、复杂推理等多种视觉任务。
*   **Benchmark（基准测试）**：在 **8 个主流多模态基准测试**上进行了评估，包括但不限于：
    *   MME, MMBench, SEED-Bench (通用能力)
    *   MMMU, CMMU (学科知识与复杂推理)
    *   以及针对细粒度感知的特定测试。
*   **对比方法**：对比了当前最先进的开源 MLLMs（如 LLaVA 系列、Qwen-VL 等），旨在证明加入 SaGe 范式后的性能增益。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及总训练时长。通常此类规模（120K 数据 + RL 阶段）的训练需要 8 张 A100 或 H100 级别的显卡进行数天的训练。
*   **代码开源**：作者已在 GitHub (https://github.com/zwyang6/SaGe) 提供了代码，可供社区复现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 120K 条精心设计的训练数据。
    *   在 8 个不同的 Benchmark 上进行验证，覆盖面广。
    *   包含**消融实验**，验证了场景图数据、SFT 阶段和 RL 阶段分别对最终性能的贡献。
*   **充分性评价**：实验设计较为充分，通过跨领域的基准测试证明了该方法的通用性。引入 RL 阶段的对比也客观地展示了结构化奖励对模型逻辑一致性的提升。

### 6. 论文的主要结论与发现
*   **结构化思维的有效性**：显式的场景图表示能显著提升 MLLM 在处理复杂视觉关系时的准确性。
*   **RL 的强化作用**：通过“节点即代理”的奖励机制，模型不仅学会了“看”物体，还学会了在复杂的视觉网络中进行高效的“导航”和“搜索”。
*   **性能提升**：SaGe 在所有测试的 8 个基准测试中均取得了显著的性能增长，尤其是在需要细粒度推理的任务中。

### 7. 优点（亮点）
*   **创新的推理范式**：将传统的场景图概念与现代 MLLM 的思维链（CoT）结合，提出了“场景图思维”。
*   **自动化流程**：构建了自动化的数据引擎，解决了结构化视觉数据获取难、成本高的问题。
*   **强化学习创新**：将图结构转化为 RL 的奖励信号，为多模态模型的对齐提供了新思路。

### 8. 不足与局限
*   **依赖前端模型质量**：自动化引擎生成的场景图质量受限于底层视觉模型和 LLM 的能力，若初始图存在错误，可能会导致模型学习到错误的逻辑。
*   **计算复杂度**：引入 RL 阶段会显著增加训练的计算开销和调参难度。
*   **实时性挑战**：在推理时生成复杂的场景图思维链可能会增加响应延迟，论文未详细讨论推理速度的权衡。

（完）
