---
title: "Segmentation before Answering: Pixel Grounding for MLLM Visual Reasoning"
title_zh: 先分割后回答：多模态大语言模型视觉推理中的像素级定位
authors: "Yake Wei, Yuan Wang, Fengyun Rao, Jing Lyu, Di Hu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05798v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态大模型视觉推理的像素级基元
tldr: 本文针对多模态大模型在视觉推理中依赖边界框进行局部缩放的局限性，提出了 SegAnswer 框架。该方法将缩放单位从粗糙的边界框转变为精确的像素级分割掩码，通过隔离目标区域有效过滤背景噪声和干扰物。实验证明，SegAnswer 在高分辨率感知、通用感知及减少幻觉方面表现优异，同时具备可靠的像素接地能力，显著提升了模型对精细视觉细节的处理水平。
selection_source: fresh_fetch
motivation: 现有的多模态大模型通常使用边界框进行局部缩放，这会引入冗余背景和干扰物体，限制了推理的精确度。
method: 提出 SegAnswer 框架，将缩放单位从边界框提升到像素级分割掩码，以实现更精准的感兴趣区域提取和视觉特征对齐。
result: 在多个基准测试中，SegAnswer 在感知能力和抑制幻觉方面均取得显著提升，并展现出优秀的分割性能。
conclusion: 像素级接地比传统的边界框缩放更能有效提升多模态大模型的视觉推理精度，是更优的局部视觉表征方式。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展已从静态感知演变为交错的视觉-语言推理，通常被称为“以图思考”。在这一推理过程中，一项基本操作是放大感兴趣区域（通常以边界框表示），以获取更精细的视觉细节。在本文中，我们提出了“先分割后回答”（SegAnswer），将放大的单位从常用的边界框转变为像素级分割掩码。通过采用细粒度掩码将目标区域从杂乱的环境中分离出来，分割后的视觉输入产生了更精确的感兴趣区域，有效地过滤了冗余背景和干扰物体。此外，分割后视觉输入的离散补丁（patches）能更无缝地与 MLLM 通过位置嵌入构建视觉标记（tokens）的方式相对齐。在实验中，我们在包括高分辨率感知、通用感知和幻觉在内的多种基准测试上评估了 SegAnswer。它实现了持续的改进，并在分割任务上表现出相当的性能，验证了其可靠的像素级定位能力。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have evolved from static perception to interleaved visual-language reasoning, often referred to as ``thinking with images''. A basic operation in this reasoning process is to zoom in on regions of interest (often represented with bounding boxes) to acquire finer visual details. In this paper, we propose \textbf{Seg}mentation before \textbf{Answer}ing (SegAnswer), which shifts the unit of zoom-in from the popular bounding box to pixel-level segmentation mask. By employing fine-grained masks to isolate the target area from cluttered environments, segmented visual input yields a more precise region of interest, effectively filtering out redundant background and interfering objects. Furthermore, the discrete patches of segmented visual input align more seamlessly with how MLLMs structure visual tokens via positional embeddings. In experiments, we evaluate SegAnswer across diverse benchmarks, including high-resolution perception, general perception, and hallucination. It achieves consistent improvements and also exhibits considerable performance on segmentation tasks, validating its capability for reliable pixel grounding.

---

## 论文详细总结（自动生成）

这是一份关于论文《Segmentation before Answering: Pixel Grounding for MLLM Visual Reasoning》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLM）在进行复杂视觉推理（即“以图思考”）时，通常采用**基于边界框（BBox）的局部放大（Zoom-in）**操作。然而，自然界物体形状多为不规则，矩形框不可避免地会引入冗余背景和相邻干扰物，导致语义模糊和视觉噪声。
*   **研究动机**：作者提出将视觉放大的基本单位从“边界框”提升到“**像素级分割掩码**”。通过精确隔离目标区域，模型可以获得更纯净的视觉输入，从而提高推理的准确性，尤其是在处理高分辨率细节和重叠物体时。

### 2. 论文提出的方法论：SegAnswer
SegAnswer 框架通过三个阶段将像素级定位集成到视觉推理中：
*   **核心思想**：在回答问题之前，先对感兴趣区域进行像素级分割，并将分割后的离散补丁（Patches）作为后续推理的输入。
*   **关键技术细节**：
    1.  **架构集成**：以 Qwen2.5-VL-7B 为底座，通过一个 MLP 投影器连接 **SAM 2.1** 作为掩码解码器。引入特殊 Token `<|seg|>` 作为触发分割的接口。
    2.  **空间位置保留**：在处理分割后的图像补丁时，**保留其在原始图像中的位置索引（Position Index）**。这种稀疏的补丁输入能无缝对齐 MLLM 的位置嵌入，有效反映物体的空间关系。
*   **三阶段训练流程**：
    *   **阶段 1：像素定位（Pixel Grounding）**。训练模型根据文本指令生成精确掩码，对齐文本语义与像素特征。
    *   **阶段 2：多模态交错 SFT**。训练模型在对话中主动判断是否需要分割，并学习处理“原图+分割图”的交错输入模式。
    *   **阶段 3：基于像素定位的推理强化学习**。使用 **DAPO（Decoupled clip and dynamic sampling Policy Optimization）** 算法，通过结果驱动的奖励函数（准确率奖励 + 格式奖励）优化推理轨迹。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：RefCOCO 系列、ReasonSeg、ADE20K 等（阶段 1）；VisualCOT（阶段 2）；ViRL39K（阶段 3）。
    *   **评估 Benchmark**：
        *   **高分辨率感知**：V*、HR-Bench 4K/8K。
        *   **通用感知**：MMBench、VisuLogic、MMVP。
        *   **幻觉评估**：POPE、Hallusionbench。
        *   **分割能力**：RefCOCO/+/g。
*   **对比方法**：包括 LLaVA-OneVision-9B、原生 Qwen2.5-VL-7B，以及同样采用视觉操作的推理模型 Pixel Reasoner 和 DeepEyes（两者均基于 BBox）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：提到了学习率设置（1e-3 至 2e-6 不等）、LoRA 微调、强化学习的 Rollout 数量为 4 等超参数细节。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 8 个主流 MLLM 推理与感知基准测试。
    *   在 3 个经典的指代分割（Referring Segmentation）数据集上验证了基础定位能力。
    *   提供了 RL 训练过程中的奖励曲线分析（准确率、格式、总分）。
*   **充分性评价**：实验设计较为全面，不仅对比了通用模型，还专门对比了同样做“视觉操作”的 SOTA 模型。通过定性分析展示了模型处理复杂空间关系（如“扫帚在折叠椅左边”）的能力，证明了像素级定位的优越性。实验对比具有客观性，且基准选择覆盖了从基础感知到高级推理的多个维度。

### 6. 论文的主要结论与发现
*   **性能提升**：SegAnswer 在所有测试基准上均优于底座模型。特别是在 V* 高分辨率基准上，比 Qwen2.5-VL-7B 提升了 **8.9%**。
*   **精准度优势**：相比基于 BBox 的方法（如 DeepEyes），SegAnswer 在 HR-Bench 8K 等极高分辨率任务上表现更佳，证明了像素级过滤背景噪声的有效性。
*   **多能性**：该模型在提升推理能力的同时，其分割性能也达到了甚至超过了某些专门的分割模型（如 LISA、Groundhog）。
*   **RL 效果**：强化学习显著提升了模型利用分割结果进行正确推理的成功率。

### 7. 优点
*   **粒度突破**：首次将视觉推理的“放大”操作从粗糙的矩形框细化到像素级掩码，解决了长久以来的背景干扰问题。
*   **空间感知**：巧妙地利用原始位置索引处理分割补丁，既减少了 Token 消耗，又保留了关键的空间拓扑信息。
*   **训练方案完备**：三阶段训练法（定位 -> 行为初始化 -> 策略优化）逻辑清晰，有效解决了视觉推理中缺乏中间步骤监督的问题。

### 8. 不足与局限
*   **计算开销**：引入 SAM 2.1 作为解码器以及额外的分割推理步骤，会增加端到端的推理延迟，论文未详细讨论推理效率的权衡。
*   **依赖性**：模型的性能高度依赖于第一阶段培养的分割能力，如果初始分割错误，可能会导致后续推理步入歧途。
*   **应用限制**：目前主要针对单次分割推理，对于需要多次、多目标复杂分割的超长链条推理场景，其稳定性和 Token 压力尚待验证。
*   **算力透明度**：缺乏训练资源消耗的具体数据，不利于其他研究者评估复现成本。

（完）
