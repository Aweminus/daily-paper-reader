---
title: Optimal Transport Q-Learning for Flow Policy Steering and Acceleration
title_zh: 用于流策略引导与加速的最优传输Q学习
authors: "Andreas Sochopoulos, Esmeralda S. Whitammer, Nikolaos Tsagkas, João Moura, Michael Gienger, Sethu Vijayakumar"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06262v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于微调视觉语言动作（VLA）模型的强化学习训练
tldr: 扩散和流策略在机器人领域表现优异，但面临推理慢和依赖高质量演示的问题。本文提出最优传输Q学习（OTQL），通过强化学习后训练来微调和加速流策略。OTQL利用优势加权条件最优传输流匹配，仅需50-60次交互即可显著提升策略性能并减少推理步数，为视觉语言动作（VLA）模型的优化提供了高效且低成本的方案。
selection_source: fresh_fetch
motivation: 旨在解决流策略在缺乏高质量演示时表现不佳以及推理速度慢导致的实时性差等问题。
method: 提出OTQL方法，通过优势加权的条件最优传输流匹配，利用机器人自身经验进行强化学习微调。
result: "实验显示OTQL将VLA模型的成功率从38%提升至76%，并将每步动作生成的推理步数减少了70%。"
conclusion: OTQL实现了在极低交互预算下对流策略的有效微调与加速，避免了复杂的模拟蒸馏过程。
---

## 摘要
扩散和流策略最近在机器人应用中展现出卓越的性能，能够准确捕捉多模态机器人轨迹分布，特别是在视觉语言动作（VLA）模型的背景下。然而，高质量的策略性能还需要快速推理和高质量的演示，而这些往往难以获取。缺乏这些要素会导致次优的策略行为以及在分布偏移下的失效。在这项工作中，我们解决了通过强化学习（RL）后训练，利用机器人经验来微调和加速次优流策略的问题。我们提出了最优传输Q学习（OTQL），这是一种利用优势加权条件最优传输流匹配来微调流策略的新方法。OTQL 可以在 50-60 个回合的交互预算内微调并加速流，同时在仿真和真实机器人任务中避免了计算昂贵的蒸馏过程。实验结果表明，OTQL 利用机器人自身的经验对流策略进行后训练，将单任务策略的平均成功率从 36% 提升至 86%，将预训练 VLA 的成功率从 38% 提升至 76%，同时将每次动作生成的推理步数减少了 70%。

## Abstract
Diffusion and flow policies have recently demonstrated remarkable performance in robotic applications by accurately capturing multimodal robot trajectory distributions, especially in the context of vision language action (VLA) models. However, high quality policy performance also requires fast inference and high quality demonstrations, which are often hard to get. Lack of these leads to suboptimal policy behaviors and failure under distribution shifts. In this work we address the problem of fine-tuning and accelerating suboptimal flow-based policies using the robot's experience through RL post-training. We introduce Optimal Transport Q-Learning (OTQL), a new method for finetuning flow policies using advantage weighted conditional optimal transport flow matching. OTQL can finetune and accelerate flows with an interaction budget of 50-60 episodes while avoiding computationally expensive distillation in simulation and real-world robot tasks. Our results show that OTQL post-trains flow policies using the robot's own experience, increasing average success percentage of single-task policies from 36% to 86% and of a pre-trained VLA from 38% to 76% while reducing the number of inference steps per action generation by 70%.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Optimal Transport Q-Learning (OTQL)**，一种旨在同时解决流策略（Flow Policies）推理速度慢和对高质量演示依赖性强的方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：扩散策略（Diffusion Policies）和流匹配（Flow Matching）在处理多模态机器人动作分布方面表现优异，但存在两个主要痛点：
    1.  **推理延迟高**：生成动作通常需要多次迭代（NFE），难以满足实时控制需求。
    2.  **数据依赖性**：依赖大量高质量人类演示，且在分布偏移（OOD）时容易失效。
*   **核心目标**：通过强化学习（RL）后训练（Post-training），利用机器人自身的少量交互经验（50-60个回合），对次优的预训练策略进行微调，在提升任务成功率的同时，显著减少推理步数。

### 2. 方法论
OTQL 的核心思想是将**条件最优传输（COT）**与**优势加权（Advantage Weighting）**相结合：
*   **核心思想**：利用最优传输理论来“拉直”流策略的积分路径（从而减少推理步数），并利用 Q 函数提供的优势值来引导流向高奖励区域。
*   **关键技术细节**：
    *   **优势加权边缘分布**：计算动作的优势值 $A(s, a) = Q(s, a) - V(s)$，并将其作为权重对目标样本进行重采样，使策略向高价值动作偏移。
    *   **条件最优传输（COT）耦合**：在训练流匹配时，不使用随机配对，而是通过求解一个带熵正则化的最优传输问题，在噪声分布和动作分布之间建立最优耦合。
    *   **成本增强**：在 OT 成本函数中加入状态（条件）的相似性约束，确保耦合过程遵循条件一致性。
    *   **算法流程**：
        1. 训练一个 Critic（Q 函数）来评估动作价值。
        2. 在每个训练批次中，根据优势值计算样本权重。
        3. 求解小批量 OT 计划，获得噪声与高价值动作的配对。
        4. 使用流匹配损失函数更新策略网络。
        5. （可选）推理时使用轻量级的拒绝采样（N=5）进一步优化。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用 **OGBench**（包含 40 个离线 RL 任务，如蚂蚁迷宫、人形机器人等）和 **MuJoCo**（微波炉开启、盒子整理）。
    *   **真实世界**：使用 KUKA IIWA 14 机器人完成 4 个任务：叠杯子、关门、移动棋子、磁带入盒。
*   **Benchmark 与对比方法**：
    *   对比了 **DSRL**（扩散引导）、**Q-Chunking**、**FQL**（流 Q 学习）、**QAM-F**、**EWFM**（能量加权流匹配）等最先进的离线/在线 RL 方法。
    *   针对 VLA 模型，对比了 **SmolVLA** 的原始性能与 SFT（监督微调）后的性能。

### 4. 资源与算力
*   **仿真训练**：使用工作站，配置为 AMD Ryzen Threadripper PRO 5965WX (24核)，128GB RAM，以及 **2张 NVIDIA GeForce RTX 4090**。
*   **真实机器人推理**：使用台式机，配置为 Intel i9-9900KF，64GB RAM，**NVIDIA GeForce RTX 2080**。
*   **训练时长**：论文未明确给出总训练小时数，但提到在真实世界中，每 10 个回合交互后进行 20k 步训练，总交互预算仅需 50-60 个回合。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 42 组仿真实验和 4 组真实世界实验，总计 46 个任务。
*   **消融实验**：对温度参数 $\lambda$、拒绝采样候选数 $N$ 以及推理步数（NFE）进行了详细消融。
*   **充分性评价**：实验设计较为充分，涵盖了从单任务策略到大规模 VLA 模型的迁移，且在离线、在线以及离线转在线等多种 RL 设定下进行了验证。对比基线涵盖了当前该领域的主流加速和引导方法，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在真实世界中，OTQL 将单任务策略的平均成功率从 **36% 提升至 86%**。
*   **VLA 增强**：将预训练的 SmolVLA 在新任务上的成功率从 **38% 提升至 76%**。
*   **推理加速**：将每次动作生成的推理步数（NFE）减少了 **70%**（通常只需 2-3 步积分即可达到高性能）。
*   **数据效率**：仅需极少的交互数据（约 50 个回合）即可实现显著的性能飞跃，且无需复杂的模拟蒸馏。

### 7. 优点
*   **高效性**：同时实现了策略引导（性能提升）和加速（推理优化），且训练过程比传统的蒸馏方法更简单。
*   **通用性**：不仅适用于小型策略，也能有效微调复杂的视觉语言动作模型（VLA）。
*   **直路径特性**：通过 OT 耦合强制流路径变直，使得在极低 NFE 下依然能保持高精度。

### 8. 不足与局限
*   **模型限制**：OTQL 要求策略必须是基于神经 ODE 的生成模型，不能直接作为“黑盒”应用于所有类型的策略。
*   **高维挑战**：小批量最优传输在高维空间中可能存在较大的近似误差，随着动作维度增加，加速效果可能减弱。
*   **人工干预**：在真实世界 RL 训练中，仍依赖人工进行回合标记和环境重置，尚未实现完全自主的闭环学习。
*   **计算开销**：虽然推理快，但在训练过程中每批次都需要求解 OT 问题，增加了额外的训练计算负担。

（完）
