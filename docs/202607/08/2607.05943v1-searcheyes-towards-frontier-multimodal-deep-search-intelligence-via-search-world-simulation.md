---
title: "SearchEyes: Towards Frontier Multimodal Deep Search Intelligence via Search World Simulation"
title_zh: SearchEyes：通过搜索世界模拟迈向前沿多模态深度搜索智能
authors: "Zhengbo Jiao, Yiming Cheng, Yilei Jiang, Kaituo Feng, Rui Huang, Tianyi Jiang, Juanxi Tian, Jiapeng li, Qunzhong Wang, Tailai Chen, Qianshan Wei, Chuan Xiao, Shanyu Rong, Yangfu Li, Yanhan Zhou, Yunpu Ma, Yifan Zhang, Xiangyu Yue"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05943v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 执行多跳推理并结合强化学习奖励的多模态搜索智能体
tldr: 针对多模态搜索智能体在多步推理中面临的数据、环境与奖励信号脱节问题，本文提出 SearchEyes 框架。该框架利用知识图谱构建模拟搜索世界，通过感知-知识链（PKC）生成包含元数据的多跳路径，并结合跳锚定策略优化（HaPO）实现步级奖励分配。实验证明，SearchEyes 在多个基准测试中达到开源模型领先水平，显著提升了复杂搜索任务的性能。
selection_source: fresh_fetch
motivation: 现有多模态搜索智能体在训练数据、搜索环境和奖励信号之间存在结构性脱节，导致推理过程中的反馈信号稀疏且难以优化。
method: 构建基于知识图谱的模拟搜索世界，利用感知-知识链（PKC）采样多跳路径，并采用跳锚定策略优化（HaPO）实现无需额外奖励模型的步级信用分配。
result: SearchEyes 在六个多模态知识密集型基准测试中取得 SOTA 性能，其中 27B 版本较最强开源基线平均提升了 6.2 个百分点。
conclusion: 通过统一搜索环境、数据生成与奖励机制，SearchEyes 为开发具备深度推理能力的前沿多模态搜索智能体提供了有效路径。
---

## 摘要
训练多模态搜索智能体进行多跳推理仍然具有挑战性，原因在于一种根本性的结构性脱节：现有的流水线独立地构建训练数据、搜索环境和奖励信号，导致合成的结构化元数据被丢弃，环境依赖于不可复现的外部引擎，且强化学习（RL）奖励在轨迹层面保持稀疏。我们提出了 SearchEyes，它使用有类型的知识图谱作为模拟搜索世界的骨干，从而统一了这三个组件。我们提出了感知-知识链（PKC），在 Wikidata5M 的视觉-知识交集上采样受约束的多跳路径，保留跳级实体元数据，这些元数据同时定义了一个自洽的搜索世界和步级奖励锚点。我们进一步提出了跳锚定策略优化（HaPO），它在不需要单独训练过程奖励模型的情况下，将这些锚点重新用于步级信用分配。在六个多模态知识密集型基准测试上的实验表明，SearchEyes 在开源多模态搜索智能体中实现了最先进的性能，其中 SearchEyes-27B 相比最强的开源基准平均提升了 6.2 个百分点。

## Abstract
Training multimodal search agents to perform multi-hop reasoning remains challenging due to a fundamental structural disconnect: existing pipelines construct training data, search environments, and reward signals independently, causing synthesized structural metadata to be discarded, environments to rely on irreproducible external engines, and RL rewards to remain sparse at the trajectory level. We present \textbf{SearchEyes}, which uses a typed knowledge graph as the backbone of a \emph{simulated search world} that unifies all three components. We propose \textbf{Perception-Knowledge Chains (PKC)} to sample constrained multi-hop paths over the visual-knowledge intersection of Wikidata5M, retaining hop-level entity metadata that simultaneously defines a self-contained search world and step-level reward anchors. We further propose \textbf{Hop-Anchored Policy Optimization (HaPO)}, which reuses these anchors for step-level credit assignment without a separately trained process reward model. Experiments on six multimodal knowledge-intensive benchmarks show that SearchEyes achieves state-of-the-art performance among open-source multimodal search agents, with SearchEyes-27B improving over the strongest open-source baseline by 6.2 points on average.%

---

## 论文详细总结（自动生成）

这是一份关于论文《SearchEyes: Towards Frontier Multimodal Deep Search Intelligence via Search World Simulation》的结构化深度分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多模态搜索智能体（Multimodal Search Agents）在训练中存在**结构性脱节**：
    1.  **数据与环境脱节**：合成数据时生成的结构化元数据（如推理路径、实体ID）在训练阶段被丢弃。
    2.  **环境不可控**：训练依赖外部搜索引擎，导致实验不可复现且API成本高昂。
    3.  **奖励信号稀疏**：强化学习（RL）通常只根据最终答案给出轨迹级奖励，对于长链条的多跳推理，这种反馈极其微弱，难以优化中间步骤。
*   **核心目标**：通过构建一个基于知识图谱（KG）的**模拟搜索世界**，将数据合成、环境模拟和奖励机制三者统一，实现“数据-环境-算法”的协同设计。

### 2. 方法论
SearchEyes 框架由三个核心部分组成：
*   **模拟搜索世界（Search World Simulation）**：
    *   利用 Wikidata5M、Wiki6M 和 Wikipedia 图像构建了一个包含 120 万实体和 580 万关系的闭环环境。
    *   实现了一个确定性的混合检索系统（BM25 + 向量检索），无需外部 API 即可进行可重复的搜索实验。
*   **感知-知识链（PKC）合成**：
    *   **约束采样**：在 KG 上采样多跳路径，强制执行“感知（P）- 知识（K）”交替，确保每道题都必须结合视觉识别和文本推理。
    *   **信息隐藏**：在生成问题时隐藏实体名称，仅提供视觉图像或模糊的领域提示，防止模型通过捷径（Shortcut）猜出答案。
    *   **防捷径过滤**：通过过滤高频节点（Hub）和元关系，提升问题的推理难度。
*   **跳锚定策略优化（HaPO）**：
    *   **核心思想**：利用 PKC 采样时保留的“金牌实体序列”作为**语义锚点**。
    *   **步级信用分配**：如果智能体在某一步检索到了路径中的关键实体，则认为该步骤是正确的。通过对比同一锚点下的不同轨迹，计算步级优势（Advantage），解决了奖励稀疏问题，且无需训练额外的过程奖励模型（PRM）。
    *   **致命错误掩码（Fatal-aware Masking）**：自动识别并屏蔽由于工具调用失败导致的无效轨迹片段。

### 3. 实验设计
*   **数据集/场景**：
    *   **通用基准**：SimpleVQA（事实性）、VDR（深度研究）、MMSearch（网页搜索）、LiveVQA（实时问答）、BrowseComp-VL（网页理解）、FVQA（事实问答）。
    *   **自建基准**：**VisSearch Bench**，专门用于评估多跳视觉搜索能力，包含 1000 个具有 P-K 交替结构的复杂问题。
*   **对比方法**：
    *   **直接推理**：GPT-4o, GPT-5, Gemini-2.5-Pro 等。
    *   **智能体工作流**：基于 Prompt 的 Kimi-K2.5, Claude-4.6-Opus 等。
    *   **专用多模态搜索模型**：MMSearch-R1, OpenSearch-VL, WebWatcher 等开源 SOTA 模型。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **SFT 阶段**：使用 8 台 NVIDIA H20 GPU 进行全参数微调。
    *   **RL 阶段**：使用 64 台 NVIDIA H20 GPU 进行在线强化学习训练。
*   **训练细节**：
    *   SFT 训练 3 个 Epoch。
    *   RL 阶段（HaPO）进行了 200 个优化步骤，每组采样 8 条轨迹（Group Size G=8）。
    *   最大序列长度 8192 词元，最大推理步数 12 步。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个主流第三方基准和 1 个自建基准上进行了测试，覆盖了从基础事实问答到长程网页浏览的多种场景。
*   **消融实验**：
    *   针对 **PKC 数据质量**做了 11 组消融（包括 P-K 交替、防捷径过滤、信息隐藏等约束的有效性）。
    *   针对 **HaPO 算法**做了 10 组消融（验证了步级奖励、平滑门控、掩码机制等的贡献）。
*   **客观性**：实验对比了从 7B 到 32B 不同规模的模型，并使用了 GPT-4o 作为公正的第三方评判器（LLM-as-judge），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **性能领先**：SearchEyes-27B 在所有开源模型中达到 SOTA，平均分比最强基线高出 6.2 分，甚至在部分指标上逼近 Gemini-3.1-Pro 等顶级闭源模型。
*   **参数效率**：SearchEyes-9B 的表现优于许多 30B 规模的模型（如 OpenSearch-VL-30B），证明了高质量合成数据和步级 RL 的高效性。
*   **多跳能力**：在 VisSearch Bench 上，SearchEyes 的表现远超 GPT-5 和 Kimi-K2.5，证明其真正掌握了跨模态的多步推理逻辑，而非简单的模式匹配。

### 7. 优点
*   **闭环模拟**：通过模拟搜索世界解决了外部环境不可控的问题，为智能体训练提供了一个稳定、可复现的“练兵场”。
*   **奖励创新**：HaPO 巧妙地利用了合成数据自带的元数据作为奖励锚点，规避了训练 PRM 的高昂成本和不稳定性。
*   **数据质量高**：PKC 流程通过复杂的图约束确保了数据的推理深度，有效防止了模型学习到“走捷径”的投机行为。

### 8. 不足与局限
*   **知识库局限性**：模拟世界基于 Wikipedia 和 Wikidata，虽然规模大，但仍无法完全覆盖实时互联网的所有动态信息和长尾知识。
*   **领域偏差**：在模拟环境中训练的智能体，在迁移到真实、杂乱的网页环境时，可能仍需一定的适应过程（尽管实验显示其泛化能力尚可）。
*   **算力门槛**：RL 阶段动用 64 台 H20 GPU，对于小型研究团队来说，复现成本依然较高。

（完）
