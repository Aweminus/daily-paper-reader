---
title: "CompactionRL: Reinforcement Learning with Context Compaction for Long-Horizon Agents"
title_zh: CompactionRL：结合上下文压缩的长程智能体强化学习
authors: "Yujiang Li, Zhenyu Hou, Yi Jing, Jie Tang, Yuxiao Dong"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05378v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 非机器人领域的长程智能体大模型强化学习
tldr: 针对长程智能体大模型受限于上下文窗口的问题，本文提出CompactionRL。该方法通过强化学习策略，将上下文压缩（总结先前交互状态）与任务执行联合优化。利用令牌级损失归一化和跨轨迹广义优势估计（GAE），使模型能从压缩的长程轨迹中学习。实验表明，该方法显著提升了GLM系列模型在软件工程和终端任务上的表现，并已应用于GLM-5.2的训练。
selection_source: fresh_fetch
motivation: 长程智能体在处理复杂任务时，交互轨迹容易超出有限的上下文窗口，限制了其持续工作的能力。
method: 提出一种联合优化任务执行与摘要生成的强化学习策略，结合令牌级损失归一化和跨轨迹广义优势估计。
result: "在SWE-bench Verified和Terminal-Bench 2.0上，CompactionRL使GLM-4.5-Air和GLM-4.7-Flash的Pass@1得分分别提升了最高7.0和6.8个百分点。"
conclusion: CompactionRL有效解决了长程智能体的上下文限制问题，通过压缩机制显著增强了模型在复杂编码和终端任务中的性能。
---

## 摘要
长程智能体大语言模型（LLMs）日益受到有限上下文窗口的限制，因为在任务完成之前，延长的交互轨迹可能会超过最大上下文长度。上下文压缩通过总结之前的交互状态并在压缩后的上下文中继续推演，提供了一种自然的解决方案，但将压缩整合到强化学习中仍未得到充分探索。我们提出了 CompactionRL，这是一种通过上下文压缩来训练长程智能体 LLM 的强化学习策略。我们的方法通过 Token 级损失归一化和跨轨迹广义优势估计（GAE），联合优化任务执行和摘要生成。这一设计使 LLM 智能体能够从压缩的长程轨迹中学习。我们在开源模型的基础上训练了 CompactionRL，并在智能体编程任务上观察到了持续的性能提升。CompactionRL 使开源 GLM-4.5-Air 模型（106B-A30B）在 SWE-bench Verified 上达到 66.8% 的 Pass@1 分数，在 Terminal-Bench 2.0 上达到 24.5%，分别获得了 7.0 和 3.1 个百分点的绝对提升。基于 GLM-4.7-Flash (30B-A3B)，CompactionRL 将 Pass@1 分数分别提高了 5.5 和 6.8 个百分点，在 SWE-bench Verified 和 Terminal-Bench 2.0 上分别达到 56.0% 和 20.2%。因此，CompactionRL 已被部署到用于训练开源 GLM-5.2 模型（750B-A40B）的强化学习流水线中。

## Abstract
Long-horizon agentic LLMs are increasingly limited by finite context windows, as extended interaction trajectories can exceed the maximum context length before a task is completed. Context compaction offers a natural solution by summarizing previous interaction states and continuing the rollout under a compressed context, but incorporating compaction into reinforcement learning remains underexplored. We propose CompactionRL, a reinforcement learning strategy to train long-horizon agentic LLMs with context compaction. Our approach jointly optimizes task execution and summary generation with token-level loss normalization and cross-trajectory generalized advantage estimation. This design enables the LLM agents to learn from compacted long-horizon trajectories. We train CompactionRL on top of open models and observe consistent performance gains on agentic coding tasks. CompactionRL enables the open GLM-4.5-Air model (106B-A30B) to achieve Pass@1 scores of 66.8% on SWE-bench Verified and 24.5% on Terminal-Bench 2.0, with absolute gains of 7.0 and 3.1 points, respectively. Built upon GLM-4.7-Flash (30B-A3B), CompactionRL improves Pass@1 by 5.5 and 6.8 points, reaching 56.0% on SWE-bench Verified and 20.2% on Terminal-Bench 2.0, respectively. CompactionRL is thus deployed in the RL pipeline for training the open GLM-5.2 model (750B-A40B).

---

## 论文详细总结（自动生成）

这是一份关于论文《CompactionRL: Reinforcement Learning with Context Compaction for Long-Horizon Agents》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：长程智能体（Long-horizon Agents）在处理复杂任务（如软件工程、终端操作）时，交互轨迹（推理、工具调用、环境反馈）极易超出大语言模型（LLM）有限的**上下文窗口**。
*   **研究动机**：虽然增加模型原生上下文长度是一种手段，但其计算成本高昂且存在“信息迷失”问题。**上下文压缩（Context Compaction）**（即总结历史并开启新轨迹）是自然的选择，但现有的强化学习（RL）框架难以有效训练这种“压缩-执行”的联合策略，导致压缩后的摘要往往丢失关键细节，影响后续任务成功率。

### 2. 论文提出的方法论：CompactionRL
*   **核心思想**：将上下文压缩视为智能体的一种**可学习技能**，通过 PPO 强化学习框架，将“任务执行动作”与“摘要生成动作”置于统一的奖励机制下进行联合优化。
*   **关键技术细节**：
    *   **触发机制**：当剩余上下文低于阈值 $T_{comp}$ 时，触发摘要生成。
    *   **上下文重构**：生成摘要 $S_t$ 后，清空历史，仅保留系统提示、摘要内容以及最近的 $k$ 步原始交互，从而释放上下文空间。
    *   **Token 级损失归一化（Token-level Loss Normalization）**：由于压缩会导致轨迹被切分为数量不等、长度不一的分段，传统的序列级平均会导致样本权重失衡。该方法通过对所有生成的 Token 进行统一归一化，消除分段数量带来的偏差。
    *   **跨轨迹广义优势估计（Cross-trajectory GAE）**：为了解决分段优化带来的信用分配（Credit Assignment）问题，该方法引入了轨迹位置修正，根据后续分段的 Token 数量对当前分段的优势值进行折扣修正，确保早期动作能正确感知到距离最终奖励的真实“时间距离”。

### 3. 实验设计
*   **数据集/场景**：
    *   **SWE-bench Verified**：软件工程基准测试，要求智能体解决真实的 GitHub Issue。
    *   **Terminal-Bench 2.0**：终端环境下的复杂问题解决任务。
*   **Benchmark 与对比方法**：
    *   **Base 模型**：GLM-4.7-Flash (30B-A3B) 和 GLM-4.5-Air (106B-A30B)。
    *   **对比策略**：包括原始模型（无压缩）、推理时启发式压缩（不参与训练）、以及标准 PPO（不带压缩感知的训练）。
    *   **外部参考**：对比了 GPT-5 mini、Qwen3 系列等公开报告的性能。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号（如 A100/H100）或具体数量。
*   **软件框架**：提到了使用开源异步强化学习框架 `slime` 进行训练。
*   **训练配置**：全局 Batch Size 为 128，学习率在 $2\times10^{-6}$ 左右，针对不同规模模型设置了 64k 或 80k 的上下文预算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流长程智能体榜单上进行了完整测试。
    *   **消融实验**：针对“摘要训练的作用”、“损失归一化”以及“跨轨迹 GAE”分别进行了对比实验。
    *   **分析实验**：分析了训练过程中摘要长度的变化、推理 Token 的增长以及策略熵的变化。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了最终得分，还深入探讨了摘要质量对任务成功率的量化影响，并验证了该方法在不同参数规模（30B 到 106B）模型上的泛化性。

### 6. 论文的主要结论与发现
*   **性能提升**：CompactionRL 在 SWE-bench Verified 上使 GLM-4.5-Air 提升了 **7.0%**，在 Terminal-Bench 2.0 上提升了 **3.1%**。
*   **摘要质量是关键**：实验证明，仅仅在推理时进行压缩效果有限，通过 RL 训练出的摘要器能保留更多“对后续任务有用”的细节（如文件路径、错误信息），而非简单的文本概括。
*   **训练动态**：经过 CompactionRL 训练的模型，生成的摘要长度会逐渐增加，且每步推理的 Token 数也会增长，表明模型学会了利用压缩释放的空间进行更深度的思考。

### 7. 优点（亮点）
*   **联合优化**：将摘要生成从“推理插件”提升为“核心能力”，解决了长程任务中信息丢失的痛点。
*   **算法修正**：针对压缩轨迹设计的 Token 级归一化和 GAE 修正，解决了 RL 在处理变长分段数据时的数学一致性问题。
*   **实用性强**：该方法已成功应用于 GLM-5.2 (750B) 的实际训练流水线中，证明了其在大规模生产环境下的可靠性。

### 8. 不足与局限
*   **训练-测试不匹配**：CompactionRL 强化的模型在“不开启压缩”的单窗口模式下表现可能不如标准 RL，说明模型产生了一定的压缩依赖。
*   **信用分配近似**：跨轨迹 GAE 虽然做了修正，但本质上仍是对全轨迹信用分配的一种近似，可能无法完美捕捉跨越多次压缩的长程因果关系。
*   **领域局限**：实验主要集中在编码和终端任务，对于需要极长程语义记忆（如长篇小说创作或法律文档分析）的非智能体任务，其效果尚待验证。

（完）
