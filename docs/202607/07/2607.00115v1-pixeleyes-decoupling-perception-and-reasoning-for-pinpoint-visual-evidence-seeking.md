---
title: "PixelEyes: Decoupling Perception and Reasoning for Pinpoint Visual Evidence Seeking"
title_zh: PixelEyes：解耦感知与推理以实现精准视觉证据搜寻
authors: "Dengxian Gong, Yuanzheng Wu, Haobo Yuan, Zhengdong Hu, Tao Zhang, Yikang Zhou, Shihao Chen, Quanzhu Niu, Kai Wang, Jason Li, Haochen Wang, Lu Qi, Shunping Ji, Ming-Hsuan Yang"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00115v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 解耦推理与感知的多轮视觉推理智能体
tldr: 针对多轮视觉推理中多模态大模型（MLLM）因推理与感知耦合导致定位不准及路径冗余的问题，本文提出PixelEyes代理。该方法通过解耦推理与感知，利用指代分割模型实现精确掩码定位，并引入语义区域广度优先搜索（BFS）优化探索逻辑。通过构建PixelEyes-6K数据集和Pinpoint-Bench基准，该研究显著提升了模型在复杂视觉证据寻找任务中的效率与精度。
selection_source: fresh_fetch
motivation: 现有模型将推理与定位任务交织，导致定位偏差引发无效的重复推理循环。
method: 采用解耦架构，由推理器确定目标并由专门的感知工具进行掩码级定位，同时结合BFS策略进行区域探索。
result: 发布了包含专家轨迹的PixelEyes-6K数据集，以及用于细粒度分析定位与推理失败的Pinpoint-Bench基准。
conclusion: PixelEyes通过显式解耦感知与推理，有效解决了视觉搜索中的冗余路径问题，为提升MLLM的视觉推理能力提供了新方案。
---

## 摘要
本文探讨了多轮视觉推理，并观察到多模态大语言模型（MLLMs）在定位目标时经常失败，导致生成冗长且冗余的轨迹。我们将这一失败归因于单个模型中推理与感知的纠缠：MLLM 同时进行推理和定位，而不准确的定位会触发额外的推理轮次，从而使轨迹膨胀。为解决这一问题，我们提出了 PixelEyes，这是一个显式解耦推理与感知的多轮视觉推理智能体，即推理器决定寻找什么，而专门的感知工具回答它在哪里。具体而言，PixelEyes 引入了：1）掩码引导的视觉搜索。调用指代分割模型以提供掩码级的精确定位，使推理器无需为不精确的定位进行补偿。2）语义区域广度优先搜索（BFS）。为了消除因重复裁剪错误子区域而导致的冗余循环，我们将探索组织为针对语义区域的广度优先搜索。为了内化这些能力，我们通过重新合成现有数据中的专家轨迹构建了 PixelEyes-6K 数据集。这显式地将我们的掩码引导搜索和 BFS 逻辑嵌入到模型中。我们进一步引入了 Pinpoint-Bench，这是一个零提示视觉搜索基准，即问题中不提供位置线索，并配有实例级掩码和边界框，可将定位失败与推理失败区分开来，从而能够对诸如“非注意盲视”等失败模式进行细粒度分析。最近的最先进 MLLMs 和视觉推理智能体在 Pinpoint-Bench 上仍有很大的提升空间，证明了该基准的质量和难度。代码和模型已开源。

## Abstract
This paper explores multi-turn visual reasoning and observes that MLLMs repeatedly fail to localize the target, leading to long, redundant trajectories. We attribute this failure to the entanglement of reasoning and perception within a single model, the MLLM reasons and localizes simultaneously, and inaccurate localization triggers additional reasoning turns that bloat the trajectory. To solve this problem, we propose PixelEyes, a multi-turn visual reasoning agent that explicitly decouples reasoning from perception, i.e., the reasoner decides what to look for, while a specialized perception tool answers where it is. Specifically, PixelEyes introduces 1) Mask-guided Visual Search. A referring segmentation model is invoked to provide mask-precise localization, freeing the reasoner from the need to compensate for imprecise grounding. 2) Semantic-region Breadth-first Search (BFS). To eliminate redundant loops caused by repeatedly cropping incorrect sub-regions, we organize exploration as a breadth-first search over semantic regions. To internalize these capabilities, we construct the PixelEyes-6K dataset by resynthesizing expert trajectories from existing data. This explicitly embeds our mask-guided search and BFS logic into the model. We further introduce Pinpoint-Bench, a zero-hint visual search benchmark, i.e., no location cues are provided in the question, with instance-level masks and bounding boxes that separate localization failures from reasoning failures, enabling fine-grained analysis of failure modes such as inattentional blindness. Recent state-of-the-art MLLMs and visual reasoning agents leave large headroom on Pinpoint-Bench, demonstrating its quality and difficulty. Code and models are open-sourced.

---

## 论文详细总结（自动生成）

以下是对论文《PixelEyes: Decoupling Perception and Reasoning for Pinpoint Visual Evidence Seeking》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在复杂的多轮视觉推理任务中，多模态大模型（MLLM）需要通过多次观察和裁剪图像局部来寻找证据。
*   **核心问题**：现有模型通常将“推理”（决定看什么）与“感知”（定位在哪里）耦合在一起。这种耦合导致了两个主要问题：
    1.  **定位不准**：模型在描述目标位置时精度不足，导致后续推理基于错误的视觉信息。
    2.  **路径冗余**：由于缺乏系统的探索策略，模型经常陷入重复裁剪同一区域或无效区域的死循环，导致推理轨迹冗长且低效。
*   **整体含义**：本文旨在通过“解耦”架构，让推理器专注于逻辑决策，让专门的感知工具负责精准定位，从而提升视觉搜索的效率和准确性。

### 2. 论文提出的方法论
PixelEyes 智能体采用了**感知与推理显式解耦**的架构，核心技术包括：
*   **推理与感知解耦**：推理器（基于大语言模型）仅负责生成搜索目标的文本描述；感知工具（指代分割模型，如 LISA 或类似模型）接收描述并输出精确的**实例级掩码（Mask）**。
*   **掩码引导的视觉搜索（Mask-guided Search）**：不同于传统的边界框（Bounding Box），掩码能提供像素级的定位，减少背景噪声干扰，使推理器能更清晰地看到目标。
*   **语义区域广度优先搜索（BFS）**：为了避免冗余探索，PixelEyes 将图像探索逻辑建模为基于语义区域的 BFS。它会系统地记录已探索区域，并优先覆盖未探索的语义块，确保搜索过程不重不漏。
*   **PixelEyes-6K 数据集**：研究者通过重新合成现有视觉数据，构建了包含 6,000 条专家轨迹的数据集。这些轨迹显式地包含了 BFS 探索逻辑和掩码定位指令，用于微调模型以使其内化这种解耦推理能力。

### 3. 实验设计
*   **数据集与基准**：
    *   **训练集**：PixelEyes-6K（自建，包含专家搜索轨迹）。
    *   **测试基准（Pinpoint-Bench）**：本文新提出的零提示（Zero-hint）视觉搜索基准。该基准不提供任何位置线索，要求模型在复杂场景中寻找微小证据，并配有实例级掩码和框，用于区分“定位失败”和“推理失败”。
*   **对比方法**：
    *   **闭源 SOTA 模型**：GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro。
    *   **开源视觉推理智能体**：如 InternLM-XComposer2.5-7B 等。
*   **评估指标**：成功率（SR）、平均轮次（Average Turns，衡量效率）、定位精度等。

### 4. 资源与算力
*   **算力说明**：论文摘要及核心片段中**未明确说明**具体的 GPU 型号、数量及训练总时长。通常此类 7B 规模模型的微调需要 8 张 A100 或 H100 级别的显卡进行数天的训练，但具体数值需参考论文的实验设置章节（Appendix 或 Implementation Details）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   构建了 6,000 条高质量专家轨迹进行微调。
    *   在 Pinpoint-Bench 上对多种主流模型进行了详尽的对比测试。
    *   **消融实验**：验证了解耦架构、掩码引导以及 BFS 策略各自对性能的贡献。
*   **充分性评价**：实验设计较为充分。通过引入 Pinpoint-Bench，研究者不仅对比了最终结果，还深入分析了模型为何失败（如“非注意盲视”问题），这种细粒度的分析增强了实验的客观性和说服力。

### 6. 论文的主要结论与发现
*   **解耦的优越性**：显式解耦感知与推理能显著减少推理轨迹的冗余，提高搜索成功率。
*   **掩码的重要性**：相比边界框，掩码定位能显著降低推理器在处理复杂背景时的认知负担。
*   **SOTA 仍有差距**：即使是 GPT-4o 等顶级模型，在没有任何位置提示的 Pinpoint-Bench 任务中仍表现出明显的定位困难，证明了该任务的挑战性。
*   **非注意盲视**：实验发现模型往往“看到了”目标（在视觉编码器中存在特征），但“没意识到”那是目标，这进一步证明了推理与感知协同的重要性。

### 7. 优点
*   **架构创新**：通过 BFS 引入了系统性的探索逻辑，解决了视觉智能体常见的“原地打转”问题。
*   **精度提升**：引入指代分割工具实现像素级定位，是视觉搜索从“粗放”向“精细”转变的重要一步。
*   **高质量基准**：Pinpoint-Bench 填补了细粒度视觉证据搜寻评估的空白，具有较高的学术价值。

### 8. 不足与局限
*   **工具依赖性**：PixelEyes 的性能高度依赖于底层指代分割模型的准确性。如果感知工具识别错误，推理器很难纠正。
*   **推理延迟**：多轮调用感知工具和 BFS 逻辑虽然提高了准确性，但相比单次推理模型，其计算开销和响应延迟会更高。
*   **场景覆盖**：虽然在视觉搜索上表现优异，但在需要极强常识推理（而非视觉发现）的任务中，其优势可能不明显。

（完）
