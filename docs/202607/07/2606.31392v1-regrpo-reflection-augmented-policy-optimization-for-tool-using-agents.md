---
title: "ReGRPO: Reflection-Augmented Policy Optimization for Tool-Using Agents"
title_zh: ReGRPO：面向工具使用智能体的反思增强策略优化
authors: "Binjie Zhang, Mike Zheng Shou"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31392v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 工具使用型视觉语言智能体的反射增强策略优化
tldr: 针对工具增强型视觉语言模型在多步任务中易出错且难以自我修复的问题，本文提出ReGRPO框架。该框架通过构建包含错误类型、证据和修复计划的“反思链”（RoT）数据进行预热微调，并结合改进的组相对策略优化（GRPO）算法，使智能体学会在工具调用失败时进行反思与纠错。实验证明，ReGRPO在GTA和GAIA基准测试中显著提升了开源控制器的性能。
selection_source: fresh_fetch
motivation: 现有的监督微调缺乏失败恢复信号，而强化学习的稀疏奖励难以指导具体的步骤修复，导致工具使用智能体鲁棒性不足。
method: 提出一种结构化反思数据引擎构建RoT三元组，并利用带反思成本约束的组相对策略优化（GRPO）联合优化反思与纠错动作。
result: 在GTA和GAIA数据集上的实验结果显示，ReGRPO在相同底座下一致优于强力开源基线，达到了领先的控制水平。
conclusion: ReGRPO通过引入反思机制和优化的强化学习策略，有效增强了视觉语言模型在复杂工具调用场景下的容错与自我修复能力。
---

## 摘要
工具增强的视觉语言模型（VLM）可以通过调用外部工具来解决多模态、多步骤任务，但在实践中仍然表现脆弱。现有工作存在两个共同的差距：监督微调（SFT）主要基于成功的轨迹构建，在工具失败后几乎无法提供恢复信号；而稀疏的轨迹级强化学习（RL）奖励对于哪一步失败以及如何修复提供的指导有限。我们引入了 ReGRPO（反思增强的分组相对策略优化），这是一个在工具使用智能体中学习反思引导纠错的框架。ReGRPO 从一个结构化的反思数据引擎开始：我们执行“差一点成功”的动作以收集有根据的失败观察，然后构建反思链（Reflection-of-Thought）三元组（错误类型、证据、修复计划），并将其与纠正后的动作配对，用于热启动 SFT。随后，我们利用分组相对优势在局部轨迹内联合优化反思标记和纠正动作，并加入反思成本项以减少不必要的反思。在 GTA 和 GAIA 上的实验表明，在相同的骨干网络和工具集下，ReGRPO 始终优于强大的开源基线，并在对比的开源控制器中取得了最佳结果。代码和 RoT 数据可在 https://github.com/showlab/ReGRPO 获取。

## Abstract
Tool-augmented vision-language models (VLMs) can solve multimodal, multi-step tasks by calling external tools, yet they remain fragile in practice. Existing works have two common gaps. Supervised fine-tuning (SFT) is built mostly on successful trajectories and offers little signal for recovery after tool failures, while sparse trajectory-level RL rewards provide limited guidance on which step failed and how to repair it. We introduce ReGRPO (Reflection-augmented Group Relative Policy Optimization), a framework that learns reflection-guided correction in tool-using agents. ReGRPO starts with a structured reflective data engine: we execute near-miss actions to collect grounded failure observations, then build Reflection-of-Thought triplets (ErrorType, Evidence, FixPlan) paired with corrected actions for warm-start SFT. We then optimize reflection tokens and corrective actions jointly within local trajectories using group-relative advantages, and include a reflection-cost term to reduce unnecessary reflection. Experiments on GTA and GAIA show that, under the same backbone and tool suite, ReGRPO consistently outperforms strong open-source baselines and achieves the best results among the compared open-source controllers. Code and RoT data are available at https://github.com/showlab/ReGRPO.

---

## 论文详细总结（自动生成）

### 论文总结：ReGRPO —— 面向工具使用智能体的反思增强策略优化

#### 1. 核心问题与整体含义（研究动机和背景）
工具增强型视觉语言模型（VLM）在处理多模态、多步骤任务时表现脆弱。现有的训练方法存在两个主要缺陷：
*   **监督微调（SFT）的局限性：** 训练数据大多仅包含成功的轨迹，当模型在推理中出现微小偏差（如 OCR 识别错误或坐标偏移）时，缺乏如何恢复和纠错的信号。
*   **强化学习（RL）的局限性：** 传统的 RL 采用稀疏的轨迹级奖励（成功或失败），难以指导模型识别具体是哪一步出错以及如何修复。
**研究动机：** 赋予智能体“自我反思”和“局部纠错”的能力，使其在工具调用失败时能够根据观察到的证据进行诊断并重新规划。

#### 2. 方法论：核心思想与技术细节
ReGRPO 框架包含三个关键组件：
*   **结构化反思数据引擎（Structured Reflective Data Engine）：**
    *   **失败诱导：** 对成功的工具调用动作进行微扰（如移动边界框、更改工具参数），执行后获取真实的失败观察（如 API 报错、空 OCR 结果）。
    *   **RoT 三元组生成：** 利用教师模型（GPT-4o）生成“反思链”（Reflection-of-Thought），包含：**ErrorType**（错误类型，如参数无效、接地漂移）、**Evidence**（证据，引用具体的失败观察）、**FixPlan**（修复计划，具体的纠错策略）。
*   **反思增强的组相对策略优化（ReGRPO Training）：**
    *   **联合优化：** 将反思 Token 和纠正动作 Token 纳入同一个局部轨迹目标进行优化。
    *   **奖励函数：** $R(\tau) = \lambda_{exec} \cdot \mathbb{1}\{success\} - \eta \cdot C(\tau) + \lambda_{val} \cdot V$。其中 $C(\tau)$ 是反思成本项（惩罚过长的反思），$V$ 是可选的训练期验证器分数。
    *   **算法：** 采用 GRPO 算法，通过组内相对优势（Advantage）来缩减方差，使模型学习哪些反思是有助于成功的。
*   **零验证器推理阶段（Zero-Verifier Inference）：**
    *   **确定性触发器：** 推理时不调用昂贵的外部验证器，而是根据工具报错、空观察或策略置信度（低于动态阈值）自动触发反思块。

#### 3. 实验设计
*   **数据集/场景：**
    *   **GTA：** 侧重视觉感知、局部操作（如读收据、图表）和短推理链。
    *   **GAIA：** 侧重文档理解（PDF、PPTX、XLSX）、网页导航和逻辑推理。
*   **Benchmark：** 采用 AnsAcc（答案准确率）、ToolAcc（工具调用有效性）和 CodeExec（执行成功率）。
*   **对比方法：**
    *   **闭源模型：** GPT-4o, GPT-4o-mini。
    *   **开源模型：** LLaVA-NeXT-8B, InternVL2-8B, MiniCPM-V-8.5B, Qwen2-VL-7B。
    *   **专门智能体框架：** MAT-AGENT, SPORT。

#### 4. 资源与算力
*   **模型底座：** Qwen2-VL-7B。
*   **训练细节：** 使用 LoRA（rank 32）微调，AdamW 优化器，学习率 $1.0 \times 10^{-6}$，每个设备 batch size 为 2。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量及总训练时长，但提到了在 RL 阶段采用了离线组和在线自探索组的混合训练。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 GTA 和 GAIA 两个主流基准进行了全面测试，并进行了详尽的消融实验（包括 SFT 数据对比、不同 RL 算法对比、反思形式对比等）。
*   **公平性：** 所有消融实验均在相同的 Qwen2-VL-7B 骨干网络和工具集下进行，确保了方法论改进（反思机制）是性能提升的主因。
*   **客观性：** 引入了多种子统计和 VQA 能力保持测试（MMBench），证明了强化学习未损害模型的基础视觉能力。

#### 6. 主要结论与发现
*   **性能领先：** 在相同底座下，ReGRPO 在 GTA 和 GAIA 上均取得了开源控制器中的最佳结果（GTA AnsAcc 67.66%，GAIA AnsAcc 23.35%）。
*   **反思的价值：** 结构化的反思（RoT）比自由格式的反思更有效；即使不使用训练期的验证器奖励（$\lambda_{val}=0$），仅靠执行成功奖励和反思成本约束也能取得显著提升。
*   **纠错能力：** ReGRPO 能够有效识别“静默失败”（如工具返回了结果但不是目标对象），并通过反思重新定位目标。

#### 7. 优点
*   **闭环纠错：** 填补了工具使用智能体在“失败恢复”方面的空白，将反思从简单的提示词工程提升为可学习的策略变量。
*   **推理高效：** 采用单路径推理和确定性触发机制，无需在推理时多次调用大模型进行验证，降低了延迟和成本。
*   **数据驱动：** 提出的结构化反思数据引擎能够自动将原始失败转化为高质量的监督信号。

#### 8. 不足与局限
*   **错误分类局限：** 目前仅定义了四种错误类型（参数无效、工具不匹配、信息不足、接地漂移），可能无法覆盖所有复杂的现实失败场景。
*   **教师模型依赖：** 初始 RoT 数据的质量高度依赖于 GPT-4o 等强力教师模型。
*   **单次纠错限制：** 推理时每步最多允许一次反思纠错，对于需要多次尝试的极复杂任务可能仍显不足。

（完）
