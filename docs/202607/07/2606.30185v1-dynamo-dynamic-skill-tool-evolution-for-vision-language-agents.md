---
title: "Dynamo: Dynamic Skill-Tool Evolution for Vision-Language Agents"
title_zh: Dynamo：视觉-语言智能体的动态技能-工具演化
authors: "Yutao Sun, Yanting Miao, Hao-Xuan Ma, Mengyu Zhou, Mingshuai Chen, Tiancheng Zhao, Dexin Wang, Lei Lv, Li Xu, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30185v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言智能体的动态技能工具演化
tldr: Dynamo是一个无需训练的视觉语言模型（VLM）增强框架。它通过分析少量标注数据中的成功与失败案例，动态演化出可复用的推理技能和可执行的视觉工具，并将其存储在持久化库中。该方法在不更新模型权重的情况下，显著提升了多种VLM在视觉推理任务上的表现，缩小了与强化学习方法的差距，且计算成本更低。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型提升方法通常依赖于昂贵的重新训练或繁琐的人工提示词与工具设计。
method: 通过自我反思生成针对认知瓶颈的推理技能和针对感知瓶颈的视觉工具，并构建持久化能力库供推理时调用。
result: "在4个基准测试和5个模型上平均提升了5.6%的准确率，并能以极低的计算成本达到接近强化学习方法的性能。"
conclusion: Dynamo证明了通过动态演化技能和工具库，可以在不改变模型权重的前提下有效增强视觉语言代理的推理能力。
---

## 摘要
提升视觉-语言模型（VLM）在视觉推理上的表现通常需要重新训练或手工设计的提示词与工具。我们提出了 Dynamo，这是一个无需训练的框架，可以在不更新任何权重的情况下适配冻结的 VLM。在一个较小的有标签训练子集上，智能体检查其自身的正确与错误尝试，并演化出两种互补的能力：针对认知瓶颈的可重用推理技能，以及针对感知瓶颈的可执行视觉工具。每个生成的工具都与一项指定何时调用它的技能配对，且这两种能力类型都会累积在一个持久化库中。在四个视觉推理基准测试和五个 VLM 骨干网络上，Dynamo 在所有 20 个模型-基准设置中均提升了直接推理性能（平均准确率提升 5.6）。当预先给定工具集时，该框架会学习何时调用每个工具，且每步工具选择在每个测试的骨干网络上都有所改进。与特定任务的强化学习（RL）（如 VTool-R1、DeepEyes）相比，Dynamo 以极小的计算开销弥补了 65-99% 的 RL 性能差距，并在可用时与 RL 产生叠加效应。

## Abstract
Improving vision-language models (VLMs) on visual reasoning typically requires retraining or hand-designed prompts and tools. We present Dynamo, a training-free framework that adapts a frozen VLM without any weight updates. On a small labeled training subset, the agent inspects its own correct and incorrect attempts and evolves two complementary capabilities: reusable reasoning skills for cognitive bottlenecks, and executable visual tools for perceptual ones. Each generated tool is paired with a skill that specifies when to invoke it, and both capability types accumulate in a persistent library. Across four visual reasoning benchmarks and five VLM backbones, Dynamo improves direct inference on all 20 model--benchmark settings (avg. +5.6 acc). When the tool set is given in advance, the framework learns when to call each tool, and per-step tool choice improves on every tested backbone. Against task-specific RL (VTool-R1, DeepEyes), Dynamo closes 65--99% of the RL gap at a fraction of the compute, and combines additively with RL when available.

---

## 论文详细总结（自动生成）

### 论文总结：Dynamo - 视觉-语言智能体的动态技能-工具演化

#### 1. 核心问题与整体含义
现有的视觉-语言模型（VLM）在处理复杂的视觉推理任务时，通常依赖于昂贵的人工标注数据进行微调（SFT）或复杂的强化学习（RL）流程。论文的核心问题是：**能否让冻结的 VLM 智能体通过观察自身在少量标注数据上的表现，自主构建一套针对特定任务的“能力库”？** 
研究背景在于，VLM 往往面临两类瓶颈：一是**认知瓶颈**（看得见但想不对），二是**感知瓶颈**（看不清或证据不明显）。Dynamo 旨在通过演化“推理技能”和“视觉工具”来分别解决这两类问题，实现无需权重更新的自我进化。

#### 2. 方法论
Dynamo 提出了一个**无需训练（Training-free）**的演化框架，其核心思想是构建一个持久化的能力库 $\mathcal{C} = (\mathcal{S}, \mathcal{T})$，包含技能（Skills）和工具（Tools）。

*   **演化循环（Evolution Loop）**：
    1.  **诊断（Diagnose）**：智能体分析在训练子集上的正确和错误尝试，识别瓶颈类型（认知型或感知型）。
    2.  **探索（Explore）**：生成器（Generator）根据诊断结果提出多个候选的“技能+工具”组合。
    3.  **验证与晋升（Validate & Promote）**：在训练集上评估候选方案，将能显著提升准确率且不产生负面回归的最优候选者存入持久库。
*   **关键技术细节**：
    *   **技能（Skills）**：结构化的 Markdown 标准作业程序（SOP），包含使用时机、策略步骤、常见陷阱和示例。
    *   **工具（Tools）**：短小的 Python 程序（如 OpenCV/PIL 脚本），用于图像裁剪、缩放、对比度调整或图表重绘。
    *   **掌控力（Mastery）**：每个工具都配有一项技能，规定了工具的适用边界，防止盲目调用。
    *   **在线适配**：设计了滚动窗口机制，能够检测输入分布的变化并动态更新能力库。

#### 3. 实验设计
*   **数据集/场景**：
    *   **视觉推理**：ChartQA（图表）、MathVista（数学）、HRBench4K（高分辨率感知）、V*（视觉搜索）。
    *   **工具掌控**：GTA 基准测试。
    *   **RL 对比**：VTool-R1（结构化 QA）和 DeepEyes（高分辨率感知）协议。
*   **骨干网络（Backbones）**：GPT-4o, o4-mini, GPT-5.4, Doubao-Seed-2.0, Qwen3.5-27B, 以及 Qwen2.5-VL (3B/7B)。
*   **对比方法**：Zero-shot 基准、仅技能（Skill Only）、仅工具（Tool Only）、以及特定任务的强化学习方法（VTool-R1, DeepEyes）。

#### 4. 资源与算力
*   **算力消耗**：论文明确指出该方法**无需任何权重更新（$\nabla\theta = 0$）**，因此不需要 GPU 训练算力。
*   **推理开销**：主要的计算成本来自于调用冻结 VLM 的 API。演化过程仅需 10% 的训练集，运行 3 轮迭代。每项任务的总 API 调用量在几千次左右，远低于 RL 所需的数万次 Rollouts。
*   **具体型号**：文中未提及运行推理的具体 GPU 型号，因为其核心贡献在于算法框架而非底层算力实现。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个模型在 4 个基准上的 20 组设置，平均准确率提升 5.6%。
*   **消融实验**：对比了“仅技能”、“仅工具”和“全功能”模式，验证了两者互补的必要性。
*   **RL 对比**：在多个任务上与 RL 方法进行了直接对比，证明了其能弥补 65-99% 的性能差距。
*   **充分性评价**：实验设计非常全面，跨越了不同参数规模的模型（从 3B 到 GPT 级别）和不同类型的视觉任务，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **普遍有效性**：Dynamo 在所有测试的模型和基准上均实现了性能提升。
*   **RL 替代/补充**：Dynamo 能以极低的成本达到接近 RL 的效果，且当 RL 可用时，两者可以叠加使用（Dynamo + RL 效果最优）。
*   **技能与工具的协同**：技能解决了推理逻辑问题，工具解决了视觉感知问题，两者的结合是处理复杂视觉任务的关键。
*   **在线适应性**：系统能有效检测并适应任务分布的漂移。

#### 7. 优点
*   **高效节能**：无需微调，极大地降低了适配新任务的门槛和成本。
*   **可解释性强**：生成的技能（SOP）和工具（Python 代码）是人类可读的，便于调试和理解。
*   **即插即用**：作为一个外挂框架，可以轻松应用于任何现有的 VLM。
*   **动态演化**：具备类似人类的学习机制，能从错误中总结经验并形成持久记忆。

#### 8. 不足与局限
*   **诊断依赖性**：演化质量高度依赖于骨干模型自身的反思和诊断能力。如果模型太弱，可能无法准确识别瓶颈。
*   **反馈信号需求**：演化过程需要准确的正确性信号（如 Ground-truth 标签或可靠的验证器），在完全无监督的环境下难以运行。
*   **模态限制**：目前主要集中在静态图像任务，尚未扩展到视频、3D 空间推理或医疗影像等更专业的领域。
*   **选择偏差**：在验证阶段可能存在对特定训练子集的过拟合风险（虽然实验显示泛化性良好）。

（完）
