---
title: "CritiqueDriveVLM: From Verifier-Guided Reinforcement Learning to Latent Thought Distillation for Autonomous Driving"
title_zh: CritiqueDriveVLM：从验证器引导的强化学习到面向自动驾驶的潜空间思维蒸馏
authors: "Zhaohong Liu, Hao Ye, Xianlin Zhang, Mengshi Qi"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04179v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 验证器引导的强化学习用于驾驶中的VLM推理
tldr: "针对自动驾驶中视觉语言模型（VLM）存在的推理幻觉和高延迟问题，本文提出CritiqueDriveVLM框架。该框架首先通过多维验证器引导的强化学习培养出具备强大逻辑推理能力的System-2教师模型，随后利用潜空间思维蒸馏技术将其推理能力压缩至轻量化的System-1学生模型中。实验表明，该方法在显著提升推理准确率的同时，将推理延迟降低了88%，实现了高可靠性与低延迟的平衡。"
selection_source: fresh_fetch
motivation: 传统的监督微调易导致推理幻觉，而思维链方法虽能提升逻辑性但推理延迟过高，难以满足自动驾驶的实时性需求。
method: 提出一种结合多维验证器引导的强化学习与潜空间思维蒸馏的三阶段框架，将复杂的逻辑推理能力内化并压缩。
result: "在DriveLMM-01基准上，教师模型将选择题准确率提升至76.54%，而蒸馏后的学生模型在保持竞争力的同时将推理延迟降低了88%。"
conclusion: 该研究成功实现了自动驾驶VLM从复杂推理到高效执行的转化，为低延迟、高可靠的自动驾驶系统提供了可行路径。
---

## 摘要
端到端视觉语言模型（VLM）在自动驾驶领域展现出巨大潜力。然而，标准的监督微调（SFT）通常面临推理幻觉和保守偏差的问题。虽然传统的工具增强框架和思维链（CoT）方法缓解了这些问题，但它们会导致极高的 Token 消耗和不可接受的延迟，使得实时部署变得不切实际。为了解决这种可靠性与效率之间的权衡，我们提出了 CritiqueDriveVLM，这是一个将推理能力直接内化到 VLM 中的新型统一三阶段框架。首先，我们引入了由多维验证器引导的批判驱动多轮强化学习（RL）。通过提供细粒度的标量反馈和多轮惩罚，我们迫使策略内化逻辑推演，从而培养出一个强大的 System-2 教师模型，在无需脆弱外部工具的情况下实现高精度。随后，我们提出了潜空间思维蒸馏以克服延迟瓶颈。通过将学生模型的潜空间表示与教师模型完全收敛的推理状态对齐，我们将深层逻辑能力压缩进一个快速、无需 CoT 的 System-1 学生模型中。在广泛使用的 DriveLMM-01 基准测试上进行的广泛实验证明了显著的改进。与基础模型相比，我们的无工具教师模型将多选题质量（MCQ）从 55.54% 显著提升至 76.54% 的当前最高水平。至关重要的是，我们蒸馏出的学生模型在保持竞争性推理深度的同时，大幅缩短了生成长度，平均仅为 28 个 Token。这将推理延迟降低了 88%（从 3482 毫秒降至 416 毫秒），为低延迟自动驾驶铺平了一条高度稳健的道路。我们的源代码可在 https://github.com/MICLAB-BUPT/CritiqueDriveVLM 获取。

## Abstract
End-to-end Vision-Language Models (VLMs) show immense potential in autonomous driving. However, standard Supervised Fine-Tuning (SFT) often suffers from reasoning hallucinations and conservative biases. While traditional tool-augmented frameworks and Chain-of-Thought (CoT) approaches mitigate these issues, they incur exorbitant token consumption and unacceptable latency, rendering real-time deployment impractical. To resolve this reliability-efficiency trade-off, we propose CritiqueDriveVLM, a novel unified three-stage framework internalizing reasoning directly into the VLM. First, we introduce Critique-Driven Multi-Turn Reinforcement Learning (RL) guided by a multi-dimensional verifier. By providing granular scalar feedback and a multi-turn penalty, we force the policy to internalize logical deduction, cultivating a robust System-2 Teacher that achieves high accuracy without fragile external tools. Subsequently, we propose Latent Thought Distillation to overcome the latency bottleneck. By aligning the Student's latent representations with the Teacher's fully converged reasoning states, we compress deep logical capabilities into a fast, CoT-free System-1 Student. Extensive experiments on the widely-used DriveLMM-01 benchmark demonstrate remarkable improvements. Compared to the base model, our tool-free Teacher significantly boosts Multiple Choice Quality (MCQ) from 55.54% to a state-of-the-art 76.54%. Crucially, our distilled Student preserves competitive reasoning depth while drastically minimizing generation length to an average of merely 28 tokens. This slashes inference latency by 88% (from 3482 ms to 416 ms), paving a highly robust pathway for low-latency autonomous driving.Our source code is available at https://github.com/MICLAB-BUPT/CritiqueDriveVLM.

---

## 论文详细总结（自动生成）

以下是对论文《CritiqueDriveVLM: From Verifier-Guided Reinforcement Learning to Latent Thought Distillation for Autonomous Driving》的结构化总结：

### 1. 核心问题与整体含义
论文针对自动驾驶领域中视觉语言模型（VLM）面临的**“可靠性-效率”权衡**问题。
*   **研究动机**：标准的监督微调（SFT）易导致模型产生推理幻觉和保守偏差；而现有的思维链（CoT）或工具增强方法虽然提升了逻辑性，但因生成 Token 过多或依赖外部 API，导致推理延迟极高（数秒级），无法满足自动驾驶的实时性需求。
*   **核心目标**：开发一个既具备深层逻辑推理能力（System-2），又能实现超低延迟执行（System-1）的端到端自动驾驶框架。

### 2. 方法论
论文提出了一个统一的三阶段框架，旨在将推理能力内化并压缩：
*   **阶段 1：热身 SFT 与验证器构建**：
    *   使用高质量 CoT 数据进行 SFT，规范输出格式（`<think>` 和 `<answer>` 标签）。
    *   构建一个**多维验证器**，从感知、逻辑、安全三个维度对模型输出进行评分，并生成针对性的自然语言批判（Critique）。
*   **阶段 2：批判驱动的多轮强化学习（RL）**：
    *   采用 **GRPO（群体相对策略优化）** 算法。
    *   **核心机制**：引入多轮交互反馈循环。如果模型首轮回答错误，验证器提供批判建议引导其修正。
    *   **奖励设计**：组合了格式奖励、准确性奖励、验证器标量奖励，以及**步进衰减多轮惩罚**（强制模型内化逻辑，减少对外部批判的依赖），从而培养出强大的“教师模型”。
*   **阶段 3：潜空间思维蒸馏（Latent Thought Distillation）**：
    *   **核心思想**：将教师模型的深层逻辑压缩到不输出 CoT 的学生模型中。
    *   **技术细节**：通过余弦相似度损失函数，将学生模型在预测答案时的隐藏状态（Hidden State）与教师模型推理结束时的隐藏状态进行对齐。这使得学生模型无需生成冗长的推理文本即可获得等同的逻辑深度。

### 3. 实验设计
*   **数据集与基准**：主要在 **DriveLMM-o1** 基准测试上进行，该基准基于 nuScenes 数据集，包含超过 18,000 个涉及复杂驾驶场景的 VQA 对。
*   **对比方法**：
    *   通用大模型：GPT-4o, InternVL2.5-8B, Qwen2.5/3-VL 系列。
    *   驾驶专用模型：DriveLMM-o1, AgentThink（工具增强型）, OmniDrive-R1（强化学习型）。
*   **评估指标**：综合推理得分、多选题质量（MCQ）、风险评估、交通规则遵守、场景感知等。

### 4. 资源与算力
*   **硬件环境**：使用了单台配备 **8 张 NVIDIA A100 GPU** 的服务器。
*   **训练细节**：
    *   基础模型为 Qwen3-VL-8B。
    *   每个阶段训练 2 个 Epoch。
    *   SFT 和蒸馏阶段使用 LoRA 技术。
    *   推理测试在单张 A100 GPU 上完成。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了全面的性能对比、消融实验和定性案例分析。
*   **充分性评价**：
    *   **消融实验**：详细验证了准确性奖励、验证器奖励、多轮交互及惩罚机制对最终性能的贡献，证明了各模块的必要性。
    *   **公平性**：在相同的硬件和数据集（DriveLMM-o1）下对比了当前最先进的开源和闭源模型，具有较高的客观性。
    *   **效率分析**：专门对比了有无 CoT 情况下的 Token 消耗与延迟，数据详实。

### 6. 主要结论与发现
*   **性能突破**：教师模型在 MCQ 指标上达到 **76.54%**，刷新了 SOTA 记录，且无需依赖外部感知工具。
*   **效率飞跃**：蒸馏后的学生模型在保持高精度的同时，平均生成长度仅 **28 Tokens**，推理延迟从 3482ms 降至 **416ms**（降低了 **88%**）。
*   **内化效应**：证明了通过潜空间对齐，可以将复杂的 System-2 推理能力成功转化为 System-1 的直觉反应。

### 7. 优点与亮点
*   **创新架构**：首次在自动驾驶领域结合了多轮强化学习与潜空间蒸馏，解决了“想得深”与“跑得快”的矛盾。
*   **摆脱工具依赖**：通过强化学习让模型内化感知能力，避免了传统方法中外部 API 故障导致的系统脆弱性。
*   **端到端优化**：潜空间蒸馏方法比传统的文本蒸馏更能保留复杂的空间和逻辑语义。

### 8. 不足与局限
*   **场景覆盖**：虽然在 nuScenes 基础上表现优异，但实验仍局限于离线数据集，缺乏在真实开放道路或高动态模拟器中的闭环测试。
*   **时序能力**：目前主要处理单帧或短序列图像，对于需要长期记忆和复杂时序逻辑的驾驶任务（如预测数秒后的交通演变）仍有提升空间。
*   **算力门槛**：尽管推理高效，但三阶段的训练过程（尤其是多轮 RL）对算力和高质量标注数据的需求较高。

（完）
