---
title: "RL Forgets! Towards Continual Policy Optimization"
title_zh: 强化学习也会遗忘！迈向持续策略优化
authors: "Mao-Lin Luo, Zhe-Xu Wang, Zi-Hao Zhou, Bo Ye, Jian Zhao, Min-Ling Zhang, Tong Wei"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04364v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于使视觉语言模型适应多模态推理的强化学习
tldr: 本研究挑战了强化学习在持续学习中不易遗忘的观点，通过新基准MRCL证实其仍存在严重遗忘问题。为此提出CPO框架，这是一种无需重放的持续策略优化方法，通过行为KL目标和参数移动正则化限制策略漂移。实验显示CPO在减少遗忘的同时提升了模型性能，为多模态模型持续适配提供了新路径。
selection_source: fresh_fetch
motivation: 验证强化学习在多模态模型持续学习中是否能避免灾难性遗忘，并解决现有基准过时的问题。
method: 提出无需重放的持续策略优化（CPO）框架，结合行为KL目标与参数移动正则化以抑制策略漂移。
result: "CPO在Qwen3-VL-8B上减少了13.7%的遗忘，并将预训练能力提升了7.0%。"
conclusion: 强化学习在持续训练中仍受遗忘困扰，而CPO通过理论证实的正则化方法有效平衡了新旧任务的学习。
---

## 摘要
持续后训练正成为将视觉语言模型适配到不断演进的任务中的核心范式。近期的研究越来越倾向于使用强化学习而非监督微调，这是基于一种信念，即强化学习本质上更不容易发生遗忘。然而，这一信念尚未得到充分验证，因为现有证据主要源自过时或同质化的基准测试。为了重新审视这一假设，我们引入了 MRCL，这是一个基于多样化且近期发布的多模态数据集构建的多模态推理持续学习基准。在 MRCL 上的实验表明，强化学习在持续后训练过程中仍可能遭受严重的灾难性遗忘。为应对这一挑战，我们提出了持续策略优化（CPO），这是一个基于前序任务行为 KL 目标的无重放框架。CPO 使用一种经过理论验证的参数移动正则化来限制在先前任务上的策略漂移。在多个模型规模上的广泛实验表明，CPO 在减少遗忘的同时，能够保持并在某些情况下提升预训练模型的能力。在 Qwen3-VL-8B 上，CPO 将遗忘减少了 13.7%，并将预训练能力提升了 7.0%。实现代码可在 https://github.com/MaolinLuo/CPO 获取。

## Abstract
Continual post-training is becoming a central paradigm for adapting vision-language models to evolving tasks. Recent work has increasingly favored reinforcement learning over supervised fine-tuning, driven by the belief that reinforcement learning is inherently less prone to forgetting. However, the belief remains insufficiently validated, as existing evidence is largely drawn from outdated or homogeneous benchmarks. To revisit this assumption, we introduce MRCL, a Multimodal Reasoning Continual Learning benchmark built from diverse and recently released multimodal datasets. Experiments on MRCL show that reinforcement learning can still suffer from severe catastrophic forgetting during continual post-training. To address this challenge, we propose Continual Policy Optimization (CPO), a replay-free framework grounded in the prior-task behavioral KL objective. CPO uses a theoretically justified parameter-movement regularization to limit policy drift on previous tasks. Extensive experiments across multiple model scales demonstrate that CPO consistently reduces forgetting while preserving, and in some cases improving, pretrained model capabilities. On Qwen3-VL-8B, CPO reduces forgetting by 13.7\% and improves pretrained capability by 7.0\%. The implementation code is available at https://github.com/MaolinLuo/CPO.

---

## 论文详细总结（自动生成）

### 论文总结：《RL Forgets! Towards Continual Policy Optimization》

#### 1. 核心问题与整体含义
*   **研究背景**：随着视觉语言模型（VLM）在动态环境中的部署，如何让模型在学习新任务的同时不遗忘旧知识（持续学习）成为核心挑战。
*   **研究动机**：近期学术界普遍认为强化学习（RL）在持续后训练中比监督微调（SFT）更具抗遗忘性。然而，作者指出这一结论大多基于过时或同质化的基准测试，可能存在数据污染（预训练数据包含测试集）。
*   **核心问题**：论文旨在重新审视“RL 天然抗遗忘”的假设，并解决 RL 在复杂多模态推理任务中依然存在的灾难性遗忘问题。

#### 2. 方法论：持续策略优化 (CPO)
*   **核心思想**：提出一种**无需数据重放（Replay-free）**的持续 RL 框架。其核心逻辑是将难以计算的“前序任务行为 KL 散度”约束，转化为易于计算的“参数移动正则化”。
*   **关键技术细节**：
    *   **理论界限**：证明了前序任务的性能下降受限于当前策略与旧策略在旧数据分布下的 token 级 KL 散度。
    *   **Fisher 信息近似**：通过泰勒展开将 KL 约束转化为基于 Fisher 信息矩阵的二阶近似。
    *   **参数移动代理**：理论证明参数在训练过程中的**累积移动量（Movement）**可以作为 Fisher 敏感度的噪声排名代理，从而避免了复杂的梯度计算。
    *   **正则化机制**：在训练新任务时，识别出前序任务中移动最显著的 Top-p% 参数，并对这些“重要参数”施加 L1 正则化，限制其偏离旧值。

#### 3. 实验设计
*   **MRCL 基准测试**：作者构建了全新的“多模态推理持续学习”基准，包含 2025 年下半年发布的 5 类任务：
    *   **MedBookVQA**（医疗影像）、**Navigation**（迷宫导航）、**We-Math2.0**（复杂数学）、**Puzzle**（空间推理）、**FinMME**（金融图表）。
*   **对比方法**：
    *   **SFT 类**：全参数 SFT、LoRA、O-LoRA、SEFE、KeepLoRA。
    *   **RL 类**：GRPO（带 KL 约束）、GSPO（不带 KL 约束）。
*   **评估指标**：MFT（微调准确率）、MFN（最终准确率）、MTA（任务平均准确率），以及在 11 个外部通用基准（如 MMMU-Pro, MathVista）上的零样本表现。

#### 4. 资源与算力
*   **硬件环境**：使用了 **NVIDIA H100** GPU 集群。
*   **算力分配**：
    *   2B/4B 模型的 LoRA 实验：单卡 H100。
    *   2B/4B 全参数 SFT 及 8B LoRA：2 张 H100。
    *   8B 全参数 SFT：4 张 H100。
    *   **所有 RL 实验**：统一使用 **8 张 H100**。
*   **训练细节**：每个 Prompt 采样 8 个 Rollouts，最大生成长度 2048。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 Qwen3-VL 的 2B、4B、8B 三种参数规模，验证了方法的扩展性（Scaling）。
*   **充分性**：
    *   进行了 5 阶段的持续学习实验，展示了完整的性能演变轨迹。
    *   包含了消融实验（如 L1 vs L2 正则化的对比）。
    *   在 11 个第三方基准上进行了广泛的泛化性测试。
*   **客观性**：通过使用 2025 年的新数据，有效排除了预训练数据污染对遗忘评估的干扰，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **RL 并非免疫遗忘**：在更具挑战性的 MRCL 基准下，标准 RL 方法（如 GRPO）依然表现出严重的灾难性遗忘。
*   **KL 约束的局限**：GRPO 默认的 KL 约束是基于“当前任务”的，它主要防止过拟合，对“旧任务”的保护作用有限，甚至会损害学习新任务的塑性。
*   **CPO 的优越性**：CPO 在 Qwen3-VL-8B 上将遗忘减少了 **13.7%**，同时将预训练能力的保持度提升了 **7.0%**，实现了稳定性与塑性的平衡。

#### 7. 优点
*   **理论扎实**：从 KL 散度界限出发，推导出参数移动作为 Fisher 信息的代理，具有较强的数学支撑。
*   **高效实用**：无需存储旧数据（隐私友好），且不增加额外的梯度计算开销，适合大规模模型。
*   **基准前瞻性**：MRCL 基准的发布为多模态持续学习研究提供了更可靠的测试床。

#### 8. 不足与局限
*   **超参数敏感性**：Top-p% 的比例选择和正则化强度 $\lambda$ 可能需要针对不同模型架构进行手动调优。
*   **任务顺序依赖**：论文未深入探讨不同任务顺序（如先易后难 vs 先难后易）对 CPO 效果的影响。
*   **模态广度**：虽然涵盖了多种推理任务，但主要集中在静态图像，未涉及视频流或实时交互场景的持续学习。

（完）
