---
title: "Be Faithful When Response: Returning Fluent and Grounded Answers for Vision-Language Models Reinforcement Learning"
title_zh: 响应时保持忠实：为视觉语言模型强化学习返回流畅且有依据的回答
authors: "Peng, Lee, Yin Zhang, Yanglin Zhang, Haonan Wu, Zishan Liu, Ruoxi Zang, Xin Zhu, Jiayin Zheng, Jian Yao, Zefeng Ji, Fei Ma"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.29984v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 强化学习用于提升视觉语言模型推理能力
tldr: "针对视觉语言模型（VLM）在强化学习中容易忽略视觉证据、产生不实推理的问题，本文提出了一种“忠实预热”（Faithful Warm-Start, FWS）策略。通过从通用基准中构建包含显式因果关系的FaithfulQA数据集，并利用VLM评判器进行纯化，使模型在RL优化前掌握视觉忠实推理能力。实验证明该方法能显著提升回答准确性，稳定训练过程并减少幻觉。"
selection_source: fresh_fetch
motivation: 直接对VLM进行强化学习容易导致模型过度依赖语言先验而忽视视觉信息，产生逻辑通顺但缺乏视觉依据的推理。
method: 提出FWS策略，通过构建具有强因果一致性的FaithfulQA数据集，在稀疏奖励的RL阶段前对模型进行忠实性监督预热。
result: 实验结果显示，该策略显著提高了答案准确率，增强了RL训练的稳定性，并有效减少了视觉上不支持的推理过程。
conclusion: 在强化学习之前引入忠实性预热阶段，对于提升视觉语言模型在复杂推理任务中的可靠性和一致性至关重要。
---

## 摘要
强化学习（RL）是提升视觉语言模型（VLMs）推理能力的重要范式。然而，直接将强化学习应用于多模态推理过程可能会导致不稳定性，这是由于模型利用了语言先验、忽略了视觉证据，并生成了虽然流畅但缺乏视觉依据的推理轨迹。由此产生了一个问题：在应用强化学习之前，能否初步引导策略进入视觉忠实的推理状态？为此，我们提出了一种忠实预热（Faithful Warm-Start, FWS）策略，该策略首先从六个通用的视觉问答（VQA）基准测试中筛选出具有显式视觉-语言因果关系的样本，构建了 FaithfulQA 数据集。在该数据集中，每个图像-问题对都包含一定程度的视觉观察、问题要求、常识知识、领域知识以及最终答案。随后，利用基于 VLM 的裁判进一步净化数据集，确保强因果一致性和视觉忠实性。这一预热阶段使模型在后续稀疏答案级奖励下的强化学习优化之前，具备了理解具有因果依据的视觉-语言模式的能力。实验结果表明，这种忠实监督提高了答案准确性，稳定了强化学习训练，并减少了缺乏视觉支持的推理。

## Abstract
Reinforcement Learning (RL) is an important paradigm for improving the reasoning capabilities of Vision-Language Models (VLMs). However, directly applying RL to rollout multimodal reasoning can lead to instability, due to the exploitation of language priors, the neglect of visual evidence, and the generation of reasoning traces that are fluent yet not visually grounded. The question arises: Can initially steer the policy toward visually faithful reasoning regime before applying reinforcement learning? To this end, we propose a Faithful Warm-Start (FWS) strategy that first curates samples with explicit vision-language causal relationships from six general VQA benchmarks to construct the FaithfulQA dataset, where each of the image-question pairs gains a certain degree of visual observations, question requirements, commonsense knowledge, domain knowledge, and the final answer. Subsequently, a VLM-based judge is employed to further purify the dataset, ensuring strong causal consistency and visual faithfulness. This warm-start stage equips the model with the capability to understand causally grounded vision-language patterns before subsequent RL optimization under sparse answer-level rewards. Experimental results show that such faithful supervision improves answer accuracy, stabilizes RL training, and reduces visually unsupported reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Be Faithful When Response: Returning Fluent and Grounded Answers for Vision-Language Models Reinforcement Learning》的结构化总结：

### 1. 核心问题与整体含义
该研究关注**视觉语言模型（VLM）在强化学习（RL）过程中的“忠实性”缺失问题**。
*   **研究背景**：虽然 RL 能显著提升 VLM 的推理能力，但由于奖励信号通常是稀疏的（仅基于最终答案的正确性），模型容易学会“走捷径”。
*   **核心问题**：模型往往利用语言先验（Language Priors）或数据集偏差来生成看似通顺但缺乏视觉依据的推理过程（即“幻觉”推理）。例如，看到湿地就说是下雨，而忽略了图中明显的洒水车。
*   **研究动机**：作者认为 RL 的初始策略状态至关重要。如果初始模型视觉扎根（Visual Grounding）能力弱，RL 优化会进一步加剧这种不实推理。

### 2. 方法论：忠实预热策略（FWS）
论文提出了一种**忠实预热（Faithful Warm-Start, FWS）**策略，旨在 RL 之前通过高质量数据引导模型进入忠实推理状态。
*   **FaithfulQA 数据集构建**：从 6 个通用 VQA 基准中筛选样本，将推理过程分解为四个结构化单元：
    1.  **视觉扎根（VG）**：从图像中直接观察到的证据。
    2.  **问题要求（QR）**：外部常识或领域知识。
    3.  **中间推理（II）**：基于事实和规则的逻辑步骤。
    4.  **最终预测（FP）**：支持答案的最终推导。
*   **单元级因果干预（Causal Intervention）**：为了验证推理单元是否真实有效，研究者逐一移除推理单元。如果移除某个单元导致模型从“答对”变为“答错”，则证明该单元具有强因果权重，而非冗余描述。
*   **VLM 评判器纯化**：使用更强大的模型（Qwen3VL-8B-Thinking）作为裁判，从视觉扎根、单元因果性、非矛盾性、忠实度四个维度打分（0-2分），仅保留满分且因果度量大于零的样本。
*   **训练流程**：先用纯化后的数据进行有监督微调（SFT），再进行基于答案奖励的 RL 优化。

### 3. 实验设计
*   **数据集/场景**：涵盖了 6 个代表性多模态推理基准：AI2D（图表）、TextbookQA（教科书问答）、PlotQA（绘图推理）、ScienceQA（科学问答）、ChartQA（图表分析）和 Geo3K（几何问题）。
*   **基准模型（Backbone）**：Qwen3-VL-2B-Instruct。
*   **对比实验**：
    *   Base model（原始模型）。
    *   不同规模的 FWS-SFT（20K, 40K, 60K 样本量）。
    *   Base model + RL vs. FWS-SFT + RL。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号（如 A100/H100）、数量或具体的训练时长。
*   **模型规模**：实验主要基于 2B 参数的模型进行训练，并使用 8B 参数的模型作为评判器。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 13.7 万原始样本、最终精炼至 6 万高忠实度样本的数据集。
*   **充分性**：实验覆盖了从图表到几何等多种复杂推理场景，并进行了不同数据规模（20K-60K）的消融实验。
*   **客观性**：通过对比 RL 训练过程中的奖励动态曲线（Reward Dynamics），直观展示了预热策略对训练稳定性的提升。

### 6. 主要结论与发现
*   **性能提升**：FWS 显著提高了所有基准测试的准确率，即使只有 20K 样本也能带来明显增益。
*   **训练稳定性**：经过 FWS 预热的模型在 RL 阶段表现出更快的收敛速度和更高的奖励平台，避免了原始模型在 RL 过程中的剧烈波动。
*   **减少漂移**：FWS 有效抑制了模型在推理时的“思维漂移”，使生成的推理轨迹与视觉证据高度一致。

### 7. 优点
*   **视角独特**：关注 RL 的“初始状态”，提出了“先学会忠实推理，再进行强化优化”的思路。
*   **因果验证**：引入单元级干预实验来量化推理过程的真实性，比单纯的文本匹配更具科学性。
*   **结构化推理**：将推理过程拆解为 VG/QR/II/FP 四部分，增强了模型输出的可解释性。

### 8. 不足与局限
*   **依赖强模型评判**：数据纯化高度依赖于 Qwen3VL-8B-Thinking 的判断，可能存在评判器自身的偏见。
*   **算力细节缺失**：缺乏具体的硬件消耗和训练效率数据，难以评估复现成本。
*   **泛化性验证**：虽然在 6 个基准上表现良好，但对于更开放、无固定答案的视觉对话场景的适用性尚待验证。
*   **奖励设计单一**：RL 阶段仍主要依赖结果奖励（Outcome-level），未来若能结合过程奖励（Process-level）可能会更强。

（完）
