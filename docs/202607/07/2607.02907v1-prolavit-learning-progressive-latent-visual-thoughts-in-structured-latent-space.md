---
title: "ProLaViT: Learning Progressive Latent Visual Thoughts in Structured Latent Space"
title_zh: ProLaViT：在结构化潜空间中学习渐进式潜视觉思维
authors: "Peiming Li, Yifan Wang, Xiaotian Zhang, Zhiyuan Hu, Shiyu Li, Zheng Wei, Yang Tang"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02907v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 针对复杂视觉推理任务的结构化视觉推导
tldr: 针对多模态大模型在复杂视觉推理中的局限，本文提出ProLaViT框架。该框架通过内生自蒸馏机制在连续潜空间内实现结构化视觉推导，无需外部专家模型。通过构建程序化合成流水线及设计粗到细因果链与辩证推理链，模型能有效处理空间与逻辑任务。此外，引入距离权重多样性损失以防止特征退化，显著提升了视觉推理的准确性、可解释性与效率。
selection_source: fresh_fetch
motivation: 现有多模态模型在处理复杂视觉推理时，面临计算成本高、过度依赖外部工具或缺乏严密认知逻辑等挑战。
method: 提出一种基于内生自蒸馏的潜空间视觉思维框架，结合程序化合成数据、双重推理范式及拓扑感知约束损失。
result: 实验证明ProLaViT在视觉中心基准测试中表现优异，在保持高效率的同时显著提升了推理准确性和可解释性。
conclusion: 该研究验证了在结构化潜空间内进行渐进式视觉思维推导能有效增强多模态大模型的复杂逻辑与空间推理能力。
---

## 摘要
多模态大语言模型（MLLMs）已取得显著进展，但在需要多步感知和逻辑推演的复杂视觉推理任务中仍面临挑战。虽然显式视觉生成会产生极高的计算成本，但现有的潜空间方法往往依赖外部专家或缺乏严密的认知逻辑。在本文中，我们提出了 ProLaViT（渐进式潜视觉思维），这是一个使 MLLMs 能够在连续潜空间中进行结构化视觉推导的框架。与依赖异构外部模型的工作不同，ProLaViT 利用内源性自蒸馏机制，使用模型自身的视觉编码器来监督潜思维。为了实现这一点，我们构建了一个可扩展的程序化合成流水线，使模型能够在无需推理时工具的情况下内化算法精度。我们设计了两种推理范式：(1) 用于空间任务的从粗到细因果链，引导注意力从全局上下文转向局部目标；(2) 用于逻辑任务的辩证推理链，结合反事实思维进行验证。此外，我们提出了一种距离加权多样性损失，以施加拓扑感知约束，通过增强语义区分度来防止特征退化。大量实验表明，ProLaViT 在以视觉为核心的基准测试中优于基准模型，以高效率实现了卓越的准确性和可解释性。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable progress but still struggle with complex visual reasoning tasks requiring multi-step perception and logical deduction. While explicit visual generation incurs prohibitive computational costs, existing latent approaches often rely on external experts or lack rigorous cognitive logic. In this paper, we introduce ProLaViT (Progressive Latent Visual Thought), a framework empowering MLLMs to perform structured visual derivation in the continuous latent space. Unlike works dependent on heterogeneous external models, ProLaViT leverages an endogenous self-distillation mechanism, utilizing the model's own visual encoder to supervise latent thoughts. To facilitate this, we construct a scalable programmatic synthesis pipeline enabling the model to internalize algorithmic precision without inference time tools. We design two reasoning paradigms: (1) Coarse-to-Fine Causal Chain for spatial tasks, guiding attention from global context to local targets. (2) Dialectical Reasoning Chain for logical tasks, incorporating counter-factual thinking for verification. Furthermore, we propose a Distance-Weighted Diversity Loss to impose topology-aware constraints, preventing feature degeneration by enforcing semantic distinctiveness. Extensive experiments demonstrate that ProLaViT outperforms baselines on vision-centric benchmarks, achieving superior accuracy and interpretability with high efficiency.

---

## 论文详细总结（自动生成）

### ProLaViT：在结构化潜空间中学习渐进式潜视觉思维

#### 1. 核心问题与研究动机
多模态大语言模型（MLLMs）虽然在通用任务上表现出色，但在处理需要**多步感知**和**逻辑推演**的复杂视觉推理任务（如空间定位、几何分析）时仍存在局限。
*   **文本思维链（Textual CoT）的局限**：文本过于抽象，难以捕捉细粒度的空间细节，导致“模态鸿沟”和视觉幻觉。
*   **显式视觉生成（Explicit Generation）的局限**：生成中间图像辅助推理的计算成本极高，难以实时应用。
*   **现有潜空间方法（Latent Reasoning）的局限**：通常采用“一步到位”的预测模式，缺乏严密的逻辑结构，且容易出现**潜空间坍缩（Latent Collapse）**，即不同推理步骤的特征变得冗余且不可区分。

#### 2. 方法论
ProLaViT 提出在结构化潜空间中进行**渐进式视觉推导（Progressive Visual Derivation）**：
*   **核心思想**：将复杂的视觉任务分解为一系列结构化的潜思维步骤，模拟人类“定位→聚焦→隔离”的认知过程。
*   **两种推理范式**：
    1.  **从粗到细因果链（空间任务）**：通过“全局网格→边界框定位→局部裁剪→精细分割”逐步缩小注意力范围。
    2.  **辩证推理链（逻辑任务）**：采用“假设→批判→验证”的循环，通过反事实模拟（如错误的拼图排列）来强化逻辑校验。
*   **内生自蒸馏（Endogenous Self-Distillation）**：不依赖外部专家模型（如SAM），而是利用模型自身冻结的视觉编码器作为教师，监督潜思维的生成。
*   **程序化合成流水线**：通过代码自动生成带有几何变换（裁剪、标注、路径等）的辅助图像序列，为中间推理步骤提供精确的监督信号。
*   **距离权重多样性损失（Distance-Weighted Diversity Loss）**：引入拓扑感知约束，根据推理步骤之间的因果距离施加不同的相似度惩罚，强制模型在不同步骤中产生具有语义区分度的特征，防止特征退化。

#### 3. 实验设计
*   **数据集与场景**：涵盖了感知密集型的多个维度，包括：
    *   **空间/感知**：MMVP, VStar, CV-Bench。
    *   **逻辑/拼图**：VisPuzzle, BLINK-Jigsaw。
    *   **图表分析**：ChartQA。
    *   **综合感知**：BLINK。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL-7B-Instruct。
    *   **文本空间推理**：标准 SFT、文本 CoT SFT。
    *   **潜空间推理**：CoVT（依赖外部专家）、LVR、一步式潜空间预测（One-step Latent Pred）。

#### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA H20 GPU**。
*   **训练时长**：总训练时间约为 **28 GPU 小时**（相比之下，生成式 CoT 方法通常需要超过 120 小时）。
*   **参数更新**：采用 LoRA 微调（Rank=16），视觉编码器和 LLM 主干在训练期间保持冻结。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流视觉推理基准测试上进行了定量评估。
*   **消融实验**：非常充分，涵盖了：
    1.  **结构消融**：对比“一步式”与“渐进式”结构的优劣。
    2.  **损失函数消融**：验证距离权重多样性损失对缓解潜空间坍缩的作用。
    3.  **教师模型消融**：对比内生编码器与外部专家（DINOv2, SDXL-VAE）的效果。
    4.  **步骤顺序消融**：验证因果顺序（从粗到细）对推理性能的影响。
*   **客观性**：通过可视化余弦相似度热图直观展示了特征区分度，实验设计遵循了严谨的对照原则。

#### 6. 主要结论与发现
*   **渐进式优于一步式**：在复杂任务（如拼图）上，渐进式推导比单步预测准确率提升显著（BLINK-J 提升达 10%）。
*   **内生监督更稳健**：使用模型自身的视觉特征作为监督信号，比引入外部异构模型（如 DINO）更能保持领域一致性，特别是在图表分析（ChartQA）中表现更优。
*   **多样性约束至关重要**：如果没有多样性损失，后续推理步骤的特征会迅速趋同，导致推理链失效。
*   **效率优势**：ProLaViT 在推理延迟上仅比基座模型增加 21%，远低于显式调用工具或生成图像的方法（延迟增加 240%）。

#### 7. 优点
*   **高效性**：无需推理时调用外部工具或生成像素，推理速度快。
*   **解释性**：通过结构化的潜思维步骤，使模型的推理过程具有一定的拓扑逻辑可循。
*   **内生性**：自蒸馏机制降低了系统复杂度，避免了外部模型的领域偏差。
*   **鲁棒性**：距离权重损失巧妙地平衡了“上下文保留”与“特征区分度”。

#### 8. 不足与局限
*   **任务覆盖**：目前主要集中在静态图像的空间和逻辑推理，尚未扩展到视频等动态时序任务。
*   **步骤固定**：实验中统一使用了 4 个推理步骤，对于极度复杂的任务，固定步数可能限制了模型的表达上限。
*   **依赖合成数据**：模型能力的提升高度依赖于程序化合成数据的质量和多样性，可能存在向真实复杂场景迁移的泛化风险。

（完）
