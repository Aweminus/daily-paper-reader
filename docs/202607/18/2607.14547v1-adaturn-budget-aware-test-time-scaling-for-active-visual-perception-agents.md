---
title: "AdaTurn: Budget-Aware Test-Time Scaling for Active Visual Perception Agents"
title_zh: AdaTurn：面向主动视觉感知智能体的预算感知测试时缩放
authors: "Susan Liang, Chao Huang, Filippos Bellos, Jing Bi, Jason J Corso, Chenliang Xu"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14547v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 主动视觉感知智能体将推理与图像接地动作交替进行
tldr: 针对主动视觉感知智能体在不同推理步数限制下表现不佳的问题，本文提出 AdaTurn 框架。该框架通过感知预算的策略训练，解决了传统模型在步数受限时因“灾难性截断”而无法输出有效答案的缺陷。核心技术 FA-DAPO 将超预算事件转化为可训练的决策步骤，使模型能在资源耗尽时整合现有证据给出最佳答案，显著提升了低预算下的任务准确率并保持了高预算下的扩展性。
selection_source: fresh_fetch
motivation: 现有主动视觉智能体在部署时面临多变的推理步数限制，常因未达到预期步数被截断而导致无法生成有效答案。
method: 提出 AdaTurn 框架，通过 FA-DAPO 算法将强制结束转化为可训练的决策步骤，并引入随机预算训练和负载均衡调度。
result: "在 VisualProbe-Medium 任务中，4步限制下的准确率从 36.7% 提升至 47.6%，同时在多种模型架构上表现出良好的迁移性。"
conclusion: AdaTurn 有效解决了主动感知中的预算敏感性问题，实现了在不同推理成本下的性能优化与灵活扩展。
---

## 摘要
主动视觉智能体通过在多轮交互中交替进行推理与图像落地（image-grounding）动作来解决细粒度图像任务。然而，部署时的展开预算（rollout budgets）很少是固定的：某些请求允许长路径展开，而另一些则要求智能体在严格的轮次限制下行动。现有方法在训练策略时往往假设展开预算是隐藏的，因此当可用预算小于智能体偏好的轨迹长度时，交互往往在生成任何有效答案之前就被截断；我们将这种失败称为“灾难性截断”（catastrophic truncation）。为了克服这一挑战，我们提出了 AdaTurn，这是一个预算感知框架，它以允许的轮次数量为条件来约束智能体，并显式地训练由预算引起的边界行为。我们的核心组件——强制回答 DAPO（FA-DAPO），将超出预算事件从被掩蔽或惩罚的失败转化为一个可训练的最终决策步骤，教导模型在无法进一步使用工具时综合部分证据。我们在训练和推理过程中进一步随机化展开预算，并引入了一个负载均衡调度器，使这些操作具有实际可行性。AdaTurn 显著提高了低预算下的准确率，例如在 4 轮交互时将 VisualProbe-Medium 的准确率从 36.7% 提升至 47.6%，同时在更大预算下保持了强大的缩放能力，并能有效地迁移到多种骨干网络和通用多模态基准测试中。

## Abstract
Active visual agents solve fine-grained image tasks by interleaving reasoning with image-grounding actions across multiple turns. However, deployment-time rollout budgets are rarely fixed: some requests permit long rollouts, while others require the agent to act under a tight turn limit. Existing methods train the policy as if the rollout budget were hidden, so when the available budget is smaller than the trajectory the agent prefers, the interaction is often truncated before any valid answer is produced; we term this failure \emph{catastrophic truncation}. To overcome this challenge, we present AdaTurn, a budget-aware framework that conditions the agent on the allowed number of turns and explicitly trains the boundary behavior induced by the budget. Our key component, Forced-Answer DAPO (FA-DAPO), converts the over-budget event from a masked or penalized failure into a trainable final-decision step, teaching the model to synthesize partial evidence when further tool use is no longer possible. We further randomize rollout budgets during both training and inference and introduce a load-balanced scheduler that makes such operations practical. AdaTurn substantially improves low-budget accuracy, for example raising VisualProbe-Medium from 36.7% to 47.6% at four turns, while preserving strong scaling at larger budgets and transferring effectively to multiple backbones and general multimodal benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaTurn: Budget-Aware Test-Time Scaling for Active Visual Perception Agents》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：主动视觉智能体（Active Visual Agents）通过多轮交互（如缩放、裁剪图像）来处理细粒度视觉任务。然而，实际部署时的“轮次预算”（Turn Budget）往往是动态变化的。
*   **核心痛点**：现有模型通常在固定预算下训练，对部署时的预算限制“视而不见”。这导致两种失败模式：
    1.  **灾难性截断（Catastrophic Truncation）**：在低预算下，智能体因坚持调用工具而未能在限制步数内给出最终答案，导致任务直接失败。
    2.  **过早停止或资源浪费**：在高预算下，智能体可能在还有提升空间时过早停止，或无法有效利用额外轮次进行验证。
*   **研究目标**：开发一种能够感知预算、并根据剩余轮次灵活调整策略（在工具调用与直接回答之间权衡）的视觉智能体。

### 2. 方法论
AdaTurn 框架的核心思想是将“预算”作为任务输入的一部分，并显式训练模型在边界条件下的决策能力。
*   **预算感知条件化**：将最大允许轮次 $T_{max}$ 作为 Prompt 的一部分输入模型，使策略 $\pi_\theta$ 依赖于当前剩余资源。
*   **强制回答 DAPO (FA-DAPO)**：
    *   基于 DAPO（一种比 GRPO 更稳健的强化学习算法）进行优化。
    *   **关键创新**：当模型在最后一轮（$t = T_{max}$）仍试图调用工具时，系统会拦截该请求，并插入一个强制指令（如“预算已耗尽，请根据现有证据回答”）。
    *   **学习信号转换**：将原本会被视为失败或被掩码处理的“超预算行为”转化为一个可训练的决策步骤，教导模型在无法获取更多视觉细节时，如何综合已有的部分观察结果给出最佳推测。
*   **动态预算训练**：在训练期间随机采样不同的 $T_{max}$，强制模型学习不同节奏下的推理路径。
*   **负载均衡调度器**：针对不同样本因预算不同导致的 GPU 计算负载不均问题，提出了一种基于预算排序和贪婪分配的调度算法，显著提升了训练吞吐量。

### 3. 实验设计
*   **数据集与基准**：
    *   **核心任务**：VisualProbe (Easy/Medium/Hard)、V* Bench（视觉搜索）。
    *   **高分辨率感知**：HR-Bench (4K/8K)。
    *   **真实世界推理**：MME-RealWorld。
    *   **通用能力**：OCRBench, ChartQA, DocVQA, MathVista, ScienceQA 等。
*   **对比方法**：
    *   闭源模型：GPT-4o。
    *   开源 SOTA 视觉智能体：DeepEyes、Mini-o3、Pixel Reasoner、Chain-of-Focus。
    *   基础 VLM：LLaVA-OneVision、Qwen2.5-VL。

### 4. 资源与算力
*   **硬件环境**：
    *   **监督微调 (SFT)**：使用 1 个节点（8 张 A100 GPU），耗时约 2 小时。
    *   **强化学习 (RL)**：使用 2 个节点（共 16 张 A100 GPU），耗时约 3 天。
*   **模型骨干**：主要基于 Qwen2.5-VL-7B-Instruct，并在 Qwen3-VL-4B/8B 上验证了迁移性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4 轮到 32 轮的不同预算场景，并在多个异构基准上进行了测试。
*   **消融实验**：非常充分。作者对比了“掩码前缀”、“仅末轮 RL”、“增加格式奖励”等多种变体，证明了全路径参与 RL 且仅以准确率为目标的 FA-DAPO 效果最好。
*   **客观性**：针对具有随机性的基准（如 VisualProbe），采用了 32 次重复实验取平均值的严谨做法。

### 6. 主要结论与发现
*   **低预算性能飞跃**：在 4 轮限制下，AdaTurn 在 VisualProbe-Medium 上的准确率比 Mini-o3 提升了 **10.9%**，在 MME-RealWorld 上提升了 **13.9%**。
*   **高预算扩展性**：在增加预算至 32 轮时，AdaTurn 依然能保持与 SOTA 相当或更优的性能，证明其并未因追求速度而牺牲深度推理能力。
*   **跨模型迁移**：该方法在不同规模的骨干网络（4B, 7B, 8B）上均表现出一致的增益。
*   **通用性保持**：在非智能体类的通用多模态任务（如 OCR、数学推理）上，AdaTurn 保持了基础模型的水平，未出现灾难性遗忘。

### 7. 优点
*   **实用性强**：直接解决了视觉智能体在实际部署中面临的延迟与计算成本约束问题。
*   **算法优雅**：FA-DAPO 巧妙地解决了 RL 训练中边界样本处理的难题，将“截断错误”转化为“学习机会”。
*   **系统优化**：负载均衡调度器的引入（1.34x 加速）体现了作者在算法与工程结合上的深度思考。

### 8. 不足与局限
*   **工具集限制**：目前主要集中在“缩放/裁剪”这类视觉工具，尚未扩展到 Web 搜索、代码执行等更复杂的工具链。
*   **场景局限**：实验主要针对静态图像，未涉及视频流等需要实时动态调整预算的场景。
*   **幻觉风险**：在极低预算且证据严重不足时，强制回答机制虽然避免了截断，但仍可能诱导模型产生基于猜测的幻觉（如实验中的失败案例所示）。

（完）
