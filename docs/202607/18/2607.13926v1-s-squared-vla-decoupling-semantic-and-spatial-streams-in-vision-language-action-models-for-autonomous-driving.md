---
title: "S-squared-VLA: Decoupling Semantic and Spatial Streams in Vision-Language-Action Models for Autonomous Driving"
title_zh: S-squared-VLA：自动驾驶视觉-语言-动作模型中语义与空间流的解耦
authors: "Jianguo Yu, Rukang Wang, Duanfeng Chu, Chen Wang, Renju Feng, Liping Lu"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13926v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于自动驾驶推理的视觉-语言-动作 (VLA) 模型
tldr: 针对自动驾驶中视觉语言动作模型（VLA）因语言标记与连续轨迹不匹配导致的“空间表示坍缩”问题，本文提出S-squared-VLA。该模型通过解耦语义流和空间流，分别提取多尺度语义特征和未压缩的空间几何先验，并利用双流规划适配器进行融合。在NAVSIM基准测试中，该方法刷新了SOTA记录，显著提升了驾驶安全性和规划精度。
selection_source: fresh_fetch
motivation: 传统VLA模型将感知与控制耦合，导致精细的空间和几何先验在自回归语言瓶颈中丢失，产生空间表示坍缩。
method: 提出解耦语义与空间流的架构，语义流负责意图推理，空间流保留原始视觉特征并引入辅助感知监督，最后通过双流适配器融合。
result: "在NAVSIM闭环基准测试中取得87.1的PDMS得分，并以98.4%的无碰撞率创下纯监督微调设置下的新纪录。"
conclusion: 通过显式解耦语义与空间表征，S-squared-VLA有效解决了VLA模型在自动驾驶中的空间感知瓶颈，提升了复杂场景下的导航安全性。
---

## 摘要
视觉-语言模型（VLMs）在自动驾驶的高层推理方面展现出巨大潜力，但在生成精确的底层控制动作方面仍存在根本性困难。这一局限性源于离散语言标记与连续轨迹规划之间固有不匹配所导致的语义-物理鸿沟。虽然视觉-语言-动作（VLA）架构试图通过将感知与控制统一到单一策略中来弥合这一鸿沟，但这种耦合产生了新的瓶颈。标准 VLA 模型会经历严重的空间表示崩溃，从而不可逆地降低了对于安全、边界感知导航至关重要的细粒度空间和几何先验。为了解决这一局限性，我们提出了 S-squared-VLA，它显式地解耦了视觉-语言-动作模型中的语义流和空间流。语义流利用分层桥接提取多尺度 VLM 特征，以实现鲁棒的意图推理。与此同时，一个独立的空间流绕过了自回归语言瓶颈，直接保留了来自视觉编码器的未压缩空间特征。通过集成辅助感知监督，该流显式地为模型提供了丰富的空间和几何先验。最后，双流规划适配器通过级联注意力机制将高层语义意图与精确的空间约束相融合。在 NAVSIM 闭环基准测试上的评估表明，S-squared-VLA 实现了 87.1 的预测驾驶员模型得分（PDMS），在纯监督微调（SFT）设置下树立了 VLA 模型的新标杆。通过缓解传统 VLM 的空间表示崩溃，我们的框架显著优于基准模型，在所有评估方法中实现了最高的无碰撞（NC）率（98.4%）。

## Abstract
Vision-Language Models (VLMs) have demonstrated remarkable potential for high-level reasoning in autonomous driving, yet they fundamentally struggle to generate precise, low-level control actions. This limitation is rooted in a semantic-physical gap caused by the inherent mismatch between discrete language tokens and continuous trajectory planning. While Vision-Language-Action (VLA) architectures attempt to bridge this gap by unifying perception and control into a single policy, this entanglement creates a new bottleneck. Standard VLAs experience a severe spatial representation collapse, which irreversibly degrades the fine-grained spatial and geometric priors essential for safe, boundary-aware navigation. To address this limitation, we propose the S-squared-VLA, which explicitly decouples the semantic and spatial streams in Vision-Language-Action models. The semantic stream leverages hierarchical bridging to extract multi-scale VLM features for robust intent reasoning. In parallel, an independent spatial stream bypasses the autoregressive language bottleneck, directly preserving uncompressed spatial features from the visual encoder. By integrating auxiliary perception supervision, this stream explicitly equips the model with rich spatial and geometric priors. Finally, a dual-stream planning adapter fuses high-level semantic intent with precise spatial constraints via cascaded attention mechanisms. Evaluations on the NAVSIM closed-loop benchmark show that S-squared-VLA achieves a Predictive Driver Model Score (PDMS) of 87.1, establishing a new state-of-the-art for VLA models under a purely supervised fine-tuning (SFT) setting. By mitigating the spatial representation collapse of traditional VLMs, our framework significantly outperforms baselines, achieving the highest No Collision (NC) rate of 98.4 among all evaluated methods.

---

## 论文详细总结（自动生成）

这篇论文提出了 **S²-VLA**，旨在解决视觉-语言-动作（VLA）模型在自动驾驶任务中因“空间表示坍缩”而导致的底层控制精度不足问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的视觉-语言模型（VLM）在处理自动驾驶时存在**语义-物理鸿沟**。由于语言标记（Tokens）是离散的，而轨迹规划需要连续的空间信息，深度抽象的语义特征往往会丢失细粒度的几何先验。
*   **研究动机**：现有的 VLA 模型通常将感知、推理与控制耦合在单一流中，导致模型过度依赖语义逻辑而忽视了物理边界约束（即“空间表示坍缩”），这在需要精确避障和边界感知的驾驶场景中极具风险。

### 2. 方法论：S²-VLA 架构
核心思想是**显式解耦语义流与空间流**，并通过专门的适配器进行融合。
*   **多尺度语义流 (Semantic Stream)**：
    *   采用 **InternVL3-2B** 作为骨干网络。
    *   通过**稀疏多尺度采样**提取不同层（如第3, 8, 13...层）的特征，平衡高层意图推理与底层细节。
    *   注入可学习的“动作查询（Action Queries）”来聚合与执行相关的特征。
*   **任务驱动的空间流 (Spatial Stream)**：
    *   **绕过语言瓶颈**：直接从视觉编码器（ViT）提取未压缩的特征。
    *   **辅助感知监督**：引入两个辅助头——**地图头 (Map Head)** 预测 BEV 语义地图，**代理头 (Agent Head)** 预测周围障碍物的边界框。这迫使该流学习环境的度量布局和几何边界。
*   **双流规划适配器 (Dual-Stream Planning Adapter)**：
    *   采用级联的交叉注意力机制。第一阶段将规划 Token 与语义特征对齐以确定导航意图；第二阶段引入空间流特征进行几何精炼，确保轨迹符合物理约束。
    *   **损失函数**：结合了轨迹回归损失（L1）、运动平滑约束（加速度和加速度变化率）、以及辅助感知的分类与回归损失。

### 3. 实验设计
*   **数据集与基准**：在 **NAVSIM** 闭环基准测试上进行评估。该基准强调智能体与动态环境的交互。
*   **评价指标**：主要指标为 **PDMS**（预测驾驶员模型得分），涵盖无碰撞率（NC）、可行驶区域合规性（DAC）、碰撞时间（TTC）、舒适度（Comf）和车辆进度（EP）。
*   **对比方法**：
    *   **端到端（E2E）模型**：如 UniAD, TransFuser, DiffusionDrive 等。
    *   **VLM/VLA 模型**：如 InternVL3-2B 原型, ReCogDrive, AutoVLA, ImagiDrive 等。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练流程**：分为三个阶段：
    1.  VQA 数据集上的指令微调（3 epochs）。
    2.  冻结 VLM，使用 LoRA 训练意图提取和辅助感知模块（4 epochs）。
    3.  冻结感知部分，专门优化视觉精炼模块以对齐空间约束（4 epochs）。
*   **优化器**：AdamW，学习率为 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了定量对比实验、定性可视化分析以及系统的消融实验。
*   **充分性**：
    *   **定量实验**：对比了 10 余种主流模型，涵盖了纯视觉和多模态融合方案。
    *   **消融实验**：分别验证了语义特征、空间特征和辅助感知任务对最终得分的贡献（PDMS 从 84.1 提升至 87.1）。
    *   **客观性**：为了公平竞争，作者特意排除了其他模型中可能使用的强化学习（RL）后处理技术，仅在纯监督微调（SFT）设置下进行对比。

### 6. 主要结论与发现
*   **性能领先**：S²-VLA 取得了 **87.1 的 PDMS 得分**，刷新了 SFT 设置下的 VLA 模型纪录。
*   **安全性极高**：实现了 **98.4% 的无碰撞率 (NC)**，是所有评估模型中最高的，证明了空间流对安全性的显著提升。
*   **解耦的必要性**：实验证明，单纯增加语义推理（VLM）并不能解决几何对齐问题，只有引入显式的空间流才能有效减少压线和碰撞。

### 7. 优点
*   **架构创新**：通过解耦双流，巧妙解决了 VLM 在底层控制上的“软肋”。
*   **安全性导向**：辅助感知任务（地图和代理预测）为模型提供了坚实的物理先验。
*   **高效性**：在不依赖昂贵的激光雷达（LiDAR）和复杂的迭代扩散解码器的情况下，性能逼近甚至超过了部分 LiDAR 融合模型。

### 8. 不足与局限
*   **计算开销**：双流架构和密集特征提取增加了推理时的计算负担，可能影响实时性。
*   **传感器限制**：目前主要基于单目摄像头，虽然表现优异，但在极端深度感知场景下可能仍逊色于多相机或 LiDAR 方案。
*   **训练复杂性**：三阶段训练过程相对繁琐，对超参数调节有一定要求。

（完）
