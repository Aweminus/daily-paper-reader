---
title: "ReferTrack: Referring Then Tracking for Embodied Visual Tracking"
title_zh: ReferTrack：具身视觉跟踪中的先指代后跟踪
authors: "Hanjing Ye, Tianle Zeng, Jiazhao Zhang, Shaoan Wang, Zibo Zhang, Weisi Situ, Yuchen Zhou, Yonggen Ling, Hong Zhang"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20061v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 具有思维链推理的具身视觉追踪视觉-语言-动作策略
tldr: 针对具身视觉追踪（EVT）中目标识别与轨迹规划难以对齐的问题，本文提出ReferTrack框架。该框架采用“先指代后追踪”范式，通过在图像空间显式选择目标边界框并结合时序几何特征（TVBI tokens）来生成导航路点。在EVT-Bench基准测试中，ReferTrack在单视角下达到了SOTA性能，并在真实机器人平台上验证了其鲁棒的迁移能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言动作策略在抽象空间进行推理，导致目标识别与图像空间检测对齐弱且难以监督。
method: 提出一种先从索引边界框中选择目标，再根据该显式决策解码追踪路点的范式，并利用滑动窗口队列和TVBI标记保留时序运动线索。
result: 在EVT-Bench的单目标、干扰和歧义追踪任务中均取得单视角SOTA表现，甚至超越了部分多相机基准。
conclusion: ReferTrack通过显式地将语言指令关联到图像检测，显著提升了具身智能体在复杂环境下的目标追踪性能与泛化能力。
---

## 摘要
具身视觉跟踪（EVT）要求移动智能体仅利用机载视觉，持续跟随自然语言描述的特定目标。虽然近期的视觉-语言-动作（VLA）策略统一了目标识别和轨迹规划，但其思维链（CoT）推理通常在抽象的空间潜变量中运行，这些变量难以监督，且与显式的图像空间检测对齐较弱。为解决这一问题，我们提出了 ReferTrack，这是一种利用单个前向摄像头实现 EVT 落地的“先指代后跟踪”范式。我们的模型首先从一组索引边界框中选择目标，然后根据这一基于图像落地的决策解码跟踪路径点。为了保留随时间变化的目标运动线索，ReferTrack 维护了一个先前选择的边界框滑动窗口队列，通过时间-视角-边界框指示器（TVBI）标记将其几何特征注入视觉历史中。我们通过在自定义的 Refer-QA 数据集上进行协同训练，进一步增强了目标识别能力。在 EVT-Bench 上，ReferTrack 实现了最先进的单视图性能，在单目标、干扰和歧义跟踪拆分任务中的成功率分别为 89.4%、73.3% 和 74.1%——在重识别任务中达到甚至超过了多个多摄像头基准模型。最后，在四足和人形机器人上的真实世界部署验证了其鲁棒的从仿真到现实（sim-to-real）的迁移能力。代码可在 https://github.com/MedlarTea/referTrack 获取。

## Abstract
Embodied visual tracking (EVT) requires a mobile agent to continuously follow a specific target described in natural language using only onboard vision. While recent vision-language-action (VLA) policies unify target identification and trajectory planning, their chain-of-thought (CoT) reasoning often operates in abstract spatial latents that are difficult to supervise and weakly aligned with explicit image-space detections. To address this, we introduce ReferTrack, a referring-then-tracking paradigm that grounds EVT using a single forward-facing camera. Our model first selects the target from an indexed set of bounding boxes, then decodes tracking waypoints conditioned on this image-grounded decision. To preserve target motion cues over time, ReferTrack maintains a sliding-window queue of previously selected bounding boxes, injecting their geometric features into the visual history via temporal-viewpoint-bbox indicator (TVBI) tokens. We further enhance target identification by co-training on a custom Refer-QA dataset. On EVT-Bench, ReferTrack achieves state-of-the-art single-view performance with success rates of 89.4%, 73.3%, and 74.1% on the single-target, distracted, and ambiguity tracking splits, respectively -- matching or even surpassing several multi-camera baselines on identification-heavy tasks. Finally, real-world deployments on legged and humanoid robots validate its robust sim-to-real transfer capabilities. Code is available at https://github.com/MedlarTea/referTrack.

---

## 论文详细总结（自动生成）

以下是对论文《ReferTrack: Referring Then Tracking for Embodied Visual Tracking》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：具身视觉跟踪（EVT）要求机器人仅依靠机载摄像头，根据自然语言指令（如“跟随穿蓝色T恤的人”）在复杂动态环境中持续追踪目标。
*   **核心问题**：现有的视觉-语言-动作（VLA）模型通常在抽象的空间潜变量中进行推理，导致目标识别（Identification）与实际图像空间的检测结果对齐较弱，且难以进行有效监督。
*   **研究动机**：将目标识别转化为一个显式的“指代”任务，即从一组检测到的边界框（Bbox）中选择目标，从而增强模型在复杂场景下的视觉落地（Grounding）能力。

### 2. 方法论
*   **核心思想**：提出“先指代后跟踪”（Referring Then Tracking）范式。模型首先从图像中检测出的候选人索引列表中选出目标，再基于该显式决策生成导航轨迹。
*   **关键技术细节**：
    *   **Refer-CoT（指代思维链）**：在预测动作前，模型先输出一个特殊的索引标记（如 `<ped_2>` 或 `<NO_EXIST>`），将识别过程简化为受限的多选题，便于监督学习。
    *   **TVBI（时间-视角-边界框指示器）**：在视觉历史序列中插入目标几何特征标记。通过滑动窗口队列保存过去 $H$ 帧的目标 Bbox，并将其编码注入视觉 Token 流中，为模型提供显式的运动线索。
    *   **双分支架构**：基于 Qwen3-4B 大语言模型，结合 SigLIP 和 DINOv2 视觉编码器，通过 MLP 投影器处理图像、指令和候选框目录。
    *   **协同训练**：利用自定义的 **Refer-QA 数据集**（基于 SYNTH-PEDES 构建）进行联合训练，强化模型在静态图像中根据描述选择正确索引的能力。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：在 Habitat 3.0 模拟器中使用 **EVT-Bench** 基准测试。
    *   **任务拆分**：单目标追踪（STT）、干扰追踪（DT，多人在场）和歧义追踪（AT，多人特征相似）。
*   **对比方法**：
    *   **经典/强化学习方法**：如 IBVS、PoliFormer、EVT 等。
    *   **SOTA VLA 模型**：如 TrackVLA、TrackVLA++、NavFoM、Uni-NaVid、VLingNav 等。
    *   **多相机基准**：对比了使用 3-4 个摄像头的模型性能。
*   **真实世界部署**：在 **Unitree Go2（四足机器人）** 和 **Unitree G1（人形机器人）** 上进行了实地验证。

### 4. 资源与算力
*   **模型规模**：使用了 **Qwen3-4B** 作为语言模型骨干。
*   **训练数据**：1.3M 条导航轨迹样本 + 1.3M 条 Refer-QA 样本（1:1 混合）。
*   **算力说明**：文中提到在“远程高性能服务器”和“云 GPU 服务器”上运行，训练采用两阶段监督微调（SFT）。**具体 GPU 型号、数量及训练总时长未在文中明确给出**，但提到推理频率可达 10.6 Hz。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境下的三大核心任务（STT, DT, AT），并进行了详细的消融实验。
*   **消融实验**：验证了 Refer-CoT、TVBI 以及 Oracle（真值）Bbox 对性能的影响，证明了目标识别是当前 EVT 的主要瓶颈。
*   **客观性与公平性**：对比了单视角与多视角模型，实验设置遵循了 EVT-Bench 的标准协议，并使用了相同的 ReID 数据集进行协同训练，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：ReferTrack 在单视角设置下取得了 SOTA 性能，STT/DT/AT 的成功率分别达到 89.4%、73.3% 和 74.1%。
*   **超越多视角**：在识别任务繁重的 DT 和 AT 拆分中，ReferTrack 的单相机表现甚至超过了部分使用 3-4 个相机的基准模型。
*   **识别是关键**：实验表明，显式的图像空间指代可以有效弥补相机视野的不足，并减少对昂贵的强化学习（RL）微调的依赖。
*   **泛化能力**：成功实现了从仿真到现实的迁移，在不同形态的机器人（四足与人形）上均表现稳健。

### 7. 优点
*   **显式落地**：通过索引 Bbox 解决了 VLA 模型在抽象空间推理难以监督的问题。
*   **架构简洁**：单 Token 的 Refer-CoT 设计高效且易于集成到现有的自回归生成框架中。
*   **数据效率**：仅通过 SFT 即可达到甚至超过 RL 训练模型的效果。
*   **时序记忆**：TVBI 机制有效地将历史几何信息转化为可利用的导航先验。

### 8. 不足与局限
*   **检测器依赖**：模型性能高度依赖于前端目标检测器（如 YOLO11）的质量，若检测器漏检，模型将无法指代。
*   **单视角限制**：虽然性能强劲，但在目标完全脱离前向视野且缺乏历史线索时，单相机仍存在物理局限。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，不利于其他研究者评估复现成本。

（完）
