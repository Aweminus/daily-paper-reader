---
title: Visual Access Boundaries in Vision-Language Model Reasoning
title_zh: 视觉语言模型推理中的视觉访问边界
authors: "Hiroto Osaka, Shohei Taniguchi, Gouki Minegishi, Kai Yamashita, Masahiro Suzuki, Yutaka Matsuo"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12815v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 研究VLM思维链推理过程中的视觉信息获取
tldr: 本研究探讨了视觉语言模型（VLM）在思维链（CoT）推理时对图像信息的依赖机制。通过引入“视觉访问扫描”干预手段，研究者定义了维持模型性能所需的最小图像访问边界（VAB）。实验表明，CoT 并非通过延长对原始图像 Token 的访问来提升性能，而是通过在语言侧对已提取的隐藏状态进行深度计算。此外，研究指出 CoT 的效能受限于感知读取阶段，即模型从隐藏状态中提取可靠视觉属性的能力。
selection_source: fresh_fetch
motivation: 探究 VLM 在进行长链条推理时，是需要持续访问图像 Token 还是主要依赖早期处理生成的视觉隐藏信息。
method: 提出视觉访问扫描（Visual Access Sweep）方法，通过因果干预屏蔽模型对图像 Token 的注意力，从而确定维持准确率的最小访问边界。
result: 实验证明 CoT 与直接回答的视觉访问边界高度相似，表明 CoT 的优势源于语言侧的计算扩展而非持续的视觉输入。
conclusion: CoT 的性能提升受限于感知读取瓶颈，即模型从视觉隐藏状态中可靠提取并输出属性的能力。
---

## 摘要
思维链（CoT）提示被广泛用作视觉语言模型（VLM）的测试时缩放策略，但目前尚不清楚当 VLM 生成更长的推理轨迹时，究竟是什么得到了扩展。我们探究 CoT 是否需要持续访问图像 token，还是主要在模型前向传播早期已获取的视觉信息上运行。我们引入了“视觉访问扫描”（Visual Access Sweep），这是一种因果干预方法，它在层深度和生成时间维度上屏蔽了从生成 token 查询到图像 token 键的注意力，并将“视觉访问边界”（VAB）定义为保持任务准确性的最小访问区域。在 Qwen2.5-VL 和 InternVL3 的六种模型配置中，无 CoT 直接回答和 CoT 提示均表现出有限的 VAB。在 Qwen2.5-VL-32B 以及 14B 和 38B 规模的 InternVL3 中，当以无 CoT 全访问目标为基准评估 CoT 时，尽管生成长度显著增加，其 VAB 层与无 CoT 边界的差异最多仅为两层。这表明 CoT 提升性能的主要方式并非在整个推理轨迹中延长对图像 token 的直接访问，而是在源自图像的隐藏状态信息上扩展语言侧的计算。我们进一步证明，CoT 的增益受限于感知读取（perceptual readout）。当模型能够可靠地读取所查询的视觉属性时，CoT 会有所帮助；而当读取不可靠时，CoT 则无效。符号属性预言机（symbolic-attribute oracle）显示，一旦以文本形式提供真实属性，CoT 就能提高计数能力；而单对象探测与解码（probe-vs-decode）检查显示，困难属性可以从隐藏状态中线性恢复，但模型自身却难以输出。综上所述，这些分析表明瓶颈在于读取而非计数。

## Abstract
Chain-of-Thought (CoT) prompting is widely used as a test-time scaling strategy for Vision-Language Models (VLMs), but it remains unclear what is extended when VLMs generate longer reasoning traces. We ask whether CoT requires continued access to image tokens, or whether it mainly operates over visual information already made available earlier in the forward pass. We introduce Visual Access Sweep, a causal intervention that masks attention from generated-token queries to image-token keys along layer depth and generation time, and define the Visual Access Boundary (VAB) as the minimal access region that preserves task accuracy. Across six model configurations from Qwen2.5-VL and InternVL3, both no-CoT direct answering and CoT prompting exhibit finite VABs. In Qwen2.5-VL-32B and InternVL3 at 14B and 38B scales, when CoT is evaluated against the no-CoT full-access target, its VAB layer differs from the no-CoT boundary by at most two layers, despite substantially longer generations. This suggests that CoT does not primarily improve performance by prolonging direct image-token access throughout the reasoning trace, but by extending language-side computation over image-derived hidden-state information. We further show that CoT gains are constrained by perceptual readout. CoT helps when the queried visual attribute can be reliably read out by the model, but not when that readout is unreliable. A symbolic-attribute oracle shows that CoT can improve counting once ground-truth attributes are supplied as text, while a single-object probe-vs-decode check shows that hard attributes can be linearly recoverable from hidden states yet difficult for the model itself to output. Together, these analyses place the bottleneck at readout rather than counting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Access Boundaries in Vision-Language Model Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**思维链（CoT）提示在视觉语言模型（VLM）推理中的作用机制**。
*   **研究动机**：虽然 CoT 能显著提升 VLM 的推理表现，但其背后的原理尚不明确。研究者试图回答：CoT 的性能提升是因为模型在长推理过程中需要**持续、直接地访问图像信息**，还是因为它在**语言侧对早期已提取的视觉隐藏信息**进行了更深度的计算？
*   **背景**：以往研究多通过观察注意力权重来推测视觉信息的使用情况，但注意力低并不代表不需要。本研究通过**因果干预**的方法，试图界定视觉信息访问的“必要性边界”。

### 2. 论文提出的方法论
论文引入了**视觉访问扫描（Visual Access Sweep）**和**视觉访问边界（VAB）**的概念：
*   **核心思想**：通过在 Transformer 的注意力机制中实施因果干预，屏蔽（Mask）掉生成 Token 对图像 Token 的访问。
*   **关键技术细节**：
    *   **双轴干预**：在“层深度（Layer Depth, $\ell$）”和“生成时间（Generation Time, $t$）”两个维度进行扫描。
    *   **注意力屏蔽**：对于超过设定阈值 $(\ell_{cutoff}, t_{cutoff})$ 的区域，将生成 Token 查询（Query）到图像 Token 键（Key）的注意力权重设为负无穷（即 Softmax 后为 0）。
    *   **VAB 定义**：在保证任务准确率下降不超过容差 $\epsilon$（默认为 5%）的前提下，模型所需的最小视觉访问区域。
*   **算法流程**：通过改变屏蔽的起始层和起始 Token 位置，观察模型准确率的变化曲线，从而确定 VAB。

### 3. 实验设计
*   **数据集/场景**：
    1.  **受控属性计数任务**：程序化生成的合成图像，包含颜色、形状、位置、角度、尺寸 5 种属性，旨在排除外部知识干扰，固定推理操作（计数），仅改变感知难度。
    2.  **GQA 真实图像任务**：从 GQA 数据集中提取的 Yes/No 问答对，用于验证结论在真实场景下的泛化性。
*   **Benchmark 与对比方法**：
    *   **对比模式**：直接回答（Direct） vs. 思维链（CoT）。
    *   **对比模型**：Qwen2.5-VL (3B, 7B, 32B)、InternVL3 (8B, 14B, 38B) 以及 Llama-3.2-11B-Vision（交叉注意力架构）。
    *   **辅助实验**：符号属性预言机（Oracle Bypass）、单对象探测（Probe）与解码（Decode）对比。

### 4. 资源与算力
论文详细列出了实验所用的算力资源：
*   **硬件**：使用 3 台 NVIDIA H100 NVL (96 GB) 节点。
*   **算力消耗**：
    *   主实验扫描：约 1100 GPU 小时。
    *   初步实验与控制实验：消耗了约同等规模的额外算力。
*   **模型状态**：所有实验均在冻结的公共 VLM 检查点上进行推理干预，未进行微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个模型家族、7 种不同规模的模型配置。
*   **充分性**：
    *   **多维度验证**：不仅做了主干任务，还进行了“空槽控制（Null-sink）”和“后期查询屏蔽”等消融实验，排除了干预手段本身带来的伪影干扰。
    *   **因果性强**：通过 Oracle 实验（直接给文本属性）证明了推理能力的瓶颈在于感知读取而非逻辑计数。
*   **客观性**：实验设计严谨，通过固定推理程序（计数）来隔离变量，确保了对比的公平性。

### 6. 主要结论与发现
1.  **存在有限的视觉访问边界（VAB）**：无论是 Direct 还是 CoT，模型都只需要在前 $N$ 层访问图像 Token。超过该边界后，屏蔽图像访问几乎不影响准确率。
2.  **CoT 并不依赖延长的视觉访问**：尽管 CoT 生成的 Token 数量是 Direct 的数十倍，但其 VAB 在层深度上与 Direct 非常接近（通常差异在 2 层以内）。这表明 **CoT 的增益主要源于语言侧对已提取信息的处理，而非反复“看图”**。
3.  **感知读取是核心瓶颈**：CoT 的有效性取决于模型能否将视觉属性可靠地转化为语言符号。如果模型无法正确“读取”属性（如角度、尺寸），即使内部隐藏状态含有该信息（通过 Probe 证明），CoT 也无法提升性能。
4.  **Oracle 验证**：一旦通过文本提供真实属性，CoT 在所有属性上的计数准确率均大幅提升，证明了 VLM 的逻辑推理能力（计数）通常优于其感知读取能力。

### 7. 优点（亮点）
*   **方法论创新**：提出了一种新颖的因果干预框架（Visual Access Sweep），将观测性问题转化为因果性结论，比单纯分析注意力权重更具说服力。
*   **受控实验设计**：通过合成数据集精准控制变量，成功隔离了“感知难度”与“推理难度”。
*   **实用启发**：提出了“选择性 CoT 路由”策略，即仅在感知读取可靠时使用 CoT，可节省约 50% 的 Token 成本而不损失太多精度。

### 8. 不足与局限
*   **任务覆盖范围**：实验主要集中在计数和简单的视觉问答上。对于需要“迭代视觉搜索”或“动态视觉反馈”的极复杂任务（如寻找微小差异），该结论是否成立尚待验证。
*   **机制解释**：虽然确定了 VAB 的存在，但尚未完全揭示视觉信息是如何在 VAB 之后的层中通过残差流（Residual Stream）传递和保持的。
*   **架构限制**：主要针对 Decoder-only 的前缀融合架构，虽然对交叉注意力模型做了初步测试，但对更复杂的、具有显式感知反馈循环的架构研究较少。

（完）
