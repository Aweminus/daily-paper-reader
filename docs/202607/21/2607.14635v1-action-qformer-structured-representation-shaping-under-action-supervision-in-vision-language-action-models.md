---
title: "Action QFormer: Structured Representation Shaping under Action Supervision in Vision-Language-Action Models"
title_zh: Action QFormer：视觉-语言-动作模型中动作监督下的结构化表示塑造
authors: "Yufeng Ji, Wenhao Tang, Haoyi Niu, Koushil Sreenath, Yi Wu, Zhongyu Li"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14635v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于动作预测的视觉-语言-动作 (VLA) 模型
tldr: 针对视觉-语言-动作（VLA）模型中动作监督可能破坏原有表征稳定性的问题，本文提出了 Action QFormer。这是一种基于查询的动作接口，通过指令调节的查询将多模态信息重组为面向动作的表征。该方法在零样本仿真到现实的导航任务中表现优异，显著提升了任务成功率和指令遵循的准确性，为优化 VLA 模型中的信息组织与表征塑造提供了新思路。
selection_source: fresh_fetch
motivation: 直接将动作监督应用于多模态路径会破坏支持语言处理和物体定位的表征稳定性。
method: 提出 Action QFormer 接口，利用指令条件查询在动作生成前对多模态信息进行结构化重组。
result: "在零样本导航任务中，成功率从 18.8% 提升至 56.3%，并显著提高了指令生成的准确性。"
conclusion: 提升 VLA 性能的关键在于优化多模态信息的组织方式，并有效控制动作监督对表征的塑造过程。
---

## 摘要
视觉-语言-动作（VLA）模型中的动作监督通常被视为学习动作预测的下游目标。在本文中，我们将其研究为一种塑造继承的多模态表示的力量。我们表明，这种塑造具有双重效应：它对于形成动作兼容的表示是必要的，但当动作监督过于直接地应用于继承的多模态路径时，它也可能破坏支持语言侧处理和物体基准（object grounding）的表示的稳定性。为了解决这种张力，我们引入了 Action QFormer，这是一种基于查询的面向动作的接口，它使用以指令为条件的查询，在下游动作生成之前将继承的多模态信息重新组织为面向动作的表示。在零样本仿真到现实（sim-to-real）导航中，Action QFormer 将平均闭环任务成功率从 18.8% 提高到 56.3%，将固定指令动作生成的正确率从 22.5% 提高到 75.5%，并几乎消除了分布外（out-of-distribution）的指令生成。进一步的分析表明，Action QFormer 改变了动作监督塑造继承的多模态表示的方式，减少了广泛的上游重写，同时保留了有针对性的、有时是建设性的动作监督自适应。这些结果表明，提高 VLA 性能不仅需要更强大的预训练骨干网络，还需要更好的方法来选择和组织继承的多模态信息，同时控制其在动作监督下的塑造方式。

## Abstract
Action supervision in vision-language-action (VLA) models is often treated as a downstream objective for learning action prediction. In this paper, we study it instead as a force that shapes inherited multimodal representations. We show that this shaping has a dual effect: it is necessary for forming action-compatible representations, but when action supervision is applied too directly to the inherited multimodal pathway, it can also destabilize representations that support language-side processing and object grounding. To address this tension, we introduce Action QFormer, a query-based action-facing interface that uses instruction-conditioned queries to reorganize inherited multimodal information into action-facing representations before downstream action generation. In zero-shot sim-to-real navigation, Action QFormer improves average closed-loop task success from 18.8% to 56.3%, raises fixed-instruction action-generation correctness from 22.5% to 75.5%, and nearly eliminates out-of-distribution instruction generations. Further analyses show that Action QFormer changes how action supervision shapes inherited multimodal representations, reducing broad upstream rewriting while preserving targeted and sometimes constructive action-supervised adaptation. These results suggest that improving VLA performance requires not only stronger pretrained backbones, but also better ways of selecting and organizing inherited multimodal information while controlling how it is shaped under action supervision.

---

## 论文详细总结（自动生成）

这篇论文《Action QFormer: Structured Representation Shaping under Action Supervision in Vision-Language-Action Models》深入探讨了视觉-语言-动作（VLA）模型在微调过程中，动作监督信号如何影响预训练表征的稳定性，并提出了一种创新的接口架构。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在构建 VLA 模型时，通常会继承预训练视觉语言模型（VLM）的强大表征。然而，直接使用动作监督（Action Supervision）进行微调是一把“双刃剑”：一方面，它是将通用表征转化为动作预测能力的必要手段；另一方面，过于直接的动作监督会破坏预训练模型中原有的语言处理和物体定位（Object Grounding）能力的稳定性，导致模型在复杂指令下的泛化性能下降。
*   **研究背景**：现有的 VLA 模型往往将动作预测视为简单的下游任务，忽略了动作监督对底层多模态路径的“重塑”效应。

### 2. 论文提出的方法论
*   **核心思想**：引入一个名为 **Action QFormer** 的中间接口，在多模态骨干网络和动作生成头之间建立一个缓冲层。其目的是在不破坏骨干网络原有表征的前提下，通过结构化的方式提取并重组面向动作的信息。
*   **关键技术细节**：
    *   **指令条件查询（Instruction-conditioned Queries）**：使用一组可学习的查询向量，这些向量以任务指令为条件。
    *   **结构化重组**：Action QFormer 通过交叉注意力机制（Cross-Attention），从骨干网络输出的多模态特征中筛选出与当前动作相关的视觉和语言信息。
    *   **解耦重塑**：通过这种基于查询的机制，动作监督的压力主要集中在 Action QFormer 及其后续的动作头上，从而减少了对上游预训练路径的破坏性重写。

### 3. 实验设计
*   **场景与任务**：主要针对**零样本仿真到现实（Sim-to-Real）的导航任务**。机器人需要根据复杂的自然语言指令在未见过的真实室内环境中移动。
*   **Benchmark**：
    *   **闭环任务成功率**：衡量机器人在真实环境中的实际表现。
    *   **固定指令动作生成正确率**：评估模型对特定指令的理解与动作匹配度。
    *   **分布外（OOD）指令生成分析**：检测模型在面对未见指令时是否会产生崩溃或乱码。
*   **对比方法**：主要对比了标准的 VLA 微调方法（即直接在 VLM 骨干网络后接动作头并进行全量或部分微调）。

### 4. 资源与算力
*   **算力说明**：论文摘要和核心段落中未明确列出具体的 GPU 型号、数量或总训练时长。通常此类 VLA 研究涉及大规模视觉语言模型的微调，可能需要 A100 或 H100 等级别的计算资源，但具体数值需参考论文的实验设置附录（原文提取文本中未包含此细节）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大量的仿真训练和真实世界的零样本迁移测试。
    *   **成功率提升显著**：平均闭环成功率从 18.8% 提升至 56.3%。
    *   **消融与分析**：论文不仅对比了最终性能，还深入分析了表征空间的演变，证明了 Action QFormer 确实减少了对上游权重的“广泛重写”。
*   **充分性评价**：实验设计较为充分，特别是在 Sim-to-Real 的严苛环境下验证了方法的鲁棒性。通过对指令生成质量的定量分析，客观地展示了该方法在保持语言理解能力方面的优势。

### 6. 主要结论与发现
*   **动作监督的副作用**：直接的动作监督会导致预训练表征的“坍缩”，尤其是在处理复杂指令和物体关联时。
*   **接口的重要性**：Action QFormer 能够有效缓解上述问题，它通过“有针对性的自适应”代替了“盲目的全局重写”。
*   **性能飞跃**：在零样本导航任务中，该方法显著提高了成功率（提升约 3 倍），并几乎消除了模型在处理分布外指令时产生的异常输出。

### 7. 优点
*   **架构创新**：不同于单纯增加数据量，该方法从信息流组织的角度解决了 VLA 训练中的稳定性问题。
*   **泛化能力强**：在 Sim-to-Real 任务中表现优异，证明了其提取的特征具有更好的通用性和抗噪性。
*   **理论深度**：对动作监督如何重塑表征进行了深入的定性与定量分析，为后续 VLA 研究提供了理论参考。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在导航任务上，尚未在更复杂的机械臂操纵（Manipulation）任务中广泛验证。
*   **计算开销**：虽然保护了骨干网络，但引入额外的 QFormer 结构会增加模型的参数量和推理时的计算复杂度。
*   **超参数敏感性**：查询向量的数量和初始化方式可能对最终效果有较大影响，论文中对这一部分的鲁棒性讨论相对较少。

（完）
