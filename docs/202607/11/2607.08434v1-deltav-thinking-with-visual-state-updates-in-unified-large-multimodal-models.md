---
title: "DeltaV: Thinking with Visual State Updates in Unified Large Multimodal Models"
title_zh: DeltaV：在统一大多模态模型中利用视觉状态更新进行思考
authors: "Pengjie Wang, Linger Deng, Zujia Zhang, Shaojie Zhang, Zhenbo Luo, Pei Fu, Jian Luan, Xiang Bai, Yuliang Liu"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08434v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 通过文本推理和中间视觉状态进行交错多模态推理
tldr: "针对统一大多模态模型在交织推理中生成全量图像导致的视觉冗余和监督稀疏问题，本文提出DeltaV模型。该模型采用视觉增量更新而非全量生成，通过预测紧凑的更新Token捕捉推理过程中的视觉变化。引入TSIM Router实现Token预算的动态分配，并构建了包含105万样本的StructCoT数据集。实验表明，DeltaV在减少55.6%视觉Token的同时提升了推理性能，在多个基准测试中超越了更大规模的模型。"
selection_source: carryover_cache
motivation: 传统模型在多模态推理中重复生成全量图像，导致计算资源浪费且难以聚焦于关键的视觉状态变化。
method: 提出基于视觉增量更新的DeltaV模型，利用TSIM Router根据视觉变化幅度自适应调整Token分配，并结合大规模StructCoT数据集进行训练。
result: "该方法在保持重建质量的前提下平均减少了55.6%的视觉Token，且在多模态推理性能上显著优于全量生成方案及同类模型。"
conclusion: 视觉增量更新范式有效提升了多模态大模型的推理效率与准确性，是实现高效交织多模态推理的有力途径。
---

## 摘要
当前的统一大多模态模型（ULMMs）通过文本推理和中间视觉状态支持交错式多模态推理，但通常将每个视觉状态生成为一张完整的图像。这种全图生成范式引入了大量的视觉 token 冗余，并稀释了对稀疏但对推理至关重要的状态转换的监督。我们提出了 DeltaV，这是一种将全图生成替换为视觉更新的 ULMM。以历史视觉状态为条件，DeltaV 增量式地预测紧凑的更新 token，以捕捉推理步骤中的视觉变化，从而避免对未改变内容的重复建模。为了使每次更新的 token 预算与视觉变化的幅度相匹配，DeltaV 引入了时间相似性（TSIM）路由器，一旦边际重建增益低于阈值，该路由器就会停止分配 token。为了支持更多样化和更具泛化能力的推理，我们进一步构建了 StructCoT，这是一个包含 105 万个样本、涵盖 44 个任务领域的大规模交错式多模态推理数据集。实验表明，视觉更新范式在不损害重建保真度的情况下，平均减少了 55.6% 的新生成视觉 token，并比全图生成在多模态推理性能上提升了 3.3%。通过 StructCoT 和大规模多模态数据训练，DeltaV-2B 在域内多模态推理评估中比规模大得多的开源模型高出 8.4%，并在外部多模态推理和理解基准测试中比同等规模的 Qwen3-VL-2B 高出 5.9%。代码、模型和 StructCoT 将在 https://github.com/Pengjie-W/DeltaV 发布。

## Abstract
Current Unified Large Multimodal Models (ULMMs) support interleaved multimodal reasoning through textual reasoning and intermediate visual states, but typically generate each visual state as a full image. This full-image generation paradigm introduces substantial visual-token redundancy and dilutes supervision on sparse yet reasoning-critical state transitions. We propose DeltaV, a ULMM that replaces full-image generation with visual updates. Conditioned on historical visual states, DeltaV incrementally predicts compact update tokens that capture the visual changes across reasoning steps, avoiding repeated modeling of unchanged content. To align the token budget of each update with the magnitude of visual change, DeltaV introduces a temporal similarity (TSIM) Router, which stops allocating tokens once the marginal reconstruction gain falls below a threshold. To support more diverse and generalizable reasoning, we further construct StructCoT, a large-scale interleaved multimodal reasoning dataset with 1.05M samples spanning 44 task domains. Experiments show that the visual-update paradigm reduces newly generated visual tokens by 55.6\% on average without compromising reconstruction fidelity, and improves multimodal reasoning by 3.3\% over full-image generation. Trained with StructCoT and large-scale multimodal data, DeltaV-2B further outperforms substantially larger open-source models by 8.4\% on in-domain multimodal reasoning evaluations and surpasses the comparable-scale Qwen3-VL-2B by 5.9\% on external multimodal reasoning and understanding benchmarks. Code, models, and StructCoT will be released at https://github.com/Pengjie-W/DeltaV.

---

## 论文详细总结（自动生成）

以下是对论文《DeltaV: Thinking with Visual State Updates in Unified Large Multimodal Models》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的统一大多模态模型（ULMMs）在进行交错式多模态推理（如：文本推理 -> 视觉状态 -> 文本推理）时，通常会为每个中间步骤生成一张**完整的图像**。
*   **研究动机**：
    *   **视觉冗余**：在连续的推理步骤中，视觉状态的变化往往是局部的（例如棋盘上挪动一个棋子），生成全图会导致大量重复计算和 Token 浪费。
    *   **监督稀疏**：全图生成掩盖了关键的状态转换，使得模型难以聚焦于对推理至关重要的微小视觉变化。
    *   **效率瓶颈**：高昂的视觉 Token 开销限制了模型进行长链条、多步骤视觉推理的能力。

### 2. 方法论
DeltaV 提出了一种从“全量生成”到“增量更新”的范式转变，核心技术包括：
*   **视觉增量更新（Visual Update）**：模型不再从头生成图像，而是以历史视觉状态为条件，仅预测捕捉视觉变化的紧凑“更新 Token”。通过这种方式，模型只需建模“增量（Delta）”部分。
*   **时间相似性路由器（TSIM Router）**：
    *   **动态预算分配**：引入 TSIM Router 根据视觉变化的剧烈程度自适应调整 Token 数量。
    *   **早停机制**：当新增 Token 带来的边际重建增益低于预设阈值时，路由器会停止分配 Token，从而在保证质量的前提下最大限度压缩开销。
*   **StructCoT 数据集构建**：
    *   为了训练这种能力，研究者构建了包含 **105 万个样本**的大规模数据集，涵盖 44 个任务领域。
    *   该数据集将复杂的推理任务结构化为“文本-视觉-文本”的交错链条，为模型提供密集的视觉状态转换监督。

### 3. 实验设计
*   **数据集与场景**：
    *   **域内评估**：使用 StructCoT-Eval 评估模型在结构化多模态推理上的表现。
    *   **外部基准**：在 MMMU（综合理解）、MathVista（数学推理）、AI2D（图表理解）等主流多模态基准上进行测试。
*   **对比方法**：
    *   **全量生成基准**：对比了采用全图生成模式的同架构模型。
    *   **开源 SOTA 模型**：包括 Qwen3-VL-2B、Chameleon、Show-o 等。
*   **评估指标**：重建保真度（PSNR/SSIM）、推理准确率、视觉 Token 消耗量。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中未明确给出具体的 GPU 型号、数量及总训练时长。
*   **模型规模**：主要实验基于 **2B（20亿参数）** 规模的模型进行，旨在证明该范式在轻量级模型上的高效性。

### 5. 实验数量与充分性
*   **实验规模**：使用了 105 万样本的大规模数据集进行训练，实验覆盖了从基础理解到复杂推理的 44 个领域，样本量充足。
*   **消融实验**：对比了“全图生成”与“增量更新”的性能差异，验证了 TSIM Router 在 Token 节省上的贡献。
*   **客观性**：通过在多个第三方标准基准（如 MMMU, MathVista）上进行对比，证明了该方法的泛化能力，而非仅仅在自建数据集上有效。

### 6. 主要结论与发现
*   **高效性**：视觉增量更新范式在不损失图像重建质量的前提下，平均减少了 **55.6%** 的新生成视觉 Token。
*   **性能提升**：相比全图生成，DeltaV 在多模态推理性能上提升了 **3.3%**。
*   **竞争力**：DeltaV-2B 在域内评估中超过了规模大得多的模型（提升 8.4%），在外部基准测试中比同规模的 Qwen3-VL-2B 高出 **5.9%**。

### 7. 优点
*   **范式创新**：首次在统一多模态模型中引入“视觉增量”概念，解决了长序列推理中的冗余问题。
*   **动态自适应**：TSIM Router 实现了 Token 预算的按需分配，体现了极高的计算效率。
*   **数据贡献**：StructCoT 数据集的发布为多模态 CoT（思维链）研究提供了重要资源。

### 8. 不足与局限
*   **历史依赖性**：增量更新高度依赖于历史视觉状态的准确性，若早期步骤出现视觉幻觉，可能会随推理链条累积误差。
*   **架构限制**：该方法目前主要针对统一架构（Unified Models），在非统一架构（如仅有 Encoder 的 VLM）上的适用性有待验证。
*   **极端变化场景**：对于视觉场景发生剧烈、全局性改变的任务，增量更新的优势可能会减弱，甚至退化为全量生成。

（完）
