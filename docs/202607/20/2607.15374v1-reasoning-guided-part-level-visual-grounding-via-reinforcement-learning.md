---
title: Reasoning-Guided Part-Level Visual Grounding via Reinforcement Learning
title_zh: 基于强化学习的推理引导部件级视觉定位
authors: "Kazi Sajeed Mehrab, Hani Alomari, Najibul Haque Sarker, Chia-Wei Tang, Zaber Ibn Abdul Hakim, Anuj Karpatne, Chris Thomas"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15374v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过强化学习进行推理引导的定位
tldr: 针对多模态大模型在细粒度部件定位上的不足，本文提出OP-HRG策略。该方法通过“先定位整体、再定位局部”的层次化推理，并结合自我反思机制，利用强化学习框架GRPO进行分阶段奖励训练。实验表明，4B规模的模型在多个部件数据集上超越了7B模型及SAM3，展现了卓越的推理与定位能力。
selection_source: fresh_fetch
motivation: 现有模型在处理部件定位时缺乏物体-部件的层次化理解，导致在处理复杂查询时表现不佳。
method: 提出一种由粗到细的层次化反射定位策略（OP-HRG），并采用分阶段奖励的强化学习框架（GRPO）进行训练。
result: 仅4B参数的模型在PascalPart等多个部件定位基准测试中优于7B模型和SAM3，并能迁移至推理分割任务。
conclusion: 引入物体-部件层次化推理和强化学习反馈机制，能显著提升多模态模型对细粒度视觉目标的理解与定位精度。
---

## 摘要
多模态大语言模型 (MLLMs) 能够很好地根据自由形式的语言查询定位完整物体，但当查询指向部件而非物体时，其表现往往不佳。我们将此归因于缺失的物体-部件层级结构，因为部件是在与物体相同的单步操作中进行定位的。我们提出了物体-部件层级反思定位 (OP-HRG)，这是一种由粗到精的推理引导定位策略，首先定位父物体，然后定位其中的部件。随后通过自检对结果进行反思，并扩展为对预测的裁剪区域进行重新编码，以检查其正在修正的区域。我们引入了一个部件感知的 GRPO 框架，通过阶段性奖励来训练我们的流水线。以此方式训练的 4B 模型在 PascalPart、PartImageNet 和 InstructPart 数据集上优于 7B 定位大语言模型和 SAM3，并能迁移至推理分割任务。

## Abstract
Multimodal large language models (MLLMs) ground whole objects well from free-form language queries, but they struggle when the query names a part rather than the object. We trace this to a missing object-part hierarchy, since parts are localized in the same single step used for objects. We propose Object-Part Hierarchical Reflective Grounding (OP-HRG), a coarse-to-fine reasoning-guided grounding strategy that first localizes the parent object and then the part within it. A self-check then reflects on the result, with an extension to re-encode the predicted crop to inspect the region it is correcting. We introduce a part-aware GRPO framework to train our pipeline with stage-wise rewards. A 4B model trained this way outperforms 7B grounding LLMs and SAM3 across PascalPart, PartImageNet, and InstructPart, and transfers to reasoning segmentation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reasoning-Guided Part-Level Visual Grounding via Reinforcement Learning》的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的多模态大语言模型（MLLMs）在定位完整物体（如“汽车”）方面表现出色，但在定位物体的细粒度部件（如“汽车左前轮”）时往往力不从心。
*   **核心问题**：作者认为这是因为模型缺乏“物体-部件”的层次化理解。传统模型通常尝试在单步操作中直接定位部件，忽略了部件必须依附于父物体这一逻辑前提。
*   **整体含义**：本文旨在通过引入层次化推理和强化学习（RL）反馈机制，提升模型对复杂、细粒度视觉目标的理解与定位精度。

### 2. 方法论：OP-HRG 策略与 GRPO 框架
*   **核心思想（OP-HRG）**：提出“物体-部件层次化反思定位”策略。
    1.  **由粗到精定位**：模型首先定位父物体（Object），再在父物体范围内定位具体的部件（Part）。
    2.  **自我反思机制**：模型在生成初步定位后，会进行自我检查（Self-check），判断预测的部件是否确实位于父物体内且符合描述。
    3.  **区域重编码**：为了提高精度，模型会提取预测区域的裁剪图（Crop）并重新编码，以更清晰地审视细节。
*   **关键技术细节**：
    *   **GRPO 强化学习**：采用组相对策略优化（Group Relative Policy Optimization），这是一种无需评论家（Critic）模型的 RL 算法，通过组内样本的相对得分来计算优势函数。
    *   **分阶段奖励（Stage-wise Rewards）**：
        *   **物体奖励**：鼓励模型准确识别并定位父物体。
        *   **部件奖励**：基于 IoU（交并比）评估部件定位的准确性。
        *   **反思奖励**：如果模型能正确识别自己的错误并修正，则给予奖励。
        *   **格式奖励**：确保模型输出符合预定义的推理格式。

### 3. 实验设计
*   **数据集**：
    *   **PascalPart**：经典的部件级标注数据集。
    *   **PartImageNet**：大规模部件分割数据集。
    *   **InstructPart**：包含复杂推理查询的部件定位数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **GLaMM、LISA、Shikra、UNINEXT** 等主流定位模型。
    *   对比了最新的分割模型 **SAM3**。
    *   评估指标主要采用 **mIoU**（平均交并比）和 **Accuracy**（准确率）。

### 4. 资源与算力
*   **硬件环境**：论文提到实验使用了 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练细节**：模型基于 InternVL2-4B 构建。训练过程分为监督微调（SFT）阶段和强化学习（GRPO）阶段。具体的训练时长未在摘要中详述，但通常此类规模的模型在 8 卡 A100 上需数天时间完成 RL 训练。

### 5. 实验数量与充分性
*   **实验规模**：作者在三个主流部件数据集上进行了详尽的对比实验。
*   **消融实验**：非常充分。作者分别验证了“层次化步骤”、“自我反思机制”、“区域重编码”以及“GRPO 奖励设计”对最终性能的贡献。
*   **客观性**：通过与更大参数规模（7B）的模型以及专门的分割模型（SAM3）对比，证明了该方法在参数效率上的优势，实验设计较为客观公平。

### 6. 主要结论与发现
*   **小模型胜大模型**：经过 OP-HRG 训练的 **4B 参数模型**在多个指标上超越了 **7B 参数**的竞争对手。
*   **层次化推理的必要性**：先找物体再找部件的逻辑显著降低了模型的搜索空间，减少了误报。
*   **RL 的优越性**：相比于单纯的 SFT，GRPO 强化学习能更好地对齐模型的推理逻辑与视觉定位结果，尤其是在处理“反思”和“修正”逻辑时。
*   **泛化能力**：该模型不仅在部件定位上表现优异，还能有效迁移至通用的推理分割任务中。

### 7. 优点
*   **逻辑严密**：将人类“由整体到局部”的视觉观察习惯引入模型推理过程。
*   **训练高效**：利用 GRPO 减少了对额外 Critic 模型的依赖，降低了 RL 训练的显存开销。
*   **自纠错能力**：引入反思机制，使模型具备了识别并修正自身视觉幻觉的能力。

### 8. 不足与局限
*   **推理延迟**：由于采用了“物体-部件-反思”的多步推理，且涉及图像裁剪重编码，其推理速度（Latency）可能慢于单步定位模型。
*   **依赖父物体识别**：如果模型在第一步未能正确识别父物体，可能会导致后续部件定位的连锁失败（尽管反思机制能缓解这一点）。
*   **复杂场景限制**：在物体极度密集或重叠的场景下，确定“父物体”的边界本身可能存在挑战。

（完）
