---
title: "ToolVerse: Unlocking Massive Environments and Long-Horizon Tasks for Agentic Reinforcement Learning"
title_zh: ToolVerse：为智能体强化学习解锁大规模环境与长程任务
authors: "Shuaiyu Zhou, Fengpeng Yue, Zengjie Hu, Yuanzhe Shen, Chenyang Zhang, feng hong, Cao Liu, Ke Zeng"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15660v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 扩展智能体强化学习环境和长程推理的框架
tldr: ToolVerse是一个旨在提升LLM智能体在复杂、大规模及动态环境下长程推理能力的强化学习框架。它通过整合约400个真实世界协议中的4500个工具构建大规模环境，利用工具依赖图生成长程任务数据集GUST，并提出细粒度的Turn-Aware相对优势算法解决信用分配问题，显著增强了智能体在长程工具调用中的鲁棒性与性能。
selection_source: fresh_fetch
motivation: 现有的LLM智能体在面对大规模、多样化且动态的真实世界工具集成任务时，难以保持鲁棒性和长程推理的有效性。
method: 构建了包含4500个工具的大规模环境，利用动态解锁采样算法生成GUST数据集，并提出Turn-Aware相对优势算法优化强化学习训练。
result: 实验结果显示，ToolVerse显著提升了LLM在长程工具使用任务中的表现，并在多个智能体基准测试中展现出强大的推理能力。
conclusion: ToolVerse通过规模化环境构建和针对性的强化学习算法，有效解决了智能体在复杂动态场景下的长程任务处理难题。
---

## 摘要
尽管大语言模型（LLM）智能体在紧凑且定义明确的场景中展现出强大的推理能力，但在面对需要无缝工具集成的规模化、多样化且动态的现实世界环境时，它们难以保持鲁棒性和有效性。为了填补这一空白，我们推出了 ToolVerse，这是一个综合性框架，旨在扩展智能体强化学习（Agentic RL）环境，并使智能体能够在工具集成推理（TIR）任务中执行复杂的长程推理。首先，ToolVerse 从包含约 4500 个工具的近 400 个真实世界模型上下文协议（MCP）中，自动构建了大规模的可执行智能体训练环境。其次，我们提出了一种基于工具依赖图的任务设计策略，利用动态解锁采样算法生成长程任务，并产出了 GUST（图解锁采样任务）数据集。第三，为了缓解长程智能体强化学习中的信用分配问题，我们提出了一种细粒度的轮次感知相对优势算法。我们使用 ToolVerse 进行了广泛的智能体强化学习训练，并在多个智能体基准测试上评估了我们的框架。实验结果表明，我们的框架显著增强了 LLM 在长程工具使用方面的能力，实现了显著的性能提升，并展示了在动态环境中的鲁棒推理能力。

## Abstract
While LLM agents demonstrate strong reasoning abilities in compact and well-defined scenarios, they struggle to maintain robustness and effectiveness when faced with large-scale, diverse, and dynamic real-world environments that demand seamless tool integration. To address this gap, we introduce ToolVerse, a comprehensive framework that scales up agentic RL environments and enables agents to perform complex long-horizon reasoning in Tool-Integrated Reasoning (TIR) tasks. First, ToolVerse automatically builds the massive executable agent training environments from nearly 400 real-world Model Context Protocols (MCPs) that contain about 4500 tools. Second, we propose a task design strategy based on a tool dependency graph, utilizing Dynamic Unlocking Sampling Algorithm to generate long-horizon tasks, and produce GUST (Graph Unlocking Sampling Tasks) dataset. Third, to alleviate the credit assigment problem in long-horizon agentic RL, we propose a fine-grained Turn-Aware Relative Advantage algorithm. We conduct extensive Agentic RL training using ToolVerse and evaluate our framework on serveral agentic benchmarks. Experimental results demonstrate that our framework significantly strengthens LLMs' capabilities in long-horizon tool use, achieving a marked performance boost and showcasing robust reasoning within dynamic environments.

---

## 论文详细总结（自动生成）

### 论文总结：ToolVerse —— 解锁大规模环境与长程任务的智能体强化学习框架

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 LLM 智能体在简单、定义明确的场景（如仅使用搜索或代码解释器）中表现良好，但在面对大规模、多样化且动态的真实世界环境时，往往缺乏鲁棒性。现有的智能体强化学习（Agentic RL）面临三大挑战：
*   **环境局限性**：训练环境通常仅限于单一或少数工具，缺乏复杂性。
*   **任务设计困难**：构建需要多步逻辑依赖、长程推理的工具集成任务非常复杂。
*   **信用分配（Credit Assignment）难题**：在多轮交互中，稀疏的终端奖励难以精准反馈中间步骤的正确性，导致训练不稳定或效率低下。

#### 2. 方法论：核心思想与关键技术
ToolVerse 提出了一个闭环框架，包含以下三个核心组件：
*   **大规模可执行环境构建**：利用模型上下文协议（MCP），自动将 422 个真实世界工具集（包含 4438 个工具）转化为可执行的 Python 环境，并配合模拟数据库（Mock Database）实现状态化交互。
*   **基于图的任务合成（GUST 数据集）**：
    *   **工具依赖图（TDG）**：利用 LLM 推断工具间的参数和语义依赖关系。
    *   **动态解锁采样算法（DUS）**：基于 TDG 的拓扑排序，确保生成的任务序列具有逻辑先后顺序（如必须先查询 ID 才能删除订单），从而产生长程推理任务。
*   **轮次感知相对优势算法（TARA）**：
    *   将奖励分解为**局部优势（Local Advantage）**（当前步骤的正确性）和**未来优势（Future Advantage）**（对后续步骤的贡献）。
    *   引入**一致性门控（Consistency Gate）**：如果当前步骤错误，则屏蔽未来的奖励流，防止智能体通过“瞎猜”获得信用。
    *   在 GRPO 算法基础上进行细粒度改进，将优势分配到每一轮次的 Token 上。

#### 3. 实验设计
*   **数据集/场景**：使用了自建的 **GUST 数据集**（包含 2987 个数据项，覆盖 8 大宏观领域）。
*   **Benchmark（基准测试）**：
    *   **BFCL-v3 Multi-Turn**：评估 Python API 交互能力。
    *   **$\tau^2$-Bench**：评估航空、零售、电信等真实领域的双控制交互。
    *   **ACEBench-Agent**：评估动态环境下的多步多轮推理。
*   **对比方法**：对比了 DeepSeek-V3.2、GPT-4.1、Kimi-K2 等先进模型，以及 ToolRL、agentflow、SimpleTIR 等开源强化学习基线方法。

#### 4. 资源与算力
*   **算力设备**：使用了由 **32 台 NVIDIA A100 GPU** 组成的集群。
*   **训练配置**：全局 Batch Size 为 128，学习率为 $1 \times 10^{-6}$。
*   **时长与规模**：虽然未明确给出总训练小时数，但详细列出了最大 Prompt 长度（8192）和响应长度（24000），显示了极高的计算负载。

#### 5. 实验数量与充分性
*   **实验规模**：在 Qwen2.5-14B、Qwen3-4B/8B 等多个参数规模的模型上进行了验证。
*   **消融实验**：非常充分。包括对 TARA 算法组件（局部 vs 未来奖励）、超参数（$\lambda, \gamma$ 灵敏度）以及环境缩放（从 100 到 422 个环境的效果）的深入探讨。
*   **客观性**：每个评估均运行 4 次并取平均值（Average@4），并使用了 DeepSeek-V3.2 作为模拟用户进行动态交互，保证了评估的公正性。

#### 6. 主要结论与发现
*   **性能显著提升**：ToolVerse 在所有基准测试中均显著优于原始模型和现有基线。例如，Qwen3-8B 在 ACEBench-Agent 上提升了 15.15 个百分点。
*   **TARA 的有效性**：相比于朴素的 GRPO，TARA 算法通过细粒度的信用分配，使模型收敛更快且最终性能更高。
*   **环境多样性的重要性**：实验证明，增加训练环境的数量（Environment Scaling）能显著增强智能体在未见任务上的泛化能力。

#### 7. 优点与亮点
*   **自动化程度高**：从工具定义到可执行环境、再到长程任务生成的全流程自动化，极大地降低了智能体训练数据的获取成本。
*   **逻辑严密**：引入图论中的拓扑约束（DUS 算法）来生成任务，确保了长程任务的因果一致性，而非随机组合。
*   **理论支撑**：对 TARA 算法进行了数学推导，证明了其在减少方差和抑制“干扰动作（Distractor）”方面的优势。

#### 8. 不足与局限
*   **依赖预定义协议**：任务生成仍依赖于现有的工具协议（MCP），可能限制了智能体发现全新工具交互模式的能力。
*   **模拟环境偏差**：虽然使用了模拟数据库，但与复杂的真实物理世界或高度动态的生产环境相比，仍存在保真度差距。
*   **奖励定义**：TARA 算法仍依赖于每轮次的规则校验（如 JSON 匹配），在极度模糊或无标准答案的开放式任务中可能难以应用。

（完）
