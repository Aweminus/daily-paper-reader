---
title: "Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR"
title_zh: 超越“看哪里”：面向多模态 RLVR 的轨迹引导强化学习
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Mingzhu Chen, Jiancan Wu, Kuien Liu, Xiang Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26126v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于多模态推理和视觉接地的强化学习
tldr: 针对多模态大语言模型在强化学习中存在的“能看到但无法有效利用视觉证据进行推理”的问题，本文提出了轨迹引导强化学习（TGRL）。该方法利用更强模型的专家推理轨迹来引导策略模型，将视觉证据融入细粒度的推理过程中。通过引入标记级重加权和轨迹过滤技术，TGRL 在多个基准测试上显著提升了推理性能，有效弥合了视觉感知与逻辑推理之间的鸿沟。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习方法虽然提升了视觉定位能力，但模型往往无法将获取的视觉事实有效转化为后续的逻辑推理步骤。
method: 提出 TGRL 框架，利用专家轨迹引导模型进行细粒度推理，并结合标记级重加权和轨迹过滤来优化策略。
result: 在多个多模态推理基准测试上的实验表明，TGRL 持续提升了模型的推理表现并增强了推理链的逻辑性。
conclusion: TGRL 成功增强了模型整合视觉证据的能力，为解决多模态推理中的感知与逻辑脱节问题提供了有效方案。
---

## 摘要
多模态大语言模型（MLLM）中具有可验证奖励的强化学习（RLVR）的最新进展主要集中在提高最终答案的正确性和增强视觉定位。然而，一个关键瓶颈仍然存在：尽管模型能够关注相关的视觉区域，但它们往往无法有效地将视觉证据整合到后续的推理中，导致推理链与视觉事实的关联较弱。为了解决这一问题，我们提出了轨迹引导强化学习（TGRL），该方法利用来自更强模型的专家推理轨迹，引导策略模型将视觉证据整合到细粒度的推理过程中。我们进一步引入了标记级重加权和轨迹过滤，以确保稳定且有效的策略优化。在多个多模态推理基准上的广泛实验表明，TGRL 持续提升了推理性能，并有效地弥合了视觉感知与逻辑推理之间的差距。

## Abstract
Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) for multimodal large language models (MLLMs) have mainly focused on improving final answer correctness and strengthening visual grounding. However, a critical bottleneck remains: although models can attend to relevant visual regions, they often fail to effectively incorporate visual evidence into subsequent reasoning, leading to reasoning chains that are weakly grounded in visual facts. To address this issue, we propose Trajectory-Guided Reinforcement Learning (TGRL), which guides the policy model to integrate visual evidence into fine-grained reasoning processes using expert reasoning trajectories from stronger models. We further introduce token-level reweighting and trajectory filtering to ensure stable and effective policy optimization. Extensive experiments on multiple multimodal reasoning benchmarks demonstrate that TGRL consistently improves reasoning performance and effectively bridges the gap between visual perception and logical reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在多模态大语言模型（MLLM）中，带有可验证奖励的强化学习（RLVR，如 DeepSeek-R1 风格的训练）已成为提升推理能力的有效手段。
*   **核心问题**：现有的多模态 RLVR 存在“感知与推理脱节”的瓶颈。模型虽然能通过视觉定位（Visual Grounding）看到相关区域，但往往无法将视觉证据正确转化为逻辑推理步骤，导致推理链虽然看起来在“思考”，却基于错误的视觉事实。
*   **研究动机**：现有的改进方法（如图像增强或感知奖励）只解决了“看哪里”的问题，而没有解决“如何利用看到的证据进行推理”的问题。

### 2. 论文提出的方法论：TGRL
论文提出了**轨迹引导强化学习（Trajectory-Guided Reinforcement Learning, TGRL）**，核心思想是利用更强模型的专家推理轨迹来对齐“感知-推理”的转换过程。

*   **核心技术细节**：
    1.  **轨迹集成（Trajectory Integration）**：在 RL 的 Rollout 阶段，将当前策略生成的“在策略（On-policy）”轨迹与专家模型生成的“离策略（Off-policy）”轨迹混合，构建联合 Rollout 组。
    2.  **轨迹重加权（Trajectory Reweighting）**：
        *   **重要性采样修正**：处理专家轨迹与当前策略之间的分布差异。
        *   **自适应标记级权重**：引入差异度量（Discrepancy），对学生模型预测概率远低于专家模型的 Token 赋予更高权重，强化关键推理步骤的学习。
    3.  **轨迹过滤（Trajectory Filtering）**：仅保留最终答案正确的专家轨迹，过滤掉可能存在的错误引导信号，确保模型学习的是正确的推理路径。
*   **算法流程**：将上述机制集成到 GRPO 或 DAPO 等主流 RL 算法中，通过修改优势函数（Advantage）的计算范围和 Token 级的重要性权重，实现专家引导与自主探索的平衡。

### 3. 实验设计
*   **训练数据集**：
    *   **Geo3K**：包含 2.1K 几何推理样本。
    *   **ViRL39K**：包含 39K 多模态推理实例的大规模数据集。
*   **评估基准（Benchmarks）**：
    *   **推理能力**：MathVista, WeMath, MathVision, MathVerse。
    *   **感知/幻觉**：HallusionBench。
*   **对比方法**：
    *   基础模型：Qwen2.5-VL-7B。
    *   RL 算法：GRPO, DAPO。
    *   SOTA 方法：VLAA-Thinker, Perception-R1, ThinkLite, NoisyRollout, R1-Onevision 等。

### 4. 资源与算力
*   **模型配置**：
    *   **学生策略模型**：Qwen2.5-VL-3B 和 Qwen2.5-VL-7B。
    *   **专家模型**：Qwen2.5-VL-32B（用于生成引导轨迹）。
*   **训练细节**：
    *   Geo3K 训练 15 个 Epoch，ViRL39K 训练 2 个 Epoch。
    *   使用 EasyR1 训练框架。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 A100/H100）及具体数量，但从模型规模（7B/32B）和 RL 训练特性来看，通常需要大规模 GPU 集群支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2.1K 到 39K 不同规模的训练数据，并在 5 个主流基准上进行了测试。
*   **消融实验**：针对轨迹过滤（Filter）、重加权（Reweight）以及超参数 $\beta$（控制权重敏感度）进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分。通过在 GRPO 和 DAPO 两种不同的 RL 框架上应用 TGRL 并均取得提升，证明了该方法的通用性。对比对象包含了最新的 R1 类多模态模型，具有较强的时效性和公平性。

### 6. 论文的主要结论与发现
*   **性能提升**：TGRL 在所有测试基准上均显著优于原始的 GRPO 和 DAPO。例如，在 39K 数据规模下，TGRL-DAPO 的平均分从 58.15 提升至 60.68。
*   **弥合鸿沟**：实验证明，仅仅提供最终答案的奖励是不够的，通过专家轨迹引导细粒度的推理过程，能有效减少“感知正确但推理错误”的情况。
*   **专家模型的影响**：即使专家模型（32B）并非顶级闭源模型（如 Gemini 2.5 Pro），TGRL 依然能通过结构化的引导显著提升学生模型的能力。

### 7. 优点
*   **切中痛点**：精准识别了多模态推理中“感知到推理转换”这一关键瓶颈。
*   **机制优雅**：通过 Token 级的重加权和过滤，巧妙地解决了离策略专家数据引入时的分布偏移和噪声问题。
*   **通用性强**：作为一种插件式的改进，可以轻松集成到现有的各种基于组的策略优化算法中。

### 8. 不足与局限
*   **专家依赖**：该方法高度依赖于一个更强的专家模型来提供高质量轨迹。如果专家模型本身在某些复杂场景下存在偏见或错误，可能会限制上限。
*   **计算开销**：引入专家轨迹和额外的 Token 级计算会增加训练时的显存占用和计算复杂度。
*   **自我进化潜力**：目前主要依赖外部专家，未来是否能通过模型“自我生成+自我筛选”实现闭环进化（类似纯文本 R1 的冷启动后阶段）尚待探索。

（完）
