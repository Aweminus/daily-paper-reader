---
title: "DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation"
title_zh: DyGeoVLN：将动态几何基础模型注入视觉语言导航
authors: "Xiangchen Liu, Hanghan Zheng, Jeil Jeong, Minsung Yoon, Lin Zhao, Zhide Zhong, Haoang Li, Sung-Eui Yoon"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21269v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 导航代理的视觉语义推理
tldr: 针对视觉语言导航（VLN）在动态现实场景中泛化能力不足的问题，本文提出DyGeoVLN框架。该框架通过将动态几何基础模型融入VLN，利用跨分支特征融合实现显式的3D空间表示和视觉语义推理。此外，引入了无位姿、自适应分辨率的令牌剪枝策略，有效降低了长程导航的推理成本。实验证明该方法在多个基准测试中达到SOTA，并具有极强的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLN方法多基于静态场景假设，难以应对动态且复杂的现实环境。
method: 提出DyGeoVLN框架，通过融合动态几何基础模型增强3D空间感知，并采用自适应令牌剪枝策略优化长程导航效率。
result: 在多个主流基准测试上取得了最先进的性能，并在真实世界环境中表现出显著的鲁棒性。
conclusion: 将动态几何感知与高效的令牌管理相结合，是提升视觉语言导航智能体在动态场景中表现的有效途径。
---

## 摘要
视觉语言导航（VLN）要求智能体理解视觉观测和语言指令，以便在未知环境中进行导航。大多数现有方法依赖于静态场景假设，难以泛化到动态的现实场景中。为了应对这一挑战，我们提出了 DyGeoVLN，一个动态几何感知的 VLN 框架。我们的方法通过跨分支特征融合将动态几何基础模型注入 VLN 框架，从而实现显式的 3D 空间表示和视觉语义推理。为了在长时程、动态导航中高效压缩历史 token 信息，我们进一步引入了一种新颖的无位姿且自适应分辨率的 token 剪枝策略。该策略可以去除时空冗余 token，以降低推理成本。大量的实验表明，我们的方法在多个基准测试上达到了最先进的性能，并在现实环境中表现出强大的鲁棒性。

## Abstract
Vision-language Navigation (VLN) requires an agent to understand visual observations and language instructions to navigate in unseen environments. Most existing approaches rely on static scene assumptions and struggle to generalize in dynamic, real-world scenarios. To address this challenge, we propose DyGeoVLN, a dynamic geometry-aware VLN framework. Our method infuses a dynamic geometry foundation model into the VLN framework through cross-branch feature fusion to enable explicit 3D spatial representation and visual-semantic reasoning. To efficiently compress historical token information in long-horizon, dynamic navigation, we further introduce a novel pose-free and adaptive-resolution token-pruning strategy. This strategy can remove spatio-temporal redundant tokens to reduce inference cost. Extensive experiments demonstrate that our approach achieves state-of-the-art performance on multiple benchmarks and exhibits strong robustness in real-world environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《DyGeoVLN: Infusing Dynamic Geometry Foundation Model into Vision-Language Navigation》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
视觉语言导航（VLN）要求智能体在未知环境中根据指令移动。现有的多模态大语言模型（MLLM）虽然在语义理解上表现出色，但存在两个核心痛点：
*   **缺乏3D空间推理能力**：大多数模型基于2D图像-文本预训练，难以在长程轨迹中保持全局一致的3D空间感知，尤其是在包含行人等动态物体的复杂场景中，静态几何假设失效。
*   **Token 爆炸与冗余**：长程导航会积累海量视觉 Token，导致推理成本激增。现有方法要么简单下采样（丢失细节），要么依赖模拟器提供的完美位姿和深度（难以在现实硬件中部署）。

**DyGeoVLN** 的核心意义在于通过引入“动态几何基础模型”，将显式的3D空间感知与MLLM的语义能力深度融合，并提出了一种不依赖外部定位系统的 Token 压缩策略。

### 2. 方法论：核心思想与关键技术
该框架由三个核心组件构成：

*   **动态几何基础模型 (DGFM)**：
    *   **核心思想**：将单目深度估计（Depth Anything）与前馈几何编码器结合。
    *   **技术细节**：通过“点图 Transformer”提取多级3D嵌入，并利用“零均值卷积（Zero-mean Conv）”将几何特征注入视觉骨干网络。这种设计允许模型在保留预训练语义知识的同时，学习动态场景的3D结构。
    *   **分层解码**：专门的解码器头分别预测相机位姿、局部点云和全局几何 Token。
*   **跨分支特征融合架构**：
    *   采用双分支设计：2D语义分支（基于 Qwen2-VL）和3D空间分支（DGFM）。
    *   通过**多头交叉注意力机制**将空间 Token 融合进视觉 Token，使 LLM 能够同时处理“是什么（语义）”和“在哪里（几何）”。
*   **自适应分辨率与占用感知 Token 剪枝**：
    *   **无位姿（Pose-free）**：利用 DGFM 自身推断的位姿和点云进行体素化，不依赖外部传感器。
    *   **自适应分辨率**：近处物体使用细粒度体素，远处使用粗粒度体素。
    *   **重要性补偿**：引入重要性评分机制，确保即使在稀疏区域也能保留关键的几何/语义 Token。

### 3. 实验设计
*   **数据集与基准**：
    *   **动态基准**：HA-VLN（包含多人类交互、社交建模）。
    *   **静态基准**：R2R-CE（连续环境下的经典 VLN 测试）。
    *   **自建数据集**：**DyHM3D**，包含 50,000 条以人类为中心的动态 3D 轨迹，用于训练 DGFM。
*   **对比方法**：包括传统的 VLN 模型（如 VLN-CMA）、基于 MLLM 的模型（NaVid, Uni-NaVid, NaVILA, StreamVLN）以及基于 3D 特征场的模型（g3D-LF）。
*   **现实世界验证**：使用 Unitree Go1 四足机器人，在走廊、大厅、拥挤房间等真实场景中进行 20 个回合的实测。

### 4. 资源与算力
*   **模型基础**：视觉编码器和 LLM 初始化自 Qwen2-VL。
*   **训练算法**：使用了 DAgger（Dataset Aggregation）算法来增强模型的泛化能力。
*   **硬件部署**：现实世界推理在本地服务器运行，机器人端使用 NVIDIA Jetson Orin Nano 进行感知数据打包和低级控制（MPC 控制器）。
*   **说明**：文中**未明确给出**具体的 GPU 训练数量、具体型号及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从仿真（静态+动态）到现实世界的全流程验证。
*   **消融实验**：针对“视觉语义分支”、“空间几何分支”、“动态空间注入”和“Token 剪枝”分别做了对比实验，验证了各组件的必要性。
*   **客观性**：在动态基准 HA-VLN 上，SR（成功率）提升了约 12%-13%，CR（碰撞率）降低了约 10%-11%，数据提升显著且对比了最新的 SOTA 模型（如 2025 年的 StreamVLN），具有较强的时效性和公平性。

### 6. 主要结论与发现
*   **几何感知的必要性**：显式注入 3D 几何信息能显著提升智能体在动态环境中的避障能力和路径遵循精度。
*   **动态鲁棒性**：DGFM 在处理人体运动引起的几何畸变方面远优于 π3 和 VGGT 等现有几何模型。
*   **效率与性能的平衡**：提出的 Token 剪枝策略在减少计算量的同时，通过保留关键空间特征，反而提升了导航成功率（SR 提升 3%），证明了“去粗取精”的有效性。

### 7. 优点：亮点总结
*   **动态场景突破**：通过自建 DyHM3D 数据集和 DGFM 模型，解决了 VLN 在动态人类环境中的痛点。
*   **解耦传感器依赖**：实现了“无位姿”剪枝，仅需单目 RGB 即可完成复杂的空间占用推理，极大地降低了硬件部署门槛。
*   **端到端融合**：将几何基础模型与 MLLM 深度耦合，而非简单的插件式组合，实现了更高维度的时空推理。

### 8. 不足与局限
*   **深度估计依赖**：模型性能高度依赖于单目深度估计（Depth Anything）的准确性，在极端光照或无纹理区域可能失效。
*   **计算延迟风险**：虽然有 Token 剪枝，但 MLLM 本身的推理延迟对于高速移动的机器人仍是一个挑战（文中未详细讨论实时帧率）。
*   **训练成本**：虽然未披露具体算力，但融合两个基础模型并进行 DAgger 训练，其计算开销显然高于传统的轻量级导航模型。

（完）
