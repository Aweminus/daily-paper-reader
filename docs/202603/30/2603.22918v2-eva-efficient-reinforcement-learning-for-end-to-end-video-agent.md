---
title: "EVA: Efficient Reinforcement Learning for End-to-End Video Agent"
title_zh: EVA：面向端到端视频智能体的高效强化学习
authors: "Yaolun Zhang, Ruohui Wang, Jiahao Wang, Yepeng Tang, Xuanyu Zheng, Haonan Duan, Hao Lu, Hanming Deng, Lewei Lu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22918v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于端到端视频智能体推理的强化学习
tldr: 针对多模态大模型在长视频理解中存在的冗余和效率问题，本文提出了EVA框架。EVA是一个基于强化学习的端到端视频智能体，通过“先规划后感知”的迭代推理机制（总结-规划-行动-反思），实现自主、高效的视频理解。通过SFT、KTO和GRPO三阶段训练，EVA在六个基准测试中表现优异，显著提升了视频理解的准确性与效率。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在处理长视频时存在计算冗余且缺乏自适应推理能力，导致理解效率低下。
method: 提出EVA框架，采用“先规划后感知”的迭代推理模式，并结合SFT、KTO和GRPO三阶段强化学习流水线进行训练。
result: "在六个视频理解基准测试中，EVA比通用大模型提升了6-12%，比现有的自适应智能体方法提升了1-3%。"
conclusion: EVA证明了通过强化学习训练的端到端视频智能体能够实现更高效、更精准的查询驱动型视频理解。
---

## 摘要
由于视频包含广泛的时间依赖性和冗余帧，导致其 Token 序列过长，使用多模态大语言模型（MLLM）进行视频理解仍然具有挑战性。现有方法通常将 MLLM 视为被动识别器，在没有自适应推理的情况下处理整个视频或均匀采样的帧。最近基于智能体的方法引入了外部工具，但仍依赖于人工设计的工作流和感知优先策略，导致在长视频上的效率低下。我们提出了 EVA，这是一个面向端到端视频智能体的高效强化学习框架，它通过迭代的“总结-计划-行动-反思”推理实现了“先计划后感知”。EVA 自主决定看什么、何时看以及如何看，从而实现了查询驱动且高效的视频理解。为了训练此类智能体，我们设计了一个简单而有效的三阶段学习流水线——包括监督微调（SFT）、Kahneman-Tversky 优化（KTO）和群体相对策略优化（GRPO）——从而桥接了监督模仿学习和强化学习。我们进一步为每个阶段构建了高质量的数据集，支持稳定且可复现的训练。我们在六个视频理解基准测试上评估了 EVA，展示了其全面的能力。与现有基准相比，EVA 在通用 MLLM 基准上实现了 6-12% 的显著提升，并比之前的自适应智能体方法进一步提高了 1-3%。

## Abstract
Video understanding with multimodal large language models (MLLMs) remains challenging due to the long token sequences of videos, which contain extensive temporal dependencies and redundant frames. Existing approaches typically treat MLLMs as passive recognizers, processing entire videos or uniformly sampled frames without adaptive reasoning. Recent agent-based methods introduce external tools, yet still depend on manually designed workflows and perception-first strategies, resulting in inefficiency on long videos. We present EVA, an Efficient Reinforcement Learning framework for End-to-End Video Agent, which enables planning-before-perception through iterative summary-plan-action-reflection reasoning. EVA autonomously decides what to watch, when to watch, and how to watch, achieving query-driven and efficient video understanding. To train such agents, we design a simple yet effective three-stage learning pipeline - comprising supervised fine-tuning (SFT), Kahneman-Tversky Optimization (KTO), and Group Relative Policy Optimization (GRPO) - that bridges supervised imitation and reinforcement learning. We further construct high-quality datasets for each stage, supporting stable and reproducible training. We evaluate EVA on six video understanding benchmarks, demonstrating its comprehensive capabilities. Compared with existing baselines, EVA achieves a substantial improvement of 6-12% over general MLLM baselines and a further 1-3% gain over prior adaptive agent methods.

---

## 论文详细总结（自动生成）

以下是对论文《EVA: Efficient Reinforcement Learning for End-to-End Video Agent》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的多模态大模型（MLLM）在视频理解中通常充当“被动识别器”，即直接处理整段视频或均匀采样的帧。这种方式在处理长视频时存在两个主要痛点：
*   **计算冗余**：长视频包含大量无关帧，全量处理会导致 Token 序列过长，消耗极高算力。
*   **感知盲目**：均匀采样可能遗漏关键瞬间，且模型在未进行逻辑规划前就进行视觉感知，容易被噪声干扰。
**EVA 的核心目标**是将 MLLM 转化为“主动智能体”，通过“先规划后感知”的模式，自主决定看什么、何时看以及如何看，从而实现高效、精准的长视频理解。

### 2. 方法论：核心思想与关键技术
EVA 提出了一个基于强化学习的端到端框架，其核心流程如下：
*   **“先规划后感知”范式**：模型在接触视觉输入前，先根据用户查询（Query）进行推理，制定观察计划。
*   **迭代推理循环**：采用“总结（Summary）— 规划（Plan）— 行动（Action）— 反思（Reflection）”的闭环。
    *   **工具调用**：设计了灵活的 `frame_select` 工具，允许智能体自主控制时间窗口（start/end）、采样频率（nframes）和空间分辨率（resize）。
*   **三阶段训练流水线**：
    1.  **SFT（监督微调）**：使用 10k 高质量合成数据进行“冷启动”，培养模型基本的工具调用格式和交替推理能力。
    2.  **KTO（Kahneman-Tversky 优化）**：利用 11k 成功与失败的策略轨迹，引导模型偏好有效策略，纠正“盲目猜测”等错误行为，为后续强化学习提供稳定的行为先验。
    3.  **GRPO（群体相对策略优化）**：在线强化学习阶段，通过准确率奖励（MCQ 的 CSV 奖励和开放式问题的 ROUGE 奖励）和格式奖励，优化模型的推理深度与计算效率。

### 3. 实验设计
*   **数据集与基准测试**：在 6 个主流视频理解 Benchmark 上进行了评估，包括：
    *   **LSDBench**：评估采样效率与准确性的平衡。
    *   **LongVideoBench / MLVU / VideoMME / LVBench**：评估长视频理解能力。
    *   **Video-Holmes**：评估零样本（Zero-shot）视频推理能力。
    *   **ELV-Halluc**：评估语义聚合幻觉。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.0-Flash。
    *   **静态采样模型**：Qwen2.5-VL, InternVL2.5, Video-R1 等。
    *   **自适应智能体**：VideoAgent, FrameThinker, VideoMTR 等。

### 4. 资源与算力
*   **算力投入**：使用了 **32 台 NVIDIA H100 GPU**。
*   **训练细节**：
    *   **SFT**：2 个 Epoch，Batch Size 为 8，学习率 2e-6。
    *   **KTO**：学习率保持不变，beta 设为 0.1。
    *   **GRPO**：1 个 Epoch，Batch Size 为 64，每个样本生成 8 个 Rollout，学习率 1e-6。
*   **基础模型**：以 Qwen2.5-VL-7B-Instruct 为底座。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从短视频到超长视频（超过 6600 秒）的多种场景，测试了 6 个不同维度的基准测试，实验设计较为全面。
*   **消融实验**：详细对比了 SFT、KTO、GRPO 三个阶段对性能和效率（Token 消耗、推理轮数）的影响，并探讨了 GRPO 阶段数据组成（选择题 vs 开放题）的作用。
*   **客观性**：通过报告视觉 Token 消耗量与准确率的对比，客观展示了 EVA 在保持高精度的同时显著降低了计算成本。

### 6. 主要结论与发现
*   **性能领先**：EVA 在多个基准测试中超越了现有的开源模型和自适应智能体方法，相比通用 MLLM 提升了 6-12%。
*   **效率极高**：在 LSDBench 上，EVA 仅使用约 10k 视觉 Token 就达到了 51.8% 的准确率，而 Gemini-2.0-Flash 达到类似水平需要消耗超过 700k Token。
*   **策略进化**：通过强化学习，模型学会了从“被动模仿”转向“主动探索”，能够先通过低分辨率全局扫描定位关键区间，再通过高分辨率局部缩放（Zoom-in）获取细节。

### 7. 优点（亮点）
*   **端到端自主性**：不同于以往依赖固定工作流的智能体，EVA 能够动态调整空间分辨率和时间粒度。
*   **训练稳定性**：引入 KTO 阶段有效解决了强化学习中常见的收敛难和“奖励作弊（Reward Hacking）”问题。
*   **先规划后感知**：这种范式有效避免了无关视觉信息的干扰，节省了大量视觉 Token。

### 8. 不足与局限
*   **工具依赖**：目前的推理循环依赖于预定义的工具接口，对于未见过的或极其复杂的查询分布，其泛化能力可能受限。
*   **推理延迟**：虽然节省了视觉 Token，但多轮迭代推理（Summary-Plan-Action）会增加文本 Token 的生成量和推理轮数，可能在实时性要求极高的场景下存在延迟。
*   **模型底座限制**：实验主要基于 7B 规模的模型，尚未在更大规模（如 72B）或更多样化的底座上验证其扩展性。

（完）
