---
title: "VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models"
title_zh: VP-VLA：视觉提示作为视觉-语言-动作模型的接口
authors: "Zixuan Wang, Yuxin Chen, Yuqi Liu, Jinhui Ye, Pengguang Chen, Changsheng Lu, Shu Liu, Jiaya Jia"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22003v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 具有高层推理能力的视觉-语言-动作模型双系统框架
tldr: 针对视觉-语言-动作（VLA）模型在空间精度和鲁棒性上的不足，本文提出VP-VLA框架。该框架采用双系统架构，通过视觉提示接口解耦高层规划与底层执行。System 2规划器将指令分解并生成视觉锚点（如十字准星和边界框），System 1控制器则依据这些提示生成精确动作。实验证明，该方法在多个基准测试中显著提升了任务成功率。
selection_source: fresh_fetch
motivation: 传统的VLA模型采用黑盒映射方式，导致在处理复杂指令时空间精度较差且泛化能力有限。
method: 提出一种双系统框架，利用视觉提示（如边界框和准星）作为接口，将高层任务规划与底层运动控制解耦。
result: "在Robocasa和SimplerEnv仿真环境中，VP-VLA的成功率分别提升了5%和8.3%，优于QwenOFT等基准模型。"
conclusion: 通过引入结构化视觉提示接口，可以有效增强VLA模型的空间感知能力和复杂任务的执行可靠性。
---

## 摘要
视觉-语言-动作（VLA）模型通常将视觉观测和语言指令直接映射为机器人控制信号。这种“黑盒”映射迫使单次前向传播同时处理指令解释、空间定位和底层控制，往往导致空间精度较差，且在分布外（out-of-distribution）场景中的鲁棒性有限。为了解决这些局限性，我们提出了 VP-VLA，这是一个双系统框架，通过结构化视觉提示接口将高层推理与底层执行解耦。具体而言，“系统 2 规划器”将复杂指令分解为子任务，并识别相关的目标物体和目标位置。这些空间锚点随后作为结构化视觉提示（如十字准星和边界框）直接叠加在视觉观测上。在这些提示的引导下，并通过训练过程中引入的一种新型辅助视觉定位目标进行增强，“系统 1 控制器”能够可靠地生成精确的底层执行动作。在 Robocasa-GR1-Tabletop 基准测试和 SimplerEnv 仿真中的实验表明，VP-VLA 将成功率分别提高了 5% 和 8.3%，超越了包括 QwenOFT 和 GR00T-N1.6 在内的竞争基准模型。

## Abstract
Vision-Language-Action (VLA) models typically map visual observations and linguistic instructions directly to robotic control signals. This "black-box" mapping forces a single forward pass to simultaneously handle instruction interpretation, spatial grounding, and low-level control, often leading to poor spatial precision and limited robustness in out-of-distribution scenarios. To address these limitations, we propose VP-VLA, a dual-system framework that decouples high-level reasoning and low-level execution via a structured visual prompting interface. Specifically, a "System 2 Planner" decomposes complex instructions into sub-tasks and identifies relevant target objects and goal locations. These spatial anchors are then overlaid directly onto visual observations as structured visual prompts, such as crosshairs and bounding boxes. Guided by these prompts and enhanced by a novel auxiliary visual grounding objective during training, a "System 1 Controller" reliably generates precise low-level execution motions. Experiments on the Robocasa-GR1-Tabletop benchmark and SimplerEnv simulation demonstrate that VP-VLA improves success rates by 5% and 8.3%, surpassing competitive baselines including QwenOFT and GR00T-N1.6.

---

## 论文详细总结（自动生成）

这是一份关于论文《VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型通常采用“端到端黑盒”架构，将视觉观测和文本指令直接映射为机器人动作。这种方式存在三大缺陷：
*   **空间精度不足**：模型难以在单一前向传播中同时完成高层语义理解和底层精确空间定位。
*   **鲁棒性差**：模型容易过拟合训练集的场景分布，在面对新物体或新位置（OOD 场景）时表现脆弱。
*   **指令遵循虚假**：研究发现许多 VLA 模型并未真正理解指令，而是依赖环境先验。
**研究动机**：通过引入“视觉提示（Visual Prompting）”作为中间接口，将高层推理与底层控制解耦，提升机器人在复杂、多阶段任务中的空间感知和泛化能力。

### 2. 方法论：核心思想与关键技术
VP-VLA 提出了一个**双系统架构（Dual-System Framework）**：
*   **System 2 规划器（高层推理）**：
    *   **事件驱动的任务分解**：利用预训练 VLM（如 Qwen3-VL）将复杂指令分解为子任务列表。为了降低计算开销，仅在“事件触发”（如夹爪状态改变）时调用规划器更新目标。
    *   **视觉提示生成**：规划器识别当前子任务的目标物体和目标位置，利用分割模型（SAM3）生成视觉提示。
    *   **结构化接口**：在原始图像上叠加**十字准星（Crosshair）**表示交互锚点（如抓取点），叠加**边界框（Bounding Box）**表示空间约束（如放置区域）。
*   **System 1 控制器（底层执行）**：
    *   将原始图像与带有视觉提示的图像共同作为输入，生成连续的动作序列（Action Chunking）。
*   **辅助视觉定位目标（Grounding Loss）**：
    *   在训练中引入辅助任务，要求模型预测视觉提示的坐标（离散化为 1000 个 bin）。这强制模型内部表示与视觉提示对齐，防止模型将提示视为噪声。

### 3. 实验设计
论文在仿真和现实世界中进行了广泛评估：
*   **仿真基准**：
    *   **Robocasa-GR1-Tabletop**：包含 24 个厨房任务，涉及多步交互和复杂几何体。
    *   **SimplerEnv**：基于 BridgeDataV2 和 Fractal 数据集，评估模型在分布外（OOD）场景下的表现。
*   **对比方法**：包括 QwenOFT、GR00T-N1.5/N1.6、$\pi_0$、$\pi_{0.5}$、OpenVLA 等当前最先进的 VLA 模型。
*   **现实世界场景**：设计了垃圾分类（语义泛化）、按属性取物（颜色/属性泛化）和蛋托放置（精确空间定位）三个实验套件。

### 4. 资源与算力
*   **模型基础**：使用 Qwen3-VL-4B-Instruct 作为高层规划器和底层控制器的骨干网络。
*   **算力投入**：
    *   使用 **8 张 GPU** 进行训练。
    *   在 SimplerEnv 实验中，模型微调了 70k 步，Batch Size 为 32（每张卡）。
    *   现实世界实验中，总 Batch Size 为 256，动作块大小（Action Chunk Size）为 16。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每个任务进行 50 次独立试验以确保统计显著性。
*   **消融实验**：针对定位损失（Grounding Loss）、视觉提示类型（准星 vs 点）、提示叠加方式等进行了详细对比。
*   **充分性评价**：实验设计非常充分。不仅涵盖了标准基准测试，还专门设计了针对 OOD 物体、OOD 位置和复杂空间组合逻辑的实验，客观地证明了该方法在解决 VLA 模型“空间盲区”方面的有效性。

### 6. 主要结论与发现
*   **性能提升**：在 Robocasa 上平均成功率提升 5%，在 SimplerEnv 上提升 8.3%，刷新了多项 SOTA 记录。
*   **泛化能力**：VP-VLA 在面对从未见过的物体（如 OOD 蔬菜、玩具）和从未见过的空间位置时，性能下降幅度远小于基准模型。
*   **解耦优势**：通过视觉提示，模型将复杂的“意图理解”转化为简单的“视觉追踪”，显著降低了底层控制器的学习难度。
*   **定位损失的关键性**：消融实验证明，如果没有辅助定位损失，模型无法有效利用视觉提示。

### 7. 优点（亮点）
*   **双系统解耦**：模仿人类“快思考与慢思考”机制，兼顾了高层逻辑和底层精度。
*   **直观的视觉接口**：使用准星和边界框作为接口，比纯文本或纯坐标更符合视觉模型的处理特性。
*   **事件驱动机制**：避免了每帧都调用大模型规划器，平衡了推理性能与计算效率。
*   **强泛化性**：在现实世界 OOD 实验中表现惊人，证明了该架构的实用价值。

### 8. 不足与局限
*   **依赖前端模型**：系统的上限受限于 System 2 规划器（VLM）和分割模型（SAM）的准确性。如果 SAM 无法正确分割物体，后续执行将失败。
*   **推理延迟**：尽管采用了事件驱动，但在调用 System 2 的那一帧仍可能存在瞬时延迟。
*   **数据标注成本**：虽然论文提到可以使用规则生成标注，但高质量的视觉提示标注在扩展到更大规模数据集时仍具有挑战性。
*   **动态物体处理**：目前主要针对桌面静态或半静态物体，对于高速运动物体的处理能力尚未验证。

（完）
