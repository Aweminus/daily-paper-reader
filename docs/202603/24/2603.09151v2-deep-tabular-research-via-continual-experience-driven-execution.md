---
title: Deep Tabular Research via Continual Experience-Driven Execution
title_zh: 通过持续经验驱动执行的深度表格研究
authors: "Junnan Dong, Chuang Zhou, Zheng Yuan, Yifei Yu, Qiufeng Wang, Yinghui Li, Siyu An, Di Yin, Xing Sun, Feiyue Huang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09151v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 非机器人领域的闭环决策智能体框架
tldr: 针对大型语言模型在处理具有复杂层级和非规范布局的非结构化表格时面临的长程分析难题，本文提出了“深度表格研究（DTR）”框架。该框架将表格推理视为闭环决策过程，通过构建层次化元图映射搜索空间、引入期望感知策略优化路径选择，并利用孪生结构化记忆进行持续经验积累，实现了策略规划与底层执行的有效分离，显著提升了复杂表格任务的推理性能。
selection_source: carryover_cache
motivation: 现有的语言模型在处理具有层级表头和非规范布局的复杂非结构化表格时，难以进行多步跨区域的深度推理和长程分析。
method: 提出一种基于智能体的闭环决策框架，通过层次化元图捕捉语义，并结合期望感知选择策略与孪生结构化记忆机制进行持续优化。
result: 在挑战性的非结构化表格基准测试中，该方法显著优于现有模型，验证了将战略规划与底层执行分离的必要性。
conclusion: 通过将表格推理转化为由经验驱动的持续执行过程，可以有效解决复杂非结构化环境下的长程深度表格研究任务。
---

## 摘要
大语言模型在处理非结构化表格上的复杂长程分析任务时往往面临困难，这些表格通常具有层级化和双向表头以及非规范布局。我们将这一挑战形式化为深度表格研究（DTR），它需要对相互依赖的表格区域进行多步推理。为了解决 DTR，我们提出了一种新颖的智能体框架，将表格推理视为一个闭环决策过程。我们精心设计了耦合的查询与表格理解机制，用于路径决策和操作执行。具体而言：(i) DTR 首先构建一个层级元图以捕捉双向语义，将自然语言查询映射到操作级搜索空间；(ii) 为了在该空间中导航，我们引入了一种期望感知选择策略，优先处理高效用的执行路径；(iii) 至关重要的是，历史执行结果被合成到孪生结构化记忆中（即参数化更新和抽象文本），从而实现持续优化。在具有挑战性的非结构化表格基准测试上的广泛实验验证了该方法的有效性，并强调了在长程表格推理中将战略规划与底层执行分离的必要性。

## Abstract
Large language models often struggle with complex long-horizon analytical tasks over unstructured tables, which typically feature hierarchical and bidirectional headers and non-canonical layouts. We formalize this challenge as Deep Tabular Research (DTR), requiring multi-step reasoning over interdependent table regions. To address DTR, we propose a novel agentic framework that treats tabular reasoning as a closed-loop decision-making process. We carefully design a coupled query and table comprehension for path decision making and operational execution. Specifically, (i) DTR first constructs a hierarchical meta graph to capture bidirectional semantics, mapping natural language queries into an operation-level search space; (ii) To navigate this space, we introduce an expectation-aware selection policy that prioritizes high-utility execution paths; (iii) Crucially, historical execution outcomes are synthesized into a siamese structured memory, i.e., parameterized updates and abstracted texts, enabling continual refinement. Extensive experiments on challenging unstructured tabular benchmarks verify the effectiveness and highlight the necessity of separating strategic planning from low-level execution for long-horizon tabular reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：通过持续经验驱动执行的深度表格研究 (DTR)

#### 1. 核心问题与整体含义（研究动机和背景）
大型语言模型（LLMs）虽然在处理结构化数据方面取得了进展，但在面对现实世界中**非结构化、非规范布局**的表格（如具有层级化双向表头、合并单元格、缺失值或隐式语义的电子表格）时仍表现乏力。此外，实际的分析任务往往是**长程（Long-horizon）且多步**的，需要跨区域的数据获取、数值计算和综合分析。
论文将这一挑战形式化为**深度表格研究（Deep Tabular Research, DTR）**，旨在解决复杂表格上的多步推理难题。其核心动机在于：现有的 TableQA 方法大多依赖单次推理或清理过的模式，无法处理复杂的执行路径选择及执行过程中的错误传播。

#### 2. 方法论：核心思想与关键技术
DTR 框架将表格推理视为一个**闭环决策过程**，通过将高层战略规划与底层代码执行解耦，实现经验驱动的持续优化。

*   **核心思想**：通过“规划-执行-反馈-修正”的循环，利用历史执行经验指导当前的路径选择。
*   **关键技术细节**：
    *   **表格理解与元图构建**：提取表格的显式/隐式元信息，识别双向层级表头，构建层级元图 $G_T$ 来捕捉复杂的行列关系。
    *   **查询引导的操作映射**：将自然语言查询分解为原子操作（如 FILTER, GROUP, AGG 等），形成操作空间。
    *   **期望感知路径规划**：引入类似 UCB（置信上限算法）的得分公式：
        $$E(\pi) = \hat{R}(\pi) + \alpha \cdot P(\pi) \sqrt{\frac{\log \sum N(\pi')}{1+N(\pi)}}$$
        其中 $\hat{R}(\pi)$ 是预期回报，$N(\pi)$ 是执行次数。该机制在探索（尝试新路径）与利用（选择高回报路径）之间取得平衡。
    *   **孪生结构化记忆（Siamese Memory）**：同时维护**参数化执行反馈**（成功率、耗时、类型一致性）和**抽象经验总结**（自然语言形式的策略建议），用于动态调整后续的路径选择。

#### 3. 实验设计
*   **数据集/场景**：
    *   **DTR-Bench**（本文构建）：包含 500 个场景驱动的问答对，涵盖 8 种用户角色（如投资者、社会研究员），侧重于相关性分析、异常检测等深度任务。
    *   **RealHitBench**：一个现有的挑战性非结构化表格基准，包含事实核查、数值推理、结构理解等任务。
*   **对比方法**：
    *   **表格专用模型**：TableGPT2-7B, TableLLM-7B, StructGPT。
    *   **通用 LLM**：DeepSeek-V3, DeepSeek-V3.2。
    *   **工作流/智能体框架**：ST-Raptor, TreeThinker, Code Loop（迭代调试代码的基线）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件资源（如 GPU 型号、数量）或训练/推理的总时长。实验主要基于现有的预训练模型（如 DeepSeek-V3, Qwen3）作为骨干网络进行推理阶段的智能体框架评估。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型基准上进行了全面对比，涵盖了准确率、分析深度、可行性、美观度等多个维度。
*   **消融实验**：系统地验证了元信息提取、查询分解、期望感知策略和抽象经验记忆四个组件的边际贡献。
*   **充分性与公平性**：
    *   进行了 **LLM 调用预算分析**，探讨了性能与成本（调用次数）的权衡。
    *   展示了**路径演化分析**，通过 500 个查询的批次演化证明了模型从探索到利用的收敛过程。
    *   实验设计较为客观，通过胜率（Win Rate）和得分率（Score Rate）等指标多维度衡量性能。

#### 6. 主要结论与发现
*   **DTR 框架显著优于基线**：在 DTR-Bench 上，DTR (基于 DeepSeek-V3) 在准确率和分析深度上均大幅领先于纯 LLM 和其他智能体框架。
*   **规划与执行分离的必要性**：将高层操作序列规划与底层代码生成分离，能有效降低代码错误率（从 42.3% 降至 28.4%）。
*   **经验驱动的有效性**：通过孪生记忆积累经验，模型能够随着任务执行逐渐优化路径选择，避免了盲目的重复尝试。
*   **效率平衡**：DTR 在平均 4.78 次 LLM 调用下达到了性能甜点区，比过度迭代的 Code Loop 更高效。

#### 7. 优点（亮点）
*   **任务定义前瞻性**：提出了“深度表格研究”概念，将视角从简单的问答转向复杂的长程数据分析。
*   **闭环反馈机制**：引入了期望感知选择和孪生记忆，使智能体具备了从失败和成功中持续学习的能力。
*   **结构化建模**：通过元图有效解决了非结构化表格中层级表头难以解析的痛点。
*   **实用性强**：生成的分析报告不仅准确，而且在可视化美观度和逻辑深度上更接近人类专家。

#### 8. 不足与局限
*   **依赖基础模型能力**：DTR 的表现高度依赖于底层 LLM 的推理和代码生成能力，在较弱的模型（如 Qwen3-1.7B）上提升有限。
*   **错误传播风险**：尽管有反馈机制，但在极长程的任务中，早期的逻辑错误仍可能导致后续路径的级联失效。
*   **计算延迟**：作为一种多步智能体框架，其推理延迟显著高于单次生成的 LLM，可能不适用于实时性要求极高的场景。
*   **评估主观性**：部分指标（如分析深度、美观度）依赖于 LLM-as-a-Judge，可能存在潜在的评估偏差。

（完）
