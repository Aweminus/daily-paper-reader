---
title: "Chain-of-Adaptation: Surgical Vision-Language Adaptation with Reinforcement Learning"
title_zh: 适配链：基于强化学习的手术视觉-语言适配
authors: "Jiajie Li, Chenhui Xu, Meihuan Liu, Jinjun Xiong"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20116v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用强化学习进行推理的视觉语言适配框架
tldr: 针对传统微调在特定领域（如手术视觉）中容易破坏预训练多模态先验并降低泛化能力的问题，本文提出了Chain-of-Adaptation (CoA) 框架。该框架通过强化学习和结构化推理格式，在整合领域知识的同时保留了模型的通用推理与感知能力。在手术基准测试中的实验证明，CoA在准确性、泛化性和稳定性方面均优于监督微调，为视觉语言模型的领域专业化提供了可靠路径。
selection_source: fresh_fetch
motivation: 传统的监督微调会损害预训练模型的多模态先验知识，导致在特定领域应用时泛化性能下降。
method: 提出CoA框架，利用强化学习和结构化推理格式来实现领域对齐，同时保留模型的核心视觉语言能力。
result: 在手术视觉基准测试的分布内和分布外设置下，CoA均表现出比监督微调更高的准确率和更强的泛化能力。
conclusion: CoA有效地平衡了领域专业化与通用能力的保留，是视觉语言模型进行领域自适应的一种稳健且高效的方法。
---

## 摘要
在特定领域数据集上进行的传统微调可能会无意中改变模型的预训练多模态先验，从而导致泛化能力下降。为解决这一问题，我们提出了适配链（Chain-of-Adaptation, CoA），这是一种旨在整合领域知识，同时保持模型固有推理和感知能力的适配框架。CoA 引入了一种结构化推理格式，通过强化学习在不牺牲通用多模态能力的情况下增强领域对齐。在标准手术基准测试中，无论是在分布内还是分布外设置下，实验均表明 CoA 比监督微调具有更高的准确性、更强的泛化能力和更稳定的表现。此外，消融研究证实 CoA 有效地保留了模型的核心视觉-语言能力，为视觉-语言模型（VLM）的领域专业化提供了一条可靠的途径。

## Abstract
Conventional fine-tuning on domain-specific datasets can inadvertently alter a model's pretrained multimodal priors, leading to reduced generalization. To address this, we propose Chain-of-Adaptation (CoA), an adaptation framework designed to integrate domain knowledge while maintaining the model's inherent reasoning and perceptual capabilities. CoA introduces a structured reasoning format that enhances domain alignment without sacrificing general multimodal competence by reinforcement learning. Experiments on standard surgical benchmarks, under both in-distribution and out-of-distribution settings, demonstrate that CoA achieves higher accuracy, stronger generalization, and more stable behavior than supervised fine-tuning. Furthermore, ablation studies confirm that CoA effectively preserves the model's core visual-language abilities, providing a reliable pathway for domain specialization in VLMs.

---

## 论文详细总结（自动生成）

以下是对论文《Chain-of-Adaptation: Surgical Vision-Language Adaptation with Reinforcement Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将通用的视觉-语言模型（VLM）适配到高度专业化的手术领域，同时避免传统监督微调（SFT）带来的“灾难性遗忘”、模型坍缩（Model Collapse）以及对通用多模态能力的破坏。
*   **研究背景**：手术领域存在显著的分布偏移（图像模态、专业术语、专家级推理需求）。现有的手术数据集多为低级标注（如类别标签），缺乏丰富的语言描述和推理逻辑。作者发现，即使是轻量级的 SFT 也会导致模型输出长度剧减、多样性丧失，并产生严重的幻觉（如将无关组织误认为十二指肠）。

### 2. 方法论：Chain-of-Adaptation (CoA)
CoA 框架包含两个核心组成部分：**结构化推理格式**和**两阶段训练流水线**。
*   **结构化四阶段推理格式**：
    1.  `<general description>`：仅描述低级视觉属性（颜色、几何、纹理），不涉及专业术语，用于保留预训练的通用感知能力。
    2.  `<evidence>`：整合任务信息、视觉观察和领域知识，提取推理线索。
    3.  `<thought>`：进行深层逻辑推理。
    4.  `<answer>`：给出最终的专业结论。
*   **两阶段训练流程**：
    1.  **冷启动（Cold Start）**：利用强模型（如 Gemini-Flash-2.5）对约 1 万张无标注手术图像生成符合 CoA 格式的伪标签，初步注入领域知识和格式规范。
    2.  **强化学习（RLVR）**：采用 **GRPO（Group Relative Policy Optimization）** 算法。模型针对同一输入生成多组响应，根据“格式合规性”和“任务准确性（如 F1 分数）”计算可验证奖励，通过相对偏好优化策略，而非直接模仿静态标签。

### 3. 实验设计
*   **数据集与场景**：
    *   **分布内（ID）评估**：EndoVis2018（机器人手术）和 CholecT50（腹腔镜胆囊切除术），任务为手术对象/器械识别。
    *   **分布外（OOD）评估**：GraSP 数据集（前列腺切除术），用于测试跨领域泛化性。
    *   **通用能力评估**：MMBench 和 MMStar，用于检测模型是否保留了通用的视觉问答能力。
*   **对比方法**：Base 模型（Qwen3-VL-8B-Instruct）、SFT、冷启动+SFT、冷启动+RLVR (CoA)。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 × NVIDIA H100 GPU**。
*   **训练细节**：混合精度训练；SFT 和冷启动阶段学习率为 1e-5；RLVR 阶段学习率为 1e-6，每条提示生成 8 个响应，KL 散度系数 $\beta=0.001$。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个手术专业数据集和 2 个通用多模态基准。
*   **消融实验**：对比了“无推理过程的 RLVR”、“传统 CoT 格式的 RLVR”以及“CoA 格式的 RLVR”。
*   **充分性评价**：实验设计较为全面，不仅关注手术任务的准确率，还通过雷达图分析了类别均衡性，并通过通用基准测试验证了抗遗忘能力，逻辑闭环完整，对比公平。

### 6. 主要结论与发现
*   **性能提升**：CoA 在 EndoVis2018 上的 F1 分数提升了 27%（从 0.657 到 0.837），在 CholecT50 上提升了 10%。
*   **克服 SFT 缺陷**：SFT 容易过拟合高频类别（导致长尾类别性能坍缩），而 CoA 在处理罕见器械和复杂场景时表现出更强的鲁棒性和类别均衡性。
*   **泛化与保留**：CoA 在未见过的 GraSP 数据集上表现优于 SFT；同时在通用基准（MMStar/MMBench）上保持了与原模型相当甚至略优的水平，证明了结构化推理对通用能力的保护作用。

### 7. 优点与亮点
*   **解耦感知与推理**：通过 `<general description>` 强制模型先进行通用视觉描述，有效地将通用感知能力作为正则化项，缓解了领域适配中的知识冲突。
*   **可验证奖励的应用**：在手术这种标注稀缺的领域，利用 RLVR 探索模型潜力，比单纯模仿低质量的 SFT 标签更有效。
*   **系统性分析**：论文深入探讨了 SFT 在专业领域失效的根本原因（模型坍缩、语言空间扭曲），具有较强的理论指导意义。

### 8. 不足与局限
*   **任务局限性**：目前主要集中在“识别类”任务（器械、步骤识别），这类任务的奖励易于量化验证。对于更复杂的开放式手术咨询或长文本生成任务，如何设计“可验证奖励”仍是挑战。
*   **伪标签依赖**：冷启动阶段依赖于闭源强模型（Gemini）生成的伪标签，若初始伪标签存在领域偏差，可能会影响后续 RL 的收敛质量。
*   **计算成本**：RL 训练（尤其是 GRPO 采样）比 SFT 消耗更多的计算资源和时间。

（完）
