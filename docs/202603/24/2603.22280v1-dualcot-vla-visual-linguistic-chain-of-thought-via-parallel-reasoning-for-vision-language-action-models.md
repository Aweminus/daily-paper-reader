---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：基于并行推理的视觉-语言思维链视觉-语言-动作模型
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言动作模型的视听思维链推理
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务规划和精细空间感知方面的不足，本文提出 DualCoT-VLA。该方法结合了用于高层规划的语言思维链（CoT）和用于底层空间理解的视觉思维链，并通过并行推理机制取代传统的自回归解码，在显著降低推理延迟的同时，克服了单模态推理的局限性，提升了模型在多步复杂任务中的表现。
selection_source: fresh_fetch
motivation: 现有VLA模型难以同时兼顾高层逻辑规划与底层视觉细节，且自回归思维链推理存在高延迟和误差累积问题。
method: 提出一种双重思维链方法，整合视觉与语言CoT，并利用两组可学习查询标记实现单步并行推理。
result: 在LIBERO、RoboCasa GR1基准测试及真实机器人平台上均取得了最先进的性能表现。
conclusion: DualCoT-VLA通过并行化的多模态思维链，有效增强了机器人的复杂任务处理能力与实时推理效率。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在需要逻辑规划的复杂多步任务以及需要细粒度空间感知的精确操作中往往表现不佳。最近的研究引入了思维链（CoT）推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记，将自回归推理转变为单步前向推理。广泛的实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

### DualCoT-VLA 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的视觉-语言-动作（VLA）模型通常将视觉和指令直接映射为动作，但在处理需要**长程逻辑规划**和**精细空间感知**的复杂任务时表现欠缺。虽然近期研究引入了思维链（CoT）推理（即“先思考再行动”），但仍存在两大瓶颈：
*   **模态孤立**：现有的 CoT 方法通常仅限于单一模态。纯语言 CoT 擅长逻辑规划但缺乏空间精度；纯视觉 CoT 捕捉细节但难以进行长程规划。
*   **推理延迟与误差累积**：大多数 CoT 模型依赖于**自回归（Autoregressive）**解码，逐个 Token 生成推理过程导致高延迟，且一旦中间步骤出错，后续动作会产生复合误差。

#### 2. 方法论：核心思想与关键技术
论文提出了 **DualCoT-VLA**，其核心思想是在**连续潜空间**内通过**并行推理机制**实现双模态 CoT。
*   **并行隐式推理**：引入两组可学习的查询标记（Query Tokens）——视觉 CoT 查询（$Q_{vis}$）和语言 CoT 查询（$Q_{lin}$）。这些标记与视觉观测、语言指令一起输入 VLM 主干网络，在一次前向传播中同时提取推理特征。
*   **视觉 CoT（几何蒸馏）**：利用预训练的视觉基础模型（Depth Anything 3, DA3）作为教师。通过交叉注意力机制，将 VLM 输出的隐状态与 DA3 的稠密空间特征对齐，使模型内化 3D 空间感知能力。
*   **语言 CoT（步骤级监督）**：利用轻量级辅助 LLM（如 Qwen3-0.6B）作为解码器。在训练阶段，要求 VLM 的语言隐状态能够作为前缀引导辅助 LLM 生成显式的任务规划文本（状态跟踪、空间定位、动作制定），从而迫使 VLM 在潜空间中压缩逻辑规划信息。
*   **动作生成**：将富含推理信息的隐状态输入到基于 **Flow-Matching** 的 Diffusion Transformer (DiT) 动作头中，预测连续动作序列。
*   **推理阶段**：丢弃辅助的教师模型（DA3 和 LLM），仅保留 VLM 主干和动作头，实现单步高效推理。

#### 3. 实验设计
*   **仿真基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，评估模型的泛化与长程规划能力。
    *   **RoboCasa GR1**：涉及 29 自由度灵巧手的复杂桌面任务，测试精细操作。
*   **真实世界场景**：使用 AgileX Cobot 双臂机器人，设计了简单（放面包）、中等（放积木）、困难（收集多种水果）三种难度的任务。
*   **对比方法**：
    *   **非 CoT 模型**：OpenVLA, $\pi_0$, PD-VLA, GR00T 系列。
    *   **自回归 CoT 模型**：CoT-VLA, ThinkAct, DeepThinkVLA, LaRA-VLA 等。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 GPU** 上进行。
*   **模型规模**：VLM 主干采用 Qwen3-VL-4B，辅助解码器为 Qwen3-0.6B。
*   **训练细节**：LIBERO 训练的学习率为 2.5e-5，Batch Size 为 48；RoboCasa 训练的学习率为 3e-5，Batch Size 为 256。
*   **说明**：文中未明确给出具体的 GPU 数量和总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 上进行了 500 个回合的评估，在 RoboCasa 上进行了 24 个任务、每个任务 50 个回合的评估，真机实验每个任务 25 次尝试。
*   **消融实验**：专门对比了“无 CoT”、“仅视觉 CoT”、“仅语言 CoT”以及“双模态 CoT”的效果，验证了各组件的互补性。
*   **客观性**：通过与多种 SOTA 模型（包括最新的 $\pi_0$ 和 LaRA-VLA）在统一基准下对比，并提供了推理延迟的定量分析，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：DualCoT-VLA 在 LIBERO（平均成功率 98.8%）和 RoboCasa（平均成功率 55.1%）上均刷新了 SOTA 记录。
*   **推理高效**：相比于自回归 CoT 模型（延迟约 3178ms），DualCoT-VLA 的总推理延迟仅为 **83.2ms**，仅比非 CoT 模型多出约 7ms，完全满足高频实时控制需求。
*   **模态互补**：视觉 CoT 显著提升了空间感知类任务的成功率，而语言 CoT 则在长程逻辑任务中起到了关键作用。

#### 7. 优点（亮点）
*   **并行化创新**：通过 Query Tokens 将原本低效的自回归推理转变为单步并行推理，解决了 VLA 模型“想得深”与“跑得快”之间的矛盾。
*   **隐式与显式的结合**：训练时利用显式文本和几何特征进行强监督，推理时使用隐式潜空间特征，既保证了推理质量又兼顾了效率。
*   **强大的空间感知**：通过蒸馏 DA3 的几何先验，使模型在处理灵巧手等高精度操作时具有明显优势。

#### 8. 不足与局限
*   **数据依赖**：训练过程需要高质量的显式 CoT 文本标注（虽然可以通过大模型自动生成，但仍存在成本和潜在的幻觉风险）。
*   **教师模型限制**：模型的上限可能受限于教师模型（DA3 和辅助 LLM）的能力。
*   **应用范围**：虽然在桌面操作中表现优异，但在更广阔的开放场景（如移动导航结合操作）中的表现尚待进一步验证。

（完）
