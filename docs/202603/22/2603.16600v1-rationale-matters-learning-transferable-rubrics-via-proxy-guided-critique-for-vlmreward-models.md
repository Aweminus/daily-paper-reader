---
title: "Rationale Matters: Learning Transferable Rubrics via Proxy-Guided Critique for VLMReward Models"
title_zh: 理由至关重要：通过代理引导的批判为视觉语言模型奖励模型学习可迁移的评分标准
authors: "Weijie Qiu, Dai Guan, Junxin Wang, Zhihang Li, Yongbo Gai, Mengyu Zhou, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16600v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将代理引导的准则验证引入VLM奖励模型的强化学习中
tldr: 本研究针对视觉语言奖励模型（GRM）中评分准则（rubric）缺乏优化的问题，提出了 Proxy-GRM 框架。该方法引入轻量级代理模型，通过强化学习将准则对偏好预测的准确性作为奖励信号，从而显式提升准则的质量和一致性。实验表明，Proxy-GRM 在多个多模态奖励基准上取得了 SOTA 性能，且具有极高的数据效率和准则可迁移性。
selection_source: fresh_fetch
motivation: 现有的视觉语言奖励模型通常将评分准则视为附带产物，缺乏对其质量的直接优化和有效监督。
method: 提出 Proxy-GRM，利用轻量级代理模型根据准则预测偏好，并将预测准确率作为强化学习的奖励来优化准则生成。
result: 在 VL-Reward Bench 等多个基准上达到 SOTA 水平，且训练数据量仅为基准方法的四分之一。
conclusion: 显式优化推理准则能显著增强奖励模型的准确性、内部一致性以及在未见评估器上的可迁移性。
---

## 摘要
针对视觉语言模型 (VLM) 的生成式奖励模型 (GRM) 通常通过三阶段流水线评估输出：评分标准生成、基于标准的评分以及最终判定。然而，中间的评分标准很少被直接优化。先前的研究通常要么将评分标准视为附带产物，要么依赖昂贵的 LLM-as-judge 检查，而这些检查无法提供可微信号，且在训练时的指导作用有限。我们提出了 Proxy-GRM，它将代理引导的评分标准验证引入强化学习 (RL)，以显性地提升评分标准质量。具体而言，我们训练了轻量级代理智能体（Proxy-SFT 和 Proxy-RL），它们接收候选评分标准以及原始查询和偏好对，然后仅使用该评分标准作为证据来预测偏好排序。代理的预测准确率作为评分标准质量的奖励，激励模型生成内部一致且具有可迁移性的评分标准。仅使用约 5 万条数据样本，Proxy-GRM 在 VL-Reward Bench、Multimodal Reward Bench 和 MM-RLHF-Reward Bench 上均达到了最先进的结果，优于使用四倍数据训练的方法。消融实验表明，Proxy-SFT 是比 Proxy-RL 更强的验证器，且隐式奖励聚合表现最佳。至关重要的是，学习到的评分标准可以迁移到未见过的评估器，在无需额外训练的情况下提高测试时的奖励准确率。我们的代码可在 https://github.com/Qwen-Applications/Proxy-GRM 获取。

## Abstract
Generative reward models (GRMs) for vision-language models (VLMs) often evaluate outputs via a three-stage pipeline: rubric generation, criterion-based scoring, and a final verdict. However, the intermediate rubric is rarely optimized directly. Prior work typically either treats rubrics as incidental or relies on expensive LLM-as-judge checks that provide no differentiable signal and limited training-time guidance. We propose Proxy-GRM, which introduces proxy-guided rubric verification into Reinforcement Learning (RL) to explicitly enhance rubric quality. Concretely, we train lightweight proxy agents (Proxy-SFT and Proxy-RL) that take a candidate rubric together with the original query and preference pair, and then predict the preference ordering using only the rubric as evidence. The proxy's prediction accuracy serves as a rubric-quality reward, incentivizing the model to produce rubrics that are internally consistent and transferable. With ~50k data samples, Proxy-GRM reaches state-of-the-art results on the VL-Reward Bench, Multimodal Reward Bench, and MM-RLHF-Reward Bench, outperforming the methods trained on four times the data. Ablations show Proxy-SFT is a stronger verifier than Proxy-RL, and implicit reward aggregation performs best. Crucially, the learned rubrics transfer to unseen evaluators, improving reward accuracy at test time without additional training. Our code is available at https://github.com/Qwen-Applications/Proxy-GRM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rationale Matters: Learning Transferable Rubrics via Proxy-Guided Critique for VLM Reward Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉语言模型（VLM）生成式奖励模型（GRM）在评估输出时，通常遵循“生成评分标准（Rubric）→ 逐项评估 → 最终判定”的流程。然而，**中间生成的 Rubric 质量从未被直接优化或监督**。
*   **研究动机**：
    *   **事后合理化风险**：模型可能为了凑出预设的答案而生成虚假的 Rubric，而非基于原则进行评估。
    *   **缺乏可迁移性**：低质量的 Rubric 无法引导独立评估者得出正确结论。
    *   **现有方案局限**：依赖 LLM-as-a-Judge 成本高且不可微，无法闭环指导训练。
*   **整体含义**：本文旨在通过引入“代理引导的验证”机制，将 Rubric 的质量转化为可学习的奖励信号，从而训练出更具解释性、一致性和可迁移性的多模态奖励模型。

### 2. 方法论：Proxy-GRM 框架
*   **核心思想**：定义“Rubric 可迁移性”——如果一个 Rubric 是高质量的，那么一个独立的代理智能体（Proxy Agent）在仅给定该 Rubric 和原始输入的情况下，也应能预测出正确的偏好结果。
*   **关键技术细节**：
    1.  **代理智能体训练**：
        *   **Proxy-SFT**：在高质量标注数据上微调，使其学会严格遵循 Rubric 进行判定。
        *   **Proxy-RL**：在 SFT 基础上通过强化学习进一步增强其对不同表述 Rubric 的鲁棒性。
    2.  **策略模型训练（Proxy-GRM）**：
        *   **阶段一：冷启动 SFT**：使用约 10k 条蒸馏数据进行格式和基础能力初始化。
        *   **阶段二：代理引导的强化学习**：使用 **GRPO（群组相对策略优化）** 算法。
*   **奖励函数设计**：
    *   $r = r_{acc} + r_{proxy} + 0.5 \cdot r_{format}$
    *   $r_{acc}$：最终判定是否正确。
    *   $r_{proxy}$：**核心创新点**。将生成的 Rubric 喂给冻结的 Proxy Agent，若 Proxy 判定正确，则给予奖励。
    *   $r_{format}$：XML 标签格式奖励。

### 3. 实验设计
*   **数据集与场景**：
    *   训练数据：从 LLaVA-Critic、RLAIF-V、RLHF-V 等开源集合中筛选并蒸馏出约 60k 样本。
    *   测试基准（Benchmarks）：
        1.  **VL-RewardBench**：涵盖通用理解、幻觉检测和视觉推理。
        2.  **Multimodal Reward Bench**：涵盖数学、代码、安全等 7 个维度。
        3.  **MM-RLHF-Reward Bench**：包含多轮对话、长文本、视频评估等挑战。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Claude-3.5/3.7-Sonnet, Gemini-1.5-Pro。
    *   **开源 SOTA 奖励模型**：R1-Reward, Unified-Reward, IXC-2.5-Reward 等。

### 4. 资源与算力
*   **模型规模**：策略模型和代理模型均基于 **Qwen2.5-VL-7B**。
*   **训练框架**：使用 `ms-swift` 进行 SFT，使用 `verl` 框架进行 GRPO 强化学习。
*   **算力细节**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长。但提到了超参数：RL 阶段 Batch Size 为 256，学习率为 5e-6。
*   **数据效率**：强调了极高的数据效率，仅用 **50k** 样本即超过了使用 200k+ 样本训练的基准模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流 Benchmark 上进行了全面测试。
    *   **消融实验**：对比了不同规模的 Proxy（3B/7B/32B）、不同训练方式的 Proxy（SFT vs RL）、不同的奖励聚合配置（隐式 vs 显式）。
    *   **迁移性实验**：验证了生成的 Rubric 在未见过的评估器（如 Qwen2.5-VL-32B-Instruct）上的表现。
*   **充分性评价**：实验设计非常详尽且具有说服力。特别是关于“Proxy-SFT 优于 Proxy-RL”的发现，以及 Rubric 跨模型迁移的测试，展示了研究的深度和客观性。

### 6. 主要结论与发现
*   **SOTA 性能**：Proxy-GRM 在所有测试基准上均达到或超过了现有最强开源模型，甚至在某些维度上超越了 Claude-3.7-Sonnet。
*   **过程监督的重要性**：显式优化中间的 Rubric 步骤比单纯增加训练数据量更有效。
*   **Proxy-SFT 悖论**：发现 SFT 训练的代理比 RL 训练的代理更适合做验证器，因为 RL 可能会为了结果准确而牺牲过程的忠实度（内部一致性）。
*   **Rubric 可迁移性**：Proxy-GRM 生成的 Rubric 具有普适性，能显著提升其他第三方模型的评估准确率。

### 7. 优点
*   **方法论创新**：首次提出将 Rubric 的“可迁移性”作为强化学习的闭环奖励信号，解决了 GRM 中间过程不可控的问题。
*   **极高的数据效率**：仅需 1/4 的数据量即可达到甚至超越同类模型。
*   **解释性强**：生成的 Rubric 不再是“废话”，而是真正具有指导意义的评估准则。
*   **开源贡献**：提供了代码和模型权重，具有很高的实用价值。

### 8. 不足与局限
*   **代理依赖性**：如果 Proxy Agent 本身能力过弱（如实验中的 3B 模型），会引入噪声并损害训练效果。
*   **计算开销**：在 RL 训练的 Rollout 阶段，需要额外调用一次 Proxy Agent 进行推理，增加了训练时的计算负担。
*   **任务覆盖**：虽然在多模态基准上表现优异，但在极度专业化（如医疗影像、超长视频流）领域的泛化能力尚待验证。
*   **多代理冲突**：消融实验显示简单集成多个代理反而会降低效果，如何有效利用多个专家代理仍是挑战。

（完）
