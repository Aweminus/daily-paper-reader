---
title: Ruyi2.5 Technical Report
title_zh: Ruyi2.5 技术报告
authors: "Huan Song, Shuyu Tian, Qingfei Zhao, Wenhao Hong, Jiang Liu, Ting Long, Jiawei Shao, Xuelong Li"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17311v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于多模态行为推理的强化学习微调
tldr: Ruyi2.5是基于AI Flow框架的多模态家族模型，延续了“一次训练，多次部署”范式，通过共享骨干架构实现多尺度模型协同训练。该研究推出了隐私保护摄像服务系统Ruyi2.5-Camera，采用边缘去标识化与云端推理的两阶段架构，并提出BPPO算法显著提升强化学习微调效率。实验证明其在通用基准上比肩Qwen3-VL，且在隐私监控任务中表现卓越。
selection_source: fresh_fetch
motivation: 旨在解决多模态模型在不同规模部署时的语义一致性问题，并提升监控场景下的隐私保护能力与强化学习训练效率。
method: 采用共享骨干架构进行多尺度协同训练，结合信息瓶颈引导的特征映射实现隐私脱敏，并提出二元前缀策略优化（BPPO）加速微调。
result: Ruyi2.5在通用多模态基准上达到Qwen3-VL水平，BPPO训练速度提升2-3倍，且在隐私受限的监控任务中显著优于同类模型。
conclusion: Ruyi2.5通过统一的训练范式和创新的隐私保护及优化算法，为高效、安全的多模态模型部署提供了强有力的技术支撑。
---

## 摘要
我们介绍了 Ruyi2.5，这是一个基于 AI Flow 框架构建的多模态家族模型。Ruyi2.5 将 Ruyi2 的“一次训练，多次部署”范式扩展到多模态领域，构建了一个共享骨干网络架构，在单一统一流水线中协同训练不同规模的模型，从而确保了所有部署层级间的语义一致性。基于 Ruyi2.5，我们开发了 Ruyi2.5-Camera 模型作为一种隐私保护摄像头服务系统，该系统将 Ruyi2.5-Camera 实例化为一个两阶段识别流水线：边缘模型在源头应用信息瓶颈引导的不可逆特征映射对原始帧进行去标识化处理，而云端模型则执行深度行为推理。为了加速强化学习微调，我们进一步提出了二值前缀策略优化（Binary Prefix Policy Optimization, BPPO），该方法通过二值响应选择减少样本冗余，并将梯度更新集中在响应前缀上，相比 GRPO 实现了 2 到 3 倍的训练加速。实验表明，Ruyi2.5 在通用多模态基准测试中与 Qwen3-VL 表现相当，而 Ruyi2.5-Camera 在隐私受限的监控任务中显著优于 Qwen3-VL。

## Abstract
We present Ruyi2.5, a multimodal familial model built on the AI Flow framework. Extending Ruyi2's "Train Once, Deploy Many" paradigm to the multimodal domain, Ruyi2.5 constructs a shared-backbone architecture that co-trains models of varying scales within a single unified pipeline, ensuring semantic consistency across all deployment tiers. Built upon Ruyi2.5, Ruyi2.5-Camera model is developed as a privacy-preserving camera service system, which instantiates Ruyi2.5-Camera into a two-stage recognition pipeline: an edge model applies information-bottleneck-guided irreversible feature mapping to de-identify raw frames at the source, while a cloud model performs deep behavior reasoning. To accelerate reinforcement learning fine-tuning, we further propose Binary Prefix Policy Optimization (BPPO), which reduces sample redundancy via binary response selection and focuses gradient updates on response prefixes, achieving a 2 to 3 times training speedup over GRPO. Experiments show Ruyi2.5 matches Qwen3-VL on the general multimodal benchmarks, while Ruyi2.5-Camera substantially outperforms Qwen3-VL on privacy-constrained surveillance tasks.

---

## 论文详细总结（自动生成）

### Ruyi2.5 技术报告深度总结

#### 1. 核心问题与研究动机
*   **隐私与理解的矛盾**：在公共安全、医疗监控等场景中，自动行为识别（如跌倒、打斗）需求巨大，但传输原始视频流会泄露个人身份信息（PII）。现有的物理遮蔽或加密方法往往会显著降低语义理解的准确性。
*   **部署效率与一致性**：现代多模态大模型（MLLM）通常需要针对不同终端（边缘/云端）独立训练不同规模的模型，这不仅耗费巨大算力，还难以保证不同规模模型间语义空间的一致性。
*   **强化学习（RL）训练瓶颈**：现有的 RLVR（如 GRPO）在处理长链推理时，存在样本冗余度高、计算成本昂贵（全序列反向传播）的问题。

#### 2. 核心方法论
Ruyi2.5 基于 **AI Flow** 理论框架，提出了以下核心技术：
*   **共享骨干家族模型架构（Shared-Backbone Familial Model）**：
    *   采用“一次训练，多次部署”范式。通过参数继承和灵活分支，在单一流水线中协同训练不同深度和宽度的子模型（如 1.9B 和 8B）。
    *   确保了边缘轻量级模型与云端大规模模型共享统一的语义空间，支持无缝的任务路由。
*   **Ruyi2.5-Camera 隐私保护流水线**：
    *   **边缘端**：利用信息瓶颈（Information Bottleneck）理论引导的不可逆特征映射，对原始图像进行去标识化处理，生成无法还原视觉内容但保留动作特征的抽象向量。
    *   **云端**：大模型基于脱敏特征进行深度行为推理，处理遮挡、多主体交互和长时序动态。
*   **二元前缀策略优化（BPPO）**：
    *   **二元样本选择**：在每个 Prompt 生成的响应组中，仅保留一个正样本和一个负样本进行优化，消除组内冗余梯度。
    *   **前缀梯度约束**：仅对响应的前 $n$ 个 Token（前缀）进行梯度传播，因为早期推理步骤对最终结果具有决定性影响。
    *   **公式目标**：结合重要性采样、组相对优势估计、剪切稳定化和 KL 正则化，实现高效对齐。

#### 3. 实验设计
*   **数据集与场景**：
    *   **通用任务**：涵盖视觉问答（VQA）、图像描述、STEM 推理、文档理解（OCR、图表、PDF）以及多模态代码生成。
    *   **特定场景**：针对隐私敏感的监控场景（如洗手间、更衣室、医疗设施），构建了专门的行为识别基准测试。
    *   **强化学习数据**：针对几何数学问题（平面/立体几何、图表推理）构建了专用 RL 数据集。
*   **Benchmark 与对比方法**：
    *   在通用多模态基准上对比了 **Qwen3-VL**。
    *   在隐私监控任务中，对比了 Qwen3-VL-8B 与 Ruyi2.5-Camera 的不同版本（1.9B, 8B）。

#### 4. 资源与算力
*   **报告说明**：文中**未明确说明**具体的 GPU 型号（如 H100/A100）、显卡数量或总训练时长。
*   **模型配置**：提供了模型层数和隐藏层维度的细节。例如，1.9B 版本包含 1 个 Decoder Block，8B 版本包含 36 个，两者均使用隐藏维度为 1152 的 ViT 视觉编码器。

#### 5. 实验数量与充分性
*   **实验规模**：报告展示了在通用多模态理解和特定行为识别两个大类下的实验结果。
*   **充分性评价**：
    *   **对比维度**：涵盖了不同参数规模（1.9B vs 8B）的对比，验证了家族模型架构的有效性。
    *   **消融与效率**：通过 BPPO 与 GRPO 的对比，给出了 2-3 倍加速的量化结论。
    *   **客观性**：实验不仅关注准确率，还引入了精确率（Precision）、召回率（Recall）和 F1 分数，评价较为全面。但缺乏更多同类开源模型（如 LLaVA, InternVL）在隐私场景下的横向对比。

#### 6. 主要结论与发现
*   **性能对齐**：Ruyi2.5 在通用多模态任务上的表现与 Qwen3-VL 持平，证明了家族协同训练不会导致性能退化。
*   **隐私场景优势**：Ruyi2.5-Camera-8B 在隐私监控任务中 F1 分数达到 93.67，显著优于 Qwen3-VL-8B（82.77），证明了“边缘脱敏+云端推理”架构的优越性。
*   **训练加速**：BPPO 算法在保持策略质量的同时，将 RL 微调速度提升了 2-3 倍。

#### 7. 优点
*   **架构创新**：将“家族模型”概念引入多模态，解决了多尺度模型独立训练的冗余问题。
*   **隐私保护闭环**：基于信息瓶颈理论实现了数学意义上的不可逆脱敏，而非简单的视觉遮盖。
*   **训练效率**：BPPO 针对长链推理的冗余问题提出了极具实操性的优化方案。

#### 8. 不足与局限
*   **硬件细节缺失**：缺乏具体的算力消耗和训练环境描述，不利于其他研究者评估复现成本。
*   **应用限制**：虽然在监控场景表现优异，但对于需要极高实时性的边缘端（如 1.9B 以下更小模型）的性能边界探索不足。
*   **偏差风险**：数据过滤阶段使用了高能力模型进行评估，可能引入教师模型的固有偏见（Hallucination 传递）。

（完）
