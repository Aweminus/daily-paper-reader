---
title: Improving Visual Reasoning with Iterative Evidence Refinement
title_zh: 通过迭代证据细化提升视觉推理能力
authors: "Zeru Shi, Kai Mei, Yihao Quan, Dimitris N. Metaxas, Ruixiang Tang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.14117v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过迭代证据细化提高视觉语言模型的视觉推理能力
tldr: "视觉语言模型在复杂推理中常需重新审视图像细节，但现有方法依赖外部缩放或裁剪工具，效率较低。本文提出SIEVE框架，通过端到端的自我回访机制，利用模型内部信号提取显著区域嵌入并注入推理链。该方法采用强化学习训练模型自主决定回访时机与内容，无需外部重编码即可增强视觉基准。实验显示，SIEVE在多个基准测试中平均提升8%的性能，显著增强了模型的感知与推理能力。"
selection_source: fresh_fetch
motivation: 现有视觉语言模型在推理时依赖外部工具重新获取细节，导致效率低下且推理链条中断。
method: 提出SIEVE框架，利用强化学习训练模型在推理过程中自动提取并注入关键区域的内部嵌入表示。
result: "在多个视觉推理基准测试中，SIEVE平均性能提升了8%，并有效改善了感知、推理和幻觉问题。"
conclusion: 通过内部表征的自我回访机制，模型无需外部工具即可实现更精准的图像辅助推理。
---

## 摘要
视觉语言模型（VLM）在图像推理方面的能力日益增强，但鲁棒的视觉推理通常需要将中间步骤重新关联到基础视觉证据中。最近的方法通常依赖于缩放或裁剪等外部图像操作，以便在推理过程中重新获取细粒度细节，这需要额外的图像重新编码，并可能干扰推理轨迹。我们认为，VLM 已经提供了用于识别和重用视觉证据的强大内部信号，这些信号可以直接被利用来支持基于图像的推理。受此启发，我们提出了一种端到端的自我回顾（self-revisit）框架 SIEVE，该框架训练模型通过内部表示重新调用图像证据。SIEVE 自动提取显著图像区域的嵌入，并在需要额外关联时将其注入推理链，使后续步骤能够在无需外部工具调用或重新编码的情况下，以相关的视觉线索为条件。我们使用强化学习来教导模型何时触发视觉回顾，以及在推理过程中检索并插入哪些区域嵌入。在多个视觉推理基准测试上的实验，结合感知、推理和幻觉评估，表明 SIEVE 带来了持续的性能提升，在多个基准测试中平均性能提高了 8%。

## Abstract
Vision language models (VLMs) are increasingly capable of reasoning over images, but robust visual reasoning often requires re-grounding intermediate steps in the underlying visual evidence. Recent approaches typically rely on external image operations such as zooming or cropping to re-access fine-grained details during inference, which requires additional image re-encoding and can disrupt the reasoning trajectory. We argue that VLMs already provide strong internal signals for identifying and reusing visual evidence, and that these signals can be directly leveraged to support image-grounded reasoning. Motivated by this insight, we propose an end-to-end self-revisit framework, SIEVE, that trains models to re-engage image evidence through internal representations. SIEVE automatically extracts embeddings of salient image regions and injects them into the reasoning chain when additional grounding is needed, enabling later steps to condition on relevant visual cues without external tool calls or re-encoding. We use reinforcement learning to teach the model when to trigger visual revisiting and which region embeddings to retrieve and insert during the reasoning process. Experiments on multiple visual reasoning benchmarks, together with perception, reasoning, and hallucination evaluations, show that SIEVE yields consistent gains, improving performance by 8 percent on average across several benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Improving Visual Reasoning with Iterative Evidence Refinement》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管视觉语言模型（VLM）在多模态推理上取得了长足进步，但在长程推理（如思维链 CoT）中，模型往往会逐渐偏向文本生成，而忽略了初始图像中的细粒度视觉证据。
*   **现有局限**：目前主流的解决方法（如 OpenAI o3 风格的方案）依赖于外部工具进行“缩放（Zoom）”或“裁剪（Crop）”。这种方式存在两个弊端：一是需要对子图进行重复编码，计算开销大；二是外部操作会中断推理流的连续性，且难以精确集成到 CoT 的中间步骤中。
*   **核心假设**：VLM 内部的原始视觉嵌入（Embeddings）已经包含了足够的细粒度信息，瓶颈在于模型缺乏在推理过程中主动、选择性地“回访”这些内部证据的能力。

### 2. 方法论：核心思想与关键技术
论文提出了 **SIEVE** 框架，这是一种端到端的自我回访（Self-revisit）机制：
*   **核心思想**：在推理过程中，当模型意识到需要更多视觉细节时，直接从原始编码的隐藏状态中提取相关区域的嵌入，并将其作为“视觉证据”实时注入到当前的推理序列中，无需调用外部工具或重新编码图像。
*   **关键技术细节**：
    *   **自我引导的证据识别（Algorithm 1）**：
        1.  **文本锚点发现**：利用**梯度显著性（Gradient Saliency）**识别对预测结果影响最大的文本 Token（如特定的物体名称或属性）。
        2.  **视觉区域定位**：在模型的中间层（Middle Layers）计算文本锚点与图像 Patch 嵌入之间的跨模态相似度，定位出最相关的空间区域。
        3.  **证据提取**：将选定区域的 Patch 嵌入拼接成“证据快照（Evidence Snapshot）”。
    *   **视觉基准强化学习（RL）**：
        *   使用 **GRPO** 算法训练模型。
        *   **动作空间**：模型可以决定继续生成文本，或者触发“嵌入注入”操作。
        *   **奖励函数设计**：由格式奖励（Rformat）、结果正确性奖励（Rres）、嵌入使用奖励（Remb）和动作稳定性奖励（Ract）四部分组成，旨在引导模型在正确的时间点使用正确的视觉证据。

### 3. 实验设计
*   **数据集与场景**：
    *   **高分辨率推理**：V* Bench, HR-Bench (4K & 8K)。
    *   **通用感知与推理**：MME-Real-Lite, RealWorldQA, MathVista, LogicVista, WeMath。
    *   **幻觉评估**：HallusionBench。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen3-VL-4B/8B-Instruct。
    *   **对比方法**：Vanilla（原始模型）、DyFo、ZoomEye、Zoom-Refine（均为基于缩放/裁剪的工具增强方法）以及纯 GRPO 训练的消融版本。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   训练数据仅需约 **1,500 个样本**（来自 COCO 2017）。
    *   进行 60 个 Rollout Steps，每个 Batch 包含 16 个 Prompts，每个 Prompt 生成 8 个 Rollouts。
    *   最大响应长度设为 8,192 Tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 个主流基准测试集上进行了评估，涵盖了从基础感知到复杂数学逻辑推理的多个维度。
*   **消融实验**：
    *   验证了注入嵌入的有效性（对比随机注入）。
    *   分析了不同 Transformer 层对证据提取质量的影响（证明中间层效果最好）。
    *   探讨了 K 值（注入区域数量）的影响（发现 K=1 时效果最佳，过多会引入噪声）。
    *   验证了动作奖励对防止奖励崩溃和生成冗余内容的重要性。
*   **评价**：实验设计全面且客观，通过与多种 SOTA 工具增强方法的对比，证明了 SIEVE 在性能和效率上的双重优势。

### 6. 主要结论与发现
*   **性能提升**：SIEVE 在多个基准测试中平均提升了 **8%** 的性能，在 MME-Real-Lite 等任务上提升甚至超过 10%。
*   **内部信号有效性**：实验证明 VLM 内部隐藏状态确实蕴含了未被充分利用的细粒度视觉信息，通过 RL 可以有效激活这种“自我回访”能力。
*   **层级特性**：中间层表征在跨模态对齐上表现最稳定，而浅层噪声多，深层则过于偏向任务输出。

### 7. 优点
*   **高效性**：无需外部图像处理工具，避免了重复编码图像的计算开销，推理延迟增加极小。
*   **连续性**：将视觉证据直接注入推理链，保持了 CoT 的逻辑连贯性。
*   **数据效率**：仅需极少量的训练数据（1.5k）即可让模型学会复杂的证据回访策略。
*   **可解释性**：通过可视化提取的区域嵌入，可以直观看到模型推理时的“注意力”所在。

### 8. 不足与局限
*   **Patch 粒度限制**：受限于 Qwen-VL 等模型的 Patch 分割机制，提取的区域可能存在微小的边界漂移（Box Drift）。
*   **噪声敏感性**：当注入的区域过多（K 值增大）时，模型容易受到无关视觉信息的干扰，导致性能下降。
*   **模型依赖性**：该方法高度依赖于基座模型中间层表征的质量，对于表征能力较弱的模型，提升可能有限。

（完）
