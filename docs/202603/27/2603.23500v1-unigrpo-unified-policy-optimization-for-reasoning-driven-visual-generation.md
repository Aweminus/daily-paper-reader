---
title: "UniGRPO: Unified Policy Optimization for Reasoning-Driven Visual Generation"
title_zh: UniGRPO：推理驱动视觉生成的统一策略优化
authors: "Jie Liu, Zilyu Ye, Linxiao Yuan, Shenhan Zhu, Yu Gao, Jie Wu, Kunchang Li, Xionghui Wang, Xiaonan Nie, Weilin Huang, Wanli Ouyang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23500v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 推理驱动多模态生成的强化学习
tldr: 本研究针对文本与图像交错生成的统一模型，提出了 UniGRPO 强化学习框架。该框架将推理驱动的图像生成建模为具有稀疏奖励的马尔可夫决策过程，通过整合标准 GRPO 和改进的 FlowGRPO，实现了文本推理与视觉合成策略的联合优化。通过引入速度场 MSE 惩罚和移除无分类器引导，该方法在提升生成质量的同时增强了系统的可扩展性，为多模态模型的后期训练奠定了基础。
selection_source: fresh_fetch
motivation: 旨在解决交错生成模型中如何统一优化文本推理与图像合成，以提升推理驱动的视觉生成质量。
method: 提出 UniGRPO 框架，结合 GRPO 与 FlowGRPO，并引入速度场 MSE 惩罚项以替代传统的 KL 散度约束。
result: 实验表明该方法显著增强了模型通过推理提升图像生成的能力，并有效缓解了强化学习中的奖励作弊问题。
conclusion: UniGRPO 为未来全交错多模态模型的规模化后期训练提供了一个高效、稳健且可扩展的基准方案。
---

## 摘要
能够进行交错生成的统一模型已成为一种极具前景的范式，社区正日益趋向于将自回归建模用于文本生成，将流匹配（flow matching）用于图像生成。为了推进这一方向，我们提出了一个专为交错生成定制的统一强化学习框架。我们在其基本单元上验证了我们的方法：单轮推理驱动的图像生成，即模型首先通过推理扩展用户提示词，随后进行图像合成。我们将这一多模态生成过程建模为具有稀疏终端奖励的马尔可夫决策过程（MDP），并引入 UniGRPO，利用 GRPO 联合优化文本和图像生成策略。我们采用极简主义方法以避免过度设计，通过无缝集成用于推理的标准 GRPO 和用于视觉合成的 FlowGRPO，利用了两种模态已有的成熟训练方案。为了确保对多轮交错生成的可扩展性，我们对原始 FlowGRPO 进行了两项关键改进：(1) 消除无分类器指导（classifier-free guidance），以保持线性的、无分支的展开（rollouts），这对于扩展到涉及多轮交互和多条件生成（如编辑）的复杂场景至关重要；(2) 将标准的潜空间 KL 惩罚替换为直接作用于速度场（velocity fields）的 MSE 惩罚，从而提供更稳健、更直接的正则化信号，有效缓解奖励作弊（reward hacking）问题。我们的实验表明，这种统一的训练方案通过推理显著提升了图像生成质量，为未来全交错模型的后训练提供了一个稳健且可扩展的基准。

## Abstract
Unified models capable of interleaved generation have emerged as a promising paradigm, with the community increasingly converging on autoregressive modeling for text and flow matching for image generation. To advance this direction, we propose a unified reinforcement learning framework tailored for interleaved generation. We validate our approach on its fundamental unit: a single round of reasoning-driven image generation, where the model first expands the user prompt through reasoning, followed by image synthesis. Formulating this multimodal generation process as a Markov Decision Process with sparse terminal rewards, we introduce UniGRPO to jointly optimize text and image generation policies using GRPO. Adopting a minimalist methodology to avoid over-design, we leverage established training recipes for both modalities by seamlessly integrating standard GRPO for reasoning and FlowGRPO for visual synthesis. To ensure scalability to multi-round interleaved generation, we introduce two critical modifications to the original FlowGRPO: (1) eliminating classifier-free guidance to maintain linear, unbranched rollouts, which is essential for scaling to complex scenarios involving multi-turn interactions and multi-condition generation (e.g., editing); and (2) replacing the standard latent KL penalty with an MSE penalty directly on the velocity fields, providing a more robust and direct regularization signal to mitigate reward hacking effectively. Our experiments demonstrate that this unified training recipe significantly enhances image generation quality through reasoning, providing a robust and scalable baseline for the future post-training of fully interleaved models.

---

## 论文详细总结（自动生成）

以下是对论文《UniGRPO: Unified Policy Optimization for Reasoning-Driven Visual Generation》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着多模态模型向“交错生成”（Interleaved Generation）演进，社区逐渐形成了“自回归（AR）处理文本 + 流匹配（Flow Matching）处理图像”的架构共识。然而，如何通过统一的强化学习（RL）框架来联合优化这两种模态，特别是利用“思维链推理”来提升视觉生成质量，仍是一个挑战。
本研究旨在提出一种名为 **UniGRPO** 的统一策略优化框架，将“提示词 -> 推理（Thinking） -> 图像生成”这一过程建模为一个完整的马尔可夫决策过程（MDP），通过联合优化文本推理和视觉合成策略，使模型能够通过“思考”生成更高质量、更符合指令的图像。

### 2. 方法论：核心思想与关键技术
UniGRPO 的核心思想是将离散的语言动作和连续的视觉动作整合在同一个 RL 循环中，利用组相对策略优化（GRPO）进行训练。

*   **统一 MDP 建模**：
    *   **状态空间**：包含输入提示词、已生成的推理 Token 以及当前的图像噪声潜变量。
    *   **动作空间**：文本阶段为词表中的 Token 预测；图像阶段为流匹配中的下一步去噪潜变量。
    *   **奖励机制**：采用**稀疏终端奖励**，即仅在图像完全生成后，根据最终图像质量和对齐度给出奖励。
*   **关键技术改进（针对可扩展性）**：
    *   **移除无分类器引导（CFG-Free Training）**：在训练中取消 CFG，保持线性的、无分支的采样路径。这降低了计算开销，并使模型将对齐能力内化到权重中，便于扩展到多轮交互。
    *   **速度场 MSE 正则化（Velocity-Based Regularization）**：放弃传统的潜空间 KL 散度惩罚，改为直接对预测的速度场（Velocity Fields）施加 MSE 惩罚。这提供了更均匀的约束，有效防止了 RL 训练中常见的“奖励作弊”（Reward Hacking）和图像伪影。
*   **算法流程**：针对每个提示词生成一组（Group）推理链和对应的图像，计算组内相对优势（Advantage），并同时更新文本自回归策略和图像流匹配策略。

### 3. 实验设计
*   **基础模型**：以 **Bagel**（一种交错生成模型）为底座，并先进行了一轮监督微调（SFT）以建立强基准。
*   **数据集/场景**：
    *   **TA (Text Alignment) Benchmark**：内部评估集，包含 150 个复杂提示词，由 VLM 根据多个检查点进行评分。
    *   **GenEval**：标准的文本到图像生成评测集，重点考察物体计数、空间关系和属性绑定。
*   **对比方法**：
    *   **ReFL**（奖励反向传播）、**FPO/AWR**（优势加权匹配）。
    *   **消融基准**：仅优化文本（TextGRPO）、仅优化图像（FlowGRPO）、不带推理的生成等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量（如 H100 或 A100 的具体规模）。
*   **训练参数**：提到了训练分辨率为 1024x1024，组大小（Group Size）为 24，Batch Size 为 32，训练步数在 2000 到 5000 步之间。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了主实验对比（表 1 和表 2）、训练曲线分析（图 3）、CFG 影响消融（图 4）以及正则化策略消融（图 5）。
*   **充分性评价**：实验设计较为全面，涵盖了定量指标和定性分析。通过与多种主流扩散模型 RL 方法（如 ReFL, FPO）进行对比，证明了 UniGRPO 的优越性。消融实验针对性强，客观地验证了速度场 MSE 和移除 CFG 的必要性。

### 6. 主要结论与发现
*   **联合优化更优**：同时优化推理文本和图像生成的效果优于单独优化其中任何一项。
*   **推理驱动增益**：通过 RL 优化的推理过程（Thinking Trace）变得更加任务导向，能有效引导模型生成更符合逻辑的图像（如正确处理物体数量和空间位置）。
*   **正则化至关重要**：速度场 MSE 惩罚比传统的 KL 惩罚更能稳定训练，能有效消除图像中的网格伪影和过度饱和问题。
*   **CFG 可替代性**：在 RL 阶段移除 CFG 不仅不会降低性能，反而能通过策略优化使模型内化对齐能力。

### 7. 优点（亮点）
*   **极简主义设计**：没有引入复杂的辅助网络或多阶段流程，直接在统一模型上实现端到端 RL。
*   **高可扩展性**：通过移除 CFG 和优化正则化，为未来处理多轮对话、图像编辑等复杂交错场景扫清了计算障碍。
*   **解决奖励作弊**：提出的速度场 MSE 正则化是处理流匹配模型 RL 训练的一个稳健方案。

### 8. 不足与局限
*   **场景局限**：目前仅在“单轮推理 -> 图像生成”这一基本单元上进行了验证，尚未展示在真正的长程多轮交错对话中的表现。
*   **依赖奖励模型**：性能高度依赖于外部奖励模型（如 InternVL）的准确性，若奖励模型存在偏见，生成结果也会受限。
*   **算力细节缺失**：缺乏具体的算力消耗报告，难以评估该方法在大规模生产环境下的训练成本。

（完）
