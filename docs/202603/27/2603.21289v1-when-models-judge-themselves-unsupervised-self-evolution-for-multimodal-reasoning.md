---
title: "When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning"
title_zh: 当模型自我评判：多模态推理的无监督自我进化
authors: "Zhengxian Wu, Kai Shi, Chuanrui Zhang, Zirui Liao, Jun Yang, Ni Yang, Qiuying Peng, Luyuan Zhang, Hangrui Xu, Tianhuang Su, Zhenyu Yang, Haonan Lu, Haoqian Wang"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21289v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 无机器人参与的多模态推理无监督自我进化
tldr: 针对多模态大模型推理提升依赖高成本标注数据的问题，本文提出一种无监督自进化训练框架。该框架通过对同一输入采样多个推理路径，利用自一致性信号作为训练先验，并引入受限的判别器调制机制来动态重权不同质量的路径。结合组相对策略优化（GRPO），模型能在无标签数据上实现性能的持续提升。实验表明，该方法在五个数学推理基准上表现优异，为多模态模型的规模化自进化提供了有效路径。
selection_source: fresh_fetch
motivation: 现有多模态推理模型的性能提升高度依赖昂贵的人工标注或教师模型蒸馏，限制了其大规模扩展。
method: 提出一种无监督自进化框架，利用自一致性信号和受限判别器调制来重权推理路径，并采用组相对策略优化（GRPO）进行训练。
result: 在五个数学推理基准测试中，该方法在不使用人工标注或外部奖励模型的情况下，显著提升了模型的推理性能和泛化能力。
conclusion: 该研究验证了多模态模型通过无监督自进化实现性能提升的可行性，为构建可扩展的自进化智能系统提供了新方案。
---

## 摘要
近期多模态大语言模型的进展在推理任务上取得了强劲表现，但这些提升很大程度上依赖于高质量的标注数据或教师模型蒸馏，这两者都成本高昂且难以扩展。为解决这一问题，我们提出了一种用于多模态推理的无监督自我进化训练框架，在不使用人工标注答案或外部奖励模型的情况下实现了稳定的性能提升。对于每个输入，我们采样多条推理轨迹，并联合建模它们的组内结构。我们利用 Actor 的自一致性信号作为训练先验，并引入一种基于有界判别器（Judge）的调制机制，以持续对不同质量的轨迹进行重新加权。我们进一步将调制后的分数建模为组级分布，并将绝对分数转换为每组内的相对优势，从而实现更稳健的策略更新。通过在无标签数据上使用组相对策略优化（GRPO）进行训练，我们的方法在五个数学推理基准测试中持续提升了推理性能和泛化能力，为迈向自我进化的多模态模型提供了一条可扩展的路径。代码可在 https://dingwu1021.github.io/SelfJudge/ 获取。

## Abstract
Recent progress in multimodal large language models has led to strong performance on reasoning tasks, but these improvements largely rely on high-quality annotated data or teacher-model distillation, both of which are costly and difficult to scale.To address this, we propose an unsupervised self-evolution training framework for multimodal reasoning that achieves stable performance improvements without using human-annotated answers or external reward models. For each input, we sample multiple reasoning trajectories and jointly model their within group structure.We use the Actor's self-consistency signal as a training prior, and introduce a bounded Judge based modulation to continuously reweight trajectories of different quality.We further model the modulated scores as a group level distribution and convert absolute scores into relative advantages within each group, enabling more robust policy updates. Trained with Group Relative Policy Optimization (GRPO) on unlabeled data, our method consistently improves reasoning performance and generalization on five mathematical reasoning benchmarks, offering a scalable path toward self-evolving multimodal models.The code are available at https://dingwu1021.github.io/SelfJudge/.

---

## 论文详细总结（自动生成）

以下是对论文《When Models Judge Themselves: Unsupervised Self-Evolution for Multimodal Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态大语言模型（MLLM）在推理任务上的进步高度依赖高质量的人工标注数据或强力教师模型的蒸馏，这些资源成本高昂且难以规模化扩展。
*   **研究背景**：现有的无监督自进化方法（如“多数投票”机制）虽然能提供伪标签，但存在严重缺陷：
    *   **系统性偏差**：高一致性的答案不一定正确，模型可能在错误路径上产生共识并不断自我强化。
    *   **训练不稳定**：单一的伪标签信号容易导致梯度波动。
    *   **模式崩溃**：训练后期模型往往会迅速收敛到少数几个模式，导致输出熵降低和响应长度坍缩。

### 2. 方法论：核心思想与关键技术
该论文提出了一种**无监督自进化框架**，通过同一模型的两个角色（Actor 和 Judge）协作，在无标签数据上实现持续进化。
*   **Actor-Judge 协作机制**：
    *   **Actor（演员）**：针对输入采样 $n$ 条推理轨迹，形成自一致性（Self-Consistency, SC）分布。
    *   **Judge（评判员）**：由初始模型冻结而成，对每条轨迹的答案正确性、推理质量和视觉对齐进行评分。
*   **奖励调制与校准**：
    *   **初始奖励**：基于答案出现的频率（SC 信号）。
    *   **有界调制**：引入一个连续可微且有界的校准函数 $g(s)$，利用 Judge 的评分对 SC 奖励进行重权。这种“有界”设计防止了 Judge 的噪声在无监督循环中被无限放大。
*   **组内分布建模（核心优化）**：
    *   采用 **GRPO（组相对策略优化）**。
    *   不直接使用绝对分数，而是将一组轨迹的分数通过指数归一化（Energy-based scaling）转化为组内相对优势（Relative Advantage）。
    *   **公式逻辑**：通过匹配奖励诱导的目标分布，使策略更新不再是简单地放大早期主导模式，而是平滑地重新分配概率质量，从而维持探索能力并防止崩溃。

### 3. 实验设计
*   **训练数据集**：Geometry3k、GeoQA、MMR1（均为无标签使用）。
*   **评估基准（Benchmarks）**：
    *   **数学推理**：MathVision, MathVerse, WeMath, LogicVista, DynaMath。
    *   **通用/图表推理（泛化性测试）**：ChartQA, MMVP。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL-7B-Instruct。
    *   **无监督自进化基线**：VisionZero, EvoLMM, MM-UPT（基于多数投票）。
    *   **有监督/蒸馏方法**：R1-Onevision, Vision-R1 等。

### 4. 资源与算力
*   **硬件环境**：单节点 8 × NVIDIA A800 (80GB) GPU。
*   **训练框架**：基于 Verl 框架实现。
*   **训练时长**：文中提到有监督 GRPO 训练约需 10.5 小时，而本方法由于涉及在线采样和 Judge 评分，引入了约 **1.4 倍**的额外时间开销。
*   **超参数**：训练 20 个 epoch，学习率 $1 \times 10^{-6}$，每条题目采样 8 条轨迹。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 5 个核心数学基准上进行了详尽测试。
    *   **消融实验**：分别验证了自一致性、Judge 评分、分布建模（Distributional Modeling）各自的贡献。
    *   **模型扩展性**：验证了从 2B 到 32B 不同规模（如 InternVL3, GLM-4, Qwen2.5-VL 系列）底座模型的有效性。
    *   **泛化性实验**：在非数学任务（图表、通用视觉）上验证了性能提升。
*   **充分性评价**：实验设计非常充分，不仅关注准确率，还深入分析了训练动力学（如熵的变化、响应长度、Pass@10 等指标），客观地展示了方法在防止模式崩溃方面的优势。

### 6. 主要结论与发现
*   **性能显著提升**：在 MathVision 上，无监督训练使准确率从 25.0% 提升至 30.9%（+5.9%），效果接近甚至超过部分有监督蒸馏方法。
*   **克服多数投票局限**：相比于传统的多数投票（MM-UPT），该方法能有效纠正“错误的共识”，在 Pass@10 指标上表现更稳健。
*   **训练稳定性**：通过组内相对优化，模型在训练过程中保持了更健康的输出熵和响应长度，避免了过早的确定性坍缩。

### 7. 优点（亮点）
*   **完全脱离外部监督**：不需要人工标注，也不需要外部更强的奖励模型（Reward Model），实现了真正的“闭环自进化”。
*   **奖励建模的创新**：将 Judge 评分作为“调制信号”而非“绝对权威”，结合有界函数设计，极大地增强了无监督学习的鲁棒性。
*   **理论支撑**：对 GRPO 在分布匹配（Distribution Matching）方面的作用做了深入的数学推导，解释了为何该方法能防止策略崩溃。

### 8. 不足与局限
*   **Judge 的能力上限**：自进化的效果仍受限于初始 Judge 模型的判别能力。如果 Actor 和 Judge 同时对某一错误达成强共识，模型仍会学到错误知识。
*   **计算开销**：在线采样 8 条轨迹并进行 Judge 评分增加了推理成本和训练时长（1.4x）。
*   **自进化停滞风险**：论文提到如何超越 Judge 自身能力上限（即如何实现“左脚踩右脚上天”）仍是未来需要解决的难题。

（完）
