---
title: Memory-Augmented Vision-Language Agents for Persistent and Semantically Consistent Object Captioning
title_zh: 用于持久且语义一致物体描述的记忆增强视觉语言智能体
authors: "Tommaso Galliena, Stefano Rosa, Tommaso Apicella, Pietro Morerio, Alessio Del Bue, Lorenzo Natale"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24257v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于探索的记忆增强视觉语言智能体
tldr: 针对视觉语言模型在不同视角下对同一物体描述不一致的问题，本文提出了一种增强记忆的视觉语言智能体。该模型将数据关联、物体描述和探索策略集成在统一的自回归框架中，通过物体级情节记忆确保跨序列的语义一致性。通过自监督学习和多视图伪标签训练，该方法在物体描述准确性和自相似性上显著优于现有基准，实现了可扩展且一致的场景表征。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在多视角下对同一物体的描述往往缺乏一致性，且现有方法多采用离线或多阶段流程，难以实现实时的语义持久性。
method: 提出一种统一的自回归框架，利用当前观测、俯视图和序列化的物体级情节记忆令牌，同步处理数据关联、物体描述和探索策略。
result: "在手动标注的测试集上，该模型在标准描述指标上提升了11.86%，在描述自相似性上提升了7.39%。"
conclusion: 该研究证明了通过集成物体级记忆和自监督训练，可以有效提升具身智能体在复杂环境中的语义一致性和描述性能。
---

## 摘要
视觉语言模型（VLM）在不同视角下对同一物体的描述往往不一致，这阻碍了具身智能体随时间构建一致语义表示的能力。先前的方法通过离线多视图聚合或多阶段流水线来解决不一致性，这些流水线将探索、数据关联和描述学习解耦，对先前观察到的物体的推理能力有限。在本文中，我们引入了一种统一的记忆增强视觉语言智能体，它在单个自回归框架内同时处理数据关联、物体描述和探索策略。该模型处理当前的 RGB 观测、自顶向下的探索地图以及序列化为物体级标记（tokens）的物体级情境记忆，从而确保在长序列中保持持久的物体身份和语义一致性。为了以自监督方式训练模型，我们利用基于分歧的策略和伪描述模型在逼真的 3D 环境中收集了一个数据集，该伪描述模型强制要求多视图描述历史之间的一致性。在人工标注的物体级测试集上的广泛评估表明，与基准模型相比，标准描述评分提高了高达 11.86%，描述自相似性提高了 7.39%，同时通过紧凑的场景表示实现了可扩展的性能。代码、模型权重和数据可在 https://github.com/hsp-iit/epos-vlm 获取。

## Abstract
Vision-Language Models (VLMs) often yield inconsistent descriptions of the same object across viewpoints, hindering the ability of embodied agents to construct consistent semantic representations over time. Previous methods resolved inconsistencies using offline multi-view aggregation or multi-stage pipelines that decouple exploration, data association, and caption learning, with limited capacity to reason over previously observed objects. In this paper, we introduce a unified, memory-augmented Vision-Language agent that simultaneously handles data association, object captioning, and exploration policy within a single autoregressive framework. The model processes the current RGB observation, a top-down explored map, and an object-level episodic memory serialized into object-level tokens, ensuring persistent object identity and semantic consistency across extended sequences. To train the model in a self-supervised manner, we collect a dataset in photorealistic 3D environments using a disagreement-based policy and a pseudo-captioning model that enforces consistency across multi-view caption histories. Extensive evaluation on a manually annotated object-level test set, demonstrate improvements of up to +11.86% in standard captioning scores and +7.39% in caption self-similarity over baseline models, while enabling scalable performance through a compact scene representation. Code, model weights, and data are available at https://github.com/hsp-iit/epos-vlm

---

## 论文详细总结（自动生成）

这是一份关于论文《Memory-Augmented Vision-Language Agents for Persistent and Semantically Consistent Object Captioning》（EPOS-VLM）的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉语言模型（VLM）在处理静态图像时表现优异，但在具身智能（Embodied AI）场景下存在严重缺陷。当智能体在 3D 环境中移动时，由于视角、距离和遮挡的变化，VLM 对同一个物理物体的描述往往前后矛盾（语义不一致），且缺乏“物体持久性”意识。
*   **核心问题**：如何让具身智能体在导航过程中，针对同一物体构建跨视角、时间持久且语义一致的语言描述，并利用这种语义理解来指导其探索行为？

### 2. 方法论
EPOS-VLM 提出了一种统一的、记忆增强的视觉语言动作（VLA）框架：
*   **核心思想**：将**数据关联（物体匹配）**、**物体描述**和**动作预测**集成到一个单一的自回归 Transformer 框架中，通过“物体级情节记忆”实现长程推理。
*   **关键技术细节**：
    *   **结构化记忆 Token 化**：将情节记忆（包含物体 ID、3D 坐标、历史描述及其频率）序列化为特定的文本 Token（如 `[OBJ-ID]`, `[POS]`, `[CAP-HISTORY]`），直接作为 VLM 的上下文输入。
    *   **统一输出序列**：模型在每个时间步处理当前 RGB 观测、探索地图和记忆 Token，随后按顺序输出：`[MATCH]`（将当前检测到的物体与记忆 ID 关联）、`[CAPTION]`（生成一致性描述）和 `[ACTION]`（导航动作）。
    *   **3D-CPS 伪描述生成器**：在训练阶段，利用 3D 几何信息聚合多视角观察，通过一个大型 VLM（Qwen3-VL-30B）生成过滤掉视角相关细节、仅保留物体本质属性的“伪标签”来监督模型学习。
    *   **主动探索策略**：模型被训练为倾向于重新访问那些描述分歧大（语义不稳定）的物体，以通过更多视角消除歧义。

### 3. 实验设计
*   **数据集与场景**：
    *   使用 **Habitat** 仿真平台，基于 **HM3D**（128 个训练场景，28 个测试场景）和 **Gibson**（32 个场景，用于跨域泛化测试）数据集。
    *   **Benchmark**：构建了一个手动标注的物体级描述测试集，包含准确的物体属性描述。
*   **对比方法**：
    *   **基础 VLM**：Qwen3-VL-2B, BLIP-2, InternVL-3B, CoCa, Florence。
    *   **具身描述基准**：IC3 (2023), ECO-score (2024), 以及 Galliena 等人之前的多阶段流水线方法 (2025)。
    *   **策略对比**：随机目标策略、前沿探索（Frontier Exploration）策略。

### 4. 资源与算力
*   **硬件**：使用了 **2 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   采用 **LoRA**（低秩自适应）进行高效微调。
    *   训练 3 个 Epoch，使用 AdamW 优化器，学习率为 $2 \times 10^{-4}$。
    *   Batch Size 为 16，梯度累加步数为 4。
*   **推理效率**：相比于基于点云的密集表征方法，EPOS-VLM 的内存占用极低（<10 KB vs >1 GB），且推理时间保持恒定（约 0.7s/步）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    1.  **伪标签质量评估**：证明 3D-CPS 生成的标签优于现有聚合方法。
    2.  **描述准确性与一致性**：在 HM3D 和 Gibson 上进行了大量对比。
    3.  **消融实验**：验证了“情节记忆”模块对性能的贡献（移除记忆后性能大幅下降）。
    4.  **数据关联测试**：评估了模型在长程导航中追踪物体 ID 的能力。
    5.  **策略效能实验**：对比了不同导航策略对最终语义理解质量的影响。
*   **评价**：实验设计非常充分且客观。通过跨数据集（Gibson）的测试验证了模型的泛化能力，并提供了详细的定性分析（可视化描述演变过程）。

### 6. 论文的主要结论与发现
*   **性能提升**：EPOS-VLM 在标准描述指标（如 SPICE）上比最强基准提升了 **11.86%**，在描述自相似性（一致性）上提升了 **7.39%**。
*   **记忆的重要性**：实验证明，将物体历史作为 Token 引入上下文，比单纯依赖视觉特征更能有效解决语义漂移问题。
*   **动作与语义的耦合**：主动选择视角的策略能显著提高描述质量，证明了“为理解而行动”的有效性。
*   **可扩展性**：基于文本 Token 的记忆表征在处理长序列时比几何地图（点云）更具计算优势。

### 7. 优点（亮点）
*   **端到端集成**：首次将数据关联、描述和导航动作统一在一个自回归 VLM 框架下，避免了多阶段方法中的误差累积。
*   **紧凑的表征**：通过 Token 化记忆解决了具身智能中长期存在的“内存爆炸”问题，实现了极高的计算效率。
*   **自监督闭环**：提出了一套完整的从自动数据采集、伪标签生成到端到端微调的流程。

### 8. 不足与局限
*   **依赖外部检测器**：模型仍需依赖预训练的实例分割模型（如 Mask2Former）来提供物体边界框，若检测器失效，后续流程会受影响。
*   **静态环境假设**：实验主要在静态 3D 场景中进行，未讨论动态环境（物体移动或消失）下的记忆更新机制。
*   **传感器噪声**：虽然在仿真中表现良好，但在现实世界中，深度传感器的噪声可能会影响 3D 坐标的准确性，进而影响 Token 化的位置信息。

（完）
