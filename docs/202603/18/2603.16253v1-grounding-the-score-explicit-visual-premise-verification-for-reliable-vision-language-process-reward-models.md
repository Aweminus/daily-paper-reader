---
title: "Grounding the Score: Explicit Visual Premise Verification for Reliable Vision-Language Process Reward Models"
title_zh: 评分落地：面向可靠视觉语言过程奖励模型的显式视觉前提验证
authors: "Junxin Wang, Dai Guan, Weijie Qiu, Zhihang Li, Yongbo Gai, Zhengyi Yang, Mengyu Zhou, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16253v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于对视觉语言中间推理步骤进行评分的过程奖励模型
tldr: 针对视觉语言过程奖励模型（VL-PRM）在感知与推理评估上的混淆问题，本文提出显式视觉前提验证（EVPV）框架。该方法通过让模型生成步骤级视觉清单，并将其与独立提取的图像结构化约束进行匹配，计算视觉可靠性信号来校准奖励分数。EVPV成功解耦了感知不确定性与逻辑评估，在多个多模态推理基准上显著提升了重排序准确率和错误定位能力。
selection_source: fresh_fetch
motivation: 现有的VL-PRM作为黑盒评估器，无法区分推理错误与视觉感知偏差，导致评分存在系统性误报和漏报。
method: 引入EVPV接口，通过对比步骤依赖的视觉清单与独立提取的结构化视觉约束，实现对步骤奖励的可靠性门控校准。
result: 实验表明EVPV在VisualProcessBench及六个多模态推理基准上均优于强基准模型，显著增强了Best-of-N重排序效果。
conclusion: 显式验证视觉前提能有效提升多模态奖励模型的可靠性，为解决视觉幻觉和逻辑评估解耦提供了有力证据。
---

## 摘要
视觉语言过程奖励模型 (VL-PRMs) 越来越多地被用于对中间推理步骤进行评分，并在测试时扩展 (test-time scaling) 下对候选结果进行重排序。然而，它们通常作为黑盒评判器运行：较低的步骤得分可能反映了真实的推理错误，或者仅仅是验证器对图像的误感知。这种感知与推理之间的纠缠导致了系统性的假阳性（奖励幻觉视觉前提）和假阴性（惩罚正确的落地陈述），从而损害了重排序和错误定位的效果。我们提出了显式视觉前提验证 (EVPV)，这是一种轻量级的验证接口，它根据步骤所依赖的视觉前提的可靠性来调节步骤评分。该方法提示策略生成逐步的视觉清单，使所需的视觉事实显式化，同时由约束提取器独立地从输入图像中导出结构化的视觉约束。EVPV 将清单中的主张与这些约束进行匹配，以计算标量视觉可靠性信号，并通过可靠性门控校准 PRM 步骤奖励：当可靠性较低时，视觉依赖步骤的奖励会被衰减；当可靠性较高时，奖励则被保留。这在无需逐步调用工具的情况下，实现了感知不确定性与逻辑评估的解耦。在 VisualProcessBench 和六个多模态推理基准测试上的实验表明，EVPV 改进了步骤级验证，并相比强基准模型持续提升了 Best-of-N 重排序的准确率。此外，向提取的约束中注入受控的损坏会导致性能单调下降，这提供了因果证据，证明收益源于约束保真度和显式前提验证，而非偶然的提示效应。代码开源地址：https://github.com/Qwen-Applications/EVPV-PRM

## Abstract
Vision-language process reward models (VL-PRMs) are increasingly used to score intermediate reasoning steps and rerank candidates under test-time scaling. However, they often function as black-box judges: a low step score may reflect a genuine reasoning mistake or simply the verifier's misperception of the image. This entanglement between perception and reasoning leads to systematic false positives (rewarding hallucinated visual premises) and false negatives (penalizing correct grounded statements), undermining both reranking and error localization. We introduce Explicit Visual Premise Verification (EVPV), a lightweight verification interface that conditions step scoring on the reliability of the visual premises a step depends on. The policy is prompted to produce a step-wise visual checklist that makes required visual facts explicit, while a constraint extractor independently derives structured visual constraints from the input image. EVPV matches checklist claims against these constraints to compute a scalar visual reliability signal, and calibrates PRM step rewards via reliability gating: rewards for visually dependent steps are attenuated when reliability is low and preserved when reliability is high. This decouples perceptual uncertainty from logical evaluation without per-step tool calls. Experiments on VisualProcessBench and six multimodal reasoning benchmarks show that EVPV improves step-level verification and consistently boosts Best-of-N reranking accuracy over strong baselines. Furthermore, injecting controlled corruption into the extracted constraints produces monotonic performance degradation, providing causal evidence that the gains arise from constraint fidelity and explicit premise verification rather than incidental prompt effects. Code is available at: https://github.com/Qwen-Applications/EVPV-PRM

---

## 论文详细总结（自动生成）

### 论文总结：评分落地（Grounding the Score）—— 显式视觉前提验证

#### 1. 核心问题与整体含义
多模态大模型在处理数学推理任务时，其错误通常源于两个相互交织的来源：**视觉感知错误**（如读错坐标、看错几何关系）和**符号推理错误**（如代数计算错误）。现有的视觉语言过程奖励模型（VL-PRM）通常作为“黑盒”运行，无法区分这两种错误。这导致了严重的可靠性问题：
*   **假阳性**：模型基于幻觉出的视觉前提进行了“逻辑连贯”的推理，PRM 却给出了高分。
*   **假阴性**：PRM 自身感知能力不足，误判了正确的视觉描述。
论文的核心动机是实现**感知与推理的解耦**，确保“感知正确是逻辑评估的前提”。

#### 2. 方法论：显式视觉前提验证 (EVPV)
EVPV 引入了一个轻量级的验证接口，通过以下三个关键步骤校准步骤奖励：
*   **步骤级视觉清单 (Visual Checklist)**：提示策略模型在生成推理步骤时，显式声明该步骤依赖的视觉事实（如“半径为3”）。
*   **结构化视觉约束提取 (Constraint Extraction)**：独立于推理过程，使用一个专门的提取器从图像中导出结构化事实（JSON 格式），涵盖数值读取、几何关系（平行、垂直等）和组合结构。
*   **可靠性门控奖励 (Reliability-Gated Rewards)**：
    *   **匹配**：将视觉清单中的主张与提取的约束进行类型感知匹配，计算支持得分。
    *   **聚合**：使用几何平均值计算整体视觉可靠性信号 $r$。
    *   **校准**：公式为 $R_t = \alpha(r) R_{base}$。当可靠性 $r$ 较低时，衰减视觉依赖步骤的奖励，使其趋向中性；只有当视觉前提可靠时，才完全信任 PRM 的逻辑评分。

#### 3. 实验设计
*   **数据集与场景**：
    *   **步骤级验证**：使用 **VisualProcessBench**，评估模型对推理步骤正误判断的准确性。
    *   **下游推理任务**：在 **MathVista, MathVision, MathVerse-VO, WeMath, LogicVista, MMMU** 六个基准上进行 Best-of-N (N=8) 重排序实验。
*   **对比方法**：
    *   **强基准 PRM**：VisualPRM, QWEN-VL-PRM-7B。
    *   **工具集成方法**：TIM-PRM（通过外部工具查询视觉证据）。
    *   **闭源/开源模型作为 Judge**：GPT-4o, Gemini 2.5 Pro, InternVL2.5 系列。

#### 4. 资源与算力
*   **模型规模**：约束提取器和步骤验证器均基于 **InternVL2.5-8B** 进行微调。
*   **训练细节**：使用了 20,000 个样本进行约束提取器的蒸馏训练（教师模型为 Qwen3-VL-235B）。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）数量及总训练时长，但提到了 SFT 阶段学习率为 $2 \times 10^{-5}$，Batch Size 为 16，训练 3 个 Epoch。

#### 5. 实验数量与充分性
*   **实验规模**：实验设计非常全面，涵盖了从步骤级 F1 分数到下游任务重排序准确率的完整评估。
*   **因果分析**：通过向约束中注入受控噪声（Flip Ratio），观察到性能的单调下降，有力证明了收益确实来自视觉前提的保真度。
*   **消融实验**：进行了 **27 组**消融配置，测试了证据类型（纯文本 vs 结构化）、历史长度、视觉采样温度等变量。
*   **客观性**：使用了多个不同领域的基准测试，并对比了当前最先进的开源和闭源模型，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：EVPV-PRM 在 VisualProcessBench 上取得了最高的 Macro-F1 分数，并在所有 InternVL2.5 策略规模下持续提升了重排序准确率。
*   **感知是瓶颈**：实验发现 VisualProcessBench 中约 74% 的步骤错误源于视觉误解，证明了显式验证前提的必要性。
*   **解耦效应**：通过可靠性门控，模型能够有效识别并抑制那些基于幻觉前提的“虚假正确”步骤。

#### 7. 优点
*   **解耦设计**：首次系统性地在 PRM 中实现了感知不确定性与逻辑评估的显式解耦。
*   **轻量高效**：相比于 TIM-PRM 等需要每步调用工具的方法，EVPV 仅需一次性提取约束，更适合大规模重排序部署。
*   **因果扎实**：通过人工注入噪声的实验，清晰地展示了视觉事实质量与推理验证效果之间的因果联系。

#### 8. 不足与局限
*   **依赖约束提取质量**：如果约束提取器本身漏掉关键信息或产生错误，会直接导致奖励门控失效。
*   **清单完整性风险**：策略模型生成的视觉清单可能不完整或过于模糊，影响匹配精度。
*   **全局信号局限**：目前使用单一样本级的可靠性信号来校准所有视觉步骤，未来可能需要更精细的“步骤-主张”级条件验证。
*   **应用范围**：主要集中在数学和几何推理，对于更宽泛的开放域视觉问答效果尚待验证。

（完）
