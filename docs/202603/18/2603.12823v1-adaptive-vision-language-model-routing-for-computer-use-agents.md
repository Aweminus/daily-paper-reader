---
title: Adaptive Vision-Language Model Routing for Computer Use Agents
title_zh: 计算机使用智能体的自适应视觉语言模型路由
authors: "Xunzhuo Liu, Bowei He, Xue Liu, Andy Luo, Haichen Zhang, Huamin Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12823v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于VLM的计算机使用智能体执行GUI操作
tldr: 针对计算机操作智能体（CUA）在视觉语言模型（VLM）调用中成本与精度不平衡的问题，本文提出自适应VLM路由（AVR）框架。该框架通过轻量级语义路由层，根据动作难度和模型置信度，将任务动态分配给最经济且满足精度要求的模型。结合历史交互上下文，AVR在保持高精度的同时显著降低了推理成本，并集成了安全防护机制，为高效、安全的智能体设计提供了新方案。
selection_source: fresh_fetch
motivation: 现有的计算机操作智能体通常固定使用单一VLM，无法在不同难度的GUI任务中灵活平衡推理成本与定位精度。
method: 提出AVR框架，利用多模态嵌入评估任务难度并结合小模型置信度，将动作路由至满足可靠性阈值的最廉价模型。
result: "在ScreenSpot-Pro等基准测试中，AVR在精度损失仅为2个百分点以内的前提下，实现了高达78%的推理成本缩减。"
conclusion: 自适应路由机制能有效统一智能体的运行效率与安全性，是构建高性能、低成本计算机操作智能体的关键技术。
---

## 摘要
计算机使用智能体（CUA）通过依赖视觉语言模型（VLM）来解释屏幕截图并预测定位的工具调用，从而将自然语言指令转换为图形用户界面（GUI）操作（如点击、按键和滚动）。然而，不同 VLM 的定位准确率差异巨大，而目前的 CUA 系统通常不考虑任务难度，将每个操作都路由到单个固定模型。我们提出了自适应 VLM 路由（AVR），这是一个在 CUA 编排器和 VLM 池之间插入轻量级语义路由层的框架。对于每个工具调用，AVR 通过多模态嵌入估计操作难度，探测小型 VLM 以测量置信度，并将操作路由到其预测准确率满足目标可靠性阈值的最廉价模型。对于具有先前 UI 交互记忆的“热”智能体，检索到的上下文进一步缩小了小型模型和大型模型之间的能力差距，使得许多操作无需升级即可处理。我们将路由形式化为成本与准确率的权衡，推导出基于阈值的模型选择策略，并使用 ScreenSpot-Pro 定位数据以及 OpenClaw 智能体路由基准对 AVR 进行评估。在这些设置中，AVR 预计可降低高达 78% 的推理成本，同时与全大模型基准相比，准确率差距保持在 2 个百分点以内。当与 Visual Confused Deputy 护栏结合使用时，AVR 还会将高风险操作直接升级到最强的可用模型，从而在单一路由框架内实现了效率与安全性的统一。相关模型、基准和代码已提供：https://github.com/vllm-project/semantic-router。

## Abstract
Computer Use Agents (CUAs) translate natural-language instructions into Graphical User Interface (GUI) actions such as clicks, keystrokes, and scrolls by relying on a Vision-Language Model (VLM) to interpret screenshots and predict grounded tool calls. However, grounding accuracy varies dramatically across VLMs, while current CUA systems typically route every action to a single fixed model regardless of difficulty. We propose \textbf{Adaptive VLM Routing} (AVR), a framework that inserts a lightweight semantic routing layer between the CUA orchestrator and a pool of VLMs. For each tool call, AVR estimates action difficulty from multimodal embeddings, probes a small VLM to measure confidence, and routes the action to the cheapest model whose predicted accuracy satisfies a target reliability threshold. For \textit{warm} agents with memory of prior UI interactions, retrieved context further narrows the capability gap between small and large models, allowing many actions to be handled without escalation. We formalize routing as a cost--accuracy trade-off, derive a threshold-based policy for model selection, and evaluate AVR using ScreenSpot-Pro grounding data together with the OpenClaw agent routing benchmark. Across these settings, AVR projects inference cost reductions of up to 78\% while staying within 2 percentage points of an all-large-model baseline. When combined with the Visual Confused Deputy guardrail, AVR also escalates high-risk actions directly to the strongest available model, unifying efficiency and safety within a single routing framework. Materials are also provided Model, benchmark, and code: https://github.com/vllm-project/semantic-router.

---

## 论文详细总结（自动生成）

### 论文总结：计算机使用智能体的自适应视觉语言模型路由 (AVR)

#### 1. 论文的核心问题与整体含义
随着计算机使用智能体（CUA）的发展，系统需要频繁调用视觉语言模型（VLM）来解析屏幕截图并执行点击、输入等操作。**核心矛盾**在于：
*   **高昂成本**：每个任务通常需要 5-20 步迭代，每步携带数千 token 的截图，导致单次任务成本高达 $0.10–$0.40。
*   **能力不匹配**：并非所有操作都需要顶级模型（如 GPT-4o）。实验表明，模型规模与 GUI 定位精度并非线性相关，小型专业模型在特定任务上甚至优于通用大模型。
*   **研究动机**：通过动态路由机制，将简单任务分配给廉价小模型，复杂或高风险任务分配给大模型，从而在保证精度的前提下大幅降低成本。

#### 2. 论文提出的方法论：AVR 框架
AVR 在智能体编排器与 VLM 池之间引入了一个轻量级语义路由层，核心技术包括：
*   **难度分类（Difficulty Classification）**：使用 120M 参数的多模态嵌入模型（SigLIP 图像编码 + MiniLM 文本编码）提取操作区域和指令的特征。通过计算与已知“难/易”聚类中心的余弦相似度，预估任务难度 $d(t_i)$。
*   **置信度探测（Logprob Probing）**：先对小型 VLM（如 7B）进行非流式探测，提取输出 token 的平均对数概率（logprobs）作为置信度分数。
*   **自适应阈值路由**：路由决策结合了难度预估和置信度。对于简单任务，使用较低的置信度阈值；对于困难任务，要求更高的置信度，否则升级（Escalation）至大模型（如 72B）。
*   **记忆补偿（Memory-Compensated Routing）**：针对“热”智能体，检索之前的成功交互历史并注入 Prompt。论文提出“记忆均衡假设”，认为记忆对小模型的能力提升远大于大模型，从而减少了升级需求。
*   **安全集成**：整合了“视觉混淆代理”护栏，一旦检测到高风险操作（如点击“删除所有”），强制路由至最强模型进行二次验证。

#### 3. 实验设计
*   **数据集/场景**：
    *   **ScreenSpot-Pro**：包含 26 个专业应用程序的 GUI 定位基准，用于评估不同规模 VLM 的定位精度。
    *   **OpenClaw**：一个智能体路由基准，包含 5 类典型任务（如 Shell 命令、知识检索等），用于验证置信度路由的有效性。
*   **对比方法**：
    *   **全大模型基准（All-Large）**：所有操作均由 Qwen2.5-VL-72B 或 GPT-4o 执行。
    *   **固定路由/冷启动路由**：不带记忆或不带难度分类的路由策略。
    *   **不同模型族**：对比了 Qwen2.5-VL 系列（3B/7B/72B）、InternVL2.5、OS-Atlas 等。

#### 4. 资源与算力
*   **硬件支持**：论文提到在 **AMD Instinct MI300X GPU** 上同时部署了 Qwen2.5-7B 和 MiniMax-M2.1 (139B) 进行 OpenClaw 基准测试。
*   **模型参数**：路由层使用的嵌入模型仅 120M 参数，推理开销极低。
*   **训练细节**：文中未详细说明路由层本身的训练时长，主要侧重于推理侧的动态选择逻辑。

#### 5. 实验数量与充分性
*   **实验覆盖**：涵盖了 26 个专业软件（如 VS Code, Photoshop, Blender 等）的定位方差分析，实验场景较为广泛。
*   **消融实验**：对比了“冷启动”与“热启动（带记忆）”的效果，以及加入难度分类前后的成本变化。
*   **客观性评价**：实验数据结合了已发表的 SOTA 模型表现和实测的路由分布。但需要注意的是，CUA 定位的成本缩减部分主要基于**分析性预测（Analytical Projection）**，而非全流程的端到端实测。

#### 6. 论文的主要结论与发现
*   **成本大幅下降**：在保持与 72B 模型精度差距小于 2% 的情况下，AVR 预计可降低 **52%（冷启动）至 78%（带记忆+难度分类）** 的推理成本。
*   **记忆是“均衡器”**：记忆注入能显著提升小模型的置信度（从 0.83 升至 0.96），使其在特定上下文中达到大模型水平。
*   **定位精度非线性**：7B 的专业模型（如 OS-Atlas）定位精度可达 18.9%，远超 1.8T 参数的 GPT-4o（0.8%），证明了路由的必要性。

#### 7. 优点
*   **实用性强**：直接针对生产环境下 CUA 部署的“成本痛点”。
*   **多维优化**：不仅考虑成本和精度，还将安全性（护栏机制）统一到路由决策中。
*   **轻量化**：路由层开销极小，且利用了 VLM 原生的 logprobs 信号。

#### 8. 不足与局限
*   **预测性偏差**：核心的成本缩减数据（表4）是基于 OpenClaw 的分布推导出的预测值，缺乏在 OSWorld 等实时交互环境下的端到端闭环验证。
*   **延迟开销**：探测小模型会增加一轮往返时延（RTT），虽然小模型生成快，但对于极短任务，探测开销可能抵消成本优势。
*   **截图 Token 瓶颈**：CUA 的主要成本在于截图 Token，如果截图分辨率极高，即使路由到小模型，输入成本依然很高。
*   **冷启动限制**：新部署的智能体在没有记忆积累时，成本优化空间受限。

（完）
