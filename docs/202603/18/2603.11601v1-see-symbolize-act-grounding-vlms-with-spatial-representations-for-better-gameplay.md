---
title: "See, Symbolize, Act: Grounding VLMs with Spatial Representations for Better Gameplay"
title_zh: 观察、符号化、行动：利用空间表示增强视觉语言模型（VLM）的游戏表现
authors: "Ashish Baghel, Paras Chopra"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11601v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用空间表示增强VLM在游戏决策中的接地能力
tldr: 本研究探讨了如何通过结合视觉图像与空间符号表示来增强视觉语言模型（VLM）在交互式游戏环境中的决策能力。通过在Atari、VizDoom和AI2-THOR等平台上的实验，对比了纯图像、自提取符号及真实符号等多种输入方案。研究发现，准确的符号化接地能显著提升模型表现，但模型自主提取符号的可靠性受限于场景复杂度，揭示了感知质量是当前VLM智能体的主要瓶颈。
selection_source: fresh_fetch
motivation: 尽管VLM擅长描述视觉场景，但在将感知转化为精确的交互动作（接地）方面仍面临巨大挑战。
method: 在多种游戏环境中对比了仅视觉帧、视觉帧结合自提取或真实符号以及纯符号输入的决策效果。
result: 实验表明准确的符号信息能大幅优化模型决策，但模型自提取符号的能力在复杂场景下表现不佳。
conclusion: 符号化接地对提升VLM性能至关重要，而提高感知阶段的符号提取精度是未来发展的核心。
---

## 摘要
视觉语言模型（VLM）擅长描述视觉场景，但在将感知转化为精确、具身的动作方面仍面临挑战。我们研究了同时为 VLM 提供视觉帧和场景的符号化表示是否能提高其在交互式环境中的表现。我们在 Atari 游戏、VizDoom 和 AI2-THOR 上评估了三种最先进的 VLM，并对比了仅视觉帧、视觉帧结合自提取符号、视觉帧结合真实（ground-truth）符号以及仅符号的流水线。我们的结果表明，当符号信息准确时，所有模型都能从中受益。然而，当 VLM 自行提取符号时，其表现取决于模型能力和场景复杂度。我们进一步研究了 VLM 从视觉输入中提取符号信息的准确性，以及这些符号中的噪声如何影响决策和游戏表现。我们的研究结果表明，只有在符号提取可靠的情况下，符号锚定对 VLM 才有益，并强调感知质量是未来基于 VLM 的智能体的核心瓶颈。

## Abstract
Vision-Language Models (VLMs) excel at describing visual scenes, yet struggle to translate perception into precise, grounded actions. We investigate whether providing VLMs with both the visual frame and the symbolic representation of the scene can improve their performance in interactive environments. We evaluate three state-of-the-art VLMs across Atari games, VizDoom, and AI2-THOR, comparing frame-only, frame with self-extracted symbols, frame with ground-truth symbols, and symbol-only pipelines. Our results indicate that all models benefit when the symbolic information is accurate. However, when VLMs extract symbols themselves, performance becomes dependent on model capability and scene complexity. We further investigate how accurately VLMs can extract symbolic information from visual inputs and how noise in these symbols affects decision-making and gameplay performance. Our findings reveal that symbolic grounding is beneficial in VLMs only when symbol extraction is reliable, and highlight perception quality as a central bottleneck for future VLM-based agents.

---

## 论文详细总结（自动生成）

### 论文总结：See, Symbolize, Act: Grounding VLMs with Spatial Representations for Better Gameplay

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言模型（VLMs）在理解和描述静态视觉场景方面表现卓越，但在需要精确空间推理和实时交互的任务（如机器人控制、具身 AI 或游戏）中，往往难以将感知转化为准确的动作（即“接地”问题，Grounding）。
*   **研究动机**：现有的 VLM 智能体在玩 Atari 等游戏时，常因无法精确识别物体坐标而导致操作失误。
*   **核心问题**：为 VLM 提供显式的**空间符号表示**（如物体坐标、类别标签）是否能显著提升其决策质量？这种提升是依赖于符号的准确性，还是模型本身的推理能力？

#### 2. 论文提出的方法论
论文提出了一种“观察-符号化-行动”的框架，通过四种不同的数据流（Pipelines）来隔离视觉信息和符号信息对决策的影响：
*   **核心思想**：对比“纯视觉感知”与“视觉+结构化空间数据”的决策差异。
*   **关键技术细节**：
    *   **F (Frame-only)**：基准测试，仅输入原始游戏图像。
    *   **F+S-self (Frame + Self-Extracted Symbols)**：两阶段过程。首先让 VLM 从图像中提取物体 ID、坐标（x, y）和置信度，然后将这些自提取符号与图像一同输入 VLM 进行决策。
    *   **F+S-GT (Frame + Ground-Truth Symbols)**：使用 OCAtari 工具直接从游戏内存（RAM）获取完美的物体坐标，消除感知误差，作为性能上限。
    *   **S-GT (Symbol-only)**：仅提供完美坐标，不提供视觉图像，测试符号信息是否足以支撑决策。
*   **算法流程**：游戏在每一帧暂停，VLM 接收指定格式的 Prompt（包含动作空间说明和输入数据），输出 JSON 格式的推理过程和动作指令。

#### 3. 实验设计
*   **实验场景**：
    *   **Atari 游戏**：Pong（简单）、Breakout（中等）、Space Invaders（复杂/物体密集）。
    *   **3D 环境**：VizDoom（第一人称射击，测试 3D 空间感）。
    *   **具身 AI 环境**：AI2-THOR（厨房任务，测试光照真实感和物体关系）。
*   **Benchmark 模型**：Claude-4-Sonnet、GPT-4o、Gemini-2.5-Pro。
*   **对比指标**：累积奖励（Gameplay Reward）、检测准确率（F1 Score）、定位精度（IoU）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究主要是在 **Zero-shot（零样本）** 设置下评估现有的商业 VLM API，不涉及大规模模型训练或微调。实验重点在于推理阶段的 Prompt Engineering 和 Pipeline 设计。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个 Atari 游戏上进行了跨模型、跨 Pipeline 的对比。
    *   扩展到了 VizDoom 和 AI2-THOR 两个复杂 3D 场景。
    *   **消融实验 1**：测试了 4 种不同分辨率（从 160x210 到 1280x720）对符号提取准确性的影响。
    *   **消融实验 2（噪声分析）**：在 10 个独立随机种子下，通过注入 5 个等级的高斯噪声（σ=0.0 到 0.4）来测试模型对坐标误差的鲁棒性。
*   **客观性与公平性**：实验设计较为严谨，通过引入 Ground-Truth 建立了性能天花板，并通过多模型对比排除了单一模型的偏见。

#### 6. 主要结论与发现
*   **符号接地的有效性**：当符号信息准确时（F+S-GT），所有模型的表现均大幅提升。
*   **感知是核心瓶颈**：VLM 自提取符号（F+S-self）的效果高度依赖模型能力。Claude-4-Sonnet 提取精度最高（F1=0.715），因此受益明显；而 GPT-4o 和 Gemini 提取精度较低，错误的符号反而会误导决策，导致性能下降。
*   **视觉背景不可或缺**：纯符号输入（S-GT）在大多数情况下表现糟糕，证明 VLM 需要视觉图像作为“脚手架”来理解符号的语义上下文。
*   **分辨率至关重要**：提高输入分辨率能显著提升符号提取的 F1 分数，从而间接提升游戏表现。

#### 7. 优点
*   **系统性强**：通过四种 Pipeline 优雅地解耦了视觉感知与逻辑推理。
*   **跨领域验证**：不仅局限于 2D Atari，还验证了 3D 和光照真实感环境，增强了结论的普适性。
*   **深入的误差分析**：通过噪声消融实验，量化了 VLM 对空间定位误差的敏感度（即使 16-20 像素的误差也会导致性能大幅下滑）。

#### 8. 不足与局限
*   **实时性缺失**：由于每帧都需要调用 VLM API，推理延迟极高，无法应用于真正的实时游戏或快速反应任务。
*   **成本问题**：频繁调用高参数量模型的 API 成本昂贵，限制了长轨迹实验的开展。
*   **环境多样性**：虽然涵盖了 3D 场景，但任务类型仍相对单一（主要是射击和物体搬运），未涉及更复杂的策略规划或长期记忆任务。
*   **模型依赖性**：结论受限于当前测试的三个模型，随着 VLM 版本的快速迭代，感知能力的提升可能会改变“自提取符号”的收益曲线。

（完）
