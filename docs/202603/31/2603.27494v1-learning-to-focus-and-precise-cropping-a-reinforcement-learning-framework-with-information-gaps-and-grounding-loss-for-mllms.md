---
title: "Learning to Focus and Precise Cropping: A Reinforcement Learning Framework with Information Gaps and Grounding Loss for MLLMs"
title_zh: 学习聚焦与精确裁剪：一种面向多模态大语言模型（MLLMs）的带有信息差与定位损失的强化学习框架
authors: "Xuanpu Zhao, Zhentao Tan, Dianmo Sheng, Tianxiang Chen, Yao Liu, Yue Wu, Tao Gong, Qi Chu, Nenghai Yu"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27494v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于智能体的MLLM推理强化学习框架
tldr: 针对多模态大模型在复杂场景中过度依赖全局信息而忽视局部细节的问题，本文提出了一种无需轨迹监督的两阶段强化学习框架。通过引入“信息差”机制调整全局图像粒度，促使模型通过关注局部裁剪区域获取信息增益，并结合Grounding Loss提升裁剪精度。该方法在多个高分辨率视觉问答基准测试中达到SOTA水平，显著增强了模型对细粒度细节的感知与推理能力。
selection_source: fresh_fetch
motivation: 现有模型在处理复杂视觉任务时过度依赖全局输入，导致对裁剪区域内的细粒度细节利用不足且定位不够精准。
method: 提出两阶段强化学习框架，通过“信息差”机制强制模型关注局部区域，并利用少量标注引入Grounding Loss优化裁剪精度。
result: 实验证明该方法显著提升了模型对裁剪区域的关注度，并在多个高分辨率视觉问答基准测试中取得了SOTA性能。
conclusion: 该框架为提升多模态大模型在复杂场景下的细粒度感知与推理能力提供了一种高效且无需轨迹监督的新方案。
---

## 摘要
为了增强多模态大语言模型（MLLMs）在复杂视觉场景中的感知和推理能力，最近的研究引入了基于智能体的工作流。在这些工作中，MLLMs 自主利用图像裁剪工具来分析感兴趣区域以进行问答。虽然现有的训练策略（如采用监督微调和强化学习的策略）已取得显著进展，但我们的实证分析揭示了一个关键局限性：模型对全局输入存在强烈依赖，而对裁剪区域内细节的依赖较弱。为了解决这一问题，我们提出了一种无需轨迹监督的新型两阶段强化学习框架。在第一阶段，我们通过调整全局图像的粒度引入了“信息差”（Information Gap）机制。该机制由裁剪区域提供的信息增益驱动，训练模型通过关注关键区域来回答问题。第二阶段通过结合定位损失（grounding loss）并使用少量边界框标注，进一步提高了裁剪精度。实验表明，我们的方法显著增强了模型对裁剪区域的关注，使其在高清视觉问答基准测试中达到了最先进的性能。我们的方法为 MLLMs 感知和推理细粒度细节提供了一种更高效的途径。代码开源地址：https://github.com/XuanPu-Z/LFPC。

## Abstract
To enhance the perception and reasoning capabilities of multimodal large language models in complex visual scenes, recent research has introduced agent-based workflows. In these works, MLLMs autonomously utilize image cropping tool to analyze regions of interest for question answering. While existing training strategies, such as those employing supervised fine-tuning and reinforcement learning, have made significant progress, our empirical analysis reveals a key limitation. We demonstrate the model's strong reliance on global input and its weak dependence on the details within the cropped region. To address this issue, we propose a novel two-stage reinforcement learning framework that does not require trajectory supervision. In the first stage, we introduce the ``Information Gap" mechanism by adjusting the granularity of the global image. This mechanism trains the model to answer questions by focusing on cropped key regions, driven by the information gain these regions provide. The second stage further enhances cropping precision by incorporating a grounding loss, using a small number of bounding box annotations. Experiments show that our method significantly enhances the model's attention to cropped regions, enabling it to achieve state-of-the-art performance on high-resolution visual question-answering benchmarks. Our method provides a more efficient approach for perceiving and reasoning fine-grained details in MLLMs. Code is available at: https://github.com/XuanPu-Z/LFPC.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning to Focus and Precise Cropping: A Reinforcement Learning Framework with Information Gaps and Grounding Loss for MLLMs》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的多模态大语言模型（MLLMs）在处理复杂视觉场景时，虽然引入了“裁剪工具（Cropping Tool）”作为智能体工作流，但存在**“形式化调用”**的问题。
*   **研究背景**：作者通过实证分析发现，模型往往在执行裁剪动作前就已经预测了答案，裁剪区域仅被用于“确认”既有结论。当把裁剪区域替换为随机噪声时，模型性能几乎不下降，这表明模型过度依赖全局低分辨率输入，而忽视了局部高分辨率细节。
*   **研究动机**：如何强制模型真正“关注”裁剪区域，并提高裁剪定位的精确度，且不依赖昂贵的教师模型推理轨迹（Trajectory-free）。

### 2. 方法论：核心思想与关键技术
该论文提出了一种**两阶段纯强化学习（RL）框架**：

*   **第一阶段：学习聚焦（Learn to Focus）**
    *   **核心思想**：引入**“信息差（Information Gap）”机制**。
    *   **技术细节**：通过“离线分辨率选择”策略，将全局输入图像进行下采样，降低其分辨率直到模型仅凭全局图无法正确回答问题。而当模型调用裁剪工具时，提取的是原始高分辨率图像的切片。
    *   **效果**：这种分辨率的不对称性创造了信息差，迫使模型必须依赖高分辨率的裁剪区域才能获得正确答案，从而获得奖励。
    *   **优化算法**：采用 **GRPO（Group Relative Policy Optimization）** 算法，奖励函数包含准确性奖励、格式奖励和工具使用奖励。

*   **第二阶段：学习精确裁剪（Learn to Crop Precisely）**
    *   **核心思想**：解决模型为了贪婪获取信息而倾向于裁剪过大区域的问题。
    *   **技术细节**：引入**定位奖励（Grounding Reward）**。利用少量人工标注的层级化边界框（从最小必要区域到上下文相关区域）。
    *   **公式构成**：组合了 **IoU 奖励**（仅在覆盖率超过阈值时发放）和 **L1 距离奖励**（提供稠密梯度，引导中心点对齐）。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **HR-Bench 8K & 4K**：评估极高分辨率下的细粒度感知。
    *   **V\***：评估属性识别和空间推理能力。
*   **对比方法**：
    *   **SFT+RL 类**：CoF-sft, Pixel Reasoner, Mini-o3。
    *   **纯 RL 类**：DeepEyes。
*   **实验设置**：对比了在不同视觉 Token 预算（1,024 vs 16,384）下的表现。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   模型基础：Qwen2.5-VL-7B-Instruct。
    *   训练时长：第一阶段约 **320 A100-hours**，第二阶段约 **96 A100-hours**，离线分辨率选择耗时约 2.5 A100-hours。
    *   总计约 **418.5 A100-hours**，比对比方法 DeepEyes（480 hours）更高效。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验覆盖了 3 个主流高分辨率基准测试。
    *   消融实验非常详尽，包括：两阶段有效性验证、数据选择策略、L1 奖励的影响、分辨率选择策略对比。
    *   **初步分析实验**：专门设计了“真值裁剪区”和“噪声裁剪区”的对比实验，有力证明了现有模型的缺陷。
*   **客观性**：通过限制 Token 预算（1024）来挑战其他使用高 Token（16384）的模型，证明了其方法的效率优势，实验设计具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：在 1024 Token 限制下，该方法显著优于所有对比模型；甚至在 1024 Token 下的表现超过了某些模型在 16384 Token 下的表现。
*   **关注度提升**：通过信息差训练，模型对裁剪区域的依赖度（Acc Delta）从基线的 3%-4% 提升到了 11%-18%。
*   **效率优化**：由于模型学会了精确裁剪更小的区域，推理时的 Token 消耗和时间成本大幅降低。

### 7. 优点（亮点）
*   **机制创新**：巧妙利用“信息差”解决了 RL 训练中的快捷路径（Shortcut）学习问题。
*   **无需轨迹监督**：不依赖 GPT-4o 等强模型的推理轨迹，降低了训练门槛和成本。
*   **端到端优化**：通过层级化 Bbox 标注和复合奖励函数，同时优化了“答题准确度”和“工具调用精度”。

### 8. 不足与局限
*   **标注依赖**：第二阶段虽然只需要少量标注（256例），但仍需人工干预，未能实现完全的自我进化。
*   **工具单一**：目前仅集成了裁剪工具，对于需要分割（Segmentation）或更复杂视觉操作的任务，其泛化能力尚待验证。
*   **性能上限**：第二阶段带来的性能提升（约 1%-3%）相比第一阶段较小，暗示在极复杂场景下的定位精度仍有提升空间。

（完）
