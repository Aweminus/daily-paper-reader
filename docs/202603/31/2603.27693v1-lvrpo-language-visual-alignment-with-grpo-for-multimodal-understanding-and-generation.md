---
title: "LVRPO: Language-Visual Alignment with GRPO for Multimodal Understanding and Generation"
title_zh: LVRPO：利用 GRPO 实现多模态理解与生成的语言-视觉对齐
authors: "Shentong Mo, Sukmin Yun"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27693v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于强化学习的多模态推理与对齐偏好优化
tldr: 针对统一多模态预训练中语言与视觉对齐不充分、难以兼顾理解与生成的问题，本文提出了 LVRPO 框架。该框架利用组相对策略优化（GRPO）通过偏好驱动的强化学习信号直接优化模型行为，实现了语言与视觉表示的显式对齐。LVRPO 无需辅助编码器或手工设计的跨模态目标，在多模态理解、生成和推理任务中均表现优异，显著提升了模型的语义一致性和可控性。
selection_source: fresh_fetch
motivation: 现有的统一多模态预训练方法依赖隐式对齐信号，在处理细粒度推理和可控生成时效果欠佳。
method: 提出 LVRPO 框架，利用 GRPO 强化学习算法通过偏好优化直接对齐语言与视觉表示。
result: 在涵盖理解、生成和推理的多个基准测试中，LVRPO 均显著优于现有的强基线模型。
conclusion: LVRPO 证明了基于强化的偏好优化能有效提升多模态模型的跨模态一致性与综合性能。
---

## 摘要
统一多模态预训练已成为在单一基础模型中联合建模语言和视觉的一种极具前景的范式。然而，现有方法在很大程度上依赖于隐式或间接的对齐信号，在同时支持多模态理解和生成方面仍表现欠佳，特别是在需要细粒度语言-视觉推理和可控生成的场景中。在这项工作中，我们提出了 LVRPO，这是一种基于语言-视觉强化学习的偏好优化框架，利用组相对策略优化（GRPO）显式地对齐语言和视觉表示。LVRPO 并非在表示层引入额外的对齐损失，而是通过偏好驱动的强化信号直接优化多模态模型的行为，从而在理解和生成任务中促进语言与视觉之间一致且具有语义基础的交互。这种形式化方法无需辅助编码器或手工设计的跨模态目标即可实现有效对齐，并能自然地扩展到多种多模态能力。实验表明，LVRPO 在涵盖多模态理解、生成和推理的广泛基准测试中，始终优于强大的统一预训练基线模型。

## Abstract
Unified multimodal pretraining has emerged as a promising paradigm for jointly modeling language and vision within a single foundation model. However, existing approaches largely rely on implicit or indirect alignment signals and remain suboptimal for simultaneously supporting multimodal understanding and generation, particularly in settings that require fine-grained language-visual reasoning and controllable generation. In this work, we propose LVRPO, a language-visual reinforcement-based preference optimization framework that explicitly aligns language and visual representations using Group Relative Policy Optimization (GRPO). Instead of introducing additional alignment losses at the representation level, LVRPO directly optimizes multimodal model behaviors through preference-driven reinforcement signals, encouraging consistent and semantically grounded interactions between language and vision across both understanding and generation tasks. This formulation enables effective alignment without requiring auxiliary encoders or handcrafted cross-modal objectives, and naturally extends to diverse multimodal capabilities. Empirically, LVRPO consistently outperforms strong unified-pretraining baselines on a broad suite of benchmarks spanning multimodal understanding, generation, and reasoning.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **LVRPO**（Language-Visual Reinforcement-based Preference Optimization），这是一个旨在通过强化学习显式对齐多模态模型中“理解”与“生成”能力的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的统一多模态预训练模型（如 BAGEL, Emu3）主要依赖隐式对齐信号（如预测下一个 Token），导致模型在处理细粒度语义对齐、复杂指令遵循和可控生成时表现不佳。
*   **研究背景**：
    *   **表示层对齐的局限性**：如 REPA 等方法强行让生成特征模仿固定的视觉编码器（如 DINOv2），这会引入“特征僵化”，且无法保证最终任务的行为正确性。
    *   **语义断层**：模型可能擅长描述图像（理解），但在根据指令编辑图像（生成）时失败，因为其内部的“世界知识”在不同任务头之间未能一致激活。
*   **整体含义**：LVRPO 试图通过**行为驱动的强化学习**（而非静态特征模仿）来闭合理解与生成之间的环路，使模型生成的输出在语义上更扎实、在逻辑上更符合指令。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将统一多模态模型视为一个策略（Policy），利用**组相对策略优化（GRPO）**算法，根据输出结果的语义一致性和指令遵循质量进行直接优化。
*   **关键技术细节**：
    *   **GRPO 算法**：这是一种无需独立 Critic 网络（评论家网络）的强化学习方法。对于每个提示词（Prompt），模型采样 $G$ 个输出，通过计算组内相对奖励（Advantage）来更新参数，显著降低了计算开销。
    *   **多维奖励函数 ($R$)**：
        1.  **语义接地奖励 ($r_{sem}$)**：利用冻结的 **SigLIP 2** 编码器计算生成图像与文本提示之间的密集语义相似度。
        2.  **指令遵循奖励 ($r_{ins}$)**：基于规则的校验器，检查物体数量、位置、分辨率等可验证约束。
        3.  **知识一致性奖励 ($r_{kn}$)**：利用 VQA 代理（如 PaLI-3）验证生成内容是否符合特定的事实属性。
    *   **统一架构 (MoT)**：采用 **Mixture-of-Transformers (MoT)** 架构，将模型分为“推理专家”和“生成专家”，通过路由机制减少模态间的梯度干扰。
    *   **生成路径**：使用修正流匹配（Rectified Flow Matching）进行图像生成。

### 3. 实验设计
*   **数据集**：
    *   **训练**：使用 50 万个高质量“指令-行为”样本，涵盖科学问答（ScienceQA）、人类偏好数据（Pick-a-Pic v2）和合成约束指令。
    *   **预训练基础**：基于 BAGEL 模型，使用万亿级 Token 的交织文本-图像数据。
*   **基准测试 (Benchmarks)**：
    *   **图像生成**：GenEval（评估组合属性）。
    *   **世界知识**：WISE（评估知识与图像的对齐）。
    *   **图像编辑**：GEdit-Bench, IntelligentBench。
    *   **多模态理解**：MMMU, MathVista, MME, MMBench 等。
*   **对比方法**：包括纯生成模型（SDXL, Flux.1）、纯理解模型（InternVL2, Qwen2-VL）以及统一模型（Janus-Pro, Emu3, BAGEL）。

### 4. 资源与算力
*   **模型规模**：主要实验基于 **7B** 参数规模的统一模型，同时也测试了 1.3B 和 13B 版本。
*   **训练配置**：使用了 **DeepSpeed ZeRO-3** 优化策略。
*   **训练时长**：在对齐阶段进行了 **5,000 步** 训练。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但提到 $G=8$ 的组采样在 7B 模型上每步耗时约 2.1 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了四大核心能力（生成、推理、编辑、理解），并在超过 10 个主流基准测试上进行了评估。
*   **消融实验**：
    *   测试了不同组大小 $G$（2 到 16）对性能的影响。
    *   分析了奖励模型（SigLIP 2）冻结与否的影响（证明了冻结可防止“奖励作弊”）。
    *   探讨了理解与生成任务之间的“跷跷板效应”。
*   **充分性评价**：实验设计较为全面，不仅对比了开源模型，还对比了 GPT-4o 和 Gemini 2.0 等闭源 SOTA 模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：LVRPO 在 GenEval（0.91）和 WISE（0.85）上取得了 SOTA 结果，显著优于基线模型 BAGEL。
*   **正向干扰**：优化生成任务的行为不仅没有损害理解能力，反而提升了理解性能（如在 MathVista 和 MMMU 上的得分），证明了跨模态协同效应。
*   **知识激活**：通过偏好优化，模型能更好地激活其内部的语言知识库来指导视觉生成（例如在生成“绿色消防车”时能克服训练数据中“消防车通常是红色”的先验偏差）。

### 7. 优点
*   **算法高效**：引入 GRPO 避免了复杂的 Critic 网络训练，使多模态 RL 变得可行且高效。
*   **显式对齐**：通过行为反馈而非特征逼近，解决了生成内容与指令不符的顽疾。
*   **架构解耦**：MoT 架构有效缓解了离散推理梯度与连续生成梯度之间的冲突。

### 8. 不足与局限
*   **奖励模型依赖**：性能高度依赖于冻结的奖励模型（SigLIP 2），如果奖励模型存在视觉盲点（如极细粒度的纹理），LVRPO 也会受到限制。
*   **计算成本**：虽然比 PPO 高效，但组采样（Group Sampling）过程相比传统的监督微调（SFT）仍增加了显著的推理开销。
*   **偏差风险**：如果奖励函数设计不当，模型可能会出现“奖励作弊”（Reward Hacking），生成符合指标但视觉质量差的图像（虽论文中已通过冻结模型部分缓解）。

（完）
