---
title: Reliable Reasoning in SVG-LLMs via Multi-Task Multi-Reward Reinforcement Learning
title_zh: 通过多任务多奖励强化学习实现 SVG-LLM 中的可靠推理
authors: "Haomin Wang, Qi Wei, Qianli Ma, Shengyuan Ding, Jinhui Yin, Kai Chen, Hongjie Zhang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16189v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于视觉语言任务的思维链强化学习
tldr: 针对现有SVG生成模型泛化性差、代码冗余及缺乏推理过程的问题，本文提出CTRL-S框架。该框架引入思维链（CoT）机制以显式化推理过程，并构建了包含14.5万样本的高质量数据集SVG-Sophia。通过结合GRPO算法与多奖励优化（涵盖视觉相似度、格式及代码效率），CTRL-S在多项SVG生成任务中显著提升了结构一致性、视觉保真度及代码质量。
selection_source: fresh_fetch
motivation: 现有SVG生成模型存在代码冗余、缺乏显式推理能力以及在复杂任务中泛化性能受限的问题。
method: 提出CTRL-S框架，通过构建SVG-Sophia数据集、引入思维链机制，并采用基于GRPO的多任务多奖励强化学习进行优化。
result: 实验表明，CTRL-S在SVG代码精炼、文本转SVG及图像转SVG任务中均取得了更高的成功率和更优的视觉效果。
conclusion: 结合思维链推理与多奖励强化学习能有效提升SVG大语言模型的逻辑可靠性、代码效率及生成质量。
---

## 摘要
随着视觉语言模型的飞速发展，越来越多的研究开始探索其在 SVG 生成任务中的潜力。尽管现有方法通过构建大规模 SVG 数据集和引入 SVG 特定标记（tokens）提升了性能，但仍面临泛化能力有限、代码输出中存在冗余路径以及缺乏显式推理等问题。在本研究中，我们提出了 CTRL-S（SVG 思维链强化学习），这是一个统一的框架，通过引入思维链机制，在 SVG 生成过程中显式地展示模型的推理过程。为了支持这种结构化推理，我们构建了 SVG-Sophia，这是一个包含 14.5 万个样本的高质量数据集，涵盖了 SVG 代码优化、文本生成 SVG（Text-to-SVG）以及图像生成 SVG（Image-to-SVG）任务。通过训练模型生成组级（group-level）结构化 SVG 代码，CTRL-S 显著提高了结构连贯性和视觉保真度。此外，我们采用了 GRPO 算法并设计了一个多奖励优化框架，整合了 DINO、图文相似度、格式以及代码效率奖励。通过联合多奖励优化和多任务训练，我们的方法系统地增强了整体生成能力。大量实验表明，CTRL-S 优于现有方法，实现了更高的任务成功率、更优的 SVG 代码质量以及卓越的视觉保真度。

## Abstract
With the rapid advancement of vision-language models, an increasing number of studies have explored their potential for SVG generation tasks. Although existing approaches improve performance by constructing large-scale SVG datasets and introducing SVG-specific tokens, they still suffer from limited generalization, redundant paths in code outputs, and a lack of explicit reasoning. In this work, we present CTRL-S (Chain-of-Thought Reinforcement Learning for SVG), a unified framework that introduces a chain-of-thought mechanism to explicitly expose the model's reasoning process during SVG generation. To support this structured reasoning, we construct SVG-Sophia, a high-quality dataset containing 145K samples across SVG code refinement, Text-to-SVG, and Image-to-SVG tasks. By training the model to generate group-level structured SVG code, CTRL-S significantly improves structural coherence and visual fidelity. Furthermore, we adopt the GRPO algorithm and design a multi-reward optimization framework, incorporating DINO, image-text similarity, format, and code efficiency rewards. Through joint multi-reward optimization and multi-task training, our approach systematically enhances overall generation capabilities. Extensive experiments show that CTRL-S outperforms existing methods, achieving higher task success rates, superior SVG code quality, and exceptional visual fidelity.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：CTRL-S 框架

#### 1. 核心问题与整体含义（研究动机和背景）
可缩放矢量图形（SVG）因其分辨率无关性和易编辑性在现代设计中至关重要。尽管现有的视觉语言模型（VLM）在 SVG 生成上取得了进展，但仍面临三大核心挑战：
*   **缺乏显式推理**：模型往往直接输出代码，缺乏对图形结构的逻辑规划，导致复杂图形生成失败。
*   **代码冗余与低效**：生成的 SVG 代码常包含大量重复路径，降低了渲染效率和可读性。
*   **泛化与对齐问题**：模型在处理文本描述与视觉细节的精确对齐时表现不稳，且缺乏自我纠错能力。
**研究动机**：通过引入思维链（CoT）推理和多奖励强化学习，使模型能够像人类设计师一样“先规划、后绘图”，并具备自动优化代码的能力。

#### 2. 论文提出的方法论
论文提出了 **CTRL-S**（Chain-of-Thought Reinforcement Learning for SVG）框架，核心流程如下：
*   **SVG-Sophia 数据集构建**：利用 Claude-Sonnet-4.5 标注了 14.5 万个高质量样本，将原始 SVG 重构为带有详细注释和“组级（`<g>`标签）”结构的代码，并配以分步推理过程。
*   **两阶段监督微调（SFT）**：
    1.  **Token 对齐**：在 100 万个样本上训练 SVG 特定标记的嵌入。
    2.  **CoT 结构化对齐**：在 SVG-Sophia 上训练模型学习“推理步骤-代码块”的一一对应关系。
*   **多任务多奖励 GRPO 强化学习**：采用群体相对策略优化（GRPO）算法，同时优化 Text-to-SVG、Image-to-SVG 和代码精炼（Refinement）任务。
*   **四重奖励函数设计**：
    1.  **格式奖励 ($R_{format}$)**：确保输出包含正确的 `<think>` 和 `<svg>` 标签且可渲染。
    2.  **DINO 奖励 ($R_{dino}$)**：利用 DINOv2 提取特征，衡量生成图形与目标图形的视觉深度相似度。
    3.  **图文相似度奖励 ($R_{lclip}$)**：利用 Long-CLIP 确保生成内容符合长文本指令。
    4.  **代码效率奖励 ($R_{eff}$)**：惩罚冗余代码，鼓励简洁高效的路径表达。

#### 3. 实验设计
*   **数据集与场景**：
    *   **SArena-Icon**：用于评估 Text-to-SVG 和 Image-to-SVG 的标准基准。
    *   **SVG-Sophia Refinement Benchmark**：专门用于评估代码纠错和优化能力。
*   **对比方法**：
    *   **传统方法**：IconShop, VectorFusion, DiffVG, LIVE 等。
    *   **通用大模型**：GPT-5.2, Claude-Sonnet-4.5, Gemini-3-Pro, Llama 4 系列, Qwen3-VL 等。
    *   **专用 SVG 模型**：StarVector, LLM4SVG, OmniSVG, InternSVG。
*   **评估指标**：FID、CLIP-T2I/I2I、DINO 相似度、SSIM、LPIPS、成功率（SR）及 Token 数量。

#### 4. 资源与算力
论文明确记录了实验所需的算力资源：
*   **SFT 阶段**：使用 **48 张 NVIDIA H200 GPU**，第一阶段学习率 1e-4，第二阶段 5e-5，全局 Batch Size 为 96。
*   **RL 阶段**：使用 **32 张 GPU**，基于 `verl` 框架实现 GRPO，训练时长约为 **12 小时**，全局 Batch Size 为 128。
*   **推理采样**：在 RL 训练的 Rollout 阶段，每个 Prompt 采样 16 个响应。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大核心任务的定量对比，并针对 CoT 机制、奖励函数组成、奖励权重比例（2:1:1）以及多任务联合训练的效果进行了详细的**消融实验**。
*   **客观性与公平性**：
    *   测试集与训练集严格分离。
    *   在代码精炼任务中，使用了包括 GPT-5.2 和 Gemini-3-Pro 在内的多种模型生成“缺陷代码”作为输入，确保了评估的无偏性。
    *   引入了 100 名人类标注者对数据集进行审核和修正，保证了 Ground Truth 的质量。

#### 6. 主要结论与发现
*   **性能领先**：CTRL-S 在所有任务上均达到 SOTA 水平，尤其在视觉保真度（DINO/SSIM）和语义对齐（CLIP）上显著超过闭源最强模型。
*   **推理红利**：引入 CoT 不仅提高了生成成功率，还使代码具备了极高的可读性和模块化特征。
*   **RL 的显著作用**：相比 SFT 版本，RL 后的模型在保持视觉质量的同时，生成的 Token 数量大幅减少（如从 1.1k 降至 346），推理速度提升约 38%。
*   **多任务互补**：联合训练代码精炼任务能显著增强模型对图像到矢量转换的理解能力。

#### 7. 优点
*   **结构化对齐**：首次实现了推理步骤与 SVG `<g>` 标签组的严格一一对应，极大方便了后续的人工编辑。
*   **多维度奖励**：综合考虑了格式、视觉、语义和效率，解决了以往模型“能画出来但代码乱七八糟”的问题。
*   **自我修复能力**：通过 Refinement 任务，赋予了模型识别并修正自身或他人代码错误的能力。

#### 8. 不足与局限
*   **场景复杂度限制**：实验主要集中在图标（Icons）和简单平面图形，对于具有复杂渐变、极高路径密度的艺术插画或真实照片级矢量化，其表现尚待验证。
*   **计算成本**：虽然推理效率提升，但 RL 阶段需要大量的计算资源进行采样（Rollout），对于资源受限的团队门槛较高。
*   **模型偏见风险**：数据集基于 Claude-Sonnet-4.5 生成，可能继承了特定模型在审美或代码风格上的偏见。

（完）
