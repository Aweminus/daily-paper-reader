---
title: Recursive Belief Vision Language Action Models
title_zh: 递归信念视觉-语言-动作模型
authors: "Vaidehi Bagaria, Bijo Sebastian, Nirav Kumar Patel"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20659v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于长程任务智能体的视觉-语言-动作模型
tldr: RB-VLA 针对现有视觉-语言-动作模型（VLA）在长程任务中缺乏持久状态表示、推理延迟高及感知混淆等问题，提出了一种基于递归信念（Recursive Belief）的架构。该模型通过自监督世界模型目标训练，维护一个紧凑的潜状态来编码历史和动力学信息，并结合 VLM 提供的意图，通过扩散策略实现闭环控制。实验表明，RB-VLA 在多阶段任务中显著优于现有模型，且大幅降低了推理延迟。
selection_source: carryover_cache
motivation: 现有 VLA 模型过度依赖短期观测，缺乏处理长程任务和部分可观测环境所需的持久性动作条件状态表示。
method: 提出一种信念中心架构，利用自监督世界模型维护紧凑潜状态，并结合单次查询的 VLM 意图驱动扩散策略。
result: 在多阶段任务中成功率显著超越 pi_0，推理延迟降低达 5 倍，且消除了随时间增长的内存开销。
conclusion: 递归信念模块是提升模型性能的核心，为长程、多阶段的机器人操作提供了高效且具备因果推理能力的控制方案。
---

## 摘要
视觉-语言-动作模型（VLA）必须使智能体能够在部分可观测性下执行长时程任务。然而，大多数现有方法仍以观测为驱动，依赖于短上下文窗口或对视觉语言模型（VLM）的重复查询。这会导致任务进度丢失、感知混淆下的动作重复以及高推理延迟。虽然语义对齐（semantic grounding）很重要，但长时程操作从根本上需要持久的、以动作为条件的（action-conditioned）状态表示。当前的 VLA 缺乏此类表示，且表现出有限的时间和物理推理能力，使其不适用于多阶段控制。本文介绍了 RB-VLA，这是一种以信念为中心的架构，通过自监督世界模型目标进行训练，能够维持一个编码了任务相关历史、动力学和物体交互的紧凑潜状态。VLM 在每个任务中仅被查询一次以提供高层意图，而信念模块则负责跟踪任务进度，并在部分可观测性下实现感知阶段、具有因果基础的控制，且无需存储原始观测数据或随时间增加内存开销。信念和意图共同作为扩散策略（diffusion policy）的条件，以实现鲁棒的闭环执行。RB-VLA 在长时程基准测试中优于先前的 VLA，在多阶段取放和堆叠任务中，其成功率分别比 pi_0 高出 52.5% 和 37.5%。与基准模型相比，它还将推理延迟降低了多达五倍，并消除了现有 VLA 中观察到的随时间步增长的内存占用。消融实验表明，信念模块是性能提升的主要驱动力，将成功率从无信念时的 32.5% 提高到了有信念时的 77.5%。

## Abstract
Vision-language-action models must enable agents to execute long-horizon tasks under partial observability. However, most existing approaches remain observation-driven, relying on short context windows or repeated queries to vision-language models (VLMs). This leads to loss of task progress, action repetition under perceptual aliasing, and high inference latency. While semantic grounding is important, long-horizon manipulation fundamentally requires persistent, action-conditioned state representations. Current VLAs lack such representations and exhibit limited temporal and physical reasoning, making them ill-suited for multi-stage control. This paper introduces RB-VLA, a belief-centric architecture trained with self-supervised world-model objectives that maintains a compact latent state encoding task-relevant history, dynamics, and object interactions. Queried once per task, the VLM provides high-level intent, while the belief tracks task progress and enables phase-aware, causally grounded control under partial observability without storing raw observations or scaling memory with time. The belief and intent jointly condition a diffusion policy for robust closed-loop execution. RB-VLA outperforms prior VLAs on long-horizon benchmarks, achieving 52.5 percent and 37.5 percent higher success rates on multi-stage pick-and-place and stacking tasks, respectively, compared to pi_0. It also reduces inference latency by up to five times relative to baselines and eliminates memory growth across timesteps observed in existing VLAs. Ablations show the belief module is the primary driver of performance, increasing success rates from 32.5 percent without belief to 77.5 percent with belief.

---

## 论文详细总结（自动生成）

这是一份关于论文《Recursive Belief Vision Language Action Models (RB-VLA)》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作模型（VLA）在处理**长程（Long-horizon）、多阶段且具有部分可观测性**的机器人任务时表现不佳。
*   **研究背景**：
    *   **反应式局限**：大多数 VLA 遵循马尔可夫假设，仅依赖当前观测或短时历史，导致在遮挡或感知混淆（如抓取两个相似物体）时容易丢失进度。
    *   **计算瓶颈**：频繁调用大型视觉语言模型（VLM）导致高延迟；通过累积 Token 增加上下文会导致内存随时间步线性增长。
    *   **缺乏物理基础**：VLM 擅长语义推理，但缺乏对物理动力学和状态演化的显式建模。

### 2. 方法论：RB-VLA 架构
RB-VLA 的核心思想是将**语义任务理解**与**因果状态估计**解耦，引入递归信念（Recursive Belief）机制。
*   **解耦语义意图（Intent）**：在任务开始时（$t=0$）仅查询一次 VLM（如 Qwen2.5-VL），通过注意力池化提取静态的“意图向量” $I_t$。这避免了重复推理，锁定了任务目标。
*   **递归信念估计器（Belief Estimator）**：
    *   采用类似 RSSM 的世界模型结构，维护一个固定大小的潜状态 $b_t$。
    *   **递归更新**：$b_t = GRU(b_{t-1}, [e_t, z_t])$，其中 $e_t$ 是当前观测证据，$z_t$ 是捕获不确定性的随机变量。
    *   **训练目标**：使用自监督目标，包括未来潜状态预测（$x_{t+1}, x_{t+5}$）、KL 散度约束以及逆动力学损失（预测动作 $a_t$），确保信念状态包含动力学信息而非仅是视觉特征。
*   **低层动作生成**：使用扩散变压器（Diffusion Transformer）作为策略头，以信念 $b_t$ 和意图 $I_t$ 为条件，实现 50Hz 的高频闭环控制。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：RoboSuite、LIBERO-Long、LIBERO-Object 及 UR5 MuJoCo 环境。
    *   **任务类型**：单/多对象取放（含遮挡）、多块堆叠、家庭任务（放抽屉/柜子）。
*   **基准模型（Benchmarks）**：对比了 **$\pi_0$**、**OpenVLA**、**GROOT-N1** 和 **RT1-X**。
*   **评估指标**：任务成功率、推理延迟、内存增长率、信念表示的预测一致性。

### 4. 资源与算力
*   **硬件**：使用了 **12 台 NVIDIA A100 GPU (80GB)**。
*   **训练细节**：
    *   信念模型：1.5 亿参数，训练 8 万次迭代。
    *   VLM：基于 Qwen2.5-VL-7B（冻结权重）。
    *   扩散控制器：1.2 亿参数，训练 10 万次迭代。
    *   数据集：约 4 万条模拟轨迹（约 4000 万时间步）。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 40 次独立测试，涵盖了随机物体位置、光照变化和观测噪声。
*   **消融实验**：系统地测试了“无信念模块”、“无随机变量 $z$”、“无动力学预测目标”等变体，证明了信念模块是性能提升的核心（成功率从 32.5% 提升至 77.5%）。
*   **真实世界验证**：在 UR5 机械臂上进行了 25 次真实试验，验证了算法的 Sim-to-Real 迁移能力。
*   **评价**：实验设计较为全面，对比了当前最前沿的开源 VLA 模型，且针对长程任务中的痛点（遮挡、延迟）做了专项分析，具有较高的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在多阶段取放任务中，成功率比 $\pi_0$ 高出 **52.5%**；在堆叠任务中高出 **37.5%**。
*   **高效推理**：由于无需重复调用 VLM，推理延迟比 OpenVLA 降低了 **5 倍**。
*   **内存恒定**：递归信念机制使得内存占用不随任务时长增加，解决了 Token 累积导致的内存爆炸问题。
*   **鲁棒性**：在物体被遮挡后，模型仍能通过信念状态记住物体位置和任务阶段，避免了重复抓取或动作停滞。

### 7. 优点与亮点
*   **架构优雅**：通过“单次语义查询 + 持续信念更新”的模式，完美平衡了高层逻辑与低层控制的频率需求。
*   **动力学驱动**：信念状态不仅是历史的压缩，更是通过预测未来状态训练出来的，具备了物理演化的直觉。
*   **实用性强**：极低的延迟和恒定的内存占用使其非常适合部署在算力受限的真实机器人硬件上。

### 8. 不足与局限
*   **语义静态性**：目前 VLM 仅在 $t=0$ 查询，若任务中途发生需要重新规划的突发情况（如指令改变或环境剧变），模型可能需要额外的触发机制来重新查询 VLM。
*   **数据依赖**：虽然信念模型是自监督的，但仍需要大量轨迹数据来学习动力学，对于极度稀疏或全新的物理交互可能泛化受限。
*   **任务复杂度**：实验主要集中在取放和堆叠，对于更复杂的非刚体操作或精细工具使用尚待验证。

（完）
