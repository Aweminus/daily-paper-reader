---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种面向视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的强化学习框架
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步障碍，并首次将可训练的世界模型集成到分布式异步流水线中以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA水平，展现出超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 大规模VLA模型在强化学习训练中存在计算效率低下和数据获取困难的瓶颈。
method: 提出一种完全异步解耦的框架，物理隔离训练、推理与采样，并集成可插拔的世界模型生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现超线性吞吐量扩展及卓越的硬件利用率。
conclusion: AcceRL为大规模VLA模型的高效分布式强化学习训练提供了一种极具扩展性和样本效率的解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样（rollouts）过程来消除同步障碍。至关重要的是，AcceRL 是首个将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中以生成虚拟经验的框架。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展和高效的硬件利用率。在算法层面，其世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AcceRL**，一个专为大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习（RL）与世界模型框架。以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：大规模 VLA 模型（如 7B 参数量级）在进行强化学习微调时面临严重的效率瓶颈。传统的同步 RL 框架存在“长尾效应”，即训练速度受限于最慢的物理仿真环境采样和推理延迟。
*   **核心挑战**：物理模拟器频率低、模型推理开销大、数据需求量极高，导致硬件利用率低下且训练周期漫长。
*   **整体含义**：AcceRL 通过物理隔离训练、推理和采样过程，并引入可训练的世界模型进行“想象中学习”，旨在打破物理仿真的时间限制，实现极高吞吐量和样本效率的 VLA 模型训练。

### 2. 论文提出的方法论
*   **核心思想**：采用**完全解耦的异步架构**。将系统分为训练池（Trainer Pool）、推理池（Inference Pool）和采样池（Rollout Pool），消除全局同步屏障。
*   **关键技术细节**：
    *   **宏观异步（Macro-Asynchrony）**：训练与采样解耦。训练器不断从重放池采样更新，采样器独立运行并流式传输轨迹。
    *   **微观异步（Micro-Asynchrony）**：环境交互与模型推理解耦。采用“推理即服务（IaaS）”模式，通过动态窗口机制实现高效的 GPU 批处理推理。
    *   **世界模型（World Model）集成**：集成基于扩散模型（DIAMOND）的观测模型和奖励模型。Rollout 进程交替进行真实环境采样和基于世界模型的“虚拟想象”采样，后者提供高频、低成本的合成经验。
    *   **算法优化**：
        *   **GIPO 算法**：使用高斯重要性采样策略优化，通过软权重代替硬截断，缓解异步带来的策略滞后（Policy Lag）问题。
        *   **词表瘦身（Vocabulary Slimming）**：将 VLA 输出层裁剪至仅包含动作 Token，降低显存占用并提升吞吐。
        *   **价值重计算与全局优势归一化**：确保在异步环境下优势估计的准确性。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操作基准测试。
*   **Benchmark 分类**：包括 LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Long（长程任务）和 LIBERO-Goal（目标）。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于正交微调的监督学习基准。
    *   **SimpleVLA-RL**：典型的同步 RL 训练方案。
    *   **RLinf-VLA**：近期的高效 VLA 强化学习框架。
*   **消融实验**：针对价值重计算机制、GIPO 算法效果以及世界模型带来的效率提升进行了专项对比。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   训练端：使用了多达 **7 台 NVIDIA H200 GPU** 进行训练吞吐量扩展性测试。
    *   采样端：扩展至 **64 个 Rollout Workers**。
*   **算力优化**：利用 DeepSpeed **ZeRO-2** 优化器状态分区技术，显著降低了单卡显存压力，实现了超线性的吞吐量增长。
*   **训练时长**：文中未明确给出总训练小时数，但强调了在 400 次更新内即可达到近乎最优性能。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个复杂的机器人任务子集，进行了吞吐量扩展性实验、SOTA 性能对比实验以及多项消融实验。
*   **充分性评价**：实验设计较为充分。不仅验证了最终的成功率（Success Rate），还深入分析了系统层面的吞吐量（SPS）和算法层面的样本效率。
*   **客观性**：通过对比多种最新的 RL-VLA 框架，并使用标准化的 LIBERO 基准，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达到 **99.1%**，远超监督学习基准（90.7%）。
*   **超线性扩展**：得益于 ZeRO 优化和异步解耦，训练吞吐量随 GPU 数量增加呈现超线性增长（7 卡 H200 达到 104.22 SPS）。
*   **样本效率飞跃**：引入世界模型后，在线样本效率提升了 **200 倍（20,000%）**，仅需 1 万次真实环境交互即可完成复杂任务微调。
*   **稳定性**：GIPO 算法相比标准 PPO，在异步环境下表现出更强的鲁棒性和更快的收敛速度（提速约 7.5 倍）。

### 7. 优点
*   **架构创新**：首次将可训练的世界模型与分布式异步 RL 流水线深度整合，解决了 VLA 训练中“仿真慢、数据贵”的核心痛点。
*   **系统优化极致**：通过物理隔离和 IaaS 模式，极大化了 GPU 的算力利用率（利用率保持在 94% 以上）。
*   **即插即用**：世界模型模块设计灵活，可作为插件增强现有的 RL 流程。

### 8. 不足与局限
*   **模型覆盖范围**：目前主要针对 VLA 模型的动作微调，尚未支持大规模语言模型（LLM）的全量对齐训练。
*   **世界模型开销**：虽然减少了物理仿真需求，但训练和运行基于扩散模型的世界模型本身也需要额外的计算资源。
*   **误差累积风险**：尽管文中提到了限制想象步长（Horizon H），但在极度复杂的动态场景下，世界模型的预测漂移仍可能影响策略质量。

（完）
