---
title: AsgardBench -- Evaluating Visually Grounded Interactive Planning Under Minimal Feedback
title_zh: AsgardBench —— 评估极简反馈下的视觉落地交互式规划
authors: "Andrea Tupini, Lars Liden, Reuben Tan, Yu Wang, Jianfeng Gao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15888v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉感知的交互式规划与计划调整
tldr: AsgardBench是一个专注于评估具身智能交互式规划能力的基准测试，旨在测试模型在极简反馈（仅图像、动作历史和成功/失败信号）下，根据视觉观察调整高层动作序列的能力。该基准包含12类、108个任务实例，通过系统性改变物体状态和场景配置，强调条件分支和执行中的计划修复。实验表明，领先的视觉语言模型在缺乏视觉输入时表现骤降，揭示了其在视觉定位和状态跟踪方面的局限性。
selection_source: fresh_fetch
motivation: 现有基准常将高层推理与底层控制混淆，或提供过多反馈，缺乏对纯视觉驱动下交互式规划能力的独立评估。
method: 在受控模拟器中构建了108个任务实例，仅提供图像和轻量级信号，要求模型根据视觉反馈进行计划修复和条件分支决策。
result: 实验发现主流视觉语言模型在交互式规划中表现脆弱，其性能高度依赖视觉输入，且在状态跟踪方面存在明显短板。
conclusion: AsgardBench揭示了当前模型在利用视觉信息进行动态计划调整方面的不足，为提升具身智能的视觉定位与推理能力提供了评估工具。
---

## 摘要
通过 AsgardBench，我们旨在评估视觉落地的、高层动作序列生成和交互式规划，特别关注执行过程中基于视觉观察而非导航或底层操作的规划调整。在具身智能（Embodied AI）基准测试领域，AsgardBench 针对的是交互式规划这一能力类别。它比离线高层规划更复杂，因为它要求智能体根据环境反馈修正规划，但又与底层执行有所区别。与以往将推理与导航混为一谈，或提供丰富的纠错反馈以替代感知的具身智能基准测试不同，AsgardBench 将智能体的输入限制为图像、动作历史和轻量级的成功/失败信号，从而在没有底层控制噪声的可控模拟器中隔离出交互式规划。该基准测试包含跨越 12 个任务类型的 108 个任务实例，每个实例都通过物体状态、放置位置和场景配置进行了系统性的变化。这些受控的变化创建了条件分支，使得同一条指令可能根据智能体观察到的情况而需要不同的动作序列，从而强调了执行过程中的条件分支和规划修复。我们对领先的视觉语言模型的评估表明，在没有视觉输入的情况下，性能会急剧下降，这揭示了视觉落地和状态跟踪方面的弱点，最终损害了交互式规划。我们的基准测试聚焦于一个更具体的问题：当情况不如预期时，模型是否真的能利用其所见来调整规划？

## Abstract
With AsgardBench we aim to evaluate visually grounded, high-level action sequence generation and interactive planning, focusing specifically on plan adaptation during execution based on visual observations rather than navigation or low-level manipulation. In the landscape of embodied AI benchmarks, AsgardBench targets the capability category of interactive planning, which is more sophisticated than offline high-level planning as it requires agents to revise plans in response to environmental feedback, yet remains distinct from low-level execution. Unlike prior embodied AI benchmarks that conflate reasoning with navigation or provide rich corrective feedback that substitutes for perception, AsgardBench restricts agent input to images, action history, and lightweight success/failure signals, isolating interactive planning in a controlled simulator without low-level control noise. The benchmark contains 108 task instances spanning 12 task types, each systematically varied through object state, placement, and scene configuration. These controlled variations create conditional branches in which a single instruction can require different action sequences depending on what the agent observes, emphasizing conditional branching and plan repair during execution. Our evaluations of leading vision language models show that performance drops sharply without visual input, revealing weaknesses in visual grounding and state tracking that ultimately undermine interactive planning. Our benchmark zeroes in on a narrower question: can a model actually use what it sees to adapt a plan when things do not go as expected?

---

## 论文详细总结（自动生成）

以下是对论文《AsgardBench -- Evaluating Visually Grounded Interactive Planning Under Minimal Feedback》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的具身智能（Embodied AI）基准测试（如 ALFRED, ALFWorld）往往将高层推理与底层的导航、抓取控制混为一谈。此外，许多基准测试提供过于详细的文本反馈（如“动作失败，因为杯子不在水槽里”），这实际上充当了“视觉代理”，导致模型可以通过文本先验或记忆模板完成任务，而无需真正的视觉感知。
*   **研究动机**：作者认为，具身智能的核心挑战之一是**视觉落地的交互式规划**——即智能体必须根据视觉观察实时检测环境状态（如物体是否脏、容器是否开启），并在假设失效时修正计划。AsgardBench 旨在通过抽象掉底层控制噪声，隔离并评估这一特定能力。

### 2. 论文提出的方法论
*   **核心思想**：在受控的 3D 模拟器（AI2-THOR）中，通过系统性地改变物体状态和场景配置，强制模型进行“条件分支”推理。
*   **关键技术细节**：
    *   **动作抽象**：移除了导航和底层物理抓取。使用 `FIND` 动作自动将智能体置于物体前，`PUT` 动作自动处理放置逻辑。
    *   **极简反馈**：仅提供图像、动作历史和简单的“成功/失败”信号，不提供失败的具体原因。
    *   **闭环规划**：模型在每一步都要输出一个完整的后续动作序列（Suggested Plan），但环境仅执行第一条指令。随后，模型根据更新后的视觉观察重新评估并修正计划。
    *   **辅助机制**：
        *   **手部叠加层（Hand Overlay）**：在图像中叠加半透明手部，消除“物体是拿在手里还是放在桌上”的视觉歧义。
        *   **记忆脚手架（Things to Remember）**：要求模型在每步输出中生成一段文本记忆，并在下一步作为输入，以辅助跨步的状态跟踪。

### 3. 实验设计
*   **场景与任务**：基于 AI2-THOR 的厨房、客厅和浴室场景。包含 12 种任务类型（如“煮蛋并盛盘”、“清理镜子”），共 108 个测试实例。
*   **任务变体**：通过改变初始状态（如杯子是干净还是脏、水槽里是否有杂物）产生不同的执行路径。
*   **对比方法**：
    *   **主流模型**：GPT-4o, Kimi-K2.5, Claude Opus 4.5, Gemini 3 Pro, Qwen3-VL, GLM-4.6V, Llama-4 等。
    *   **实验条件对比**：图像模式 vs. 纯文本模式（Text-Only）；不同反馈级别（无反馈、简单反馈、详细反馈）。
    *   **消融实验**：移除手部叠加层、移除记忆脚手架、单图输入 vs. 双图输入（当前帧与前一帧）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**训练模型所使用的算力，因为该研究主要针对现有的闭源和开源视觉语言模型（VLM）进行推理评估。
*   **推理设置**：模型通过 Azure Foundry 和 OpenRouter 接口调用。所有模型推理时温度（Temperature）设为 0，以保证结果的可重复性。

### 5. 实验数量与充分性
*   **实验规模**：针对 108 个任务实例，每个模型在多种消融条件下进行了多次独立运行（取中位数），实验总数达到数千次任务尝试。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了从闭源顶尖模型到开源轻量级模型的广泛梯度，且消融实验设计严密，涵盖了视觉、记忆、反馈等多个维度。
    *   **公平性**：所有模型使用统一的 Prompt 结构和任务实例；通过预生成的固定测试集确保了对比的基准一致。

### 6. 论文的主要结论与发现
*   **视觉落地的必要性**：在 AsgardBench 中，视觉输入对性能至关重要。所有模型在“纯文本”模式下的表现均大幅下降（通常下降 50% 以上），证明该基准成功隔离了视觉推理需求。
*   **状态跟踪是瓶颈**：即使是顶尖模型（如 GPT-4o, Kimi-K2.5）也经常在状态跟踪上出错，例如忘记物体已经洗过，或者在视野改变后丢失物体位置。
*   **反馈的替代效应**：提供“详细反馈”会显著提升性能，甚至让纯文本模型追平视觉模型。这证实了现有基准测试若提供过多文本反馈，会掩盖模型视觉能力的不足。
*   **视觉误判**：模型常出现视觉幻觉，如将金属倒影误认为火焰，或将背景杂物误认为目标物体。

### 7. 优点
*   **能力隔离**：成功将高层规划与底层控制解耦，为评估具身智能的“大脑”推理能力提供了纯净的测试场。
*   **受控的复杂性**：通过物体状态的系统性变化（如脏/净、开/关）创造了自然的条件分支，比固定路径的任务更具挑战性。
*   **诊断性强**：消融实验（如手部叠加层）精准地指出了模型在空间关系和状态估计上的具体弱点。

### 8. 不足与局限
*   **生态效度（Ecological Validity）**：由于抽象掉了导航和物理抓取，该基准无法评估智能体在处理物理不确定性和连续控制时的表现。
*   **场景多样性**：受限于 AI2-THOR 模拟器，视觉多样性和物体种类仍有限，可能无法完全代表真实世界的复杂视觉环境。
*   **规划格式偏好**：要求模型每步输出完整序列可能更偏向于“规划导向型”的大模型，而对“反应型”策略不够友好。
*   **反馈依赖**：虽然旨在评估极简反馈，但实验显示模型对“成功/失败”信号仍有极高依赖，完全移除反馈后性能损失巨大。

（完）
