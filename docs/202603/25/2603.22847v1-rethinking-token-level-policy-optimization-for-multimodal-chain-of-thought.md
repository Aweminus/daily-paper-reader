---
title: Rethinking Token-Level Policy Optimization for Multimodal Chain-of-Thought
title_zh: 重新审视多模态思维链的词元级策略优化
authors: "Yunheng Li, Hangyi Kuang, Hengrui Zhang, Jiangxia Cao, Zhaojie Liu, Qibin Hou, Ming-Ming Cheng"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22847v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言模型多模态思维链推理的标记级策略优化
tldr: 本研究针对多模态思维链（CoT）推理中感知落地与多步推理交织的特性，指出传统强化学习方法在优化粒度上的不足。通过分析Token级动态，本文提出PEPO算法，利用隐藏状态相似度提取感知先验，并结合Token熵通过平滑门控机制产生Token级优势。PEPO可无缝集成至GRPO等框架，无需额外监督。实验证明，该方法在几何推理、视觉拼图等任务上表现优异，显著提升了多模态模型的推理性能与训练稳定性。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习方法通常采用粗粒度优化，忽视了推理轨迹中感知落地与探索性推理在Token层面的差异性。
method: 提出PEPO算法，通过隐藏状态相似度导出感知先验，并利用平滑门控机制将其与Token熵结合，生成细粒度的Token级优势函数。
result: 在几何推理、视觉定位、视觉拼图和少样本分类等多个基准测试中，PEPO在保持训练稳定的同时，性能一致优于GRPO和DAPO等强基线。
conclusion: 通过在Token级别平衡感知与探索，可以在不增加额外监督或辅助分支的情况下，显著增强多模态大模型的复杂推理能力。
---

## 摘要
多模态思维链（CoT）推理要求大型视觉语言模型构建推理轨迹，将感知落地（perceptual grounding）与多步推理交织在一起。然而，现有的带可验证奖励的强化学习（RLVR）方法通常以粗粒度优化推理，对 CoT 进行统一处理，而未区分其不同程度的视觉落地。在这项工作中，我们对多模态推理轨迹进行了词元级分析，并表明成功的推理具有结构化的词元动态特征，反映了感知落地和探索性推理。基于这一分析，我们提出了感知-探索策略优化（PEPO），它从隐藏状态相似性中推导出感知先验，并通过平滑门控机制将其与词元熵相结合，从而产生词元级优势。PEPO 与现有的 RLVR 框架（如 GRPO 和 DAPO）无缝集成，既不需要额外的监督，也不需要辅助分支。在多种多模态基准测试上的广泛实验表明，该方法在几何推理、视觉落地、视觉谜题求解和少样本分类等领域，相较于强大的强化学习基准模型取得了持续且稳健的提升，同时保持了稳定的训练动态。代码：https://github.com/xzxxntxdy/PEPO

## Abstract
Multimodal Chain-of-Thought (CoT) reasoning requires large vision-language models to construct reasoning trajectories that interleave perceptual grounding with multi-step inference. However, existing Reinforcement Learning with Verifiable Rewards (RLVR) methods typically optimize reasoning at a coarse granularity, treating CoT uniformly without distinguishing their varying degrees of visual grounding. In this work, we conduct a token-level analysis of multimodal reasoning trajectories and show that successful reasoning is characterized by structured token dynamics reflecting both perceptual grounding and exploratory inference. Building upon this analysis, we propose Perception-Exploration Policy Optimization (PEPO), which derives a perception prior from hidden state similarity and integrates it with token entropy through a smooth gating mechanism to produce token-level advantages. PEPO integrates seamlessly with existing RLVR frameworks such as GRPO and DAPO, requiring neither additional supervision nor auxiliary branches. Extensive experiments across diverse multimodal benchmarks demonstrate consistent and robust improvements over strong RL baselines, spanning geometry reasoning, visual grounding, visual puzzle solving, and few-shot classification, while maintaining stable training dynamics. Code: https://github.com/xzxxntxdy/PEPO

---

## 论文详细总结（自动生成）

# 论文总结：重新审视多模态思维链的词元级策略优化 (PEPO)

### 1. 核心问题与整体含义
*   **研究动机**：大型视觉语言模型（LVLM）在进行多模态思维链（CoT）推理时，现有的强化学习方法（如 GRPO）通常采用**序列级（Sequence-level）**的监督信号。这种粗粒度的优化方式将整个推理轨迹视为一个整体，忽略了不同词元（Token）在“视觉感知落地”和“逻辑推理探索”上的贡献差异。
*   **核心问题**：如何实现更细粒度的词元级优化，使模型在推理过程中既能准确挂钩视觉信息（感知），又能在关键决策点进行有效尝试（探索）？

### 2. 方法论：感知-探索策略优化 (PEPO)
*   **核心思想**：通过计算 Token 级别的感知先验和探索动态，动态调整每个 Token 在策略梯度更新中的权重，从而将序列级优势（Advantage）重新分配到具体的 Token 上。
*   **关键技术细节**：
    1.  **感知建模（Perception Modeling）**：利用模型内部隐藏状态，计算响应 Token 与所有视觉 Token 之间的平均余弦相似度。相似度越高，代表该 Token 的生成越依赖于视觉输入。
    2.  **探索建模（Exploration Modeling）**：利用输出 Logits 计算 Token 熵（Entropy）。高熵 Token 通常对应推理过程中的不确定点或逻辑转折点（如“Aha moment”）。
    3.  **平滑门控融合（Smooth Gating Fusion）**：设计了一个平滑门控机制，将归一化后的感知得分和熵得分结合，生成 Token 权重 $w_t$。该机制确保感知信号占主导，同时允许熵信号在视觉相关的 Token 上进行调节。
    4.  **Token 级优势分配**：将 GRPO 的序列级优势 $A$ 转化为 Token 级优势 $A_t = [(1-\lambda) + \lambda w_t] A$，其中 $\lambda$ 随训练过程动态增加，实现从序列级到 Token 级的平滑过渡。

### 3. 实验设计
*   **数据集与场景**：
    *   **几何与逻辑推理**：Geometry3K, MathVista, MathVerse, LogicVista。
    *   **视觉定位**：RefCOCO, LISA-Grounding。
    *   **少样本分类**：FGVC Aircraft, Flower102。
    *   **视觉拼图**：PuzzleVQA, AlgoPuzzleVQA。
    *   **大规模扩展性**：ViRL39K。
*   **基准模型（Backbones）**：Qwen2.5-VL-3B-Instruct, InternVL3-2B-Instruct。
*   **对比方法**：GRPO（基准）、DAPO、High-Entropy RL（仅基于熵的优化）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A40 GPU**。
*   **软件框架**：基于 Swift 框架实现，使用 DeepSpeed ZeRO-2 进行分布式优化。
*   **训练细节**：采用全参数微调，bfloat16 精度，开启梯度检查点（Gradient Checkpointing），使用 FlashAttention-2 加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 大类任务、超过 10 个主流基准测试，实验覆盖面广。
*   **消融实验**：非常充分。作者分别验证了感知组件、探索组件、平滑门控设计、超参数 $\alpha$ 敏感度、隐藏层选择以及 Token 权重调度策略的有效性。
*   **客观性**：通过跨领域（Out-of-domain）测试验证了模型的泛化能力，并报告了多次运行的平均结果，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：PEPO 在所有任务上均显著优于 GRPO 和 DAPO。例如，在 Qwen2.5-VL-3B 上，几何推理平均提升了 3.67 个百分点。
*   **训练效率**：PEPO 引入的额外计算开销极低，计算权重的耗时占比（$\rho$）**低于 1%**，且由于生成的推理链更简洁，实际吞吐量有时甚至高于基准方法。
*   **训练稳定性**：相比于纯熵优化（High-Entropy RL）容易导致训练崩溃，PEPO 结合感知先验后表现出极强的稳定性。
*   **定性发现**：PEPO 训练的模型能生成逻辑更严密、视觉对齐度更高的推理轨迹，减少了幻觉和逻辑矛盾。

### 7. 优点
*   **无需额外监督**：不需要额外的感知标注或辅助模型（如检测器），直接利用模型自身的隐藏状态。
*   **即插即用**：可以无缝集成到现有的 RLVR 框架（如 GRPO、DAPO）中，兼容性强。
*   **计算高效**：通过简单的相似度计算实现细粒度优化，几乎不增加训练负担。

### 8. 不足与局限
*   **模型规模验证**：实验主要在 2B-3B 规模的模型上完成，尚未在 7B、72B 等更大参数规模的模型上验证其扩展性。
*   **任务局限性**：虽然涵盖了多种推理任务，但对于视频理解、长文档分析等更长上下文场景的验证尚不充分。
*   **超参数依赖**：虽然 $\alpha$ 具有一定的鲁棒性，但在不同性质的任务（如分类 vs 推理）间仍需微调以达到最优效果。

（完）
