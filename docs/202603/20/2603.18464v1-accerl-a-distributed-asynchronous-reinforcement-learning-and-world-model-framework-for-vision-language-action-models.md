---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉-语言-动作模型的强化学习
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习训练中面临的计算效率瓶颈和数据获取难题。
method: 采用完全异步且解耦的架构物理隔离核心环节，并引入即插即用的世界模型生成虚拟经验。
result: 在LIBERO基准上取得SOTA性能，实现吞吐量的超线性扩展及卓越的硬件利用率。
conclusion: AcceRL证明了异步解耦架构与世界模型结合能显著提升大规模VLA模型的训练效率与稳定性。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：分布式异步强化学习与世界模型框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
大规模视觉-语言-动作（VLA）模型在具身智能领域展现了巨大潜力，但将其应用于强化学习（RL）时面临两大瓶颈：
*   **计算效率低下（同步障碍）：** 传统同步RL框架在环境交互、策略推理和模型更新之间存在严格的顺序依赖。由于物理模拟器速度慢且VLA模型推理延迟高，导致高性能GPU频繁处于空闲状态（即“长尾效应”）。
*   **数据饥渴：** 大规模VLA模型需要海量交互数据，而物理模拟器的步进频率限制了数据获取速度。
**AcceRL** 旨在通过完全异步的解耦架构和集成“世界模型”来打破这些限制，实现高效的分布式训练。

#### 2. 方法论：核心思想与关键技术
AcceRL 提出了双层异步机制和世界模型增强方案：
*   **宏观异步（训练与采样解耦）：** 训练器（Trainer）与采样器（Rollout Worker）物理隔离。采样器持续将轨迹存入重放池，训练器独立采样更新，互不阻塞。
*   **微观异步（交互与推理解耦）：** 采用“推理即服务（Inference-as-a-Service）”模式。采样器仅负责环境步进，推理请求发送至中心化推理池，通过动态批处理（Dynamic Batching）最大化GPU利用率。
*   **世界模型（World Model）集成：** 首次在异步流水线中加入可训练的世界模型（基于扩散模型DIAMOND）。智能体可以在“想象”中进行虚拟交互，生成高保真合成经验，绕过物理模拟器的速度限制。
*   **算法优化：**
    *   **GIPO（高斯重要性采样策略优化）：** 替代标准PPO的硬截断，通过平滑权重缓解异步带来的策略滞后（Policy Lag）问题。
    *   **词表瘦身（Vocabulary Slimming）：** 裁剪语言模型头，仅保留动作Token，降低显存占用并提升吞吐。
    *   **Token级优化：** 在Token级别计算重要性采样比率，增强数值稳定性。

#### 3. 实验设计
*   **基准测试：** 使用 **LIBERO** 机器人操作基准，涵盖 LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）四个子集。
*   **对比方法：** 
    *   监督学习基准：OpenVLA-OFT。
    *   强化学习SOTA：SimpleVLA-RL、RLinf-VLA。
*   **评估指标：** 任务成功率、系统吞吐量（SPS，每秒样本数）、硬件利用率。

#### 4. 资源与算力
*   **GPU型号：** 实验中提到了使用 **NVIDIA H200** GPU 进行吞吐量测试。
*   **规模：** 训练器扩展至 **7个 GPU**，采样器扩展至 **64个节点**。
*   **优化技术：** 集成了 DeepSpeed **ZeRO-2** 优化，通过分割优化器状态和梯度来降低显存压力。
*   **训练时长：** 文中未明确给出总训练小时数，但展示了在 400 次更新或 10,000 步环境交互内即可实现快速收敛。

#### 5. 实验数量与充分性
*   **实验覆盖：** 论文在 LIBERO 的四个核心任务集上进行了全面测试，并展示了学习曲线。
*   **消融实验：** 针对“价值重计算（Value Re-computation）”和“GIPO 算法有效性”做了专门的对比实验。
*   **系统扩展性实验：** 详细测试了随着 GPU 和采样节点增加，系统吞吐量的变化情况。
*   **评价：** 实验设计较为充分，涵盖了从系统性能到算法精度的多维度验证，对比对象均为当前领域内最先进的框架，具有较强的客观性。

#### 6. 主要结论与发现
*   **性能领先：** AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达 99.1%，远超模仿学习。
*   **超线性扩展：** 借助 ZeRO 优化和异步架构，训练器吞吐量随 GPU 数量增加呈现超线性增长（7张 H200 达到 104.22 SPS）。
*   **样本效率飞跃：** 引入世界模型后，在线样本效率提升了 **200倍（20,000%）**，在极少量的真实环境交互下即可完成策略微调。
*   **稳定性：** GIPO 算法相比 PPO 在异步环境下表现出更强的鲁棒性，收敛速度提升约 7.5 倍。

#### 7. 优点与亮点
*   **架构彻底解耦：** 解决了具身智能训练中常见的“GPU等待模拟器”的顽疾。
*   **即插即用的世界模型：** 将生成式 AI（扩散模型）与分布式 RL 流水线深度融合，实现了“在想象中学习”。
*   **系统级优化：** 词表瘦身和推理池化等设计非常贴合大规模 VLA 模型的实际部署需求。

#### 8. 不足与局限
*   **模型对齐限制：** 论文提到目前该框架尚不支持大规模语言模型的全量后训练（Post-training）对齐。
*   **世界模型开销：** 虽然减少了物理交互，但训练和运行高保真扩散模型本身需要额外的计算资源。
*   **误差累积风险：** 尽管采用了短时域（Short-horizon）想象，但长期的自回归预测仍可能存在复合误差。

（完）
