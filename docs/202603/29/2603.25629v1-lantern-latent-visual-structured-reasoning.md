---
title: "LanteRn: Latent Visual Structured Reasoning"
title_zh: LanteRn：潜空间视觉结构化推理
authors: "André G. Viveiros, Nuno Gonçalves, Matthias Lindemann, André Martins"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25629v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态模型中的潜空间视觉结构化推理
tldr: "针对当前大语言模型在视觉推理中过度依赖文本描述而缺乏细粒度空间理解的问题，本文提出了 LanteRn 框架。该框架允许模型在推理过程中交织语言与紧凑的潜空间视觉表示，直接在潜空间进行视觉推理。通过监督微调和强化学习两阶段训练，LanteRn 在 VisCoT、V* 和 Blink 等基准测试中显著提升了视觉定位和细粒度推理能力，为高效多模态推理提供了新方向。"
selection_source: fresh_fetch
motivation: 现有大模型通常将视觉内容转化为文本进行推理，这限制了其对细粒度空间和视觉信息的理解效率与准确性。
method: 提出 LanteRn 框架，使模型能生成并关注连续的潜空间视觉思维嵌入，并采用监督微调与强化学习两阶段策略进行训练。
result: "在 VisCoT、V* 和 Blink 等多个感知核心基准测试中，LanteRn 在视觉定位和细粒度推理任务上均取得了显著的性能提升。"
conclusion: 利用内部潜空间表示进行推理是提升多模态模型效率和细粒度理解能力的一种极具前景的方向。
---

## 摘要
尽管语言推理模型在许多任务中表现出色，但视觉推理对于当前的大型多模态模型（LMMs）而言仍具挑战性。因此，大多数 LMM 默认将感知内容转化为文本，这对于需要细粒度空间和视觉理解的任务来说是一个严重的局限。虽然最近的方法通过调用工具或生成中间图像在“以图思考”方面取得了进展，但它们要么依赖外部模块，要么因直接在像素空间进行推理而产生不必要的计算开销。在本文中，我们介绍了 LanteRn，这是一个使 LMM 能够将语言与紧凑的潜视觉表示交织在一起的框架，从而允许视觉推理直接在潜空间中进行。LanteRn 增强了视觉-语言 Transformer 的能力，使其在推理过程中能够生成并关注连续的视觉思维嵌入。我们分两个阶段训练该模型：首先通过监督微调将视觉特征锚定在潜状态中，随后通过强化学习使潜推理与任务级效用对齐。我们在三个以感知为中心的基准测试（VisCoT、V* 和 Blink）上评估了 LanteRn，观察到在视觉定位和细粒度推理方面有一致的提升。这些结果表明，内部潜表示为更高效的多模态推理提供了一个极具前景的方向。

## Abstract
While language reasoning models excel in many tasks, visual reasoning remains challenging for current large multimodal models (LMMs). As a result, most LMMs default to verbalizing perceptual content into text, a strong limitation for tasks requiring fine-grained spatial and visual understanding. While recent approaches take steps toward thinking with images by invoking tools or generating intermediate images, they either rely on external modules, or incur unnecessary computation by reasoning directly in pixel space. In this paper, we introduce LanteRn, a framework that enables LMMs to interleave language with compact latent visual representations, allowing visual reasoning to occur directly in latent space. LanteRn augments a vision-language transformer with the ability to generate and attend to continuous visual thought embeddings during inference. We train the model in two stages: supervised fine-tuning to ground visual features in latent states, followed by reinforcement learning to align latent reasoning with task-level utility. We evaluate LanteRn on three perception-centric benchmarks (VisCoT, V*, and Blink), observing consistent improvements in visual grounding and fine-grained reasoning. These results suggest that internal latent representations provide a promising direction for more efficient multimodal reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《LanteRn: Latent Visual Structured Reasoning》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的大型多模态模型（LMMs）在处理视觉任务时，通常采用“关于图像的思考（thinking about images）”模式，即将视觉信息转化为文本描述后再进行推理。这种方式会导致高维视觉信息的丢失，尤其在需要细粒度空间理解和复杂感知任务中表现不佳。
*   **核心问题**：如何让模型实现“以图思考（thinking with images）”，即在推理过程中直接利用视觉特征，同时避免调用外部工具的复杂性或直接在像素空间生成图像带来的巨大计算开销。
*   **整体含义**：本文提出了 **LanteRn** 框架，通过在文本序列中交织“潜空间视觉思维嵌入（Latent Visual Thoughts）”，使模型能够在潜空间内直接进行结构化视觉推理。

### 2. 论文提出的方法论
LanteRn 核心思想是将推理过程建模为文本标记与连续潜向量的混合轨迹。
*   **核心架构**：基于 Qwen2.5-VL-3B，扩展了三个特殊控制标记：`<|lvr_start|>`、`<|lvr_sep|>` 和 `<|lvr_end|>`。
    *   **文本模式**：正常生成文本 Token。
    *   **潜视觉模式**：生成开始标记后，模型连续输出 $K$ 个隐藏状态向量（不经过语言头投影），作为内部视觉“思维”嵌入，随后返回文本模式。
*   **两阶段训练策略**：
    1.  **监督微调 (SFT) - 视觉锚定**：利用视觉编码器作为“教师”。对于推理路径中涉及的图像区域（ROI），通过平均池化提取特征作为目标值，训练模型通过 MSE 损失函数在潜空间“重建”这些视觉特征。
    2.  **强化学习 (RL) - 任务对齐**：采用 **GRPO（组相对策略优化）** 算法。
        *   **混合动作空间**：同时优化离散文本和连续潜向量。
        *   **潜状态重放 (Latent State Replay)**：在策略更新时固定采样时的潜向量，以稳定重要性采样比率，同时允许梯度回传至潜向量生成参数。
        *   **奖励设计**：结合准确率奖励（Accuracy Reward）和格式奖励（Format Reward）。

### 3. 实验设计
*   **数据集**：
    *   **SFT 阶段**：基于 Visual-CoT 构建的合成数据集，利用 Qwen3-VL-235B 生成带有边界框标注的推理链。
    *   **RL 阶段**：使用 VIRL-39k 数据集，包含多样的视觉推理问题，无需区域级标注。
*   **Benchmark（基准测试）**：
    *   **VisCoT**：评估多步视觉推理。
    *   **V\***：评估视觉搜索能力（包括直接归因 DA 和相对位置 RP 子集）。
    *   **Blink**：评估核心视觉感知（包括对象定位 OL 和相对位置 RP 子集）。
*   **对比方法**：
    *   **Qwen2.5-VL-3B**：原始基座模型。
    *   **LanteRn-NTP**：消融基准，将中间推理步骤视为标准文本 Token 处理，不使用连续潜向量，以验证潜空间表示的独特作用。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **模型规模**：实验主要基于 3B 参数规模的模型（Qwen2.5-VL-3B），并提到在某些基准上达到了 7B 规模模型的性能水平。
*   **实现工具**：使用了 Hugging Face 的 TRL 库进行强化学习训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对潜向量大小 $K$ 进行了消融实验（$K \in \{4, 8, 16, 32\}$）。
    *   在三个主流视觉推理基准及其多个子任务上进行了全面评估。
*   **充分性与客观性**：实验设计较为严谨，通过引入 NTP（Next Token Prediction）基准，排除了“性能提升仅源于推理步骤增加”的可能性。对比了 SFT 和 RL 两个阶段的效果，证明了 RL 在提升任务效用方面的关键作用。但受限于算力，未在更大规模（如 7B+）的模型上进行验证。

### 6. 论文的主要结论与发现
*   **潜空间推理的有效性**：LanteRn 在感知核心任务（如对象定位和空间关系）上显著优于纯文本推理模型。
*   **RL 的转变作用**：SFT 阶段主要让模型学会“重建”视觉特征，而 RL 阶段则促使潜表示从“视觉保真”转向“任务驱动”，显著提升了最终答案的准确率。
*   **性能表现**：在 Blink RP 任务中，RL 后的模型性能从 SFT 的 0.68 提升至 0.81，证明了潜空间思维在复杂关系推理中的潜力。

### 7. 优点
*   **高效性**：相比于生成像素图像的视觉推理，潜空间推理计算成本更低，且保留了高维结构信息。
*   **端到端集成**：无需依赖外部工具或复杂的流水线，直接在 Transformer 内部实现交织推理。
*   **训练创新**：提出的“潜状态重放”机制有效解决了强化学习在混合（离散+连续）动作空间中训练不稳定的问题。

### 8. 不足与局限
*   **固定容量限制**：目前使用固定数量（$K$）的潜标记，无法根据任务复杂度动态调整推理长度。
*   **可解释性挑战**：潜空间中的“视觉思维”是连续向量，难以像文本或图像那样直观地被人类理解或可视化。
*   **数据领域局限**：训练数据主要集中在特定的视觉推理领域，对于更广泛、通用的多模态场景的泛化能力仍需进一步验证。
*   **算力细节缺失**：缺乏具体的训练资源消耗报告，不利于其他研究者评估复现成本。

（完）
