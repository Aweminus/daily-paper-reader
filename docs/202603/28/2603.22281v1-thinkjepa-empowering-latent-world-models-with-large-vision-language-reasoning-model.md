---
title: "ThinkJEPA: Empowering Latent World Models with Large Vision-Language Reasoning Model"
title_zh: ThinkJEPA：利用大视觉语言推理模型赋能潜世界模型
authors: "Haichao Zhang, Yijiang Li, Shwai He, Tushar Nagarajan, Mingfei Chen, Jianglin Lu, Ang Li, Yun Fu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22281v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 利用大型视觉语言推理模型增强潜空间世界模型
tldr: ThinkJEPA 结合了 JEPA 潜空间世界模型与大视觉语言模型（VLM）的优势。针对 JEPA 在长程语义捕捉上的不足以及 VLM 在密集预测上的局限，该研究提出双时间路径框架：利用 JEPA 分支处理细粒度动作，利用 VLM 分支提供长程语义引导。通过层次化金字塔表示提取模块，实现了 VLM 推理信号向潜空间预测的有效迁移，显著提升了长程轨迹预测的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的潜空间世界模型难以捕捉长程语义，而视觉语言模型虽具备强语义推理能力却难以进行细粒度的密集状态预测。
method: 提出一种双时间路径框架，结合处理细粒度运动的密集 JEPA 分支和提供长程语义引导的 VLM “思考者”分支，并利用层次化金字塔模块提取特征。
result: 在手部操作轨迹预测实验中，该方法在性能上优于纯 VLM 和纯 JEPA 基准模型，展现出更强的长程预测鲁棒性。
conclusion: 通过将 VLM 的高层推理能力引入 JEPA 框架，可以有效增强潜空间世界模型对复杂长程动态的建模能力。
---

## 摘要
潜世界模型（例如 V-JEPA2）的最新进展在根据视频观测预测未来世界状态方面展现出了广阔的前景。然而，基于短观测窗口的密集预测限制了时间上下文，并可能使预测器偏向于局部的低级外推，从而难以捕捉长程语义并降低了下游任务的效用。相比之下，视觉语言模型（VLM）通过对均匀采样的视频帧进行推理，提供了强大的语义基础和通用知识，但由于计算驱动的稀疏采样、将细粒度交互状态压缩为面向文本表示的语言输出瓶颈，以及在适应小型动作条件数据集时的数据分布不匹配，它们并不适合作为独立的密集预测器。我们提出了一种 VLM 引导的 JEPA 风格潜世界建模框架，通过双时间路径将密集帧动力学建模与长程语义引导相结合：一个用于捕捉细粒度运动和交互线索的密集 JEPA 分支，以及一个具有更大时间步长、用于提供丰富知识引导的均匀采样 VLM “思考者”（thinker）分支。为了有效地迁移 VLM 的渐进式推理信号，我们引入了一个分层金字塔表示提取模块，将多层 VLM 表示聚合为与潜空间预测兼容的引导特征。在手部操作轨迹预测上的实验表明，我们的方法优于强 VLM 基线和 JEPA 预测器基线，并表现出更稳健的长程推演行为。

## Abstract
Recent progress in latent world models (e.g., V-JEPA2) has shown promising capability in forecasting future world states from video observations. Nevertheless, dense prediction from a short observation window limits temporal context and can bias predictors toward local, low-level extrapolation, making it difficult to capture long-horizon semantics and reducing downstream utility. Vision--language models (VLMs), in contrast, provide strong semantic grounding and general knowledge by reasoning over uniformly sampled frames, but they are not ideal as standalone dense predictors due to compute-driven sparse sampling, a language-output bottleneck that compresses fine-grained interaction states into text-oriented representations, and a data-regime mismatch when adapting to small action-conditioned datasets. We propose a VLM-guided JEPA-style latent world modeling framework that combines dense-frame dynamics modeling with long-horizon semantic guidance via a dual-temporal pathway: a dense JEPA branch for fine-grained motion and interaction cues, and a uniformly sampled VLM \emph{thinker} branch with a larger temporal stride for knowledge-rich guidance. To transfer the VLM's progressive reasoning signals effectively, we introduce a hierarchical pyramid representation extraction module that aggregates multi-layer VLM representations into guidance features compatible with latent prediction. Experiments on hand-manipulation trajectory prediction show that our method outperforms both a strong VLM-only baseline and a JEPA-predictor baseline, and yields more robust long-horizon rollout behavior.

---

## 论文详细总结（自动生成）

### ThinkJEPA：利用大视觉语言推理模型赋能潜世界模型 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了如何提升**潜世界模型（Latent World Models）**在复杂视频序列中的长程预测能力。
*   **研究动机**：现有的 JEPA（联合嵌入预测架构）模型（如 V-JEPA）擅长捕捉短窗口内的细粒度运动，但缺乏长程语义理解和通用世界知识，容易陷入局部外推。
*   **背景挑战**：视觉语言模型（VLM）虽具备强语义推理能力，但由于计算成本高（通常采用稀疏采样）、语言输出瓶颈（将物理细节压缩为文本）以及领域数据不匹配，无法直接作为高效的密集物理预测器。
*   **核心目标**：将 VLM 的“思考”能力（语义引导）与 JEPA 的“观察”能力（密集动力学建模）结合，构建一个既懂物理细节又懂高层逻辑的世界模型。

#### 2. 方法论：核心思想与关键技术
ThinkJEPA 提出了一个**双时间路径（Dual-Temporal Pathway）**框架：
*   **密集 JEPA 分支**：对视频进行高频采样，提取细粒度的运动和交互特征，作为预测的基础。
*   **均匀采样 VLM 分支**：以大步长均匀采样视频帧，输入 VLM（如 Qwen3-VL Thinking）进行全局推理，提供长程语义背景。
*   **层次化金字塔表示提取（Hierarchical Pyramid Representation Extraction）**：
    *   不只使用 VLM 的最后一层输出，而是聚合了视觉编码器（ViT）令牌和语言模型（LLM）多个中间层的隐藏状态。
    *   这种设计保留了从低级视觉到高级推理的渐进式信号。
*   **层级引导注入**：利用 **FiLM（特征线性调制）** 机制，将提取的 VLM 引导特征逐层注入到 JEPA 预测器中，调节其对未来潜状态的预测。
*   **递归推演（Recursive Rollout）**：支持通过将预测的未来潜状态重新输入模型，实现超越观测窗口的长程预测。

#### 3. 实验设计
*   **数据集/场景**：
    *   **EgoDex**：大规模第一视角灵巧操作基准，包含 3D 手部姿态标注。
    *   **EgoExo4D**：大规模多视角人类技能活动数据集。
*   **任务**：基于过去观测预测未来 32 帧的 3D 手部运动轨迹。
*   **对比方法（Baselines）**：
    *   **Qwen3-VL Thinking (VLM-only)**：仅使用 VLM 特征进行预测。
    *   **V-JEPA Predictor (JEPA-only)**：标准的潜空间预测模型。
    *   **轨迹预测基准**：包括 Behavior Cloning (BC)、DDPM (扩散模型) 和 Flow Matching (FM) 等 6 种主流策略表示。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了超参数设置，如训练 Batch Size 为 14，评估为 6；学习率为 $10^{-3}$；使用了 V-JEPA-L (ViT-Large) 作为骨干网络。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   两个大型数据集上的定量对比（ADE/FDE 轨迹误差、潜空间距离指标）。
    *   **消融实验**：针对 VLM 令牌来源（编码器 vs. 自回归令牌）、VLM 层级选择、调节机制（FiLM vs. Cross-attn vs. AdaLN）、时间采样步长等进行了详细测试。
    *   **长程推演测试**：评估了在 4、8、16、32 步递归推演下的性能演变。
*   **充分性评价**：实验设计较为**充分且客观**。通过对比不同架构（Decoder-only vs. Encoder-Decoder）和不同生成范式（扩散、流匹配），证明了其方法在轨迹预测任务上的优越性。

#### 6. 主要结论与发现
*   **性能提升**：ThinkJEPA 在所有指标上均优于纯 VLM 和纯 JEPA 基准。在 EgoDex 上，其轨迹预测精度（Acc）显著高于 V-JEPA（0.596 vs 0.471）。
*   **长程鲁棒性**：在递归推演实验中，ThinkJEPA 的误差增长速度远慢于基准模型，证明 VLM 的语义引导能有效抑制长程预测中的误差累积。
*   **特征互补**：实验证明 VLM 的中间层特征（金字塔结构）比单一末层特征更能提供有效的物理推理线索。

#### 7. 优点（亮点）
*   **创新的双路径设计**：巧妙解决了密集动力学与长程语义在计算预算下的矛盾。
*   **金字塔特征提取**：挖掘了 VLM 内部推理过程的价值，而非仅将其视为黑盒分类器。
*   **通用性**：该框架不改变 JEPA 的潜空间预测本质，使其易于集成到现有的世界模型工作流中。

#### 8. 不足与局限
*   **计算开销**：虽然采用了缓存机制，但推理时仍需运行庞大的 VLM（如 Qwen3-VL），这增加了系统的整体延迟和显存占用。
*   **任务局限**：实验主要集中在手部操作轨迹预测，尚未在更广泛的具身智能任务（如全身导航、复杂环境交互）中验证。
*   **端到端训练**：目前 VLM 分支主要作为特征提供者（Cached），若能实现更深度的端到端联合微调，可能会有进一步提升，但计算压力会巨大。

（完）
