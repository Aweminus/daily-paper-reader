---
title: "Meanings and Measurements: Multi-Agent Probabilistic Grounding for Vision-Language Navigation"
title_zh: 意义与度量：用于视觉语言导航的多智能体概率对齐
authors: "Swagat Padhan, Lakshya Jain, Bhavya Minesh Shah, Omkar Patil, Thao Nguyen, Nakul Gopalan"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19166v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉语言导航中的智能体定位
tldr: 针对机器人视觉语言导航中视觉语言模型（VLM）难以处理复杂度量约束的问题，本文提出了MAPG框架。该框架通过多智能体协作将语言查询分解为结构化子组件，利用VLM进行语义定位，并结合概率组合生成符合度量一致性的3D空间决策。实验证明MAPG在HM-EQA及新提出的MAPG-Bench基准上表现优异，并成功应用于真实机器人。
selection_source: carryover_cache
motivation: 现有的视觉语言模型在处理包含精确度量约束（如“冰箱右侧两米”）的复杂语义查询时表现不佳。
method: 提出一种多智能体概率定位框架，将查询分解后利用VLM定位，再通过概率组合实现度量一致的决策。
result: 在HM-EQA和新基准MAPG-Bench上均取得显著性能提升，并完成了真实场景下的机器人演示。
conclusion: MAPG有效地结合了VLM的语义能力与概率推理的度量精度，提升了机器人在复杂物理环境中的语言理解与执行能力。
---

## 摘要
与人类协作的机器人必须将自然语言目标转化为可执行的、具有物理关联的决策。例如，执行诸如“走到冰箱右侧两米处”之类的指令，需要对 3D 场景中的语义引用、空间关系和度量约束进行关联。虽然近期的视觉语言模型 (VLM) 展示了强大的语义对齐能力，但它们并非专门为在物理定义的空间中推理度量约束而设计的。在这项工作中，我们通过实验证明，最先进的基于 VLM 的对齐方法在处理复杂的度量-语义语言查询时面临挑战。为了解决这一局限性，我们提出了 MAPG（多智能体概率对齐），这是一个智能体框架，它将语言查询分解为结构化的子组件，并调用 VLM 对每个组件进行对齐。随后，MAPG 以概率方式组合这些对齐输出，从而在 3D 空间中生成度量一致且可执行的决策。我们在 HM-EQA 基准测试上评估了 MAPG，并展示了其相对于强基准模型的持续性能提升。此外，我们引入了一个新的基准测试 MAPG-Bench，专门用于评估度量-语义目标对齐，填补了现有语言对齐评估中的空白。我们还展示了一个真实世界的机器人演示，表明当结构化场景表示可用时，MAPG 可以从仿真环境迁移到现实世界。

## Abstract
Robots collaborating with humans must convert natural language goals into actionable, physically grounded decisions. For example, executing a command such as "go two meters to the right of the fridge" requires grounding semantic references, spatial relations, and metric constraints within a 3D scene. While recent vision language models (VLMs) demonstrate strong semantic grounding capabilities, they are not explicitly designed to reason about metric constraints in physically defined spaces. In this work, we empirically demonstrate that state-of-the-art VLM-based grounding approaches struggle with complex metric-semantic language queries. To address this limitation, we propose MAPG (Multi-Agent Probabilistic Grounding), an agentic framework that decomposes language queries into structured subcomponents and queries a VLM to ground each component. MAPG then probabilistically composes these grounded outputs to produce metrically consistent, actionable decisions in 3D space. We evaluate MAPG on the HM-EQA benchmark and show consistent performance improvements over strong baselines. Furthermore, we introduce a new benchmark, MAPG-Bench, specifically designed to evaluate metric-semantic goal grounding, addressing a gap in existing language grounding evaluations. We also present a real-world robot demonstration showing that MAPG transfers beyond simulation when a structured scene representation is available.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MAPG (Multi-Agent Probabilistic Grounding)** 的框架，旨在解决机器人视觉语言导航（VLN）中精确度量与语义理解结合的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉语言模型（VLM）虽然在语义对齐（如识别“冰箱”）方面表现出色，但在处理涉及**精确度量约束**（如“冰箱右侧两米处”）的物理空间推理时非常脆弱。
*   **研究动机**：人类指令通常混合了语义（对象属性）、空间关系（左右前后）和度量（距离、时间、比例）。目前的具身智能系统往往将目标定位视为单步决策或离散动作预测，缺乏对连续 3D 空间中几何一致性的显式建模，导致导航误差随步数累积。

### 2. 方法论：MAPG 框架
MAPG 采用多智能体协作模式，将复杂的语言查询分解并转化为概率分布：
*   **编排器 (Orchestrator)**：利用 LLM 将自然语言指令解析为结构化的“空间描述子句 (SDCs)”，提取出**锚点对象**（如冰箱）、**空间谓词**（如右侧）和**度量约束**（如 2 米）。
*   **定位智能体 (Grounding Agent)**：在 3D 语义场景图（Scene Graph）中检索匹配的物体实例，结合 CLIP 相似度和空间显著性更新信念状态。
*   **空间智能体 (Spatial Agent)**：为每个约束生成连续的概率密度函数 (PDF)：
    *   **方向内核**：使用 von Mises-Fisher 分布建模方向（如“右侧”）。
    *   **度量内核**：使用径向高斯分布建模距离（如“2 米”）。
*   **概率组合**：通过“专家乘积 (Product of Experts)”机制，将多个内核在对数空间叠加并归一化，生成最终的目标位置分布图。
*   **规划接口**：规划器从高概率区域采样航点，通过 RRT* 等算法生成可执行路径。

### 3. 实验设计
*   **数据集与场景**：
    *   **HM-EQA (GraphEQA)**：标准的具身问答基准，侧重于语义推理。
    *   **MAPG-Bench (新提出)**：基于 30 个 HM3D 室内场景，包含 100 个经过人工标注的度量-语义查询，专门评估 3D 空间中的精确坐标定位。
*   **对比方法**：
    *   **GraphEQA**：基于语义场景图的强基准。
    *   **SRGPT**：专门针对空间推理优化的 VLM。
    *   **通用 VLM 基准**：包括 GPT-4o、Gemini 1.5 Pro、Claude 3.5 Opus 等。
*   **评估指标**：目标定位距离误差（O-O/O-W Error）、航向角/俯仰角误差、任务成功率（TSR）及轨迹效率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **模型调用**：实验主要依赖于闭源商业模型（如 GPT-4o, Gemini 2.5 Pro, Claude 4.6 Opus）的 API 调用，以及 Habitat-Sim 仿真环境进行渲染和物理模拟。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 30 个复杂的室内场景和 100 组精细标注的查询，实验组设置包括了对象对对象（O-O）和对象对世界（O-W）的定位。
*   **消融实验**：进行了多项关键消融，包括“显式空间推理器 vs. 纯思维链（CoT）提示词”以及“遮挡环境下的鲁棒性测试”。
*   **充分性评价**：实验设计较为全面，通过对比不同底座模型（GPT/Gemini/Claude）证明了框架的通用性。引入新基准 MAPG-Bench 弥补了现有数据集在度量精度评估上的不足，实验结果具有客观性。

### 6. 主要结论与发现
*   **精度大幅提升**：MAPG 将对象对世界的定位误差从基准模型的 **5.82 米降低至 0.07 米**（下降 98.8%），航向角误差从 13.5° 降至 1.9°。
*   **结构化优于规模化**：消融实验显示，单纯依靠更强的 VLM 或复杂的 CoT 提示词无法替代显式的概率几何建模。
*   **现实迁移性**：通过 Robotis AI Worker 机器人的真实演示，证明了只要具备结构化场景表示，MAPG 即可从仿真环境无缝迁移至现实世界。

### 7. 优点与亮点
*   **模块化与可解释性**：将黑盒 VLM 推理转化为可解释的概率内核组合，便于调试和理解机器人的决策逻辑。
*   **规划器友好**：输出的是连续的 PDF 而非单一坐标点，允许规划器根据环境可行性（如避障）灵活选择最优航点。
*   **多约束处理**：能够自然地处理复合指令（如“在 A 的左边且在 B 的附近”）。

### 8. 不足与局限
*   **依赖场景图质量**：如果场景图构建不完整（如物体被遮挡未被记录），系统将无法定位锚点。
*   **参考系歧义**：对于“在...前面”这类指令，若物体本身缺乏明确的朝向定义（如圆桌），系统仍会产生歧义。
*   **地图对齐风险**：自由空间地图与语义场景图之间的微小错位可能导致生成的航点位于不可达区域。

（完）
