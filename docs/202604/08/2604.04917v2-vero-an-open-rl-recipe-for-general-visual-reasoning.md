---
title: "Vero: An Open RL Recipe for General Visual Reasoning"
title_zh: Vero：通用视觉推理的开源强化学习方案
authors: "Gabriel Sarch, Linrong Cai, Qunzhong Wang, Haoyang Wu, Danqi Chen, Zhuang Liu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04917v2"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉语言模型通用视觉推理的强化学习方案
tldr: Vero 是一套开源的视觉推理强化学习（RL）方案，旨在打破闭源模型的垄断。研究团队构建了包含 60 万样本的 Vero-600K 数据集，涵盖图表、科学、空间理解等六大领域，并设计了任务路由奖励机制。实验表明，Vero 在 30 个基准测试中显著提升了基础模型性能，甚至超越了部分闭源思维模型，证明了广泛的数据覆盖是实现通用视觉推理 RL 扩展的关键。
selection_source: fresh_fetch
motivation: 现有的高性能视觉语言模型强化学习方案多为闭源，缺乏公开的通用视觉推理训练数据和方法。
method: 构建了包含 60 万样本的 Vero-600K 数据集，并设计了针对异构答案格式的任务路由奖励机制进行大规模 RL 训练。
result: Vero 在 VeroEval 的 30 个基准测试中平均提升基础模型 3.6-5.3 分，在多数任务上优于 Qwen3-VL-8B-Thinking。
conclusion: 广泛的任务覆盖是视觉推理 RL 扩展的核心驱动力，不同类别的推理模式具有独特性且难以孤立迁移。
---

## 摘要
构建一个能够处理图表、科学、空间理解和开放式任务的视觉推理器需要什么？最强大的视觉语言模型（VLM）表明，这种广泛的视觉推理已触手可及，但其背后的方案仍不明确，被锁定在带有非公开数据的专有强化学习（RL）流水线中。我们推出了 Vero，这是一个完全开源的 VLM 系列，在多种视觉推理任务中达到或超过了现有的开源权重模型。我们在六个广泛的任务类别中扩展了 RL 数据和奖励，构建了包含来自 59 个数据集的 60 万个样本的数据集 Vero-600K，并设计了能够处理异构答案格式的任务路由奖励。Vero 实现了最先进的性能，在我们的 30 个具有挑战性的基准测试集 VeroEval 上，相比四个基础模型平均提升了 3.6-5.3 个百分点。以 Qwen3-VL-8B-Instruct 为起点，Vero 在没有额外专有思考数据的情况下，在 30 个基准测试中的 23 个上优于 Qwen3-VL-8B-Thinking。当从相同的基础模型进行训练时，Vero-600K 在各个任务类别上均超过了现有的 RL 数据集。系统性的消融实验表明，不同的任务类别会诱发出性质截然不同的推理模式，这些模式在孤立情况下迁移效果较差，这表明广泛的数据覆盖是实现强大 RL 扩展的主要驱动力。所有数据、代码和模型均已发布。

## Abstract
What does it take to build a visual reasoner that works across charts, science, spatial understanding, and open-ended tasks? The strongest vision-language models (VLMs) show such broad visual reasoning is within reach, but the recipe behind them remains unclear, locked behind proprietary reinforcement learning (RL) pipelines with non-public data. We introduce Vero, a family of fully open VLMs that matches or exceeds existing open-weight models across diverse visual reasoning tasks. We scale RL data and rewards across six broad task categories, constructing Vero-600K, a 600K-sample dataset from 59 datasets, and designing task-routed rewards that handle heterogeneous answer formats. Vero achieves state-of-the-art performance, improving over four base models by 3.6-5.3 points on average across VeroEval, our suite of 30 challenging benchmarks. Starting from Qwen3-VL-8B-Instruct, Vero outperforms Qwen3-VL-8B-Thinking on 23 of 30 benchmarks without additional proprietary thinking data. When trained from the same base model, Vero-600K exceeds existing RL datasets across task categories. Systematic ablations reveal that different task categories elicit qualitatively distinct reasoning patterns that transfer poorly in isolation, suggesting that broad data coverage is the primary driver of strong RL scaling. All data, code, and models are released.

---

## 论文详细总结（自动生成）

这是一份关于论文《Vero: An Open RL Recipe for General Visual Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 GPT-5、Qwen3-VL 等前沿视觉语言模型（VLM）在复杂推理上表现卓越，但其背后的强化学习（RL）方案（包括训练数据、奖励设计和代码）均处于闭源状态。现有的开源 RL 尝试通常局限于数学等狭窄领域，缺乏通用的视觉推理方案。
*   **核心问题**：如何构建一个在图表分析、科学推理、空间理解及开放式任务中均具备强大能力的通用视觉推理器？
*   **整体含义**：Vero 旨在提供一套完全开源的 RL 训练“配方”，证明通过大规模多样化数据和精心设计的奖励机制，开源模型也能达到或超越闭源模型的推理水平。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用单阶段 RL 流程，通过**广泛的任务覆盖**和**任务路由奖励（Task-routed Rewards）**来驱动通用视觉推理能力的涌现。
*   **关键技术细节**：
    *   **Vero-600K 数据集**：从 59 个原始数据集中筛选出 60 万个样本，划分为六大类别：STEM、空间与动作、知识与识别、图表与 OCR、定位/计数/搜索、描述与指令遵循。
    *   **数据过滤流水线**：包含启发式筛选、人工质量控制、基于 LLM（Qwen3-235B）的问题歧义过滤，以及答案格式标准化。
    *   **RL 算法**：使用 **GSPO（Group Sequence Policy Optimization）**，这是 GRPO 的一种变体，通过序列级重要性采样比率来提高训练稳定性。
    *   **任务路由奖励设计**：针对不同任务设计了 10 种奖励函数，包括数值解析（math-verify）、IoU/F1（定位任务）、JSON 匹配（Web 动作）以及针对开放式任务的 **LLM-as-judge**。
    *   **格式约束**：强制模型生成 `<think>...</think><answer>...</answer>` 结构，并对思维链（CoT）内容和最终答案的 LaTeX 框选（\boxed）进行奖励。

### 3. 实验设计
*   **数据集与场景**：训练集为 Vero-600K；评估集为自建的 **VeroEval**，包含 30 个具有挑战性的基准测试（如 ChartQA-Pro、MathVision、ScreenSpotPro 等）。
*   **对比方法**：
    *   **基础模型**：Qwen2.5-VL-7B、Qwen3-VL-8B-Instruct、MiMo-VL-7B-SFT。
    *   **推理模型**：Qwen3-VL-8B-Thinking（闭源 RL 训练）、MiMo-VL-7B-RL。
    *   **开源 RL 基准**：LLaVA-OV-1.5-RL、VL-Rethinker、OpenMMReasoner。
    *   **前沿模型**：GPT-5 Nano。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 张 NVIDIA H100 或 H200 GPU** 进行训练。
*   **训练参数**：训练步数为 2,000 步，全局 Batch Size 为 256，每个 Prompt 生成 8 个 Rollout。
*   **上下文长度**：根据基础模型不同，支持 24k 到 36k 的上下文长度。
*   **训练时长**：文中未明确给出总小时数，但提到在 8 卡集群上完成了 60 万样本的单轮迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个不同的基础模型上验证了方案的通用性。
    *   进行了**跨任务迁移实验**（Cross-task Transfer），分析了单一领域训练对其他领域的影响。
    *   进行了**消融实验**，涵盖数据过滤策略、任务混合比例（Uniform vs. Weighted）、奖励函数设计以及 RL 算法对比（DAPO vs. GRPO vs. GSPO）。
*   **充分性与公平性**：实验设计非常系统且客观。通过在相同基础模型上对比不同 RL 数据集（如对比 ViRL、OpenMMReasoner），确保了评估的公平性。

### 6. 主要结论与发现
*   **数据多样性是核心**：广泛的任务覆盖是 RL 扩展的主要驱动力。不同任务诱发的推理模式（如 STEM 的回溯行为 vs. 定位任务的直接搜索）差异巨大，难以互相替代。
*   **负迁移风险**：在单一领域（如数学）进行 RL 训练会导致模型在其他领域（如开放式对话）性能大幅下降，而多样化混合训练能消除这种负迁移。
*   **均匀混合最优**：在任务类别权重上，简单的均匀采样（Uniform Mixture）优于基于准确率或推理长度的复杂加权方案。
*   **超越闭源**：Vero-Qwen3I-8B 在 30 个基准测试中的 23 个上超过了官方的 Qwen3-VL-Thinking，证明了开源 RL 配方的有效性。

### 7. 优点（亮点）
*   **完全开源**：不仅发布了模型权重，还公开了完整的 600K 训练数据、过滤代码和 RL 训练流水线，极具学术贡献。
*   **任务路由奖励**：解决了 VLM 在 RL 训练中处理异构答案格式（坐标、数值、文本、JSON）的难题。
*   **深入的行为分析**：通过认知行为框架和技能提取技术，量化分析了模型在不同任务下的推理逻辑差异，而非仅仅关注分数。

### 8. 不足与局限
*   **模态限制**：目前仅关注静态图像推理，未包含视频数据或多轮对话任务。
*   **模型规模**：主要在 7B-9B 参数规模的模型上进行验证，尚未探讨该配方在超大规模模型（如 70B+）上的表现。
*   **行为分析的描述性**：对推理行为的分析主要是描述性的，尚未完全揭示这些行为与最终准确率之间的因果机制。
*   **奖励黑盒风险**：虽然使用了开源 LLM 作为 Judge，但 LLM 评分本身仍存在偏见或被“奖励破解”（Reward Hacking）的风险（尽管文中设计了惩罚机制）。

（完）
