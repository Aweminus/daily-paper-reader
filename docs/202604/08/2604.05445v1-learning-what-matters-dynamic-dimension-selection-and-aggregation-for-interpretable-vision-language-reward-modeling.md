---
title: "Learning What Matters: Dynamic Dimension Selection and Aggregation for Interpretable Vision-Language Reward Modeling"
title_zh: 学习关键要素：用于可解释视觉-语言奖励建模的动态维度选择与聚合
authors: "Qiyuan Chen, Hongsen Huang, Jiahe Chen, Qian Shao, Jintai Chen, Hongxia Xu, Renjie Hua, Chuan Ren, Jian Wu"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05445v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于强化学习的可解释视觉语言奖励建模
tldr: 针对视觉语言奖励模型在可解释性与效率之间的权衡难题，本文提出VL-MDR框架。该框架通过视觉感知门控机制，将评估动态分解为21个细粒度维度（如幻觉、推理等）并自适应加权，兼顾了判别式模型的高效与生成式模型的可解释性。基于构建的32.1万对多维度偏好数据，VL-MDR在VL-RewardBench等基准上表现优异，并能有效通过DPO对齐提升模型可靠性，减少视觉幻觉。
selection_source: fresh_fetch
motivation: 旨在解决现有视觉语言奖励模型在生成式（可解释但慢）与判别式（高效但黑盒）之间的矛盾。
method: 提出VL-MDR框架，利用视觉感知门控机制动态选择并聚合21个细粒度维度的评分以实现可解释的奖励建模。
result: 在VL-RewardBench上超越了现有开源奖励模型，并通过DPO对齐显著降低了视觉语言模型的幻觉现象。
conclusion: VL-MDR为视觉语言模型提供了一种兼具高效性、可解释性与可扩展性的对齐解决方案。
---

## 摘要
视觉-语言奖励建模面临一个困境：生成式方法具有可解释性但速度较慢，而判别式方法虽然高效，却表现为不透明的“黑盒”。为了弥补这一差距，我们提出了 VL-MDR（Vision-Language Multi-Dimensional Reward），这是一个将评估动态分解为细粒度、可解释维度的框架。VL-MDR 不再输出单一的标量，而是采用一种视觉感知的门控机制来识别相关维度，并针对每个特定输入自适应地为这些维度（例如幻觉、推理）分配权重。为了支持这一研究，我们构建了一个包含 32.1 万个视觉-语言偏好对的数据集，并在 21 个细粒度维度上进行了标注。广泛的实验表明，VL-MDR 在 VL-RewardBench 等基准测试中始终优于现有的开源奖励模型。此外，我们证明了由 VL-MDR 构建的偏好对能有效实现 DPO 对齐，从而减轻视觉幻觉并提高可靠性，为视觉语言模型（VLM）对齐提供了一种可扩展的解决方案。

## Abstract
Vision-language reward modeling faces a dilemma: generative approaches are interpretable but slow, while discriminative ones are efficient but act as opaque "black boxes." To bridge this gap, we propose VL-MDR (Vision-Language Multi-Dimensional Reward), a framework that dynamically decomposes evaluation into granular, interpretable dimensions. Instead of outputting a monolithic scalar, VL-MDR employs a visual-aware gating mechanism to identify relevant dimensions and adaptively weight them (e.g., Hallucination, Reasoning) for each specific input. To support this, we curate a dataset of 321k vision-language preference pairs annotated across 21 fine-grained dimensions. Extensive experiments show that VL-MDR consistently outperforms existing open-source reward models on benchmarks like VL-RewardBench. Furthermore, we show that VL-MDR-constructed preference pairs effectively enable DPO alignment to mitigate visual hallucinations and improve reliability, providing a scalable solution for VLM alignment.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning What Matters: Dynamic Dimension Selection and Aggregation for Interpretable Vision-Language Reward Modeling》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：在视觉-语言模型（LVLM）的对齐过程中，奖励模型（RM）至关重要。现有的奖励模型分为两类：**生成式 RM**（如 LLaVA-Critic）虽具有可解释性但推理速度慢且存在随机性偏见；**判别式 RM**（如 Skywork-VL）虽然高效，但输出的是单一标量，属于不可解释的“黑盒”，无法区分模型是感知错误还是推理错误。
*   **核心目标**：开发一种既具备判别式模型的高效率，又具备生成式模型的可解释性和细粒度反馈能力的视觉-语言奖励模型。

### 2. 方法论
*   **核心思想**：提出 **VL-MDR** 框架，将评估过程解耦为“确定评价标准”和“执行质量评分”两个阶段。
*   **关键技术细节**：
    1.  **查询-响应解耦架构**：从 Backbone 的最后隐藏层提取两个表示：指令表示 $h_q$（捕捉用户意图）和响应表示 $h_r$（捕捉生成内容）。
    2.  **视觉感知维度预测（Gating）**：利用 $h_q$ 预测 21 个预定义维度（如物体计数、逻辑推理、安全性等）的相关性概率，并选择 Top-$k$（实验中 $k=3$）作为当前任务的激活维度。
    3.  **细粒度评分与自适应聚合**：
        *   **评分头**：对所有维度进行独立评分。
        *   **权重头**：根据指令动态计算各维度的重要性权重。
        *   **掩码聚合**：通过掩码机制剔除无关维度，将激活维度的评分加权求和，得到最终的标量奖励值。
*   **损失函数**：采用多任务学习，结合了维度预测损失（BCE）、细粒度排序损失（Pairwise Ranking）和整体奖励损失。

### 3. 实验设计
*   **数据集构建**：
    *   整合了 7 个主流 VLM 偏好数据集（如 VLFeedback, RLAIF-V, WildVision 等），共 41.4 万个样本。
    *   利用 Qwen3-VL-235B、GLM-4.5V 和 InternVL3-78B 进行多模型一致性过滤，最终保留 **32.1 万个高质量偏好对**，并标注了 21 个细粒度维度。
*   **Benchmark**：
    *   **VL-RewardBench**：评估通用、幻觉和推理能力。
    *   **Multimodal RewardBench**：全面评估知识、推理、安全等。
    *   **MM-RLHF-Reward Bench**：侧重于严格排序和复杂任务。
*   **对比方法**：包括 GPT-4o 等闭源模型，以及 LLaVA-Critic（生成式）、Skywork-VL-Reward（判别式）等开源模型。

### 4. 资源与算力
*   **模型底座**：Qwen2.5-VL-7B-Instruct。
*   **算力投入**：
    *   **训练**：使用 **8 台 NVIDIA H100 GPU**。
    *   **参数量**：奖励头仅增加 17.86M 参数（约占总参数量的 0.25%）。
    *   **推理效率**：在 NVIDIA H20 GPU 上测试，VL-MDR 的推理耗时与传统判别式模型相当（约 0.218 GPU 小时完成全量测试），远低于生成式模型（约 0.4-0.5 GPU 小时）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流奖励模型基准上进行了全面对比，并在 4 个下游任务基准上验证了 DPO 对齐效果。
*   **消融实验**：针对监督粒度（标量 vs 7维 vs 21维）、门控机制的有无、以及 Top-$k$ 的取值进行了详细测试。
*   **充分性评价**：实验设计较为充分，涵盖了性能对比、效率分析、可解释性验证及下游应用，对比基线包含了目前最强的开源和闭源模型，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：VL-MDR 在多个基准测试中均优于现有的开源 7B 级别奖励模型，甚至在某些维度上接近闭源模型。
*   **细粒度监督的价值**：显式的维度监督比单一标量监督能提供更强的信号，有助于模型学习更复杂的评估逻辑。
*   **动态门控的有效性**：通过 Top-$k$ 选择相关维度可以有效过滤无关噪声，在 $k=3$ 时性能达到最优。
*   **下游增益**：使用 VL-MDR 标注的数据进行 DPO 对齐，能显著降低 LLaVA-OneVision 等模型的视觉幻觉，提升可靠性。

### 7. 优点
*   **兼顾效率与透明度**：在保持判别式模型单次前向传播速度的同时，提供了类似生成式模型的维度级解释。
*   **高质量数据贡献**：构建并开源了大规模、细粒度标注的视觉-语言偏好数据集，对社区具有重要价值。
*   **架构优雅**：通过查询-响应解耦和掩码聚合机制，实现了对复杂多模态任务的结构化评估。

### 8. 不足与局限
*   **标注源偏见**：数据标注依赖于现有的强模型（如 Qwen3-VL），可能继承了这些模型的固有偏见或局限性。
*   **维度覆盖面**：虽然定义了 21 个维度，但仍可能无法完全覆盖所有特定领域（如极专业的医疗或法律视觉分析）的微小细节。
*   **模态局限**：目前主要集中在图像-文本任务，尚未扩展到视频或音频等多模态奖励建模。

（完）
