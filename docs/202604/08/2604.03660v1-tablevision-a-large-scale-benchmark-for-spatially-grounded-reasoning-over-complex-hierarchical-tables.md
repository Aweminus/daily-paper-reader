---
title: "TableVision: A Large-Scale Benchmark for Spatially Grounded Reasoning over Complex Hierarchical Tables"
title_zh: TableVision：针对复杂层级表格空间定位推理的大规模基准测试
authors: "Xiaoyu Chen, Lu Dai, Hanqing Wang, Zhuoyu Li, Wenbin Dai, Yanzong Zheng, Zhenggang Xia, Junyong Lin, Hui Xiong"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03660v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 复杂分层表格的空间定位推理
tldr: "针对多模态大模型在处理复杂层级表格时面临的“感知瓶颈”，本文提出了TableVision基准。该基准包含6,799条具有像素级空间标注的推理轨迹，涵盖感知、推理和分析三个认知层面。研究发现，通过显式的空间约束可以显著缓解感知过载，提升模型在复杂表格上的逻辑推理能力，并提出了一个两阶段解耦框架，将准确率提升了12.3%，为文档理解提供了新视角。"
selection_source: fresh_fetch
motivation: 多模态大模型在处理复杂层级表格时存在感知瓶颈，难以在隐式生成过程中保持准确的空间注意力。
method: 构建了TableVision基准，通过渲染技术生成包含像素级空间标注的推理轨迹，并将任务划分为感知、推理和分析三个层级。
result: "实验证明显式空间约束能显著提升模型性能，提出的两阶段解耦框架使整体准确率提高了12.3%。"
conclusion: 该研究强调了空间定位在复杂表格推理中的关键作用，并为解决多模态模型的感知过载问题提供了有效的评估工具和方法。
---

## 摘要
结构化表格在金融、医疗和科学研究等专业领域中对于传递高密度信息至关重要。尽管多模态大语言模型（MLLMs）取得了进展，但在处理具有层级布局的复杂表格时，其推理性能仍然受限。在本文中，我们通过定量分析发现了一个关键的“感知瓶颈”。我们发现，随着任务复杂度的增加，所涉及的离散视觉区域数量呈不成比例的增长。这种处理密度导致了内部的“感知过载”，使得 MLLMs 在隐式生成过程中难以维持准确的空间注意力。为了解决这一瓶颈，我们推出了 TableVision，这是一个专为空间定位推理设计的大规模、轨迹感知基准测试。TableVision 将表格任务划分为三个认知层面（感知、推理和分析），涵盖 13 个子类别。通过利用基于渲染的确定性定位流水线，该数据集将多步逻辑推导与像素级精确的空间真值显式耦合，包含 6,799 条高保真推理轨迹。在诊断性探测的支持下，我们的实证结果表明，显式的空间约束能显著恢复 MLLMs 的推理潜力。此外，我们的两阶段解耦框架在测试集上实现了 12.3% 的稳健整体准确率提升。TableVision 为文档理解中感知与逻辑之间的协同作用提供了一个严谨的测试平台和全新的视角。

## Abstract
Structured tables are essential for conveying high-density information in professional domains such as finance, healthcare, and scientific research. Despite the progress in Multimodal Large Language Models (MLLMs), reasoning performance remains limited for complex tables with hierarchical layouts. In this paper, we identify a critical Perception Bottleneck through quantitative analysis. We find that as task complexity scales, the number of involved discrete visual regions increases disproportionately. This processing density leads to an internal "Perceptual Overload," where MLLMs struggle to maintain accurate spatial attention during implicit generation. To address this bottleneck, we introduce TableVision, a large-scale, trajectory-aware benchmark designed for spatially grounded reasoning. TableVision stratifies tabular tasks into three cognitive levels (Perception, Reasoning, and Analysis) across 13 sub-categories. By utilizing a rendering-based deterministic grounding pipeline, the dataset explicitly couples multi-step logical deductions with pixel-perfect spatial ground truths, comprising 6,799 high-fidelity reasoning trajectories. Our empirical results, supported by diagnostic probing, demonstrate that explicit spatial constraints significantly recover the reasoning potential of MLLMs. Furthermore, our two-stage decoupled framework achieves a robust 12.3% overall accuracy improvement on the test set. TableVision provides a rigorous testbed and a fresh perspective on the synergy between perception and logic in document understanding.

---

## 论文详细总结（自动生成）

这是一份关于论文《TableVision: A Large-Scale Benchmark for Spatially Grounded Reasoning over Complex Hierarchical Tables》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于多模态大语言模型（MLLMs）在理解**复杂层级表格**（Hierarchical Tables）时的局限性。
*   **研究动机**：传统的表格处理依赖 OCR 将表格序列化为文本（如 Markdown），这破坏了表格的 2D 拓扑结构。虽然 MLLMs 能够直接处理图像，但在面对高密度、多层级表头的复杂表格时，性能显著下降。
*   **核心发现**：作者通过定量分析指出，限制模型能力的并非逻辑推理深度，而是**“感知瓶颈”**（Perception Bottleneck）。随着任务复杂度提升，模型需要关注的离散视觉区域（单元格、表头）数量呈指数级增长，导致模型在隐式生成过程中产生“感知过载”，无法准确维持空间注意力。

### 2. 论文提出的方法论
为了解决感知瓶颈，论文提出了 **TableVision** 基准测试及配套的解耦推理框架。
*   **TableVision 数据集**：包含 6,799 条高保真推理轨迹，将任务分为三个认知层级：
    *   **L1 感知**（如检索、列表、结构识别）；
    *   **L2 推理**（如比较、算术、排序、计数）；
    *   **L3 分析**（如复合算术、跨层级聚合、多跳推理）。
*   **确定性构建流水线**：采用基于渲染的方法，从 JSON 结构化数据直接生成图像，从而获得像素级精确的单元格坐标（Bounding Box），避免了使用 LLM 或 OCR 标注带来的坐标幻觉。
*   **两阶段解耦框架（Locate-then-Reason）**：
    *   **阶段 1（视觉定位）**：模型首先预测与问题相关的行表头、列表头和单元格的坐标及语义标签。
    *   **阶段 2（接地推理）**：将阶段 1 预测的坐标作为显式空间锚点输入模型，引导模型进行多步逻辑计算（CoT）并得出最终答案。

### 3. 实验设计
*   **数据集/场景**：使用 TableVision 测试集（1,301 个查询），涵盖 13 个细分任务类别。
*   **Benchmark 对象**：评估了 10 个主流 MLLM（参数量 4B-9B），包括 MiniCPM-V-2.6、InternVL3-8B、Qwen3-VL-8B-Instruct、Glyph 等。
*   **对比实验**：
    *   **Zero-shot**：直接预测答案。
    *   **Oracle Probing**：提供真值坐标（GT Bbox）以测试模型的逻辑上限。
    *   **End-to-End SFT**：直接微调模型生成 CoT 和答案。
    *   **Decoupled SFT（本文方法）**：先定位再推理。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 张 NVIDIA A100 (40GB) GPU**。
*   **训练细节**：基于 LLaMA-Factory 框架，使用 LoRA 策略微调 Qwen3-VL-8B-Instruct。
*   **参数设置**：LoRA rank=32, alpha=64；训练 2 个 epoch；使用 AdamW 优化器，学习率 1e-5，采用余弦衰减调度。

### 5. 实验数量与充分性
*   **实验规模**：对 10 个不同架构的模型进行了全面的零样本评估，并针对本文提出的框架进行了消融实验和 Oracle 探测实验。
*   **充分性与公平性**：
    *   实验涵盖了从感知到复杂分析的多个难度梯度，能够有效区分模型的感知错误与逻辑错误。
    *   通过 Oracle 实验（提供真值坐标后准确率从 59.3% 飙升至 80.0%）有力地证明了“感知瓶颈”的存在。
    *   对比模型均处于相近的参数量级（4B-9B），保证了对比的公平性。

### 6. 论文的主要结论与发现
*   **感知是核心瓶颈**：在复杂分析任务（L3）中，模型失败的主要原因是无法在密集的表格网格中精确定位信息，而非逻辑计算能力不足。
*   **显式空间约束的有效性**：通过引入显式的坐标定位步骤，两阶段框架在测试集上实现了 **12.3% 的整体准确率提升**。
*   **任务复杂度的影响**：随着所需空间锚点数量的增加（如 L3 任务平均需要 10.87 个 Bbox），端到端模型的性能急剧下降，而解耦框架表现出更强的鲁棒性。

### 7. 优点：方法或实验设计上的亮点
*   **数据质量高**：通过渲染技术实现的“确定性定位”解决了多模态数据集中常见的坐标不准问题。
*   **认知分层科学**：将任务分为感知、推理、分析三个层级，为评估 MLLM 的细分能力提供了清晰的维度。
*   **解耦思想实用**：提出的“先定位再推理”框架不仅提升了性能，还增强了模型推理过程的可解释性（可以观察模型到底看错了哪里）。

### 8. 不足与局限
*   **误差累积风险**：两阶段框架高度依赖第一阶段的定位准确性。在极其复杂的 L3 任务中，如果定位出现偏差，会直接导致后续推理错误。
*   **L1 任务性能回退**：实验发现，在简单的感知任务（L1）中，强制执行复杂的定位和 CoT 步骤反而导致准确率轻微下降（相比 Zero-shot），这可能源于多任务微调中的任务干扰。
*   **泛化性验证受限**：由于现有其他表格数据集普遍缺乏精确的坐标标注，目前难以在其他数据集上进行直接的跨领域泛化评估。

（完）
