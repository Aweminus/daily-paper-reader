---
title: "Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation"
title_zh: Firebolt-VL：基于跨模态调制的高效视觉-语言理解
authors: "Quoc-Huy Trinh, Mustapha Abdullahi, Bo Zhao, Debesh Jha"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04579v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于细粒度推理任务的视觉语言模型
tldr: Firebolt-VL 是一款高效的多模态模型，旨在解决现有模型计算成本高且难以捕捉细粒度视觉信息的问题。它采用 Liquid Foundation Model 替代 Transformer 解码器，并引入 Token-Grid 相关性模块，通过线性复杂度的状态空间模型实现跨模态调制。该模型在保持推理效率的同时，显著提升了细粒度视觉理解能力。
selection_source: fresh_fetch
motivation: 现有的多模态模型因 Transformer 的二次复杂度导致计算开销巨大，且小模型在处理细粒度视觉推理任务时表现不佳。
method: 采用 Liquid Foundation Model (LFM) 作为解码器，并结合 Token-Grid 相关性模块与 FiLM 调节技术，实现高效的跨模态特征融合。
result: 实验表明 Firebolt-VL 在多个基准测试中实现了准确的细粒度理解，并显著提高了推理效率。
conclusion: Firebolt-VL 证明了通过线性时间复杂度的架构设计，可以在资源受限的环境下实现高性能的视觉语言理解。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在视觉-语言理解方面取得了显著进步，但其高昂的计算成本限制了在个人助手、文档理解和智能摄像头等资源受限场景中的部署。大多数现有方法依赖于基于 Transformer 的交叉注意力机制，其二次复杂度阻碍了效率。此外，小型视觉-语言模型通常难以精确捕捉细粒度的、与任务相关的视觉区域，导致在细粒度推理任务上的性能下降，限制了其在现实世界中的有效性。为了解决这些问题，我们推出了 Firebolt-VL，这是一种高效的视觉-语言模型，它将基于 Transformer 的解码器替换为液体基础模型（Liquid Foundation Model, LFM）解码器。为了进一步增强视觉定位，我们提出了一个 Token-Grid 相关性模块，该模块计算文本 token 与图像块之间的轻量级相关性，并通过带有 FiLM 条件的状态空间模型进行调制。这使得模型能够在保持线性时间推理的同时，选择性地强调与文本提示相关的视觉区域。在多个基准测试上的实验结果表明，Firebolt-VL 在显著提高效率的同时，实现了准确的细粒度理解。我们的模型和代码可在以下网址获取：https://fireboltvl.github.io

## Abstract
Recent advances in multimodal large language models (MLLMs) have enabled impressive progress in vision-language understanding, yet their high computational cost limits deployment in resource-constrained scenarios such as personal assistants, document understanding, and smart cameras. Most existing methods rely on Transformer-based cross-attention, whose quadratic complexity hinders efficiency. Moreover, small vision-language models often struggle to precisely capture fine-grained, task-relevant visual regions, leading to degraded performance on fine-grained reasoning tasks that limit their effectiveness in the real world. To address these issues, we introduce Firebolt-VL, an efficient vision-language model that replaces the Transformer-based decoder with a Liquid Foundation Model (LFM) decoder. To further enhance visual grounding, we propose a Token-Grid Correlation Module, which computes lightweight correlations between text tokens and image patches and modulates via the state-space model with FiLM conditioning. This enables the model to selectively emphasize visual regions relevant to the textual prompt while maintaining linear-time inference. Experimental results across multiple benchmarks demonstrate that Firebolt-VL achieves accurate, fine-grained understanding with significantly improved efficiency. Our model and code are available at: https://fireboltvl.github.io

---

## 论文详细总结（自动生成）

以下是对论文《Firebolt-VL: Efficient Vision-Language Understanding with Cross-Modality Modulation》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLMs）虽然在视觉理解上取得了巨大进步，但面临两大瓶颈：一是基于 Transformer 的架构具有二次计算复杂度，导致在资源受限设备上推理效率低下；二是小型模型往往难以精确捕捉与任务相关的细粒度视觉细节，导致在复杂推理任务中表现不佳。
*   **核心问题**：如何在保持极高推理效率（线性复杂度）的同时，增强模型对细粒度视觉区域的感知和定位能力？

### 2. 方法论
Firebolt-VL 引入了两个关键创新来替代传统的 Transformer 架构：
*   **Liquid Foundation Model (LFM) 解码器**：将传统的 Transformer 替换为基于“液体”假设的 LFM2 模型。LFM 具有线性时间复杂度，显著降低了处理长序列时的内存和计算开销。
*   **跨模态调制器 (Cross-Modal Modulator, CMM)**：
    *   **Token-Grid 相关性计算**：计算文本 Token 与图像网格（Grid）之间的相关性矩阵，通过 Softmax 归一化确定每个文本 Token 关注的视觉区域。
    *   **Top-k 采样**：仅保留相关性最高的 $k$ 个视觉块，以减少噪声并聚焦关键信息。
    *   **FiLM 调制**：利用特征线性调制（FiLM）技术，根据视觉上下文对文本特征进行缩放（Scale）和偏移（Shift），实现视觉引导的特征重塑。
    *   **状态空间模型 (SSM) 融合**：使用 S4 或 S4D 等结构化状态空间模型处理调制后的特征，以线性复杂度捕捉序列间的局部和全局依赖关系。

### 3. 实验设计
*   **数据集**：
    *   **第一阶段（预训练）**：使用 CC3M 数据集初始化 CMM 模块。
    *   **第二阶段（指令微调）**：使用 LLaVA-CoT 和处理后的 MMPR-v1.2 数据集进行端到端训练。
*   **Benchmark（基准测试）**：涵盖了 VQAv2（视觉问答）、POPE（物体幻觉）、AI2D（图表理解）、MMMU（多学科推理）、MME（综合感知）、SQA-Image（科学问答）和 MMB（综合能力）。
*   **对比方法**：
    *   **大型模型（>7B）**：IDEFICS, OpenFlamingo v2, MiniGPT-4, Chameleon, FastV。
    *   **小型模型（<3B）**：Kosmos-2, MobileVLM (v1/v2), MoE-LLaVA, LLaVA-Phi, SmolVLM2。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA H100 80GB GPU**。
*   **训练配置**：
    *   第一阶段：Batch Size 为 128，学习率 $5 \times 10^{-4}$。
    *   第二阶段：Batch Size 为 8，学习率 $1 \times 10^{-4}$。
    *   两个阶段各训练 5 个 Epoch，通常在第 2 个 Epoch 选出最佳模型。
*   **推理效率**：在 H100 上达到了 **46.67 tokens/s** 的吞吐量，优于同类轻量级模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个主流基准测试上进行了定量评估，并提供了丰富的定性对比案例。
*   **消融实验**：
    *   **SSM 变体对比**：对比了 Mamba、S4D 和 S4，发现结构化 SSM（S4/S4D）在处理空间网格关系时优于 Mamba。
    *   **融合策略对比**：对比了 Prepend（直接拼接）、Cross-attention（交叉注意力）和 CMM，证明了 CMM 在感知任务（MME）上的巨大优势。
    *   **Top-k 敏感度**：测试了 $k=1$ 到 $5$ 的影响，确定 $k=4$ 为最佳平衡点。
*   **充分性评价**：实验设计较为全面，不仅对比了同量级的小模型，还跨级对比了大模型，验证了其在效率和性能上的双重优势。

### 6. 主要结论与发现
*   **性能卓越**：Firebolt-VL（0.8B 参数）在 MME 感知评分上达到了 1376.2，超过了许多 7B 甚至 9B 的模型。
*   **效率领先**：得益于 LFM 和 SSM 的线性复杂度，Firebolt-VL 的吞吐量显著高于基于 Transformer 的 MobileVLM 系列和 SmolVLM2。
*   **细粒度能力**：定性分析显示，CMM 模块使模型能够精准定位图像中的微小文字（如搜索栏内容）或复杂图表关系。

### 7. 优点
*   **架构创新**：成功将 Liquid Foundation Model 和 SSM 应用于多模态领域，打破了 Transformer 的复杂度限制。
*   **高效融合**：CMM 模块通过轻量级的 FiLM 和 Top-k 机制，实现了比传统交叉注意力更高效且精准的跨模态对齐。
*   **落地潜力**：极小的参数量（0.8B）和高吞吐量使其非常适合部署在手机、智能摄像头等边缘侧设备。

### 8. 不足与局限
*   **物体幻觉风险**：在 POPE 基准测试中表现略逊于部分模型，作者推测这可能是因为 SSM 的隐式状态传播在抑制幻觉方面不如显式注意力机制有效。
*   **输入限制**：目前仅支持单图输入，尚未扩展到多图或视频序列。
*   **任务覆盖**：虽然在感知任务上表现强劲，但在某些需要极长链条推理的复杂科学任务（如 SQA-I）中，相比于 2B 级模型仍有提升空间。

（完）
