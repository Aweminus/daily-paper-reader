---
title: "$AutoDrive\\text{-}P^3$: Unified Chain of Perception-Prediction-Planning Thought via Reinforcement Fine-Tuning"
title_zh: AutoDrive-P3：通过强化微调实现的统一感知-预测-规划思维链
authors: "Yuqi Ye, Zijian Zhang, Junhong Lin, Shangkun Sun, Changhao Peng, Wei Gao"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28116v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过强化微调实现视觉语言模型的驾驶思维链推理
tldr: 针对现有自动驾驶VLM模型缺乏推理链或模块协同不足的问题，本文提出AutoDrive-P3框架。该框架通过构建P3-CoT数据集和P3-GRPO分层强化学习算法，实现了感知、预测、规划（P3）的统一思维链推理。模型支持详细与快速双思维模式，在nuScenes和NAVSIM等基准测试中达到了SOTA水平，显著提升了决策的安全性和可解释性。
selection_source: fresh_fetch
motivation: 旨在解决现有VLM在自动驾驶中因跳过感知预测阶段或模块间缺乏协同而导致的决策能力受损问题。
method: 提出统一的P3-CoT推理链，并利用P3-GRPO分层强化学习算法对感知、预测和规划任务进行渐进式监督微调。
result: 在nuScenes开环及NAVSIM闭环基准测试中均取得了最先进的规划性能，证明了该框架的有效性。
conclusion: AutoDrive-P3通过结构化推理和强化学习，成功实现了感知、预测与规划的深度协同，提升了自动驾驶的安全性与效率。
---

## 摘要
视觉语言模型 (VLM) 因其在处理长尾场景中的卓越表现，正越来越多地被应用于端到端自动驾驶系统。然而，目前基于 VLM 的方法存在两个主要局限性：1) 一些 VLM 在没有思维链 (CoT) 推理的情况下直接输出规划结果，绕过了关键的感知和预测阶段，这造成了显著的领域差距并损害了决策能力；2) 其他 VLM 虽然可以生成感知、预测和规划任务的输出，但采用了碎片化的决策方法，这些模块独立运行，导致严重缺乏协同作用，从而削弱了真实的规划性能。为了解决这些局限性，我们提出了 AutoDrive-P3，这是一个通过结构化推理无缝集成感知 (Perception)、预测 (Prediction) 和规划 (Planning) 的新颖框架。我们引入了 P3-CoT 数据集以促进连贯推理，并提出了 P3-GRPO，这是一种分层强化学习算法，可为所有三个任务提供渐进式监督。具体而言，AutoDrive-P3 逐步生成感知、预测和规划的思维链推理及答案，其中感知为后续的预测和规划提供关键信息，而感知和预测共同助力最终的规划决策，从而实现更安全、更具可解释性的自动驾驶。此外，为了平衡推理效率与性能，我们引入了双重思考模式：详细思考和快速思考。在开环 (nuScenes) 和闭环 (NAVSIMv1/v2) 基准测试上的广泛实验表明，我们的方法在规划任务中达到了最先进的性能。代码可在 https://github.com/haha-yuki-haha/AutoDrive-P3 获取。

## Abstract
Vision-language models (VLMs) are increasingly being adopted for end-to-end autonomous driving systems due to their exceptional performance in handling long-tail scenarios. However, current VLM-based approaches suffer from two major limitations: 1) Some VLMs directly output planning results without chain-of-thought (CoT) reasoning, bypassing crucial perception and prediction stages which creates a significant domain gap and compromises decision-making capability; 2) Other VLMs can generate outputs for perception, prediction, and planning tasks but employ a fragmented decision-making approach where these modules operate separately, leading to a significant lack of synergy that undermines true planning performance. To address these limitations, we propose ${AutoDrive\text{-}P^3}$, a novel framework that seamlessly integrates $\textbf{P}$erception, $\textbf{P}$rediction, and $\textbf{P}$lanning through structured reasoning. We introduce the ${P^3\text{-}CoT}$ dataset to facilitate coherent reasoning and propose ${P^3\text{-}GRPO}$, a hierarchical reinforcement learning algorithm that provides progressive supervision across all three tasks. Specifically, ${AutoDrive\text{-}P^3}$ progressively generates CoT reasoning and answers for perception, prediction, and planning, where perception provides essential information for subsequent prediction and planning, while both perception and prediction collectively contribute to the final planning decisions, enabling safer and more interpretable autonomous driving. Additionally, to balance inference efficiency with performance, we introduce dual thinking modes: detailed thinking and fast thinking. Extensive experiments on both open-loop (nuScenes) and closed-loop (NAVSIMv1/v2) benchmarks demonstrate that our approach achieves state-of-the-art performance in planning tasks. Code is available at https://github.com/haha-yuki-haha/AutoDrive-P3.

---

## 论文详细总结（自动生成）

### 论文总结：AutoDrive-P3 —— 统一感知-预测-规划思维链的强化微调框架

#### 1. 核心问题与研究动机
论文针对当前基于视觉语言模型（VLM）的自动驾驶系统存在的两大瓶颈：
*   **推理缺失（Planning-Only）**：部分模型直接输出规划轨迹，跳过了感知和预测的中间推理步骤，导致决策缺乏逻辑支撑且领域差距大。
*   **模块碎片化（Fragmented Decision-making）**：虽然有些模型能输出感知和预测结果，但各模块独立运行，缺乏协同效应，无法将感知/预测的准确性转化为规划的安全性。
**研究动机**：旨在通过构建一个统一的“感知-预测-规划”（P3）思维链（CoT），并利用强化学习进行全流程监督，实现更安全、可解释且协同的端到端驾驶决策。

#### 2. 方法论
核心思想是将自动驾驶任务建模为连续的结构化推理过程，并通过分层强化学习进行优化。
*   **P3-CoT 数据集**：基于 nuScenes 和 NAVSIM 构建，包含关键目标的 2D 框（感知）、未来动作/位置（预测）以及 ego 车辆轨迹（规划），并利用 Qwen2.5-VL-72B 生成连贯的推理文本。
*   **P3-GRPO 算法**：改进了 DeepSeek 提出的组相对策略优化（GRPO）算法。
    *   **分层奖励机制**：总奖励 $R = \lambda_{format}R_{format} + \lambda_{perc}R_{perc} + \lambda_{pred}R_{pred} + \lambda_{plan}R_{plan}$。
    *   **感知奖励**：基于 IoU、精确率和召回率评估关键目标检测。
    *   **预测奖励**：结合检测质量评估目标未来行为（如直行、停止）的准确性。
    *   **规划奖励**：通过 L2 距离和闭环指标（如 PDMS）评估轨迹质量。
*   **双思考模式**：
    *   **Detailed Thinking**：输出完整的推理链+答案，提升可解释性。
    *   **Fast Thinking**：仅输出各模块答案，通过 vLLM 加速实现近实时推理（约 1Hz）。

#### 3. 实验设计
*   **数据集与场景**：
    *   **nuScenes**：开环基准，评估 L2 位移误差和碰撞率。
    *   **NAVSIM (v1/v2)**：闭环/伪仿真基准，使用 PDMS 和 EPDMS 综合评分（包含碰撞、舒适度、交通规则依从性等）。
*   **对比方法**：
    *   非自回归模型：UniAD, VAD, ST-P3 等。
    *   自回归/VLM 模型：GPT-Driver, DriveVLM, OmniDrive, OpenDriveVLA, AutoVLA 等。

#### 4. 资源与算力
*   **硬件环境**：训练使用了 **8 张 NVIDIA A100 GPU**。
*   **推理加速**：在 **H100 GPU** 上使用 **vLLM 0.8.0** 框架进行推理测试。
*   **训练细节**：SFT 阶段训练 10 个 epoch；P3-GRPO 强化学习阶段在 nuScenes 上训练 5 个 epoch，在 NAVSIM 上训练 10 个 epoch。

#### 5. 实验数量与充分性
*   **实验规模**：在 nuScenes（约 2.5 万帧）和 NAVSIM（约 11.5 万帧）上进行了大规模验证。
*   **消融实验**：
    *   对比了“仅 SFT”、“SFT + 仅规划奖励 GRPO”与“SFT + P3-GRPO”的效果。
    *   探讨了组大小（Group Size）、历史轨迹输入、传感器模态（图像 vs 视频）以及 KL 散度约束对性能的影响。
*   **客观性**：实验涵盖了开环和闭环两种主流评估方式，对比了从传统端到端到最新 VLM 类的多种 SOTA 方法，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **协同效应显著**：P3-GRPO 算法通过同时监督感知和预测，显著提升了最终的规划性能。在 nuScenes 上，其碰撞率比现有 SOTA 方法降低了约 **40%**。
*   **强化学习优于 SFT**：单纯的监督微调（SFT）在复杂场景下表现有限，而 GRPO 能够通过探索多种推理路径进一步优化决策边界。
*   **模型规模效率**：使用较小的参数量（Qwen2.5-3B）和较少的数据量，在多个指标上超越了参数量更大（如 7B）或数据量更多（如 1000k 样本）的模型。

#### 7. 优点与亮点
*   **统一推理架构**：首次在 VLM 驾驶领域实现了感知、预测、规划三位一体的强化学习闭环。
*   **分层监督**：解决了以往 GRPO 仅针对规划结果进行奖励的“黑盒”问题，增强了中间过程的准确性。
*   **效率平衡**：提出的“快慢思考”切换模式为 VLM 在自动驾驶中的实时部署提供了可行路径。

#### 8. 不足与局限
*   **幻觉风险**：尽管有 CoT 引导，VLM 仍可能在推理过程中产生与视觉事实不符的“幻觉”。
*   **离线局限**：强化学习是在离线模拟器或静态数据集上进行的，缺乏与真实动态交通流的实时交互反馈。
*   **推理延迟**：即便有快速模式，1Hz 的频率对于高速驾驶场景（通常需要 10Hz+）仍显不足。

（完）
