---
title: "AgentV-RL: Scaling Reward Modeling with Agentic Verifier"
title_zh: AgentV-RL：利用智能体化验证器扩展奖励建模
authors: "Jiazheng Zhang, Ziche Fu, Zhiheng Xi, Wenqing Jing, Mingxu Chai, Wei He, Guoqiang Zhang, Chenghao Fan, Chenxin An, Wenxiang Chen, Zhicheng Liu, Haojie Pan, Dingwei Zhu, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16004v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 通过智能体验证器扩展强化学习推理的奖励建模
tldr: 针对LLM验证器在复杂推理中易受错误传播影响且缺乏外部知识约束的问题，本文提出Agentic Verifier框架。该框架将奖励建模转化为多轮、工具增强的审议过程，引入前向追踪与后向核查的双向Agent协作机制。通过强化学习（AgentV-RL），验证器能自主交替使用工具与推理，显著提升了评估的可靠性与可解释性，为Agent化奖励建模提供了新范式。
selection_source: fresh_fetch
motivation: 传统验证器在处理复杂或知识密集型任务时，容易因中间步骤错误导致误判，且缺乏外部工具支持来保证计算的准确性。
method: 提出一种结合前向与后向Agent的双向验证框架，并利用强化学习训练模型自主进行多轮工具调用与推理审议。
result: "实验显示Agentic Verifier在多种测试时缩放策略下表现优异，其4B模型在性能上超越现有最先进的ORM达25.2%。"
conclusion: 将奖励建模转变为具备工具使用能力的Agent化交互过程，是解决复杂推理验证难题并实现高效测试时缩放的有效途径。
---

## 摘要
验证器已被证明能通过测试时扩展（TTS）增强大语言模型（LLM）的推理能力。然而，它们在复杂领域面临重大挑战。错误中间推理导致的误差传播可能会使看似合理的解法产生假阳性，而缺乏外部落地（grounding）则使得验证器在计算或知识密集型任务上不可靠。为应对这些挑战，我们提出了智能体化验证器（Agentic Verifier），这是一个将奖励建模转化为多轮、工具增强的审议过程的框架。我们引入了互补的正向和反向智能体：一个从前提推导结论以追踪解法，另一个则根据基础前提重新检查结论。这种双向过程实现了对解法全面、可靠且可解释的评估。为了便于实际部署，我们提出了 AgentV-RL。通过主动探索和强化学习，验证器能够自主地将工具使用与内部推理交织在一起。大量实验表明，智能体化验证器在并行和顺序 TTS 下均能产生一致的性能提升。值得注意的是，我们的 4B 变体性能超过了最先进的 ORM 25.2%，使其成为智能体化奖励建模的一种极具前景的范式。

## Abstract
Verifiers have been demonstrated to enhance LLM reasoning via test-time scaling (TTS). Yet, they face significant challenges in complex domains. Error propagation from incorrect intermediate reasoning can lead to false positives for seemingly plausible solutions, while lacking external grounding makes verifiers unreliable on computation or knowledge-intensive tasks. To address these challenges, we propose Agentic Verifier, a framework that transforms reward modeling into a multi-turn, tool-augmented deliberative process. We introduce complementary forward and backward agents: one traces solutions from premises to conclusions, while the other re-checks conclusions against their underlying premises. This bidirectional process enables a comprehensive, reliable, and interpretable assessment of solutions. To facilitate practical deployment, we propose AgentV-RL. Through proactive exploration and reinforcement learning, the verifier autonomously interleaves tool-use with internal reasoning. Extensive experiments show that Agentic Verifier yields consistent performance gains under both parallel and sequential TTS. Notably, our 4B variant surpasses state-of-the-art ORMs by 25.2%, positioning it as a promising paradigm for agentic reward modeling.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AgentV-RL**，这是一种通过“智能体化验证器（Agentic Verifier）”来增强大语言模型（LLM）奖励建模的新范式。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的奖励模型（如 ORM 和 PRM）通常是单轮、非交互式的，且仅输出标量分数。这导致了两个核心痛点：
    1.  **错误传播**：验证器容易被看似合理但逻辑错误的中间步骤误导。
    2.  **缺乏外部落地（Grounding）**：在处理计算密集型或知识密集型任务时，验证器容易产生幻觉。
*   **核心目标**：将奖励建模从“静态评分”转变为“动态审议”，通过多轮推理和外部工具（如 Python 解释器）来提高验证的可靠性和可解释性。

### 2. 方法论
*   **Agentic Verifier 框架**：引入了两个互补的智能体角色：
    *   **正向智能体（Forward Agent）**：执行“充分性检查”，从前提逐步推导至结论。
    *   **后向智能体（Backward Agent）**：执行“必要性检查”，从结论反向追溯至题目约束。
*   **三阶段审议流程**：
    1.  **规划（Planning）**：将复杂解法分解为可验证的原子步骤。
    2.  **验证（Validation）**：多轮“思考-行动-观察”循环，支持调用 Python 解释器进行数值计算。
    3.  **裁决（Verdict）**：综合所有证据，给出最终的二元判断（True/False）及置信度分数。
*   **AgentV-RL 训练方案**：
    *   **合成数据引擎**：利用高质量数据集（如 Polaris, DeepScaleR）自动生成验证轨迹，并进行质量控制。
    *   **两阶段训练**：
        1.  **拒绝采样微调（RFT/SFT）**：对齐模型的多轮决策和工具使用行为。
        2.  **强化学习（RL）**：采用 **GRPO（群体相对策略优化）** 算法，通过二元奖励信号（判断是否与真值一致）激励模型进行自主探索和长程推理。

### 3. 实验设计
*   **数据集**：涵盖数学推理核心榜单，包括 **GSM8K**、**MATH500**、**Gaokao2023** 和 **AIME24**。此外还测试了 **LiveCodeBench**（代码）和 **HotpotQA**（多跳问答）以验证泛化性。
*   **测试场景**：
    1.  **并行缩放（Best-of-N, BoN）**：验证器从 N 个候选解中挑选最优解。
    2.  **顺序缩放（Iterative Refinement）**：验证器提供反馈，指导生成模型进行自我修正。
*   **对比方法**：
    *   **文本推理 LLM**：Qwen2.5-7B, Llama3.1-8B, Qwen3-4B, DeepSeek-R1-Distill-14B 等。
    *   **结果奖励模型（ORM）**：Skywork-V2, InternLM2-20B, INF-ORM-70B 等。
    *   **过程奖励模型（PRM）**：Qwen2.5-Math-PRM, Math-Shepherd 等。

### 4. 资源与算力
*   **算力投入**：
    *   **SFT 阶段**：使用 16 台 NVIDIA **A100 (80GB)** GPU。
    *   **RL 阶段**：使用 32 台 NVIDIA **A100 (80GB)** GPU。
*   **训练细节**：SFT 训练 15K 样本（1 个 epoch）；RL 阶段在 50K 样本上优化。最大序列长度设为 21,600 tokens。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括在 4 个核心数学榜单上的 BoN 性能（N=32, 64, 128）、多轮修正实验（1-3 轮）、消融实验（正向 vs 后向、有无工具、训练阶段对比）以及模型规模缩放实验（0.6B 到 4B）。
*   **客观性与公平性**：所有验证器变体均在相同的候选解池上进行评估，确保了对比的严谨性。实验覆盖了从基础数学到竞赛级（AIME）的难度跨度，结果具有代表性。

### 6. 主要结论与发现
*   **性能飞跃**：Agentic Verifier 的 **4B 变体** 在 MATH500 上达到了 79.0% 的准确率，显著超过了参数量大 10 倍以上的 **INF-ORM-70B**（55.4%）。
*   **双向验证的必要性**：消融实验证明，结合正向和后向检查的效果优于单一方向，能更有效地识别隐蔽的逻辑漏洞。
*   **推理时间扩展（TTS）**：随着推理时采样轨迹数量的增加，验证精度持续提升，证明了该框架具备良好的测试时缩放特性。
*   **泛化能力**：在代码和多跳问答任务上也表现出优于基座模型的性能，证明了框架的通用性。

### 7. 优点
*   **创新的范式**：将奖励建模从简单的分类任务提升为具备工具能力的智能体交互过程。
*   **高可解释性**：通过多轮推理轨迹，用户可以清晰看到验证器为何判定某个解法错误。
*   **高效的训练方案**：AgentV-RL 成功将复杂的智能体能力蒸馏到较小规模（4B）的模型中，实现了极高的性价比。

### 8. 不足与局限
*   **计算开销**：多轮审议和工具调用显著增加了推理延迟（Latency）和 Token 消耗（相比基座模型，Token 消耗增加了约 3 倍）。
*   **工具依赖**：验证器的上限受限于外部工具（如 Python 解释器）的覆盖范围，对于无法通过代码验证的逻辑（如纯文字证明）可能仍存在局限。
*   **实时性挑战**：由于其多轮交互的本质，该方法目前更适合离线评估或对延迟不敏感的高难度推理任务，不适合实时交互场景。

（完）
