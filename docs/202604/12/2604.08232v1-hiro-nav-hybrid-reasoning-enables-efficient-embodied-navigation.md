---
title: "HiRO-Nav: Hybrid ReasOning Enables Efficient Embodied Navigation"
title_zh: HiRO-Nav：混合推理实现高效具身导航
authors: "He Zhao, Yijun Yang, Zichuan Lin, Deheng Ye, Chunyan Miao"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08232v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于大型推理模型的具身导航智能体，用于序列决策
tldr: HiRO-Nav 针对具身导航中大推理模型（LRM）计算开销大的问题，提出了一种混合推理策略。该方法通过监测动作熵，仅在动作不确定性较高（高熵）的关键时刻激活推理能力，而在简单场景下保持反射性动作。通过混合监督微调和在线强化学习，HiRO-Nav 在保持高成功率的同时显著降低了 Token 消耗，实现了效率与性能的平衡。
selection_source: fresh_fetch
motivation: 现有的具身导航智能体在每一步都进行推理会导致巨大的计算开销，且并非所有场景都需要复杂推理。
method: 提出基于动作熵的自适应推理机制，并结合混合监督微调与在线强化学习来优化高熵动作的决策质量。
result: 在 CHORES-S ObjectNav 基准测试中，HiRO-Nav 在成功率和 Token 使用效率之间取得了优于全推理或无推理基准的效果。
conclusion: 混合推理策略能有效识别导航中的关键决策点，在显著降低计算成本的同时提升具身智能体的任务执行能力。
---

## 摘要
基于大语言推理模型（LRMs）构建的具身导航智能体能够处理复杂的、多模态环境输入，并进行每一步的落地推理，以优化长程任务中的序列决策。然而，一个关键问题仍然存在：如何智能且高效地利用 LRMs 的推理能力来完成长程导航任务？在简单场景中，智能体应当表现出反射式的行动，而在复杂场景中，它们应在行动前进行深思熟虑的推理。为了实现这一目标，我们引入了混合推理导航（HiRO-Nav）智能体，这是首个能够根据自身动作熵自适应地决定是否在每一步执行思考的智能体。具体而言，通过研究智能体动作熵在导航轨迹中的演变，我们观察到只有一小部分动作表现出高熵，而这些动作通常会将智能体引导至新颖场景或关键物体。此外，研究动作熵与任务完成度（即 Q 值）之间的关系表明，改进高熵动作对任务成功的贡献更为显著。因此，我们提出了一套定制的训练流程，包括作为冷启动的混合监督微调，以及随后采用所提混合推理策略的在线强化学习，从而仅针对高熵动作显式激活推理，在显著降低计算开销的同时提高决策质量。在 CHORES-S ObjectNav 基准测试上的广泛实验表明，与全量思考（dense-thinking）和不思考（no-thinking）的基准模型相比，HiRO-Nav 在成功率和 Token 效率之间实现了更好的权衡。

## Abstract
Embodied navigation agents built upon large reasoning models (LRMs) can handle complex, multimodal environmental input and perform grounded reasoning per step to improve sequential decision-making for long-horizon tasks. However, a critical question remains: \textit{how can the reasoning capabilities of LRMs be harnessed intelligently and efficiently for long-horizon navigation tasks?} In simple scenes, agents are expected to act reflexively, while in complex ones they should engage in deliberate reasoning before acting.To achieve this, we introduce \textbf{H}ybr\textbf{i}d \textbf{R}eas\textbf{O}ning \textbf{Nav}igation (\textbf{HiRO-Nav}) agent, the first kind of agent capable of adaptively determining whether to perform thinking at every step based on its own action entropy. Specifically, by examining how the agent's action entropy evolves over the navigation trajectories, we observed that only a small fraction of actions exhibit high entropy, and these actions often steer the agent toward novel scenes or critical objects. Furthermore, studying the relationship between action entropy and task completion (i.e., Q-value) reveals that improving high-entropy actions contributes more positively to task success.Hence, we propose a tailored training pipeline comprising hybrid supervised fine-tuning as a cold start, followed by online reinforcement learning with the proposed hybrid reasoning strategy to explicitly activate reasoning only for high-entropy actions, significantly reducing computational overhead while improving decision quality. Extensive experiments on the \textsc{CHORES}-$\mathbb{S}$ ObjectNav benchmark showcases that HiRO-Nav achieves a better trade-off between success rates and token efficiency than both dense-thinking and no-thinking baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《HiRO-Nav: Hybrid ReasOning Enables Efficient Embodied Navigation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身导航任务要求智能体在长程序列决策中具备感知与推理能力。虽然大型推理模型（LRMs）通过思维链（CoT）显著提升了决策质量，但现有的“每步必思考”（Dense Thinking）范式存在两大缺陷：
*   **计算开销巨大**：在实时导航中产生高延迟和高 Token 消耗。
*   **过度思考（Overthinking）**：在简单场景（如直走）中进行冗余推理反而可能引入幻觉，降低任务成功率。
**研究动机**：探索如何让智能体像人类一样，在简单场景下依靠直觉（反射），在复杂关键点（如路口、目标识别）才进行深思熟虑。

### 2. 方法论：核心思想与关键技术
HiRO-Nav 是首个能根据**动作熵（Action Entropy, AE）**自适应决定是否进行推理的导航智能体。

*   **核心思想**：通过监测模型输出动作的第一个 Token 的熵值来衡量不确定性。研究发现，仅约 30% 的高熵动作对应于探索新区域或接近关键物体的决策点。
*   **混合推理策略**：
    1.  **动作预测**：首先在“不思考”模式下预测动作。
    2.  **熵值判定**：计算该动作的熵 $H$。若 $H > \tau$（阈值），则触发推理模式。
    3.  **重写决策**：在推理模式下，模型生成 CoT 过程并重新输出最终动作。
    4.  **无思考窗口（NTW）**：为防止陷入连续高熵的死循环，引入窗口期限制推理频率。
*   **训练流程**：
    *   **混合监督微调（H-SFT）**：利用 Gemini-2.0-Flash 对专家轨迹中的高熵样本生成推理轨迹，构建混合数据集进行冷启动。
    *   **两阶段在线强化学习（RL）**：
        *   **阶段 I**：仅优化“不思考”能力，提升基础导航水平。
        *   **阶段 II**：引入混合推理策略，专门优化推理能力，并使用 KL 散度约束防止“不思考”能力的退化（模式崩溃）。

### 3. 实验设计
*   **数据集/场景**：使用 **CHORES-S ObjectNav** 基准测试，包含 200 个场景中的 200 个任务。
*   **环境**：AI2-Thor 模拟器，使用 Stretch RE-1 机器人配置。
*   **对比方法**：
    *   **闭源模型**：GPT-4o（无推理）、o3（全推理）、Gemini 2.5 Pro（全推理）。
    *   **开源基准**：NaVILA、Qwen2.5VL-3B。
    *   **策略对比**：No-Thinking（不思考）、Dense-Thinking（每步思考）、Thinking-Every-K-Steps（固定步长思考）。
*   **评估指标**：成功率（SR）、路径长度加权成功率（SEL）、每集平均 Token 数（#Token/E）。

### 4. 资源与算力
*   **模型基础**：采用 Qwen2.5VL-3B 作为骨干网络。
*   **训练框架**：使用 Verl-Agent 框架进行 RL 训练。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到了 SFT 阶段 Batch Size 为 256，RL 阶段 Rollout Size 为 48。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 SFT 到 RL 的全流程对比，并在 200 个测试任务上进行了验证。
*   **消融实验**：非常充分。分别针对“两阶段 RL 的必要性”、“KL 惩罚的作用”、“动作熵阈值 $\tau$ 的选取”、“无思考窗口（NTW）的大小”以及“动作熵计算方法（首词 vs 全句）”进行了详细实验。
*   **鲁棒性测试**：额外测试了在非真值（由 Mask R-CNN 和 Depth-Anything 生成）的语义地图下的表现，证明了方法的实用性。
*   **客观性**：通过 Pass@k 评估探索了性能上限，并与 SOTA 方法 Poliformer 进行了对比，结果具有说服力。

### 6. 主要结论与发现
*   **性能与效率的平衡**：HiRO-Nav 在 SR 和 SEL 上均达到 SOTA，同时 Token 消耗仅略高于“不思考”基准，远低于“全量思考”模型。
*   **高熵即关键**：实验证实，仅对高熵动作进行推理能获得最高 Q 值提升，验证了“按需思考”的科学性。
*   **两阶段训练至关重要**：直接进行混合 RL 会导致模型在非推理步的表现大幅下降，必须分阶段优化并引入 KL 约束。

### 7. 优点（亮点）
*   **自适应机制**：打破了固定步长或全量推理的死板模式，实现了计算资源的动态分配。
*   **训练策略创新**：针对 LRM 在 RL 过程中容易出现的模式崩溃问题，提出了有效的两阶段训练方案。
*   **长效记忆集成**：成功将标注语义地图（ASM）与 VLM 结合，增强了模型在长程任务中的空间推理能力。

### 8. 不足与局限
*   **反应式延迟**：目前的策略是“先预测动作，发现熵高再重写”，这在实际部署中意味着高熵步需要进行两次推理尝试，增加了瞬时延迟。
*   **阈值敏感性**：虽然论文给出了推荐值，但熵阈值 $\tau$ 可能仍需根据不同环境或基础模型进行手动调优。
*   **感知依赖**：性能高度依赖于外部感知模块（如 ASM 的构建质量），虽然做了鲁棒性测试，但在极端复杂或动态环境下的表现仍待验证。

（完）
