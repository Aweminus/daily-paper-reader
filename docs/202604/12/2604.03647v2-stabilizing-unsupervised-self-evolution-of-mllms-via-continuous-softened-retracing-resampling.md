---
title: Stabilizing Unsupervised Self-Evolution of MLLMs via Continuous Softened Retracing reSampling
title_zh: 通过连续平滑回溯重采样稳定多模态大语言模型的无监督自我演化
authors: "Yunyao Yu, Zhengxian Wu, Zhuohong Chen, Hangrui Xu, Zirui Liao, Xiangwen Deng, Zhifang Liu, Senyuan Shi, Haoqian Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03647v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 多模态大语言模型的自我演化与推理路径
tldr: 本研究针对多模态大语言模型（MLLM）无监督自进化中伪标签质量不稳定的问题，提出了CSRS框架。该框架通过回溯推理机制（RRM）探索长尾推理路径，利用平滑频率奖励（SFR）提供连续反馈信号，并结合视觉语义扰动（VSP）强化逻辑推理。实验证明，CSRS在MathVision等基准测试上显著提升了模型性能，在几何任务的无监督自进化中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的自进化方法过度依赖多数投票来选择伪标签，容易受到模型内在偏差的影响，导致推理路径的客观正确性难以保证。
method: 提出了包含回溯推理机制（RRM）、平滑频率奖励（SFR）和视觉语义扰动（VSP）的CSRS框架，旨在优化推理路径探索并提供更精准的连续反馈。
result: 在Qwen2.5-VL-7B模型上，CSRS显著提升了MathVision等基准的推理表现，并在几何任务的无监督自进化中取得了SOTA结果。
conclusion: CSRS通过改进反馈信号的质量和推理路径的多样性，有效稳定了MLLM的无监督自进化过程，增强了模型的数学逻辑能力。
---

## 摘要
在多模态大语言模型（MLLMs）的无监督自我演化中，后训练阶段反馈信号的质量对于稳定且有效的学习至关重要。然而，现有的自我演化方法主要依赖多数投票来选择出现频率最高的输出作为伪金标准答案，这可能源于模型的固有偏见，而无法保证推理路径的客观正确性。为了应对这种退化，我们提出了 MLLM 自我演化中的连续平滑回溯重采样（CSRS）。具体而言，我们引入了一种回溯重推理机制（RRM），使模型能够从锚点开始重新推理，以扩大对长尾推理路径的探索。同时，我们提出了平滑频率奖励（SFR），用连续信号取代二元奖励，并根据采样推理集中答案的频率来校准奖励。此外，结合视觉语义扰动（VSP），CSRS 确保模型优先考虑数学逻辑而非视觉表象。实验结果表明，CSRS 显著提升了 Qwen2.5-VL-7B 在 MathVision 等基准测试上的推理性能。我们在几何任务的无监督自我演化方面达到了最先进（SOTA）的水平。我们的代码可在 https://github.com/yyy195/CSRS 获取。

## Abstract
In the unsupervised self-evolution of Multimodal Large Language Models, the quality of feedback signals during post-training is pivotal for stable and effective learning. However, existing self-evolution methods predominantly rely on majority voting to select the most frequent output as the pseudo-golden answer, which may stem from the model's intrinsic biases rather than guaranteeing the objective correctness of the reasoning paths. To counteract the degradation, we propose Continuous Softened Retracing reSampling (CSRS) in MLLM self-evolution. Specifically, we introduce a Retracing Re-inference Mechanism (RRM) that the model re-inferences from anchor points to expand the exploration of long-tail reasoning paths. Simultaneously, we propose Softened Frequency Reward (SFR), which replaces binary rewards with continuous signals, calibrating reward based on the answers' frequency across sampled reasoning sets. Furthermore, incorporated with Visual Semantic Perturbation (VSP), CSRS ensures the model prioritizes mathematical logic over visual superficiality. Experimental results demonstrate that CSRS significantly enhances the reasoning performance of Qwen2.5-VL-7B on benchmarks such as MathVision. We achieve state-of-the-art (SOTA) results in unsupervised self-evolution on geometric tasks. Our code is avaible at https://github.com/yyy195/CSRS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Stabilizing Unsupervised Self-Evolution of MLLMs via Continuous Softened Retracing reSampling》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是多模态大语言模型（MLLM）的**无监督自我演化（Unsupervised Self-Evolution）**。
*   **研究背景**：为了减少对昂贵人工标注的依赖，研究者尝试让模型通过内部反馈（如多数投票）自主生成伪标签进行强化学习。
*   **核心问题**：现有的方法（如多数投票）存在“确认偏差”循环。模型倾向于重复强化其认为正确的路径，导致探索空间收缩，最终引发**模型崩溃（Model Collapse）**。此外，传统的二元奖励（0/1）过于稀疏且粗糙，无法引导模型处理复杂的逻辑决策点。

### 2. 方法论：CSRS 框架
论文提出了 **CSRS（连续平滑回溯重采样）** 框架，包含三个核心组件：
*   **回溯重推理机制（RRM）**：
    *   **核心思想**：不再从头开始采样，而是在初始推理路径（母轨迹）中设置“回溯锚点”。
    *   **技术细节**：将推理路径在特定比例（如 70% 处）截断，以此为起点进行多次局部重采样。这能强迫模型在关键逻辑决策点进行深度探索，打破初始偏见的累积。
*   **平滑频率奖励（SFR）**：
    *   **核心思想**：将离散的二元奖励替换为基于频率变化的连续信号。
    *   **技术细节**：计算答案在“母轨迹集”与“重推理集”中的频率差异。如果一个答案在局部重采样后频率上升，说明该路径具有潜在的逻辑稳健性。通过 `tanh` 函数平滑处理，动态放大低频但可能正确的“长尾”路径奖励，同时抑制高频错误路径的过度拟合。
*   **视觉语义扰动（VSP）**：
    *   **核心思想**：在重推理阶段对图像加入高斯噪声。
    *   **技术细节**：通过扰动低级视觉特征，迫使模型依赖不变的数学逻辑结构而非视觉表象（如“看起来像某个形状”）来得出结论。

### 3. 实验设计
*   **训练数据集**：Geometry3K、GeoQA、MMR1（均为几何/数学相关）。
*   **测试基准（Benchmarks）**：
    *   **MathVision**：衡量多模态数学推理。
    *   **MathVerse**：评估模型是否真正理解视觉图表。
    *   **MathVista**：综合数学推理基准。
    *   **We-Math**：评估类人数学推理能力。
*   **对比方法**：
    *   **基座模型**：Qwen2.5-VL-7B、InternVL3-8B、ThinkLite-VL-7B。
    *   **SOTA 基准**：MM-UPT（目前领先的无监督后训练框架）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A800-80G GPU**。
*   **训练时长**：在 Qwen2.5-VL-7B 上进行 15 个 epoch 的训练，总耗时约 **36 小时**。
*   **软件框架**：基于 veRL 框架和 GRPO 算法实现。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流数学推理基准上进行了全面测试，并跨越了 3 个不同的模型家族（Qwen, InternVL, ThinkLite）验证泛化性。
*   **消融实验**：详细测试了 RRM、SFR、VSP 各个组件的独立贡献，以及超参数（如回溯率 $\omega$、奖励敏感度 $\beta$）的影响。
*   **充分性评价**：实验设计较为充分。除了准确率对比，还通过 t-SNE 语义空间可视化、熵值变化曲线、高置信度样本比例等维度深入分析了“模型崩溃”的缓解情况。实验对比在相同基座和设置下进行，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：CSRS 在所有基准上均优于 MM-UPT。在 Qwen2.5-VL-7B 上，MathVision 提升 2.34%，We-Math 提升 3.28%，达到了无监督自进化的 SOTA 水平。
*   **缓解崩溃**：理论证明并实验观察到，连续平滑奖励能减缓策略分布的极化，使模型在训练后期仍能保持逻辑多样性。
*   **逻辑优先**：视觉扰动实验证明，模型在 CSRS 引导下更倾向于执行真正的数学推导，而非简单的模式匹配。

### 7. 优点（亮点）
*   **理论支撑**：论文不仅提出了方法，还给出了数学证明，解释了为什么连续奖励比 0/1 奖励在自进化过程中更稳定。
*   **机制创新**：引入“回溯锚点”概念，解决了长程推理中采样稀疏的问题，比全局随机采样更高效。
*   **即插即用**：该框架不依赖外部真实标签，具有很强的自主进化潜力。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在几何和数学推理任务上，尚未在更广泛的开放式通用视觉问答（Open-ended VQA）中验证其有效性。
*   **计算开销**：重采样机制（RRM）虽然提高了质量，但也增加了推理阶段的计算成本（每个样本需要生成更多轨迹）。
*   **锚点选择**：目前采用固定比例的回溯锚点，未来可能需要更智能的机制来动态识别推理路径中的“关键逻辑节点”。

（完）
