---
title: "GUIDE: Resolving Domain Bias in GUI Agents through Real-Time Web Video Retrieval and Plug-and-Play Annotation"
title_zh: GUIDE：通过实时网络视频检索和即插即用标注解决 GUI 智能体中的领域偏差
authors: "Rui Xie, Zhi Gao, Chenrui Shi, Zirui Shang, Lu Chen, Qing Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26266v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 使用视觉语言模型进行界面交互的GUI智能体
tldr: 针对GUI智能体在特定软件领域因缺乏经验而导致的领域偏差（规划与定位能力不足），本文提出GUIDE框架。这是一种无需训练、即插即用的增强方案，通过字幕驱动的Video-RAG从网络教程视频中检索相关知识，并利用自动标注流水线从视频关键帧中提取操作流程与UI布局信息。实验证明，GUIDE能显著提升智能体在复杂任务中的表现并减少操作步骤，具有良好的通用性。
selection_source: fresh_fetch
motivation: GUI智能体由于在训练中缺乏特定领域软件的操作数据，导致其在处理专业应用时面临规划不熟练和UI定位不准的领域偏差问题。
method: 提出GUIDE框架，通过三阶段视频检索获取相关教程，并利用逆动力学范式的自动标注流水线将视频知识转化为智能体的规划和定位增强信息。
result: "在OSWorld基准测试中，GUIDE在不改变模型参数的情况下，使各类智能体的性能提升超过5%并有效缩短了任务执行步骤。"
conclusion: GUIDE证明了通过实时检索并转化网络视频教程中的专业知识，可以作为一种架构无关的有效手段来消除GUI智能体的领域偏差。
---

## 摘要
大语言视觉模型赋予了 GUI 智能体强大的界面理解与交互通用能力。然而，由于训练过程中缺乏特定领域软件操作数据的暴露，这些智能体表现出显著的领域偏差——它们缺乏对特定应用程序操作工作流（规划）和 UI 元素布局（定位）的熟悉度，从而限制了其在真实世界任务中的表现。在本文中，我们提出了 GUIDE（通过教学视频驱动的专业知识消除 GUI 偏差），这是一个无需训练、即插即用的框架，通过检索增强的自动标注流水线，从网络教学视频中自主获取领域特定专业知识，从而解决 GUI 智能体的领域偏差。GUIDE 引入了两项关键创新：首先，字幕驱动的 Video-RAG 流水线通过字幕分析解锁视频语义，执行渐进式三阶段检索（领域分类、主题提取和相关性匹配），以识别与任务相关的教学视频；其次，基于逆动力学范式构建的全自动标注流水线将经过 UI 元素检测增强的连续关键帧输入到视觉语言模型（VLM）中，推断出所需的规划和定位知识，并将其注入智能体的相应模块中，以解决领域偏差的两种表现形式。在 OSWorld 上的广泛实验证明了 GUIDE 作为多智能体系统和单模型智能体即插即用组件的通用性。在不修改任何模型参数或架构的情况下，它持续带来超过 5% 的性能提升并减少了执行步骤，验证了 GUIDE 作为一种与架构无关的增强方案，能够有效弥合 GUI 智能体的领域偏差。

## Abstract
Large vision-language models have endowed GUI agents with strong general capabilities for interface understanding and interaction. However, due to insufficient exposure to domain-specific software operation data during training, these agents exhibit significant domain bias - they lack familiarity with the specific operation workflows (planning) and UI element layouts (grounding) of particular applications, limiting their real-world task performance. In this paper, we present GUIDE (GUI Unbiasing via Instructional-Video Driven Expertise), a training-free, plug-and-play framework that resolves GUI agent domain bias by autonomously acquiring domain-specific expertise from web tutorial videos through a retrieval-augmented automated annotation pipeline. GUIDE introduces two key innovations. First, a subtitle-driven Video-RAG pipeline unlocks video semantics through subtitle analysis, performing progressive three-stage retrieval - domain classification, topic extraction, and relevance matching - to identify task-relevant tutorial videos. Second, a fully automated annotation pipeline built on an inverse dynamics paradigm feeds consecutive keyframes enhanced with UI element detection into VLMs, inferring the required planning and grounding knowledge that are injected into the agent's corresponding modules to address both manifestations of domain bias. Extensive experiments on OSWorld demonstrate GUIDE's generality as a plug-and-play component for both multi-agent systems and single-model agents. It consistently yields over 5% improvements and reduces execution steps - without modifying any model parameters or architecture - validating GUIDE as an architecture-agnostic enhancement to bridge GUI agent domain bias.

---

## 论文详细总结（自动生成）

### 论文总结：GUIDE —— 通过实时网络视频检索与即插即用标注解决 GUI 智能体的领域偏差

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 GUI 智能体（如基于 GPT-4V 或 Qwen-VL 的智能体）虽然具备强大的通用界面理解能力，但在面对特定专业软件（如 GIMP、LibreOffice）时，常表现出明显的**领域偏差（Domain Bias）**。这种偏差主要体现在两个层面：
*   **规划层面（Planning-level）：** 不熟悉特定软件的操作流（例如，知道要调亮度，但不知道在 GIMP 中路径是“Colors -> Brightness”而非“Image -> Adjustments”）。
*   **定位层面（Grounding-level）：** 不熟悉特定 UI 布局和控件样式，难以在复杂界面中精准找到目标元素。
传统的微调或手动标注方法成本高且难以覆盖不断更新的软件。本文提出 **GUIDE** 框架，旨在利用互联网上海量的视频教程作为知识源，通过实时检索和自动标注，为智能体提供即插即用的专业知识增强。

#### 2. 方法论：核心思想与关键技术
GUIDE 是一个无需训练、架构无关的协作系统，包含三个核心代理：
*   **字幕驱动的 Video-RAG 流水线（检索代理）：**
    *   利用字幕（Subtitle）作为视觉像素与任务需求之间的语义桥梁。
    *   **三阶段过滤：** 1) 领域分类（通过字幕判断是否为实际操作演示）；2) 主题提取（结合标题和字幕生成精确的操作描述）；3) 相关性匹配（双锚点机制确保视频内容与任务高度相关）。
*   **全自动标注流水线（标注代理）：**
    *   **逆动力学范式（Inverse Dynamics）：** 给定连续关键帧 $s_t$ 和 $s_{t+1}$，推断其间的操作 $a_t$。
    *   **感知增强：** 使用 OmniParser 提取 UI 元素图（Et），结合 Whisper ASR 字幕上下文，辅助 VLM（如 GPT-5.1）进行推理。
    *   **知识分解：** 将标注转化为**规划知识**（操作流、专家注意事项）和**定位知识**（关键 UI 元素的视觉特征与功能描述），均以自然语言形式呈现。
*   **即插即用集成（集成代理）：**
    *   将提取的知识注入智能体的 Prompt 中。支持**多智能体模式**（分别注入规划和定位模块）和**单模型模式**（通过引导式思维链 CoT 模板统一注入）。

#### 3. 实验设计
*   **数据集/场景：** 使用 **OSWorld** 基准测试，包含 361 个真实世界的计算机任务，涵盖 Ubuntu 环境下的 10 个应用领域（如 Chrome, GIMP, Calc, Writer, VS Code 等）。
*   **对比方法与基准：**
    *   **基础模型：** Seed-1.8, Qwen3-VL-8B, 以及多智能体系统 AgentS3。
    *   **配置对比：** 无标注（Baseline）、仅规划增强（+Planning）、规划与定位双通道增强（+Plan. & Gnd.）。
    *   **外部对比：** 与同类工作 Watch & Learn 进行对比。

#### 4. 资源与算力
*   **训练算力：** 论文强调该方法是 **Training-free（无需训练）** 的，因此不涉及模型训练阶段的 GPU 算力消耗。
*   **推理与标注开销：**
    *   标注过程使用了 GPT-5.1、GPT-4o 等 API。
    *   本地执行部分：Whisper ASR（Base 模型）和 OmniParser 用于 UI 元素检测。
    *   **成本分析：** 标注一个视频约需 0.25 美元（GPT-5.1），全量测试集（约 427 个视频）总成本约 114.6 美元，远低于人工标注成本。

#### 5. 实验数量与充分性
*   **实验规模：** 在 361 个任务上进行了完整测试，涵盖了 10 个不同的软件领域。
*   **消融实验：** 针对定位元素数量（k 值）、标注模型强度（从 GPT-4.1 Mini 到 GPT-5.1）进行了详细消融。
*   **客观性：** 引入了人工评估（Human Evaluation）来验证视频分类的准确率（100% Precision）和主题提取的质量（96% 可接受率），实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能提升：** GUIDE 在不同架构的智能体上稳定提升了 **4.5% - 7.5%** 的成功率。
*   **瓶颈识别：** 规划知识贡献了约 85%-91% 的性能增益，说明**规划层面的领域偏差是当前 GUI 智能体的主要瓶颈**。
*   **效率优化：** 定位知识能显著减少成功任务的操作步骤（如在 VLC 和 Calc 领域分别减少 5.0 和 3.7 步），降低了探索成本。
*   **通用性：** 证明了该框架对开源模型、闭源模型及多智能体架构均有效。

#### 7. 优点（亮点）
*   **即插即用：** 无需修改模型参数或架构，具有极强的可移植性。
*   **实时性：** 能够针对新任务实时从 YouTube 检索最新视频，解决了离线语料库过时的问题。
*   **语义深度：** 相比仅依赖标题的检索，字幕驱动的 Video-RAG 能更精准地捕捉视频内部的操作逻辑。
*   **知识解耦：** 将视频信息转化为坐标无关（Coordinate-free）的自然语言描述，增强了知识在不同分辨率或软件版本间的迁移能力。

#### 8. 不足与局限
*   **检索依赖：** 性能高度依赖于 YouTube 视频的质量和覆盖范围。对于极少数冷门或跨应用工作流，检索不到视频时会退化为基础模型表现。
*   **误导风险：** 如果检索到的视频与任务目标存在细微偏差（如版本差异或操作路径不一致），可能会误导智能体进入错误的分支（论文中分析了 Failure Cases）。
*   **API 成本：** 虽然比人工便宜，但对于大规模实时部署，频繁调用高强度 VLM API 仍存在一定的经济成本。
*   **视频格式限制：** 目前主要针对录屏教学视频，对于动画演示或非实操类视频的过滤仍有提升空间。

（完）
