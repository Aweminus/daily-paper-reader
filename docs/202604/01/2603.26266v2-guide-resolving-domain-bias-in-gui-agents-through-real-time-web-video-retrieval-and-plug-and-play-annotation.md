---
title: "GUIDE: Resolving Domain Bias in GUI Agents through Real-Time Web Video Retrieval and Plug-and-Play Annotation"
title_zh: GUIDE：通过实时网页视频检索和即插即用标注解决 GUI 智能体中的领域偏差
authors: "Rui Xie, Zhi Gao, Chenrui Shi, Zirui Shang, Lu Chen, Qing Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26266v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 使用视觉语言模型进行界面交互的GUI智能体
tldr: 针对GUI智能体在特定软件中因缺乏领域知识而导致的规划与定位偏差，本文提出GUIDE框架。这是一种无需训练、即插即用的增强方案，通过字幕驱动的Video-RAG从网络教程视频中检索相关知识，并利用自动标注流水线从视频关键帧中提取操作逻辑与UI布局信息。实验证明，GUIDE能显著提升多种智能体在OSWorld上的任务成功率并减少执行步骤，展现了极强的通用性。
selection_source: fresh_fetch
motivation: GUI智能体由于训练数据中缺乏特定软件的操作细节，在实际应用中面临操作流程不熟悉和UI元素定位不准的领域偏差挑战。
method: 引入字幕驱动的Video-RAG进行三阶段视频检索，并结合逆动力学范式的自动标注流水线，从视频中提取规划与定位知识注入智能体。
result: "在OSWorld基准测试中，GUIDE在不改变模型参数的前提下，使多智能体和单模型智能体的性能提升均超过5%，并显著降低了操作步数。"
conclusion: GUIDE通过自主获取网络视频中的领域专家知识，有效解决了GUI智能体的领域偏差问题，是一种架构无关的即插即用增强框架。
---

## 摘要
大语言视觉模型赋予了 GUI 智能体强大的界面理解与交互通用能力。然而，由于训练过程中缺乏特定领域软件操作数据的暴露，这些智能体表现出显著的领域偏差——它们缺乏对特定应用程序操作工作流（规划）和 UI 元素布局（定位）的熟悉度，从而限制了其在现实任务中的表现。在本文中，我们提出了 GUIDE（通过教学视频驱动的专业知识消除 GUI 偏差），这是一个无需训练、即插即用的框架，通过检索增强的自动标注流水线，从网页教程视频中自主获取领域特定专业知识，从而解决 GUI 智能体的领域偏差。GUIDE 引入了两项关键创新：首先，一种字幕驱动的 Video-RAG 流水线通过字幕分析解锁视频语义，执行渐进式三阶段检索（领域分类、主题提取和相关性匹配），以识别与任务相关的教程视频。其次，基于逆动力学范式构建的全自动标注流水线将经过 UI 元素检测增强的连续关键帧输入到视觉语言模型（VLM）中，推断出所需的规划和定位知识，并将其注入智能体的相应模块中，以解决领域偏差的两种表现形式。在 OSWorld 上的广泛实验证明了 GUIDE 作为多智能体系统和单模型智能体即插即用组件的通用性。在不修改任何模型参数或架构的情况下，它持续带来超过 5% 的性能提升并减少了执行步骤，验证了 GUIDE 是一种与架构无关的增强方案，能够弥合 GUI 智能体的领域偏差。

## Abstract
Large vision-language models have endowed GUI agents with strong general capabilities for interface understanding and interaction. However, due to insufficient exposure to domain-specific software operation data during training, these agents exhibit significant domain bias - they lack familiarity with the specific operation workflows (planning) and UI element layouts (grounding) of particular applications, limiting their real-world task performance. In this paper, we present GUIDE (GUI Unbiasing via Instructional-Video Driven Expertise), a training-free, plug-and-play framework that resolves GUI agent domain bias by autonomously acquiring domain-specific expertise from web tutorial videos through a retrieval-augmented automated annotation pipeline. GUIDE introduces two key innovations. First, a subtitle-driven Video-RAG pipeline unlocks video semantics through subtitle analysis, performing progressive three-stage retrieval - domain classification, topic extraction, and relevance matching - to identify task-relevant tutorial videos. Second, a fully automated annotation pipeline built on an inverse dynamics paradigm feeds consecutive keyframes enhanced with UI element detection into VLMs, inferring the required planning and grounding knowledge that are injected into the agent's corresponding modules to address both manifestations of domain bias. Extensive experiments on OSWorld demonstrate GUIDE's generality as a plug-and-play component for both multi-agent systems and single-model agents. It consistently yields over 5% improvements and reduces execution steps - without modifying any model parameters or architecture - validating GUIDE as an architecture-agnostic enhancement to bridge GUI agent domain bias.

---

## 论文详细总结（自动生成）

### 论文总结：GUIDE —— 通过实时网页视频检索解决 GUI 智能体的领域偏差

#### 1. 核心问题与研究动机
*   **核心问题**：现有的 GUI 智能体（基于视觉语言模型 VLM）虽然具备通用的界面理解能力，但在面对特定专业软件（如 GIMP、LibreOffice）时存在显著的**领域偏差（Domain Bias）**。
*   **偏差表现**：
    1.  **规划偏差（Planning-level）**：不熟悉特定软件的操作流（例如：在 GIMP 中调整亮度是在“颜色”菜单而非“图像”菜单）。
    2.  **定位偏差（Grounding-level）**：不熟悉特定软件的 UI 布局和控件样式，难以精准定位元素。
*   **研究背景**：传统的微调或手动标注成本高、覆盖面窄且难以跟上软件更新。本文旨在利用互联网上海量的视频教程作为知识源，实现智能体的自主学习和即插即用式增强。

#### 2. 方法论：GUIDE 框架
GUIDE 是一个无需训练、架构无关的框架，由三个协同工作的智能体组成：
*   **检索智能体（Retrieval Agent）**：
    *   **字幕驱动的 Video-RAG**：利用视频字幕作为语义桥梁。
    *   **三阶段过滤**：1) **领域分类**（通过字幕判断是否为实际操作演示）；2) **主题提取**（结合标题和字幕提炼核心操作）；3) **相关性匹配**（双锚点评分，确保视频与任务高度相关）。
*   **标注智能体（Annotation Agent）**：
    *   **逆动力学范式（Inverse Dynamics）**：给定连续关键帧 $s_t, s_{t+1}$，推断其间的操作 $a_t$。
    *   **多模态输入**：结合关键帧、OmniParser 提取的 UI 元素图、视频主题和字幕上下文。
    *   **知识分解**：将推断结果转化为**规划知识**（操作流、注意事项）和**定位知识**（关键 UI 元素的视觉描述与功能预测）。
*   **集成方式（GUI Agent Integration）**：
    *   **即插即用**：将提取的自然语言知识注入智能体的 Prompt 中。支持单模型智能体（统一 Prompt）和多智能体系统（按职责分发知识）。

#### 3. 实验设计
*   **数据集/场景**：使用 **OSWorld** 基准测试，包含 369 个真实世界的计算机任务（排除 8 个依赖 Google Drive 的任务，实测 361 个）。
*   **覆盖领域**：涵盖 Chrome、GIMP、LibreOffice (Calc/Impress/Writer)、VS Code、VLC、Thunderbird 等 10 个应用领域。
*   **对比对象**：
    *   **基础模型**：Seed-1.8、Qwen3-VL-8B、AgentS3（多智能体系统）。
    *   **对比方法**：Baseline（无标注）、仅注入规划知识、注入规划+定位知识。同时与同类工作 **Watch & Learn** 进行对比。

#### 4. 资源与算力
*   **模型调用**：标注流水线主要使用 **GPT-5.1**（用于逆动力学标注和知识分解），检索阶段使用 **GPT-4.1** 和 **GPT-4.1-mini**。
*   **本地工具**：使用 Whisper (base) 进行语音转文字，OmniParser 进行 UI 元素检测。
*   **算力说明**：论文**未明确提及**具体的 GPU 训练集群配置，因为该方法是**无需训练（Training-free）**的。
*   **成本分析**：论文提供了详细的 API 成本核算，完成整个 OSWorld 基准测试的 API 总花费约为 **114.6 美元**（平均每个视频标注成本约 0.25 美元）。

#### 5. 实验数量与充分性
*   **实验规模**：在 361 个任务上进行了完整测试，检索覆盖率达 82.8%。
*   **消融实验**：
    *   针对知识通道（规划 vs 定位）的消融。
    *   针对定位元素数量（$k$ 值）的影响分析。
    *   针对不同标注模型（GPT-5.1, Qwen3, Seed 等）的性能对比。
*   **质量评估**：引入了人工评估（3 名标注员），对视频分类准确率、主题提取质量和“有意义帧”过滤效果进行了量化验证。
*   **评价**：实验设计全面，涵盖了不同架构的智能体，对比实验和消融实验充分，客观地展示了知识注入带来的增量收益。

#### 6. 主要结论与发现
*   **性能提升**：GUIDE 在不同架构上稳定提升了 **4.5%–7.5%** 的成功率。
*   **瓶颈识别**：**规划偏差**是主要瓶颈，规划知识贡献了约 85%-91% 的性能增益；定位知识在 UI 复杂的领域（如 GIMP, Calc）具有显著的补充作用。
*   **效率优化**：注入知识后，智能体在成功任务中的执行步数有所减少（如 VLC 减少 5 步），证明了操作更加精准。
*   **通用性**：证明了互联网视频可以作为 GUI 智能体持续进化的廉价且高质量的知识库。

#### 7. 优点与亮点
*   **即插即用**：无需修改模型参数或架构，对开源和闭源模型均有效。
*   **字幕驱动创新**：相比仅依赖标题的检索，字幕驱动的 Video-RAG 极大地提高了检索的查准率（分类准确率达 94.3%）。
*   **逆动力学标注**：通过 VLM 自动将视频转化为结构化知识，解决了视频数据难以直接被智能体利用的问题。
*   **鲁棒性设计**：知识以“参考”而非“指令”形式注入，允许智能体在环境发生变化时优先信任实时视觉观察。

#### 8. 不足与局限
*   **检索依赖性**：如果检索到的视频与任务目标存在细微偏差（如版本差异或操作路径不同），可能会误导智能体。
*   **视频格式限制**：对于非录屏类教程（如动画演示或纯文字幻灯片视频），标注效果较差。
*   **实时性开销**：虽然减少了操作步数，但由于 Prompt 变长，单步推理的延迟略有增加（平均增加约 2.1 秒）。
*   **覆盖范围**：对于跨应用程序的复杂长程任务，目前互联网视频的覆盖率仍有提升空间。

（完）
