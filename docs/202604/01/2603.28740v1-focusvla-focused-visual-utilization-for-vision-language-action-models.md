---
title: "FocusVLA: Focused Visual Utilization for Vision-Language-Action Models"
title_zh: FocusVLA：视觉-语言-动作模型的聚焦视觉利用
authors: "Yichi Zhang, Weihao Yuan, Yizhuo Zhang, Xidong Zhang, Jia Wan"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28740v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 改进视觉-语言-动作模型中的动作生成
tldr: FocusVLA 针对视觉-语言-动作（VLA）模型中存在的架构偏见、视觉 Token 过多及任务无关噪声等瓶颈，提出了一种增强视觉利用率的新范式。通过模态级联注意力和聚焦注意力机制，该模型能够动态筛选并聚焦于任务相关的视觉细节，从而有效抑制噪声并强化视觉到动作的映射。实验证明，该方法在多种机器人任务中显著提升了操作精度和收敛速度。
selection_source: carryover_cache
motivation: 现有的VLA模型由于架构偏见和视觉信息冗余，往往忽略关键视觉细节并受到无关噪声干扰，限制了动作生成的质量。
method: 引入了模态级联注意力以消除学习捷径，并结合聚焦注意力机制动态选择任务相关的视觉补丁并抑制噪声。
result: 在模拟和真实世界基准测试中，FocusVLA 在灵巧操作任务上表现优异，且显著提高了训练效率和任务成功率。
conclusion: 本研究证明了优化视觉信息的利用方式比提升视觉表征质量对 VLA 模型更为关键，FocusVLA 为此提供了高效的架构方案。
---

## 摘要
视觉-语言-动作 (VLA) 模型通过将策略建立在丰富的视觉-语言信息之上来改进动作生成。然而，当前的自回归策略受限于三个瓶颈：(1) 架构偏差导致模型忽视视觉细节，(2) 过多的视觉 token 使得注意力难以聚焦在正确区域，以及 (3) 与任务无关的视觉信息引入了大量噪声——这些因素共同严重损害了动作质量。在本文中，我们研究了如何有效地利用不同的视觉表示进行动作生成。为此，我们首先通过实验验证了上述问题，并表明 VLA 的性能主要受限于视觉信息的利用方式，而非视觉表示的质量。基于这些见解，我们提出了 FocusVLA，这是一种引导模型注意力转向任务相关视觉区域的新型范式，从而有效地连接视觉与动作。具体而言，我们首先提出了模态级联注意力 (Modality Cascaded Attention) 以消除捷径路径，从而迫使 VLA 模型依赖任务相关的视觉细节来生成动作。此外，我们提出了聚焦注意力 (Focus Attention)，它动态选择任务相关的视觉补丁 (patches) 以控制信息量，同时显式调节其影响力以抑制任务无关的噪声。在模拟和真实世界机器人基准测试上的广泛实验表明，FocusVLA 不仅能有效利用视觉细节执行灵巧操作，还能在各种任务中显著提高性能并加速收敛。

## Abstract
Vision-Language-Action (VLA) models improve action generation by conditioning policies on rich vision-language information. However, current auto-regressive policies are constrained by three bottlenecks: (1) architectural bias drives models to overlook visual details, (2) an excessive number of visual tokens makes attention difficult to focus on the correct regions, and (3) task-irrelevant visual information introduces substantial noise - together severely impairing the quality of action. In this paper, we investigate how to effectively utilize different visual representations for action generation. To this end, we first empirically validate the above issues and show that VLA performance is primarily limited by how visual information is utilized, rather than by the quality of visual representations. Based on these insights, we introduce FocusVLA, a novel paradigm that directs the model's attention to task-relevant visual regions to effectively bridge vision to action. Specifically, we first propose Modality Cascaded Attention to eliminate shortcut pathways, thereby compelling VLA models to rely on task-relevant visual details for action generation. Furthermore, we propose Focus Attention, which dynamically selects task-relevant visual patches to control information quantity while explicitly modulating their influence to suppress task-irrelevant noise. Extensive experiments on both simulated and real-world robotic benchmarks demonstrate that FocusVLA not only effectively leverages visual details to perform dexterous manipulations, but also substantially improves performance and accelerates convergence across a variety of tasks.

---

## 论文详细总结（自动生成）

### FocusVLA：视觉-语言-动作模型的聚焦视觉利用——论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文指出，当前的自回归视觉-语言-动作（VLA）模型在处理精细操作任务时存在性能瓶颈。研究者发现，限制 VLA 性能的关键不在于视觉表征（Encoder）的质量，而在于**视觉信息的利用效率**。具体存在三个痛点：
*   **架构偏见（Architectural Bias）：** 现有的混合注意力机制存在“结构捷径”，导致模型倾向于依赖易学习的动作查询（Action Queries）而忽略复杂的视觉细节。
*   **信息过载（Information Overload）：** 视觉 Token 数量过多，稀释了模型的注意力，使其难以锁定关键操作区域。
*   **任务无关噪声（Task-Irrelevant Noise）：** 背景等无关信息引入了大量噪声，降低了信噪比，干扰了动作生成的准确性。

#### 2. 方法论：核心思想与关键技术
FocusVLA 提出了一种引导模型关注任务相关区域的新范式，核心技术包括：
*   **模态级联注意力（Modality Cascaded Attention）：** 放弃了将所有模态混合在一起的传统做法，改为解耦的级联设计。动作潜变量（Action Latent）依次与自身、动作查询、视觉特征进行独立的注意力交互。这种顺序强制模型在进行动作推理前必须先提取视觉细节，消除了结构捷径。
*   **聚焦注意力（Focus Attention）：**
    *   **补丁级聚焦（Patch-level Focus）：** 基于交叉注意力分数，动态筛选 Top-K 个最相关的视觉补丁，剔除冗余背景，减少信息过载。
    *   **通道级聚焦（Channel-level Focus）：** 引入元素级门控机制（Element-wise Gate），对视觉特征通道进行自适应加权，显式抑制任务无关的噪声通道。
*   **特征融合：** 结合了 VLM 的高层语义特征和视觉骨干网络（如 DINOv2/SigLIP）的底层空间细节，实现互补。

#### 3. 实验设计
*   **数据集/场景：**
    *   **LIBERO：** 包含 Spatial（空间）、Object（物体）、Goal（目标）、Long（长程）四个任务套件。
    *   **RoboTwin 2.0：** 具有强领域随机化的双臂协作模拟基准。
    *   **真实世界：** 在 Realman 机器人平台上进行了水果抓取、杯子堆叠、方块放置等实验。
*   **对比方法：** 包括 OpenVLA、OpenVLA-OFT、UniVLA、Spatial Forcing、VLA-Adapter-Pro、Pi0、Pi0.5、EVO-1 等 SOTA 模型。

#### 4. 资源与算力
论文明确记录了实验的算力消耗：
*   **LIBERO 实验：** 使用 4 台 NVIDIA A100 GPU，总 Batch Size 为 64。单权重模型训练 100k 步，多权重模型根据任务不同训练 10k 至 80k 步。
*   **RoboTwin 实验：** 使用 8 台 NVIDIA A100 GPU，总 Batch Size 为 64。简单设置训练 20k 步，困难设置训练 100k 步。
*   **模型规模：** 核心策略网络基于 Qwen2.5-0.5B，总可训练参数约为 342.7M。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比：** 在 LIBERO 的 40 个任务和 RoboTwin 的多种任务上进行了测试，涵盖了单任务（Multi-weights）和多任务（Single-weight）设置。
*   **消融实验：** 针对注意力模式（混合 vs 级联）、Token 数量（128 到 512）、门控粒度（单参数 vs 元素级）、视觉表征类型（VLM vs DS vs VGGT）进行了详尽的对比。
*   **可视化分析：** 通过注意力热图直观展示了 FocusVLA 如何从“发散”转向“聚焦”，增强了实验的可解释性。

#### 6. 主要结论与发现
*   **利用率胜过表征：** 实验证明，通过优化视觉利用方式（如级联注意力），即使使用相同的视觉特征，性能也能获得巨大提升。
*   **SOTA 性能：** FocusVLA 在 LIBERO 平均成功率达到 98.7%，在 0.5B 规模下超越了许多 7B 规模的模型。
*   **训练效率：** 相比 VLA-Adapter，FocusVLA 实现了 1.5 倍至 5 倍的训练收敛加速。
*   **鲁棒性：** 聚焦机制显著增强了模型在背景变化和目标变化下的指令遵循能力。

#### 7. 优点
*   **架构创新：** 级联注意力机制有效解决了 VLA 模型中长期存在的“视觉忽略”问题。
*   **高效轻量：** 以较小的参数量（0.5B）实现了极高的操作精度，具有很强的实用价值。
*   **即插即用：** 提出的聚焦注意力思想可以推广到其他自回归策略架构中。

#### 8. 不足与局限
*   **VLM 内部优化不足：** 目前的优化主要集中在策略网络（Policy）端，而 VLM 编码器内部的视觉利用仍有改进空间。
*   **泛化规模限制：** 虽然在特定基准上表现优异，但尚未在超大规模数据集（如 Open X-Embodiment）上进行全面预训练验证。
*   **真实世界挑战：** 真实世界实验的成功率仍受限于数据量，且在极端环境变化（如光照、背景剧烈变动）下仍存在失效风险。

（完）
