---
title: "POINTS-Seeker: Towards Training a Multimodal Agentic Search Model from Scratch"
title_zh: POINTS-Seeker：迈向从零开始训练多模态智能体搜索模型
authors: "Yikun Liu, Yuan Liu, Le Tian, Xiao Zhou, Jiangchao Yao, Yanfeng Wang, Weidi Xie"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14029v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 从零训练的多模态代理搜索模型，可与外部环境交互
tldr: 针对大型多模态模型受限于静态知识的问题，本文提出了POINTS-Seeker，探索从零开始构建多模态智能体搜索模型。研究引入了“智能体播种”阶段以激发自主行为，并针对长程交互中的信息过载瓶颈，创新性地提出了V-Fold自适应历史压缩方案，将历史上下文渲染至视觉空间以保留关键证据。POINTS-Seeker-8B在六个基准测试中均达到SOTA水平，有效解决了知识密集型视觉推理难题。
selection_source: fresh_fetch
motivation: 现有多模态模型受限于静态参数知识，且简单的工具挂载模式难以应对长程交互中的信息过载与证据定位难题。
method: 提出了“智能体播种”训练阶段以培养自主行为，并采用V-Fold方案将历史对话渲染为视觉信息进行自适应压缩。
result: POINTS-Seeker-8B在六个多样化的基准测试中持续超越现有模型，显著提升了长程、知识密集型任务的推理表现。
conclusion: 从零构建的智能体搜索模型结合高效的历史压缩机制，是解决复杂多模态视觉推理任务的有效途径。
---

## 摘要
虽然多模态大模型（LMMs）展现出令人印象深刻的视觉感知能力，但它们在认知上仍受限于其静态的参数化知识。为了超越这些界限，多模态搜索模型被用于主动与外部环境交互以进行证据检索。不同于现有的将通用 LMMs 与搜索工具作为模块化扩展进行简单改造的主流范式，我们探索了从零开始构建多模态智能体搜索模型的潜力。具体而言，我们做出了以下贡献：(i) 我们引入了“智能体播种”（Agentic Seeding），这是一个专门设计的阶段，旨在构建诱发智能体行为所需的基础先验；(ii) 我们发现了长程交互中的性能瓶颈，即不断增加的交互历史量削弱了模型定位真实证据的能力。为了缓解这一问题，我们提出了 V-Fold，这是一种自适应的历史感知压缩方案，它在高保真地保留近期对话轮次的同时，通过渲染将历史上下文折叠到视觉空间中；(iii) 我们开发了 POINTS-Seeker-8B，这是一款最先进的多模态智能体搜索模型，在六个不同的基准测试中始终优于现有模型，有效解决了长程、知识密集型视觉推理的挑战。

## Abstract
While Large Multimodal Models (LMMs) demonstrate impressive visual perception, they remain epistemically constrained by their static parametric knowledge. To transcend these boundaries, multimodal search models have been adopted to actively interact with the external environment for evidence retrieval. Diverging from prevailing paradigms that merely retrofit general LMMs with search tools as modular extensions, we explore the potential of building a multimodal agentic search model from scratch. Specifically, we make the following contributions: (i) we introduce Agentic Seeding, a dedicated phase designed to weave the foundational precursors necessary for eliciting agentic behaviors; (ii) we uncover a performance bottleneck in long-horizon interactions, where the increasing volume of interaction history overwhelms the model's ability to locate ground-truth evidence. To mitigate this, we propose V-Fold, an adaptive history-aware compression scheme that preserves recent dialogue turns in high fidelity while folding historical context into the visual space via rendering; and (iii) we develop POINTS-Seeker-8B, a state-of-the-art multimodal agentic search model that consistently outperforms existing models across six diverse benchmarks, effectively resolving the challenges of long-horizon, knowledge-intensive visual reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《POINTS-Seeker: Towards Training a Multimodal Agentic Search Model from Scratch》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大型多模态模型（LMMs）虽然感知能力强，但受限于训练时的静态知识，在处理实时或知识密集型查询时容易产生“幻觉”。
*   **核心问题**：
    1.  **范式局限**：现有搜索智能体多是在通用模型上通过微调“挂载”工具，智能体行为仅停留在表面任务层，缺乏深层认知基础。
    2.  **长程瓶颈**：在多轮搜索交互中，历史记录的堆积会导致“中间迷失”（Lost-in-the-Middle）效应，模型难以在冗长的上下文中精准定位关键证据。
*   **整体含义**：本文探索了**从零开始（From Scratch）**构建原生多模态搜索智能体的路径，旨在将自主搜索和推理能力内化为模型的底层属性。

### 2. 方法论：核心思想与关键技术
*   **智能体播种（Agentic Seeding）**：在预训练之后、正式微调之前引入的一个中间训练阶段。通过注入多模态推理数据、文本推理（数学/代码）和ReAct风格的文本智能体轨迹，在模型形成期植入系统2思维（慢思考）和规划能力。
*   **自动化数据合成**：
    *   **VQA重构**：从现有数据集提取实体，生成去偏查询，并配对搜索获取的图像。
    *   **轨迹生成与过滤**：利用Claude-4.5-Sonnet生成包含`<think>`、`<tool_call>`、`<tool_response>`和`<answer>`的结构化轨迹，并由Qwen3-235B作为裁判进行正确性过滤。
*   **训练流水线**：
    *   **SFT**：对齐工具调用格式。
    *   **RL（强化学习）**：采用**GRPO（群体相对策略优化）**算法，基于结果（准确性、格式、工具调用规范）进行奖励，鼓励模型自主探索最优搜索路径。
*   **V-Fold（自适应历史感知视觉压缩）**：
    *   **核心逻辑**：当交互历史超过阈值（如8k tokens）时，保留最近的$k$轮对话为高保真文本，而将更早的“陈旧”观测结果（如长网页内容）通过渲染引擎转为图像，再映射为视觉Token。
    *   **优势**：将$O(L)$的文本长度转为固定且较小的视觉Token量，既保留了原始证据的视觉可达性，又避免了注意力稀释。

### 3. 实验设计
*   **数据集/场景**：涵盖了6个极具挑战性的多模态基准测试：
    *   **MMSearch / MMSearch-Plus**：多模态搜索能力。
    *   **FVQA / SimpleVQA / LiveVQA**：知识密集型和实时事实性问答。
    *   **BrowseComp-VL (BC-VL)**：复杂网页浏览与综合推理。
*   **对比方法**：
    *   **直接回答**：GPT-5, Gemini-2.5 Flash, Claude-4-Sonnet, Qwen3-VL。
    *   **RAG/智能体工作流**：GPT-4o, Gemini-2.5 Flash等。
    *   **开源智能体模型**：MMSearch-R1, WebWatcher, DeepEyesV2, Vision-DeepResearch等。

### 4. 资源与算力
*   **模型底座**：初始化自Qwen2-VL-ViT和Qwen3-8B-Base。
*   **训练细节**：
    *   **上下文长度**：最大支持32k tokens。
    *   **批大小**：全局Batch Size为64。
    *   **RL配置**：每个样本进行8次Rollout，训练60步。
*   **算力说明**：文中提到使用了内部开发的类Megatron框架进行训练，并使用SGLang加速推理，但**未明确说明具体的GPU型号（如H100/A100）数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：在6个主流基准上进行了全面测试，并针对核心设计（播种阶段、SFT、RL、V-Fold）进行了详尽的**消融实验**。
*   **深度分析**：
    *   针对长程交互（>10轮）构建了“Hard”子集，专门验证V-Fold的有效性。
    *   分析了工具调用的分布（Web Search使用率最高）。
    *   测试了模型在通用视觉任务（MMMU, MathVista等）上的表现，证明了智能体训练不仅没导致能力退化，反而提升了通用推理。
*   **评价**：实验设计客观、对比维度全面，通过多模型、多场景的横向对比证明了其方法的稳健性。

### 6. 主要结论与发现
*   **原生能力更强**：从零开始的“智能体播种”比后期补丁式的微调更能提升模型在复杂任务中的逻辑一致性。
*   **视觉压缩有效性**：V-Fold在BC-VL等长程任务中将响应长度缩短了约25%，同时提升了约1.8%的准确率，证明了“以视觉代文本”压缩历史的可行性。
*   **SOTA表现**：POINTS-Seeker-8B在所有六个基准上均达到或超越了现有最强模型（包括部分参数量更大的模型）。

### 7. 优点
*   **创新性压缩机制**：V-Fold巧妙地利用了多模态模型的视觉处理能力来解决长文本注意力稀释问题，比单纯的摘要或截断更保真。
*   **训练范式领先**：提出了“智能体播种”概念，为构建原生智能体模型提供了新思路。
*   **数据效率高**：仅需5k条渲染轨迹即可让模型学会V-Fold机制，展现了极强的泛化能力。

### 8. 不足与局限
*   **工具多样性有限**：目前仅支持网页搜索、图像搜索和网页访问三种工具，尚未扩展到地图、学术数据库等更专业的API。
*   **上下文窗口限制**：虽然有V-Fold，但物理上下文仍限制在32k，对于极度复杂的超长程任务（如连续浏览数十个网页）可能仍有力不从心之处。
*   **RL规模受限**：受限于搜索API的配额和成本，强化学习阶段的训练规模相对较小，可能尚未完全挖掘出RL的潜力。

（完）
