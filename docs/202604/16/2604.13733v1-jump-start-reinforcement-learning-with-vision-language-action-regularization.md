---
title: Jump-Start Reinforcement Learning with Vision-Language-Action Regularization
title_zh: 利用视觉-语言-动作正则化引导强化学习
authors: "Angelo Moroncelli, Roberto Zanetti, Marco Maccarini, Loris Roveda"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13733v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合视觉-语言-动作正则化的强化学习
tldr: "针对强化学习在长程任务中探索效率低和视觉语言模型（VLA）在精确控制上的局限，本文提出VLAJS方法。该方法将VLA作为临时的动作引导源，通过方向性动作一致性正则化辅助PPO算法进行早期探索。VLAJS无需演示数据，通过稀疏且随时间衰减的引导，显著提升了学习效率，在多项机器人操纵任务中减少了50%以上的环境交互，并成功实现了真机部署。"
selection_source: fresh_fetch
motivation: 强化学习在处理稀疏奖励的长程任务时探索效率低下，而现有的VLA模型虽具备推理能力但难以直接用于高频精确控制。
method: 提出VLAJS框架，在PPO算法中引入方向性动作一致性正则化，利用VLA提供的稀疏动作建议引导早期探索并随训练过程逐渐退火。
result: "在六项模拟操纵任务中，VLAJS的样本效率显著优于基线模型，交互次数减少超过50%，并在真实机器人上验证了零样本迁移能力。"
conclusion: VLAJS成功结合了VLA的通用推理能力与RL的高频控制优势，为解决复杂机器人任务提供了一种高效的预训练模型引导方案。
---

## 摘要
强化学习 (RL) 能够实现机器人操纵的高频闭环控制，但由于探索效率低下和信用分配困难，将其扩展到具有稀疏或不完善奖励的长程任务仍然具有挑战性。视觉-语言-动作 (VLA) 模型利用大规模多模态预训练来提供通用的任务级推理，但目前的局限性阻碍了它们在快速且精确操纵中的直接应用。在本文中，我们提出了视觉-语言-动作引导 (VLAJS)，这是一种将稀疏的 VLA 指导与同策略 RL 相结合的方法，旨在提高探索和学习效率。VLAJS 将 VLA 视为高层动作建议的瞬态来源，用于引导早期探索并改善信用分配，同时保留 RL 基于状态的高频控制。我们的方法通过方向性动作一致性正则化增强了近端策略优化 (PPO)，在训练早期使 RL 智能体的动作与 VLA 指导进行软对齐，而无需强制执行严格模仿、不需要演示数据，也不依赖于持续的教师查询。VLA 指导以稀疏方式应用并随时间退火，使智能体能够在线适应并最终超越指导策略。我们在六个具有挑战性的仿真操纵任务（提升、取放、销钉重定向、销钉插入、拨动和推动物体）中评估了 VLAJS，并在真实的 Franka Panda 机器人上验证了其中一部分任务。VLAJS 在样本效率方面始终优于 PPO 和蒸馏式基准方法，在多个任务中减少了超过 50% 的环境交互需求。现实世界实验证明了零样本的仿真到现实迁移，以及在杂乱环境、物体变化和外部扰动下的鲁棒执行能力。

## Abstract
Reinforcement learning (RL) enables high-frequency, closed-loop control for robotic manipulation, but scaling to long-horizon tasks with sparse or imperfect rewards remains difficult due to inefficient exploration and poor credit assignment. Vision-Language-Action (VLA) models leverage large-scale multimodal pretraining to provide generalist, task-level reasoning, but current limitations hinder their direct use in fast and precise manipulation. In this paper, we propose Vision-Language-Action Jump-Starting (VLAJS), a method that bridges sparse VLA guidance with on-policy RL to improve exploration and learning efficiency. VLAJS treats VLAs as transient sources of high-level action suggestions that bias early exploration and improve credit assignment, while preserving the high-frequency, state-based control of RL. Our approach augments Proximal Policy Optimization (PPO) with a directional action-consistency regularization that softly aligns the RL agent's actions with VLA guidance during early training, without enforcing strict imitation, requiring demonstrations, or relying on continuous teacher queries. VLA guidance is applied sparsely and annealed over time, allowing the agent to adapt online and ultimately surpass the guiding policy. We evaluate VLAJS on six challenging manipulation tasks: lifting, pick-and-place, peg reorientation, peg insertion, poking, and pushing in simulation, and validate a subset on a real Franka Panda robot. VLAJS consistently outperforms PPO and distillation-style baselines in sample efficiency, reducing required environment interactions by over 50% in several tasks. Real-world experiments demonstrate zero-shot sim-to-real transfer and robust execution under clutter, object variation, and external perturbations.

---

## 论文详细总结（自动生成）

以下是对论文《Jump-Start Reinforcement Learning with Vision-Language-Action Regularization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对机器人操纵任务中强化学习（RL）面临的两大难题：**长程任务（Long-horizon）中的探索效率低下**以及**奖励函数设计不完善导致的信用分配困难**。
虽然现有的视觉-语言-动作（VLA）模型（如 OpenVLA、Octo）具备强大的语义推理和通用化能力，但由于推理延迟高、控制频率低且缺乏精确的闭环反馈，难以直接用于高频、高精度的机器人控制。本文旨在结合两者的优势：利用 VLA 的语义先验来“引导”（Jump-Start）RL 的早期探索，同时保持 RL 在高频状态控制下的精确性和鲁棒性。

### 2. 方法论：VLAJS 框架
核心思想是将 VLA 模型作为**稀疏、瞬态的辅助引导源**，而非持续的模仿对象。
*   **稀疏 VLA 查询与时间离散化**：为了降低计算开销，VLA 仅在每个 Rollout 中被稀疏查询（如仅占总步数的 20%）。查询到的动作增量通过线性插值或球面线性插值（SLERP）分解到后续的多个控制步中。
*   **方向性动作一致性损失（核心技术）**：不同于传统的策略蒸馏（使用 MSE 损失强制匹配动作大小），VLAJS 采用**余弦失配损失（Cosine Misalignment Loss）**。它只要求 RL 智能体的动作方向与 VLA 建议一致，允许智能体自行学习动作幅值。这解决了 VLA 动作尺度与高频 RL 控制不匹配的问题。
*   **基于奖励趋势的引导退火与停用**：引入自适应机制，根据奖励增长情况动态减少 VLA 查询频率。一旦智能体表现稳定（如平均奖励超过阈值），则永久关闭 VLA 引导，确保智能体最终能超越“教师”并减少计算开销。

### 3. 实验设计
*   **场景与任务**：在 **ManiSkill** 仿真环境中设置了六项任务：抓取方块、取放方块、销钉重定向、销钉插入、拨动方块和推动方块。
*   **实验设置**：
    *   **用例 1（长程任务）**：将任务步数延长 10 倍，测试探索能力。
    *   **用例 2（次优奖励设计）**：简化奖励函数（如仅提供稀疏的成功信号），测试信用分配能力。
*   **基准方法（Benchmark）**：
    *   **PPO**：标准的同策略强化学习。
    *   **Sparse RPD**：基于 MSE 损失的稀疏策略蒸馏（Refined Policy Distillation）。
    *   **VLAJS (RPD)**：使用本文的跳启机制但保留 MSE 损失的消融版本。
*   **真机验证**：在 Franka Panda 机器人上进行了零样本（Zero-shot）迁移实验，涵盖抓取、放置、重定向等任务。

### 4. 资源与算力
*   论文提到研究得到了瑞士 AI 倡议的支持，并使用了 **CSCS（瑞士国家超算中心）的 Alps 系统**进行计算。
*   **具体型号与时长**：文中未明确给出具体的 GPU 数量（如 A100/H100 的具体个数）或总训练小时数。但提到 VLA 查询会带来显著的墙钟时间（Wall-clock）开销，因此稀疏查询是维持训练可行性的关键。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个仿真任务，每个实验运行了 **6 个随机种子**，并提供了 95% 的置信区间。
*   **消融与对比**：对比了不同的 VLA 教师（OpenVLA、Octo）、不同的相机视角以及不同的损失函数（方向性 vs MSE）。
*   **充分性评价**：实验设计较为充分，不仅在仿真中验证了性能提升（样本效率提升 50% 以上），还通过真机实验证明了其在复杂环境（如人类干扰、物体变化）下的鲁棒性，客观地展示了该方法在解决 RL 探索难题上的有效性。

### 6. 主要结论与发现
*   **效率大幅提升**：VLAJS 在多个任务中将达到相同成功率所需的环境交互次数减少了 50% 以上。
*   **超越教师**：由于引导是瞬态的，RL 智能体在引导停止后能继续优化，最终成功率远高于原始 VLA 模型。
*   **方向引导优于动作匹配**：方向性损失比 MSE 损失更鲁棒，尤其是在 VLA 建议不精确或控制频率不一致时。
*   **零样本迁移**：训练后的状态策略（State-based policy）配合视觉检测器，能直接部署到真实机器人，且对视觉扰动（如人手进入画面）比纯视觉 VLA 模型更鲁棒。

### 7. 优点
*   **灵活性高**：不要求 VLA 教师非常精确，即使是弱教师也能提供有效的探索偏置。
*   **计算友好**：通过稀疏查询和自动停用机制，平衡了基础模型的高推理成本与 RL 的大规模交互需求。
*   **鲁棒性强**：方向性正则化避免了对教师动作幅值的过度拟合，使智能体保留了自主探索空间。

### 8. 不足与局限
*   **依赖 VLA 质量**：虽然对教师要求不高，但仍需 VLA 提供“起码正确”的方向引导；若 VLA 在特定任务上完全失效，引导效果会大打折扣。
*   **系统复杂性**：在训练流程中集成大型 VLA 模型增加了工程实现难度和显存压力。
*   **状态观测限制**：RL 策略目前主要依赖特权状态（Privileged State），在真实世界部署时需要额外的视觉检测器（如 YOLO）来获取物体位姿，这可能引入感知误差。

（完）
