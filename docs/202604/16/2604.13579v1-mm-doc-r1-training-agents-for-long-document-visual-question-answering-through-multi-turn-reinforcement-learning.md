---
title: "MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning"
title_zh: MM-Doc-R1：通过多轮强化学习训练长文档视觉问答智能体
authors: "Jiahang Lin, Kai Hu, Binghai Wang, Yuhao Zhou, Zhiheng Xi, Honglin Guo, Shichun Liu, Junzhe Wang, Shihan Dou, Enyu Zhou, Hang Yan, Zhenhua Han, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13579v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 使用多轮强化学习处理长文档视觉问答的智能体视觉感知工作流
tldr: 针对长文档视觉问答中复杂多跳查询的挑战，本文提出MM-Doc-R1框架。该框架采用具备视觉感知能力的智能体工作流，通过迭代式信息发现与综合来解决问题。核心贡献是提出了基于相似度的策略优化（SPO）算法，通过轨迹相似度加权平均奖励来改进多轮强化学习中的基准估计，解决了GRPO在多轮场景下的偏差问题。实验表明，该方法在MMLongbench-Doc基准上显著优于现有模型。
selection_source: fresh_fetch
motivation: 传统RAG系统因单次检索限制，难以应对长文档中需要多步推理和视觉感知的复杂多跳问答任务。
method: 引入具备视觉意识的智能体工作流，并提出SPO算法，利用轨迹间的语义相似度来提供更准确的多轮强化学习基准估计。
result: "MM-Doc-R1在MMLongbench-Doc基准上超越前人工作10.4%，且SPO算法相比GRPO在不同规模模型上均有5%以上的性能提升。"
conclusion: 通过集成智能体迭代搜索与改进的多轮强化学习算法，本研究显著提升了长文档视觉问答的性能与学习稳定性。
---

## 摘要
传统的检索增强生成 (RAG) 系统由于采用单次检索，在处理长文档中的复杂多跳查询时往往面临挑战。我们提出了 MM-Doc-R1，这是一个新颖的框架，它采用一种具备智能体能力且视觉感知的流程，通过迭代的信息发现与综合来解决长文档视觉问答问题。为了激励智能体的信息搜索能力，我们提出了基于相似性的策略优化 (SPO)，旨在解决现有如 GRPO 等多轮强化学习 (RL) 算法中的基准估计偏差问题。我们的核心见解是，在多轮强化学习中，两条轨迹的语义越相似，其共享的基准估计就越准确。利用这一点，SPO 通过对多条轨迹的奖励进行相似度加权平均来计算更精确的基准，而不像 GRPO 那样不恰当地将初始状态的基准应用于所有中间状态。这为我们的智能体提供了更稳定、更准确的学习信号，从而实现了超越 GRPO 的卓越训练性能。我们在 MMLongbench-Doc 基准测试上的实验表明，MM-Doc-R1 的表现优于之前的基准模型 10.4%。此外，SPO 展现出优于 GRPO 的性能，在 Qwen3-8B 上提升了 5.0%，在 Qwen3-4B 上提升了 6.1%。这些结果突显了我们的集成框架和新颖训练算法在推动复杂长文档视觉问答技术达到当前领先水平方面的有效性。

## Abstract
Conventional Retrieval-Augmented Generation (RAG) systems often struggle with complex multi-hop queries over long documents due to their single-pass retrieval. We introduce MM-Doc-R1, a novel framework that employs an agentic, vision-aware workflow to address long document visual question answering through iterative information discovery and synthesis. To incentivize the information seeking capabilities of our agents, we propose Similarity-based Policy Optimization (SPO), addressing baseline estimation bias in existing multi-turn reinforcement learning (RL) algorithms like GRPO. Our core insight is that in multi-turn RL, the more semantically similar two trajectories are, the more accurate their shared baseline estimation becomes. Leveraging this, SPO calculates a more precise baseline by similarity-weighted averaging of rewards across multiple trajectories, unlike GRPO which inappropriately applies the initial state's baseline to all intermediate states. This provides a more stable and accurate learning signal for our agents, leading to superior training performance that surpasses GRPO. Our experiments on the MMLongbench-Doc benchmark show that MM-Doc-R1 outperforms previous baselines by 10.4%. Furthermore, SPO demonstrates superior performance over GRPO, boosting results by 5.0% with Qwen3-8B and 6.1% with Qwen3-4B. These results highlight the effectiveness of our integrated framework and novel training algorithm in advancing the state-of-the-art for complex, long-document visual question answering.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《MM-Doc-R1: Training Agents for Long Document Visual Question Answering through Multi-turn Reinforcement Learning》** 的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**长文档视觉问答（Long Document Visual Question Answering）**中的复杂多跳（Multi-hop）查询问题展开研究。
*   **研究动机**：传统的检索增强生成（RAG）系统通常采用“单次检索”模式，仅依赖初始查询进行匹配，难以处理需要跨页面、跨模态（文字与图表结合）多次跳转推理的复杂问题。
*   **核心挑战**：如何在长达数十甚至上百页的文档中，让智能体（Agent）学会像人类一样通过“查目录-搜关键词-读具体页面”的迭代过程来收集证据，并解决多轮强化学习（RL）中基准估计不准导致的训练不稳定问题。

### 2. 方法论：核心思想与关键技术
论文提出了 **MM-Doc-R1** 框架，包含一个智能体工作流和一种改进的强化学习算法。

#### **A. 智能体工作流（Agentic Workflow）**
1.  **文档解析**：利用 OCR 工具将 PDF 转换为 Markdown，提取目录（TOC）和页面截图。
2.  **规划器（Planner）**：基于目录生成初始研究计划，将复杂问题拆解为子查询。
3.  **搜索者（Seeker）**：核心迭代模块，配备两个工具：
    *   `search`：基于 BM25 的文本关键词检索。
    *   `read`：调用视觉语言模型（VLM，如 Qwen2.5-VL）对特定页面进行深度视觉理解。
4.  **回答者（Answer Agent）**：整合所有检索到的证据生成最终答案。

#### **B. 核心算法：基于相似度的策略优化（SPO）**
针对 DeepSeek 提出的 GRPO 算法在多轮 RL 场景下的缺陷进行了改进：
*   **GRPO 的局限**：GRPO 假设同一组（Group）内的所有轨迹共享初始状态的基准（Baseline），但在多轮对话中，不同轨迹会迅速发散，导致基准估计出现巨大偏差。
*   **SPO 核心思想**：认为**语义越相似的轨迹，其共享的基准估计越准确**。
*   **技术细节**：SPO 使用冻结的 BGE-M3 模型计算轨迹间的语义相似度，并以此作为权重对组内奖励进行加权平均，从而为每个轨迹计算一个动态、内容感知的基准。这降低了优势函数（Advantage）估计的方差，使学习更稳定。

### 3. 实验设计
*   **数据集**：主要在 **MMLongbench-Doc**（包含 1,082 个复杂问题）上进行评估。
*   **训练数据**：使用 LongDocURL 的子集进行强化学习训练。
*   **对比基准（Baselines）**：
    *   文本 RAG：BM25, BGE-M3。
    *   多模态 RAG：ColQwen2.5-7B。
    *   智能体系统：Mdoc Agent, M3doc RAG。
*   **评估指标**：准确率（ACC）、F1 分数、召回率（Recall）以及针对不同证据类型（文本、表格、图表等）的细分得分。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H100 GPU** 进行分布式训练。
*   **软件框架**：基于 `verl` 强化学习框架实现。
*   **训练细节**：
    *   模型规模：Qwen3-4B 和 Qwen3-8B。
    *   超参数：Rollout 长度为 4，Batch Size 为 4，学习率 5e-7，KL 系数为 0.005。
    *   推理工具：`read` 工具调用了 Qwen2.5-7B-VL（Zero-shot 模式）。

### 5. 实验数量与充分性
*   **实验规模**：在 1,082 个测试样本上进行了全面测试，涵盖了单跳、多跳及不可回答（Unanswerable）等多种场景。
*   **消融实验**：非常充分。作者分别验证了目录（TOC）、页面 OCR、VLM 阅读模块对最终性能的贡献。
*   **算法对比**：直接对比了 SPO 与 GRPO 在不同参数规模（4B/8B）下的收敛速度和最终精度。
*   **客观性**：实验设置了“人类表现”和“真值证据上限（Upper Bound）”作为参照，对比方法涵盖了当前最先进的开源模型和 Agent 框架，评估较为公正。

### 6. 主要结论与发现
1.  **性能飞跃**：MM-Doc-R1 在 MMLongbench-Doc 上比之前的最佳基准提升了 **10.4%** 的准确率。
2.  **多跳能力强**：在需要多页证据的问题上，性能提升高达 **10.0%**。
3.  **SPO 算法优越性**：相比 GRPO，SPO 在 4B 模型上提升了 **6.1%**，在 8B 模型上提升了 **5.0%**，且训练曲线更平滑、收敛更快。
4.  **高召回率**：仅需平均阅读 3.35 页即可达到 66.3% 的召回率，效率远高于传统检索。

### 7. 优点（亮点）
*   **算法创新**：SPO 解决了多轮 RL 中轨迹发散导致的基准估计偏差问题，具有较强的理论和实践意义。
*   **视觉感知**：通过将 VLM 作为 Agent 的“阅读工具”，有效解决了长文档中图表、布局等非文本信息的处理难题。
*   **测试时间扩展（Test-time Scaling）**：通过增加推理时的计算量（多轮迭代）换取了显著的精度提升，符合当前大模型发展的趋势。

### 8. 不足与局限
*   **解析依赖**：系统高度依赖初始 OCR 和目录提取的质量，如果文档结构极度混乱，性能会受限。
*   **推理延迟**：由于是多轮迭代 Agent，平均每题耗时约 109-154 秒，虽然在长文档分析场景中可接受，但不适用于实时性要求极高的任务。
*   **语言局限**：目前主要在英文基准上验证，跨语言（如中文或多语言混合）的鲁棒性尚待进一步研究。
*   **静态限制**：目前仅针对静态 PDF/图像，未涉及动态或交互式数字文档。

（完）
