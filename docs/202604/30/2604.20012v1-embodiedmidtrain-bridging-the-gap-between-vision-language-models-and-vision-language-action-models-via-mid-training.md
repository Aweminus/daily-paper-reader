---
title: "EmbodiedMidtrain: Bridging the Gap between Vision-Language Models and Vision-Language-Action Models via Mid-training"
title_zh: EmbodiedMidtrain：通过中期训练弥合视觉语言模型与视觉语言动作模型之间的差距
authors: "Yiyang Du, Zhanqiu Guo, Xin Ye, Liu Ren, Chenyan Xiong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20012v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 桥接视觉语言模型与智能体动作模型
tldr: 本研究针对视觉语言模型（VLM）与具身智能动作模型（VLA）之间的分布差异，提出了EmbodiedMidtrain框架。通过分析发现VLA数据在VLM分布中占据紧凑且分离的区域，研究者构建了一个轻量级邻近估计器，从大规模VLM池中筛选出与具身任务高度对齐的数据进行“中段训练”。实验证明，该方法能显著提升机器人在操纵任务中的表现，为VLA微调提供了更强的初始化，且在性能上可媲美更大规模的专家模型。
selection_source: fresh_fetch
motivation: 现有的VLA模型多直接使用未针对具身领域优化的通用VLM，导致两者之间存在显著的数据分布鸿沟，限制了下游任务性能。
method: 提出一种中段训练数据引擎，利用轻量级可学习邻近估计器从海量VLM数据中筛选出与VLA对齐的样本，在正式微调前对模型进行预热训练。
result: 在三个机器人操纵基准测试中，该方法一致提升了不同骨干模型的性能，达到了与更大规模专家模型相当的水平。
conclusion: 中段训练通过捕捉空间推理等关键对齐信号，为VLA微调提供了更优的初始化，是弥合通用视觉语言能力与具身动作能力差距的有效途径。
---

## 摘要
视觉语言动作模型（VLAs）从视觉语言模型（VLMs）中继承了视觉和语言能力，但大多数 VLA 是基于现成的、未针对具身领域进行适配的 VLM 构建的，这限制了它们的下游性能。在这项工作中，我们提出了 EmbodiedMidtrain，旨在弥合 VLM 与 VLA 之间的差距。我们首先刻画了两者之间的数据分布差异，结果表明 VLA 数据占据了与更广泛的 VLM 分布基本分离的紧凑区域，而对齐程度在不同 VLM 数据源之间以及同一数据源内部都存在显著差异。随后，我们构建了一个中期训练数据引擎，利用轻量级的可学习邻近度估计器从庞大的 VLM 池中筛选出与 VLA 最对齐的候选数据，并在下游 VLA 微调之前，使用这些精选的混合数据对 VLM 进行中期训练。在三个机器人操控基准测试上的实验表明，中期训练在不同的 VLM 骨干网络上均能持续提升性能，其结果可与专家级 VLA 以及具有更大模型规模和训练预算的现成 VLM 相媲美。进一步的分析表明，中期训练为 VLA 微调提供了更强大的初始化，其增益从训练初期就开始显现，并在整个训练过程中不断扩大。此外，该数据引擎捕捉到了数据集级和样本级的对齐信号，在保留 VLM 数据多样性的同时，更倾向于空间推理任务而非以文本为中心的任务。我们将发布所有代码、数据和模型，以供未来研究使用。

## Abstract
Vision-Language-Action Models (VLAs) inherit their visual and linguistic capabilities from Vision-Language Models (VLMs), yet most VLAs are built from off-the-shelf VLMs that are not adapted to the embodied domain, limiting their downstream performance. In this work, we propose EmbodiedMidtrain to bridge the gap between VLMs and VLAs. We first characterize the data distribution gap between them, showing that VLA data occupy compact regions that are largely separated from the broader VLM distribution, while the degree of alignment varies substantially both across and within VLM data sources. Then, we build a mid-training data engine that leverages a lightweight learnable proximity estimator to select the most VLA-aligned candidates from a large VLM pool, and mid-trains the VLM on this curated mixture before downstream VLA fine-tuning. Experiments on three robot manipulation benchmarks show that mid-training consistently improves performance across different VLM backbones, achieving results competitive with expert VLAs and off-the-shelf VLMs trained with larger model scale and training budgets. Further analysis reveals that mid-training provides a stronger initialization for VLA fine-tuning, with gains emerging from the earliest steps and widening throughout training. Moreover, the data engine captures both dataset-level and sample-level alignment signals, favoring spatial reasoning over text-centric tasks while preserving the diversity of the VLM data. We will release all code, data and models for future research.

---

## 论文详细总结（自动生成）

### 论文总结：EmbodiedMidtrain —— 通过中期训练弥合 VLM 与 VLA 的分布鸿沟

#### 1. 核心问题与整体含义（研究动机和背景）
视觉语言动作模型（VLA）通常直接采用现成的通用视觉语言模型（VLM）作为骨干网络。然而，通用 VLM 的预训练数据（如图像描述、视觉问答）与具身智能所需的 VLA 数据（如机器人操作轨迹）之间存在显著的**分布差异（Distribution Gap）**。
*   **研究动机**：作者发现 VLA 数据在表示空间中形成了一个紧凑且与通用 VLM 数据高度分离的区域。直接在 VLA 数据上微调未适配的 VLM，往往难以充分发挥 VLM 的潜力。
*   **核心目标**：提出一种名为 **EmbodiedMidtrain** 的中期训练（Mid-training）框架，在正式进行 VLA 微调之前，先通过精选的、与具身任务高度对齐的 VLM 数据对模型进行“预热”，从而提供更强的初始化状态。

#### 2. 方法论：核心思想与关键技术
该方法的核心是构建一个**中期训练数据引擎**，通过样本级筛选来重塑训练分布。
*   **分布分析**：利用最大均值差异（MMD）和 t-SNE 可视化量化 VLM 与 VLA 数据的距离，证实了分布不均性。
*   **邻近度估计器（Proximity Estimator）**：
    *   **核心思想**：将数据筛选建模为“领域成员分类”问题。
    *   **技术细节**：在冻结的 VLM 特征 $\phi(x)$ 之上训练一个轻量级的线性分类器 $f$。
    *   **公式**：得分函数定义为 $s(x) = \sigma(f(\phi(x)))$。根据密度比（Density Ratio）理论，该分类器输出的概率值代表了某个 VLM 样本属于 VLA 分布的可能性。
*   **中期训练流程**：
    1.  从大规模 VLM 池中筛选出得分最高的 $K$ 个样本（如 120 万条）。
    2.  在 VLM 骨干网络上进行全参数或部分参数的中期训练。
    3.  将中期训练后的模型作为初始化，进行下游 VLA 任务的微调。

#### 3. 实验设计
*   **评估基准（Benchmarks）**：
    *   **Calvin ABC-D**：测试机器人在不同场景配置下的长程操作泛化能力。
    *   **SimplerEnv Bridge**：真实到模拟（Real-to-Sim）的桌面操作任务。
    *   **Libero-10**：包含 10 个具有挑战性的长程操作任务。
*   **对比方法**：
    *   **专家 VLA 模型**：如 OpenVLA (7.7B)、$\pi_0$ (3.1B)。
    *   **现成 VLM 基线**：包括 Qwen2.5VL、Qwen3VL 系列（2B 到 30B）、Paligemma 系列、KosMos-2 等。
*   **骨干网络**：主要在 InternVL3.5-1B 和 Qwen3VL-2B 上验证。

#### 4. 资源与算力
*   **算力提及情况**：论文提到了训练参数和步骤，但**未明确给出具体的 GPU 型号和总数量**。
*   **训练细节**：
    *   **中期训练**：全局 Batch Size 为 256，训练 5,000 步。
    *   **VLA 微调**：全局 Batch Size 为 256，训练 16,000 步。
    *   **框架**：使用了 LLaMA-Factory 框架，并在 CMU FLAME 中心和 DeltaAI 集群上运行。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流机器人操纵基准，对比了超过 10 种不同规模和架构的模型。
*   **消融实验**：
    *   对比了“随机选择”与“邻近度选择”的效果。
    *   对比了不同的邻近度度量指标（如特征空间距离、困惑度 PPL、Delta PPL）。
    *   验证了数据筛选结果在不同架构（InternVL 到 Qwen）之间的**可迁移性**。
*   **充分性评价**：实验设计较为全面，不仅关注最终成功率，还分析了训练动力学（Training Dynamics）和数据多样性保持情况，论证过程客观且具有说服力。

#### 6. 主要结论与发现
*   **性能提升**：1.1B 规模的模型经过中期训练后，在 Calvin 等基准上超越了 7B-8B 规模的现成模型，甚至优于专门的专家 VLA。
*   **初始化优势**：中期训练提供的优势从微调的第一步就已显现，且随着训练进行，与基线的差距不断扩大。
*   **对齐信号**：邻近度估计器倾向于选择涉及**空间推理、坐标定位**的样本，而自动过滤掉纯文本识别（如书名识别）等无关样本。
*   **多样性保持**：筛选后的数据在保持具身对齐的同时，依然保留了 VLM 原始数据的广泛视觉分布，避免了过拟合到狭窄的机器人领域。

#### 7. 优点（亮点）
*   **轻量且高效**：无需改变模型架构，仅通过数据侧的优化即可实现显著的性能跨越。
*   **通用性强**：提出的数据引擎具有跨模型迁移能力，证明了 VLM 与 VLA 之间的对齐信号具有某种通用特征。
*   **深入的分布分析**：通过 MMD 和 t-SNE 提供了直观的理论支撑，解释了为什么简单的全量微调效果不佳。

#### 8. 不足与局限
*   **环境局限性**：所有实验均在**模拟环境**中完成，缺乏在真实物理机器人上的部署验证。
*   **候选池依赖**：中期训练的效果高度依赖于初始 VLM 候选池的质量，如果候选池中本身缺乏高质量的空间/具身相关数据，该方法的效果可能会受限。
*   **算力细节模糊**：虽然给出了训练步数，但缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
