---
title: "SketchVLM: Vision language models can annotate images to explain thoughts and guide users"
title_zh: SketchVLM：视觉语言模型可以通过标注图像来解释思路并引导用户
authors: "Brandon Collins, Logan Bolton, Hung Huy Nguyen, Mohammad Reza Taesiri, Trung Bui, Anh Totti Nguyen"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22875v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 迷宫导航和轨迹预测中的视觉推理
tldr: 针对现有视觉语言模型（VLM）仅能输出文本、难以直观解释推理过程的问题，本文提出了 SketchVLM。这是一个无需训练且模型无关的框架，使 VLM 能够生成可编辑的 SVG 覆盖层，通过在图像上绘图、标注来解释其推理逻辑。在迷宫导航、轨迹预测等七项任务中，该方法显著提升了推理准确率和标注质量，并支持人机协作。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型缺乏直观的视觉解释能力，导致用户难以验证其推理过程的正确性。
method: 提出一种无需训练的框架，利用 VLM 生成非破坏性、可编辑的 SVG 矢量图形覆盖在原图上进行标注和解释。
result: 在视觉推理和绘图任务中，该方法将准确率最高提升了 28.5 个百分点，标注质量提升了 1.48 倍。
conclusion: SketchVLM 通过视觉标注增强了模型的推理透明度和准确性，为更高效的人机协作提供了新途径。
---

## 摘要
在回答有关图像的问题时，人类会自然地通过指点、标记和绘图来解释其推理过程。相比之下，现代视觉语言模型（VLM）（如 Gemini-3-Pro 和 GPT-5）仅以文本形式响应，这使得用户难以验证。我们提出了 SketchVLM，这是一个无需训练且与模型无关的框架，它使 VLM 能够在输入图像上生成非破坏性、可编辑的 SVG 叠加层，从而以视觉方式解释其答案。在涵盖视觉推理（迷宫导航、落球轨迹预测和物体计数）和绘图（部件标注、连点成线以及在物体周围绘制形状）的七个基准测试中，相较于图像编辑和经过微调的草图绘制基准模型，SketchVLM 将视觉推理任务的准确率提高了多达 28.5 个百分点，标注质量提升了多达 1.48 倍，同时生成的标注与模型给出的答案更加一致。我们发现，单轮生成已经能够实现极高的准确率和标注质量，而多轮生成则为人类与 AI 的协作开辟了更多机会。交互式演示和代码可在 https://sketchvlm.github.io/ 获取。

## Abstract
When answering questions about images, humans naturally point, label, and draw to explain their reasoning. In contrast, modern vision-language models (VLMs) such as Gemini-3-Pro and GPT-5 only respond with text, which can be difficult for users to verify. We present SketchVLM, a training-free, model-agnostic framework that enables VLMs to produce non-destructive, editable SVG overlays on the input image to visually explain their answers. Across seven benchmarks spanning visual reasoning (maze navigation, ball-drop trajectory prediction, and object counting) and drawing (part labeling, connecting-the-dots, and drawing shapes around objects), SketchVLM improves visual reasoning task accuracy by up to +28.5 percentage points and annotation quality by up to 1.48x relative to image-editing and fine-tuned sketching baselines, while also producing annotations that are more faithful to the model's stated answer. We find that single-turn generation already achieves strong accuracy and annotation quality, and multi-turn generation opens up further opportunities for human-AI collaboration. An interactive demo and code are at https://sketchvlm.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《SketchVLM: Vision language models can annotate images to explain thoughts and guide users》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代视觉语言模型（VLM，如 GPT-5、Gemini-3-Pro）在处理图像相关问题时，通常仅能输出长段文本。这种“纯文本”的解释方式在处理复杂空间推理（如汽车维修、迷宫导航）时，用户难以快速理解和验证模型推理的正确性。
*   **研究动机**：人类在解释图像问题时习惯于指点、圈画和标注。作者希望赋予 VLM 这种“边想边画”的能力，通过在原图上叠加视觉标注（SVG 覆盖层）来直观展示其推理逻辑，从而提高模型的可解释性和用户信任度。

### 2. 方法论
SketchVLM 是一个**无需训练（Training-free）**且**模型无关（Model-agnostic）**的框架，其核心思想是将 VLM 转化为一个能够输出结构化绘图指令的“标注员”。
*   **视觉提示（Visual Prompting）**：在输入图像的左侧和下方添加坐标刻度尺（Grid），帮助模型建立精确的空间参考系。
*   **系统提示词工程**：通过精心设计的系统提示词，指导模型以特定的 XML/JSON 格式输出绘图指令。支持的基元包括：直线、箭头、文本标签、矩形、椭圆以及贝塞尔曲线（Bézier curves）。
*   **非破坏性叠加（SVG Overlay）**：模型输出的坐标点通过最小二乘法拟合为平滑的 SVG 曲线，并作为一个独立的透明图层覆盖在原始图像上。这种方式不会修改原始像素，保证了图像的完整性。
*   **生成模式**：
    *   **单轮生成**：一次性输出所有标注和最终答案，效率高。
    *   **多轮生成**：模拟人机协作，模型每画一笔都会将渲染后的图像反馈给自己，逐步完成复杂引导任务。

### 3. 实验设计
*   **任务与数据集**：涵盖了 7 项任务，分为两类：
    1.  **视觉推理**：迷宫导航（Maze Navigation）、落球轨迹预测（Ball Drop）、物理理解（VPCT）、物体计数（Counting）。
    2.  **绘图与定位**：连点成线（Connect-the-Dots）、部件标注（Part Labeling）、物体形状绘制（Drawing Shapes）。
*   **对比方法（Baselines）**：
    *   **图像编辑模型**：Nano Banana Pro（通过修改像素进行标注）。
    *   **微调专用模型**：ViLaSR、ThinkMorph（专门针对绘图任务微调过的模型）。
    *   **原生 VLM**：Gemini-3-Pro、GPT-5 的纯文本输出模式。
*   **评估指标**：任务准确率（Accuracy）、标注与文本的一致性（Alignment）、标注质量（Quality，采用 VLM-as-a-Judge 和人工评分）。

### 4. 资源与算力
*   **训练算力**：由于该方法是**无需训练**的，因此不涉及大规模训练算力。
*   **推理算力**：实验主要通过 API 调用前沿模型（Gemini-3-Pro-Preview, GPT-5）。
*   **本地运行**：对比模型（如 ThinkMorph 和 ViLaSR）在本地 GPU 上运行，但文中未详细列出具体的 GPU 型号和数量。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了数千张图像（如 COCO 验证集中的 1000 张、PACO/Pascal-Part 中的 985 张等），样本量充足。
*   **消融实验**：详细探讨了坐标网格（Grid）对不同模型的影响、单轮与多轮生成的优劣、以及贝塞尔曲线与直线的对比。
*   **客观性**：引入了“VLM 裁判”与“人工标注员”的对比研究，计算了 Kappa 系数和皮尔逊相关性，证明了自动化评估指标的有效性。整体实验设计严谨，对比了多种不同技术路线的基准模型。

### 6. 主要结论与发现
*   **性能提升**：SketchVLM 在视觉推理任务中将准确率最高提升了 **28.5%**。
*   **标注质量**：其生成的标注质量比微调模型高出 **1.48 倍**，且标注与模型给出的文本答案高度一致（Alignment > 94%）。
*   **模型差异**：Gemini-3-Pro 在配合坐标网格时表现最佳，而 GPT-5 在没有网格的情况下定位更准。
*   **泛化能力**：相比于在特定绘图任务上微调的模型，基于前沿大模型的 SketchVLM 在未见过的物理推理任务上表现出极强的零样本泛化能力。
*   **单轮 vs 多轮**：单轮生成在准确率上与多轮相当，但速度快 6 倍；多轮生成更适合需要人类干预的交互式引导。

### 7. 优点
*   **非破坏性**：SVG 图层可编辑、可撤销，不损坏原图，适合专业工作流。
*   **零训练成本**：直接利用现有最强 VLM 的指令遵循能力，易于部署和迁移。
*   **高解释性**：通过视觉轨迹展示“思考过程”，使用户能一眼看出模型是否在“瞎猜”（例如轨迹是否穿墙）。
*   **灵活性**：支持多种绘图基元，能够处理从简单的计数到复杂的物理轨迹模拟。

### 8. 不足与局限
*   **模型依赖性**：该方法高度依赖于底层 VLM 的指令遵循和空间感知能力。在较小的开源模型（如 Qwen2.5-VL-7B）上效果较差。
*   **坐标系敏感**：模型对坐标系的微小变化（如 0-1000 与 0-2000 缩放）非常敏感，可能导致标注偏移。
*   **缺乏纠错机制**：目前模型无法在发现画错后主动“擦除”或修改之前的笔画（虽然多轮模式有潜力，但尚未完全实现自动纠错）。
*   **应用限制**：对于极小目标的定位（如 COCO 中的小物体），SVG 标注的精度仍略逊于专门的检测框输出。

（完）
