---
title: Hybrid Latent Reasoning with Decoupled Policy Optimization
title_zh: 基于解耦策略优化的混合潜空间推理
authors: "Tao Cheng, Shi-Zhe Chen, Hao Zhang, Yixin Qin, Jinwen Luo, Zheng Wei"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20328v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 结合策略优化的多模态大模型混合潜空间推理
tldr: 针对多模态大模型在CoT推理中因视觉信号离散化导致的语义丢失问题，本文提出HyLaR框架，实现了离散文本与连续视觉潜表征的交替推理。为解决混合动作空间的优化难题，引入了DePO解耦策略优化方法，通过独立置信域约束和vMF KL正则化提升强化学习效率。实验表明，该方法在细粒度感知和多模态理解任务上均达到领先水平。
selection_source: fresh_fetch
motivation: 传统多模态CoT将视觉信号离散化会导致细粒度信息丢失，而现有的潜空间推理方法在处理离散-连续混合动作空间的优化时面临巨大挑战。
method: 提出HyLaR框架交替生成文本与视觉潜表征，并采用DePO算法通过解耦策略梯度和闭式vMF KL正则化进行高效的强化学习优化。
result: 实验结果显示，HyLaR在细粒度感知和通用多模态理解基准测试中均优于标准MLLM及现有的潜空间推理先进方法。
conclusion: 该研究证明了通过解耦优化策略整合离散与连续表征，能显著增强多模态模型在复杂推理任务中的感知与理解能力。
---

## 摘要
思维链（CoT）推理显著提升了多模态大语言模型（MLLM）解决复杂问题的能力。然而，将 CoT 适配到视觉领域通常需要将信号离散化以符合 LLM 的输入要求，这会导致早期的语义崩溃并丢失细粒度细节。虽然外部工具可以缓解这一问题，但它们引入了僵化的瓶颈，将推理局限于预定义的操作。尽管最近的潜空间推理范式通过内化视觉状态来克服这些局限，但优化由此产生的离散-连续混合动作空间仍然具有挑战性。在这项工作中，我们提出了 HyLaR（混合潜空间推理），这是一个将离散文本生成与连续视觉潜表征无缝交织的框架。具体而言，在初始的冷启动有监督微调（SFT）之后，我们引入了 DePO（解耦策略优化），以在该混合空间内实现有效的强化学习。DePO 分解了策略梯度目标，对文本和潜空间组件分别应用独立的置信域约束，并结合了精确的闭式 von Mises-Fisher (vMF) KL 正则化项。广泛的实验表明，HyLaR 在细粒度感知和通用多模态理解基准测试中均优于标准 MLLM 和最先进的潜空间推理方法。代码可在 https://github.com/EthenCheng/HyLaR 获取。

## Abstract
Chain-of-Thought (CoT) reasoning significantly elevates the complex problem-solving capabilities of multimodal large language models (MLLMs). However, adapting CoT to vision typically discretizes signals to fit LLM inputs, causing early semantic collapse and discarding fine-grained details. While external tools can mitigate this, they introduce a rigid bottleneck, confining reasoning to predefined operations. Although recent latent reasoning paradigms internalize visual states to overcome these limitations, optimizing the resulting hybrid discrete-continuous action space remains challenging. In this work, we propose HyLaR (Hybrid Latent Reasoning), a framework that seamlessly interleaves discrete text generation with continuous visual latent representations. Specifically, following an initial cold-start supervised fine-tuning (SFT), we introduce DePO (Decoupled Policy Optimization) to enable effective reinforcement learning within this hybrid space. DePO decomposes the policy gradient objective, applying independent trust-region constraints to the textual and latent components, alongside an exact closed-form von Mises-Fisher (vMF) KL regularizer. Extensive experiments demonstrate that HyLaR outperforms standard MLLMs and state-of-the-art latent reasoning approaches across fine-grained perception and general multimodal understanding benchmarks. Code is available at https://github.com/EthenCheng/HyLaR.

---

## 论文详细总结（自动生成）

### 论文总结：HyLaR - 基于解耦策略优化的混合潜空间推理

#### 1. 核心问题与整体含义（研究动机和背景）
多模态大语言模型（MLLM）在处理复杂视觉任务时，通常依赖**显式思维链（CoT）**。然而，现有的 CoT 存在两个主要瓶颈：
*   **早期语义崩溃（Early Semantic Collapse）：** 强制将高带宽的连续视觉信号压缩为离散的文本 Token，会导致细粒度视觉细节丢失，使模型倾向于依赖语言先验而非视觉事实。
*   **外部工具局限性：** 现有的“Think-with-Images”范式依赖外部缩放/裁剪工具，引入了推理延迟和僵化的操作瓶颈。
*   **优化难题：** 虽然“潜空间推理”能保留视觉保真度，但如何同时优化**离散文本**和**连续向量**构成的混合动作空间，在强化学习（RL）领域仍是一个巨大挑战。

#### 2. 方法论：核心思想与关键技术
本文提出了 **HyLaR** 框架及配套的 **DePO** 优化算法：
*   **HyLaR 框架：** 允许模型在生成过程中通过特殊的控制 Token（如 `<|canvas_start|>`）进入“画布模式”。在此模式下，模型不输出文本，而是将隐藏层状态作为连续的“视觉工作记忆”进行循环迭代，实现深度视觉思考。
*   **两阶段训练流程：**
    1.  **阶段 I：冷启动 SFT：** 使用辅助的图像压缩模块，将中间推理步骤的图像特征注入 LLM。通过交叉熵损失（文本）和 MSE 损失（潜表征对齐）进行联合训练。
    2.  **阶段 II：解耦策略优化 (DePO)：** 这是本文的核心创新，专门用于优化混合动作空间。
*   **DePO 关键技术细节：**
    *   **超球面几何建模：** 考虑到 LLM 的表征通常经过归一化（如 RMSNorm），DePO 放弃了传统的欧几里得高斯假设，改用 **von Mises-Fisher (vMF) 分布**。这使得概率估计和 KL 散度可以简化为精确的**闭式余弦距离**。
    *   **解耦置信域剪切（Decoupled Clipping）：** 发现连续向量的概率比率波动远大于离散 Token。DePO 为两者设置了独立的剪切范围（文本 0.2-0.28，潜表征 0.05），以稳定训练。
    *   **零方差 KL 正则化：** 利用 vMF 的特性，直接计算当前策略与参考策略之间的几何偏差，避免了采样带来的高方差。

#### 3. 实验设计
*   **数据集：**
    *   **SFT 阶段：** 使用 Zebra-CoT（约 9.6 万样本），涵盖科学问题、2D/3D 视觉推理及逻辑游戏。
    *   **RL 阶段：** 整合了 DeepEyes、Thyme 和 CodeDance（约 4.8 万样本），侧重于高分辨率输入和“图像思考”任务。
*   **Benchmark（基准测试）：**
    *   **高分辨率感知：** V*、HRBench-4K/8K（目标极小，极具挑战）。
    *   **通用多模态理解：** MMStar、MMVP、SeedBench2Plus、BLINK、HallusionBench（幻觉诊断）。
*   **对比方法：** 包括闭源模型（GPT-4o, Gemini-3-Flash）、开源模型（Qwen2.5-VL, InternVL3）、Agent 类模型（DeepEyesV2, Thyme）以及潜空间推理模型（LVR, Monet, SkiLa）。

#### 4. 资源与算力
*   **硬件：** 实验使用了 **8 张 NVIDIA H20 GPU**。
*   **训练细节：** SFT 阶段训练 1 个 Epoch，学习率为 $10^{-5}$；RL 阶段学习率为 $10^{-6}$，Rollout Size 为 8。
*   **时长：** 文中未明确给出总训练时长，但提到 SFT 限制为 1 Epoch 以防止过拟合。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 8 个主流基准测试上进行了评估，涵盖了从基础感知到复杂推理的多个维度。
*   **消融实验：** 做了非常详尽的消融研究，包括：
    *   潜空间推理步数（$K_{test}$）的影响。
    *   不同 RL 算法（GRPO, DAPO, VLPO）的对比。
    *   DePO 超参数（损失权重 $\alpha$、剪切比例 $\epsilon$）的敏感度分析。
    *   分布假设（vMF vs. Gaussian）的对比。
*   **客观性：** 实验对比了多种 SOTA 模型，并使用了统一的评估流水线（如对 SkiLa 和 Monet 进行了复现测试），确保了对比的公平性。

#### 6. 主要结论与发现
*   **性能领先：** HyLaR 在高分辨率感知任务上显著优于现有潜空间模型（如在 V* 上比 Monet 高 3.67%），甚至在某些指标上超越了依赖外部工具的 Agent 模型。
*   **外推能力：** RL 优化后的模型展现出比 SFT 模型更强的“推理外推”能力，即在推理时增加潜空间步数（$K_{test}$）不会像 SFT 模型那样出现性能衰减（Over-thinking）。
*   **几何对齐的重要性：** 实验证明基于 vMF 的超球面建模比传统的欧几里得高斯建模更符合 LLM 的内在表征逻辑，训练更稳定且效果更好。

#### 7. 优点
*   **理论严谨：** 针对 LLM 隐藏层的几何特性提出了 vMF 建模，解决了混合空间 RL 优化的数学失配问题。
*   **架构优雅：** 无需外部工具或复杂的像素级生成，通过纯潜空间递归实现“深度思考”，推理效率较高。
*   **鲁棒性强：** 显著降低了多模态模型在处理细微视觉特征时的幻觉问题。

#### 8. 不足与局限
*   **数据依赖：** 冷启动 SFT 阶段仍需要带有中间推理图像（Ground-truth canvases）的高质量数据集，这类数据的获取成本较高。
*   **任务覆盖：** 目前主要验证了感知和 VQA 类任务，对于需要多步外部交互的复杂具身智能（Embodied AI）或开放域 Agent 场景的适用性尚待进一步探索。
*   **计算开销：** 虽然避免了像素生成，但多步潜空间递归仍会增加一定的推理计算量（FLOPs）。

（完）
