---
title: Visual Reasoning through Tool-supervised Reinforcement Learning
title_zh: 通过工具监督强化学习实现视觉推理
authors: "Qihua Dong, Gozde Sahin, Pei Wang, Zhaowei Cai, Robik Shrestha, Hao Yang, Davide Modolo"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19945v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于多模态大模型复杂视觉推理的工具监督强化学习
tldr: 针对多模态大模型在复杂视觉推理任务中难以有效掌握工具使用的问题，本文提出了ToolsRL框架。该框架引入了缩放、旋转、翻转等基础视觉工具，并设计了两阶段强化学习课程：第一阶段通过工具特定奖励学习工具调用，第二阶段结合准确率奖励优化推理任务。这种方法有效避免了异构任务间的优化冲突，显著提升了模型在复杂视觉推理中的工具使用能力和任务表现。
selection_source: fresh_fetch
motivation: 旨在解决多模态大模型在处理复杂视觉推理任务时，难以高效且准确地学习和掌握工具使用的问题。
method: 提出了ToolsRL框架，通过两阶段强化学习课程，先后利用工具监督奖励和任务准确率奖励来解耦工具掌握与任务推理。
result: 实验结果表明，该工具监督的课程训练方法不仅效率高，且使模型在处理复杂视觉推理任务时具备了极强的工具调用能力。
conclusion: ToolsRL证明了通过结构化的强化学习课程和直接的工具监督，可以显著增强多模态大模型在视觉推理中的工具使用效能。
---

## 摘要
在本文中，我们研究了多模态大语言模型如何有效掌握工具使用，以解决复杂的视觉推理任务。为此，我们提出了一种新颖的工具监督强化学习（ToolsRL）框架，通过直接的工具监督来实现更有效的工具使用学习。我们专注于一系列简单、原生且可解释的视觉工具，包括放大、旋转、翻转以及绘制点/线，这些工具的监督信号易于收集。我们开发了一种强化学习课程方案，其中第一阶段仅由一组动机明确的工具特定奖励进行优化，第二阶段则在允许调用工具的同时，使用以准确率为目标的奖励进行训练。通过这种方式，模型在利用工具完成视觉推理任务之前就已掌握了工具调用能力，从而避免了这些异构任务之间潜在的优化冲突。实验表明，工具监督的课程训练是高效的，且 ToolsRL 能够在复杂的视觉推理任务中实现强大的工具使用能力。

## Abstract
In this paper, we investigate the problem of how to effectively master tool-use to solve complex visual reasoning tasks for Multimodal Large Language Models. To achieve that, we propose a novel Tool-supervised Reinforcement Learning (ToolsRL) framework, with direct tool supervision for more effective tool-use learning. We focus on a series of simple, native, and interpretable visual tools, including zoom-in, rotate, flip, and draw point/line, whose tool supervision is easy to collect. A reinforcement learning curriculum is developed, where the first stage is solely optimized by a set of well motivated tool-specific rewards, and the second stage is trained with the accuracy targeted rewards while allowing calling tools. In this way, tool calling capability is mastered before using tools to complete visual reasoning tasks, avoiding the potential optimization conflict among those heterogeneous tasks. Our experiments have shown that the tool-supervised curriculum training is efficient and ToolsRL can achieve strong tool-use capabilities for complex visual reasoning tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ToolsRL** 的框架，旨在通过工具监督的强化学习（Reinforcement Learning, RL）提升多模态大语言模型（MLLM）在复杂视觉推理任务中的表现。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：虽然 MLLM 在文本推理上取得了巨大进步，但在处理复杂的视觉分析（如旋转文本识别、杂乱场景中的微小物体定位）时仍然乏力。
*   **核心问题**：现有的工具使用训练方法存在局限性。**SFT（监督微调）**依赖昂贵且难以扩展的人工专家轨迹；而**标准 RL** 通常只提供最终结果的稀疏奖励，缺乏对工具调用时机和执行细节的明确指导，导致模型调用工具频率低且难以形成多步推理链。
*   **研究动机**：探索如何让模型自主、高效地掌握视觉工具（如放大、旋转、绘图），以生成中间视觉证据辅助推理。

### 2. 方法论
核心思想是**“先学工具，再做任务”**，通过两阶段课程学习解耦工具掌握与任务推理。
*   **核心工具集**：包括原生且可解释的视觉工具：`Zoom-in`（放大）、`Rotate/Flip`（旋转/翻转）、`Draw Point/Line`（画点/线）。
*   **两阶段课程学习**：
    1.  **阶段 1：工具监督阶段**。仅优化工具特定的奖励函数。模型被要求识别并操作视觉元素，目标是学会“如何正确使用工具”。
    2.  **阶段 2：任务准确率阶段**。使用标准的问答（QA）奖励（如准确率），允许模型自由调用已掌握的工具来解决复杂问题。
*   **关键技术细节（奖励设计）**：
    *   **Zoom-in 奖励**：采用改进的 F1 分数（ModF1），降低对“放大范围过大”的惩罚，重点奖励覆盖目标区域的行为。
    *   **旋转/翻转奖励**：基于图像是否恢复到标准朝向的二元奖励。
    *   **绘图奖励**：统一的坐标相似度评分，通过匈牙利算法匹配预测点/线与真实标注，提供连续的边际奖励。
*   **算法基础**：使用 **GRPO**（Group Relative Policy Optimization）算法进行策略优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **文档理解**：DocVQA、InfoVQA（及其旋转翻转增强版 RF）。
    *   **空间推理**：HR-Bench（高分辨率感知）、V-Star、Visual Probe。
    *   **图表/表格理解**：ChartQA、CharXiv、TableVQA，以及自建的合成数据集 Read-Value 和 Compare-and-Count。
*   **对比方法**：包括 Qwen2.5-VL（基座模型）、DeepEyes、Mini-o3、Point-RFT、Simple o3 等 SOTA 模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **4 个节点，每个节点配备 8 张 NVIDIA H200 GPU**（共 32 张 H200）。
*   **训练细节**：每个阶段训练 200 个 step，学习率为 $1 \times 10^{-6}$，batch size 为 256。
*   **基座模型**：Qwen2.5-VL-7B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 大领域（文档、空间、图表）的十余个主流基准测试上进行了验证。
*   **消融实验**：非常充分。作者对比了“仅准确率奖励”、“工具条件奖励”、“无课程学习的混合奖励”以及不同奖励权重（如 $w_{fp}$）对结果的影响。
*   **客观性**：通过 LLM-Judge（使用 Qwen2.5-VL-72B）进行自动化评估，并提供了详细的定性案例分析和工具调用频率统计，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：ToolsRL 在多数基准测试中达到 SOTA，尤其在处理旋转文档和高分辨率图像时优势明显。
*   **课程学习的必要性**：直接在第二阶段训练会导致模型倾向于纯文本推理而忽略工具；先进行工具监督训练能显著提高工具调用频率（平均每样本 3.4 次，远高于对比方法的 $\le 1$ 次）。
*   **自发推理模式**：模型学会了多步视觉搜索、视觉验证以及组合工具使用（如先放大再画点），这些行为是自发涌现的。

### 7. 优点
*   **高效且可扩展**：利用易于获取的原子级标注（如 BBox、旋转角度）代替昂贵的专家轨迹，降低了数据成本。
*   **训练稳定性**：两阶段解耦避免了异构任务（工具操作 vs. 文本生成）之间的优化冲突。
*   **通用性强**：设计的工具奖励函数具有通用性，可推广至代码生成或具身智能等其他领域。

### 8. 不足与局限
*   **工具范围有限**：目前仅限于基础的视觉操作，尚未涉及更复杂的外部工具（如分割模型或 OCR 引擎）。
*   **依赖阶段 1 的标注**：虽然标注易得，但仍需要针对特定任务准备工具层面的 Ground Truth，无法完全脱离监督信号。
*   **推理开销**：由于模型倾向于进行多步工具调用，推理时的计算成本和延迟会相应增加。

（完）
