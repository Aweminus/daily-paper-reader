---
title: "Rethinking Reinforcement Fine-Tuning in LVLM: Convergence, Reward Decomposition, and Generalization"
title_zh: 重新思考 LVLM 中的强化微调：收敛性、奖励分解与泛化
authors: "Carter Adams, Rafael Oliveira, Gabriel Almeida, Sofia Torres"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19857v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: LVLM中用于智能体能力和多步推理的强化微调
tldr: 本研究针对大型视觉语言模型（LVLM）中带有可验证奖励的强化微调（RLVR）进行了理论探讨。通过引入工具增强马尔可夫决策过程（TA-MDP）框架，深入分析了组相对策略优化（GRPO）在复合奖励下的收敛性、奖励分解的子优化间隙以及跨域泛化的理论基础。研究证明了GRPO的收敛速率，并为Visual-ARFT等方法的实证成功提供了严谨的数学解释。
selection_source: fresh_fetch
motivation: 尽管RLVR在提升LVLM的智能体能力方面取得了显著成功，但其复合奖励对收敛的影响及跨域泛化机制在理论上仍不明确。
method: 提出了工具增强马尔可夫决策过程（TA-MDP）形式化框架，并在此基础上对GRPO算法的收敛性、奖励分解和泛化界限进行了理论推导。
result: 证明了GRPO以O(1/√T)的速率收敛，推导出了奖励分解的子优化界限，并建立了能够解释强跨域迁移能力的PAC-Bayes泛化界。
conclusion: 该研究为LVLM的强化微调提供了坚实的理论支撑，揭示了复合奖励结构与模型泛化性能之间的内在联系。
---

## 摘要
带有可验证奖励的强化微调（RLVR）已成为一种强大的范式，用于赋予多模态大模型（LVLMs）智能体能力，如工具使用和多步推理。尽管取得了显著的经验性成功，尤其是视觉智能体强化微调（Visual-ARFT），但该范式的理论基础仍不甚明了。特别是，有两个关键问题缺乏严谨的回答：(i) 可验证奖励的复合结构（格式合规性、答案准确性、工具可执行性）如何影响群体相对策略优化（GRPO）的收敛性；(ii) 为什么在少量工具增强任务上的训练能够迁移到分布外（OOD）领域？我们通过引入工具增强马尔可夫决策过程（TA-MDP）来填补这些空白，这是一个对具有有限深度工具调用的多模态智能体决策进行建模的形式化框架。在该框架内，我们确立了三个主要结果。首先，我们证明了在复合可验证奖励下，GRPO 以 O(1/√T) 的速率收敛到一阶驻点，且显式依赖于奖励组件的数量和群体大小（定理 1）。其次，我们推导出了一个奖励分解定理，该定理界定了分解后的各组件优化与联合优化之间的次优性间隙，为奖励分解何时有益提供了精确的表征（定理 2）。第三，我们为工具增强策略建立了 PAC-Bayes 泛化界，解释了在 Visual-ARFT 中观察到的强大分布外迁移能力（定理 3）。

## Abstract
Reinforcement fine-tuning with verifiable rewards (RLVR) has emerged as a powerful paradigm for equipping large vision-language models (LVLMs) with agentic capabilities such as tool use and multi-step reasoning. Despite striking empirical successes, most notably Visual Agentic Reinforcement Fine-Tuning (Visual-ARFT), the theoretical underpinnings of this paradigm remain poorly understood. In particular, two critical questions lack rigorous answers: (i)~how does the composite structure of verifiable rewards (format compliance, answer accuracy, tool executability) affect the convergence of Group Relative Policy Optimization (GRPO), and (ii)~why does training on a small set of tool-augmented tasks transfer to out-of-distribution domains? We address these gaps by introducing the \emph{Tool-Augmented Markov Decision Process} (TA-MDP), a formal framework that models multimodal agentic decision-making with bounded-depth tool calls. Within this framework, we establish three main results. First, we prove that GRPO under composite verifiable rewards converges to a first-order stationary point at rate $O(1/\sqrt{T})$ with explicit dependence on the number of reward components and group size (\textbf{Theorem~1}). Second, we derive a \emph{Reward Decomposition Theorem} that bounds the sub-optimality gap between decomposed per-component optimization and joint optimization, providing a precise characterization of when reward decomposition is beneficial (\textbf{Theorem~2}). Third, we establish a PAC-Bayes generalization bound for tool-augmented policies that explains the strong out-of-distribution transfer observed in Visual-ARFT (\textbf{Theorem~3}).

---

## 论文详细总结（自动生成）

这篇论文对大型视觉语言模型（LVLM）中的强化学习微调（RLVR）进行了深入的理论探讨，以下是详细的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管基于可验证奖励的强化微调（RLVR，如 GRPO 算法）在提升 LVLM 的智能体能力（如工具调用、多步推理）方面取得了显著成功（例如 Visual-ARFT），但其背后的理论机制尚不明确。
*   **研究动机**：论文旨在回答三个关键理论疑问：
    1.  由格式、准确性、工具执行等多组件构成的**复合奖励**如何影响 GRPO 的收敛？
    2.  将复合奖励**分解**为独立组件进行优化，在什么条件下是合理的？
    3.  为什么在极少量任务上训练的智能体策略具有极强的**跨领域泛化（OOD）能力**？

### 2. 方法论：TA-MDP 框架与三大定理
论文提出了**工具增强马尔可夫决策过程（TA-MDP）**，将工具调用建模为具有有限深度（$D_{max}$）的子序列。
*   **核心思想**：将智能体行为形式化，区分生成状态、工具调用状态和返回状态。
*   **关键技术细节**：
    *   **定理 1（收敛性）**：证明了在复合奖励下，GRPO 以 $O(1/\sqrt{T})$ 的速率收敛。收敛速度受奖励组件数量 $K$、群体大小 $G$ 和 KL 惩罚系数 $\beta$ 的显式影响。
    *   **定理 2（奖励分解）**：推导了“奖励组件对齐度（$\alpha$）”的概念。当不同奖励组件（如格式和准确性）正相关时，分解优化与联合优化的差距极小，证明了简单加号连接奖励的合理性。
    *   **定理 3（泛化界限）**：利用 PAC-Bayes 理论证明，工具增强策略的有效复杂度由工具调用深度决定，而非原始状态空间维度。工具充当了“复杂度降低器”，解释了为何模型能从少量样本中学习到强大的通用能力。

### 3. 实验设计
*   **实验场景**：
    1.  **合成 TA-MDP 环境**：用于受控实验，调整状态空间大小、工具数量、调用深度和奖励组件数。
    2.  **真实世界基准**：MAT-Coding（代码）、MAT-Search（搜索）。
    3.  **分布外（OOD）测试**：2WikiMultihopQA 和 HotpotQA（纯文本多跳问答）。
*   **对比方法（Benchmark）**：标准 GRPO（单标量奖励）、PPO、DPO、分解式 GRPO 以及 Visual-ARFT 原始方法。

### 4. 资源与算力
*   **模型使用**：实验采用了 **Qwen2.5-VL-7B** 作为基础模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。实验部分主要展示了迭代次数（如 10,000 次迭代）和收敛趋势，侧重于验证理论界的正确性而非单纯追求 SOTA 性能。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的消融实验，包括奖励组件数 $K$（1-4）、群体大小 $G$（4-64）、对齐度 $\alpha$ 以及工具深度 $D_{max}$（0-3）。
*   **充分性评价**：实验设计非常充分且具有针对性。通过合成环境验证了定理中的每一个参数依赖关系，并结合真实 LVLM 任务验证了理论的实用性。实验过程考虑了随机种子（5个种子取平均），结果表现出较强的一致性，论证过程客观公平。

### 6. 主要结论与发现
*   **收敛规律**：增加奖励组件会增加梯度方差，从而减慢收敛，但可以通过增大 GRPO 的群体大小 $G$ 来补偿。
*   **奖励设计**：在训练过程中，格式合规性和答案准确性会自然趋于对齐（$\alpha$ 增加），这使得简单的加权求和奖励在实践中非常有效。
*   **泛化机制**：工具调用将复杂的计算外包给确定性的外部函数，降低了模型需要学习的“有效维度”，这是 LVLM 智能体能够实现强跨域迁移的核心原因。

### 7. 优点
*   **理论填补空白**：首次为 GRPO 在多模态智能体场景下的应用提供了严谨的数学证明。
*   **解释力强**：不仅给出了收敛速度，还解释了“为什么简单的奖励设计能行”以及“为什么少量数据能泛化”这两个困扰从业者的直觉问题。
*   **指导意义**：为未来 RLVR 系统的超参数选择（如 $G$ 与 $K$ 的比例）提供了理论指导。

### 8. 不足与局限
*   **假设限制**：理论推导基于 Lipschitz 连续性和有限工具调用深度的假设，在无限循环或极度非平滑的奖励函数下可能失效。
*   **常数项松散**：虽然证明了阶数，但界限中的常数项在低维情况下可能不够紧凑（Tightness Ratio 在 2-4 倍之间）。
*   **任务覆盖**：主要关注可验证奖励（Verifiable Rewards），对于难以程序化验证的开放式主观偏好任务，该理论框架的适用性尚待研究。

（完）
