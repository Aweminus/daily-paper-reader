---
title: "SSL-R1: Self-Supervised Visual Reinforcement Post-Training for Multimodal Large Language Models"
title_zh: SSL-R1：多模态大语言模型的自监督视觉强化后训练
authors: "Jiahao Xie, Alessio Tonioni, Nathalie Rauschmayr, Federico Tombari, Bernt Schiele"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20705v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于多模态大模型推理的自监督视觉强化学习
tldr: 针对多模态大语言模型（MLLM）在强化学习中过度依赖人工标注和语言先验的问题，本文提出SSL-R1框架。该框架将视觉自监督学习任务转化为可验证的视觉谜题，直接从图像生成奖励信号，实现了无需人工或外部模型干预的自监督强化学习后训练。实验表明，该方法显著提升了模型在多模态理解与推理任务上的表现，为大规模扩展可验证奖励提供了新路径。
selection_source: fresh_fetch
motivation: 现有的多模态强化学习方法受限于昂贵的人工标注和语言中心先验，难以有效提升模型的内在视觉理解能力。
method: 通过将经典的视觉自监督学习任务重新表述为可验证的视觉谜题，为强化学习提供直接源自图像的自动奖励信号。
result: 在多个多模态理解和推理基准测试中，SSL-R1显著提升了MLLM的性能，验证了视觉中心自监督任务的有效性。
conclusion: SSL-R1证明了利用自监督视觉任务构建可验证奖励的潜力，为实现大规模、自动化的多模态模型后训练提供了重要经验。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已展现出增强多模态大语言模型（MLLMs）推理能力的巨大潜力。然而，对以语言为中心的先验和昂贵人工标注的依赖，阻碍了 MLLMs 的内在视觉理解和可扩展的奖励设计。在这项工作中，我们提出了 SSL-R1，这是一个通用的自监督强化学习框架，它直接从图像中获取可验证的奖励。为此，我们重新审视了视觉领域的自监督学习（SSL），并将广泛使用的 SSL 任务重新表述为一组用于强化学习后训练的可验证视觉谜题，既不需要人工监督，也不需要外部模型监督。在这些任务上训练 MLLMs 显著提升了它们在多模态理解和推理基准测试中的性能，凸显了利用以视觉为中心的自监督任务进行 MLLM 后训练的潜力。我们认为这项工作将为设计有效的自监督可验证奖励以实现大规模强化学习提供有用的经验。项目主页：https://github.com/Jiahao000/SSL-R1。

## Abstract
Reinforcement learning (RL) with verifiable rewards (RLVR) has demonstrated the great potential of enhancing the reasoning abilities in multimodal large language models (MLLMs). However, the reliance on language-centric priors and expensive manual annotations prevents MLLMs' intrinsic visual understanding and scalable reward designs. In this work, we introduce SSL-R1, a generic self-supervised RL framework that derives verifiable rewards directly from images. To this end, we revisit self-supervised learning (SSL) in visual domains and reformulate widely-used SSL tasks into a set of verifiable visual puzzles for RL post-training, requiring neither human nor external model supervision. Training MLLMs on these tasks substantially improves their performance on multimodal understanding and reasoning benchmarks, highlighting the potential of leveraging vision-centric self-supervised tasks for MLLM post-training. We think this work will provide useful experience in devising effective self-supervised verifiable rewards to enable RL at scale. Project page: https://github.com/Jiahao000/SSL-R1.

---

## 论文详细总结（自动生成）

这是一份关于论文《SSL-R1: Self-Supervised Visual Reinforcement Post-Training for Multimodal Large Language Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：受 DeepSeek-R1 等模型成功的启发，具有可验证奖励的强化学习（RLVR）正被引入多模态大语言模型（MLLM）。
*   **核心痛点**：现有的 MLLM 强化学习方案高度依赖人工标注或外部模型监督（昂贵且难以扩展），且奖励设计往往偏向语言逻辑，导致模型在细粒度视觉感知和空间推理方面表现不足。
*   **研究动机**：探索一种无需人工干预、以视觉为中心且可大规模扩展的自监督强化学习框架，以增强 MLLM 的内在视觉理解能力。

### 2. 方法论
*   **核心思想**：将经典的视觉自监督学习（SSL）任务重新表述为“可验证的视觉谜题”，利用图像自身的结构信息生成自动奖励信号。
*   **关键技术细节**：
    *   **五大视觉谜题**：
        1.  **旋转预测**：预测图像被随机旋转的角度（如 0°, 90°, 180°, 270°）。
        2.  **视觉相似性**：从多个干扰项中识别出与参考图最相似的增强视图。
        3.  **区域修复**：在图像被遮挡后，从选项中选出正确的缺失切片。
        4.  **拼图排序**：将打乱的 3×3 图像块恢复为原始的栅格扫描顺序。
        5.  **几何对应**：在经过几何变换（如仿射变换）的图像对之间匹配特征点索引。
    *   **奖励设计**：总奖励 = 准确率奖励（R_acc，正确为 1，错误为 0；序列任务按正确比例给分）+ 格式奖励（R_format，强制要求模型使用 `<think>` 标签进行思考并用 `\boxed{}` 输出答案）。
    *   **算法流程**：采用 **GRPO（Group Relative Policy Optimization）** 算法。该算法通过对每个提示词采样一组响应，并根据组内相对得分计算优势函数，从而省去了复杂的 Critic 模型。

### 3. 实验设计
*   **数据集**：使用 COCO 数据集的 118K 张原始图像，自动生成了 591K 个 QA 对（SSL-R1-591K）。
*   **基准测试（Benchmark）**：在 13 个视觉中心基准上进行评估，分为三类：
    *   **细粒度感知**：MMVP, MMStar, MMBench, HR-Bench-8K, V*, MME-RealWorld, LISA-Grounding, OVD-Eval。
    *   **空间理解**：VSR, OmniSpatial, DA-2K。
    *   **组合理解**：Winoground, SugarCrepe++。
*   **对比方法**：
    *   基座模型：Qwen2.5-VL (3B/7B)。
    *   监督强化学习模型：ThinkLite-VL, VL-Cogito, LLaVA-Critic-R1。
    *   并发自监督方法：Visual Jigsaw。

### 4. 资源与算力
*   **训练配置**：全局 Batch Size 为 256，学习率为 1e-6。单任务训练 1000 步，多任务联合训练 2000 步。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）以及具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单任务消融实验，分析了五个 SSL 任务各自的贡献。
    *   进行了多任务组合实验，验证了任务间的协同效应。
    *   探讨了任务难度（如拼图块数、旋转角度间隔）对性能的影响。
    *   在不同规模（3B, 7B）和不同架构（Qwen2.5-VL, MiMo-VL）上进行了验证。
*   **充分性评价**：实验设计非常充分且具有说服力。作者不仅验证了下游任务的提升，还反向测试了模型在 SSL 任务本身的准确率变化，并深入分析了“负迁移”现象，体现了高度的学术严谨性。

### 6. 主要结论与发现
*   **性能提升**：SSL-R1 在所有 13 个基准测试中均实现了显著提升。SSL-R1-7B 的表现甚至超过了许多使用昂贵人工标注进行训练的监督强化学习模型。
*   **任务特性**：
    *   **几何对应**对空间推理（如深度估计）提升最大。
    *   **旋转预测**能有效解决 CLIP 模型的视觉盲点问题（在 MMVP 上提升显著）。
    *   **拼图排序**具有最均衡的迁移能力。
*   **协同与冲突**：多任务训练通常优于单任务，但某些任务间存在冲突（例如，过强的视觉相似性增强可能会损害模型对像素级细节修复的敏感度）。

### 7. 优点
*   **低成本高扩展**：完全摆脱了对人工标注的依赖，利用海量无标签图像即可实现强化学习。
*   **视觉中心化**：通过直接作用于像素和几何结构的奖励，弥补了 MLLM 在纯视觉推理上的短板。
*   **通用性强**：实验证明该框架可无缝迁移至不同的基座模型和已有的推理模型（如 ThinkLite-VL）。

### 8. 不足与局限
*   **任务冲突风险**：虽然多任务总体占优，但论文观察到了负迁移现象，如何自动平衡不同 SSL 任务的权重仍是一个挑战。
*   **推理深度限制**：目前的 SSL 任务主要集中在感知和基础推理，对于需要极长链条逻辑推理（如复杂数学证明）的帮助可能有限。
*   **算力细节缺失**：缺乏具体的算力消耗数据，使得其他研究者难以评估复现成本。

（完）
