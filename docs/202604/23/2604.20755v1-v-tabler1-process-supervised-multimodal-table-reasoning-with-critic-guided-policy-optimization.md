---
title: "V-tableR1: Process-Supervised Multimodal Table Reasoning with Critic-Guided Policy Optimization"
title_zh: V-tableR1：基于评论家引导策略优化的过程监督多模态表格推理
authors: "Yubo Jiang, Yitong An, Xin Yang, Abudukelimu Wuerkaixi, Xuxin Cheng, Fengying Xie, Zhiguo Jiang, Cao Liu, Ke Zeng, Haopeng Zhang"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20755v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态表格推理的强化学习
tldr: 针对多模态大模型在视觉推理中依赖模式匹配而非严谨逻辑的问题，本文提出 V-tableR1 框架。该框架利用表格的确定性结构作为测试场，通过专门的评论家模型对策略模型的视觉思维链进行过程监督。结合新提出的 PGPO 强化学习算法，V-tableR1 显著减少了视觉幻觉，在多个复杂表格基准测试中达到了开源模型的最优性能，甚至超越了参数量大其 18 倍的模型。
selection_source: fresh_fetch
motivation: 现有多模态模型在视觉推理时往往将其视为黑盒，缺乏严谨的多步推理，且难以将抽象逻辑与连续像素空间有效对齐。
method: 提出 V-tableR1 框架，利用表格结构进行过程监督，并采用集成过程奖励、解耦策略约束与动态采样的 PGPO 算法优化模型。
result: V-tableR1 4B 模型在复杂表格基准测试中刷新了开源模型记录，性能优于其 SFT 基线并显著抑制了视觉幻觉。
conclusion: 通过将多模态推理从黑盒模式匹配转向可验证的逻辑推导，该研究为提升模型在视觉任务中的严谨性提供了有效路径。
---

## 摘要
我们介绍了 V-tableR1，这是一个过程监督的强化学习框架，旨在引导多模态大语言模型（MLLM）进行严谨且可验证的推理。目前仅基于最终结果训练的 MLLM 通常将视觉推理视为黑盒，依赖于表面的模式匹配，而非执行严谨的多步推理。虽然具有可验证奖励的强化学习可以强制执行透明的推理轨迹，但由于将抽象逻辑落地到连续像素空间存在歧义，将其扩展到视觉领域仍面临严重阻碍。我们利用表格的确定性网格结构作为理想的视觉试验场来解决这一问题。V-tableR1 采用专门的评论家 VLM，对策略 VLM 生成的显式视觉思维链提供密集的步骤级反馈。为了优化该系统，我们提出了过程引导的直接对齐策略优化（PGPO），这是一种集成过程奖励、解耦策略约束和长度感知动态采样的新型强化学习算法。广泛的评估表明，V-tableR1 显式地惩罚了视觉幻觉和捷径猜测。通过从根本上将多模态推理从黑盒模式匹配转变为可验证的逻辑推导，V-tableR1 4B 在复杂的表格基准测试中确立了开源模型的最先进准确率，其表现优于尺寸达其 18 倍的模型，并较其 SFT 基准模型有所提升。

## Abstract
We introduce V-tableR1, a process-supervised reinforcement learning framework that elicits rigorous, verifiable reasoning from multimodal large language models (MLLMs). Current MLLMs trained solely on final outcomes often treat visual reasoning as a black box, relying on superficial pattern matching rather than performing rigorous multi-step inference. While Reinforcement Learning with Verifiable Rewards could enforce transparent reasoning trajectories, extending it to visual domains remains severely hindered by the ambiguity of grounding abstract logic into continuous pixel space. We solve this by leveraging the deterministic grid structure of tables as an ideal visual testbed. V-tableR1 employs a specialized critic VLM to provide dense, step-level feedback on the explicit visual chain-of-thought generated by a policy VLM. To optimize this system, we propose Process-Guided Direct Alignment Policy Optimization (PGPO), a novel RL algorithm integrating process rewards, decoupled policy constraints, and length-aware dynamic sampling. Extensive evaluations demonstrate that V-tableR1 explicitly penalizes visual hallucinations and shortcut guessing. By fundamentally shifting multimodal inference from black-box pattern matching to verifiable logical derivation, V-tableR1 4B establishes state-of-the-art accuracy among open-source models on complex tabular benchmarks, outperforming models up to 18x its size and improving over its SFT baseline

---

## 论文详细总结（自动生成）

这篇论文介绍了 **V-tableR1**，这是一个针对多模态大语言模型（MLLM）在表格推理任务中进行过程监督强化学习的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多模态模型在处理复杂表格推理时，往往表现出“捷径学习”（Shortcut Learning）的倾向。模型倾向于通过表面的模式匹配直接猜测答案，而非执行严谨的多步逻辑推导。
*   **研究背景**：
    *   **黑盒推理**：仅基于最终结果（Outcome-supervised）的训练导致模型内部推理过程不可见，容易产生“视觉幻觉”。
    *   **视觉落地困难**：在自然图像中，将抽象逻辑与连续像素对齐存在歧义，难以构建可验证的奖励机制。
    *   **表格的优势**：表格具有确定的网格结构，其单元格坐标和中间计算步骤是高度可验证的，是探索多模态强化学习（RLVR）的理想试验场。

### 2. 方法论与关键技术
V-tableR1 框架由**策略 VLM**、**评论家 VLM** 以及 **PGPO 算法**三部分组成：
*   **显式视觉思维链（V-CoT）**：策略模型被要求在给出答案前，先输出显式的视觉锚点（如 `<cell: Row 2, Col 3>`）和中间逻辑步骤。
*   **评论家 VLM（Critic VLM）**：使用一个参数量更大的模型（如 Qwen3-VL 32B）作为过程验证器。它通过对生成的 V-CoT 进行步骤级评分（$r_{proc}$），区分“严谨推理”、“视觉幻觉”和“捷径猜测”。
*   **PGPO 算法（过程引导的直接对齐策略优化）**：
    *   **核心思想**：在 GRPO 算法的基础上，引入了过程奖励、解耦策略约束和长度感知动态采样。
    *   **奖励机制**：采用分段门控机制。如果推理路径完美且答案正确，给予“黄金案例奖励”；如果答案正确但推理路径存在幻觉，则给予“保守惩罚”。
    *   **优化细节**：借鉴 DAPO 的解耦剪切边界以稳定长链推理，并利用 LSPO 的长度感知采样过滤低信息量的中间长度轨迹，提高训练效率。

### 3. 实验设计
*   **数据集**：涵盖了 7 个主流表格推理基准测试，分为两类：
    *   **表格事实验证（TFV）**：TabFact, InfoTabs。
    *   **表格问答（TQA）**：FinQA, HiTab, TAT-QA, TabMWP, WTQ。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4.1, Gemini-1.5-Flash。
    *   **开源模型**：InternVL-2.5 系列、LLaVA 系列、Qwen2.5-VL-72B、QVQ-72B 等。
*   **消融实验**：对比了标准 GRPO、DAPO 以及去掉过程监督的 PGPO 变体。

### 4. 资源与算力
*   **模型规模**：策略模型采用 2B 和 4B 规模，评论家模型采用 32B 规模。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 H100/A100）、节点数量及总训练时长。但从其使用的模型规模和 RL 训练步数（实验图中显示约 50 步收敛）来看，属于中大规模计算需求。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了全面测试，并针对 2B 模型进行了详细的消融实验。
*   **充分性**：实验设计较为充分。不仅对比了最终准确率，还引入了“LLM-as-a-judge”（使用 GPT-5）对推理质量（准确性、逻辑性、视觉落地）进行了定性评估。
*   **客观性**：通过对比不同参数规模的模型（从 2B 到 72B），客观展示了过程监督在提升参数效率方面的巨大优势。

### 6. 主要结论与发现
*   **性能突破**：V-tableR1 4B 在多个基准测试中刷新了开源模型记录，性能显著优于其 SFT 基线（如 FinQA 提升 10.1%）。
*   **以小博大**：4B 规模的模型在表格推理任务上超越了参数量大其 18 倍的 72B 模型（如 Qwen2.5-VL-72B）。
*   **抑制幻觉**：过程监督能有效引导模型关注正确的视觉区域，将视觉落地评分从 2.3 提升至 3.8（5 分制）。
*   **训练稳定性**：PGPO 算法相比标准 GRPO 表现出更强的稳定性，有效防止了视觉领域常见的“奖励作弊”（Reward Hacking）。

### 7. 优点与亮点
*   **创新性**：首次将过程监督（Process Supervision）大规模应用于多模态表格推理，解决了视觉逻辑落地的难题。
*   **算法集成**：PGPO 巧妙结合了稳定性（DAPO）和效率（LSPO）的优点，并融入了密集的步骤级反馈。
*   **实用性**：证明了通过高质量的强化学习，小参数模型也能具备极强的复杂推理能力。

### 8. 不足与局限
*   **领域局限性**：该方法高度依赖表格的确定性结构，目前尚不清楚如何推广到缺乏固定网格结构的自然场景视觉推理。
*   **评论家依赖**：系统性能受限于评论家 VLM 的质量，若评论家本身存在偏见或识别错误，可能误导策略模型。
*   **算力细节缺失**：未披露具体的训练资源消耗，不利于其他研究者评估复现成本。

（完）
