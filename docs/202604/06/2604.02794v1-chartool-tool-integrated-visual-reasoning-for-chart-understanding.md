---
title: "CharTool: Tool-Integrated Visual Reasoning for Chart Understanding"
title_zh: CharTool：用于图表理解的工具集成视觉推理
authors: "Situo Zhang, Yifan Zhang, Zichen Zhu, Da Ma, Lei Pan, Danyang Zhang, Zihan Zhao, Lu Chen, Kai Yu"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02794v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于视觉推理的智能体强化学习
tldr: 针对多模态大模型在图表理解中面临的高质量数据匮乏、视觉定位粗糙及数值计算不准等挑战，本文提出了DuoChart数据流水线和CharTool框架。DuoChart结合合成与真实数据构建高质量数据集，CharTool则通过集成图像裁剪和代码执行工具增强模型的局部感知与计算能力。实验表明，该方法在多个图表基准测试中显著提升了性能，甚至可媲美更大规模的闭源模型。
selection_source: fresh_fetch
motivation: 多模态大模型在图表推理时存在高质量训练数据短缺、细粒度视觉定位困难以及数值计算精度不足的问题。
method: 提出双源数据流水线DuoChart，并构建集成图像裁剪与代码执行工具的CharTool框架，配合强化学习进行训练。
result: "CharTool-7B在CharXiv和ChartQAPro基准上分别实现8.0%和9.78%的性能提升，且在跨领域数学推理任务中表现出良好的泛化能力。"
conclusion: 通过工具集成与高质量数据增强，能够有效解决多模态模型在复杂图表分析中的感知与计算瓶颈。
---

## 摘要
图表在科学和金融文献中无处不在，用于呈现结构化数据。然而，由于缺乏高质量的训练数据，以及对细粒度视觉定位和精确数值计算的需求，图表推理对于多模态大语言模型（MLLMs）而言仍然具有挑战性。为了应对这些挑战，我们首先提出了 DuoChart，这是一个可扩展的双源数据流水线，它将合成图表与真实世界图表相结合，以构建多样化、高质量的图表训练数据。随后，我们介绍了 CharTool，它为 MLLMs 配备了外部工具，包括用于局部视觉感知的图像裁剪和用于精确数值推理的基于代码的计算。通过在 DuoChart 上进行智能体强化学习，CharTool 学习了基于图表内容的工具集成推理。在六个图表基准测试上的广泛实验表明，我们的方法在不同模型规模上均一致优于强大的 MLLM 基线模型。值得注意的是，CharTool-7B 在 CharXiv (Reasoning) 上比基础模型提升了 +8.0%，在 ChartQAPro 上提升了 +9.78%，同时达到了与规模大得多的模型或闭源模型相当的性能。此外，CharTool 在域外视觉数学推理基准测试中也表现出了良好的泛化能力。

## Abstract
Charts are ubiquitous in scientific and financial literature for presenting structured data. However, chart reasoning remains challenging for multimodal large language models (MLLMs) due to the lack of high-quality training data, as well as the need for fine-grained visual grounding and precise numerical computation. To address these challenges, we first propose DuoChart, a scalable dual-source data pipeline that combines synthesized charts with real-world charts to construct diverse, high-quality chart training data. We then introduce CharTool, which equips MLLMs with external tools, including image cropping for localized visual perception and code-based computation for accurate numerical reasoning. Through agentic reinforcement learning on DuoChart, CharTool learns tool-integrated reasoning grounded in chart content. Extensive experiments on six chart benchmarks show that our method consistently improves over strong MLLM baselines across model scales. Notably, CharTool-7B outperforms the base model by **+8.0%** on CharXiv (Reasoning) and **+9.78%** on ChartQAPro, while achieving competitive performance with substantially larger or proprietary models. Moreover, CharTool demonstrates positive generalization to out-of-domain visual math reasoning benchmarks.

---

## 论文详细总结（自动生成）

### 论文总结：CharTool - 用于图表理解的工具集成视觉推理

#### 1. 核心问题与整体含义（研究动机和背景）
图表是科学和金融文献中传递结构化数据的核心媒介。尽管多模态大语言模型（MLLM）在通用视觉任务上表现强劲，但在**图表推理**方面仍面临三大瓶颈：
*   **高质量数据匮乏**：现有的合成数据集多样性不足，且往往存在布局退化（如文字重叠）的问题。
*   **视觉定位粗糙**：模型难以在密集的图表元素（如重叠的数据点、交错的趋势线）中进行细粒度的视觉感知。
*   **数值计算不准**：纯文本推理（CoT）容易产生幻觉，难以处理复杂的数学运算和统计分析。

本文旨在通过构建高质量双源数据集（DuoChart）并引入工具集成推理框架（CharTool），提升模型在复杂图表分析中的精确度和鲁棒性。

#### 2. 方法论：核心思想与关键技术
论文提出了一个结合数据增强与工具调用的完整框架：
*   **DuoChart 数据流水线**：
    *   **双源合成**：结合“代码驱动的合成图表”（提供精确的底层数据和标签）与“真实世界图表挖掘”（从 arXiv 论文中提取，确保视觉多样性和真实布局）。
    *   **多智能体 QA 生成**：利用 QA Agent 生成识别类和推理类问题，并通过 Checker Agent 进行四阶段质量验证（对齐检查、推理验证、多数投票、难度评分），最终构建了 100k 高质量样本。
*   **CharTool 推理框架**：
    *   **工具集成（TIR）**：为模型配备**图像裁剪（Crop）**工具以增强局部视觉感知，以及**代码执行（Python Interpreter）**工具以确保数值计算的绝对精确。
    *   **强化学习训练**：采用 **GRPO（群组相对策略优化）** 算法。奖励函数由准确性奖励（$R_{acc}$）、格式奖励（$R_{format}$）和工具使用奖励（$R_{tool}$）组成，引导模型学会在何时以及如何调用工具。

#### 3. 实验设计
*   **数据集与基准（Benchmarks）**：
    *   **真实世界图表**：CharXiv、ChartQAPro、ChartQA。
    *   **合成图表**：ChartBench、ChartX、ReachQA。
    *   **域外泛化（数学推理）**：MathVista、WeMath、MathVerse。
*   **对比方法**：
    *   **闭源/通用模型**：GPT-4o、Claude 3.5 Sonnet、Qwen2.5-VL 系列（3B/7B/72B）。
    *   **图表专用模型**：TinyChart、ChartGemma、ChartMoE、ECD。
    *   **视觉推理增强模型**：DeepEyes、Thyme。

#### 4. 资源与算力
*   **模型架构**：基于 Qwen2.5-VL-3B 和 7B。
*   **训练框架**：使用 SWIFT 进行冷启动监督微调（SFT），使用 VeRL 进行强化学习（RL），代码执行环境采用 SandboxFusion。
*   **具体算力细节**：文中**未明确说明**具体的 GPU 型号和数量，但提到了训练超参数（如 SFT 阶段 Batch Size 为 64，RL 阶段为 128，最大长度 32768 ）。

#### 5. 实验数量与充分性
*   **实验规模**：在 6 个图表基准和 3 个数学基准上进行了测试，涵盖了从 3B 到 7B 的模型规模。
*   **消融实验**：非常充分。分别验证了：
    1.  工具集成强化学习 vs. 纯文本 RL/CoT 的效果。
    2.  不同数据来源（arXiv vs. 合成数据）对性能的贡献。
    3.  不同工具（裁剪 vs. 代码）在不同任务中的调用分布。
    4.  DuoChart 数据集与其他合成数据集（ReachQA, ECD）的质量对比。
*   **客观性**：通过与 72B 规模模型及 GPT-4o 的直接对比，证明了该方法在参数效率上的优势，实验设计客观公平。

#### 6. 主要结论与发现
*   **性能飞跃**：CharTool-7B 在 CharXiv 推理任务上提升了 **8.0%**，在 ChartQAPro 上提升了 **9.78%**。
*   **以小博大**：仅 7B 规模的 CharTool 在多个指标上超越了 GPT-4o 和 Qwen2.5-VL-72B。
*   **工具互补性**：模型展现了动态工具选择能力。在布局复杂的 CharXiv 中更多调用裁剪工具（84%），在计算密集的 MathVista 中更多调用代码工具（77%）。
*   **泛化能力**：在未见过的视觉数学推理任务中也取得了显著提升，证明工具调用能力具有跨领域迁移性。

#### 7. 优点
*   **数据质量高**：DuoChart 解决了合成数据“假、简、乱”的问题，通过双源互补提升了模型的泛化基础。
*   **推理闭环**：将视觉定位与数值计算解耦到外部工具中，从根本上降低了 MLLM 的幻觉风险。
*   **训练策略先进**：利用 GRPO 强化学习实现了智能体化的推理路径，使模型推理过程更接近人类的“观察-思考-计算”模式。

#### 8. 不足与局限
*   **工具集有限**：目前仅集成了裁剪和代码执行，对于更复杂的视觉操作（如旋转、颜色分离）尚未覆盖。
*   **领域局限**：主要集中在图表和数学，对于表格、说明书、几何图形等其他结构化视觉文档的覆盖有待验证。
*   **奖励信号粒度**：目前的强化学习主要基于结果奖励（Outcome Reward），未来引入过程奖励（Process-level Reward）可能会进一步优化推理步骤。

（完）
