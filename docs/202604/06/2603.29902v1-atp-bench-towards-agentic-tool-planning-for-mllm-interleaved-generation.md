---
title: "ATP-Bench: Towards Agentic Tool Planning for MLLM Interleaved Generation"
title_zh: ATP-Bench：迈向多模态大语言模型交错生成的智能体工具规划
authors: "Yinuo Liu, Zi Qian, Heng Zhou, Jiahao Zhang, Yajie Zhang, Zhihang Li, Mengyu Zhou, Erchao Zhao, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29902v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态模型的智能体工具规划
tldr: "该研究针对多模态大模型在交错文本图像生成中无法兼顾事实性与创造性的问题，提出了“智能体工具规划”范式，使模型能自主决定何时及如何调用工具。为此，研究者推出了包含7,702个问答对的ATP-Bench基准测试集，并开发了MAM多智能体评估系统。实验揭示了现有模型在复杂工具规划方面的不足，为提升交错生成能力提供了重要参考。"
selection_source: fresh_fetch
motivation: 当前的交错生成方法往往将图像生成与检索视为互斥路径，难以在保证信息准确性的同时发挥创造力。
method: 引入了ATP-Bench基准测试集和MAM多智能体评估框架，通过25种视觉关键意图系统评估模型的工具规划能力。
result: 对10种主流多模态大模型的实验表明，现有模型在连贯的交错规划和工具调用精准度上仍存在显著挑战。
conclusion: 智能体工具规划是多模态交错生成的下一个里程碑，而ATP-Bench为推动该领域的发展提供了标准化的评估体系。
---

## 摘要
文本与图像交错生成是多模态大语言模型（MLLM）的一个重要前沿领域，为传达复杂信息提供了一种更直观的方式。当前的范式要么依赖于图像生成，要么依赖于检索增强，但通常将两者视为互斥的路径，未能将真实性与创造性统一起来。我们认为，该领域的下一个里程碑是智能体工具规划（Agentic Tool Planning），即模型作为中央控制器，自主决定何时、何地以及调用哪些工具，从而为视觉关键型查询生成交错式响应。为了系统地评估这一范式，我们推出了 ATP-Bench，这是一个包含 7,702 个问答对（包括 1,592 个视觉问答对）的新型基准测试，涵盖 8 个类别和 25 种视觉关键意图，并包含经人工验证的查询和标准答案。此外，为了在不依赖端到端执行和不断变化的工具后端的情况下评估智能体规划，我们提出了一种多智能体 MLLM 评测（MAM）系统。MAM 可以在不需要参考标准答案的情况下，评估工具调用的准确性，识别遗漏的工具使用机会，并评估整体响应质量。我们对 10 种最先进的 MLLM 进行了广泛实验，结果表明模型在连贯的交错规划方面表现挣扎，且在工具使用行为上表现出显著差异，这凸显了巨大的提升空间，并为推进交错生成提供了可操作的指导。数据集和代码可在 https://github.com/Qwen-Applications/ATP-Bench 获取。

## Abstract
Interleaved text-and-image generation represents a significant frontier for Multimodal Large Language Models (MLLMs), offering a more intuitive way to convey complex information. Current paradigms rely on either image generation or retrieval augmentation, yet they typically treat the two as mutually exclusive paths, failing to unify factuality with creativity. We argue that the next milestone in this field is Agentic Tool Planning, where the model serves as a central controller that autonomously determines when, where, and which tools to invoke to produce interleaved responses for visual-critical queries. To systematically evaluate this paradigm, we introduce ATP-Bench, a novel benchmark comprising 7,702 QA pairs (including 1,592 VQA pairs) across eight categories and 25 visual-critical intents, featuring human-verified queries and ground truths. Furthermore, to evaluate agentic planning independent of end-to-end execution and changing tool backends, we propose a Multi-Agent MLLM-as-a-Judge (MAM) system. MAM evaluates tool-call precision, identifies missed opportunities for tool use, and assesses overall response quality without requiring ground-truth references. Our extensive experiments on 10 state-of-the-art MLLMs reveal that models struggle with coherent interleaved planning and exhibit significant variations in tool-use behavior, highlighting substantial room for improvement and providing actionable guidance for advancing interleaved generation. Dataset and code are available at https://github.com/Qwen-Applications/ATP-Bench.

---

## 论文详细总结（自动生成）

### ATP-Bench：迈向多模态大语言模型交错生成的智能体工具规划 —— 论文深度总结

#### 1. 核心问题与整体含义
*   **研究动机**：当前的图文交错生成（Interleaved Generation）主要分为“纯生成”和“检索增强”两种范式。前者缺乏事实依据且难以处理复杂图表，后者则受限于外部语料库，缺乏针对特定查询的创造性修改能力。
*   **核心问题**：如何让多模态大语言模型（MLLM）像智能体一样，自主、灵活地协调“引用、搜索、生成、绘图、编辑”等多种手段，以生成既具事实性又具创造性的高质量交错响应？
*   **整体含义**：论文提出了**智能体工具规划（Agentic Tool Planning）**范式，并构建了首个专门评估该能力的基准测试 **ATP-Bench**，旨在推动 MLLM 从简单的内容生成器向复杂的任务规划者转变。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：将交错生成视为一个工具规划问题。模型作为中央控制器，在生成文本的过程中，根据“视觉关键意图”自主决定在何时、何处插入何种视觉内容。
*   **工具包（Toolkit）定义**：
    *   **Reference**：引用上下文或文档中的已有图像。
    *   **Diffusion**：利用扩散模型生成概念性或艺术性图像。
    *   **Search**：通过搜索引擎获取真实世界的实体图像（如地标、人物）。
    *   **Code**：编写代码生成数据驱动的图表或数学图像。
    *   **Edit**：对已有图像进行局部修改、标注或风格转换。
*   **MAM 评测系统（Multi-Agent MLLM-as-a-Judge）**：
    *   **Precision Inspector（精度检查员）**：评估工具调用的必要性、工具选择准确性、参数正确性及放置位置的合理性。
    *   **Recall Inspector（召回检查员）**：识别模型在应该使用视觉支持时却仅提供纯文本的“遗漏机会”。
    *   **Chief Judge（首席法官）**：综合上述报告，给出 0-100 的最终评分（Final Score）。

#### 3. 实验设计
*   **数据集（ATP-Bench）**：
    *   **规模**：7,702 个 QA 对，包含 1,592 个 VQA 对。
    *   **覆盖范围**：8 个类别（学术、手册、食谱、时尚、装修、产品、旅游、百科）和 25 种细分视觉意图。
    *   **构建流程**：查询收集 -> 意图验证 -> 三阶段标准答案（GT）生成（文本生成、图像插入、人工精修）。
*   **对比方法**：评估了 10 种 SOTA 模型，包括 Claude 4.5/4、Gemini 3 Pro、GPT-5、GPT-4o、Grok-4.1、Qwen3-VL-Plus、Qwen2.5-VL-72B、InternVL3.5-14B、LLaMA-3.2-11B。
*   **评估指标**：最终得分（FS）、成功率（SR）、遗漏图像数（MI）、工具采用率、F1 分数。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力（如 GPU 型号、数量或训练时长）。
*   **原因分析**：该研究侧重于基准测试的构建和对现有预训练模型的推理评估，而非从头训练模型。实验主要涉及 API 调用（如 Gemini, GPT, Claude）和开源模型的推理。

#### 5. 实验数量与充分性
*   **实验规模**：对 10 个主流模型进行了全量基准测试，涵盖了闭源顶尖模型和开源代表模型。
*   **实验维度**：
    *   **Few-shot 实验**：验证了示例对模型工具规划能力的提升作用。
    *   **消融实验**：探讨了工具能力边界定义对模型选择的影响，以及不同评测模型（Judge Model）作为裁判的一致性。
    *   **人工一致性研究**：抽取 400 个样本进行人工校验，证明了 MAM 系统与人类判断的高度一致性（相关性 > 0.8）。
*   **充分性评价**：实验设计全面、客观，通过多维度指标和人工验证确保了评估的公平性。

#### 6. 主要结论与发现
*   **模型表现**：Gemini 3 Pro 在所有指标上均处于领先地位（FS: 79.88），展现了极强的工具规划连贯性。
*   **任务难度**：学术和百科类别相对容易，而**旅游（Travel）**和**装修（Renovation）**是最难的类别，模型经常遗漏工具调用或执行错误。
*   **开源差距**：开源模型（如 LLaMA-3.2, InternVL3.5）在工具规划上表现较差，常出现格式错误或无效调用。
*   **行为偏好**：不同模型对工具的偏好不同，例如 Grok-4.1 调用工具最积极，而 GPT-5 相对保守。

#### 7. 优点
*   **范式创新**：首次将“智能体工具规划”引入交错生成领域，打破了检索与生成的二元对立。
*   **评估框架科学**：MAM 系统实现了“规划与执行解耦”，避免了因外部 API 波动导致的评估不准，且能精准捕捉“未调用”的错误。
*   **数据质量高**：结合了 QA 和 VQA，且经过严格的人工三阶段标注和验证。

#### 8. 不足与局限
*   **模态局限**：目前仅关注文本和图像，未扩展到音频、视频等更丰富的多模态输出。
*   **工具集规模**：仅定义了 5 种核心工具，可能无法完全覆盖未来更复杂的智能体应用场景。
*   **评测偏见风险**：虽然 MAM 系统经过人工验证，但作为裁判的 MLLM 本身可能存在自我偏好（Self-preference）或对特定风格的偏见。
*   **端到端闭环**：主要评估规划能力，对于工具执行后的反馈循环（如执行失败后的重试规划）探讨较少。

（完）
