---
title: Understanding the Role of Hallucination in Reinforcement Post-Training of Multimodal Reasoning Models
title_zh: 理解幻觉在多模态推理模型强化后训练中的作用
authors: "Gengwei Zhang, Jie Peng, Zhen Tan, Mufan Qiu, Hossein Nourkhiz Mahjoub, Vaishnav Tadiparthi, Kwonjoon Lee, Yanyong Zhang, Tianlong Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03179v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态推理模型的强化学习后训练
tldr: 本研究探讨了强化学习（RL）在提升多模态大模型（MLLM）视觉推理能力中的真实作用。作者提出了“幻觉作为线索”分析框架，通过引入模态特定的损坏数据（如删除关键视觉信息）来诱导模型产生幻觉。研究发现，RL后训练在纯幻觉诱导设置下仍能显著提升模型性能，甚至优于标准训练。这一发现揭示了幻觉在RL训练中的关键影响，挑战了模型真正学会视觉推理的假设，为未来开发更具模态感知力的RL训练方法提供了新视角。
selection_source: fresh_fetch
motivation: 探究强化学习在多模态模型后训练中，究竟是增强了视觉推理能力还是仅通过幻觉拟合结果。
method: 提出“幻觉作为线索”框架，通过故意损坏关键视觉信息来诱导模型产生幻觉，并以此诊断RL训练动态。
result: 实验证明，即使在缺乏关键视觉信息的幻觉诱导设置下，RL训练仍能显著提升甚至超越标准训练的推理表现。
conclusion: 研究揭示了幻觉在多模态RL训练中被低估的作用，挑战了现有训练假设，并呼吁开发更具模态感知性的训练设计。
---

## 摘要
最近强化学习（RL）在大语言推理模型中的成功，激发了越来越多的人采用 RL 对多模态大语言模型（MLLMs）进行后训练，以增强其视觉推理能力。尽管许多研究报告了性能的提升，但 RL 训练是否真正使模型能够从视觉信息中学习仍不清楚。在这项工作中，我们提出了“幻觉即线索”（Hallucination-as-Cue）框架，这是一个旨在从模型幻觉的角度研究基于 RL 的后训练对多模态推理模型影响的分析框架。具体而言，我们引入了诱导幻觉的、特定模态的损坏（corruptions），这些损坏移除或替换了推导正确答案所需的核心信息，从而迫使模型通过幻觉进行推理。通过在训练和评估过程中应用这些损坏，我们的框架为诊断 RL 训练动态和理解数据集的内在属性提供了一个独特的视角。通过在多个多模态推理基准测试上的广泛实验和分析，我们揭示了模型幻觉在 RL 训练中的作用比之前认识到的更为显著。例如，我们发现，在纯粹的幻觉诱导设置下进行 RL 后训练，仍然可以显著提高模型的推理性能，在某些情况下甚至优于标准训练。这些发现挑战了关于 MLLM 推理训练的普遍假设，并激励开发更具模态感知能力的基于 RL 的训练设计。

## Abstract
The recent success of reinforcement learning (RL) in large reasoning models has inspired the growing adoption of RL for post-training Multimodal Large Language Models (MLLMs) to enhance their visual reasoning capabilities. Although many studies have reported improved performance, it remains unclear whether RL training truly enables models to learn from visual information. In this work, we propose the Hallucination-as-Cue Framework, an analytical framework designed to investigate the effects of RL-based post-training on multimodal reasoning models from the perspective of model hallucination. Specifically, we introduce hallucination-inductive, modality-specific corruptions that remove or replace essential information required to derive correct answers, thereby forcing the model to reason by hallucination. By applying these corruptions during both training and evaluation, our framework provides a unique perspective for diagnosing RL training dynamics and understanding the intrinsic properties of datasets. Through extensive experiments and analyses across multiple multimodal reasoning benchmarks, we reveal that the role of model hallucination for RL-training is more significant than previously recognized. For instance, we find that RL post-training under purely hallucination-inductive settings can still significantly improve models' reasoning performance, and in some cases even outperform standard training. These findings challenge prevailing assumptions about MLLM reasoning training and motivate the development of more modality-aware RL-based training designs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Understanding the Role of Hallucination in Reinforcement Post-Training of Multimodal Reasoning Models》的结构化深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：近期强化学习（RL）在提升多模态大语言模型（MLLM）推理能力方面取得了显著成效（如类似 DeepSeek-R1 的后训练）。
*   **核心问题**：RL 训练究竟是让模型学会了“如何观察并结合视觉信息进行推理”，还是仅仅加强了模型内部的文本推理先验？
*   **研究动机**：由于目前的奖励机制（如 GRPO）通常仅基于最终答案的正确性（Modality-agnostic），模型可能会通过“幻觉”出推理过程并凑巧得到正确答案。作者希望通过研究幻觉在 RL 训练中的作用，揭示多模态推理训练的真实机制。

### 2. 方法论：Hallucination-as-Cue 框架
作者提出了一个名为**“幻觉即线索”**的分析框架，通过主动诱导幻觉来诊断模型行为：
*   **核心思想**：通过故意损坏（Corrupt）输入模态，移除推导答案所需的关键信息，迫使模型只能依靠幻觉进行推理。
*   **关键技术细节（三种损坏策略）**：
    1.  **空白图像替换 (BI)**：将所有训练图像替换为纯色块，完全移除视觉信息。
    2.  **随机图像替换 (RI)**：将图像替换为数据集中随机的其他图像，造成图文不匹配。
    3.  **文本信息移除 (TR)**：移除题目中的关键文本条件，仅保留图像和基础指令。
*   **算法流程**：使用 **GRPO（组相对策略优化）** 算法。在训练时，即使输入被损坏，只要模型通过幻觉生成的推理链最终得到了正确答案，该行为就会被正向强化（幻觉诱导奖励）。

### 3. 实验设计
*   **训练数据集**：
    *   **Geometry3K**：几何数学题目。
    *   **MMR1-V0**：综合性多模态数学推理数据集。
    *   **CLEVR**：经典的视觉空间推理数据集。
*   **评估基准 (Benchmarks)**：MathVision、MathVerse、MathVista、We-Math。
*   **对比设置**：
    *   **模型规模**：对比了 Qwen2.5-VL-3B 和 7B。
    *   **评估维度**：设计了 8 种设置（S1-S8），交叉对比了“正常/损坏训练”与“正常/损坏推理”的组合。

### 4. 资源与算力
*   **模型框架**：基于 Qwen2.5-VL 系列模型，使用 EasyR1 训练框架。
*   **超参数**：学习率 $1 \times 10^{-6}$，AdamW 优化器，训练 15 个 Episode，Rollout Size 为 5。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。但考虑到 Qwen2.5-VL-7B 的 RL 训练，通常需要多张 A100 或 H100 级别的显卡支持。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个训练集、4 个主流 Benchmark、2 种模型规模以及 3 种损坏策略。
*   **充分性**：实验设计较为全面，不仅验证了性能提升，还通过细粒度分析（如 MathVerse 的文本主导 vs 视觉主导分类）探讨了模型对不同模态的依赖程度。
*   **客观性**：通过引入“随机图像”这种极端不相关变量，有力地证明了模型在 RL 过程中对文本先验的过度依赖，实验逻辑严密且具有说服力。

### 6. 主要结论与发现
1.  **幻觉训练依然有效**：即使在完全没有正确视觉信息（BI 或 RI）的情况下进行 RL 训练，模型的推理性能依然能显著提升，甚至在某些 Benchmark 上超过了标准训练。
2.  **模型规模效应**：更大的模型（7B）从幻觉轨迹中获益更多。在 7B 模型上，使用随机图像（RI）训练的效果甚至优于正常图文配对训练。
3.  **视觉信息的负面影响**：在某些小模型（3B）上，移除图像（BI 推理）后的准确率反而比带图像推理更高，说明视觉信息有时会成为干扰而非助力。
4.  **本质揭示**：当前的 RL 后训练主要是在放大底层语言模型（LLM）的内在推理能力，而非真正增强了跨模态的理解与对齐。

### 7. 优点与亮点
*   **视角独特**：将通常被视为负面的“幻觉”转化为诊断工具，视角新颖。
*   **挑战直觉**：揭示了“图文不匹配训练也能提分”的惊人事实，挑战了多模态学习必须依赖精确对齐的传统假设。
*   **诊断价值**：为评估多模态数据集的质量（是否存在严重的文本偏见）提供了新方法。

### 8. 不足与局限
*   **机制解释尚浅**：虽然观察到了现象，但对于“为什么错误图像能促进通用推理能力”的深层数学或表征机制探讨较少。
*   **任务局限性**：研究主要集中在数学推理（Math Reasoning）任务上，对于需要高度视觉感知（如细粒度物体检测、OCR）的任务，结论是否成立尚待验证。
*   **方法覆盖**：主要针对 GRPO 这一种 RL 算法，未探讨其他推理范式（如潜空间推理或 MCTS）在幻觉下的表现。

（完）
