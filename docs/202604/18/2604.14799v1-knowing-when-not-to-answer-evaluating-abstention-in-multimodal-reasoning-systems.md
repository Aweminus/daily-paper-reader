---
title: "Knowing When Not to Answer: Evaluating Abstention in Multimodal Reasoning Systems"
title_zh: 知道何时不回答：评估多模态推理系统中的弃答行为
authors: "Nishanth Madhusudhan, Vikas Yadav, Alexandre Lacoste"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14799v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 多模态推理系统中的弃权机制
tldr: 本文探讨了多模态推理系统在面对证据不足时拒绝回答（EA）的能力。针对现有基准测试忽略不可回答性的现状，研究者提出了MM-AQA基准，通过视觉依赖和证据充分性两个维度构建了2079个样本。评估发现，主流视觉语言模型（VLM）在标准提示下极少拒绝回答，而多智能体系统虽有改善但存在准确率权衡。研究强调，实现有效的多模态拒绝回答需要专门的训练，而非仅靠提示工程或增加智能体规模。
selection_source: fresh_fetch
motivation: 现有的多模态模型评估范式通常假设问题皆可回答，导致模型在证据不足时仍强行响应，缺乏识别不可回答性的可靠性。
method: 引入MM-AQA基准，通过对视觉模态依赖和证据充分性进行变换，从可回答样本中构建出多种类型的不可回答实例。
result: 实验表明VLM在标准提示下几乎不拒绝回答，多智能体系统虽能提升拒绝率但面临性能权衡，且模型在处理退化或矛盾证据时表现不佳。
conclusion: 有效的多模态拒绝回答能力需要通过感知拒绝的专门训练来实现，单纯依靠提示优化或多智能体架构无法根本解决该问题。
---

## 摘要
有效弃答 (EA)，即识别证据不足并拒绝回答，对于构建可靠的多模态系统至关重要。然而，现有的视觉语言模型 (VLM) 和多智能体系统 (MAS) 评估范式通常假设问题是可回答的，从而迫使模型总是做出响应。弃答行为在纯文本设置中已有研究，但在多模态领域仍未得到充分探索；目前的基准测试要么忽略了不可回答性，要么依赖于粗糙的方法，从而遗漏了现实中的失效模式。我们引入了 MM-AQA，这是一个通过在视觉模态依赖性和证据充分性两个维度上进行转换，从可回答实例构建不可回答实例的基准测试。通过对涵盖闭源和开源模型的三个前沿 VLM 以及两种 MAS 架构在 2079 个样本上进行评估，我们发现：(1) 在标准提示下，VLM 很少弃答，甚至简单的置信度基准也优于这种设置；(2) MAS 改善了弃答表现，但引入了准确率与弃答之间的权衡；(3) 顺序设计的效果与迭代变体相当或更优，这表明瓶颈在于校准偏差而非推理深度；(4) 当图像或文本证据缺失时，模型会弃答，但在面对退化或矛盾的证据时，模型会尝试进行调和。有效的多模态弃答需要弃答感知训练，而非仅仅依靠更好的提示或增加智能体数量。

## Abstract
Effective abstention (EA), recognizing evidence insufficiency and refraining from answering, is critical for reliable multimodal systems. Yet existing evaluation paradigms for vision-language models (VLMs) and multi-agent systems (MAS) assume answerability, pushing models to always respond. Abstention has been studied in text-only settings but remains underexplored multimodally; current benchmarks either ignore unanswerability or rely on coarse methods that miss realistic failure modes. We introduce MM-AQA, a benchmark that constructs unanswerable instances from answerable ones via transformations along two axes: visual modality dependency and evidence sufficiency. Evaluating three frontier VLMs spanning closed and open-source models and two MAS architectures across 2079 samples, we find: (1) under standard prompting, VLMs rarely abstain; even simple confidence baselines outperform this setup, (2) MAS improves abstention but introduces an accuracy-abstention trade-off, (3) sequential designs match or exceed iterative variants, suggesting the bottleneck is miscalibration rather than reasoning depth, and (4) models abstain when image or text evidence is absent, but attempt reconciliation with degraded or contradictory evidence. Effective multimodal abstention requires abstention-aware training rather than better prompting or more agents.

---

## 论文详细总结（自动生成）

这是一份关于论文《Knowing When Not to Answer: Evaluating Abstention in Multimodal Reasoning Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：多模态推理系统（如视觉语言模型 VLM 和多智能体系统 MAS）在面对证据不足、信息模糊或矛盾的输入时，能否可靠地识别出“不可回答性”并选择**弃答（Abstention）**？
*   **研究背景**：当前的评估范式（如 MMMU 等基准）通常假设所有问题都有解，这迫使模型在面对不确定性时产生幻觉或过度自信。在医疗、法律等高风险领域，这种“强行回答”的行为具有潜在危害。
*   **研究意义**：论文旨在填补多模态弃答评估的空白，通过系统性地构建不可回答的基准测试，揭示现有前沿模型在校准和可靠性方面的局限。

### 2. 方法论：核心思想与技术细节
*   **MM-AQA 基准构建**：从现有的可回答基准（MMMU 和 MMLongBench-Doc）出发，通过**双轴变换**构建不可回答样本：
    *   **轴一：模态依赖性（针对 MMMU）**：根据图像对回答问题的必要程度（装饰性、部分、本质、唯一），对图像或文本进行针对性破坏。
    *   **轴二：证据充分性（针对文档推理）**：通过移除证据、损坏数据、语义矛盾或推理不可能（如时间跨度超出文档）来制造信息缺口。
*   **22 种变换类型**：包括侵略性裁剪、多重遮罩、高斯模糊、图像-问题不匹配、数值篡改、列合并歧义、时间偏移等。
*   **质量控制流水线**：
    1.  **变换路由**：根据问题类型自动选择最合适的破坏方式。
    2.  **双 VLM 共识验证（DC-QC）**：使用两个独立模型评估变换后的样本，仅保留被一致认为不可回答的样本。
    3.  **专家人工校验**：对样本进行抽样审核，确保标签的准确性。

### 3. 实验设计
*   **数据集**：
    *   **Abstain-MMMU (A-MMMU)**：553 个样本（涵盖 10 个 STEM 领域）。
    *   **Abstain-MMLongBench-Doc (A-MMLBD)**：1526 个样本（长文档理解）。
*   **评估对象**：
    *   **前沿 VLM**：GPT-5、Claude Sonnet 4.5、Qwen 2.5-32B-VL。
    *   **MAS 架构**：基于 AutoGen 构建，包含推理者（Reasoner）、验证者（Verifier）和编排者（Orchestrator），分为**顺序（Sequential）**和**迭代（Iterative）**两种模式。
*   **对比方法与基准（Baselines）**：
    *   **提示词变体**：标准提示、口头置信度（vconf）、思维链（CoT）、极端弃答条款（Extreme Abstain）。
    *   **传统基准**：随机弃答、最大概率阈值（MaxProb）、P(True) 自我评估、自洽性（Self-Consistency）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **推理细节**：由于主要评估的是闭源模型（GPT-5, Claude），实验主要通过 API 调用完成。开源模型 Qwen 2.5-32B-VL 提到是在内部托管环境下运行，用于获取 Token 级别的 Logits。

### 5. 实验数量与充分性
*   **实验规模**：共评估了 2079 个样本，涵盖了 3 种模型、3 种推理架构（独立 VLM、顺序 MAS、迭代 MAS）、6 种提示组合，总计产生了数十组对比数据。
*   **充分性评价**：
    *   **客观性**：引入了马修斯相关系数（MCC）作为核心指标，综合考虑了准确率和弃答率，避免了单一指标的偏差。
    *   **公平性**：对所有模型使用了统一的提示模板和评估流程，并设置了人类专家上限（Oracle）作为参照。
    *   **消融实验**：对变换类型进行了细分分析，探讨了不同破坏方式对模型弃答行为的影响。

### 6. 主要结论与发现
*   **VLM 默认极少弃答**：在标准提示下，前沿模型在不可回答样本上的弃答率接近于零，表现出严重的幻觉倾向。
*   **MAS 的双刃剑效应**：多智能体验证显著提升了弃答率，但导致了“准确率-弃答率”的权衡（即过度弃答，导致可回答问题的准确率下降）。
*   **推理深度非瓶颈**：迭代式 MAS 并不优于顺序式 MAS，说明问题不在于推理步数不够，而在于模型对自身知识边界的**校准错误**。
*   **结构显性原则**：模型更容易在证据完全缺失（如空白页）时弃答，但在证据存在但被退化或矛盾（如模糊、数值篡改）时，模型倾向于尝试“调和”并给出错误答案。

### 7. 优点与亮点
*   **系统性的分类法**：提出了基于模态依赖和证据充分性的变换分类法，比以往粗糙的扰动方法更贴近现实失效模式。
*   **高质量基准**：通过自动验证与人工校验相结合，确保了 MM-AQA 基准的可靠性。
*   **深入的 MAS 评估**：不仅评估了单个模型，还深入探讨了多智能体协作在弃答任务中的表现及瓶颈。

### 8. 不足与局限
*   **领域覆盖有限**：目前主要集中在 STEM 领域和文档理解，未覆盖视频、社交媒体或非技术性常识领域。
*   **QC 模块的循环论证风险**：质量控制使用了与评估对象同系列的模型，可能存在模型偏好一致性导致的偏差。
*   **缺乏训练干预**：论文主要通过提示工程和架构设计进行评估，未探讨通过微调（Fine-tuning）或强化学习（RL）来从根本上改善弃答能力。
*   **API 限制**：对于闭源模型无法获取 Logits，导致无法进行更深层的概率校准分析。

（完）
