---
title: "OpenMobile: Building Open Mobile Agents with Task and Trajectory Synthesis"
title_zh: OpenMobile：通过任务与轨迹合成构建开放式移动智能体
authors: "Kanzhi Cheng, Zehao Li, Zheng Ma, Nuo Chen, Jialin Cao, Qiushi Sun, Zichen Ding, Fangzhi Xu, Hang Yan, Jiajun Chen, Anh Tuan Luu, Jianbing Zhang, Lewei Lu, Dahua Lin"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15093v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于视觉语言模型的移动端智能体
tldr: 针对移动智能体训练数据封闭且合成方法不透明的问题，本文提出开源框架 OpenMobile。该框架通过环境记忆驱动的任务合成流水线生成多样化指令，并利用策略切换机制在轨迹生成中捕获关键的错误恢复数据。实验显示，基于该数据微调的 Qwen2.5/3-VL 模型在 AndroidWorld 等基准测试中表现卓越，显著提升了开源移动智能体的性能上限。
selection_source: fresh_fetch
motivation: 当前领先的移动智能体训练数据和合成配方大多不透明，阻碍了开源社区在该领域的研究与进步。
method: 开发了基于环境探索的任务合成流水线，并结合学习者与专家模型的策略切换机制来生成高质量轨迹。
result: "微调后的 Qwen3-VL 在 AndroidWorld 基准测试中取得 64.7% 的成功率，大幅领先于现有的开源数据方法。"
conclusion: OpenMobile 通过开源高质量的合成数据和方法论，为构建高性能、透明的移动智能体提供了重要支撑。
---

## 摘要
由视觉语言模型驱动的移动智能体在自动化移动任务方面展现了出色的能力，近期领先的模型实现了显著的性能飞跃，例如在 AndroidWorld 上达到了近 70% 的成功率。然而，这些系统通常不公开其训练数据，且对其任务和轨迹合成方案保持不透明。我们提出了 OpenMobile，这是一个合成高质量任务指令和智能体轨迹的开源框架，包含两个核心组件：(1) 一个可扩展的任务合成流水线，通过探索构建全局环境记忆，并利用其生成多样化且具有环境对齐性的指令；(2) 一种用于轨迹展开的策略切换策略。通过在学习者模型和专家模型之间交替，该策略捕捉到了标准模仿学习中经常缺失的关键错误恢复数据。在我们的数据上训练的智能体在三个动态移动智能体基准测试中取得了具有竞争力的结果：值得注意的是，我们微调的 Qwen2.5-VL 和 Qwen3-VL 在 AndroidWorld 上分别达到了 51.7% 和 64.7%，远超现有的开源数据方法。此外，我们对合成指令与基准测试集之间的重叠进行了透明分析，并验证了性能提升源于广泛的功能覆盖，而非对基准测试的过拟合。我们在 https://njucckevin.github.io/openmobile/ 发布了数据和代码，旨在弥合数据差距并促进更广泛的移动智能体研究。

## Abstract
Mobile agents powered by vision-language models have demonstrated impressive capabilities in automating mobile tasks, with recent leading models achieving a marked performance leap, e.g., nearly 70% success on AndroidWorld. However, these systems keep their training data closed and remain opaque about their task and trajectory synthesis recipes. We present OpenMobile, an open-source framework that synthesizes high-quality task instructions and agent trajectories, with two key components: (1) The first is a scalable task synthesis pipeline that constructs a global environment memory from exploration, then leverages it to generate diverse and grounded instructions. and (2) a policy-switching strategy for trajectory rollout. By alternating between learner and expert models, it captures essential error-recovery data often missing in standard imitation learning. Agents trained on our data achieve competitive results across three dynamic mobile agent benchmarks: notably, our fine-tuned Qwen2.5-VL and Qwen3-VL reach 51.7% and 64.7% on AndroidWorld, far surpassing existing open-data approaches. Furthermore, we conduct transparent analyses on the overlap between our synthetic instructions and benchmark test sets, and verify that performance gains stem from broad functionality coverage rather than benchmark overfitting. We release data and code at https://njucckevin.github.io/openmobile/ to bridge the data gap and facilitate broader mobile agent research.

---

## 论文详细总结（自动生成）

### OpenMobile: 通过任务与轨迹合成构建开放式移动智能体

#### 1. 论文的核心问题与整体含义（研究动机和背景）
当前基于视觉语言模型（VLM）的移动智能体在自动化任务方面取得了显著进展，领先的闭源模型（如 Step-GUI、MAI-UI）在 AndroidWorld 等基准测试中已接近 70% 的成功率。然而，这些系统的**训练数据和合成配方（Recipes）均不公开**，导致开源社区与工业界之间存在巨大的性能鸿沟（开源模型通常仅能达到 30% 左右）。
论文的核心问题在于：如何构建一个可扩展、高质量且透明的开源数据合成框架，以弥合这一差距，并深入理解驱动智能体性能的关键数据属性。

#### 2. 论文提出的方法论
OpenMobile 框架由两个关键阶段组成，旨在生成多样化的指令和包含错误恢复信号的轨迹：

*   **可扩展任务合成（Scalable Task Synthesis）：**
    *   **解耦探索与生成：** 不同于以往从单一轨迹推导指令的方法，OpenMobile 先通过随机游走探索环境，构建**全局环境记忆（Global Environment Memory）**。
    *   **记忆构建：** 对屏幕进行视觉去重，利用强 VLM 标注 UI 元素功能，并建立语义索引。
    *   **记忆增强合成：** 在生成指令时，同时调用“短期记忆”（邻近屏幕的功能）和“长期记忆”（语义相关的跨屏幕功能），从而合成复杂的、多步骤的、且在当前环境中可执行的任务。
*   **策略切换轨迹展开（Policy-Switching Rollout）：**
    *   **核心思想：** 传统的专家蒸馏只提供完美示范，导致智能体在推理出错时无法恢复。
    *   **错误干预切换（Error-Intervention Switch）：** 在轨迹生成过程中，由学习者模型（Learner）主导操作，并引入一个监控器（Monitor）。当监控器检测到学习者偏离目标时，触发专家模型（Expert，如 Gemini-3.1-Pro）介入纠正。这种方式强制在训练数据中加入了“错误-诊断-修正”的信号。

#### 3. 实验设计
*   **数据集与场景：** 涵盖了 20 个 Android 应用程序，合成了 2.8K 条指令和 34K 个动作步骤。
*   **Benchmark（基准测试）：**
    *   **AndroidWorld：** 行业标准的 Android 自动化测试环境。
    *   **AndroidLab：** 侧重于系统化评估和多模态能力的基准。
    *   **MobileWorld：** 包含长程任务和跨应用工作流的挑战性基准。
*   **对比方法：**
    *   **商业模型：** GPT-4o, Gemini-3-Pro。
    *   **开源/闭源领先模型：** UI-Venus, Step-GUI, MAI-UI, MobileAgent-v3.5。
    *   **开源数据基准：** UI-S1, ScaleCUA。

#### 4. 资源与算力
*   **微调细节：** 使用 LLaMA-Factory 框架，对 Qwen2.5-VL-7B 和 Qwen3-VL-8B 进行全参数监督微调（SFT）。
*   **超参数：** Batch size 为 32，学习率为 1e-5，训练 3 个 Epoch。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但考虑到 7B/8B 模型的全参数微调规模，通常需要 8 张 A100 或 H100 级别的显卡。

#### 5. 实验数量与充分性
*   **实验规模：** 论文进行了主性能对比、消融实验（针对任务合成策略和轨迹展开策略）、数据规模扩展实验（Data Scaling）。
*   **分析深度：** 包含详尽的**数据重叠分析（Overlap Analysis）**，通过语义相似度对比证明性能提升并非来自对测试集的过拟合；此外还进行了**功能覆盖度分析**和**错误恢复能力分析**。
*   **客观性：** 实验在三个不同的动态基准上运行多次并报告了方差（Pass@1 和 Pass@3），对比了多种开源和闭源基线，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能突破：** 基于 OpenMobile 数据训练的 Qwen3-VL-8B 在 AndroidWorld 上达到 **64.7%** 的成功率，显著优于之前的开源数据模型（约 30%），并接近闭源领先水平。
*   **错误恢复是关键：** 策略切换机制产生的错误恢复数据显著增强了智能体在实时环境中的鲁棒性。
*   **功能覆盖驱动泛化：** 性能的提升主要源于合成指令对应用功能的广泛覆盖，而非简单的指令重排。
*   **基础模型仍重要：** 即使数据相同，Qwen3-VL 的表现也显著优于 Qwen2.5-VL，说明底层 VLM 的 GUI 理解能力仍是性能天花板。

#### 7. 优点
*   **方法论创新：** 提出了“解耦探索与合成”的思路，通过全局记忆解决了合成指令单一、简单的痛点。
*   **解决分布偏移：** 策略切换机制有效地解决了模仿学习中的状态分布偏移问题。
*   **高度透明：** 论文对数据污染问题进行了非常坦诚且深入的定量分析，这在当前的 Agent 论文中较为罕见。
*   **开源贡献：** 提供了完整的合成流水线、数据和模型权重，对开源社区贡献巨大。

#### 8. 不足与局限
*   **探索策略简单：** 目前仅使用随机游走进行环境探索，对于深层功能或需要特定前置条件的 UI 状态覆盖可能不足。
*   **强化学习（RL）效果有限：** 文中提到尝试了轨迹级 RL，但相比 SFT 提升不明显，说明在动态 GUI 环境下的 RL 训练仍具挑战性。
*   **环境依赖：** 实验主要基于 AndroidWorld 模拟器，在真实物理手机设备上的迁移效果尚待进一步验证。
*   **算力细节缺失：** 未披露具体的训练资源消耗，不利于其他研究者评估复现成本。

（完）
