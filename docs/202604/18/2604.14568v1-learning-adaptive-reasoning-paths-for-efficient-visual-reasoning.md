---
title: Learning Adaptive Reasoning Paths for Efficient Visual Reasoning
title_zh: 学习用于高效视觉推理的自适应推理路径
authors: "Yixu Huang, Tinghui Zhu, Muhao Chen"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14568v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于高效跨模态推理的自适应视觉推理框架
tldr: "针对视觉推理模型（VRM）中存在的“推理路径冗余”和过度思考问题，本文提出了AVR框架。该框架将推理过程分解为感知、逻辑和应用三个功能，并允许模型根据任务复杂度动态选择全量推理、仅感知或直接回答三种模式。通过FS-GRPO强化学习算法训练，AVR在保持准确率的同时，将Token消耗降低了50%-90%，显著提升了视觉推理的效率。"
selection_source: fresh_fetch
motivation: 现有的视觉推理模型无论任务难易都会生成冗长的推理链，导致严重的计算资源浪费和推理路径冗余。
method: 提出AVR框架，通过FS-GRPO算法训练模型在全量推理、仅感知和直接回答三种路径间动态切换。
result: "在多个基准测试中，AVR在保持甚至提升准确率的前提下，成功减少了50%至90%的Token使用量。"
conclusion: 自适应视觉推理能有效缓解模型的过度思考问题，在保证推理质量的同时极大提高了计算效率。
---

## 摘要
视觉推理模型（VRMs）最近通过整合视觉感知与语言推理，展现了强大的跨模态推理能力。然而，它们经常面临“过度思考”（overthinking）的问题，即针对任何任务都会生成不必要的冗长推理链。我们将这一问题归因于视觉推理中的推理路径冗余（Reasoning Path Redundancy）：许多视觉问题并不需要完整的推理过程。为解决此问题，我们提出了 AVR，一个自适应视觉推理框架，它将视觉推理分解为三种认知功能：视觉感知、逻辑推理和答案应用。它进一步使模型能够动态地在三种响应格式中进行选择：全格式（Full Format）、仅感知格式（Perception-Only Format）和直接回答（Direct Answer）。AVR 使用 FS-GRPO 进行训练，这是对群体相对策略优化（Group Relative Policy Optimization）的一种改进，旨在鼓励模型在保持正确性的同时选择最高效的推理格式。在多个视觉语言基准测试上的实验表明，AVR 在保持整体准确率的同时，将 Token 使用量减少了 50% 至 90%，尤其是在感知密集型任务中。这些结果证明，自适应视觉推理可以有效缓解 VRM 中的过度思考问题。代码和数据可在以下网址获取：https://github.com/RunRiotComeOn/AVR。

## Abstract
Visual reasoning models (VRMs) have recently shown strong cross-modal reasoning capabilities by integrating visual perception with language reasoning. However, they often suffer from overthinking, producing unnecessarily long reasoning chains for any tasks. We attribute this issue to \textbf{Reasoning Path Redundancy} in visual reasoning: many visual questions do not require the full reasoning process. To address this, we propose \textbf{AVR}, an adaptive visual reasoning framework that decomposes visual reasoning into three cognitive functions: visual perception, logical reasoning, and answer application. It further enables models to dynamically choose among three response formats: Full Format, Perception-Only Format, and Direct Answer. AVR is trained with FS-GRPO, an adaptation of Group Relative Policy Optimization that encourages the model to select the most efficient reasoning format while preserving correctness. Experiments on multiple vision-language benchmarks show that AVR reduces token usage by 50--90\% while maintaining overall accuracy, especially in perception-intensive tasks. These results demonstrate that adaptive visual reasoning can effectively mitigate overthinking in VRMs. Code and data are available at: https://github.com/RunRiotComeOn/AVR.

---

## 论文详细总结（自动生成）

这篇论文针对视觉推理模型（VRM）中的“过度思考”问题，提出了自适应推理路径框架（AVR）。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉推理模型（如 Qwen3-VL-Thinking）倾向于对所有问题都生成冗长的思维链（CoT）。虽然这提升了复杂问题的表现，但对于简单任务（如 OCR 或简单识别）会导致严重的 **Token 浪费**，甚至可能因为中间推理步骤出错而降低准确率。
*   **核心概念**：论文提出了“**推理路径冗余**”（Reasoning Path Redundancy）的概念，即许多视觉问题不需要完整的“感知-推理-应用”全过程即可解决。
*   **研究目标**：使模型能够根据任务复杂度，动态选择最经济且准确的推理路径。

### 2. 方法论
AVR 框架将视觉推理分解为三个认知功能：**视觉感知**、**逻辑推理**和**答案应用**。
*   **三种响应格式**：
    1.  **全格式（Full Format）**：感知 + 推理 + 答案（适用于复杂逻辑任务）。
    2.  **仅感知格式（Perception-Only）**：感知 + 答案（适用于需要视觉细节但逻辑简单的任务）。
    3.  **直接回答（Direct Answer）**：仅答案（适用于常识或极简任务）。
*   **两阶段训练流程**：
    1.  **格式理解 SFT**：在 11k 条标注了不同格式的数据上进行微调，使模型具备生成三种格式的能力。
    2.  **格式切换 RL（FS-GRPO）**：基于群体相对策略优化（GRPO）改进的 **FS-GRPO** 算法。
*   **关键奖励函数设计**：
    *   **格式奖励 ($r_{format}$)**：正确回答给予奖励，且短格式（如直接回答）的奖励高于长格式。
    *   **多样性奖励 ($r_{div}$)**：防止模型陷入单一格式（格式崩溃），鼓励在训练早期探索不同路径。
    *   **长度缩放因子 ($s_L$)**：对超过长度阈值（如 300 tokens）的输出进行惩罚。

### 3. 实验设计
*   **数据集**：训练集包含 44k 样本（涵盖 OK-VQA, GQA, MathVerse 等 10 个数据集）。
*   **Benchmark（基准测试）**：
    *   **感知密集型**：OCRBench, TextVQA。
    *   **推理密集型**：MathVista, MathVision。
    *   **综合能力**：MMMU, MMMU-Pro, MMBench。
*   **对比方法**：
    *   官方基座模型（Instruct 版）和长推理模型（Thinking 版）。
    *   现有选择性推理方法：**TON** (Think-or-Not) 和 **ARM2**。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A6000 GPU**。
*   **训练细节**：
    *   SFT 阶段：学习率 $5 \times 10^{-5}$，训练 1 个 epoch。
    *   RL 阶段：学习率 $5 \times 10^{-7}$，KL 散度系数 0.02，每组采样 8 个响应（G=8）。
    *   推理阶段：最大 Token 限制为 8096。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个主流基准测试上进行了评估，涵盖了从 1B 到 8B 不同参数规模的模型（Qwen3-VL 和 InternVL3 两个家族）。
*   **消融实验**：针对格式组合（如去掉“仅感知”模式）、多样性奖励的必要性、以及超参数（长度阈值 $L$ 和格式奖励值 $f_k$）的敏感性做了详细测试。
*   **充分性评价**：实验设计非常充分，不仅验证了效率提升，还通过跨模型家族的测试证明了方法的通用性。

### 6. 主要结论与发现
*   **极高的效率提升**：AVR 在所有基准测试中实现了 **50%–90% 的 Token 削减**。
*   **性能保持甚至提升**：在感知密集型任务（如 OCRBench）上，AVR 准确率提升了 2-4%，因为它避免了冗长推理带来的干扰；在数学任务上，它能自动切换回长推理模式，保持了与 Thinking 模型相当的水平。
*   **自适应行为**：模型学会了“看题下菜”——数学题用全格式（约 78%），OCR 题用短格式（约 75%）。

### 7. 优点
*   **结构化分解**：将模糊的“思考”过程解构为明确的感知和推理模块，增加了可解释性。
*   **FS-GRPO 创新**：巧妙地将效率（长度惩罚）和多样性引入强化学习目标，解决了 RL 训练中常见的模式坍塌问题。
*   **通用性强**：不依赖特定架构，在 Qwen 和 InternVL 上均有效。

### 8. 不足与局限
*   **可验证奖励的依赖**：RL 阶段高度依赖有标准答案（Ground Truth）的数据，对于没有唯一答案的开放式视觉对话任务，奖励机制的设计会更复杂。
*   **SFT 数据成本**：第一阶段需要高质量的格式标注数据，虽然论文使用了 GPT-4o 生成，但在完全脱离闭源模型进行冷启动时可能存在挑战。
*   **潜在的“隐藏推理”**：虽然论文通过人工抽检认为比例较低（4.3%），但模型仍有可能在“感知”标签内偷偷进行逻辑计算，从而规避长度惩罚。

（完）
