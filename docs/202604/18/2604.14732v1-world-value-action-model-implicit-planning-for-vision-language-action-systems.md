---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于构建具有隐式规划能力的具身智能体的视觉-语言-动作模型
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏推理和评估能力的问题，本文提出了WAV模型。该模型通过学习结构化潜空间表示，结合世界模型预测未来状态和价值函数评估轨迹效用，实现了隐式规划。相比直接在动作空间搜索，WAV在潜空间进行推理，有效解决了可行轨迹概率随步数增加而指数衰减的问题，显著提升了复杂任务的成功率和泛化性。
selection_source: fresh_fetch
motivation: 现有的VLA模型多依赖直接动作预测，难以在复杂决策任务中进行长程轨迹推理和后果评估。
method: 提出WAV模型，通过世界模型预测未来状态并利用价值函数评估轨迹，在结构化潜空间中进行隐式规划以生成动作。
result: 实验证明WAV在仿真和真实场景中均优于现有SOTA方法，特别是在长程和组合任务中表现出更强的成功率和鲁棒性。
conclusion: 在潜空间进行隐式规划能有效重塑搜索分布，是提升具身智能体长程决策能力的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习以视觉观测和语言指令为条件的未来轨迹的结构化潜表征。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临随着时程增加，可行轨迹概率呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现高效的长时程决策。广泛的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：World-Value-Action (WAV) 模型

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA, RT-2）大多采用“直接动作预测”的反应式范式，缺乏对长程（Long-horizon）轨迹的推理能力和对动作后果的评估机制。这导致模型在处理复杂、多步骤的任务时，容易产生累积误差且鲁棒性不足。
本文提出了 **WAV 模型**，旨在将“世界模型预测”与“价值评估”整合进 VLA 系统，通过**潜空间隐式规划**（Implicit Planning）代替显式的动作空间搜索，使机器人能够预见未来并选择最优路径。

#### 2. 方法论：核心思想与关键技术
WAV 模型由三个紧密耦合的模块组成，均基于 Diffusion Transformer (DiT) 架构：
*   **核心思想**：将规划视为潜空间中的推理过程。模型不直接在动作空间搜索（因为可行动作在动作空间中占比随步数增加呈指数级下降），而是在学习到的轨迹潜空间中，通过迭代推理将概率质量集中在高价值且物理可行的区域。
*   **关键模块**：
    1.  **语言条件视频生成模块（世界模型）**：预测未来的视觉轨迹，捕捉环境动力学。
    2.  **轨迹价值模块**：评估预测视频轨迹的长程效用（Return）。
    3.  **动作解码模块**：结合视觉特征和价值特征，生成最终的可执行动作。
*   **训练流程（Flow Matching）**：采用三阶段训练：首先训练视频流模型，随后冻结视频模型训练价值预测，最后联合训练动作解码。
*   **推理算法（迭代潜空间优化）**：受 MPPI 启发，推理时并不固定潜变量分布，而是通过 $K$ 次迭代，根据价值评估结果不断更新视频和价值模块的噪声分布（均值和方差），从而精炼出最优轨迹。

#### 3. 实验设计
*   **仿真场景**：使用 **LIBERO** 基准测试，包含四个子集：Spatial（空间推理）、Object（物体泛化）、Goal（目标条件）和 Long（长程组合任务）。
*   **真实世界场景**：使用双臂 Piper 机器人，执行整理碗筷、铺平毛巾、以及复杂的“开抽屉-放物品-关抽屉”长程任务。
*   **对比基准（Baselines）**：
    *   **传统 VLA**：Diffusion Policy, Octo, OpenVLA, $\pi_0$ 等。
    *   **带世界模型的 VLA**：WorldVLA, DreamVLA, UniVLA, GE-ACT 等。

#### 4. 资源与算力
*   **硬件**：使用了 8 张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：
    *   LIBERO 任务：视频模块训练 4 万步，价值/动作模块训练 3 万步，总计约 **5 天**。
    *   真实世界任务：每个任务单独训练，约需 **3 天**。
*   **推理开销**：文中对比了不同迭代次数 $K$ 下的推理时间，显示 $K=3$ 是性能与效率的平衡点。

#### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的数十个任务上进行了测试，并在真实世界中对每个任务进行了 15 次独立实验。
*   **消融实验**：非常充分。作者详细研究了迭代次数 $K$、采样数量 $M$（视频）和 $N$（价值）、平滑参数 $\alpha/\beta$ 以及精英样本选择数量 $K_1/K_2$ 对成功率的影响。
*   **客观性**：通过理论分析（引理 4.1 和命题 4.2）为实验结果提供了数学支撑，证明了潜空间规划在长程任务中的必然优势。

#### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 榜单上取得了 **98.1%** 的平均成功率，达到 SOTA 水平。
*   **长程任务优势显著**：在 LIBERO-Long 任务中，WAV 相比不带规划的版本提升了约 3 个百分点；在真实世界长程任务中，成功率从基准（GE-ACT）的 35.6% 大幅提升至 **75.6%**。
*   **规划的必要性**：实验证明，随着任务时程增加，直接采样动作的失败率极高，而通过迭代精炼潜空间分布能有效缓解误差累积。

#### 7. 优点
*   **理论与实践结合**：不仅提出了模型，还通过数学证明解释了为什么在潜空间规划比在动作空间规划更有效（解决了“可行性消失”问题）。
*   **模块化设计**：将预测、评估和执行解耦，使得模型具有良好的解释性和可扩展性。
*   **鲁棒性强**：在真实世界中处理变形物体（毛巾）和多步协调任务表现出色。

#### 8. 不足与局限
*   **推理延迟**：由于采用了迭代推理（Iterative Inference），其推理速度慢于纯反应式模型，可能限制其在极高频控制场景下的应用。
*   **存储开销**：模型参数量约 2.2B，且推理时需要维护多个采样分支，对显存有一定要求。
*   **数据依赖**：价值函数的训练依赖于高质量的奖励标注或规则定义的奖励函数，这在某些极端复杂的非结构化环境中可能难以获取。

（完）
