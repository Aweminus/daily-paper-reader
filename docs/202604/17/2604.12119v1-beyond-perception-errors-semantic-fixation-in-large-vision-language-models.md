---
title: "Beyond Perception Errors: Semantic Fixation in Large Vision-Language Models"
title_zh: 超越感知错误：大视觉语言模型中的语义固着
authors: Md Tanvirul Alam
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.12119v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: VLM在抽象策略游戏中的推理
tldr: 本研究探讨了大型视觉语言模型（VLM）中的“语义固着”现象，即模型倾向于遵循默认语义先验而非提示词中的特定规则。作者开发了VLM-Fix基准，通过对比标准与反向规则下的棋盘游戏状态，发现模型在处理非标准规则时表现显著下降。研究通过提示词干预、后期训练和激活转向等方法验证了该现象，揭示了语义偏见对模型推理的深层影响。
selection_source: fresh_fetch
motivation: 旨在区分VLM的感知错误与规则映射错误，探究模型为何在提示词明确指定替代规则时仍坚持默认解释。
method: 引入VLM-Fix基准，利用四种抽象策略游戏在标准和反向规则配对下评估了14种主流VLM的性能。
result: 实验发现模型在标准规则下表现远优于反向规则，且中性提示词能缩小差距，而语义加载的提示词会加剧该偏差。
conclusion: 语义固着是VLM推理的固有缺陷，但可以通过特定的后期训练或后期激活转向技术进行有效干预和修复。
---

## 摘要
大视觉语言模型 (VLMs) 通常依赖熟悉的语义先验，但现有的评估方法未能清晰地将感知失败与规则映射失败区分开来。我们将这种行为研究为“语义固着”：即即使提示词指定了另一种同样有效的映射，模型仍保留默认解释。为了隔离这一效应，我们引入了 VLM-Fix，这是一个基于四种抽象策略游戏的受控基准测试，在成对的标准和反向规则表述下评估相同的终端棋盘状态。在 14 个开源和闭源 VLM 中，准确率一致偏向标准规则，揭示了显著的语义固着差距。提示词干预支持了这一机制：中性别名提示显著缩小了反向规则的差距，而带有语义负载的别名则再次拉大了差距。训练后表现出强烈的规则对齐性：在一种规则上训练会提高同规则的迁移效果，但会损害相反规则的迁移，而联合规则训练则能改善更广泛的迁移。为了测试合成游戏之外的外部有效性，我们在 VLMBias 上评估了类似的去熟悉化干预，并观察到了相同的定性模式。最后，后期层激活引导部分恢复了下降的性能，表明语义固着错误在后期表示中至少是部分可编辑的。项目页面、代码和数据集可在 https://maveryn.github.io/vlm-fix/ 获取。

## Abstract
Large vision-language models (VLMs) often rely on familiar semantic priors, but existing evaluations do not cleanly separate perception failures from rule-mapping failures. We study this behavior as semantic fixation: preserving a default interpretation even when the prompt specifies an alternative, equally valid mapping. To isolate this effect, we introduce VLM-Fix, a controlled benchmark over four abstract strategy games that evaluates identical terminal board states under paired standard and inverse rule formulations. Across 14 open and closed VLMs, accuracy consistently favors standard rules, revealing a robust semantic-fixation gap. Prompt interventions support this mechanism: neutral alias prompts substantially narrow the inverse-rule gap, while semantically loaded aliases reopen it. Post-training is strongly rule-aligned: training on one rule improves same-rule transfer but hurts opposite-rule transfer, while joint-rule training improves broader transfer. To test external validity beyond synthetic games, we evaluate analogous defamiliarization interventions on VLMBias and observe the same qualitative pattern. Finally, late-layer activation steering partially recovers degraded performance, indicating that semantic-fixation errors are at least partly editable in late representations. Project page, code, and dataset available at https://maveryn.github.io/vlm-fix/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Perception Errors: Semantic Fixation in Large Vision-Language Models》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：大型视觉语言模型（VLM）在处理任务时，究竟是因为“看不清”（感知错误）还是因为“脑筋转不过弯”（规则映射错误）而失败？
*   **研究动机**：现有的评估往往将感知和推理混为一谈。作者观察到 VLM 表现出一种**语义固着（Semantic Fixation）**现象（类似于心理学中的“定势效应”）：即模型过度依赖预训练中习得的常见语义先验（如“连成三子即为赢”），即使提示词明确要求使用另一种规则（如“连成三子即为输”），模型仍难以摆脱固有思维。
*   **整体含义**：本研究旨在通过隔离感知变量，深入探讨 VLM 在语义重映射（Semantic Remapping）方面的局限性。

### 2. 论文提出的方法论
*   **核心思想**：通过“相同视觉状态 + 不同规则解释”的对照实验，将语义理解从视觉感知中剥离。
*   **关键技术细节**：
    *   **VLM-Fix 基准测试**：包含四种抽象策略游戏（井字棋、四子棋、黑白棋、点格棋）。
    *   **规则配对**：为每种游戏设计“标准规则”和“反向规则”（Inverse/Misere）。例如，在井字棋中，标准规则是三连胜，反向规则是三连败。
    *   **提示词干预（Prompt Interventions）**：
        *   *Base*：使用“赢/输”等标准词汇。
        *   *Alias*：使用中性标签（如 POM/TOV）替代赢输，消除语义诱导。
        *   *SemAlias*：使用中性标签但显式定义其褒贬含义，测试语义负载的影响。
    *   **视觉变体**：改变棋盘纹理（Checkerboard）或玩家符号（Glyph），测试视觉熟悉度对固着效应的影响。
    *   **后期训练与转向**：使用监督微调（SFT）和带验证奖励的强化学习（RLVR/GRPO）进行规则对齐训练；并利用激活转向（Activation Steering）在推理阶段编辑模型表示。

### 3. 实验设计
*   **数据集/场景**：
    *   **VLM-Fix**：每种游戏生成 300 个唯一的终端棋盘状态，总计 12,000 个评估样本。
    *   **VLMBias**：作为外部验证，测试动物腿部计数等具有强烈先验偏见的现实场景。
*   **Benchmark 与对比方法**：
    *   评估了 **14 种主流 VLM**，包括闭源模型（GPT-4o/5、Claude 3.5 Sonnet）和开源模型（Qwen2.5/3-VL、InternVL3.5、Molmo2）。
    *   对比了直接回答（Direct）与思维链（CoT）提示。
    *   设置了“纯文本”和“描述性规则”作为控制组。

### 4. 资源与算力
*   **算力说明**：论文中提到了具体的训练超参数，如全局 Batch Size 为 128，使用 QLoRA（4-bit 量化）进行微调，强化学习使用了 GRPO 算法。
*   **具体资源**：文中**未明确说明**具体的 GPU 型号（如 A100/H100）数量及总训练时长，但提到了在微调 Qwen2.5-VL-3B/7B 等模型时使用了 bf16 精度。

### 5. 实验数量与充分性
*   **实验规模**：
    *   横跨 14 个模型，涵盖了从 3B 到 32B 以及顶级闭源 API 的参数梯度。
    *   每种配置（5 种提示/视觉变体 × 2 种规则 × 2 种查询目标 × 2 种输入顺序）均进行了详尽测试。
    *   包含了消融实验（如视觉扰动、输入顺序影响）和泛化性测试（VLMBias）。
*   **充分性评价**：实验设计非常充分且严谨。通过控制变量法（保持图像完全一致，仅改变文本规则），客观地证明了性能下降源于语义映射而非视觉识别。

### 6. 论文的主要结论与发现
1.  **语义固着差距显著**：所有模型在反向规则下的准确率均大幅下降（平均下降 14.6%），证明了模型对熟悉语义的依赖。
2.  **语义框架调节作用**：使用中性标签（Alias）能显著缩小规则差距，而一旦赋予标签语义含义（SemAlias），偏差会立即回归。
3.  **后期训练的规则局限性**：SFT 和 RLVR 能显著提升特定规则下的表现，但往往会导致负迁移（即学了反向规则后，标准规则表现下降），除非进行联合规则训练。
4.  **表示层可编辑性**：通过激活转向技术，可以在不重新训练的情况下，在模型的后期解码层纠正语义固着错误。
5.  **外部有效性**：在现实偏见数据集（VLMBias）上，去熟悉化干预（如翻转图像或使用别名）同样能有效降低偏见。

### 7. 优点：方法或实验设计上的亮点
*   **隔离变量的精妙性**：通过策略游戏的胜负判定这一极简模型，完美隔离了感知与逻辑映射。
*   **多维度干预**：不仅停留在提示词层面，还深入到后期训练（RL/SFT）和模型内部表示（激活转向），提供了全栈式的分析。
*   **认知心理学视角**：引入“语义固着”概念，为理解大模型的推理失败提供了新的理论框架。

### 8. 不足与局限
*   **合成环境局限**：VLM-Fix 主要基于合成的棋盘游戏，虽然有助于控制变量，但与复杂的现实世界推理场景仍有距离。
*   **激活转向的依赖性**：转向实验依赖于“捐赠者（Donor）”轨迹，即需要一个已知正确答案的参考表示，这在完全未知的推理任务中难以实现。
*   **模型规模效应**：虽然测试了多个规模，但对于超大规模模型（如 GPT-5 级别）是否能通过规模增长自发消除语义固着，研究尚处于初步观察阶段。

（完）
