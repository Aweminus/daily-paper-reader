---
title: "VLN-NF: Feasibility-Aware Vision-and-Language Navigation with False-Premise Instructions"
title_zh: VLN-NF：具有虚假前提指令的可行性感知视觉语言导航
authors: "Hung-Ting Su, Ting-Jun Wang, Jia-Fong Yeh, Min Sun, Winston H. Hsu"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10533v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 具有虚假前提推理能力的视觉语言导航智能体
tldr: 针对传统视觉语言导航（VLN）假设指令始终可行的问题，本文提出了VLN-NF基准，包含目标不存在的虚假前提指令。研究者通过LLM和VLM构建了自动化生成流水线，并提出REV-SPL评价指标。同时，提出了ROAM模型，通过两阶段混合策略结合房间级导航与基于大模型的室内探索，显著提升了智能体在不可靠指令下的判断与导航能力。
selection_source: fresh_fetch
motivation: 现有VLN研究假设指令目标必然存在，导致智能体无法处理现实中可能出现的虚假前提或不存在的目标任务。
method: 构建了VLN-NF基准，并提出ROAM模型，该模型结合了监督式房间导航与由LLM/VLM驱动、具备空间先验的室内探索策略。
result: 实验表明ROAM在REV-SPL指标上表现最优，而传统基准模型在面对虚假指令时往往存在探索不足和过早终止的问题。
conclusion: VLN-NF基准和ROAM模型为开发具备可行性感知能力的导航智能体提供了重要参考，强调了深入探索对识别虚假指令的关键作用。
---

## 摘要
传统的视觉语言导航（VLN）基准测试假设指令是可行的且所引用的目标存在，这使得智能体在处理虚假前提目标时能力不足。我们引入了 VLN-NF，这是一个包含虚假前提指令的基准测试，其中目标在指定的房间中缺失，智能体必须进行导航，通过室内探索收集证据，并明确输出“未找到”（NOT-FOUND）。VLN-NF 是通过一个可扩展的流水线构建的，该流水线使用大语言模型（LLM）重写 VLN 指令，并使用视觉语言模型（VLM）验证目标的缺失，从而生成看似合理但事实上错误的目标。我们进一步提出了 REV-SPL，用于联合评估房间到达、探索覆盖范围和决策正确性。为了应对这一挑战，我们提出了 ROAM，这是一种两阶段混合模型，结合了监督式房间级导航与由自由空间间隙先验引导的 LLM/VLM 驱动室内探索。ROAM 在对比方法中实现了最佳的 REV-SPL，而基线模型在不可靠指令下往往探索不足并过早终止。VLN-NF 项目页面见 https://vln-nf.github.io/。

## Abstract
Conventional Vision-and-Language Navigation (VLN) benchmarks assume instructions are feasible and the referenced target exists, leaving agents ill-equipped to handle false-premise goals. We introduce VLN-NF, a benchmark with false-premise instructions where the target is absent from the specified room and agents must navigate, gather evidence through in-room exploration, and explicitly output NOT-FOUND. VLN-NF is constructed via a scalable pipeline that rewrites VLN instructions using an LLM and verifies target absence with a VLM, producing plausible yet factually incorrect goals. We further propose REV-SPL to jointly evaluate room reaching, exploration coverage, and decision correctness. To address this challenge, we present ROAM, a two-stage hybrid that combines supervised room-level navigation with LLM/VLM-driven in-room exploration guided by a free-space clearance prior. ROAM achieves the best REV-SPL among compared methods, while baselines often under-explore and terminate prematurely under unreliable instructions. VLN-NF project page can be found at https://vln-nf.github.io/.

---

## 论文详细总结（自动生成）

### 论文总结：VLN-NF —— 具有虚假前提指令的可行性感知视觉语言导航

#### 1. 核心问题与整体含义
传统的视觉语言导航（VLN）任务通常基于一个理想化假设：人类给出的指令总是正确的，且目标物体必然存在于环境中。然而在现实场景中，人类经常会提供错误信息（如“去厨房拿桌上的杯子”，但杯子其实在客厅）。
本文提出了**VLN-NF (Vision-and-Language Navigation with Not-Found)** 任务，旨在研究智能体在面对“虚假前提指令”时的表现。智能体不仅需要导航到目标区域，还必须通过主动探索收集证据，并在确认目标不存在时明确输出“未找到（NOT-FOUND）”，而非盲目猜测或无休止搜索。

#### 2. 方法论
论文提出了一个完整的体系，包括数据集构建、评价指标和导航框架：
*   **VLN-NF 数据集构建流水线**：
    *   **LLM 重写器**：利用大语言模型（如 GPT-3.5）将原始可行的指令中的目标物体替换为语义合理但实际不存在的物体。
    *   **VLM 验证器**：利用视觉语言模型（如 GLIP）扫描目标房间的所有视角，确保替换后的物体确实不存在，从而生成高质量的虚假前提指令。
*   **评价指标 REV-SPL**：
    *   针对传统指标无法衡量“探索充分性”的问题，提出了 **Reach–Explore–Verify SPL**。它综合考虑了房间到达率（Reach）、决策正确性（Decision）、室内物体覆盖率（Coverage）以及路径效率。该指标会惩罚证据不足的过早停止行为。
*   **ROAM 导航框架（Room-Object Aware Movement）**：
    *   **两阶段混合架构**：第一阶段使用监督学习的“房间导航器”（基于 DUET）将智能体引导至目标房间；第二阶段使用基于 LLM/VLM 的“室内探索器”进行细粒度搜索。
    *   **FREE 引擎（Free-space Raycasting Estimation Engine）**：为了弥补 LLM 空间推理能力的不足，利用 Grounded-SAM 和射线投射技术估计候选方向的自由空间距离（Clearance），为 LLM 提供几何避障和探索潜力的先验信息。

#### 3. 实验设计
*   **数据集与场景**：基于 Matterport3D 环境和 REVERIE 数据集构建。
*   **对比方法**：
    *   **监督学习基线**：DUET（增加了 NOT-FOUND 动作分支）。
    *   **纯 LLM/VLM 代理**：NavGPT、MapGPT、SoM-Gemini-2.0-Flash。
    *   **混合模型**：ROAM（本文方法，分别搭载 GPT-3.5 和 GPT-4o）。
*   **Benchmark 评估**：在 Val-seen 和 Val-unseen 集合上评估 Reach SR（房间到达成功率）、Reach&Decision SR（到达且决策正确率）以及核心指标 REV-SPL。

#### 4. 资源与算力
*   **GPU 算力**：训练房间导航器（DUET 骨干）使用了 **NVIDIA RTX 4090**，训练时长约为 **48 小时**。
*   **API 开销**：
    *   NavGPT 推理成本约 < $8。
    *   MapGPT 推理成本约 $215。
    *   SoM-Gemini 推理成本约 $180。
    *   ROAM (GPT-3.5) 推理成本约 < $3。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **主实验**：对比了监督学习、纯 LLM 规划和混合架构在虚假指令下的表现。
*   **消融实验**：验证了两阶段架构和 FREE 几何先验对性能的提升。
*   **迁移实验**：测试了 ROAM 在标准（指令正确）的 REVERIE 任务上的通用性。
*   **敏感性分析**：分析了不同物体覆盖率阈值对评估结果的影响。
*   **错误分析**：对决策错误的原因（导航失败、感知不确定性、探索控制失败）进行了量化分解。
实验设计较为全面，涵盖了从数据集质量到模型泛化能力的各个方面，对比实验考虑了不同技术路线的代表性模型，具有较好的客观性。

#### 6. 主要结论与发现
*   **现有模型表现不佳**：传统监督学习模型（如 DUET）在面对虚假指令时倾向于过早停止，探索不足；而纯 LLM 模型虽然有常识，但在长程导航上极易失败。
*   **ROAM 的优越性**：ROAM 在 REV-SPL 指标上达到 6.1%，比监督学习基线提升了 **45%**，且仅需房间级标注。
*   **几何先验的重要性**：FREE 模块提供的空间距离信息显著提升了 LLM 在室内探索时的覆盖范围和效率。
*   **REV-SPL 的有效性**：该指标能有效区分“瞎猜”和“基于证据的决策”。

#### 7. 优点
*   **任务创新**：首次在 3D 部分可观测环境下系统性地研究“虚假前提”导航任务，更贴近现实应用。
*   **自动化流水线**：提出了一种低成本、可扩展的虚假指令生成方法，无需大量人工标注。
*   **指标科学**：REV-SPL 解决了 VLN 任务中“停止决策”难以评估的顽疾。
*   **模块化设计**：ROAM 结合了监督学习的鲁棒导航和大模型的常识推理，且 FREE 模块具有即插即用的特性。

#### 8. 不足与局限
*   **数据过滤比例较高**：为了保证验证的准确性，仅保留了约 45% 的原始 REVERIE 样本，可能存在一定的分布偏差。
*   **语言局限性**：目前仅支持英文指令。
*   **阈值敏感性**：ROAM 的性能高度依赖于物体检测模型（Grounding-DINO）的置信度阈值，在不同环境下可能需要重新调优。
*   **恢复行为单一**：目前智能体在发现目标不存在后仅能输出 NOT-FOUND 并停止，缺乏更高级的交互行为（如询问人类或寻找替代品）。

（完）
