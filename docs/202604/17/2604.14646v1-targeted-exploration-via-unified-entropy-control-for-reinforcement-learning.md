---
title: Targeted Exploration via Unified Entropy Control for Reinforcement Learning
title_zh: 通过统一熵控制实现强化学习中的针对性探索
authors: "Chen Wang, Lai Wei, Yanzhi Zhang, Chenyang Shao, Zedong Dan, Weiran Huang, Ge Lan, Yue Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14646v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 强化学习用于提升VLM和LLM的推理能力
tldr: "针对大模型强化学习中GRPO算法易出现的熵坍缩和过早收敛问题，本文提出UEC-RL框架。该框架通过统一熵控制机制，在困难任务上激活针对性探索以寻找高价值推理路径，同时利用稳定器防止熵过度增长，确保训练稳定性。实验证明，UEC-RL在LLM和VLM推理任务上显著优于基准，在Geometry3K上较GRPO提升37.9%，有效平衡了探索与收敛。"
selection_source: fresh_fetch
motivation: 解决GRPO算法在强化学习训练中因熵坍缩导致的策略多样性丧失和过早收敛问题。
method: 提出UEC-RL框架，通过在困难提示词上加强探索并结合稳定器控制熵值，实现针对性的探索与优化稳定。
result: "在LLM和VLM推理任务中表现优异，特别是在Geometry3K数据集上比GRPO实现了37.9%的相对提升。"
conclusion: UEC-RL通过精准的熵控制有效扩展了搜索空间并维持了训练鲁棒性，是提升大模型推理能力的关键技术。
---

## 摘要
强化学习（RL）的最新进展提升了大语言模型（LLM）和视觉语言模型（VLM）的推理能力。然而，广泛使用的群体相对策略优化（GRPO）始终面临熵崩溃（entropy collapse）问题，导致策略过早收敛并失去多样性。现有的探索方法在探索过程中引入了额外的偏差或方差，使得难以维持优化的稳定性。我们提出了强化学习统一熵控制（UEC-RL），这是一个为探索和稳定提供针对性机制的框架。UEC-RL 在困难提示词上激活更多探索，以搜索潜在且有价值的推理轨迹。同时，稳定器可防止熵不受控制地增长，从而在模型巩固可靠行为时保持训练稳定。这些组件共同作用，在需要时扩展搜索空间，同时在整个训练过程中保持鲁棒的优化。在 LLM 和 VLM 推理任务上的实验表明，在 Pass@1 和 Pass@$k$ 指标上，该方法相较于 RL 基准均有持续提升。在 Geometry3K 数据集上，UEC-RL 相比 GRPO 实现了 37.9% 的相对提升，表明其在不损害收敛性的情况下维持了有效的探索，并凸显了 UEC-RL 是在大模型中扩展基于 RL 的推理的关键。我们的代码可在 https://github.com/597358816/UEC-RL 获取。

## Abstract
Recent advances in reinforcement learning (RL) have improved the reasoning capabilities of large language models (LLMs) and vision-language models (VLMs). However, the widely used Group Relative Policy Optimization (GRPO) consistently suffers from entropy collapse, causing the policy to converge prematurely and lose diversity. Existing exploration methods introduce additional bias or variance during exploration, making it difficult to maintain optimization stability. We propose Unified Entropy Control for Reinforcement Learning (UEC-RL), a framework that provides targeted mechanisms for exploration and stabilization. UEC-RL activates more exploration on difficult prompts to search for potential and valuable reasoning trajectories. In parallel, a stabilizer prevents entropy from growing uncontrollably, thereby keeping training stable as the model consolidates reliable behaviors. Together, these components expand the search space when needed while maintaining robust optimization throughout training. Experiments on both LLM and VLM reasoning tasks show consistent gains over RL baselines on both Pass@1 and Pass@$k$. On Geometry3K, UEC-RL achieves a 37.9\% relative improvement over GRPO, indicating that it sustains effective exploration without compromising convergence and underscoring UEC-RL as a key for scaling RL-based reasoning in large models. Our code is available at https://github.com/597358816/UEC-RL.

---

## 论文详细总结（自动生成）

### 论文总结：通过统一熵控制实现强化学习中的针对性探索 (UEC-RL)

#### 1. 核心问题与整体含义
本文旨在解决大语言模型（LLM）和视觉语言模型（VLM）在强化学习（RL）后训练阶段，尤其是使用 **GRPO（群体相对策略优化）** 算法时面临的两个核心挑战：
*   **熵坍缩（Entropy Collapse）：** 策略熵迅速下降，导致模型过早收敛于简单的推理模式，丧失多样性，无法发现低概率但高价值的复杂推理路径。
*   **训练不稳定：** 在复杂任务（如多模态推理）中，梯度方差大，导致性能剧烈波动甚至崩溃。
研究动机在于建立一种**双向熵控制机制**，既能在遇到难题时主动增加探索（增熵），又能在巩固知识时确保收敛稳定（减熵）。

#### 2. 方法论：UEC-RL 框架
UEC-RL 的核心思想是“按需探索，稳健巩固”，包含两个关键组件：
*   **针对性探索机制（Targeted Exploration）：**
    *   **困难识别：** 如果初始的 $G$ 个采样均未得到正确答案，则将该 Prompt 标记为“困难”。
    *   **空间扩展：** 仅针对困难 Prompt，使用更高的温度系数 $t'$ 和更大的采样组 $G'$ 进行采样，以激活模型探索低概率推理路径的能力。
    *   **选择性保留：** 过滤掉低优势（Advantage）的噪声轨迹，仅保留有助于优化的有效样本。
*   **可控熵稳定器（Controllable Entropy Stabilizer）：**
    *   **经验回放：** 建立一个缓冲区，存储高优势的正确轨迹。
    *   **重复强化：** 通过定期回放这些高质量样本，根据论文推导的定理（Theorem 4.2），增加高优势动作的概率会产生正协方差，从而自然降低策略熵，引导模型向稳定解收敛。
*   **算法流程：** 1. 执行常规采样；2. 若失败则触发针对性探索采样；3. 将高质量样本存入 Buffer；4. 更新策略；5. 定期进行回放稳定。

#### 3. 实验设计
*   **数据集：**
    *   **文本推理：** DAPO-17K。
    *   **多模态推理：** 包含 6k 样本的混合数据集（涵盖 Geometry3K, ChartQA, ScienceQA 等 10 余个来源）。
    *   **领域内分析：** Geometry3K。
*   **Benchmark：**
    *   **文本：** AIME24/25, MATH, GSM8K, Minerva, ARC, MMLU pro。
    *   **多模态：** MathVision, MathVerse, MathVista, We-Math。
*   **对比方法：** GRPO (基准), DAPO (剪切优化), KL-cov (协方差感知 KL 正则), Entropy-Adv (熵增强优势函数)。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 张 NVIDIA A800 GPU (40GB)**。
*   **模型：** 初始化模型为 Qwen2.5-VL-7B-Instruct 和 Qwen2.5-Math-7B。
*   **训练细节：** 最大响应长度 8192，Batch Size 512，学习率 $1 \times 10^{-6}$。具体训练时长未明确说明，但提到 UEC-RL 的单步训练耗时约为 GRPO 的 0.79 倍（由于过滤了无效样本，效率更高）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 11 个主流文本和多模态基准上进行了测试，涵盖了从小学数学到竞赛级数学的各种难度。
*   **消融实验：** 针对探索温度 $t'$、稳定器预算 $s'$ 进行了参数敏感性分析，并对“探索模块”和“稳定模块”分别进行了移除实验。
*   **充分性评价：** 实验设计较为全面，不仅关注 Pass@1 准确率，还分析了 Pass@$k$ 曲线、熵演变曲线和响应长度变化。对比方法均为近年来的前沿改进方案，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先：** UEC-RL 在所有基准上均优于 GRPO 及其变体。在 Geometry3K 上实现了 **37.9%** 的相对提升。
*   **多样性增强：** Pass@$k$ 的提升证明 UEC-RL 学习到的策略分布更广且质量更高。
*   **训练更稳：** 相比于其他增加探索的方法（如 DAPO），UEC-RL 的熵曲线和奖励曲线更加平滑，有效避免了训练崩溃。
*   **效率优势：** 尽管引入了额外的采样步骤，但通过选择性优化有效样本，整体计算效率优于传统的 GRPO。

#### 7. 优点
*   **理论支撑：** 论文通过两个定理（4.1 和 4.2）从数学上解释了为何增加探索温度能增熵，以及重复高优势样本能减熵，逻辑严密。
*   **针对性强：** 不同于盲目增加全局熵，UEC-RL 只在模型“不会做”的难题上发力，避免了在简单任务上引入不必要的随机性。
*   **通用性：** 框架同时适用于纯文本 LLM 和多模态 VLM，展现了良好的泛化能力。

#### 8. 不足与局限
*   **超参数敏感性：** 实验表明，探索温度 $t'$ 和回放预算 $s'$ 的平衡对结果至关重要。不同难度的任务可能需要不同的参数组合，缺乏自动化的参数调节机制。
*   **计算开销：** 虽然单步效率高，但在困难 Prompt 上需要进行额外的 $G'$ 次采样，这在推理成本极高的超大规模模型上可能会带来一定的采样压力。
*   **依赖验证器：** 该方法依赖于可验证的奖励（Verifiable Rewards），对于无法自动判定对错的开放式生成任务，其适用性受限。

（完）
