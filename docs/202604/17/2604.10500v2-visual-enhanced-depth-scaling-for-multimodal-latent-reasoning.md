---
title: Visual Enhanced Depth Scaling for Multimodal Latent Reasoning
title_zh: 面向多模态潜空间推理的视觉增强深度缩放
authors: "Yudong Han, Yong Wang, Zaiquan Yang, Zhen Qu, Liyuan Pan, Xiangxiang Chu"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10500v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 多模态潜推理与视觉欠优化
tldr: 本研究针对多模态隐式推理中视觉优化不足和固定深度限制推理能力的问题，提出了视觉增强深度缩放框架。通过分析梯度动力学，发现视觉标记存在欠优化且复杂标记需要更多计算。为此，引入视觉重放模块强化细粒度感知，并结合路由深度缩放机制为复杂标记分配更多推理步骤。配合课程学习策略，该方法在提升推理速度的同时，在多个基准测试上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 发现多模态隐式推理中视觉标记因语言偏见导致欠优化，且固定模型深度限制了复杂标记的推理能力。
method: 提出视觉重放模块以增强空间一致性感知，并结合路由深度缩放机制为复杂标记动态分配推理深度。
result: 该框架在多个多模态基准测试中取得领先性能，且推理速度显著优于显式思维链（CoT）基准。
conclusion: 通过优化视觉感知与动态调整推理深度，隐式潜空间推理能够兼顾高效推理与深度逻辑表达。
---

## 摘要
多模态潜空间推理已成为一种极具前景的范式，它通过隐式特征传播取代了显式的思维链（CoT）解码，在增强表示信息量的同时降低了推理延迟。通过分析潜空间训练过程中的 token 级梯度动力学，我们揭示了两个关键观察结果：（1）由于固有的语言偏见，视觉 token 的梯度范数显著高于文本 token 且波动更大，导致了系统性的视觉欠优化；（2）语义简单的 token 收敛迅速，而复杂 token 在固定架构深度的限制下表现出持续的梯度不稳定性。为了解决这些局限性，我们提出了视觉重放模块和路由深度缩放，以协同增强视觉感知并精炼复杂的潜变量，从而实现更深层次的上下文推理。前者利用因果自注意力机制来估计 token 的显著性，通过空间相干约束强化细粒度的视觉定位。作为补充，后者机制能够自适应地为复杂 token 分配额外的推理步骤，从而实现更深层的上下文精炼。在课程学习策略的指导下，该策略将显式 CoT 逐步内化为紧凑的潜空间表示，我们的框架在多个基准测试中达到了最先进的性能，同时相比显式 CoT 基准实现了显著的推理加速。

## Abstract
Multimodal latent reasoning has emerged as a promising paradigm that replaces explicit Chain-of-Thought (CoT) decoding with implicit feature propagation, simultaneously enhancing representation informativeness and reducing inference latency. By analyzing token-level gradient dynamics during latent training, we reveal two critical observations: (1) visual tokens exhibit significantly higher and more volatile gradient norms than their textual counterparts due to inherent language bias, resulting in systematic visual under-optimization; and (2) semantically simple tokens converge rapidly, whereas complex tokens exhibit persistent gradient instability constrained by fixed architectural depths. To address these limitations, we propose a visual replay module and routing depth scaling to collaboratively enhance visual perception and refine complicated latents for deeper contextual reasoning. The former module leverages causal self-attention to estimate token saliency, reinforcing fine-grained grounding through spatially-coherent constraints. Complementarily, the latter mechanism adaptively allocates additional reasoning steps to complex tokens, enabling deeper contextual refinement. Guided by a curriculum strategy that progressively internalizes explicit CoT into compact latent representations, our framework achieves state-of-the-art performance across diverse benchmarks while delivering substantial inference speedups over explicit CoT baselines.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Vedas** 的框架，旨在通过视觉增强和动态深度缩放来优化多模态大模型的潜空间推理（Latent Reasoning）。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：多模态潜空间推理通过在隐藏空间进行特征传播（隐式推理）取代显式的文本思维链（CoT），具有推理速度快、表示信息量大的优点。
*   **核心问题**：作者通过分析训练过程中的 token 级梯度动力学，发现了两个关键瓶颈：
    1.  **视觉-文本优化失衡**：由于语言偏见，视觉 token 的梯度范数远高于文本 token 且波动剧烈，导致视觉特征在推理中被“欠优化”。
    2.  **固定深度的优化困境**：简单样本收敛快，而复杂样本在固定模型深度下梯度持续不稳定，限制了模型处理复杂逻辑的能力。

### 2. 方法论
该方法包含三个核心组件，旨在增强视觉感知并实现自适应推理：
*   **空间相干细粒度视觉重放（SCF-VR）**：
    *   **核心思想**：在每个推理步骤中动态重新引入关键视觉线索。
    *   **技术细节**：利用 Transformer 层的注意力权重识别显著视觉区域，通过滑动窗口寻找高密度区域并进行裁剪重采样。引入**自蒸馏监督**，将裁剪后的高分辨率特征与原始特征对齐，确保空间一致性。
*   **路由深度缩放（RDS）**：
    *   **核心思想**：为复杂 token 分配更多的计算资源。
    *   **技术细节**：集成一个轻量级路由网络，根据 token 的重要性得分动态决定是否让该 token 在当前层进行额外的迭代计算（Depth Scaling），从而实现 token 级的自适应推理深度。
*   **课程潜空间训练（Curriculum Training）**：
    *   **流程**：不依赖外部标注，而是采用渐进式策略。初始阶段使用标准显式 CoT 监督，随后逐步将显式推理步骤封装进紧凑的 `<latent>` token 中，使模型平滑地内化推理逻辑。

### 3. 实验设计
*   **数据集与场景**：涵盖 12 个主流基准测试，分为三大类：
    1.  **数学推理**：MathVista, MathVision, MM-Math。
    2.  **视觉中心推理**：HallusionBench, MMVP, Seed-Bench-2-Plus, HR-Bench, GQA。
    3.  **多模态组合推理**：MMStar, BLINK, ScienceQA, M3CoT。
*   **对比方法**：
    *   **零样本 VLM**：GPT-4o, Qwen2.5-VL, InternVL3.5 等。
    *   **显式 CoT 方法**：Multimodal-CoT, CCoT, ICoT 等。
    *   **强化学习/工具增强**：Vision-R1, VL-Rethinker, DeepEyes 等。
    *   **潜空间推理方法**：Laser, LVR, Monet, DMLR 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **16 台 NVIDIA H20 GPU**（每台 96GB 显存）。
*   **训练细节**：采用 DeepSpeed ZeRO-2 优化，学习率为 $4 \times 10^{-5}$，默认训练 16 个 epoch。
*   **推理效率**：相比显式 CoT，自回归步数减少了约 10 倍，推理速度提升 3-6 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 12 个不同维度的基准测试上进行了验证，并跨越了 Qwen2-VL、Qwen2.5-VL 和 Chameleon 等多种主流模型骨干（2B 到 7B 规模）。
*   **消融实验**：针对课程学习、路由参数 $\alpha$、视觉潜变量形成策略、位置编码方式以及超参数 $\lambda$ 进行了详尽的消融研究。
*   **评价**：实验设计全面，对比了从闭源 SOTA 到最新的 RL 增强模型，涵盖了准确率、推理步数和实际延迟等多个维度，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：Vedas 在多个基准测试（如 MMVP, MathVista）上达到了 SOTA 性能，尤其在需要细粒度视觉感知的任务上表现突出。
*   **效率优势**：在保持甚至超越显式 CoT 性能的同时，显著降低了推理延迟，证明了潜空间推理的实用性。
*   **视觉增强有效性**：可视化分析显示，SCF-VR 模块能准确捕捉与推理逻辑相关的视觉区域，有效缓解了长链推理中的视觉退化问题。

### 7. 优点
*   **洞察深刻**：通过梯度动力学分析揭示了多模态推理中“视觉欠优化”的本质原因，为后续研究提供了理论依据。
*   **架构灵活**：提出的 RDS 和 SCF-VR 模块是插件式的，可以轻松集成到各种预训练 MLLM 中。
*   **训练高效**：课程学习策略避免了昂贵的中间过程标注，降低了数据构建成本。

### 8. 不足与局限
*   **文本密集型任务限制**：在 SeedBench-2-Plus 等侧重文本理解的基准上提升相对有限，说明该方法更偏向于视觉逻辑的增强。
*   **超参数敏感性**：实验显示超参数 $\lambda$（损失权重）对不同规模的模型（2B vs 7B）有显著影响，可能需要针对特定模型进行精细调优。
*   **应用场景**：目前主要集中在静态图像推理，尚未在长视频或实时交互场景中验证其扩展性。

（完）
