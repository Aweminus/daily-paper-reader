---
title: "UniDoc-RL: Coarse-to-Fine Visual RAG with Hierarchical Actions and Dense Rewards"
title_zh: UniDoc-RL：具有层级化动作和稠密奖励的从粗到细视觉检索增强生成
authors: "Jun Wang, Shuo Tan, Zelong Sun, Tiancheng Gu, Yongle Zhao, Ziyong Feng, Kaicheng Yang, Cewu Lu"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14967v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: LVLM智能体利用强化学习共同执行检索和推理
tldr: 针对现有视觉RAG系统在复杂推理中缺乏细粒度语义的问题，本文提出UniDoc-RL框架。该框架将视觉信息获取建模为分层决策过程，使LVLM代理能执行从文档检索到区域裁剪的由粗到精的操作。通过引入密集多奖励机制和GRPO算法，UniDoc-RL实现了端到端训练，显著提升了模型在处理复杂视觉知识时的准确性与效率。
selection_source: fresh_fetch
motivation: 现有的视觉RAG系统依赖通用检索信号，难以捕捉复杂推理所需的细粒度视觉语义。
method: 提出一种基于强化学习的统一框架，通过分层动作空间实现从文档到区域的逐级精细化视觉感知，并结合密集多奖励机制进行优化。
result: "在三大基准测试中均优于现有SOTA模型，相比之前的强化学习方法性能提升高达17.7%。"
conclusion: UniDoc-RL证明了通过分层决策和密集奖励对LVLM进行对齐，能有效增强其在视觉RAG任务中的主动感知与推理能力。
---

## 摘要
检索增强生成 (RAG) 通过外部视觉知识扩展了大视觉语言模型 (LVLM)。然而，现有的视觉 RAG 系统通常依赖通用的检索信号，忽略了对复杂推理至关重要的细粒度视觉语义。为解决这一局限，我们提出了 UniDoc-RL，这是一个统一的强化学习框架，LVLM 智能体在其中共同执行检索、重排序、主动视觉感知和推理。UniDoc-RL 将视觉信息获取建模为具有层级化动作空间的序列决策问题。具体而言，它将视觉证据从粗粒度的文档检索逐步细化为细粒度的图像选择和主动区域裁剪，使模型能够抑制无关内容并关注信息密集区域。为了实现有效的端到端训练，我们引入了一种稠密多奖励方案，为每个动作提供任务感知的监督。基于群体相对策略优化 (GRPO)，UniDoc-RL 在不依赖独立价值网络的情况下，使智能体行为与多个目标对齐。为支持这一训练范式，我们构建了一个包含细粒度动作标注的高质量推理轨迹综合数据集。在三个基准测试上的实验表明，UniDoc-RL 持续超越了最先进的基准模型，相比此前基于强化学习的方法提升高达 17.7%。

## Abstract
Retrieval-Augmented Generation (RAG) extends Large Vision-Language Models (LVLMs) with external visual knowledge. However, existing visual RAG systems typically rely on generic retrieval signals that overlook the fine-grained visual semantics essential for complex reasoning. To address this limitation, we propose UniDoc-RL, a unified reinforcement learning framework in which an LVLM agent jointly performs retrieval, reranking, active visual perception, and reasoning. UniDoc-RL formulates visual information acquisition as a sequential decision-making problem with a hierarchical action space. Specifically, it progressively refines visual evidence from coarse-grained document retrieval to fine-grained image selection and active region cropping, allowing the model to suppress irrelevant content and attend to information-dense regions. For effective end-to-end training, we introduce a dense multi-reward scheme that provides task-aware supervision for each action. Based on Group Relative Policy Optimization (GRPO), UniDoc-RL aligns agent behavior with multiple objectives without relying on a separate value network. To support this training paradigm, we curate a comprehensive dataset of high-quality reasoning trajectories with fine-grained action annotations. Experiments on three benchmarks demonstrate that UniDoc-RL consistently surpasses state-of-the-art baselines, yielding up to 17.7% gains over prior RL-based methods.

---

## 论文详细总结（自动生成）

### UniDoc-RL：具有层级化动作和稠密奖励的从粗到细视觉 RAG 总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉检索增强生成（Visual RAG）系统主要依赖通用的检索信号，难以捕捉复杂推理所需的细粒度视觉语义。此外，视觉文档（如图表、扫描报告）包含大量冗余背景噪声，现有的“被动消费”模式直接编码全图，导致上下文浪费和推理干扰。
*   **核心问题**：如何提高视觉 RAG 的检索精度、视觉信息利用率，并解决强化学习（RL）在优化中间决策步骤（如检索、裁剪）时的信用分配（Credit Assignment）难题。

#### 2. 论文提出的方法论
*   **核心思想**：将视觉 RAG 建模为一个序列决策问题，通过**层级化动作空间**实现从粗到细的视觉证据获取，并利用**稠密多奖励机制**进行端到端优化。
*   **关键技术细节**：
    *   **层级化动作空间**：
        1.  **图像搜索 (Search)**：调用外部工具进行粗粒度候选文档检索。
        2.  **精确选择 (Select)**：由 LVLM 智能体根据语义对候选图进行重排序和筛选，过滤文档级噪声。
        3.  **视觉感知 (Perceive)**：通过 `<bbox>` 动作主动定位、裁剪并放大关键区域，模拟人类视觉注意力。
    *   **稠密多奖励系统**：
        *   **检索奖励 ($r_{ir}$)**：基于 NDCG 评估搜索结果。
        *   **选择奖励 ($r_{sel}$)**：评估所选图像是否包含真实证据（含伪监督策略）。
        *   **感知奖励 ($r_{crop}$)**：基于预测框与真实框的 IoU 评估裁剪精度。
        *   **模式奖励 ($r_{pat}$)**：确保输出符合 XML 格式规范。
        *   **结果奖励 ($r_{ans}$)**：由奖励模型评估最终答案的正确性。
    *   **算法流程**：采用 **GRPO (Group Relative Policy Optimization)** 算法，在不依赖独立价值网络的情况下，通过群体相对优势对齐多个目标。

#### 3. 实验设计
*   **数据集/场景**：
    *   **SlideVQA**：多图幻灯片问答，侧重多跳推理。
    *   **ViDoSeek**：视觉文档搜索与推理。
    *   **MMLongBench**：长上下文文档理解，涵盖文本、表格、图表等多种视觉类型。
*   **对比方法**：
    *   **Vanilla RAG**：标准检索+推理。
    *   **ReAct**：思维链+动作迭代。
    *   **Search-R1(-VL)**：基于 RL 的搜索增强基准。
    *   **VRAG-RL**：此前最先进的基于 RL 的视觉感知 RAG 方法。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **软件框架**：使用 `llama-factory` 进行监督微调（SFT），使用 `verl` 框架进行 GRPO 训练。
*   **训练细节**：SFT 阶段全参数微调，学习率 1e-5；RL 阶段学习率 1e-6，组大小（Group Size）为 5。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准测试上进行了全面评估，涵盖了 3B 和 7B 两种模型规模。
*   **消融实验**：针对“层级动作（选择与裁剪）”和“多奖励组成部分”分别进行了详尽的消融研究。
*   **充分性评价**：实验设计较为充分。不仅对比了最终准确率，还深入分析了检索召回率的变化、动作触发频率以及定性的案例分析，客观地证明了各模块的有效性。

#### 6. 论文的主要结论与发现
*   **性能领先**：UniDoc-RL 在所有基准测试中均达到 SOTA，相比之前的 RL 方法（如 VRAG-RL）提升了 **17.5% - 17.7%**。
*   **主动感知的价值**：RL 训练显著提高了模型调用裁剪工具的频率（从 SFT 的 2.4% 提升至 13.3%），且裁剪精度大幅提升，证明模型学会了“主动寻求信息”。
*   **检索优化**：通过 LVLM 的二次选择，检索召回率在各数据集上均有显著提升（如 SlideVQA 从 79.7% 提升至 85.0%）。

#### 7. 优点
*   **端到端统一框架**：将检索、重排序、感知和推理整合进单一决策过程，避免了模块间的脱节。
*   **拟人化感知**：引入的主动裁剪和放大机制非常符合人类处理复杂文档的逻辑。
*   **解决稀疏奖励问题**：设计的稠密奖励机制为中间步骤提供了明确的梯度引导，使复杂动作链的优化成为可能。
*   **高质量数据贡献**：发布了包含细粒度动作标注的推理轨迹数据集，对社区具有参考价值。

#### 8. 不足与局限
*   **推理延迟**：由于采用了“搜索-选择-感知-推理”的多轮迭代模式，系统的端到端推理延迟（Latency）可能会高于单次生成的 RAG。
*   **教师模型依赖**：训练轨迹的合成高度依赖于极高性能的教师模型（如 Qwen3-VL-235B），对于缺乏超大规模模型资源的研究者来说，复现或扩展数据具有挑战。
*   **场景局限性**：目前主要集中在文档类图像（Document-based），对于自然场景下的视觉 RAG 效果尚待进一步验证。

（完）
