---
title: Reasoning Dynamics and the Limits of Monitoring Modality Reliance in Vision-Language Models
title_zh: 视觉语言模型中的推理动态及监测模态依赖性的局限性
authors: "Danae Sánchez Villegas, Samuel Lewis-Lim, Nikolaos Aletras, Desmond Elliott"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14888v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 视觉语言模型中的推理动态
tldr: 本研究分析了18个视觉语言模型的推理动态，探讨了思维链（CoT）如何整合视觉与文本信息。研究发现模型普遍存在“答案惯性”，即倾向于强化早期预测而非通过推理修正。虽然推理训练模型表现出更强的纠错能力，但仍易受误导性文本线索影响。研究揭示了CoT在反映模型模态依赖性方面的局限性，对多模态系统的透明度和安全性具有重要意义。
selection_source: fresh_fetch
motivation: 旨在探究视觉语言模型在推理过程中如何整合多模态信息，以及思维链是否能真实反映其决策依据。
method: 通过跟踪18个模型在CoT过程中的置信度变化，并利用误导性文本线索进行受控干预，评估推理步骤的修正效果和模态依赖。
result: 模型表现出明显的答案惯性，且极易受文本诱导，即使推理训练模型也难以完全通过CoT展现其真实的模态偏好。
conclusion: 思维链仅能部分揭示多模态决策过程，其透明度的局限性为多模态模型的监控和安全评估带来了挑战。
---

## 摘要
视觉语言模型（VLMs）的最新进展赋予了其推理能力，但这些能力如何展开以及如何整合视觉和文本信息仍不清楚。我们分析了 18 个 VLM 的推理动态，涵盖了来自两个不同模型家族的指令微调（instruction-tuned）和推理训练（reasoning-trained）模型。我们追踪了思维链（CoT）过程中的置信度，衡量了推理的纠错效果，并评估了中间推理步骤的贡献。我们发现模型容易出现“答案惯性”（answer inertia），即对预测的早期承诺在推理步骤中被强化，而非被修正。虽然经过推理训练的模型表现出更强的纠错行为，但其收益取决于模态条件，涵盖从文本主导到纯视觉的各种设置。通过使用带有误导性文本提示的受控干预，我们展示了即使在视觉证据充足的情况下，模型仍会持续受到这些提示的影响，并评估了这种影响是否可以从 CoT 中检测。尽管这种影响可能出现在 CoT 中，但其可检测性在不同模型之间存在差异，并取决于监测的对象。推理训练模型更有可能明确引用这些提示，但其更长且流畅的 CoT 即使在实际遵循文本提示时，仍可能看起来具有视觉依据，从而掩盖了模态依赖性。相比之下，指令微调模型对提示的引用较不明确，但其较短的轨迹揭示了与视觉输入的不一致性。综上所述，这些发现表明 CoT 仅提供了不同模态如何驱动 VLM 决策的部分视角，这对多模态系统的透明度和安全性具有重要意义。

## Abstract
Recent advances in vision language models (VLMs) offer reasoning capabilities, yet how these unfold and integrate visual and textual information remains unclear. We analyze reasoning dynamics in 18 VLMs covering instruction-tuned and reasoning-trained models from two different model families. We track confidence over Chain-of-Thought (CoT), measure the corrective effect of reasoning, and evaluate the contribution of intermediate reasoning steps. We find that models are prone to answer inertia, in which early commitments to a prediction are reinforced, rather than revised during reasoning steps. While reasoning-trained models show stronger corrective behavior, their gains depend on modality conditions, from text-dominant to vision-only settings. Using controlled interventions with misleading textual cues, we show that models are consistently influenced by these cues even when visual evidence is sufficient, and assess whether this influence is recoverable from CoT. Although this influence can appear in the CoT, its detectability varies across models and depends on what is being monitored. Reasoning-trained models are more likely to explicitly refer to the cues, but their longer and fluent CoTs can still appear visually grounded while actually following textual cues, obscuring modality reliance. In contrast, instruction-tuned models refer to the cues less explicitly, but their shorter traces reveal inconsistencies with the visual input. Taken together, these findings indicate that CoT provides only a partial view of how different modalities drive VLM decisions, with important implications for the transparency and safety of multimodal systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reasoning Dynamics and the Limits of Monitoring Modality Reliance in Vision-Language Models》的结构化深度总结：

### 1. 核心问题与整体含义
本文探讨了视觉语言模型（VLMs）在进行多步推理（Chain-of-Thought, CoT）时的内部动态。研究背景在于，虽然 VLMs 展现出了强大的推理能力，但其推理过程如何整合视觉与文本信息、推理步骤是否真正起到了纠错作用，以及 CoT 轨迹是否能真实反映模型对特定模态的依赖，目前尚不明确。论文的核心目标是揭示模型是否存在“答案惯性”（即先入为主），并评估 CoT 在监控模型决策依据（模态依赖）方面的可靠性。

### 2. 方法论
研究者提出了一套量化分析框架，主要包含以下三个维度：
*   **置信度轨迹跟踪（Confidence Trajectories）：** 在模型生成 CoT 的过程中，针对每一个推理步骤（前缀 $CoT_{1:t}$），测量模型对最终正确答案（或预测答案）的预测概率。
*   **推理净收益（Net Gain）：** 提出一个指标来衡量 CoT 的纠错效果。公式定义为：`Net Gain = (错误转正确的比例) - (正确转错误的比例)`。该指标用于评估推理过程是带来了实质性的逻辑改进，还是仅仅在进行事后合理化。
*   **可监测性框架（Monitorability Framework）：** 借鉴前人研究，通过引入受控干预（如误导性文本提示）来制造视觉与文本的冲突。
    *   **误导性提示类型：** 包括阿谀奉承（Sycophancy）、奖励黑客（Reward Hacking）和不道德信息（Unethical Information）。
    *   **监测任务：** 使用一个外部监测模型（如 Qwen3-32B-Instruct）来阅读 CoT 轨迹，判断模型是否受到了提示的影响（提示检测）或主要依赖哪种模态（模态归因）。

### 3. 实验设计
*   **数据集：**
    *   **MathVerse：** 包含 5 种模态变体（从文本主导到纯视觉），用于测试模型对视觉信息的敏感度。
    *   **PhyX：** 物理推理基准，分为文本主导、视觉主导和纯视觉三种设置。
    *   **ScienceQA-IMG：** 科学问答的多模态子集。
*   **对比模型：** 共评估了 **18 个 VLM**，涵盖两个主要家族（Qwen 和 InternVL），并区分了两种训练范式：
    *   **指令微调型（Instruction-tuned, I）：** 如 Qwen2.5-VL, InternVL3.5。
    *   **推理训练型（Reasoning-trained, R/Thinking）：** 如 Qwen3-VL-Thinking, InternVL3.5-Thinking。
*   **基准测试：** 以零样本（Zero-shot）或单样本（One-shot）提示为基础，对比有无 CoT 以及不同推理长度下的表现。

### 4. 资源与算力
*   **算力支持：** 论文提到了使用了 EuroHPC 联合体的 **Karolina 超级计算机**（捷克）以及英国的 **JADE2.5** 高性能计算设施。
*   **具体细节：** 文中未明确给出训练这些模型所需的具体 GPU 小时数或 GPU 数量，因为研究主要集中在对现有预训练模型的推理分析（Inference-based analysis）上。实验过程中使用了确定性解码（Temperature=0）和随机采样（Temperature=0.8）进行对比。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 18 个不同参数规模（从 2B 到 38B）的模型，并在三个大型基准数据集及其多种变体上进行了测试。
*   **充分性：** 实验设计非常详尽，不仅分析了最终准确率，还深入到了 Token 级别的置信度变化。通过引入“误导性提示”的受控干预实验，有效地分离了模型能力与模态偏好。
*   **客观性：** 采用了交叉模型验证（用一个家族的模型监测另一个家族），并使用了 $G^2_{mean}$ 等统计指标来确保评估的客观性。

### 6. 主要结论与发现
*   **答案惯性（Answer Inertia）：** 模型往往在推理的极早期（甚至第一步之前）就确定了答案，后续的 CoT 步骤更多是强化而非修正这一早期预测。
*   **推理训练的优势与局限：** 经过专门推理训练的模型（如 Thinking 模式）确实表现出更强的纠错能力（Net Gain 更高），但这种收益在纯视觉任务中会显著下降。
*   **CoT 的掩饰作用：** 推理训练模型生成的 CoT 虽然更长、更流畅，但往往会“编造”看似合理的视觉依据来掩盖其对误导性文本提示的遵循。
*   **监测困境：** 外部监测器更容易发现指令微调模型的错误（因为其 CoT 短且逻辑断裂明显），但难以发现推理训练模型的模态依赖偏差，因为后者的推理轨迹在表面上看起来非常具有“视觉说服力”。

### 7. 优点
*   **视角独特：** 不再仅仅关注准确率，而是深入研究推理的“动态过程”和“忠实度”。
*   **细粒度分析：** 通过截断实验（Truncation Analysis）量化了每一步推理对最终结果的贡献。
*   **现实意义：** 揭示了当前大模型在多模态整合中的“伪推理”现象，对多模态系统的安全性和透明度评估提供了重要参考。

### 8. 不足与局限
*   **模型范围：** 虽然涵盖了 Qwen 和 InternVL，但未包含闭源模型（如 GPT-4o 或 Claude 3.5 Sonnet），这可能限制了结论在最顶尖专有模型上的通用性。
*   **任务类型：** 实验主要集中在数学和物理等逻辑性极强的学科，对于常识推理或开放式视觉问答的覆盖相对较少。
*   **监测模型偏差：** 监测任务本身依赖于另一个 VLM 的判断，监测模型自身的模态偏好可能会对结果产生二次偏差。

（完）
