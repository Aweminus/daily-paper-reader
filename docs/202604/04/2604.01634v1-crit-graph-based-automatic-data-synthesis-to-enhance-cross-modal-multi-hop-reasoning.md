---
title: "CRIT: Graph-Based Automatic Data Synthesis to Enhance Cross-Modal Multi-Hop Reasoning"
title_zh: CRIT：基于图的自动数据合成以增强跨模态多跳推理
authors: "Junyoung Sung, Seungwoo Lyu, Minjun Kim, Sumin An, Arsha Nagrani, Paul Hongsuck Seo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01634v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉语言模型中的跨模态多跳推理
tldr: 针对视觉语言模型在跨模态多步推理中存在的幻觉及视觉依据不足问题，本文提出了CRIT数据集和评测基准。该研究通过基于图的自动化流水线，合成了涵盖自然图像、视频和富文本等领域的复杂推理任务。实验证明，现有SOTA模型在处理此类任务时表现不佳，而利用CRIT进行训练能显著增强模型的跨模态推理能力，并在SPIQA等标准基准上取得显著进步。
selection_source: fresh_fetch
motivation: 现有视觉语言模型在处理需要结合文本与视觉信息的复杂多步推理时，常因训练数据缺乏模态互补性而产生幻觉。
method: 开发了一种基于图的自动化数据合成流水线，用于生成跨越多种视觉领域（如图像、视频）的复杂多步推理任务及人工验证的测试集。
result: 实验表明SOTA模型在CRIT基准上表现挣扎，但经过CRIT数据训练的模型在跨模态多步推理及多个标准基准上均有显著提升。
conclusion: CRIT数据集及其合成方法为提升视觉语言模型的跨模态推理能力提供了有效的解决方案和评估标准。
---

## 摘要
现实世界的推理通常需要结合跨模态的信息，在多跳过程中将文本上下文与视觉线索联系起来。然而，大多数多模态基准测试未能捕捉到这种能力：它们通常依赖于单张图像或图像集，其中答案仅凭单一模态即可推断。这种局限性也反映在训练数据中，交错的图文内容很少强制执行互补的多跳推理。因此，视觉语言模型（VLM）经常产生幻觉，并生成缺乏视觉证据支撑的推理轨迹。为了弥补这一差距，我们推出了 CRIT，这是一个通过基于图的自动流水线构建的新数据集和基准测试，用于生成复杂的跨模态推理任务。CRIT 涵盖了从自然图像、视频到富文本源的多个领域，并包含一个经过人工验证的测试集，以进行可靠的评估。在该基准测试上的实验表明，即使是最先进的模型在处理此类推理任务时也面临挑战。在 CRIT 上训练的模型在跨模态多跳推理方面表现出显著提升，包括在 SPIQA 和其他标准多模态基准测试上的大幅改进。

## Abstract
Real-world reasoning often requires combining information across modalities, connecting textual context with visual cues in a multi-hop process. Yet, most multimodal benchmarks fail to capture this ability: they typically rely on single images or set of images, where answers can be inferred from a single modality alone. This limitation is mirrored in the training data, where interleaved image-text content rarely enforces complementary, multi-hop reasoning. As a result, Vision-Language Models (VLMs) frequently hallucinate and produce reasoning traces poorly grounded in visual evidence. To address this gap, we introduce CRIT, a new dataset and benchmark built with a graph-based automatic pipeline for generating complex cross-modal reasoning tasks. CRIT consists of diverse domains ranging from natural images, videos, and text-rich sources, and includes a manually verified test set for reliable evaluation. Experiments on this benchmark reveal that even state-of-the-art models struggle on such reasoning tasks. Models trained on CRIT show significant gains in cross-modal multi-hop reasoning, including strong improvements on SPIQA and other standard multimodal benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《CRIT: Graph-Based Automatic Data Synthesis to Enhance Cross-Modal Multi-Hop Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现实世界的任务（如阅读带图教程）需要人类在文本和多张图像之间进行“多跳”推理。然而，现有的视觉语言模型（VLM）在处理此类任务时经常出现幻觉，且推理过程缺乏视觉依据。
*   **核心挑战**：
    1.  **基准测试缺陷**：现有基准测试往往通过单一模态（仅文本或仅图像）即可破解，未能真正测试跨模态的互补性。
    2.  **训练数据匮乏**：缺乏高质量、强制要求跨模态多步推理的交错图文数据。
    3.  **合成偏差**：直接用VLM生成推理数据容易产生循环偏见和幻觉。

### 2. 方法论
论文提出了一种**基于图的自动数据合成流水线（CRIT）**，核心思想是将多模态内容结构化为图，通过操作图结构来保证推理的复杂性和严谨性：
*   **多模态内容图构建**：
    *   利用现有的场景图（Scene Graph）作为基础，过滤出唯一实体。
    *   使用LLM（大语言模型）增加文本节点和跨模态边，构建连接多张图像与文本描述的统一图结构。
*   **文本上下文生成**：
    *   根据子图提取实体和关系，利用LLM生成多样化的叙述性文本（如故事、新闻、实验报告），并显式引用图像（如“如图像1所示”）。
*   **QA对与推理链生成**：
    *   在图中采样1至5跳的路径，确保推理链条必须跨越文本和图像模态。
    *   **强制约束**：推理的终点必须是图像中的某个属性或实体，确保答案必须从视觉信息中提取。
*   **多级过滤机制**：
    *   **单模态测试**：使用三个不同的LLM测试问题是否能在仅有文本或仅有图结构的情况下被回答，剔除单模态可解的样本。
    *   **人工验证**：对测试集进行严格的人工审核，确保逻辑严密、无歧义。

### 3. 实验设计
*   **数据集/场景**：涵盖自然图像（基于GQA）、视频帧（基于ActivityNet Captions）和科学论文（基于SPIQA）三个领域。
*   **Benchmark**：
    *   **内部**：CRIT测试集（包含1607个经过人工验证的QA对）。
    *   **外部**：SPIQA、VEGA、MMQA、FCMR（跨模态推理）；MuirBench、BLINK、MMStar等（通用多模态能力）。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4o-mini, Gemini 2.0 Flash。
    *   **开源模型**：Qwen2.5-VL (7B/72B), Intern2.5-VL, LLaVA-Onevision, Idefics2等。

### 4. 资源与算力
*   **硬件环境**：使用了 **4台 NVIDIA H100 80GB GPU**。
*   **训练细节**：采用 LoRA 微调技术，针对 Qwen2.5-VL-7B 和 Idefics2-8B 进行训练。
*   **生成模型**：使用 Qwen3-30B 等模型作为流水线中的生成器和过滤器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   合成了约 **27万个 QA 对** 用于训练。
    *   进行了零样本（Zero-shot）评估、监督微调（SFT）评估。
*   **消融实验**：
    *   验证了跨图像边的必要性（移除后33%问题不可答）。
    *   对比了“基于图生成”与“直接提示词生成”的效果（前者在77%的情况下更优）。
    *   测试了数据规模扩展（Scaling）的效果。
*   **客观性**：通过多模型交叉验证和STEM背景人员的人工审核（保留率仅41%），确保了测试集的极高可靠性。

### 6. 主要结论与发现
*   **现有模型表现不佳**：即使是 GPT-4o 等顶级模型，在 CRIT 上的准确率也仅在 30% 左右，证明了跨模态多跳推理的极高难度。
*   **微调效果显著**：在 CRIT 数据上微调后的模型，在 CRIT 测试集上的表现翻倍，且在 SPIQA 等外部基准上也取得了显著提升（如 SPIQA 指标翻倍）。
*   **错误根源**：错误分析显示，**55% 的失败源于“证据定位错误”**，即模型无法在复杂的图文上下文中找到正确的证据块。

### 7. 优点
*   **逻辑严密性**：通过图结构路径采样，从根本上保证了“多跳”和“跨模态”的真实性，避免了传统合成数据的逻辑坍塌。
*   **领域广泛**：不仅限于自然图像，还扩展到了视频序列和复杂的科学文献领域。
*   **互补性强制**：通过单模态过滤，确保了视觉信息不是“装饰品”，而是解题的“必需品”。

### 8. 不足与局限
*   **合成风格偏见**：尽管有图结构约束，生成的文本仍可能带有 LLM 特有的叙述风格。
*   **错误累积风险**：流水线包含多个 LLM 调用环节，前期图构建的错误可能会传播到最终的 QA 对中。
*   **科学领域挑战**：尽管有提升，但模型在处理科学论文等长文本、高复杂度视觉元素时的表现依然远低于人类水平。

（完）
