---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 为自主系统设计的基于VLM的代理快慢规划
tldr: "本研究提出Agentic Fast-Slow Planning框架，旨在解决大模型推理与实时控制结合中的延迟和可靠性问题。该框架通过解耦感知、推理与控制，利用云端LLM生成语义指令，并结合语义引导的A*算法与智能体细化模块将指令转化为轨迹，最后由MPC实时跟踪。在CARLA仿真中，该方法显著提升了系统的鲁棒性，减少了45%的横向偏差并缩短了12%的任务完成时间。"
selection_source: fresh_fetch
motivation: 针对现有大模型生成轨迹脆弱、延迟高以及与实时控制接口模糊的挑战，寻求一种高效融合语义推理与底层控制的方法。
method: "提出一种分层框架，利用VLM和云端LLM进行语义决策，并通过语义引导的A*算法和参数自适应模块生成可执行轨迹，最后由MPC执行。"
result: "实验表明，该框架在CARLA环境下显著降低了横向偏差（达45%）并提高了任务完成效率（超过12%）。"
conclusion: 该研究通过快慢规划的解耦设计，成功实现了大模型高级推理能力与实时控制系统的稳健集成。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射到可靠的实时控制仍具挑战。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了智能体快慢规划（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：Perception2Decision 使用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，然后通过云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；Decision2Trajectory 则将指令转换为可执行路径，其中语义引导的 A* 算法（Semantic-Guided A*）将源自语言的软成本嵌入经典搜索中，使解偏向可行轨迹，而智能体细化模块（Agentic Refinement Module）则利用反馈和记忆来调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码可在 https://github.com/cjychenjiayi/icra2026_AFSP 获取。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Agentic Fast-Slow Planning (AFSP)** 的分层框架，旨在解决大模型推理与自动驾驶实时控制之间的鸿沟。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何将大语言模型（LLM）强大的语义推理能力转化为可靠、实时的车辆控制指令。
*   **研究动机**：
    *   **现有方法局限性**：直接让 LLM 生成轨迹（Trajectory-LLM）过于脆弱且难以验证；在线调整 MPC 参数的方法则模糊了“慢速决策”与“快速控制”的界限，导致系统接口混乱。
    *   **解耦需求**：受人类认知双过程理论（快/慢系统）启发，需要一个在不同时间尺度上解耦感知、推理、规划和控制的框架，以兼顾高级智能与底层安全性。

### 2. 方法论
该框架通过两个关键“桥梁”连接云端推理与终端执行：

*   **Perception2Decision（感知到决策）**：
    *   **车载 VLM 拓扑检测器**：将原始图像压缩为以自我为中心的拓扑图（包含物体类别、距离、方位等），显著降低上传云端的带宽需求。
    *   **云端 LLM 决策器**：基于拓扑图进行链式思考（CoT）推理，输出符号化的驾驶指令（如 LEFT, RIGHT, KEEP）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹）**：
    *   **语义引导的 A* 算法（Semantic-Guided A*）**：将 LLM 的符号指令转化为“软成本”（Soft Costs）。如果路径符合指令则奖励，不符合则惩罚。这种设计允许在指令错误时通过几何避障逻辑进行修正，避免了硬约束导致的死胡同。
    *   **智能体细化模块（Agentic Refinement Module）**：利用 LLM 作为一个闭环调节器，根据规划反馈和历史记忆自动调整 A* 算法的超参数（如各项成本权重），替代了繁琐的人工调参。
*   **实时控制**：采用 **云端引导的 MPC**，在常规情况下执行本地参考路径，在复杂场景下切换至云端优化的全局参考轨迹。

### 3. 实验设计
*   **实验环境**：使用 CARLA 仿真器（Town2-7 和 Town10HD）。
*   **数据集**：构建了一个包含 49 类物体、约 20 万帧图像的 CARLA 数据集，用于 VLM 的微调。
*   **对比基准（Benchmarks）**：
    *   **纯 MPC**：仅依赖局部避障。
    *   **A*-MPC**：结合经典 A* 规划与 MPC 控制。
*   **测试场景**：包括标准地图、带扰动的地图（坐标平移、障碍物半径放大）等，模拟感知误差和环境变化。

### 4. 资源与算力
*   **训练算力**：使用了 **4 台 NVIDIA A100 GPU** 进行 VLM 的微调。
*   **训练时长**：微调过程持续了 12 个 epoch。
*   **模型选择**：基础模型采用 InternVL-2B（VLM）和 DeepSeek-V3（LLM），通过 API 或本地部署实现。

### 5. 实验数量与充分性
*   **实验规模**：
    *   VLM 性能测试：1000 帧测试集，对比了三种微调策略。
    *   决策一致性测试：200 帧图像，对比了 VLM 直接决策与“拓扑图+LLM”决策的延迟与准确度。
    *   端到端仿真：在 CARLA 中针对 3 种典型场景进行了多次重复实验（每组 10 次以减少随机性）。
*   **充分性评价**：实验设计较为全面，涵盖了从感知精度、推理延迟到最终控制鲁棒性的全链路评估。通过引入地图扰动实验，客观地验证了系统在非理想状态下的生存能力。

### 6. 主要结论与发现
*   **感知压缩有效性**：将图像转化为拓扑图后，LLM 的推理延迟从 10.24s 降至 4.13s，且决策质量保持相当。
*   **鲁棒性提升**：语义引导的 A* 算法在地图发生平移扰动时，仍能保持意图一致性，而传统 A* 容易失效。
*   **性能指标**：相比纯 MPC，AFSP 将**横向偏差减少了 45%**，**任务完成时间缩短了 12% 以上**，且速度波动更小，行驶更平顺。

### 7. 优点与亮点
*   **架构优雅**：成功实现了“云端慢速推理”与“终端快速控制”的解耦，解决了大模型上车的实时性难题。
*   **软硬结合**：语义引导的 A* 算法巧妙地将 LLM 的模糊指令转化为优化目标的软约束，兼顾了灵活性与安全性。
*   **自动化调参**：引入 Agent 闭环调参模块，解决了经典算法依赖专家经验调参的痛点。

### 8. 不足与局限
*   **云端依赖**：系统高度依赖云端 LLM 的响应，若发生网络中断或高延迟，虽然有本地 MPC 保底，但高级智能将受限。
*   **场景覆盖**：虽然在 CARLA 中表现良好，但在现实世界极其复杂的长尾场景（如极端天气、无保护左转的复杂博弈）中的表现仍需验证。
*   **计算开销**：尽管压缩了感知数据，但 Agent 闭环调参涉及多次 LLM 调用，在极端实时响应场景下可能存在瓶颈。

（完）
