---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
title_zh: 思考、行动、构建：一种基于视觉语言模型的零样本 3D 视觉定位智能体框架
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于视觉语言模型的动态视觉定位智能体框架
tldr: "本文提出Think, Act, Build (TAB)框架，旨在解决零样本3D视觉定位中过度依赖预处理点云的问题。该框架将任务重构为生成式2D到3D重建过程，利用VLM智能体在原始RGB-D流中动态追踪目标，并通过语义锚定几何扩展机制克服多视角覆盖不足。实验证明，该方法在ScanRefer和Nr3D上显著优于现有零样本方法，甚至超越了部分全监督基准。"
selection_source: fresh_fetch
motivation: 现有方法通常将3D视觉定位简化为预处理点云的提议匹配，忽略了2D VLM强大的语义解析能力与多视角几何的互补性。
method: 提出一种动态智能体框架，通过VLM调用视觉工具进行2D追踪，并利用语义锚定几何扩展机制将多视角特征聚合为3D坐标。
result: 在ScanRefer和Nr3D数据集上，该框架凭借开源模型实现了超越以往零样本方法及部分全监督基准的性能。
conclusion: 通过解耦语义理解与几何重建，并采用智能体驱动的生成式范式，可以有效提升零样本3D视觉定位的准确性与鲁棒性。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLM) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选框匹配。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLM 来解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。基于这一见解，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。至关重要的是，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基线。

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Think, Act, Build (TAB)

#### 1. 核心问题与整体含义（研究动机和背景）
3D 视觉定位（3D-VG）的核心挑战在于如何根据自然语言描述在三维空间中精准定位物体。
*   **现有瓶颈：** 现有的零样本（Zero-shot）方法大多依赖预处理好的 3D 点云，将定位任务简化为“提议匹配（Proposal Matching）”的分类问题，这限制了其在无预建地图环境中的应用。此外，仅依赖 2D 语义匹配的方法在视角剧烈变化或遮挡时表现脆弱。
*   **研究动机：** 本文提出将任务解耦——利用 **2D VLM** 处理复杂的空间语义推理，利用 **确定性多视图几何** 实例化 3D 结构。TAB 框架将 3D-VG 从静态匹配转变为动态的“推理-重建”过程，直接作用于原始 RGB-D 视频流。

#### 2. 方法论：核心思想与技术细节
TAB 框架采用 **ReAct（推理-行动）** 范式，由一个受过“3D-VG 技能”训练的 VLM 智能体驱动，分为三个阶段：
*   **Think（思考）：** 智能体根据“3D-VG 技能蓝图”解析查询（Query Analysis），将复杂指令分解为目标类别、属性、空间条件和场景特征。
*   **Act（行动）：** 调用视觉工具库（如 Grounding DINO 进行粗筛，SAM3 进行精细分割，VLM 进行语义验证和评分排序），确定参考帧并隔离目标。
*   **Build（构建）：** 这是该框架的核心，引入了 **语义锚定几何扩展（SAGE）** 机制：
    1.  **语义时间扩展（STE）：** 在参考帧前后进行双向语义追踪，获取目标的一组初始 2D 掩码。
    2.  **初始重建与质心提取：** 利用相机内参和深度图将 2D 像素逆投影到 3D 空间，计算目标的 3D 几何质心（$P_{centroid}$）。
    3.  **多视图几何扩展（MGE）：** 将 3D 质心反向投影到视频的所有帧中，通过视场检查和 Z-buffer 遮挡检查，在语义失效（如特写镜头缺少上下文）的帧中依然能精准定位并提取掩码。
    4.  **最终重建：** 聚合所有多视图掩码，通过离群点去除（SOR）和聚类（DBSCAN），生成最终的 3D 点云并计算轴向对齐包围盒（AABB）。

#### 3. 实验设计
*   **数据集：** 使用了 **ScanRefer** 和 **Nr3D**（均基于 ScanNet 室内场景）。
*   **基准测试（Benchmark）：** 
    *   对比了零样本方法（如 LLM-Grounder, ZSVG3D, SeeGround, SeqVLM, SPAZER 等）。
    *   对比了全监督方法（如 3DVG-T, BUTD-DETR, EDA, LLaVA-3D 等）。
*   **基准修正：** 作者指出原始基准存在指代歧义、类别错误和空间位置错误，并手动修正了这些噪声数据以进行更严谨的评估。

#### 4. 资源与算力
*   **模型：** 核心智能体使用 **Qwen3-VL-32B**（开源模型），视觉工具包括 Grounding DINO 和 SAM3。
*   **算力说明：** 文中未明确给出具体的 GPU 型号、数量或训练/推理总时长。但提到每段视频采样 300 帧，且扩展过程上限为 32 帧，这暗示了该方法对显存和计算延迟有一定要求。

#### 5. 实验数量与充分性
*   **实验规模：** 在两个主流数据集的多个子集（Unique/Multiple, Easy/Hard, Dep/Indep）上进行了全面测试。
*   **消融实验：** 针对 STE（语义时间扩展）和 MGE（多视图几何扩展）分别做了消融，证明了两者结合对提升定位精度（Acc@0.25/0.5）的必要性。
*   **客观性：** 实验对比了“不依赖 3D 点云输入”和“辅助 3D 提议”两种设置，确保了与不同范式方法对比的公平性。

#### 6. 主要结论与发现
*   **性能领先：** TAB 在不依赖预处理点云的情况下，性能显著超过了现有的零样本 SOTA 方法，甚至在某些指标上超越了全监督基线。
*   **几何补偿语义：** 实验发现，纯语义追踪在视角变化时极易中断，而通过 3D 质心反向投影的几何扩展能有效弥补 VLM 的感知盲区。
*   **鲁棒性：** 智能体具备故障恢复能力，若中间步骤失败（如深度噪声导致质心无效），会自动回退到稳定状态继续执行。

#### 7. 优点（亮点）
*   **范式创新：** 摆脱了对静态 3D 点云地图的依赖，实现了从原始视频流到 3D 坐标的端到端生成式定位。
*   **智能体架构：** 引入“3D-VG 技能”引导，使 VLM 能够像人类专家一样进行多步推理和工具调用。
*   **数据贡献：** 对 ScanRefer 和 Nr3D 的标注噪声进行了系统性清理，为后续研究提供了更准确的测试集。

#### 8. 不足与局限
*   **依赖 RGB-D：** 该方法高度依赖高质量的深度图（Depth Map），在深度传感器失效或噪声巨大的场景下，重建质量会受损。
*   **计算延迟：** 作为一个多步迭代的智能体框架，其推理速度可能无法满足实时性要求极高的具身导航任务。
*   **场景限制：** 目前主要在室内场景（ScanNet）验证，室外大尺度场景或动态物体的处理能力尚待验证。

（完）
