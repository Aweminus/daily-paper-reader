---
title: "OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL"
title_zh: OmniVLA-RL：一种具有空间理解能力和在线强化学习的视觉-语言-动作模型
authors: "Haoxiang Jie, Yaoyuan Yan, Xiangyu Wei, Kailin Wang, Hongjie Yan, Zhiyou Heng, Daocheng Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17706v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 集成推理专家和在线强化学习的VLA模型
tldr: OmniVLA-RL 针对具身智能中 VLA 模型空间感知不准、多模态融合欠佳及强化学习不稳定的挑战，提出了一种基于混合 Transformer (MoT) 的新架构，协同推理、空间与动作专家。通过引入 Flow-GSPO 算法，将流匹配重构为 SDE 过程并结合分组分段策略优化，显著提升了动作精度与训练鲁棒性，在 LIBERO 基准测试中刷新了 SOTA 性能。
selection_source: fresh_fetch
motivation: 旨在解决现有 VLA 模型在精确空间感知、高效多模态融合以及强化学习训练稳定性方面的核心缺陷。
method: 采用混合 Transformer (MoT) 架构整合多专家模块，并结合基于 SDE 的流匹配与分组分段策略优化 (Flow-GSPO)。
result: 在 LIBERO 和 LIBERO-Plus 基准测试中的实验结果表明，OmniVLA-RL 的性能显著超越了当前最先进的方法。
conclusion: 该研究通过架构创新和算法优化，有效克服了 VLA 模型的基础性限制，为具身智能提供了更强大的感知与决策框架。
---

## 摘要
视觉-语言-动作（VLA）模型代表了具身智能领域的范式转变，然而现有框架通常面临空间感知不精确、多模态融合欠佳以及强化学习不稳定的挑战。为了弥补这些差距，我们提出了 OmniVLA-RL，这是一种利用混合 Transformer（Mix-of-Transformers, MoT）设计来协同整合推理、空间和动作专家的创新架构。此外，我们引入了 Flow-GSPO，它将流匹配（flow matching）重新表述为随机微分方程（SDE）过程，并将其与分组分段策略优化（Group Segmented Policy Optimization, GSPO）相结合，以提高动作精度和训练鲁棒性。在 LIBERO 和 LIBERO-Plus 基准测试上的广泛评估表明，OmniVLA-RL 显著优于现有最先进的方法，有效地克服了当前 VLA 模型的根本局限性。

## Abstract
Visual-Language-Action (VLA) models represent a paradigm shift in embodied AI, yet existing frameworks often struggle with imprecise spatial perception, suboptimal multimodal fusion, and instability in reinforcement learning. To bridge these gaps, we propose OmniVLA-RL, a novel architecture that leverages a Mix-of-Transformers (MoT) design to synergistically integrate reasoning, spatial, and action experts. Furthermore, we introduce Flow-GSPO, which reformulates flow matching as a Stochastic Differential Equation (SDE) process and integrates it with Group Segmented Policy Optimization (GSPO) to enhance action precision and training robustness. Extensive evaluations on the LIBERO and LIBERO-Plus benchmarks demonstrate that OmniVLA-RL significantly outperforms state-of-the-art methods, effectively overcoming the fundamental limitations of current VLA models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OmniVLA-RL** 的新型具身智能模型，旨在解决现有视觉-语言-动作（VLA）模型在空间感知、多模态融合效率以及强化学习（RL）训练稳定性方面的核心痛点。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：现有的 VLA 模型虽然在指令遵循和场景理解上表现出色，但在**精确空间感知**（如 3D 位置和物体尺寸）方面存在短板，导致抓取和避障精度不足。
*   **现有方案缺陷**：
    *   **融合方式**：早期的“早期融合”或“后期融合”仅修改编码器或动作头，未触及大模型核心，导致语义、视觉和空间特征交互效率低下。
    *   **强化学习稳定性**：传统的 PPO 架构过于复杂，而新兴的 GRPO 在处理动作序列时容易出现训练崩溃或不稳定的问题。
*   **核心目标**：构建一个能深度融合空间智能、具备高精度动作生成能力且支持稳定在线强化学习的统一框架。

### 2. 方法论
*   **混合 Transformer (MoT) 架构**：
    *   **推理专家 (Reasoning Expert)**：基于预训练 VLM（如 PaLiGemma），提取高层语义和视觉先验。
    *   **空间专家 (Spatial Expert)**：利用 VGGT 编码器提取细粒度 3D 结构特征，并通过辅助监督头（重建点云、相机参数等）强化几何理解。
    *   **动作专家 (Action Expert)**：采用条件流匹配（Conditional Flow Matching, CFM）生成连续动作轨迹。
*   **块级因果注意力 (Block-wise Causal Attention)**：
    *   将推理和空间专家的 Token 设为“全可见前缀”，允许双向对齐。
    *   将动作 Token 设为“因果后缀”，确保动作生成的自回归特性，并防止动作去噪过程中的随机噪声干扰场景理解。
*   **Flow-GSPO 算法**：
    *   **随机流匹配**：通过 Fokker-Planck 方程将确定性的 ODE 去噪过程转化为随机微分方程（SDE），引入随机性以支持 RL 探索。
    *   **分组分段策略优化 (GSPO)**：在“动作块（Action Block）”级别而非 Token 级别进行优化，计算组内相对优势（Advantage），有效解决了长序列动作的偏差累积和不稳定性问题。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO 基准**：包含 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程）四个子集。
    *   **LIBERO-Plus**：更具挑战性的扩展版，涉及多阶段组合任务（如开抽屉并取物）。
    *   **预训练数据**：使用了大规模 3D 数据集和 DROID 机器人数据集。
*   **对比方法**：
    *   **基础 VLA**：Diffusion Policy, Octo, OpenVLA, SpatialVLA, CoT-VLA, $\pi_0$, $\pi_{0.5}$, F1 等。
    *   **RL 算法对比**：PPO 和 GRPO。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到 RL 阶段使用了 AdamW 优化器（学习率 $1 \times 10^{-5}$），进行了 200 个 RL 更新步骤，每 10 步刷新一次 Rollout Buffer。

### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的四个维度和 LIBERO-Plus 上进行了全面测试。
*   **消融实验**：针对 RL 范式（PPO vs GRPO vs Flow-GSPO）和空间专家模块的必要性进行了对比。
*   **充分性评价**：实验设计较为充分，对比了从经典的 Diffusion Policy 到最新的 $\pi_{0.5}$ 等多种 SOTA 模型，涵盖了从模仿学习到强化学习的完整链路，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：OmniVLA-RL 在 LIBERO 上达到了 **97.6%** 的平均成功率，刷新了纪录。
*   **RL 稳定性**：Flow-GSPO 在收敛速度和最终性能上显著优于 PPO 和 GRPO，尤其在复杂的多任务场景（LIBERO-Plus）中表现出更强的鲁棒性。
*   **空间智能的重要性**：消融实验显示，移除空间专家会导致性能下降 8.3%，证明了细粒度几何特征对高精度操作至关重要。

### 7. 优点
*   **架构创新**：MoT 架构实现了语义与空间的深度解耦与高效融合。
*   **算法突破**：Flow-GSPO 成功将流匹配与分组策略优化结合，解决了 VLA 模型在线微调难的痛点。
*   **训练策略**：采用三阶段渐进式训练（空间对齐 -> 动作生成 -> 在线 RL），逻辑清晰且易于收敛。

### 8. 不足与局限
*   **缺乏真机验证**：实验完全基于仿真环境（Simulation），尚未在物理机器人平台上验证“仿真到现实（Sim-to-Real）”的迁移效果。
*   **算力信息缺失**：未披露训练资源消耗，难以评估该方案的训练成本和部署门槛。
*   **缺乏世界模型**：虽然动作生成很强，但模型目前缺乏一个专门的“世界模型”来预测环境演变，这可能限制其在极长程、非结构化环境中的规划能力。

（完）
