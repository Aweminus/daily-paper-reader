---
title: Freshness-Aware Prioritized Experience Replay for LLM/VLM Reinforcement Learning
title_zh: 面向大语言模型/视觉语言模型强化学习的鲜度感知优先经验回放
authors: "Weiyu Ma, Yongcheng Zeng, Yan Song, Xinyu Cui, Jian Zhao, Xuhui Liu, Mohamed Elhoseiny"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16918v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉语言模型在智能体任务中的强化学习
tldr: 针对LLM/VLM强化学习中在线策略采样效率低的问题，本文提出了Freshness-Aware PER。该方法通过引入指数级年龄衰减机制，解决了传统优先经验回放（PER）在模型快速演化时产生的优先级过期（staleness）问题。作为首个成功将PER应用于大模型RL的工作，它在多项智能体和推理任务中显著超越了基准模型，大幅提升了样本利用率和训练效果。
selection_source: fresh_fetch
motivation: 在线策略RL在处理昂贵的多轮交互任务时样本效率极低，而直接应用PER会因大模型策略更新过快导致旧的高优先级轨迹过度主导采样。
method: 提出一种感知新鲜度的PER机制，通过基于有效样本量分析的指数年龄衰减因子来动态修正存储的优先级。
result: "在八项复杂任务中，该方法显著优于在线基准，如在Sokoban任务中性能提升达367%，而标准PER则会导致性能下降。"
conclusion: Freshness-Aware PER 成功解决了大模型强化学习中的优先级过期问题，为提升大规模生成式模型的训练效率提供了有效方案。
---

## 摘要
强化学习（RL）在大语言模型（LLM）和视觉语言模型（VLM）的后训练中取得了显著成功，其中 PPO、GRPO 和 REINFORCE++ 等同策略（on-policy）算法已成为主流范式。然而，这些方法在单次梯度更新后会丢弃所有收集到的轨迹，导致样本效率低下，对于多轮环境交互成本高昂的智能体（agentic）任务而言尤为浪费。虽然经验回放通过允许智能体重用过去的轨迹并优先处理信息量大的轨迹，提升了经典强化学习中的样本效率，但直接将优先经验回放（PER）应用于大语言模型却宣告失败。十亿参数规模模型的策略演进极快，使得存储的优先级变得陈旧，导致旧的高优先级轨迹在失去信息价值后仍长期主导采样过程。我们提出了鲜度感知 PER（Freshness-Aware PER），通过在任何基于 PER 的优先级上增加一个基于有效样本量分析的乘法指数年龄衰减，来解决优先级陈旧问题。据我们所知，鲜度感知 PER 是首个成功将 PER 应用于大语言模型/视觉语言模型强化学习的工作。我们在 0.5B、3B 和 7B 模型上，针对八个多步智能体、推理和数学竞赛任务进行了评估。鲜度感知 PER 显著优于同策略基准，在 NQ Search 上提升了 46%，在推箱子（Sokoban）上提升了 367%，在 VLM FrozenLake 上提升了 133%，而没有年龄衰减的标准 PER 则持续导致性能下降。我们的代码已在 https://github.com/Vision-CAIR/Freshness-Aware-PER 公开。

## Abstract
Reinforcement Learning (RL) has achieved impressive success in post-training Large Language Models (LLMs) and Vision-Language Models (VLMs), with on-policy algorithms such as PPO, GRPO, and REINFORCE++ serving as the dominant paradigm. However, these methods discard all collected trajectories after a single gradient update, resulting in poor sample efficiency, particularly wasteful for agentic tasks where multi-turn environment interactions are expensive. While Experience Replay drives sample efficiency in classic RL by allowing agents to reuse past trajectories and prioritize informative ones, directly applying Prioritized Experience Replay (PER) to LLMs fails. The rapid policy evolution of billion-parameter models renders stored priorities stale, causing old high-priority trajectories to dominate sampling long after they have become uninformative. We propose Freshness-Aware PER, which addresses this priority staleness problem by augmenting any PER-based priority with a multiplicative exponential age decay grounded in effective sample size analysis. To the best of our knowledge, Freshness-Aware PER is the first work to successfully apply PER to LLM/VLM reinforcement learning. We evaluate on eight multi-step agentic, reasoning, and math competition tasks with 0.5B, 3B, and 7B models. Freshness-Aware PER significantly outperforms on-policy baselines, achieving +46% on NQ Search, +367% on Sokoban, and +133% on VLM FrozenLake, while standard PER without age decay consistently degrades performance. Our code is publicly available at https://github.com/Vision-CAIR/Freshness-Aware-PER.

---

## 论文详细总结（自动生成）

这是一份关于论文《Freshness-Aware Prioritized Experience Replay for LLM/VLM Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 LLM（大语言模型）和 VLM（视觉语言模型）的强化学习（RL）后训练中，主流的同策略（On-policy）算法（如 PPO、GRPO）样本效率极低，因为它们在单次梯度更新后即丢弃昂贵的交互轨迹。
*   **研究背景**：在智能体（Agentic）任务中，多轮环境交互（如网页搜索、代码执行）成本极高。虽然经典 RL 中的优先经验回放（PER）能提高效率，但直接应用于 LLM 会失败。
*   **失效原因**：**优先级过期（Priority Staleness）**。由于 LLM 参数量巨大且策略演进极快，存储在回放池中的旧轨迹优先级（如 TD 误差）很快就会失效。旧的高优先级数据会持续主导采样，导致模型在已过时或有害的数据上过拟合，最终导致训练崩溃。

### 2. 方法论：FreshPER
*   **核心思想**：引入“鲜度感知”机制，通过**指数级年龄衰减（Exponential Age Decay）**动态修正存储的优先级，确保采样既关注信息量（Informativeness），又关注时效性（Freshness）。
*   **关键技术细节**：
    *   **理论基础**：作者通过有效样本量（ESS）分析证明，随着当前策略与行为策略（生成数据的策略）之间的 KL 散度增加，样本对梯度的贡献呈指数级下降。
    *   **核心公式**：修正后的优先级 $p_i$ 定义为：
        $$p_i = p_{base, i} \cdot \exp\left(-\frac{\Delta_i}{\tau}\right)$$
        其中，$p_{base, i}$ 是基础优先级（如奖励绝对值或优势函数），$\Delta_i$ 是该轨迹存储后的梯度更新步数，$\tau$ 是年龄衰减常数（超参数）。
    *   **算法流程**：
        1.  **收集阶段**：使用当前策略生成轨迹，记录行为概率并存入回放池。
        2.  **更新阶段**：在 CPU 异步线程中扫描回放池，根据当前步数更新所有轨迹的年龄衰减因子。
        3.  **采样阶段**：基于衰减后的优先级进行比例采样，并使用重要性采样（IS）权重修正偏差。
        4.  **训练阶段**：结合新鲜数据（On-policy）和回放数据（Off-policy）进行混合训练。

### 3. 实验设计
*   **场景与数据集**：涵盖 8 个环境，分为三类：
    *   **智能体任务**：NQ Search（检索增强问答）、Sokoban（推箱子游戏，分简单/困难版）。
    *   **推理与数学**：AIME（数学竞赛）、GSM8K（小学数学）。
    *   **视觉导航与推理**：VLM FrozenLake（视觉版冰湖）、GeoQA（几何问答）。
*   **Benchmark 模型**：Qwen2.5 系列（0.5B、3B-VL、7B）。
*   **对比方法**：
    1.  **On-Policy**：标准的 REINFORCE++（不使用回放池）。
    2.  **Standard PER**：带优先级但无年龄衰减的传统经验回放。
    3.  **FreshPER (Ours)**：本文提出的鲜度感知方法。

### 4. 资源与算力
*   **算力配置**：论文使用了分布式训练框架 ROLL，GPU 数量根据模型大小而定：
    *   **0.5B 模型**：2 个 GPU。
    *   **3B-VL 模型**：4 个 GPU。
    *   **7B 模型**：8 个 GPU。
*   **框架支持**：使用 DeepSpeed 进行分布式训练，vLLM 进行推理加速。
*   **具体型号**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100），但提到了在 8 张 GPU 上处理 7B 模型时使用了 ZeRO-3 优化。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个不同的任务上进行了主实验。
    *   针对衰减常数 $\tau$ 进行了多组消融实验（$\tau=500, 1000, 1500$）。
    *   针对重要性采样（IS）修正的稳定性进行了对比实验。
    *   设置了控制组（如 CliffWalking 和 GSM8K），验证在简单或饱和任务中回放的效果。
*   **充分性评价**：实验设计非常充分且客观。它不仅展示了成功案例，还通过控制实验划定了该方法的适用边界（即在任务极简单或模型已接近上限时，回放收益有限）。

### 6. 主要结论与发现
*   **性能飞跃**：FreshPER 在所有任务中均优于同策略基准。在 NQ Search 上提升 46%，在 Sokoban 上提升 367%，在 VLM 任务上提升 133%。
*   **PER 的局限性**：标准 PER 在 LLM 训练中往往表现得比 On-policy 更差，甚至导致训练崩溃，证明了“年龄衰减”是 PER 在大模型上成功的必要条件。
*   **任务相关性**：策略演进越快的任务（如 Sokoban），需要的衰减越剧烈（$\tau$ 越小）；而相对稳定的任务则受益于较缓的衰减。
*   **多模态通用性**：该方法在 VLM 上同样有效，证明其机制与模态无关。

### 7. 优点（亮点）
*   **首创性**：据称是首个成功将轨迹级 PER 应用于 LLM/VLM 强化学习的工作。
*   **理论支撑**：年龄衰减机制并非纯启发式，而是基于有效样本量（ESS）和 KL 散度的数学推导。
*   **即插即用**：作为优先级的一个乘法层，它可以轻松集成到 PPO、GRPO 或 REINFORCE++ 等现有算法中。
*   **工程实用性**：通过异步 CPU 线程更新优先级，避免了训练阻塞，具有较高的工程落地价值。

### 8. 不足与局限
*   **超参数敏感性**：衰减常数 $\tau$ 对结果影响较大，且在不同任务间不通用，目前仍需手动调优。
*   **模型规模覆盖**：虽然测试了 7B 模型，但尚未在超大规模模型（如 70B 或更高）上验证其表现，超大模型的策略漂移速度可能与中小模型不同。
*   **离策略偏差**：尽管使用了 IS 修正，但当策略漂移过大时，离策略数据的梯度偏差风险依然存在，论文主要通过快速衰减来规避，而非从根本上消除偏差。

（完）
