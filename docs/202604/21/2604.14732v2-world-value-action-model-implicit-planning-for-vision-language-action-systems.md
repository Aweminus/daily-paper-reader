---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 决策中视觉-语言-动作系统的隐式规划
tldr: 针对现有VLA模型在长程任务中缺乏推理能力的问题，本文提出WAV模型。该框架通过学习结构化潜空间表示，结合世界模型预测未来和价值函数评估，实现了隐式规划。相比直接在动作空间搜索，WAV在潜空间进行推理，有效解决了可行轨迹概率随时间指数衰减的问题，显著提升了复杂任务的成功率、泛化性和鲁棒性。
selection_source: fresh_fetch
motivation: 现有VLA模型多依赖直接动作预测，缺乏对长程轨迹的推理和后果评估能力，限制了其在复杂决策任务中的表现。
method: 提出WAV框架，利用世界模型预测未来状态并结合价值函数评估，在结构化潜空间中通过隐式规划生成动作。
result: 仿真和真实实验表明，WAV在任务成功率和泛化能力上显著优于现有SOTA方法，尤其擅长处理长程和组合任务。
conclusion: 在潜空间进行隐式规划能有效重塑搜索分布并提高决策效率，是构建高性能具身智能体系统的关键。
---

## 摘要
视觉-语言-动作 (VLA) 模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，大多数现有方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作 (WAV) 模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习以视觉观测和语言指令为条件的未来轨迹的结构化潜表征。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临可行轨迹概率随时间跨度增加而呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现了高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。代码可在 https://github.com/Win-commit/WAV 获取。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios. Code is available at https://github.com/Win-commit/WAV.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **WAV (World-Value-Action)** 的新型视觉-语言-动作模型，旨在通过“隐式规划”解决具身智能体在长程任务中决策能力不足的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型大多依赖于“直接动作预测”（即反应式策略），缺乏对未来轨迹的推理和后果评估能力。这导致模型在面对复杂、多步骤的长程任务时，容易出现误差累积和决策不连贯。
*   **研究背景**：虽然世界模型（World Models）能预测未来，但如何将其有效地整合进 VLA 系统以指导动作选择仍是一个挑战。直接在动作空间进行显式规划会面临“可行性诅咒”——随着规划步长增加，可行轨迹在海量动作组合中的比例呈指数级下降。
*   **整体含义**：WAV 提出将规划视为潜空间（Latent Space）中的推理过程，通过联合建模环境动力学（世界）和长程效用（价值）来生成动作。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：不直接搜索动作，而是学习未来轨迹的结构化潜表征。通过迭代推理，将概率质量集中在既符合物理动力学（可行）又具有高回报（高价值）的轨迹上。
*   **关键模块**：
    1.  **语言条件视频生成模块**：基于 Diffusion Transformer (DiT) 和 T5-XXL 编码器，预测未来的视觉演化。
    2.  **轨迹价值模块**：评估预测视频的长程效用（累积折扣奖励）。
    3.  **动作解码模块**：融合视频特征和价值特征，解码出具体的机器人动作。
*   **算法流程**：
    *   **训练**：采用三阶段流匹配（Flow Matching）策略。先训练视频生成，再训练价值预测，最后联合训练动作解码。
    *   **推理（潜空间规划）**：借鉴 MPPI 思想，在推理时对视频和价值的潜变量进行多次采样。根据价值预测的信噪比（SNR）筛选“精英”样本，迭代更新潜分布的均值和方差，最终输出优化后的动作。

### 3. 实验设计
*   **仿真环境**：使用 **LIBERO** 基准测试，涵盖 Spatial（空间推理）、Object（物体泛化）、Goal（目标条件）和 Long（长程组合）四个子集。
*   **真实世界场景**：使用双臂 Piper 机器人执行：整理碗筷（Bowl organization）、铺平毛巾（Towel flattening）、长程抽屉任务（打开-放入-关闭）。
*   **对比方法**：
    *   **基础 VLA**：Diffusion Policy, Octo, OpenVLA, $\pi_0$ 等。
    *   **带世界模型的 VLA**：WorldVLA, DreamVLA, GE-ACT 等。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A100-SXM4-80GB GPU**。
*   **训练时长**：
    *   LIBERO 任务：全参数微调约需 **5 天**。
    *   真实世界任务：每个任务的模型训练约需 **3 天**。
*   **推理开销**：文中分析了迭代次数 $K$ 对推理时间的影响，通常 $K=3$ 是性能与效率的平衡点。

### 5. 实验数量与充分性
*   **实验规模**：在 LIBERO 的数十个任务上进行了测试，并在真实世界中对每个任务进行了 15 次独立实验。
*   **消融实验**：非常充分。涵盖了迭代次数 $K$、采样数量 $M/N$、平滑参数 $\alpha/\beta$ 以及精英样本比例的敏感度分析。
*   **客观性**：通过理论证明（引理 4.1 和命题 4.2）支撑了潜空间规划的优越性，并与大量 SOTA（最先进）模型进行了横向对比，实验设计较为公正。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 榜单上取得了 98.1% 的平均成功率，刷新了 SOTA 记录。
*   **长程优势**：在 Long 任务中，隐式规划带来的提升最为显著（从 91.8% 提升至 94.4%），证明了其缓解误差累积的能力。
*   **真实世界表现**：在复杂的抽屉任务中，WAV 的成功率远超基准模型 GE-ACT（75.6% vs 35.6%），表现出更强的空间感知和多步协调能力。

### 7. 优点
*   **理论支撑强**：从数学上证明了动作空间规划的指数级失效问题，为潜空间推理提供了坚实的逻辑基础。
*   **模块化统一**：将预测、评估和执行整合在一个基于 DiT 的统一架构中，结构优雅。
*   **鲁棒性高**：通过迭代推理（Iterative Inference）动态修正偏差，比一次性预测（One-shot）更具鲁棒性。

### 8. 不足与局限
*   **计算开销**：由于涉及多次采样和迭代推理，推理延迟和显存占用高于传统的反应式 VLA 模型，可能限制其在极高频控制场景的应用。
*   **奖励函数依赖**：价值模块的训练依赖于预定义的密集奖励函数（Rule-based rewards），在缺乏此类标注的数据集上难以直接应用。
*   **实时性挑战**：虽然 $K=3$ 能平衡性能，但在资源受限的嵌入式机器人平台上，实时生成视频和评估价值仍有压力。

（完）
