---
title: "S-GRPO: Unified Post-Training for Large Vision-Language Models"
title_zh: S-GRPO：大视觉语言模型的统一后训练
authors: "Yuming Yan, Kai Tang, Sihong Chen, Ke Xu, Dan Hu, Qun Yu, Pengfei Hu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16557v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 大型视觉语言模型的强化学习后训练
tldr: 针对大视觉语言模型（LVLM）后训练中SFT易导致灾难性遗忘和RL面临冷启动优化崩溃的问题，本文提出S-GRPO统一框架。该方法引入条件化真值轨迹注入（CGI）机制，在RL探索失败时将专家轨迹作为高奖励锚点注入，动态平衡模仿学习与自主探索。实验证明S-GRPO能显著加速收敛，在提升特定领域适配能力的同时保留了模型的通用性。
selection_source: fresh_fetch
motivation: 传统的SFT和RL在LVLM后训练中分别面临分布偏移导致的遗忘问题和稀疏奖励下的优化崩溃挑战。
method: 提出S-GRPO框架，通过CGI机制在模型探索失败时注入真值轨迹并赋予最大奖励，将监督学习目标转化为策略梯度中的高优势分量。
result: 理论分析和实验结果表明，S-GRPO有效桥接了SFT与RL，大幅提升了收敛速度和领域适配效果。
conclusion: S-GRPO为LVLM提供了一种高效的统一后训练方案，在增强特定任务表现的同时成功维持了基础模型的通用多模态能力。
---

## 摘要
当前用于适配大视觉语言模型（LVLMs）的后训练方法通常分为两种范式：监督微调（SFT）和强化学习（RL）。尽管这两种方法被广泛应用，但当它们被孤立使用时，都存在效率低下的问题。SFT 强制模型的生成遵循单一的专家轨迹，由于分布偏移，通常会导致通用多模态能力的灾难性遗忘。相反，RL 探索多条生成的轨迹，但经常遇到优化崩溃——这是一种冷启动问题，即未对齐的模型在稀疏奖励的视觉任务中无法自发采样到任何领域有效的轨迹。在本文中，我们提出了监督组相对策略优化（S-GRPO），这是一个统一的后训练框架，将模仿学习的引导整合到偏好优化的多轨迹探索中。针对直接生成的视觉任务，S-GRPO 引入了条件真值轨迹注入（CGI）。当二元验证器检测到采样轨迹组内出现完全的探索失败时，CGI 会将经过验证的真值轨迹注入候选池。通过为该注入的锚点分配确定的最大奖励，S-GRPO 在组相对优势估计中强制引入正向信号。该机制将监督学习目标重新表述为策略梯度中的高优势组件，迫使模型在利用专家轨迹与探索新颖视觉概念之间实现动态平衡。理论分析和实验结果表明，S-GRPO 优雅地弥合了 SFT 与 RL 之间的差距，显著加速了收敛，并在保留基础模型通用能力的同时实现了卓越的领域适配。

## Abstract
Current post-training methodologies for adapting Large Vision-Language Models (LVLMs) generally fall into two paradigms: Supervised Fine-Tuning (SFT) and Reinforcement Learning (RL). Despite their prevalence, both approaches suffer from inefficiencies when applied in isolation. SFT forces the model's generation along a single expert trajectory, often inducing catastrophic forgetting of general multimodal capabilities due to distributional shifts. Conversely, RL explores multiple generated trajectories but frequently encounters optimization collapse - a cold-start problem where an unaligned model fails to spontaneously sample any domain-valid trajectories in sparse-reward visual tasks. In this paper, we propose Supervised Group Relative Policy Optimization (S-GRPO), a unified post-training framework that integrates the guidance of imitation learning into the multi-trajectory exploration of preference optimization. Tailored for direct-generation visual tasks, S-GRPO introduces Conditional Ground-Truth Trajectory Injection (CGI). When a binary verifier detects a complete exploratory failure within a sampled group of trajectories, CGI injects the verified ground-truth trajectory into the candidate pool. By assigning a deterministic maximal reward to this injected anchor, S-GRPO enforces a positive signal within the group-relative advantage estimation. This mechanism reformulates the supervised learning objective as a high-advantage component of the policy gradient, compelling the model to dynamically balance between exploiting the expert trajectory and exploring novel visual concepts. Theoretical analysis and empirical results demonstrate that S-GRPO gracefully bridges the gap between SFT and RL, drastically accelerates convergence, and achieves superior domain adaptation while preserving the base model's general-purpose capabilities.

---

## 论文详细总结（自动生成）

### 论文总结：S-GRPO —— 大视觉语言模型的统一后训练框架

#### 1. 核心问题与研究动机
论文关注大视觉语言模型（LVLM）在特定领域适配（如医疗、几何、工业检测）中的后训练难题。目前主要存在两种范式：
*   **监督微调（SFT）：** 强制模型遵循单一专家轨迹，容易导致“对齐税”（Alignment Tax），即模型在掌握特定领域知识的同时，会发生通用多模态能力的灾难性遗忘。
*   **强化学习（RL，如 GRPO）：** 通过多轨迹探索进行优化，但在稀疏奖励环境下存在“冷启动”问题。如果模型初始能力较弱，采样到的所有轨迹都无法通过验证（奖励全为0），会导致梯度消失和优化崩溃。
**核心动机：** 消除 SFT 和 RL 之间的界限，提出一种既能提供确定性引导又能进行自主探索的统一单阶段后训练框架。

#### 2. 方法论：S-GRPO 与 CGI 机制
S-GRPO 的核心思想是将监督信号作为“锚点”动态注入到强化学习的组相对优化过程中。
*   **核心技术：条件化真值轨迹注入（CGI）。**
    *   **采样与检测：** 在每一轮训练中，模型首先生成一组候选轨迹（$G$ 个）。使用二元验证器（如规则或奖励模型）评估这些轨迹。
    *   **动态注入：** 如果该组内所有生成的轨迹都失败了（奖励均为0），CGI 机制会触发：丢弃一个生成样本，将经过验证的**真值轨迹（Ground-Truth）**注入候选池。
    *   **优势估计重构：** 为注入的真值轨迹分配最大奖励（$R_{max}=1.0$）。在计算组内相对优势（Advantage）时，由于真值轨迹的存在，标准差不再为0，从而产生强烈的正向优势信号。
*   **算法流程：** 该方法将监督学习目标转化为策略梯度中的高优势分量。随着模型能力的提升，当它能自主生成正确答案时，CGI 触发频率降低，训练平滑地从“监督引导”过渡到“纯自主探索”。

#### 3. 实验设计
*   **数据集与场景：**
    *   **COCO Caption：** 通用图像描述任务。
    *   **Geo170K (Phase 1)：** 几何问题解决（非深度思考类）。
    *   **OpenI：** 医疗 X 射线诊断报告生成。
*   **基座模型：** Qwen2.5-VL-7B（未经过 GRPO 预对齐，确保评估公正）。
*   **对比方法：**
    *   **SFT 类：** LoRA (PEFT)、全量微调 (FFT)、带 KL 约束的持续微调 (CFFT)。
    *   **RL 类：** 标准 GRPO、DAPO、GRPON、GCPO、RCPA。
    *   **两阶段法：** 先 SFT 后 GRPO 的传统流水线。
*   **评估指标：** 领域能力（BLEU, CIDEr, ROUGE-L, SPICE）和通用能力（MMMU, MME, IFEval）。

#### 4. 资源与算力
*   **算力说明：** 论文提到实验在配备 NVIDIA GPU 的 Linux 服务器上运行，使用了 EasyR1 强化学习框架和 LlamaFactory 微调框架。
*   **缺失信息：** 论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、具体的 GPU 数量以及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了三个不同性质的数据集（通用、几何、医疗），每种场景都对比了约 10 种基准方法。
*   **消融实验：** 专门针对 CGI 机制进行了消融（无注入 vs. 无条件注入 vs. 条件注入），验证了“条件触发”在平衡领域性能与通用能力方面的必要性。
*   **参数化研究：** 探讨了奖励阈值 $\delta$ 对模型性能的影响。
*   **充分性评价：** 实验设计较为充分，通过多维度指标（领域 vs. 通用）客观地展示了“对齐税”的存在以及 S-GRPO 的缓解效果。

#### 6. 主要结论与发现
*   **解决冷启动：** S-GRPO 成功解决了标准 GRPO 在稀疏奖励下的优化崩溃问题，学习曲线显示其收敛速度极快且稳定。
*   **缓解遗忘：** 相比全量微调（FFT）导致的通用能力大幅下降（如 MME 分数减半），S-GRPO 在提升领域表现的同时，几乎完整保留了基座模型的通用指令遵循和推理能力。
*   **单阶段优越性：** S-GRPO 的单阶段效果优于传统的“SFT + RL”两阶段流水线，且计算开销更低。

#### 7. 优点
*   **设计优雅：** 通过简单的轨迹注入逻辑，在数学上统一了监督学习和强化学习的梯度目标。
*   **动态平衡：** CGI 机制能根据模型当前能力自动调节监督强度，实现了从“扶着走”到“自己走”的自然过渡。
*   **实用性强：** 无需额外的重型教师模型或复杂的课程设计，易于在大规模生产环境中部署。

#### 8. 不足与局限
*   **任务范围：** 实验主要集中在“非深度思考”（Non-deep thinking）的直接生成任务上，尚未验证其在长链条推理（CoT）任务中的表现。
*   **验证器依赖：** 效果高度依赖于二元验证器的准确性。在缺乏明确真值或难以自动评估的开放式任务中，该方法的应用受限。
*   **算力细节模糊：** 缺乏具体的训练效率数据（如 Token/sec 或显存占用），难以评估其在大规模参数模型上的扩展成本。

（完）
