---
title: "Seek-and-Solve: Benchmarking MLLMs for Visual Clue-Driven Reasoning in Daily Scenarios"
title_zh: Seek-and-Solve：面向日常场景中视觉线索驱动推理的多模态大语言模型评测基准
authors: "Xiaomin Li, Tala Wang, Zichen Zhong, Ying Zhang, Zirui Zheng, Takashi Isobe, Dezhuang Li, Huchuan Lu, You He, Xu Jia"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14041v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 日常场景中视觉线索驱动的推理
tldr: 针对现有MLLM评测基准侧重感知而忽视日常场景推理的问题，本文提出了DailyClue基准。该基准涵盖4个领域和16个子任务，旨在评估模型在复杂视觉环境中过滤噪声并识别关键线索进行推理的能力。实验结果表明，现有模型在处理此类视觉线索驱动的推理任务时仍面临巨大挑战，强调了准确识别线索对实现鲁棒推理的重要性。
selection_source: fresh_fetch
motivation: 现有基准多关注预训练知识或感知理解，缺乏对日常场景中复杂视觉线索驱动推理能力的评估。
method: 构建了包含4大领域、16个子任务的DailyClue数据集，通过真实场景和深度查询设计，强制模型主动探索并利用视觉线索进行推理。
result: 对多种MLLM和智能体模型的评估显示，该基准具有极高挑战性，现有模型在复杂线索识别与推理上表现普遍不足。
conclusion: 准确识别视觉线索是实现鲁棒推理的关键，DailyClue为提升MLLM在真实场景下的推理能力提供了重要参考。
---

## 摘要
日常场景具有视觉信息丰富的特点，要求多模态大语言模型（MLLMs）能够过滤噪声并识别关键视觉线索，以进行准确推理。然而，目前的评测基准主要侧重于评估 MLLMs 的先验知识或感知理解能力，往往忽略了推理这一关键能力。为填补这一空白，我们推出了 DailyClue，这是一个专为日常场景中视觉线索驱动推理而设计的评测基准。我们的构建遵循两个核心原则：（1）严格立足于真实的日常活动；（2）设计具有挑战性的查询，要求超越表层感知。我们的问题并非简单的识别，而是迫使 MLLMs 主动探索合适的视觉线索，并利用这些线索进行后续推理。为此，我们策划了一个涵盖四个主要日常领域和 16 个不同子任务的综合数据集。对 MLLMs 和智能体模型的全面评估凸显了该基准带来的巨大挑战。我们的分析揭示了几项关键见解，强调准确识别视觉线索对于稳健推理至关重要。

## Abstract
Daily scenarios are characterized by visual richness, requiring Multimodal Large Language Models (MLLMs) to filter noise and identify decisive visual clues for accurate reasoning. Yet, current benchmarks predominantly aim at evaluating MLLMs' pre-existing knowledge or perceptual understanding, often neglecting the critical capability of reasoning. To bridge this gap, we introduce DailyClue, a benchmark designed for visual clue-driven reasoning in daily scenarios. Our construction is guided by two core principles: (1) strict grounding in authentic daily activities, and (2) challenging query design that necessitates more than surface-level perception. Instead of simple recognition, our questions compel MLLMs to actively explore suitable visual clues and leverage them for subsequent reasoning. To this end, we curate a comprehensive dataset spanning four major daily domains and 16 distinct subtasks. Comprehensive evaluation across MLLMs and agentic models underscores the formidable challenge posed by our benchmark. Our analysis reveals several critical insights, emphasizing that the accurate identification of visual clues is essential for robust reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：Seek-and-Solve (DailyClue) 评测基准分析

#### 1. 核心问题与整体含义（研究动机和背景）
现有的多模态大语言模型（MLLM）评测基准主要集中在基础感知（如物体识别、计数）或内部知识检索上，且性能已趋于饱和（准确率常超过90%）。然而，在真实的日常场景中，环境充满噪声，准确推理往往依赖于从复杂背景中识别出关键的“视觉线索”（Visual Clues）。
**核心问题：** 现有基准缺乏对模型在日常场景中“过滤噪声、寻找线索并进行多步推理”能力的评估。
**研究意义：** 论文提出了 **DailyClue** 基准，旨在通过严格立足真实生活、设计非平凡查询，评估模型是否能像侦探一样通过细微线索解决实际问题，从而推动具身智能和多模态智能体的发展。

#### 2. 论文提出的方法论
论文构建了一个名为 **DailyClue** 的评测基准，其核心思想是“线索驱动”而非“知识驱动”。
*   **构建原则：** 
    1.  **真实性：** 场景必须源于真实的日常活动（衣食住行、社交等）。
    2.  **挑战性：** 问题不能通过直接视觉观察回答，必须通过隐含线索进行推导。
*   **构建流程（三阶段）：**
    1.  **图像采集：** 从现有数据集和互联网手动搜集具有丰富场景信息的图像。
    2.  **三元组生成：** 利用 GPT-5 和 Gemini-2.5-Pro 生成“问题-线索-答案”三元组，并进行人工修正，确保问题具有间接性且答案唯一。
    3.  **严格过滤：** 采用“共识拒绝策略”，使用 GPT-o4-mini、Gemini-2.5-Flash 和 Claude-3.7-Sonnet 进行自动筛选。如果三个模型都能答对，则认为太简单而剔除；最后由三名标注员进行人工交叉核验。
*   **分类体系：** 涵盖 4 大领域（地理位置识别、空间关系推理、日常常识推理、科学常识推理）和 16 个子任务，共 666 个精心挑选的样本。

#### 3. 实验设计
*   **评测对象：** 涵盖了 25 个模型，分为三类：
    *   **开源模型：** LLaVA 系列、InternVL 系列、Qwen 系列（包括最新的 Qwen3-VL-Thinking）。
    *   **闭源模型：** Gemini-2.5 系列、Claude-3.7/4 系列、GPT-5、o4-mini。
    *   **智能体模型（Agentic Models）：** DeepEyes、VLM-R3、TreeVGR、Thyme 等。
*   **基准对比：** 引入了 **人类基准（Human Baseline）**，由 6 名大学生在禁止使用 AI 工具的情况下完成。
*   **评测协议：** 
    *   **常规评估：** 准确率匹配。
    *   **严格评估（Rigorous Evaluation）：** 不仅要求答案正确，还要求模型在思维链（CoT）中提取的视觉线索必须与真值线索有语义交集，以排除“蒙对”的情况。

#### 4. 资源与算力
*   **推理算力：** 
    *   对于参数量小于 10B 的开源模型，使用 1-2 台 **NVIDIA A800 (80GB)** GPU。
    *   对于更大规模的模型，使用 4-8 台 **NVIDIA H20 (96GB)** GPU。
*   **训练时长：** 文中未提及模型训练时长，因为该论文主要关注评测基准的构建与模型推理评估。

#### 5. 实验数量与充分性
*   **实验规模：** 评测了 25 个主流模型，涵盖了当前多模态领域的顶尖力量，实验覆盖面广。
*   **消融实验：** 
    *   对比了不同线索来源（模型自生、其他模型生成、真值线索）对推理结果的影响。
    *   对比了不同推理模式（No CoT, Vanilla CoT, Clue-guided CoT）的效果。
*   **客观性与公平性：** 通过多模型共识过滤和人工交叉核验，确保了题目难度和质量的客观性；统一了推理提示词和硬件环境，保证了对比的公平性。

#### 6. 论文的主要结论与发现
1.  **挑战性巨大：** 即使是顶尖模型 Gemini-2.5-Pro，准确率也仅为 56.90%，远未达到饱和，证明了 DailyClue 的区分度。
2.  **线索识别是瓶颈：** 实验证明，如果直接给模型提供真值线索（GT Clue），模型性能会大幅提升（如 Claude 提升 14.86%），说明模型当前的失败主要在于“看不准”关键线索。
3.  **主动寻求线索有效：** 显式引导模型在 CoT 中先寻找线索再回答，能显著提高准确率，减少推理漂移。
4.  **存在“视觉谄媚”现象：** 模型往往过度依赖文本提示，如果输入的外部线索是错误的，模型会推翻自己的视觉观察去迎合错误文本。
5.  **人类与 AI 的差异：** 人类在空间直觉上仍优于 AI，但在知识密集型任务（如地理位置识别）上不如拥有百科全书知识的模型。

#### 7. 优点（亮点）
*   **视角独特：** 避开了传统的感知评测，专注于“线索驱动”的深度推理，更贴近真实应用场景。
*   **质量把控严苛：** 结合了最强模型自动过滤和多轮人工核验，确保了数据集的高质量和高难度。
*   **评测维度深入：** 提出了“严格评估协议”，通过检测线索交集来识别模型的虚假推理（Lucky Guess），这在现有基准中较为少见。

#### 8. 不足与局限
*   **数据规模较小：** 仅包含 666 个样本，虽然质量高，但在覆盖日常生活的极端多样性方面仍有提升空间。
*   **静态局限：** 目前仅关注静态图像推理，尚未扩展到视频或交互式环境。
*   **模型覆盖：** 由于成本和权限限制，部分极高成本的闭源模型（如文中提到的 Gemini-3-Pro 假设性版本）未能包含在内。
*   **潜在偏差：** 尽管有人工核验，但初始三元组由 GPT-5 等模型生成，可能带有一定的模型偏好或逻辑风格。

（完）
