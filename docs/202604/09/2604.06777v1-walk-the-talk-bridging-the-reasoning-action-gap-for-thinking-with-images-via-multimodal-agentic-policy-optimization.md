---
title: "Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization"
title_zh: 言行一致：通过多模态智能体策略优化弥合结合图像思考中的推理-行动差距
authors: "Wenhao Yang, Yu Xia, Jinlong Huang, Shiyin Lu, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Yuchen Zhou, Xiaobo Xia, Yuanyu Wan, Lijun Zhang, Tat-Seng Chua"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06777v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于图像思考的多模态代理策略优化
tldr: 本研究针对多模态大模型在利用视觉工具推理时存在的“言行不一”问题，提出了多模态智能体策略优化（MAPO）。该方法通过要求模型生成视觉内容的文本描述，并将描述与实际观察的语义一致性融入强化学习的奖励机制，有效缩小了文本推理与视觉行动之间的差距。实验表明，MAPO显著提升了模型在复杂视觉推理任务中的表现，并降低了训练过程中的梯度方差。
selection_source: fresh_fetch
motivation: 现有的强化学习方法仅关注最终结果，忽略了模型在推理过程中可能出现的文本逻辑合理但视觉操作错误导致的误差累积问题。
method: 提出MAPO框架，通过强制模型对工具获取的视觉内容进行文本描述，并利用描述与实际观察的语义对齐度来优化优势估计。
result: 在多个视觉推理基准测试中，MAPO均取得了优于现有主流方法的性能表现。
conclusion: MAPO通过增强推理与行动的一致性，有效解决了多模态智能体在复杂任务中的训练坍缩问题，提升了整体推理能力。
---

## 摘要
近期多模态大语言模型（MLLMs）的进展激励模型在多轮推理过程中通过主动调用视觉工具来“结合图像思考”。常见的依赖基于结果奖励的强化学习（RL）实践忽略了一个事实，即文本上的合理性往往掩盖了执行上的失败，这意味着模型在智能体推理轨迹中可能会表现出直观的文本推理，但执行的视觉动作却不精确或不相关。这种推理与行动之间的差异引入了在多轮推理过程中不断累积的噪声，严重降低了模型的多模态推理能力，并可能导致训练崩溃。在本文中，我们提出了多模态智能体策略优化（MAPO），旨在弥合模型在多模态思维链（MCoT）中生成的文本推理与视觉动作之间的差距。具体而言，MAPO 要求模型为通过工具使用获取的视觉内容生成显式的文本描述。随后，我们采用了一种新颖的优势估计方法，将这些描述与实际观测之间的语义一致性与任务奖励相结合。理论研究证明了 MAPO 背后的合理性，它本质上降低了梯度的方差；广泛的实验表明，我们的方法在多个视觉推理基准测试中均取得了优异的性能。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have incentivized models to ``think with images'' by actively invoking visual tools during multi-turn reasoning. The common Reinforcement Learning (RL) practice of relying on outcome-based rewards ignores the fact that textual plausibility often masks executive failure, meaning that models may exhibit intuitive textual reasoning while executing imprecise or irrelevant visual actions within their agentic reasoning trajectories. This reasoning-action discrepancy introduces noise that accumulates throughout the multi-turn reasoning process, severely degrading the model's multimodal reasoning capabilities and potentially leading to training collapse. In this paper, we introduce Multimodal Agentic Policy Optimization (MAPO), bridging the gap between textual reasoning and visual actions generated by models within their Multimodal Chain-of-Thought (MCoT). Specifically, MAPO mandates the model to generate explicit textual descriptions for the visual content obtained via tool usage. We then employ a novel advantage estimation that couples the semantic alignment between these descriptions and the actual observations with the task reward. Theoretical findings are provided to justify the rationale behind MAPO, which inherently reduces the variance of gradients, and extensive experiments demonstrate that our method achieves superior performance across multiple visual reasoning benchmarks.

---

## 论文详细总结（自动生成）

### 论文总结：Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization

#### 1. 核心问题与整体含义
*   **研究背景**：多模态大语言模型（MLLM）正从静态图像理解转向“结合图像思考”（Thinking with Images），即在推理过程中主动调用视觉工具（如缩放、裁剪）来获取细节。
*   **核心问题**：**推理-行动差距（Reasoning-Action Gap）**。现有的强化学习（RL）训练主要依赖最终结果奖励（Outcome-based Reward），这导致模型可能产生“言行不一”的现象：文本推理逻辑看似合理，但执行的视觉动作（如裁剪位置）却是错误或无关的。如果模型碰巧猜对答案，这种错误的关联会被强化，导致训练不稳定、噪声累积甚至训练坍缩。

#### 2. 方法论
*   **核心思想**：提出**多模态智能体策略优化（MAPO）**，通过强制模型在执行动作前先“说”出意图，并验证其“行”是否对齐，从而弥合差距。
*   **关键技术细节**：
    1.  **显式标签生成**：在多模态思维链（MCoT）的每一步，要求模型在生成工具调用指令 $a_t$ 的同时，生成一个描述预期视觉内容的文本标签 $y_t$。
    2.  **语义评分机制**：利用预训练的轻量级 **CLIP 模型** 计算标签 $y_t$ 与工具返回的实际观测图像 $I_t$ 之间的余弦相似度，作为即时语义奖励。
    3.  **轨迹感知折扣（Trajectory-aware Discount）**：引入折扣因子 $\lambda$，对多轮对话中的语义分进行加权聚合。这既能防止模型通过拉长对话来刷分（奖励黑客），也为早期的探索留有容错空间。
    4.  **策略优化**：将语义一致性得分与最终结果奖励结合，在组相对优势估计（类似 GRPO）框架下进行策略更新。
*   **理论支撑**：论文证明了 MAPO 具有**双重方差缩减**效应：通过组聚合减少空间采样方差，通过语义评分减少信号噪声方差，从而保证训练稳定性。

#### 3. 实验设计
*   **数据集/场景**：聚焦于需要精细视觉推理的高分辨率基准测试，包括：
    *   **V***：视觉搜索任务。
    *   **HR-Bench**：涵盖 4K/8K 分辨率的高清视觉理解。
    *   **MME-Realworld-Lite**：真实世界复杂场景推理。
*   **对比方法**：
    *   **闭源模型**：GPT-5, Gemini 2.5 Pro。
    *   **开源智能体模型**：DeepEyes, Thyme, Mini-o3, DeepEyesV2。
    *   **RL 算法基准**：PPO, GRPO, DAPO, GSPO。
*   **基准模型**：以 Ovis2.5-9B 作为策略骨干网络。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）及总训练时长。
*   **实现细节**：提到基于 `verl` 强化学习框架实现，使用 `vLLM` 进行推理加速，采用 FSDP（全分片数据并行）和优化器状态卸载技术。全局 Batch Size 为 96，单卡 Micro Batch Size 为 4。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流高分辨率视觉基准上进行了全面测试，并提供了详细的消融实验对比不同的 RL 变体。
*   **充分性评价**：实验设计较为充分且客观。不仅对比了最终精度，还通过**奖励曲线**展示了长时训练的稳定性，并通过**可视化案例**直观证明了模型在“推理-行动”一致性上的改进。

#### 6. 主要结论与发现
*   **性能领先**：MAPO 在多个基准测试中均优于现有的开源智能体模型（如 Mini-o3）和主流 RL 算法（如 GRPO）。
*   **训练稳定性**：传统的 GRPO 在长时训练中容易出现性能震荡甚至坍缩，而 MAPO 保持了稳健的上升趋势，证明了其在大规模智能体训练中的可扩展性。
*   **认知一致性**：通过语义对齐，模型能够更精准地定位视觉目标，显著减少了因视觉操作错误导致的幻觉。

#### 7. 优点
*   **即时反馈**：将稀疏的结果奖励转化为稠密的过程监督，有效解决了强化学习在长路径推理中的信用分配问题。
*   **低成本实现**：无需额外的人工过程标注，利用预训练 CLIP 模型即可实现自动化的语义验证，计算开销极小。
*   **理论与实践结合**：不仅有显著的实验提升，还提供了严谨的数学证明解释方差缩减的原理。

#### 8. 不足与局限
*   **依赖外部模型**：语义评分的质量高度依赖于 CLIP 模型的表征能力，若 CLIP 无法识别某些细微特征，奖励信号可能失效。
*   **工具泛化性**：目前主要验证了缩放和裁剪工具，对于涉及逻辑操作（如计算器）或外部搜索（如网页浏览）的智能体任务，如何定义“语义一致性”尚需探索。
*   **算力透明度**：缺乏具体的硬件资源消耗数据，不便于其他研究者评估复现成本。

（完）
