---
title: Hierarchical Reinforcement Learning with Augmented Step-Level Transitions for LLM Agents
title_zh: 面向大语言模型智能体的增强步级转移分层强化学习
authors: "Shuai Zhen, Yanhua Yu, Ruopei Guo, Nan Cheng, Yang Deng"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05808v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 针对LLM代理的分层强化学习
tldr: 针对LLM智能体在复杂决策任务中依赖长历史导致的高计算成本和扩展性受限问题，本文提出STEP-HRL框架。该框架采用分层强化学习，通过全局子任务进度和局部交互摘要构建增强的步级状态表示，使智能体仅需单步转换即可学习。实验证明，STEP-HRL在ScienceWorld和ALFWorld上显著提升了性能和泛化能力，并大幅降低了Token消耗。
selection_source: fresh_fetch
motivation: 现有的LLM智能体过度依赖冗长的交互历史，导致推理成本高昂且难以处理长程任务。
method: 提出一种分层强化学习框架，通过全局子任务进度和局部进度模块生成的紧凑摘要来增强单步状态转换。
result: 在ScienceWorld和ALFWorld基准测试中，该方法在性能、泛化性及Token利用效率方面均优于现有基线。
conclusion: STEP-HRL通过步级学习和分层结构，为构建高效、可扩展且具备强泛化能力的LLM智能体提供了有效方案。
---

## 摘要
大语言模型（LLM）智能体在复杂的交互式决策任务中展现出了强大的能力。然而，现有的 LLM 智能体通常依赖于日益增长的交互历史，导致计算成本高昂且可扩展性有限。在本文中，我们提出了 STEP-HRL，这是一种分层强化学习（HRL）框架，它通过仅以单步转移而非完整交互历史为条件，实现了步级学习。STEP-HRL 对任务进行分层构建，利用已完成的子任务来表示整个任务的全局进度。通过引入局部进度模块，它还迭代且有选择性地总结每个子任务内的交互历史，从而生成局部进度的紧凑摘要。这些组件共同为高层和底层策略生成了增强的步级转移。在 ScienceWorld 和 ALFWorld 基准测试上的实验结果一致表明，STEP-HRL 在性能和泛化能力方面显著优于基准模型，同时减少了 token 使用量。我们的代码可在 https://github.com/TonyStark042/STEP-HRL 获取。

## Abstract
Large language model (LLM) agents have demonstrated strong capabilities in complex interactive decision-making tasks. However, existing LLM agents typically rely on increasingly long interaction histories, resulting in high computational cost and limited scalability. In this paper, we propose STEP-HRL, a hierarchical reinforcement learning (HRL) framework that enables step-level learning by conditioning only on single-step transitions rather than full interaction histories. STEP-HRL structures tasks hierarchically, using completed subtasks to represent global progress of overall task. By introducing a local progress module, it also iteratively and selectively summarizes interaction history within each subtask to produce a compact summary of local progress. Together, these components yield augmented step-level transitions for both high-level and low-level policies. Experimental results on ScienceWorld and ALFWorld benchmarks consistently demonstrate that STEP-HRL substantially outperforms baselines in terms of performance and generalization while reducing token usage. Our code is available at https://github.com/TonyStark042/STEP-HRL.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **STEP-HRL** 的新型分层强化学习框架，旨在解决大语言模型（LLM）智能体在处理长程决策任务时对交互历史过度依赖的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 LLM 智能体通常采用“历史条件化（History-conditioned）”的决策模式，即策略依赖于不断增长的观察和动作序列。
*   **研究动机**：
    *   **计算成本高**：Transformer 的注意力机制随上下文长度呈平方级增长。
    *   **冗余干扰**：长历史中包含大量无关信息，会掩盖关键决策信号，降低推理质量。
    *   **扩展性受限**：在长程任务中，上下文窗口限制了智能体的持续操作能力。
*   **整体含义**：论文提出将决策从“依赖全历史”转变为“依赖增强的单步状态”，通过分层结构和进度摘要来实现高效的步级（Step-level）学习。

### 2. 方法论：核心思想与关键技术
STEP-HRL 的核心思想是利用**全局进度**和**局部进度**来替代原始的交互历史。

*   **分层结构**：
    *   **高层策略 ($\pi_h$)**：负责生成子任务（Subtasks），已完成的子任务序列代表了任务的“全局进度”。
    *   **底层策略 ($\pi_l$)**：在当前子任务指导下执行具体动作。
*   **局部进度模块 ($\pi_p$)**：
    *   这是一个关键创新。它在每个子任务内部，迭代地将当前动作、观察和前一时刻的进度总结成一个紧凑的文本摘要。
    *   公式化表示：$p_t^k \sim \pi_p(\cdot | g_k, a_{t-1}^k, o_t^k, p_{t-1}^k)$。
*   **增强的步级转移**：
    *   底层策略仅以当前观察、当前子任务和局部进度摘要为输入，实现了恒定大小的输入长度。
*   **两阶段训练流水线**：
    1.  **行为克隆 (BC)**：利用专家演示数据初始化高层、底层和进度策略。
    2.  **步级离线强化学习 (Offline RL)**：采用类似 IQL (Implicit Q-Learning) 的方法，通过值函数估计（Critic）和优势加权回归（Actor）在离线数据集上进一步优化策略。
*   **参数共享**：三个策略共享同一个 LLM 主干网络，通过不同的 Critic 网络进行特化训练，提高了知识迁移效率。

### 3. 实验设计
*   **数据集/场景**：
    *   **ScienceWorld**：复杂的科学实验环境，包含 30 个任务家族，要求多步推理和实验操作。
    *   **ALFWorld**：基于家庭场景的交互式任务，侧重于长程动作规划。
*   **评估维度**：分为 **Seen**（训练中见过的任务变体）和 **Unseen**（未见过的任务变体，测试泛化性）。
*   **对比方法**：
    *   提示词方法：ReAct, Reflexion。
    *   微调/强化学习方法：Swift-Sage, ETO, WKM, GLIDER（现有的分层 RL 基准）。
    *   闭源模型参考：GPT-3.5, GPT-4。
*   **主干模型**：Mistral-7B, Gemma-7B, Llama3-8B，以及 Llama-1B/3B（用于扩展性分析）。

### 4. 资源与算力
*   **硬件资源**：使用了 **8 台 NVIDIA A100 80G GPU**。
*   **训练细节**：
    *   行为克隆阶段：5 个 Epoch，Batch Size 为 128。
    *   离线 RL 阶段：3 个 Epoch，Batch Size 为 256。
    *   优化器：AdamW，采用了 LoRA（低秩自适应）进行参数高效微调。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流基准上进行了全面测试，涵盖了从 1B 到 8B 不同规模的多个模型。
    *   **消融实验**：分别验证了“无局部进度模块”、“无分层结构”和“无强化学习阶段”的影响。
    *   **效率分析**：对比了 STEP-HRL 与标准 RL、HRL 在推理时的 Token 消耗。
    *   **敏感性分析**：对离线 RL 中的超参数（如温度系数 $\beta$、分位数 $\tau$）和数据量进行了详细测试。
*   **充分性评价**：实验设计非常充分，不仅关注最终得分，还深入分析了泛化能力、计算效率和超参数敏感度，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能卓越**：STEP-HRL 在所有基准测试中均显著优于现有方法。在 ALFWorld 上成功率接近饱和（>90%），在 ScienceWorld 上的提升幅度尤其明显（相比最强基线提升了 13%-26%）。
*   **泛化能力强**：在 Unseen 任务上的表现依然稳健，证明了步级抽象有助于学习到更本质的决策逻辑。
*   **极高的 Token 效率**：由于输入长度不再随步数增长，STEP-HRL 的单步 Token 使用量保持恒定且极低，大幅降低了推理成本。
*   **模型鲁棒性**：即使是较小的模型（如 Llama-1B），在 STEP-HRL 框架下也能展现出极具竞争力的性能。

### 7. 优点（亮点）
*   **架构创新**：通过“局部进度模块”巧妙地解决了长历史压缩问题，将 POMDP 简化为更易处理的步级转移。
*   **推理高效**：解决了 LLM 智能体在长程任务中推理延迟和显存占用随步数激增的痛点。
*   **训练策略科学**：结合了 BC 的稳定性与离线 RL 的优化能力，且参数共享设计减少了训练开销。

### 8. 不足与局限
*   **数据依赖性**：该方法高度依赖高质量的专家演示数据，特别是需要为专家轨迹标注子任务和局部进度，这在某些复杂环境下成本较高。
*   **子任务终止判定风险**：底层策略同时负责动作生成和子任务终止预测，如果终止判定失误（过早或过晚），会直接影响高层规划的连贯性。
*   **环境反馈要求**：虽然论文在 ScienceWorld（稠密奖励）和 ALFWorld（稀疏奖励）上都做了测试，但在完全无奖励或极度嘈杂的环境下的表现尚待验证。

（完）
