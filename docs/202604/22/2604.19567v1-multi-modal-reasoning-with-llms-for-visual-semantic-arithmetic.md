---
title: Multi-modal Reasoning with LLMs for Visual Semantic Arithmetic
title_zh: 基于大语言模型的多模态推理在视觉语义算术中的应用
authors: "Chuou Xu, Liya Ji, Qifeng Chen"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19567v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用强化学习增强视觉语义算术中的多模态推理
tldr: 本研究探讨了大型视觉语言模型（LVLM）在视觉语义算术（如“国王”图-“男人”图+“女人”图=“女王”图）中的推理能力。针对现有模型难以从视觉细节中提取抽象概念的问题，本文定义了两项新任务并构建了IRPD数据集。提出了SAri-RFT方法，利用GRPO算法和可验证函数对模型进行强化微调。实验证明该方法在IRPD和Visual7W-Telling数据集上均取得领先水平，有效提升了机器人在复杂环境下的跨模态关系推理与决策能力。
selection_source: fresh_fetch
motivation: 视觉语义算术在机器人感知与决策中至关重要，但现有模型在从复杂视觉细节中提取简洁概念并进行关系推理方面表现不足。
method: 提出了两项新任务及IRPD数据集，并开发了基于可验证函数和GRPO算法的语义算术强化微调方法（SAri-RFT）。
result: 该方法在IRPD和真实世界数据集Visual7W-Telling上均达到了SOTA性能，显著提升了多模态大模型的跨模态关系推理能力。
conclusion: 本研究通过增强LVLM的视觉语义算术能力，为家庭机器人在复杂环境中的符号推理、工具适配和人机交互奠定了基础。
---

## 摘要
强化学习（RL）作为后训练阶段，对于增强大语言模型（LLMs）在编程和数学方面的推理能力至关重要。然而，它们在视觉语义算术（即从图像中推断关系）方面的能力仍有待探索。经典的文本类比“国王”-“男人”+“女人”=“女王”展示了关系推理，但将文本替换为“国王”和“男人”的图像会显著降低性能，因为这需要常识知识以及从无关的视觉细节中提取简洁概念的能力。这种能力对于非结构化环境中的服务和家用机器人非常重要，机器人必须推断物体、代理和动作之间的语义关系。在厨房场景中，从图像中识别出“粉末”和“蛋糕”通过“由……制成”相关联，将符号关系锚定在感知中，从而实现工具替换、任务泛化和改进的语义推理。先前的工作通过在向量算术后解码图像特征来处理语义算术，但存在模态间隙且缺乏系统性评估。在本文中，我们制定了两个新任务：二项减法和三项运算，并构建了图像-关系对数据集（IRPD）用于基准测试。我们进一步提出了语义算术强化微调（SAri-RFT），利用可验证函数和群体相对策略优化（GRPO）对大型视觉语言模型（LVLMs）进行后训练。我们的方法在 IRPD 和真实世界的 Visual7W-Telling 数据集上达到了最先进的结果。通过赋予 LVLMs 鲁棒的跨模态关系推理能力，本研究提升了家用机器人将符号推理锚定在感知中的能力，增强了复杂环境下的决策、工具适应性和人机交互。数据集和源代码在补充材料中提供。

## Abstract
Reinforcement learning (RL) as post-training is crucial for enhancing the reasoning ability of large language models (LLMs) in coding and math. However, their capacity for visual semantic arithmetic, inferring relationships from images, remains underexplored. The classic text analogy "king"-"man"+"woman" = "queen" illustrates relational reasoning, yet replacing text with images of "king" and "man" significantly reduces performance because it requires commonsense knowledge and the extraction of concise concepts from irrelevant visual details. This capability is important for service and domestic robotics in unstructured environments, where robots must infer semantic relationships among objects, agents, and actions. In a kitchen, recognizing from images that "powder" and "cake" are related by "is made of" grounds symbolic relations in perception, enabling tool substitution, task generalization, and improved semantic reasoning. Prior work approaches semantic arithmetic by decoding image features after vector arithmetic, but suffers from modality gaps and lacks systematic evaluation. In this paper, we formulate two novel tasks, two-term subtraction and three-term operations, and construct the Image-Relation-Pair Dataset (IRPD) for benchmarking. We further propose Semantic Arithmetic Reinforcement Fine-Tuning (SAri-RFT), which post-trains large vision-language models (LVLMs) using a verifiable function and Group Relative Policy Optimization (GRPO). Our method achieves state-of-the-art results on IRPD and the real-world Visual7W-Telling dataset. By equipping LVLMs with robust cross-modal relational reasoning, this work advances domestic robots' ability to ground symbolic reasoning in perception, enhancing decision-making, tool adaptability, and human-robot interaction in complex environments. Datasets and source code are provided in the supplementary material.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-modal Reasoning with LLMs for Visual Semantic Arithmetic》的结构化总结：

### 1. 论文的核心问题与整体含义
论文探讨了大型视觉语言模型（LVLM）在**视觉语义算术（Visual Semantic Arithmetic）**中的推理能力。
*   **研究动机**：虽然 LLM 在文本类比（如“国王”-“男人”+“女人”=“女王”）上表现出色，但在处理图像模态时性能大幅下降。
*   **核心挑战**：模型需要具备常识，并能从复杂的视觉背景中提取简洁的抽象概念，而非关注无关的视觉细节。
*   **应用价值**：这种能力对家用机器人至关重要，使其能通过感知理解物体间的语义关系（如“粉末”是“蛋糕”的原料），从而实现工具适配和任务泛化。

### 2. 方法论
论文提出了一套完整的框架，包括任务定义、数据集构建和微调算法：
*   **任务定义**：
    1.  **二项减法（Two-term Subtraction）**：给定主语和宾语（图像或文本），推断其关系（如：$宾语 - 主语 = 关系$）。
    2.  **三项运算（Three-term Operations）**：类比推理（如：$宾语1 - 主语1 + 主语2 = 宾语2$）。
*   **核心技术：SAri-RFT（语义算术强化微调）**：
    *   **基础模型**：使用 Qwen2-VL-7B 作为策略模型。
    *   **强化学习算法**：采用 **GRPO（群体相对策略优化）**，通过组内样本的相对奖励来优化模型，无需额外的奖励模型（RM）。
    *   **可验证奖励函数（Verifiable Reward Functions）**：
        *   对于二项任务：使用准确率奖励（$R_{acc}$）和格式奖励（$R_{format}$）。
        *   对于三项任务：设计了**软可验证奖励**（$R_{sim}$），利用 Word2Vec 计算生成答案与真值之间的余弦相似度，并通过 Sigmoid 函数缩放，以解决开放式问题的评估难题。

### 3. 实验设计
*   **数据集**：
    *   **IRPD（图像-关系对数据集）**：基于 ConceptNet 提取 18 种关系，利用 **FLUX.1-dev** 生成高质量图像，并通过 CLIP 分数和人工审核进行双重过滤。包含 1500+ 个高质量主宾对。
    *   **Visual7W-Telling**：用于验证模型在真实世界 VQA 任务中的泛化能力。
*   **对比方法（Baselines）**：
    *   **嵌入算术法**：ZeroCap、ImageBind、LanguageBind（直接在向量空间做减法）。
    *   **LVLM 推理法**：InternVL2.5-8B、Video-LLaVa、Qwen2-VL-7B（原始及 SFT 版本）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 H800 GPU**。
*   **训练时长**：训练过程大约持续 **2 小时**（共 150 个 step）。
*   **参数设置**：GRPO 每组采样数量 $n=8$，最大 Prompt 长度为 1024-2048。

### 5. 实验数量与充分性
*   **实验规模**：在 IRPD 任务上进行了 4 选项和 6 选项的分类测试，以及 1000 个实体的三项运算测试。
*   **消融实验**：针对采样组大小（$n$）、思维链（CoT）的有无、不同奖励组件（$R_{acc}, R_{sim}, R_{format}$）以及 Sigmoid 缩放的效果进行了详细对比。
*   **主观评估**：进行了用户研究（User Study），验证了 Word2Vec 相似度评分与人类判断的一致性（得分 4.2/5）。
*   **评价**：实验设计较为充分，涵盖了从合成数据到真实场景的迁移，对比基准多样，客观性较强。

### 6 主要结论与发现
*   **强化学习优于 SFT**：SAri-RFT 在二项减法任务上比 SFT 提升了超过 **50%**，在三项运算任务上提升了超过 **75%**。
*   **思维链（CoT）至关重要**：消融实验显示，去除 CoT 会导致模型在复杂类比任务中的表现剧烈下降。
*   **模态对齐不等于关系对齐**：实验证明，仅仅将文本和图像嵌入对齐（如 ImageBind）并不足以解决语义算术问题，模型必须理解向量间的方向关系。

### 7. 优点
*   **创新性**：首次正式定义了视觉语义算术任务，并引入了可验证奖励的强化学习（RLVR）来解决该问题。
*   **数据集质量高**：IRPD 数据集结合了知识图谱（ConceptNet）和先进的扩散模型（FLUX），并经过严格的人工校验。
*   **奖励设计巧妙**：通过 Word2Vec 引入“软奖励”，有效解决了生成式任务中难以给出 0/1 硬评分的痛点。

### 8. 不足与局限
*   **抽象概念处理**：数据集中仍存在一些难以视觉化的抽象术语（如“战争”导致“暴力”），模型在这些概念上的推理仍有挑战。
*   **奖励作弊风险**：在 RLVR 阶段，模型有时会倾向于通过生成模板化输出（Shortcut）来获取格式奖励，而非真正理解语义。
*   **运算类型有限**：目前仅探索了加减法，尚未涉及更复杂的语义逻辑运算。

（完）
