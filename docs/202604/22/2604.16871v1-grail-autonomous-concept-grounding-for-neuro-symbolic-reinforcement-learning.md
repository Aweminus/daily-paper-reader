---
title: "GRAIL: Autonomous Concept Grounding for Neuro-Symbolic Reinforcement Learning"
title_zh: GRAIL：神经符号强化学习的自主概念落地
authors: "Hikaru Shindo, Henri Rößler, Quentin Delfosse, Kristian Kersting"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16871v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 利用大语言模型进行概念落地的神经符号强化学习
tldr: 神经符号强化学习（NeSy-RL）通常依赖人工定义的空间关系概念，限制了其在不同环境中的适应性。本文提出GRAIL框架，旨在通过环境交互自主学习关系概念。GRAIL利用大语言模型提供初始的通用概念表示作为弱监督，随后通过交互精细化这些表示以适应特定环境语义。在Atari游戏实验中，GRAIL在性能上达到或超过了人工定义概念的智能体，实现了高效的概念对齐与策略优化。
selection_source: fresh_fetch
motivation: 传统的神经符号强化学习依赖人工定义关系概念，导致其在语义多变的环境中缺乏灵活性和适应性。
method: 提出GRAIL框架，利用大语言模型生成通用概念作为弱监督，并通过环境交互自主精细化关系概念的语义。
result: 在Kangaroo、Seaquest和Skiing等Atari游戏上的实验表明，GRAIL的性能可媲美或优于使用人工定义概念的智能体。
conclusion: GRAIL成功实现了关系概念的自主对齐，为构建更具通用性和解释性的神经符号强化学习系统提供了新路径。
---

## 摘要
神经符号强化学习（NeSy-RL）将符号推理与基于梯度的优化相结合，以实现具有可解释性和泛化性的策略。关系概念（如“在……左侧”或“靠近”）作为基础构建模块，构成了智能体感知和行动的方式。然而，传统方法需要人类专家手动定义这些概念，由于概念语义在不同环境中存在差异，这限制了其适应性。我们提出了 GRAIL（通过交互式学习落地关系智能体），这是一个通过环境交互自主落地关系概念的框架。GRAIL 利用大语言模型（LLM）提供通用概念表示作为弱监督，随后对其进行细化以捕捉特定环境的语义。该方法解决了欠定环境中普遍存在的稀疏奖励信号和概念失配问题。在 Atari 游戏 Kangaroo、Seaquest 和 Skiing 上的实验表明，在简化设置下，GRAIL 的表现达到或超过了使用手动设计概念的智能体，并揭示了在完整环境中奖励最大化与高层目标完成之间具有启发性的权衡。

## Abstract
Neuro-symbolic Reinforcement Learning (NeSy-RL) combines symbolic reasoning with gradient-based optimization to achieve interpretable and generalizable policies. Relational concepts, such as "left of" or "close by", serve as foundational building blocks that structure how agents perceive and act. However, conventional approaches require human experts to manually define these concepts, limiting adaptability since concept semantics vary across environments. We propose GRAIL (Grounding Relational Agents through Interactive Learning), a framework that autonomously grounds relational concepts through environmental interaction. GRAIL leverages large language models (LLMs) to provide generic concept representations as weak supervision, then refines them to capture environment-specific semantics. This approach addresses both sparse reward signals and concept misalignment prevalent in underdetermined environments. Experiments on the Atari games Kangaroo, Seaquest, and Skiing demonstrate that GRAIL matches or outperforms agents with manually crafted concepts in simplified settings, and reveals informative trade-offs between reward maximization and high-level goal completion in the full environment.

---

## 论文详细总结（自动生成）

以下是对论文《GRAIL: Autonomous Concept Grounding for Neuro-Symbolic Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
神经符号强化学习（NeSy-RL）旨在结合神经网络的优化能力与符号逻辑的可解释性及泛化性。然而，现有的 NeSy-RL 方法存在一个重大瓶颈：**关系概念（如“在……左侧”、“靠近”）的语义通常需要人类专家手动硬编码**。
*   **研究动机：** 相同的逻辑概念在不同环境中具有不同的物理含义。例如，在《袋鼠》（Kangaroo）游戏中，“在梯子左侧”意味着水平位置靠左且垂直对齐；而在《滑雪》（Skiing）中，则需要考虑滑行轨迹的预判。
*   **核心问题：** 如何让智能体在没有专家预定义函数的情况下，通过环境交互自主学习（落地）这些抽象概念的语义？

### 2. 方法论：核心思想与关键技术
论文提出了 **GRAIL（Grounding Relational Agents through Interactive Learning）** 框架，其核心思想是利用大语言模型（LLM）作为“弱监督者”提供通用语义先验，再通过环境交互进行精细化调整。

*   **核心架构：** 继承自 BlendRL，采用混合策略 $\pi = \beta \cdot \pi_{neu} + (1-\beta) \cdot \pi_{log}$，其中 $\beta$ 是由神经网络学习的混合权重，决定何时使用神经策略或符号策略。
*   **可微概念落地：** 将逻辑谓词（如 $left\_of$）实现为可微的估值函数（Valuation Functions），输入为对象间的相对坐标，输出为 [0, 1] 之间的真值。
*   **概念对齐（Concept Alignment）：**
    1.  **LLM 代理生成：** 使用 LLM（如 GPT-4o 或 Claude）生成 Python 代码，定义概念的通用“代理函数”（Proxy Functions）。
    2.  **对齐损失函数：** 引入概念对齐损失 $L_{CA}$，通过二元交叉熵（BCE）强制学习到的估值函数在空间网格上与 LLM 代理函数的输出保持一致。
    3.  **动态退火：** 损失函数公式为 $L = L_{BlendRL} + (1 - \gamma_{CA} \frac{t}{T}) \cdot c_{CA} \cdot L_{CA}$。随着训练进行，LLM 的监督信号逐渐减弱，允许智能体根据环境奖励微调概念语义。
*   **两阶段训练流程：**
    *   **阶段 1：** 在简化环境（无敌人）中仅训练逻辑策略和估值函数，确保概念语义正确落地。
    *   **阶段 2：** 在完整环境中冻结估值函数，联合训练神经策略和混合模块。

### 3. 实验设计
*   **实验场景：** 三款具有挑战性的 Atari 2600 游戏：*Kangaroo*（袋鼠）、*Seaquest*（潜艇救人）、*Skiing*（滑雪）。
*   **Benchmark 与工具：** 使用 **OCAtari** 提取对象级状态（位置、类别等）。
*   **对比方法：**
    1.  **NeuralPPO：** 纯神经网络基准。
    2.  **BlendRL (Hard-coded)：** 使用专家手动编写的估值函数（上限基准）。
    3.  **BlendRL + LLM：** 直接使用 LLM 生成的代码作为固定估值函数（不进行学习）。
    4.  **GRAIL (GPT-4o/Claude)：** 本文提出的自动学习方法。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确提及**具体的 GPU 型号、数量或具体的训练小时数。
*   **训练规模：** 阶段 1 训练了 1000 万步（更新频率为每 4 步一次），阶段 2 训练了 6000 万步。实验使用了 32 个并行环境进行采样。

### 5. 实验数量与充分性
*   **实验规模：** 针对 3 个环境分别进行了两阶段训练，每个实验运行了 3 个随机种子并报告了标准差。
*   **消融实验：** 详细探讨了对齐系数 $c_{CA}$ 和退火因子 $\gamma_{CA}$ 对性能的影响，并可视化了学习前后的概念热图。
*   **充分性评价：** 实验设计较为充分，不仅对比了最终奖励（Return），还对比了高层目标完成度（Goals），客观地展示了 LLM 原始输出与经过环境落地后的性能差异。

### 6. 主要结论与发现
*   **性能匹配专家：** GRAIL 学习到的概念在简化环境中达到了与专家手动设计函数相当的水平，显著优于直接使用 LLM 代码的方法。
*   **环境自适应性：** GRAIL 能够发现环境特有的语义。例如在《滑雪》中，它学会了将“左侧”概念向上方对角线延伸，以适应下坡运动的预判需求。
*   **目标与奖励的权衡：** 在完整环境中，GRAIL 在《潜艇救人》中是唯一能真正完成“救人”目标的模型，而纯神经模型虽然可能获得高分，却往往忽略了高层任务逻辑。

### 7. 优点
*   **自动化程度高：** 解决了神经符号系统对专家知识的过度依赖，实现了从语言描述到物理落地的自动化。
*   **强解释性：** 最终策略是人类可读的一阶逻辑规则，且空间概念可以通过热图直观验证。
*   **弱监督学习：** 巧妙地将 LLM 作为不完美的先验，通过强化学习进行纠偏，解决了符号落地中的歧义问题。

### 8. 不足与局限
*   **两阶段依赖：** 依赖于先在简化环境中学习概念，若直接在复杂环境中端到端学习，可能会因奖励稀疏导致概念漂移。
*   **感知层依赖：** 严重依赖于 OCAtari 等对象提取工具的准确性，若对象检测失效，符号推理将崩溃。
*   **误对齐风险：** 实验观察到，如果对齐系数设置过低，智能体可能会学习到错误的语义（如将不同平台的梯子混淆），这表明模型对超参数仍有一定敏感性。
*   **扩展性：** 目前仅验证了空间关系谓词，对于更复杂的非空间属性或高阶关系的落地尚待研究。

（完）
