---
title: "AJ-Bench: Benchmarking Agent-as-a-Judge for Environment-Aware Evaluation"
title_zh: AJ-Bench：面向环境感知评估的“智能体作为评判者”基准测试
authors: "Wentao Shi, Yu Wang, Yuyang Zhao, Yuxin Chen, Fuli Feng, Xueyuan Hao, Xi Su, Qi Gu, Hui Su, Xunliang Cai, Xiangnan He"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18240v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 强化学习用于GUI环境中的智能体行为验证
tldr: 针对大语言模型智能体在复杂环境中难以验证的问题，本文提出了AJ-Bench基准测试，旨在评估“智能体作为评委”（Agent-as-a-Judge）的能力。该基准涵盖搜索、数据系统和图形界面三大领域，包含155个任务和516条轨迹，重点考察智能体在获取信息、状态验证和过程验证方面的表现。实验证明，相比传统的LLM-as-a-Judge，该方法能显著提升验证性能，但也揭示了当前智能体验证面临的挑战。
selection_source: fresh_fetch
motivation: 传统的规则验证或LLM评委难以泛化到复杂环境，需要一种能主动与环境交互以获取验证证据的智能体评委。
method: 提出AJ-Bench基准，通过搜索、数据系统和GUI三大领域的任务，系统评估智能体评委的信息获取、状态验证和过程验证能力。
result: 实验显示Agent-as-a-Judge在性能上一致优于LLM-as-a-Judge基准，但在处理复杂验证逻辑时仍存在显著挑战。
conclusion: AJ-Bench为环境感知型评估提供了重要工具，证明了智能体评委的潜力，并指明了未来提升验证可靠性的研究方向。
---

## 摘要
随着强化学习不断扩大基于大语言模型的智能体训练规模，在复杂环境中可靠地验证智能体行为变得日益具有挑战性。现有方法依赖于基于规则的验证器或“LLM作为评判者”（LLM-as-a-Judge）模型，这些方法难以推广到狭窄领域之外。“智能体作为评判者”（Agent-as-a-Judge）通过主动与环境和工具交互以获取可验证的证据来解决这一局限性，但其能力仍有待深入探索。我们推出了 AJ-Bench 基准测试，旨在跨搜索、数据系统和图形用户界面（GUI）三个领域系统地评估“智能体作为评判者”，该基准包含 155 个任务和 516 条标注轨迹。该基准全面评估了评判智能体在信息获取、状态验证和过程验证方面的能力。实验表明，与 LLM-as-a-Judge 基准相比，该方法在性能上取得了持续提升，同时也揭示了基于智能体的验证中存在的重大开放性挑战。我们的数据和代码可在 https://aj-bench.github.io/ 获取。

## Abstract
As reinforcement learning continues to scale the training of large language model-based agents, reliably verifying agent behaviors in complex environments has become increasingly challenging. Existing approaches rely on rule-based verifiers or LLM-as-a-Judge models, which struggle to generalize beyond narrow domains. Agent-as-a-Judge addresses this limitation by actively interacting with environments and tools to acquire verifiable evidence, yet its capabilities remain underexplored.   We introduce a benchmark AJ-Bench to systematically evaluate Agent-as-a-Judge across three domains-search, data systems, and graphical user interfaces-comprising 155 tasks and 516 annotated trajectories. The benchmark comprehensively assesses judge agents' abilities in information acquisition, state verification, and process verification. Experiments demonstrate consistent performance gains over LLM-as-a-Judge baselines, while also revealing substantial open challenges in agent-based verification. Our data and code are available at https://aj-bench.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《AJ-Bench: Benchmarking Agent-as-a-Judge for Environment-Aware Evaluation》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着强化学习（RL）在提升大语言模型（LLM）智能体性能方面的作用日益凸显，如何在大规模训练中可靠地验证智能体在复杂环境下的行为成为了核心瓶颈。
*   **现有局限**：传统的“基于规则的验证”难以处理开放式任务（如事实核查）；而“LLM作为评判者”（LLM-as-a-Judge）仅依赖静态文本信号，缺乏对环境状态的真实感知，容易产生误判。
*   **研究动机**：提出“智能体作为评判者”（Agent-as-a-Judge）范式，即让评委智能体通过主动与环境交互、调用工具、重放轨迹来获取证据，从而实现更精准的评估。

### 2. 方法论：核心思想与技术流程
论文提出了 **AJ-Bench**，这是首个专门用于评估评委智能体“环境感知验证能力”的基准测试。
*   **核心思想**：将验证过程从“文本比对”转变为“交互式取证”。评委智能体被赋予操作环境的权限，通过观察环境反馈来核实被评估智能体的操作结果。
*   **三个验证维度**：
    1.  **信息获取（Search）**：通过外部搜索验证轨迹中的事实性。
    2.  **状态验证（DS）**：通过检查文件系统或数据库状态验证操作结果。
    3.  **过程验证（GUI）**：通过检查关键动作序列和界面变化验证执行逻辑。
*   **算法流程**：
    1.  **环境初始化**：根据任务重置环境。
    2.  **轨迹重放**：将被评估智能体的动作在环境中执行，达到最终状态。
    3.  **智能体评判**：评委智能体接收任务指令和执行轨迹，自主决定何时调用工具（如浏览器、文件读取、截图等）获取证据。
    4.  **输出判定**：基于获取的证据给出二进制（成功/失败）标签及推理过程。

### 3. 实验设计
*   **数据集与场景**：
    *   **搜索领域**：源自 Mind2Web2 和 WideSearch，侧重多跳推理和信息覆盖。
    *   **数据系统（DS）**：源自 MCPMark，涉及文件系统操作和 Postgres 数据库。
    *   **GUI 领域**：源自 OSWorld，涵盖 PowerPoint、Word 和 Excel 的复杂操作。
*   **Benchmark 规模**：包含 155 个任务和 516 条标注轨迹（含成功与失败样本）。
*   **对比方法**：
    *   **LLM-as-a-Judge**：包括 GPT-5、Claude 4.5、Gemini 3、Grok-4、DeepSeek-v3.2 等顶级模型的静态评估版本。
    *   **Agent-as-a-Judge**：以 GPT-5-mini 和 DeepSeek-v3.2 为基础构建的交互式评委。

### 4. 资源与算力
*   **算力说明**：论文提到 GUI 任务部署在隔离的 **AWS 实例**上以支持并发评估，DS 任务在本地重放。
*   **具体参数**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于评估（Inference-based Evaluation）而非模型训练，算力消耗主要集中在多轮交互推理和环境模拟上。

### 5. 实验数量与充分性
*   **实验组数**：进行了跨三个领域、多个子任务的全面测试，并针对推理能力（Thinking）、交互轮数（Turns）、输入模态（多模态消融）以及智能体框架（MCPMark vs ReAct）进行了多组消融实验。
*   **客观性与公平性**：
    *   使用了 F1 分数作为主要指标，并提供了 95% 置信区间。
    *   通过人工标注和规则脚本双重校验 Ground Truth 标签。
    *   对比实验在相同基座模型下进行（如 GPT-5-mini 的静态 vs 交互版本），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：Agent-as-a-Judge 在所有领域均显著优于 LLM-as-a-Judge，平均 F1 分数提升了约 **13%**。
*   **能力解耦**：强大的内在推理能力（Thinking）并不直接等同于强大的评判能力。实验显示，增加推理步数并不总是能提升评判准确率，关键在于如何有效调用工具。
*   **交互价值**：增加交互轮数能显著提升 F1 分数，尤其是在需要迭代收集信息的 Word 和 PPT 任务中。
*   **多模态复杂性**：在 GUI 领域，同时提供截图和可访问性树（A11y Tree）有时会引入噪声，并非所有场景下多模态融合都优于单模态。

### 7. 优点与亮点
*   **填补空白**：首次系统性地定义并评估了评委智能体的交互验证能力，而非仅仅将其视为文本分类器。
*   **环境感知**：通过环境重放技术，使验证过程具备了“实地考察”的真实性。
*   **跨领域覆盖**：基准设计兼顾了互联网搜索、底层系统操作和上层软件 GUI，具有很强的代表性。

### 8. 不足与局限
*   **任务多样性受限**：大部分任务是从现有基准修改而来，而非完全原创，可能存在数据分布的局限。
*   **环境稳定性**：搜索领域依赖实时网络，网络波动可能影响评估的一致性。
*   **成本与效率**：Agent-as-a-Judge 需要多轮推理和环境交互，其评估成本（Token 消耗和时间）远高于传统的静态评估。
*   **失败模式**：分析显示，评委智能体在“误解工具输出”和“证据正确但推理错误”方面仍有较高错误率。

（完）
