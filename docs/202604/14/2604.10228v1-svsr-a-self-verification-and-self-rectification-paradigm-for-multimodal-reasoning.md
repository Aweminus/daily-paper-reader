---
title: "SVSR: A Self-Verification and Self-Rectification Paradigm for Multimodal Reasoning"
title_zh: SVSR：一种用于多模态推理的自我验证与自我修正范式
authors: "Zhe Qian, Nianbing Su, Zhonghua Wang, Hebei Li, Zhongxing Xu, Yueying Li, Fei Luo, Zhuohan Ouyang, Yanbiao Ma"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10228v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 将自我验证和自我纠正集成到多模态推理流水线中
tldr: 针对多模态模型推理深度不足、易出错的问题，本文提出SVSR框架，通过自我验证与自我纠正机制提升推理的鲁棒性。该框架采用三阶段训练：首先构建包含正反向推理的高质量偏好数据集，接着进行冷启动有监督微调，最后利用半在线直接偏好优化（Semi-online DPO）持续增强模型能力。实验证明，SVSR显著提升了模型在复杂视觉理解任务中的准确性、泛化能力及隐式推理表现。
selection_source: fresh_fetch
motivation: 现有模型推理过程不完整或不一致，导致在复杂多模态任务中容易出现错误。
method: 提出包含高质量偏好数据构建、冷启动SFT及半在线DPO的三阶段训练范式，使模型具备显式的自我验证与纠正能力。
result: 在多个基准测试中显著提高了推理准确性，并展现出更强的泛化能力和隐式推理性能。
conclusion: SVSR为构建更可靠、具备内省能力且认知对齐的多模态系统提供了有效路径。
---

## 摘要
现有的多模态模型通常面临浅层推理的问题，导致由不完整或不一致的思维过程引起的错误。为了解决这一局限性，我们提出了自我验证与自我修正（SVSR），这是一个将自我验证和自我修正显式集成到模型推理流水线中的统一框架，显著提高了在复杂视觉理解和多模态推理任务中的鲁棒性和可靠性。SVSR 基于一种新颖的三阶段训练范式。首先，我们通过细化预训练视觉语言模型的推理轨迹，构建了一个高质量的统一偏好数据集，并结合了正向和反向推理以嵌入自我反思信号。其次，我们在该数据集上进行冷启动有监督微调，以学习结构化的多步推理行为。第三，我们应用半在线直接偏好优化（Semi-online DPO）过程，通过强大的教师 VLM 过滤模型生成的高质量推理轨迹，不断扩充训练语料库。该流水线使模型能够学习、诱发并完善其自我验证和自我修正的能力。在不同基准测试上的广泛实验表明，SVSR 提高了推理准确性，并对未见过的任务和问题类型具有更强的泛化能力。值得注意的是，一旦经过显式自我反思推理的训练，模型还表现出更强的隐式推理能力，即使在不提供显式推理轨迹的情况下，其表现也优于强基准模型。这些结果突显了 SVSR 在构建更可靠、具有内省性且符合认知对齐的多模态系统方面的潜力。

## Abstract
Current multimodal models often suffer from shallow reasoning, leading to errors caused by incomplete or inconsistent thought processes. To address this limitation, we propose Self-Verification and Self-Rectification (SVSR), a unified framework that explicitly integrates self-verification and self-rectification into the model's reasoning pipeline, substantially improving robustness and reliability in complex visual understanding and multimodal reasoning tasks. SVSR is built on a novel three-stage training paradigm. First, we construct a high-quality unified preference dataset by refining reasoning traces from pre-trained vision-language models, incorporating both forward and backward reasoning to embed self-reflective signals. Second, we perform cold-start supervised fine-tuning on this dataset to learn structured, multi-step reasoning behaviors. Third, we apply a Semi-online Direct Preference Optimization (Semi-online DPO) process, continuously augmenting the training corpus with high-quality, model-generated reasoning traces filtered by a powerful teacher VLM. This pipeline enables the model to learn, elicit, and refine its ability to self-verify and self-rectify. Extensive experiments across diverse benchmarks demonstrate that SVSR improves reasoning accuracy and enables stronger generalization to unseen tasks and question types. Notably, once trained with explicit self-reflective reasoning, the model also exhibits improved implicit reasoning ability, outperforming strong baselines even when no explicit reasoning traces are provided. These results highlight the potential of SVSR for building more dependable, introspective, and cognitively aligned multimodal systems.

---

## 论文详细总结（自动生成）

以下是对论文《SVSR: A Self-Verification and Self-Rectification Paradigm for Multimodal Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉语言模型（VLMs）在处理复杂任务时往往表现出“浅层推理”特征，容易因思维过程不完整或逻辑不一致而产生错误。
*   **核心问题**：如何让中小型 VLM 具备类似 OpenAI o1 的深度思考能力，即在推理过程中能够自主发现错误并进行修正，从而提高多模态推理的鲁棒性和可靠性。
*   **整体含义**：本文提出了 **SVSR** 框架，通过显式地将“自我验证”和“自我修正”集成到模型的推理流水线中，探索了在多模态领域通过增加测试时计算（Test-time Computation）来提升性能的新范式。

### 2. 方法论
SVSR 采用了一个三阶段的训练流水线：
*   **阶段 1：自我修正轨迹数据构建**
    *   利用强大的教师模型（如 GPT-4o）针对复杂问题生成包含“解决-验证-修正”循环的推理轨迹。
    *   引入了**正向推导**和**反向证明（矛盾法）**两种验证策略，以减少模型自我肯定的偏见。
    *   构建了约 5k 条高质量的结构化数据，包含 `think` 标签内的多步反思过程。
*   **阶段 2：冷启动有监督微调（SFT）**
    *   在构建的结构化数据集上对基础模型（如 Qwen2.5-VL-7B）进行微调。
    *   目标是让模型内化自我修正的语法、结构和推理流，学会使用特定的触发词（如 "Wait, let me recheck"）开启反思模式。
*   **阶段 3：半在线直接偏好优化（Semi-online DPO）**
    *   **核心思想**：通过迭代优化进一步强化模型的判断力。
    *   **流程**：模型针对同一问题生成多个候选轨迹，由教师模型根据答案正确性、逻辑连贯性和验证有效性进行评分，选出“优选（Chosen）”和“拒绝（Rejected）”对。
    *   **半在线机制**：定期刷新训练数据，确保偏好对能反映当前模型的弱点，避免数据陈旧。

### 3. 实验设计
*   **基础模型**：Qwen2.5-VL-7B-Instruct。
*   **数据集/基准测试**：
    *   **数学推理**：MathVista, DynaMath, MathVerse。
    *   **图表与文档理解**：AI2D。
    *   **通用多模态理解与幻觉评估**：MMStar, MMVet。
*   **对比方法**：
    *   **闭源模型**：GPT-4V, GPT-4o, Claude-3.5-Sonnet, Gemini-1.5-Pro。
    *   **开源模型**：InternVL2.5 系列（8B/26B）、InternVL3-9B、Qwen2-VL-7B、LLaVA-OneVision-72B 等。
    *   **消融实验**：对比了基础模型、仅冷启动 SFT 以及完整 SVSR（SFT+RFT）的效果。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到使用了 `vLLM` 推理框架，并基于 `Qwen2.5-VL` 的官方代码库进行开发。训练数据量相对较小（SFT 阶段 5k 样本，RL 阶段约 20k 样本），暗示该方法对算力的需求在可接受范围内，适合中型实验室复现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流多模态基准测试，对比了超过 10 种先进模型。
*   **分析深度**：除了准确率，还进行了深入的**行为量化分析**，包括：
    *   验证准确率（Verification Acc）与错误召回率（Error Recall）。
    *   错误转正确（Incorrect to Correct）与正确转错误（Correct to Incorrect）的比例。
    *   不同难度等级下的推理步数（Trials）分布。
*   **客观性**：实验设计较为公平，通过随机切换验证策略来减少方法论偏见，并展示了模型在未见任务上的泛化能力。

### 6. 主要结论与发现
*   **性能提升**：SVSR 在所有基准测试中均显著优于基础模型，在 MathVista 上达到 73.8% 的准确率，超越了许多参数量更大的模型。
*   **自我修正能力**：经过强化学习（RFT）后，模型识别错误的召回率显著提升，且“误改”正确答案的情况大幅减少。
*   **自适应推理**：模型学会了根据问题难度分配计算资源——简单问题快速回答，复杂问题则进行更多轮次的验证。
*   **隐式推理红利**：即使不要求模型输出显式的 `think` 轨迹，经过训练的模型在直接回答时的表现也优于基准，说明反思能力已内化。

### 7. 优点
*   **高效性**：仅需数万条高质量数据即可显著提升 7B 规模模型的推理深度。
*   **结构化范式**：提出的“解决-验证-修正”循环具有很强的可解释性和可操作性。
*   **半在线 DPO**：通过动态刷新偏好数据，有效解决了静态数据集导致的模型优化瓶颈。

### 8. 不足与局限
*   **推理延迟**：显式的自我修正过程显著增加了 Token 生成数量，导致推理时间变长，不适合实时性要求极高的场景。
*   **教师模型依赖**：数据构建和 DPO 评分高度依赖 GPT-4o 等顶级模型，若教师模型存在偏见或错误，会直接影响训练效果。
*   **能力上限**：强化学习主要在于激发模型潜能，如果基础模型本身缺乏某些底层视觉感知能力，SVSR 无法通过逻辑修正来弥补这种“硬伤”。

（完）
