---
title: Visual Enhanced Depth Scaling for Multimodal Latent Reasoning
title_zh: 视觉增强的深度缩放用于多模态潜空间推理
authors: "Yudong Han, Yong Wang, Zaiquan Yang, Zhen Qu, Liyuan Pan, Xiangxiang Chu"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10500v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉语言模型中的多模态潜空间推理
tldr: 本研究针对多模态隐式推理中视觉优化不足和固定架构深度限制推理能力的问题，提出了视觉增强深度缩放框架。通过分析梯度动力学，发现视觉标记存在欠优化且复杂标记需要更多推理步骤。为此，引入视觉重放模块增强细粒度感知，并采用路由深度缩放机制为复杂标记分配更多计算资源。结合课程学习策略将显式思维链内化为隐式表示，在提升推理性能的同时显著降低了推理延迟。
selection_source: fresh_fetch
motivation: 发现多模态隐式推理中视觉标记因语言偏见导致欠优化，且固定深度限制了复杂标记的深度推理。
method: 提出视觉重放模块以强化空间感知，并结合路由深度缩放机制为复杂标记动态分配推理步骤。
result: 该框架在多个基准测试中达到SOTA水平，且推理速度优于显式思维链基线。
conclusion: 通过优化视觉感知和动态调整推理深度，隐式推理能有效替代显式思维链并实现性能与效率的双重提升。
---

## 摘要
多模态潜空间推理已成为一种极具前景的范式，它通过隐式特征传播取代了显式的思维链（CoT）解码，在增强表示信息量的同时降低了推理延迟。通过分析潜空间训练过程中的 token 级梯度动力学，我们发现了两个关键现象：（1）由于固有的语言偏见，视觉 token 的梯度范数显著高于文本 token 且波动更大，导致了系统性的视觉优化不足；（2）语义简单的 token 收敛迅速，而复杂 token 在固定架构深度的限制下表现出持续的梯度不稳定。为了解决这些局限性，我们提出了视觉重放模块和路由深度缩放，旨在协同增强视觉感知并精炼复杂的潜变量，以实现更深层次的上下文推理。前者利用因果自注意力机制来估计 token 的显著性，通过空间相干约束强化细粒度的视觉定位。作为补充，后者机制为复杂 token 自适应地分配额外的推理步骤，从而实现更深层的上下文精炼。在将显式 CoT 逐步内化为紧凑潜空间表示的课程学习策略指导下，我们的框架在多个基准测试中达到了最先进的性能，同时相比显式 CoT 基准实现了显著的推理加速。

## Abstract
Multimodal latent reasoning has emerged as a promising paradigm that replaces explicit Chain-of-Thought (CoT) decoding with implicit feature propagation, simultaneously enhancing representation informativeness and reducing inference latency. By analyzing token-level gradient dynamics during latent training, we reveal two critical observations: (1) visual tokens exhibit significantly higher and more volatile gradient norms than their textual counterparts due to inherent language bias, resulting in systematic visual under-optimization; and (2) semantically simple tokens converge rapidly, whereas complex tokens exhibit persistent gradient instability constrained by fixed architectural depths. To address these limitations, we propose a visual replay module and routing depth scaling to collaboratively enhance visual perception and refine complicated latents for deeper contextual reasoning. The former module leverages causal self-attention to estimate token saliency, reinforcing fine-grained grounding through spatially-coherent constraints. Complementarily, the latter mechanism adaptively allocates additional reasoning steps to complex tokens, enabling deeper contextual refinement. Guided by a curriculum strategy that progressively internalizes explicit CoT into compact latent representations, our framework achieves state-of-the-art performance across diverse benchmarks while delivering substantial inference speedups over explicit CoT baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Visual Enhanced Depth Scaling for Multimodal Latent Reasoning》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨了多模态大模型（MLLM）中**潜空间推理（Latent Reasoning）**的优化问题。潜空间推理通过隐式特征传播取代显式的文本思维链（CoT），具有推理延迟低、表示信息量大的优点。
研究者通过分析训练过程中的 token 级梯度动力学，发现了两个核心瓶颈：
*   **视觉-文本优化失衡**：由于语言偏见，视觉 token 的梯度范数显著高于文本 token 且波动剧烈，导致视觉特征在训练中处于“欠优化”状态。
*   **固定深度的优化困境**：简单样本收敛快，而复杂样本在固定层数的架构下梯度持续不稳定，表明复杂推理需要更灵活的计算深度。

### 2. 论文提出的方法论
为了解决上述问题，论文提出了一个统一的框架，包含以下核心组件：
*   **空间相干细粒度视觉重放（SCF-VR）**：
    *   **注意力引导区域聚焦**：聚合所有层的注意力图，识别当前推理步骤中最相关的视觉 token。
    *   **自蒸馏监督**：在原始图像中裁剪出对应的空间连贯区域并重新编码，通过轻量级超分辨率模块（TokenSR）对潜空间 token 进行重构损失约束，确保模型捕捉细粒度视觉细节。
*   **路由深度缩放（RDS）**：
    *   引入轻量级**路由网络（Router）**，根据 token 的重要性得分动态决定是否对其进行额外的 Transformer 层迭代计算。
    *   **自适应分配**：为高难度 token 分配更多推理步骤，实现计算资源的按需分配。
*   **课程潜空间训练（Curriculum Training）**：
    *   采用渐进式策略，先训练显式 CoT，随后逐步将推理步骤封装进紧凑的 `<latent>` token 中，使模型平滑地将逻辑依赖内化。

### 3. 实验设计
*   **数据集与场景**：涵盖了 12 个主流基准测试，分为三大类：
    1.  **数学推理**：MathVista, MathVision, MM-Math。
    2.  **视觉感知**：HallusionBench, MMVP, Seed-Bench-2-Plus, HR-Bench, GQA。
    3.  **多模态组合推理**：MMStar, BLINK, ScienceQA, M3CoT。
*   **对比方法（Baselines）**：
    *   **零样本 VLM**：GPT-4o, Qwen2.5-VL, InternVL3.5。
    *   **显式 CoT 方法**：Multimodal-CoT, CCoT, ICoT。
    *   **工具/RL 增强方法**：Vision-R1, VL-Rethinker, DeepEyes。
    *   **潜空间推理方法**：Laser, LVR, Monet, DMLR。

### 4. 资源与算力
*   **硬件环境**：
    *   **训练**：使用了 **8 张 NVIDIA H100 (80GB VRAM)** GPU。
    *   **推理/评估**：使用了 **8 张 NVIDIA H20 (96GB VRAM)** GPU。
*   **软件配置**：采用 DeepSpeed ZeRO-2 优化器，Adam 优化算法，学习率为 $4 \times 10^{-5}$。
*   **训练时长**：默认训练 16 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文在 12 个不同维度的基准测试上进行了广泛测试，并针对 Qwen2-VL、Qwen2.5-VL 和 Chameleon 等多种主流架构（2B 到 7B 规模）进行了验证。
*   **消融实验**：非常充分。作者分别验证了 SCF-VR 模块、RDS 模块、课程学习策略、路由保留参数 $\alpha$、视觉重放策略（Soft-Mix vs. Patch）、位置编码方式以及超参数 $\lambda$ 对性能的影响。
*   **客观性**：通过可视化梯度动力学（Frobenius 范数和 Nuclear 范数）来支撑其研究动机，实验对比涵盖了从闭源 SOTA 到最新的开源 RL 增强模型，评价维度包括准确率、自回归步数和推理耗时，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：在 M3CoT、ScienceQA 等多个数据集上刷新了 SOTA 记录，尤其在视觉感知任务（如 MMVP）上提升显著（+4.67%）。
*   **效率极高**：相比显式 CoT，自回归生成步数减少了 **10 倍以上**，推理速度提升了 **3-6 倍**，且性能优于直接预测答案的 No-CoT 模式。
*   **视觉增强有效性**：SCF-VR 模块能有效缓解视觉幻觉，使潜空间 token 能够精准定位图像中的关键区域（如门、标志等）。
*   **动态深度必要性**：RDS 机制证明了为复杂 token 分配额外计算量能显著提升逻辑推理的严密性。

### 7. 优点
*   **理论支撑扎实**：通过 token 级梯度分析揭示了 MLLM 隐式推理的底层瓶颈，而非盲目堆砌模块。
*   **即插即用**：该框架不改变原始 VLM 的核心架构，能轻松适配各种基于 Transformer 的多模态模型。
*   **效率与精度平衡**：在保持极低推理延迟的同时，通过潜空间内化实现了接近甚至超过显式长链推理的效果。
*   **无需额外标注**：利用自蒸馏和课程学习，减少了对昂贵的人工中间推理步骤标注的依赖。

### 8. 不足与局限
*   **文本密集型任务表现平平**：在 SeedBench-2-Plus 等偏重文本理解的基准上提升相对较小，可能受限于训练数据中该类样本的稀缺。
*   **超参数敏感性**：实验显示损失权重 $\lambda$ 对不同规模的模型（2B vs 7B）影响差异巨大，需要针对特定模型进行精细调优。
*   **应用场景限制**：目前主要集中在静态图像推理，尚未在长视频理解或极长上下文推理中验证其扩展性。

（完）
