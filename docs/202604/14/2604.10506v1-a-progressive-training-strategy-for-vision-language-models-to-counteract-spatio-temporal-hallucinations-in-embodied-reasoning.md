---
title: A Progressive Training Strategy for Vision-Language Models to Counteract Spatio-Temporal Hallucinations in Embodied Reasoning
title_zh: 视觉语言模型对抗具身推理中时空幻觉的渐进式训练策略
authors: "Xiaoda Yang, Shuai Yang, Can Wang, Jingyang Xue, Menglan Tang, Checheng Yu, Xunzhe Zhou, Sashuai Zhou, Tao Jin, Lixin Yang, Xiangyu Yue, Zhou Zhao"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10506v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于视觉语言模型时空推理的思维链数据集
tldr: "针对视觉语言模型在具身推理中存在的时空幻觉问题，本文提出了一种渐进式训练策略。研究发现模型往往依赖浅层捷径而非真实因果理解，导致正反向查询性能差异巨大。为此，作者构建了精细化的思维链（CoT）数据集，并采用先监督预训练逻辑结构、后弱标签微调泛化的两阶段框架。实验证明该方法显著提升了模型精度，并将正反向性能差距从70%以上降至6.53%，有效增强了模型的真实动态推理能力。"
selection_source: fresh_fetch
motivation: 视觉语言模型在处理多图时空推理时存在严重的“幻觉”现象，表现为正反向查询性能极度不平衡，反映出模型缺乏真正的因果理解。
method: 提出一种渐进式训练框架，首先利用新构建的精细化时空思维链（CoT）数据集进行监督预训练，随后使用大规模弱标签数据进行微调以提升泛化性。
result: "该方法不仅提高了模型的基础准确率，还将正反向推理的性能差距从超过70%大幅缩减至6.53%。"
conclusion: 通过结构化的思维链引导和渐进式训练，可以有效纠正VLM固有的时间偏见，使其具备更真实的动态时空推理能力。
---

## 摘要
视觉语言模型（VLM）在静态图像理解方面取得了显著进展，但在时空推理方面仍面临严峻挑战。一个主要的瓶颈是“多图推理幻觉”，即在前向和反向时间查询之间存在巨大的性能差异，这揭示了模型依赖于表层捷径而非真正的因果理解。为了缓解这一问题，我们首先开发了一个新的思维链（CoT）数据集，将复杂的推理分解为详细的时空步骤和明确的判断。在此基础上，我们提出了一种渐进式训练框架：首先在我们的 CoT 数据集上进行监督预训练以植入逻辑结构，随后利用可扩展的弱标签数据进行微调，以实现更广泛的泛化。我们的实验表明，该方法不仅提高了主干网络的准确率，还将前向与反向性能差距从 70% 以上大幅削减至仅 6.53%。这证实了该方法能够培养真实的动态推理能力，并减少当前 VLM 固有的时间偏差。

## Abstract
Vision-Language Models (VLMs) have made significant strides in static image understanding but continue to face critical hurdles in spatiotemporal reasoning. A major bottleneck is "multi-image reasoning hallucination", where a massive performance drop between forward and reverse temporal queries reveals a dependence on superficial shortcuts instead of genuine causal understanding. To mitigate this, we first develop a new Chain-of-Thought (CoT) dataset that decomposes intricate reasoning into detailed spatiotemporal steps and definitive judgments. Building on this, we present a progressive training framework: it initiates with supervised pre-training on our CoT dataset to instill logical structures, followed by fine-tuning with scalable weakly-labeled data for broader generalization. Our experiments demonstrate that this approach not only improves backbone accuracy but also slashes the forward-backward performance gap from over 70\% to only 6.53\%. This confirms the method's ability to develop authentic dynamic reasoning and reduce the inherent temporal biases of current VLMs.

---

## 论文详细总结（自动生成）

这是一份关于论文《A Progressive Training Strategy for Vision-Language Models to Counteract Spatio-Temporal Hallucinations in Embodied Reasoning》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉语言模型（VLM）在处理多图时空推理时存在严重的“时空幻觉”。具体表现为模型高度依赖输入顺序的浅层捷径（如默认第二张图更接近完成），而非真正的因果理解。
*   **研究背景**：在具身智能任务中，模型需要判断物理状态的转换。然而，现有模型在“前向查询”和“反向查询”下的表现差异巨大（差距常超过70%），这种不一致性证明了模型并未掌握真正的动态逻辑。

### 2. 方法论
论文提出了一种名为 **STCR** 的渐进式训练框架，核心思想是模拟人类“先学理论，后做练习”的认知过程：
*   **STCR-CoT 数据集构建**：
    *   从 AgiBot 真实机器人视频中提取数据，进行10倍时间下采样以过滤噪声。
    *   采用多粒度滑动窗口（5-16帧间隔）生成图像对，涵盖从微观视觉差异到宏观逻辑推理的不同难度。
    *   **思维链（CoT）标注**：不仅提供最终答案，还包含详细的空间细节描述（如夹爪状态、物体相对位置），形成“感知-判断”的认知支架。
*   **两阶段渐进式训练**：
    *   **阶段1：CoT 监督预训练**：在全参数微调模式下，监督模型生成完整的推理链。这迫使模型将原始像素压缩为可验证的空间属性，建立“先感知、后判断”的逻辑结构。
    *   **阶段2：弱监督微调（标签微调）**：利用大规模、低成本的仅标签数据（Tag-only）进行微调。此时移除显式推理步骤，迫使模型内化阶段1学到的逻辑，实现能力的规模化泛化。
*   **正反向对比解耦**：在训练中同时加入正向和反向序列实例，使任何依赖顺序的捷径失效，强制模型关注内在因果。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：STCR-CoT 包含 3470 万个样本，涵盖 7.9 万个独特任务实例，分为抓取放置、包装、家务、烹饪等 7 大类。
    *   **测试集**：包含 35,660 个独立测试回合，覆盖 29 个高层任务类别。
*   **Benchmark**：使用了 **MMSI-Bench**（多图空间智能基准）进行评估。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-o4mini, Gemini-1.5-Pro 等。
    *   **开源 2D 模型**：Qwen2.5-VL-7B (基座), NVILA-8B, InternVL-8B, DeepSeek-VL2 等。
    *   **开源 3D 模型**：3D-LLM, Chat-Scene, 3D-LLaVA 等。

### 4. 资源与算力
*   **训练细节**：使用了 AdamW 优化器，初始学习率 2e-7，采用余弦学习率调度，开启 BF16 混合精度训练。
*   **算力配置**：文中提到 Batch Size 为 8，配合 2 步梯度累积（等效 Batch Size 16）。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模实验，训练数据达 34.7M，测试样本超 3.5 万，规模在同类研究中属于顶尖水平。
*   **消融实验**：对比了“仅分类头”、“CoT+分类头”、“仅标签”以及“仅 CoT（本文）”四种策略，验证了 CoT 初始化的必要性。
*   **充分性评价**：实验设计较为充分，通过不同窗口大小（WinSize）的测试展示了模型在不同时间跨度下的鲁棒性。对比了 2D 和 3D 感知模型，并验证了“规模法则（Scaling Law）”，即性能随弱监督数据量增加而稳步提升。

### 6. 主要结论与发现
*   **消除偏见**：STCR 框架将正反向推理的性能差距从基准模型的 70% 以上缩减至 **6.53%**，实现了对称推理。
*   **精度提升**：最终模型在测试集上达到 **87.07%** 的平均准确率，显著优于 GPT-4o 等闭源模型。
*   **奖励模型潜力**：该模型可作为具身智能的“奖励模型（Reward Model）”，提供单调且连续的反馈信号，优于现有的 VLAC 等基准。
*   **3D 瓶颈**：发现现有 3D 感知模型在语义推理上反而不如 2D 模型，暗示 3D 特征到因果推理的映射存在瓶颈。

### 7. 优点
*   **数据引擎创新**：提出了一种低成本、可扩展的数据生成方式，利用视频天然的时间顺序生成海量弱标签数据。
*   **认知架构合理**：通过 CoT 强制建立“感知-判断”支架，有效解决了深度学习中常见的“捷径学习（Shortcut Learning）”问题。
*   **鲁棒性极强**：成功解决了 VLM 对输入顺序敏感的顽疾，这对实际机器人部署至关重要。

### 8. 不足与局限
*   **算力细节缺失**：未披露具体硬件资源消耗，难以评估训练成本。
*   **领域局限性**：数据主要来源于 AgiBot 机器人视频，虽然类别丰富，但在完全不同的环境（如户外极端场景）下的泛化能力尚待验证。
*   **推理开销**：虽然训练是渐进式的，但生成详细 CoT 推理链在推理阶段可能会增加计算延迟，论文未详细讨论推理速度。

（完）
