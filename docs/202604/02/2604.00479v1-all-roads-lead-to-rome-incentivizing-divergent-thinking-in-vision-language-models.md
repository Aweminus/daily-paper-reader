---
title: "All Roads Lead to Rome: Incentivizing Divergent Thinking in Vision-Language Models"
title_zh: 殊途同归：激励视觉语言模型中的发散性思维
authors: "Xinyu Tian, Shu Zou, Zhaoyuan Yang, Mengqi He, Peter Tu, Jing Zhang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00479v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 强化学习（GRPO）增强了视觉语言模型的推理能力
tldr: 本研究探讨了强化学习（特别是GRPO）在提升视觉语言模型（VLM）推理能力中的作用与局限。作者发现RL模型虽推理深入但路径单一，易陷入“多样性坍缩”。为此，提出了多组策略优化（MUPO）方法，旨在通过激励发散性思维来探索多种解题路径。实验证明，MUPO能有效缓解局部最优问题，提升模型在基准测试上的表现。
selection_source: fresh_fetch
motivation: 现有的强化学习方法在提升VLM推理能力时容易导致多样性坍缩，使模型过早收敛于有限的策略而陷入局部最优。
method: 提出了多组策略优化（MUPO），这是一种旨在激励模型在多个解决方案中产生发散性思维的简单且有效的方法。
result: 实验结果表明，MUPO在多个基准测试中均取得了显著成效，有效提升了模型的推理能力和扩展性。
conclusion: 通过激励发散性思维，可以克服强化学习中的策略坍缩问题，从而更充分地挖掘视觉语言模型的推理潜力。
---

## 摘要
最近的研究表明，强化学习（RL），特别是群体相对策略优化（GRPO），能够本质上激发并增强视觉语言模型（VLM）的推理能力。然而，尽管展现出巨大潜力，驱动强化学习模型有效性的底层机制及其局限性仍未得到充分探索。在本文中，我们强调了强化学习模型与基础模型之间的一个根本性行为差异：前者进行更深层但更狭窄的推理，而基础模型虽然在单一路径上不够精细，却表现出更广泛且更多样化的思维模式。通过对训练动态的进一步分析，我们发现 GRPO 容易出现多样性崩溃，导致模型过早地收敛到有限的推理策略子集，并丢弃了大部分潜在的替代方案，从而导致局部最优和较差的可扩展性。为解决这一问题，我们提出了多组策略优化（MUPO），这是一种简单而有效的方法，旨在激励跨多种解决方案的发散性思维，并在公认的基准测试上证明了其有效性。项目主页：https://xytian1008.github.io/MUPO/

## Abstract
Recent studies have demonstrated that Reinforcement Learning (RL), notably Group Relative Policy Optimization (GRPO), can intrinsically elicit and enhance the reasoning capabilities of Vision-Language Models (VLMs). However, despite the promise, the underlying mechanisms that drive the effectiveness of RL models as well as their limitations remain underexplored. In this paper, we highlight a fundamental behavioral distinction between RL and base models, where the former engages in deeper yet narrow reasoning, while base models, despite less refined along individual path, exhibit broader and more diverse thinking patterns. Through further analysis of training dynamics, we show that GRPO is prone to diversity collapse, causing models to prematurely converge to a limited subset of reasoning strategies while discarding the majority of potential alternatives, leading to local optima and poor scalability. To address this, we propose Multi-Group Policy Optimization (MUPO), a simple yet effective approach designed to incentivize divergent thinking across multiple solutions, and demonstrate its effectiveness on established benchmarks. Project page: https://xytian1008.github.io/MUPO/

---

## 论文详细总结（自动生成）

以下是对论文《All Roads Lead to Rome: Incentivizing Divergent Thinking in Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：强化学习（RL），特别是群体相对策略优化（GRPO），已显著提升了视觉语言模型（VLM）的推理能力。
*   **核心问题**：作者发现 RL 训练后的模型虽然在单一路径上推理更深，但思维变得“狭隘”。相比之下，基础模型（Base Model）虽然单次表现稍逊，但具备更强的“发散性思维”，在多次尝试中能探索出更多解题路径。
*   **动机**：GRPO 在训练初期会出现“多样性坍缩（Diversity Collapse）”，导致模型过早收敛于少数几种策略，陷入局部最优，且限制了测试时缩放（Test-time Scaling）的潜力。本研究旨在让模型在保持推理深度的同时，保留基础模型的多样化探索能力。

### 2. 方法论：核心思想与关键技术
作者提出了 **MUPO（Multi-Group Policy Optimization，多组策略优化）**，作为 GRPO 的改进方案：
*   **核心思想**：将采样的多个响应划分为不同的“组”，在组内进行局部优势估计，并引入多样性奖励来拉开组间距离。
*   **关键技术细节**：
    *   **响应分组**：利用嵌入模型（Embedding Model）对生成的 $N$ 个响应进行聚类，将相似的推理路径归为一组，确保每组代表一种独特的推理模式。
    *   **局部优势估计**：不同于 GRPO 的全局归一化，MUPO 在每个组内独立计算优势函数（Advantage），防止主导策略抑制其他潜在有效的替代策略。
    *   **多样性奖励（Diversity Reward）**：计算某一响应与其它组响应之间的平均余弦距离。为了防止模型为了多样性而胡言乱语，该奖励仅在答案正确时发放。
    *   **退火机制**：多样性奖励的权重 $\lambda$ 采用余弦退火策略，训练初期鼓励广泛探索，后期转向对全局最优解的精细化利用。
    *   **负载均衡**：引入缩放因子 $w_k$，平衡不同规模组对总目标的贡献。

### 3. 实验设计
*   **数据集**：使用 **ViRL39K** 进行训练，这是一个高质量的视觉推理数据集。
*   **Benchmark（基准测试）**：
    *   **数学推理**：MathVerse, MathVista, MathVision, LogicVista, WeMath, Geometry3K。
    *   **通用能力**：MMStar, HallusionBench, MMVet。
*   **对比方法**：包括 InternVL2.5, R1-OneVision, VLAA-Thinker, Vision-R1, VLM-R1, LMM-R1 等主流开源模型，以及 GPT-5-Thinking 和 Gemini-2.5-Pro 等闭源模型。
*   **评估指标**：除了标准的 **acc@1**（贪婪解码），还引入了 **acc@4**（采样 4 次的通过率）来衡量模型的测试时扩展能力。

### 4. 资源与算力
*   **训练配置**：模型在 3B 和 7B 两个参数规模上进行了实验。
*   **超参数**：每个样本生成 $N=15$ 个响应，划分为 $K=3$ 个组，学习率为 1e-6，训练 2 个 epoch。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及总算力消耗时长。但从采样数量（N=15）来看，其训练阶段的推理开销高于标准 GRPO。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个主流基准测试，对比了超过 6 种最先进的开源 RL 模型。
*   **消融实验**：针对分组数量 $K$、多样性奖励权重 $\lambda$、负载均衡因子 $\beta$ 均做了详细的消融研究。
*   **充分性评价**：实验设计较为充分，通过 t-SNE 可视化直观展示了推理路径的分布差异，不仅验证了准确率的提升，还深入分析了训练动态（多样性随步数的变化），论证具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **RL 的局限性**：GRPO 会导致模型在训练前 20 步内多样性骤降，陷入“单模态”优化。
*   **MUPO 的优越性**：MUPO-Thinker-7B 在数学基准上平均提升了 2.5% 的 acc@1，在 acc@4 上更是大幅领先（+6.0%），证明了发散性思维对测试时缩放至关重要。
*   **性能提升**：3B 规模的 MUPO 模型在多个指标上甚至能与部分 7B 规模的基线模型持平。

### 7. 优点
*   **切中痛点**：敏锐捕捉到了当前 R1 类模型“推理深但路径窄”的问题，并给出了理论解释。
*   **即插即用**：MUPO 作为 GRPO 的替代方案，逻辑清晰，易于集成到现有的 RL 框架中。
*   **扩展性强**：通过激励多样性，显著提升了模型在 Best-of-N 等测试时计算策略下的表现。

### 8. 不足与局限
*   **计算成本**：训练时需要采样更多响应（N=15）并进行实时聚类，增加了训练的时间和显存开销。
*   **依赖嵌入模型**：多样性奖励的质量高度依赖于所使用的 Embedding 模型的表征能力，若 Embedding 无法准确区分推理逻辑的细微差别，效果可能打折。
*   **任务敏感性**：消融实验显示，数学任务需要更多分组（高 K 值），而通用任务则相反，这意味着针对不同领域可能需要手动调整超参数。

（完）
