---
title: "CORA: Conformal Risk-Controlled Agents for Safeguarded Mobile GUI Automation"
title_zh: CORA：用于安全移动端 GUI 自动化的符合性风险控制智能体
authors: "Yushi Feng, Junye Du, Qifan Wang, Zizhan Ma, Qian Niu, Yutaka Matsuo, Long Feng, Lequan Yu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09155v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 基于视觉语言模型的GUI自动化智能体，属于非机器人领域的智能体强化学习应用。
tldr: 针对移动端GUI智能体在自主操作中可能引发的隐私、财产等安全风险，本文提出了CORA框架。该框架利用保形风险控制技术，在动作执行前通过Guardian模型评估风险，并结合Diagnostician模型进行多模态诊断，为智能体提供具有统计学保障的安全边界。同时，引入Phone-Harm基准测试，证明了CORA在保障安全的同时，能有效平衡任务完成度与用户干扰。
selection_source: fresh_fetch
motivation: 现有的GUI智能体安全机制缺乏形式化验证和可调节的统计保障，难以应对不可逆的现实危害。
method: 提出基于保形风险控制的框架，通过Guardian模型校准执行边界，并利用Diagnostician模型和Goal-Lock机制进行风险诊断与意图锁定。
result: 在自建的Phone-Harm基准及公开数据集上，CORA在安全性、任务完成度与用户干扰的权衡中表现优于多种基准方法。
conclusion: CORA为自主GUI执行提供了一种实用且具有统计理论支撑的安全范式，显著提升了移动智能体在复杂环境下的可靠性。
---

## 摘要
由视觉语言模型 (VLM) 驱动的图形用户界面 (GUI) 智能体正迅速从被动辅助转向自主运行。然而，这种不受限制的动作空间使用户面临严重的、不可逆转的财务、隐私或社交损害。现有的安全保障措施依赖于提示工程、脆弱的启发式方法以及将 VLM 作为评判者，缺乏形式化验证和用户可调的保证。我们提出了 CORA（符合性风险控制 GUI 智能体），这是一种策略后、动作前的安全保障框架，可为执行有害动作提供统计学保证。CORA 将安全性重新定义为选择性动作执行：我们训练了一个 Guardian（守护者）模型，用于估计每个建议步骤的动作条件风险。我们不直接对原始分数设置阈值，而是利用符合性风险控制 (Conformal Risk Control) 来校准一个满足用户指定风险预算的“执行/放弃”边界，并将被拒绝的动作路由到一个可训练的 Diagnostician（诊断者）模型，该模型对被拒绝的动作进行多模态推理，以推荐干预措施（例如：确认、反思或中止），从而最大限度地减轻用户负担。一种 Goal-Lock（目标锁定）机制将评估锚定在明确且冻结的用户意图上，以抵御视觉注入攻击。为了严格评估这一范式，我们引入了 Phone-Harm，这是一个在真实场景下具有步骤级损害标签的移动端安全违规新基准。在 Phone-Harm 和公共基准上针对多种基线的实验验证了 CORA 改善了安全性-有用性-中断性的帕累托前沿，为自主 GUI 执行提供了一种实用的、有统计学依据的安全范式。代码和基准测试可在 cora-agent.github.io 获取。

## Abstract
Graphical user interface (GUI) agents powered by vision language models (VLMs) are rapidly moving from passive assistance to autonomous operation. However, this unrestricted action space exposes users to severe and irreversible financial, privacy or social harm. Existing safeguards rely on prompt engineering, brittle heuristics and VLM-as-critic lack formal verification and user-tunable guarantees. We propose CORA (COnformal Risk-controlled GUI Agent), a post-policy, pre-action safeguarding framework that provides statistical guarantees on harmful executed actions. CORA reformulates safety as selective action execution: we train a Guardian model to estimate action-conditional risk for each proposed step. Rather than thresholding raw scores, we leverage Conformal Risk Control to calibrate an execute/abstain boundary that satisfies a user-specified risk budget and route rejected actions to a trainable Diagnostician model, which performs multimodal reasoning over rejected actions to recommend interventions (e.g., confirm, reflect, or abort) to minimize user burden. A Goal-Lock mechanism anchors assessment to a clarified, frozen user intent to resist visual injection attacks. To rigorously evaluate this paradigm, we introduce Phone-Harm, a new benchmark of mobile safety violations with step-level harm labels under real-world settings. Experiments on Phone-Harm and public benchmarks against diverse baselines validate that CORA improves the safety--helpfulness--interruption Pareto frontier, offering a practical, statistically grounded safety paradigm for autonomous GUI execution. Code and benchmark are available at cora-agent.github.io.

---

## 论文详细总结（自动生成）

这是一份关于论文《CORA: Conformal Risk-Controlled Agents for Safeguarded Mobile GUI Automation》的结构化技术总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着视觉语言模型（VLM）驱动的 GUI 智能体从“对话辅助”转向“自主操作”，其庞大的动作空间带来了严重的潜在风险，如误转账、隐私泄露或误删数据。
*   **核心挑战**：现有的安全机制（如 Prompt 工程、简单的启发式规则或 VLM 充当裁判）存在三大缺陷：1) 缺乏形式化的统计验证；2) 无法根据用户需求灵活调节安全等级；3) 容易受到视觉注入攻击（Visual Injection）。
*   **整体含义**：论文提出了 **CORA** 框架，旨在为移动端 GUI 智能体提供一个“策略后、动作前”的安全盾牌，通过统计学手段确保有害动作的执行率被控制在用户指定的预算之内。

### 2. 方法论
CORA 将安全问题重新定义为**选择性动作执行（Selective Action Execution）**，其核心流程如下：
*   **Guardian (System 1 - 快速反应)**：一个轻量级的动作条件风险评估模型。它接收当前截图、UI 树、用户目标和建议动作，输出一个标量风险分数 $s_t$。
*   **符合性风险控制 (Conformal Risk Control, CRC)**：这是该论文的数学核心。通过在校准集上计算，确定一个最优阈值 $\hat{\tau}$。该阈值保证在统计意义上，被错误执行的有害动作期望比例低于用户设定的预算 $\alpha$（例如 $\alpha=0.01$ 表示有害动作率不超过 1%）。
*   **Diagnostician (System 2 - 深度推理)**：当动作被 Guardian 拦截时，路由至该生成式模型。它进行多模态推理，输出诊断报告，包括：1) 风险原因；2) 损害类型；3) 干预建议（中止、请求用户确认或自我反思）。
*   **Goal-Lock（目标锁定）机制**：在任务开始时冻结用户意图，将屏幕上的文字视为“不可信证据”，从而免疫试图通过屏幕内容篡改指令的视觉注入攻击。

### 3. 实验设计
*   **基准测试 (Benchmarks)**：
    *   **Phone-Harm (自建)**：包含 150 个有害任务（Harm-150）和 150 个良性任务（Normal-150），涵盖误用、注入和误操作三大类。
    *   **MobileRisk**：评估移动智能体在真实工作流中的安全性。
    *   **AndroidWorld**：评估智能体在长程导航任务中的通用能力。
*   **对比方法**：
    *   **基础策略**：GPT-5 mini, Gemini-3 Flash, UI-TARS-1.5, AutoGLM-VLM。
    *   **防御基线**：基于 Prompt 的自我检查、基于规则的启发式网关、VLM-as-critic（将 VLM 作为裁判）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **8 张 NVIDIA A100 (80GB)** GPU 的节点上运行。
*   **训练细节**：
    *   Guardian 和 Diagnostician 均基于 AutoGLM-Phone-9B 模型进行 LoRA 微调。
    *   训练时长：每个模型训练 3 个 Epoch。
    *   数据规模：Guardian 训练集包含 1,588 个样本，Diagnostician 包含 502 个样本。
*   **推理延迟**：基础策略平均每步耗时 20.52s，加入 CORA 后平均每步耗时 21.37s（System 1 增加的开销极小），若触发 System 2 诊断则该步耗时增至 34.13s。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同的基准上进行了详尽测试，涵盖了从安全性（Harm Rate）到有用性（Goal Achievement Rate）以及用户干扰（Over-Intervention Rate）的全面评估。
*   **消融实验**：针对 CRC 校准、动作条件（Action-conditioning）、诊断器设计和 Goal-Lock 机制分别做了消融研究，验证了各组件的必要性。
*   **客观性**：采用了严格的“训练-校准-测试”拆分协议，确保测试集与训练/校准集在任务和情节层面完全隔离，避免了数据泄露。

### 6. 主要结论与发现
*   **帕累托前沿优化**：CORA 在“安全性-有用性-中断性”的权衡中显著优于所有基线。在保持高任务完成率的同时，能将有害动作率控制在极低水平。
*   **统计保证有效**：实验证明 CRC 能够可靠地将风险控制在预设的 $\alpha$ 预算内。
*   **能力保留与增强**：在良性任务（AndroidWorld）中，CORA 不仅没有降低成功率，反而通过 Diagnostician 的“反思”建议，帮助智能体从误操作中恢复，将成功率从 30% 提升至 40%。

### 7. 优点
*   **理论严谨**：引入符合性预测理论，为 AI 智能体的安全性提供了可量化的统计保障。
*   **用户可调**：提供了一个“安全旋钮”（$\alpha$），允许用户根据应用场景（如金融 vs 娱乐）自定义风险承受能力。
*   **抗攻击性**：Goal-Lock 机制有效地解决了当前 GUI 智能体面临的视觉注入这一顽疾。
*   **实用性强**：作为插件式框架，无需重新训练底层的复杂策略模型。

### 8. 不足与局限
*   **序列依赖性**：虽然考虑了时间相关性，但在极长程的任务中，如何更精确地处理步骤间的复合风险仍有待深入研究。
*   **分布偏移风险**：尽管提出了加权校准方案，但在面对全新的操作系统版本或极端的 UI 风格变化时，校准的有效性可能会下降。
*   **推理成本**：虽然 System 1 很快，但 System 2 的引入在被拦截步骤上增加了约 66% 的延迟，对于实时性要求极高的场景可能存在限制。

（完）
