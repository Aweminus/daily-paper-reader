---
title: "VISOR: Agentic Visual Retrieval-Augmented Generation via Iterative Search and Over-horizon Reasoning"
title_zh: VISOR：通过迭代搜索与跨视界推理实现的智能体视觉检索增强生成
authors: "Yucheng Shen, Jiulong Wu, Jizhou Huang, Dawei Yin, Lingyong Yan, Min Cao"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09508v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 代理视觉检索增强生成与多步推理
tldr: 针对视觉检索增强生成（VRAG）在处理复杂多步推理任务时面临的证据稀疏和长程搜索漂移问题，本文提出了VISOR框架。该框架通过构建结构化证据空间实现跨页推理，并引入视觉动作评估纠正机制。同时，利用滑动窗口动态轨迹和意图注入技术缓解上下文过载。通过基于GRPO的强化学习训练，VISOR在多个长程视觉推理基准测试中达到了SOTA水平，显著提升了检索效率和准确性。
selection_source: fresh_fetch
motivation: 现有的智能体VRAG系统在处理跨页分散证据时存在推理孤立问题，且长程搜索中视觉Token的堆积会导致模型产生认知负荷并偏离搜索目标。
method: 提出了包含结构化证据空间、视觉动作纠正机制以及带意图注入的动态滑动窗口轨迹的统一框架，并采用改进的GRPO强化学习进行训练。
result: 在ViDoSeek、SlideVQA和MMLongBench等多个权威基准测试中，VISOR均取得了当前最先进的性能表现，并展现出卓越的推理效率。
conclusion: VISOR通过创新的证据管理和上下文重构机制，有效解决了长程视觉推理中的搜索漂移挑战，为复杂视觉文档理解提供了高效的智能体方案。
---

## 摘要
视觉检索增强生成 (VRAG) 赋予视觉语言模型在视觉丰富文档上进行检索和推理的能力。为了处理需要多步推理的复杂查询，智能体 VRAG 系统将推理与迭代检索交织在一起。然而，现有的智能体 VRAG 面临两个关键瓶颈：(1) 视觉证据稀疏性：关键证据散布在不同页面中却被孤立处理，阻碍了跨页推理；此外，细粒度的图像内证据通常需要精确的视觉动作，而这些动作的误用会降低检索质量；(2) 长程搜索漂移：检索页面中视觉标记 (visual tokens) 的累积稀释了上下文并导致认知过载，使智能体偏离其搜索目标。为了应对这些挑战，我们提出了 VISOR（通过迭代搜索与跨视界推理实现的视觉检索增强生成），这是一个统一的单智能体框架。VISOR 具有一个用于渐进式跨页推理的结构化证据空间，并结合了视觉动作评估与修正机制来管理视觉动作。此外，我们引入了带有滑动窗口和意图注入的动态轨迹，以缓解搜索漂移。这些设计在锚定证据空间的同时丢弃早期的原始交互，防止上下文被视觉标记淹没。我们使用基于群体相对策略优化 (GRPO) 的强化学习流水线训练 VISOR，并针对动态上下文重构定制了状态掩码和信用分配。在 ViDoSeek、SlideVQA 和 MMLongBench 上的广泛实验表明，VISOR 在长程视觉推理任务中实现了最先进的性能和卓越的效率。

## Abstract
Visual Retrieval-Augmented Generation (VRAG) empowers Vision-Language Models to retrieve and reason over visually rich documents. To tackle complex queries requiring multi-step reasoning, agentic VRAG systems interleave reasoning with iterative retrieval.. However, existing agentic VRAG faces two critical bottlenecks. (1) Visual Evidence Sparsity: key evidence is scattered across pages yet processed in isolation, hindering cross-page reasoning; moreover, fine-grained intra-image evidence often requires precise visual actions, whose misuse degrades retrieval quality; (2) Search Drift in Long Horizons: the accumulation of visual tokens across retrieved pages dilutes context and causes cognitive overload, leading agents to deviate from their search objective. To address these challenges, we propose VISOR (Visual Retrieval-Augmented Generation via Iterative Search and Over-horizon Reasoning), a unified single-agent framework. VISOR features a structured Evidence Space for progressive cross-page reasoning, coupled with a Visual Action Evaluation and Correction mechanism to manage visual actions. Additionally, we introduce a Dynamic Trajectory with Sliding Window and Intent Injection to mitigate search drift. They anchor the evidence space while discarding earlier raw interactions, preventing context from being overwhelmed by visual tokens. We train VISOR using a Group Relative Policy Optimization-based Reinforcement Learning (GRPO-based RL) pipeline with state masking and credit assignment tailored for dynamic context reconstruction. Extensive experiments on ViDoSeek, SlideVQA, and MMLongBench demonstrate that VISOR achieves state-of-the-art performance with superior efficiency for long-horizon visual reasoning tasks.

---

## 论文详细总结（自动生成）

以下是对论文《VISOR: Agentic Visual Retrieval-Augmented Generation via Iterative Search and Over-horizon Reasoning》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：视觉检索增强生成（VRAG）允许模型在包含图表、表格的视觉丰富文档中检索并推理。现有的智能体 VRAG 系统通过迭代检索来处理复杂查询，但在长程推理中面临两大瓶颈。
*   **核心问题**：
    1.  **视觉证据稀疏性（Visual Evidence Sparsity）**：关键证据散落在多页中，现有系统通常孤立处理单页，缺乏跨页证据积累；且细粒度的局部信息需要精准的“裁剪与缩放（Crop-and-Zoom）”动作，误用会导致噪声。
    2.  **长程交互中的搜索漂移（Search Drift）**：视觉 Token 数量庞大，多轮交互后上下文窗口迅速饱和，导致早期证据被淹没，智能体注意力偏离原始查询意图。

### 2. 方法论
VISOR 提出了一个统一的单智能体框架，核心技术包括：
*   **结构化证据空间（Evidence Space）**：在推理链（`think` 模块）中显式提取并存储每页的文本摘要（预检摘要和裁剪后摘要），实现跨页证据的持久化积累。
*   **视觉动作评估与纠正机制**：在执行裁剪动作前评估其必要性，减少冗余操作；若裁剪结果无意义，则通过提示词引导智能体回退到之前的推理状态。
*   **动态轨迹与滑动窗口（Dynamic Trajectory with Sliding Window）**：
    *   **固定锚点**：将原始查询和证据空间始终置于上下文顶部。
    *   **滑动窗口**：仅保留最近 $W$ 轮（实验中 $W=2$）的原始交互，丢弃旧的视觉 Token 以控制上下文长度。
*   **意图注入（Intent Injection）**：在每轮环境反馈中重新注入原始查询，防止模型在长程推理中“迷失”。
*   **训练流水线**：
    *   **SFT 阶段**：通过轨迹蒸馏学习基础智能体行为和验证搜索策略。
    *   **RL 阶段**：采用 **GRPO（群体相对策略优化）**，设计了结合答案准确性（LLM 判别）和检索效率（鼓励适度验证，惩罚过度搜索）的奖励函数。

### 3. 实验设计
*   **数据集**：
    *   **SlideVQA**：侧重于幻灯片的多跳问答。
    *   **ViDoSeek**：侧重于大规模视觉文档语料库的检索与推理。
    *   **MMLongBench**：侧重于长上下文文档理解（包含文本、表格、图表等子项）。
*   **对比方法**：
    *   **基础模型**：Vanilla RAG, ReAct。
    *   **多智能体架构**：ViDoRAG, M3RAG。
    *   **微调基准**：Search-R1-VL, VRAG-RL, MMSearch-R1, EVisRAG, R1-Router。
*   **骨干模型**：Qwen2.5-VL-7B 和 3B。

### 4. 资源与算力
*   **硬件环境**：使用了单节点 **8 张 NVIDIA A800 GPU**。
*   **训练数据量**：SFT 阶段使用了约 2,500 条高质量轨迹；RL 阶段使用了 800 条样本进行自生成 rollout。
*   **超参数**：SFT 学习率 1e-5，RL 学习率 1e-6，最大上下文长度 16,384。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流基准上进行了全面测试，涵盖了单跳、多跳、提取、逻辑推理等多个维度。
*   **消融实验**：针对意图注入（II）、证据空间（ES）和滑动窗口（SW）进行了详细的拆解实验，验证了各组件的协同效应。
*   **分析实验**：包括检索行为分析（完整度 vs. 漂移）、推理效率分析（延迟分解）、训练阶段对比以及视觉动作（Bbox）的使用率分析。
*   **客观性**：使用了统一的检索后端（ColQwen2.5）和 LLM 评判器（Qwen-Max），并进行了人类一致性验证，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：VISOR 在所有基准测试中均达到 SOTA，尤其在多跳问题上提升显著（如 SlideVQA 提升约 10%）。
*   **效率提升**：通过滑动窗口机制，VISOR 在保持高准确率的同时，推理延迟低于复杂的多智能体系统，且优于同类迭代式方法。
*   **证据空间的重要性**：消融实验证明，证据空间是解决长程推理的关键，单纯增加上下文长度或强化学习无法完全替代结构化证据管理。
*   **验证搜索策略**：RL 训练使模型学会了在给出答案前进行一次“验证性检索”，显著增强了答案的可靠性。

### 7. 优点
*   **架构优雅**：将复杂的证据管理集成在单智能体循环中，避免了多智能体通信的开销。
*   **上下文友好**：滑动窗口设计有效解决了视觉 RAG 中 Token 爆炸的问题，使长程推理成为可能。
*   **端到端优化**：通过 GRPO 强化学习直接优化检索策略和答案质量，使模型具备了更好的停止时机判断能力。

### 8. 不足与局限
*   **视觉感知瓶颈**：案例分析显示，模型在处理极其复杂的图表（如需要精准匹配图例颜色与饼图扇区）时仍会出错，这受限于骨干模型（7B）本身的视觉理解上限。
*   **判别器依赖**：RL 训练高度依赖于作为 Judge 的大模型，可能存在潜在的评判偏差。
*   **任务泛化**：虽然在文档理解上表现优异，但在需要极高实时交互或极大规模动作空间的场景（如网页实时操作）下的表现尚未验证。

（完）
