---
title: "Faithful GRPO: Improving Visual Spatial Reasoning in Multimodal Language Models via Constrained Policy Optimization"
title_zh: 忠实 GRPO：通过约束策略优化提升多模态语言模型的视觉空间推理能力
authors: "Sai Srinivas Kancheti, Aditya Kanade, Rohit Sinha, Vineeth N Balasubramanian, Tanuja Ganu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08476v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过约束策略优化（强化学习）改进视觉空间推理
tldr: 本研究针对多模态推理模型在强化学习训练中出现的思维链（CoT）逻辑不一致及视觉接地性差的问题，提出了 Faithful GRPO (FGRPO)。该方法在标准 GRPO 基础上，利用拉格朗日对偶上升法引入了逻辑一致性和视觉接地性约束，动态调整优化权重。实验表明，FGRPO 在显著提升推理质量的同时，也提高了空间推理任务的最终准确率，证明了忠实推理对模型性能的促进作用。
selection_source: fresh_fetch
motivation: 现有多模态模型在通过强化学习提升准确率时，往往牺牲了思维链的逻辑一致性及其与视觉证据的关联性。
method: 提出 FGRPO 算法，通过拉格朗日对偶上升法将逻辑一致性和视觉接地性作为约束项整合进 GRPO 的优势计算中。
result: "实验显示 FGRPO 将逻辑不一致率从 24.5% 降至 1.7%，视觉接地得分提升 13%，并超越了标准 GRPO 的准确率。"
conclusion: 通过约束策略优化强制模型进行忠实推理，不仅能改善推理过程的质量，还能有效提升多模态模型的最终任务表现。
---

## 摘要
采用可验证奖励强化学习 (RLVR) 训练的多模态推理模型 (MRM) 在视觉推理基准测试中表现出更高的准确率。然而，我们观察到准确率的提升往往以牺牲推理质量为代价：生成的思维链 (CoT) 轨迹经常与最终答案不一致，且在视觉证据上的接地性 (grounding) 较差。我们在七个具有挑战性的真实世界空间推理基准上系统地研究了这一现象，发现它影响了当代 MRM（如 ViGoRL-Spatial、TreeVGR）以及我们使用标准组相对策略优化 (GRPO) 训练的模型。我们从两个互补的维度刻画 CoT 推理质量：“逻辑一致性”（CoT 是否蕴含最终答案？）和“视觉接地性”（每个推理步骤是否准确描述了图像中的物体、属性和空间关系？）。为解决这一问题，我们提出了 Faithful GRPO (FGRPO)，这是 GRPO 的一种变体，通过拉格朗日对偶上升法将一致性和接地性作为约束条件强制执行。FGRPO 将批次级的一致性和接地性约束纳入组内优势计算中，在优化过程中自适应地调整约束的相对重要性。我们在七个空间数据集上，基于 Qwen2.5-VL-7B 和 3B 骨干网络对 FGRPO 进行了评估。结果表明，FGRPO 显著提高了推理质量，将不一致率从 24.5% 降低到 1.7%，并将视觉接地得分提高了 13%。与简单的 GRPO 相比，它还提高了最终答案的准确率，证明了忠实的推理能够带来更好的答案。

## Abstract
Multimodal reasoning models (MRMs) trained with reinforcement learning with verifiable rewards (RLVR) show improved accuracy on visual reasoning benchmarks. However, we observe that accuracy gains often come at the cost of reasoning quality: generated Chain-of-Thought (CoT) traces are frequently inconsistent with the final answer and poorly grounded in the visual evidence. We systematically study this phenomenon across seven challenging real-world spatial reasoning benchmarks and find that it affects contemporary MRMs such as ViGoRL-Spatial, TreeVGR as well as our own models trained with standard Group Relative Policy Optimization (GRPO). We characterize CoT reasoning quality along two complementary axes: "logical consistency" (does the CoT entail the final answer?) and "visual grounding" (does each reasoning step accurately describe objects, attributes, and spatial relationships in the image?). To address this, we propose Faithful GRPO (FGRPO), a variant of GRPO that enforces consistency and grounding as constraints via Lagrangian dual ascent. FGRPO incorporates batch-level consistency and grounding constraints into the advantage computation within a group, adaptively adjusting the relative importance of constraints during optimization. We evaluate FGRPO on Qwen2.5-VL-7B and 3B backbones across seven spatial datasets. Our results show that FGRPO substantially improves reasoning quality, reducing the inconsistency rate from 24.5% to 1.7% and improving visual grounding scores by +13%. It also improves final answer accuracy over simple GRPO, demonstrating that faithful reasoning enables better answers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Faithful GRPO (FGRPO)** 的新算法，旨在解决多模态大模型在强化学习过程中出现的“推理不忠实”问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然通过可验证奖励的强化学习（RLVR，如 DeepSeek-R1 采用的 GRPO）能显著提升多模态模型在空间推理任务中的准确率，但研究者发现这种提升往往伴随着**推理质量的恶化**。
*   **核心问题**：模型生成的思维链（CoT）经常出现两种失效模式：
    1.  **逻辑不一致性**：推理过程指向答案 A，但最终输出却是答案 B。
    2.  **视觉脱节（Ungroundedness）**：推理步骤中描述了图像中不存在的物体、属性或错误的空间关系。
*   **研究意义**：如果模型仅靠“走捷径”或利用数据偏差获得正确答案，而推理过程错误，那么它在真实世界应用中是不可信的。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将推理的“一致性”和“接地性”视为**硬约束**而非简单的加权奖励项。通过**拉格朗日对偶上升法（Lagrangian Dual Ascent）**，动态调整这些约束在优化过程中的权重。
*   **关键技术细节**：
    *   **三种验证信号**：
        *   **一致性奖励 ($R_C$)**：利用 LLM 裁判判断 CoT 轨迹是否在逻辑上蕴含最终答案。
        *   **语义接地奖励 ($R_S$)**：利用 VLM 裁判逐句审核推理步骤是否符合视觉事实。
        *   **空间接地奖励 ($R_G$)**：通过计算预测边界框与真实区域的 IoU（使用 CIoU 损失）来验证空间定位。
    *   **约束策略优化**：将优化目标设定为：在满足一致性和接地性阈值的前提下，最大化任务准确率。
    *   **解耦优势计算（Decoupled Normalization）**：为了防止不同信号相互抵消，FGRPO 对任务奖励和各个约束信号分别进行组内归一化，然后再通过拉格朗日乘子（$\lambda$）线性组合。
    *   **自适应调整**：拉格朗日乘子会根据当前批次的约束达成情况自动增减，无需人工调参。

### 3. 实验设计
*   **数据集/场景**：涵盖 7 个具有挑战性的真实世界空间推理基准：CVBench-2D/3D、MindCube、MMVP、OmniSpatial、RealWorldQA 和 SAT-Real。
*   **基准模型（Baselines）**：
    *   **开源模型**：Qwen2.5-VL (3B/7B) 原始版本。
    *   **多模态推理模型**：R1-OneVision、Vision-R1、ViGoRL-Spatial、TreeVGR、VL-Rethinker。
    *   **闭源模型**：GPT-4o、GPT-5-nano。
*   **对比实验**：对比了标准 GRPO（仅任务奖励）、加权奖励 GRPO 以及不同约束组合的 FGRPO。

### 4. 资源与算力
*   **算力设备**：
    *   **SFT 阶段**：使用 4 张 NVIDIA A100 (80GB) GPU，训练时长约 12 小时。
    *   **RL 阶段**：使用 8 张 NVIDIA H100 GPU。
*   **模型规模**：针对 Qwen2.5-VL 的 3B 和 7B 两个版本进行了实验。
*   **训练细节**：使用了 bf16 精度，KL 系数为 0.001，每组采样 $G=5$ 个 rollout。

### 5. 实验数量与充分性
*   **实验规模**：论文在 7 个不同的数据集上进行了广泛测试，涵盖了分布内和分布外（OOD）场景。
*   **消融实验**：非常充分。作者对比了“耦合 vs 解耦”优势计算、固定权重 vs 自适应乘子、以及不同约束项对最终性能的影响。
*   **客观性**：为了保证评估的公正性，训练时使用的裁判模型（Qwen3-VL）与评估时使用的裁判模型（GPT-5.4）是分离的，避免了“自圆其说”的偏差。

### 6. 主要结论与发现
*   **性能双增**：FGRPO 不仅显著提升了推理质量（不一致率从 24.5% 降至 1.7%），还反过来**提升了最终答案的准确率**（平均提升约 2%）。
*   **解决奖励作弊**：标准 GRPO 在训练后期准确率上升但推理质量下降（Reward Hacking），而 FGRPO 有效抑制了这一趋势。
*   **忠实性与准确性互补**：实验证明，强制模型进行忠实推理有助于其发现正确的逻辑路径，从而得到更准确的答案。

### 7. 优点
*   **理论严谨**：引入拉格朗日对偶法解决了多目标优化中权重难调的问题。
*   **实用性强**：提出的解耦归一化解决了 GRPO 在处理多奖励信号时容易出现的信号抵消问题。
*   **质量提升显著**：在保持甚至提升准确率的同时，极大地改善了 CoT 的可读性和真实性。

### 8. 不足与局限
*   **裁判模型依赖**：训练过程高度依赖在线 VLM 裁判的打分，这增加了训练的计算开销和潜在的裁判偏见风险。
*   **任务局限性**：目前主要集中在视觉空间推理任务，尚不清楚该方法在更通用的多模态任务（如长视频理解或复杂文档分析）上的表现。
*   **推理成本**：虽然训练后的模型推理速度不变，但训练阶段由于需要多次调用裁判模型，其资源消耗高于标准 GRPO。

（完）
