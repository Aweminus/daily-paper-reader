---
title: "Learning to Search: A Decision-Based Agent for Knowledge-Based Visual Question Answering"
title_zh: 学习搜索：一种用于基于知识的视觉问答的决策型智能体
authors: "Zhuohong Chen, Zhenxian Wu, Yunyao Yu, Hangrui Xu, Zirui Liao, Zhifang Liu, Xiangwen Deng, Pen Jiao, Haoqian Wang"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07146v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 将KB-VQA重新表述为具有决策能力的搜索智能体问题
tldr: 针对知识库视觉问答（KB-VQA）中固定检索流程难以适应多样化问题且检索与推理脱节的问题，本文提出一种基于决策的搜索智能体框架。该方法将KB-VQA建模为多步决策过程，智能体可根据当前状态自主选择回答、图像检索、文本检索或生成描述等操作。通过自动收集推理轨迹并进行微调，该方法在InfoSeek和E-VQA数据集上达到了SOTA性能，显著提升了模型处理长尾知识的能力。
selection_source: fresh_fetch
motivation: 现有的检索增强生成方法采用固定的顺序流程，导致检索与推理分离，难以灵活应对复杂多样的知识性问题。
method: 将KB-VQA重构为多步决策的搜索智能体问题，通过自动生成的推理轨迹微调模型，使其能自主选择检索工具并动态优化查询。
result: 在InfoSeek和E-VQA两个主流基准测试中，该方法均取得了最先进的性能，显著优于之前的基准模型。
conclusion: 将KB-VQA视为动态决策过程能有效增强模型对外部知识的利用效率，证明了搜索智能体框架在处理复杂视觉知识问答中的有效性。
---

## 摘要
基于知识的视觉问答 (KB-VQA) 要求视觉语言模型理解图像并利用外部知识，特别是针对稀有实体和长尾事实。大多数现有的检索增强生成 (RAG) 方法采用固定的流水线，按顺序检索信息、进行过滤，然后生成答案。这种设计使其难以适应多样化的提问类型。此外，它将检索与推理分离，使得模型难以决定何时搜索、如何优化查询或何时停止。结果，检索到的证据往往与问题对齐不佳。为了解决这些局限性，我们将 KB-VQA 重新表述为一个搜索智能体问题，并将解决过程建模为一个多步决策程序。在每一步中，智能体根据其当前的信息状态，从四个动作（回答、图像检索、文本检索和图像描述）中选择其一。我们进一步设计了一个自动化流水线来收集多步轨迹，记录智能体的推理过程、工具使用和中间决策。这些轨迹随后被用作微调的监督信号。在 InfoSeek 和 E-VQA 上的实验表明，我们的方法达到了最先进的性能，持续优于之前的基准模型，并证实了我们框架的有效性。

## Abstract
Knowledge-based visual question answering (KB-VQA) requires vision-language models to understand images and use external knowledge, especially for rare entities and long-tail facts. Most existing retrieval-augmented generation (RAG) methods adopt a fixed pipeline that sequentially retrieves information, filters it, and then produces an answer. Such a design makes it difficult to adapt to diverse question types. Moreover, it separates retrieval from reasoning, making it hard for the model to decide when to search, how to refine queries, or when to stop. As a result, the retrieved evidence is often poorly aligned with the question. To address these limitations, we reformulate KB-VQA as a search-agent problem and model the solving process as a multi-step decision-making procedure. At each step, the agent selects one of four actions-Answer, Image Retrieval, Text Retrieval, and Caption-based on its current information state. We further design an automated pipeline to collect multi-step trajectories that record the agent's reasoning process, tool usage, and intermediate decisions. These trajectories are then used as supervision for fine-tuning. Experiments on InfoSeek and E-VQA demonstrate that our method achieves state-of-the-art performance, consistently outperforming prior baselines and confirming the effectiveness of our framework.

---

## 论文详细总结（自动生成）

### 论文总结：Learning to Search: A Decision-Based Agent for Knowledge-Based Visual Question Answering

#### 1. 核心问题与整体含义
*   **研究背景**：基于知识的视觉问答（KB-VQA）要求模型不仅要理解图像，还要检索外部知识（如维基百科）来回答涉及长尾实体或细粒度事实的问题。
*   **核心问题**：现有的检索增强生成（RAG）方法大多采用**固定的静态流水线**（检索 $\rightarrow$ 过滤 $\rightarrow$ 生成）。这种设计存在三大缺陷：
    1.  **缺乏灵活性**：无法根据问题的难易程度调整检索深度。
    2.  **检索与推理脱节**：模型无法自主决定何时开始搜索、如何优化查询语句或何时停止。
    3.  **语义对齐差**：检索到的证据往往与问题核心需求匹配度不高。
*   **整体含义**：本文将 KB-VQA 任务从“被动消费检索结果”转变为“主动决策搜索过程”，提出了一种基于决策的搜索智能体框架（DBAgent）。

#### 2. 方法论
*   **核心思想**：将 KB-VQA 建模为一个多步决策过程。模型在每一步根据当前信息状态 $s_t$（包含图像、问题及已收集的证据）选择一个动作 $a_t$。
*   **关键技术细节**：
    *   **动作空间（Action Space）**：
        1.  `Answer`：信息足够，直接输出答案。
        2.  `Image Retrieval`：基于图像相似度搜索相关文档。
        3.  `Text Retrieval`：生成文本查询语句进行检索。
        4.  `Caption`：将视觉内容转化为任务导向的描述，辅助生成更精准的查询。
    *   **状态更新**：$s_{t+1} = (s_t, a_t, e_t)$，其中 $e_t$ 是工具返回的证据。
    *   **轨迹构建与训练**：
        *   **失败感知分支**：针对模型无法直接回答的问题，分析其失败原因（是实体未识别还是缺乏事实知识），据此构建不同的搜索路径。
        *   **难度建模**：将轨迹分为简单、中等、困难三个等级，确保训练数据的多样性。
        *   **监督微调（SFT）**：将推理过程（`<think>` 标签内）和决策动作序列化，通过交叉熵损失函数训练模型学习决策策略。

#### 3. 实验设计
*   **数据集**：
    *   **InfoSeek**：包含 1.3M 样本，侧重长尾实体和百科知识。
    *   **E-VQA (Encyclopedic-VQA)**：包含 221K 样本，侧重细粒度分类和属性查找。
*   **Benchmark 与对比方法**：
    *   **零样本 MLLMs**：GPT-4V, LLaVA-v1.5, Qwen2.5-VL-7B 等。
    *   **经典 RAG 模型**：EchoSight, Wiki-LLaVA, RORA-VLM 等。
    *   **增强推理 RAG**：ReflectiVA, VLM-PRF 等。
*   **评价指标**：Exact Match (EM) 用于 InfoSeek；BERT-based Matching (BEM) 用于 E-VQA；同时评估检索召回率。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A800 (80GB) GPU**。
*   **训练细节**：
    *   基座模型：Qwen2.5-VL-7B-Instruct。
    *   最大序列长度：16,384 tokens。
    *   训练时长：在约 200k 条轨迹上训练了 3 个 Epoch。
    *   优化器：启用 bfloat16 精度、梯度检查点（Gradient Checkpointing）和 ZeRO-3 优化。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验对比了超过 15 种基准模型。
    *   **消融实验**：涵盖了检索策略（无检索 vs. 强制检索 vs. 智能体决策）、知识库规模（10k 到 100k 的鲁棒性测试）、Top-k 参数设置等。
    *   **Oracle 分析**：在提供真实知识页面的情况下验证模型的推理上限。
*   **充分性评价**：实验设计非常充分且客观。通过在两个不同侧重点的数据集上验证，并针对长尾知识特有的“噪声干扰”和“搜索深度”问题进行了深入探讨，证明了该方法的稳健性。

#### 6. 主要结论与发现
*   **性能领先**：DBAgent 在 E-VQA 上达到 45.2%，在 InfoSeek 上达到 48.7%，显著超过了之前的 SOTA 模型（约提升 6 个百分点）。
*   **动态决策的价值**：简单的增加检索步数并不总能提升精度，DBAgent 的优势在于能根据当前状态“按需检索”，有效减少了冗余信息带来的噪声。
*   **鲁棒性**：随着外部知识库规模增大（噪声增加），DBAgent 的性能下降幅度远小于传统 RAG 方法，显示出更强的证据筛选能力。

#### 7. 优点
*   **范式创新**：将 KB-VQA 从静态流程转变为动态决策智能体，更符合人类解决复杂问题的逻辑。
*   **自动化数据工程**：设计了一套自动化的轨迹收集流水线，解决了多步推理数据标注难的问题。
*   **多模态协同**：通过 `Caption` 动作巧妙地连接了视觉感知与文本检索，提高了查询生成的质量。

#### 8. 不足与局限
*   **推理延迟**：由于采用多步迭代推理，其推理速度（Latency）明显慢于单次通过（Single-pass）的流水线模型。
*   **轨迹质量依赖**：训练用的轨迹是由教师模型自动生成的，若教师模型在某些极端长尾场景下存在偏见或错误，会影响智能体的学习效果。
*   **动作空间受限**：目前的动作仅限于检索和描述，尚未整合更复杂的工具（如计算器、知识图谱查询或外部 API）。
*   **应用限制**：在实时性要求极高的场景下，该框架的部署可能面临挑战。

（完）
