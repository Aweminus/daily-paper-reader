---
title: "StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation"
title_zh: StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型
authors: "Yiran Shi, Dongqi Guo, Tianchen Zhao, Feng Gao, Liangzhi Shi, Chao Yu, ZhiJian Mo, Qihua Xiao, XiaoShuai Peng, Qingmin Liao, Yu Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28565v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于感知和控制的视觉-语言-动作模型
tldr: 针对视觉-语言-动作（VLA）模型在边缘设备部署时因顺序执行导致的延迟和停顿问题，本文提出了StreamingVLA。该模型通过引入动作流匹配和自适应早期观测机制，实现了观测、生成与执行阶段的异步并行处理。在保持性能的同时，该方法显著提升了推理速度和动作流畅度，将延迟降低了2.4倍，执行停顿减少了6.5倍，为实时机器人控制提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的VLA模型因观测、生成和执行阶段必须顺序进行，导致系统频繁停顿且延迟较高，难以满足实时部署需求。
method: 采用动作流匹配替代动作分块以重叠生成与执行阶段，并设计动作显著性感知的自适应观测机制来重叠执行与观测阶段。
result: 在不损失性能的前提下，StreamingVLA实现了2.4倍的延迟加速，并将执行停顿频率降低了6.5倍。
conclusion: 通过将VLA流程转变为流式并行处理模式，StreamingVLA有效解决了实时机器人控制中的效率与流畅性瓶颈。
---

## 摘要
视觉-语言-动作（VLA）模型在自然语言驱动的感知与控制中表现出卓越的性能。然而，VLA 模型的高计算开销带来了严峻的效率挑战，尤其是在实际部署中资源受限的边缘平台上。由于 VLA 的不同阶段（观测、动作生成和执行）必须按顺序进行，且需等待前一阶段完成，导致系统频繁停顿且延迟较高。针对这一问题，我们进行了系统性分析，识别了实现快速且流畅生成的挑战，并提出通过“流式”方式使 VLA 具备跨阶段异步并行的能力。首先，我们消除了对动作分块（action chunking）的依赖，采用动作流匹配（action flow matching）来学习动作流的轨迹，而非对分块动作进行去噪，从而使动作生成与执行的延迟相互重叠。其次，我们设计了一种动作显著性感知的自适应观测机制，实现了执行与观测延迟的重叠。在不牺牲性能的情况下，StreamingVLA 显著提升了速度和执行流畅度，实现了 2.4 倍的延迟加速，并将执行停顿减少了 6.5 倍。

## Abstract
Vision-language-action (VLA) models have demonstrated exceptional performance in natural language-driven perception and control. However, the high computational cost of VLA models poses significant efficiency challenges, particularly for resource-constrained edge platforms in real-world deployments. However, since different stages of VLA (observation, action generation and execution) must proceed sequentially, and wait for the completion of the preceding stage, the system suffers from frequent halting and high latency. To address this, We conduct a systematic analysis to identify the challenges for fast and fluent generation, and propose enabling VLAs with the ability to asynchronously parallelize across VLA stages in a "streaming" manner. First, we eliminate the reliance on action chunking and adopt action flow matching, which learns the trajectory of action flows rather than denoising chunk-wise actions. It overlaps the latency of action generation and execution. Second, we design an action saliency-aware adaptive observation mechanism, thereby overlapping the latency of execution and observation. Without sacrificing performance, StreamingVLA achieves substantial speedup and improves the fluency of execution. It achieves a 2.4 $\times$ latency speedup and reduces execution halting by 6.5 $\times$.

---

## 论文详细总结（自动生成）

### StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型

#### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型（如 $\pi_0$ 系列）在机器人控制中表现出色，但其推理开销巨大。
*   **核心问题**：现有的 VLA 模型通常采用“动作分块（Action Chunking）”技术，即顺序执行“观测-生成-执行”三个阶段。这种同步流水线导致了严重的**执行停顿（Halting）**和**高延迟**，使得机器人动作不连贯、反应迟慢，难以在资源受限的边缘平台上实时运行。
*   **整体含义**：本文旨在通过“流式（Streaming）”架构，实现 VLA 各阶段的异步并行处理，从而在不损失任务成功率的前提下，大幅提升机器人的动作流畅度和执行速度。

#### 2. 方法论
论文提出了 **StreamingVLA** 框架，包含两个核心技术：
*   **基于状态的动作流匹配（Action Flow Matching, AFM）**：
    *   **核心思想**：放弃传统的“去噪分块动作”模式，改为学习动作流的轨迹。
    *   **技术细节**：将动作建模为对“物理状态”的更新。模型预测的是速度场 $v_\theta$，通过积分更新状态 $x_{t+1} = x_t + v_\theta(x_t, t|o)\Delta t$。
    *   **改进点**：针对大规模 VLA，引入了“动作空间状态”以对齐非线性控制器；并修改了归一化层（移除偏移项），以保持动作累加的加性性质，防止训练崩溃。
    *   **效果**：允许动作“逐个”生成并立即执行，实现了生成与执行阶段的重叠。
*   **动作显著性感知的自适应早期观测（Adaptive Early Observation, AEO）**：
    *   **核心思想**：在当前动作序列尚未执行完时，提前开始下一轮的 VLM 观测处理。
    *   **技术细节**：引入一个轻量级 Transformer 预测器，评估剩余动作对环境观测的影响（动作显著性）。如果预测剩余动作对视觉特征改变较小，则触发早期观测；反之则等待，以避免因环境剧变导致的感知偏差。
    *   **效果**：实现了执行与观测阶段的重叠。

#### 3. 实验设计
*   **数据集/场景**：主要使用 **LIBERO** 仿真基准测试，涵盖四个子任务集：LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程），共 40 个不同任务。
*   **Benchmark**：以 SOTA 模型 **$\pi_{0.5}$** 为基准。
*   **对比方法**：
    *   **RTC (Real-Time Chunking)**：实时分块流策略。
    *   **SmolVLA**：通过提前观测加速的轻量级 VLA。
    *   **VLASH**：通过微调缓解早期观测偏差的方法。
    *   **Temporal Ensembling**：时间集成方法。
*   **真实世界验证**：在 Franka Panda 机器人上进行了“取放物体”任务的实机部署。

#### 4. 资源与算力
*   **GPU 型号与数量**：使用了 **4 台 NVIDIA A800-SXM4-80GB** GPU 进行训练。
*   **训练时长**：
    *   主模型训练：60,000 次迭代，耗时约 **10-20 小时**。
    *   自适应预测器训练：54,000 次迭代，耗时约 **3-4 小时**。
*   **推理环境**：在单张 A800 GPU 上进行评估。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 50 次 Rollout（总计 2000 次测试），提供了统计学上的成功率对比。
*   **消融实验**：系统地验证了动作流匹配（AFM）、归一化修改（NM）、状态对齐（SA）以及不同早期观测（EO）策略的有效性。
*   **充分性评价**：实验设计较为充分，不仅涵盖了仿真环境下的多维度任务，还对比了多种最新的加速方案，并进行了真实世界的部署验证，证明了方法的通用性和实用性。

#### 6. 论文的主要结论与发现
*   **性能保持**：StreamingVLA 在 LIBERO 上的平均成功率为 94.9%，与基准模型 $\pi_{0.5}$（95.1%）几乎持平，性能损失微乎其微。
*   **速度提升**：实现了 **2.4 倍** 的端到端延迟加速（单步动作时间从 74.5ms 降至 31.6ms）。
*   **流畅度提升**：执行停顿时间（Halting Gap）减少了 **6.5 倍**（从 232.3ms 降至 36.0ms），显著改善了机器人动作的连贯性。
*   **自适应的重要性**：相比于盲目的早期观测，基于显著性的自适应策略能有效避免在关键动作（如开门、放手）时产生感知错误。

#### 7. 优点
*   **异步流水线设计**：打破了 VLA 传统的同步执行瓶颈，通过重叠计算与执行时间，最大化了硬件利用率。
*   **状态建模创新**：将动作流匹配引入大规模 VLA，并解决了归一化层破坏加性性质的工程难题。
*   **轻量级插件**：自适应预测器仅增加约 5% 的计算开销，却能显著提升早期观测的鲁棒性。

#### 8. 不足与局限
*   **依赖基础模型**：该方法主要针对基于扩散或流匹配的 VLA 模型，对于纯自回归（Token-based）生成的 VLA 模型适用性未做深入探讨。
*   **预测器开销**：虽然预测器很轻量，但在极低功耗的嵌入式芯片上，额外的视觉特征处理仍可能产生竞争。
*   **环境复杂性**：在动态变化极快的环境中，自适应阈值的设定可能需要更复杂的调优。

（完）
