---
title: "Vero: An Open RL Recipe for General Visual Reasoning"
title_zh: Vero：一种用于通用视觉推理的开源强化学习方案
authors: "Gabriel Sarch, Linrong Cai, Qunzhong Wang, Haoyang Wu, Danqi Chen, Zhuang Liu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04917v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 视觉语言模型通用视觉推理的开放强化学习方案
tldr: Vero 是一套开源的视觉推理强化学习（RL）方案，旨在打破闭源模型的垄断。研究者构建了包含 60 万样本的 Vero-600K 数据集，涵盖图表、科学、空间理解等六大领域，并设计了任务路由奖励机制。Vero 在 30 个基准测试中表现优异，超越了 Qwen3-VL-8B-Thinking 等模型，证明了广泛的数据覆盖是提升视觉推理能力的关键。
selection_source: fresh_fetch
motivation: 现有的高性能视觉语言模型强化学习方案多为闭源，缺乏公开且通用的视觉推理训练配方。
method: 构建了包含 60 万样本的 Vero-600K 数据集，并采用任务路由奖励机制在六大任务类别上进行大规模强化学习训练。
result: Vero 在 VeroEval 的 30 个基准测试中显著提升了基础模型性能，在多项指标上超越了同规模的闭源或半开源模型。
conclusion: 广泛的任务覆盖是视觉推理强化学习扩展的核心驱动力，开源数据和方法能有效提升通用视觉推理能力。
---

## 摘要
构建一个能够胜任图表、科学、空间理解和开放式任务的视觉推理器需要什么？最强大的视觉语言模型（VLM）表明，这种广泛的视觉推理已触手可及，但其背后的方案仍不明确，被锁定在带有非公开数据的私有强化学习（RL）流水线中。我们推出了 Vero，这是一个完全开源的 VLM 系列，在多种视觉推理任务中达到或超过了现有的开源权重模型。我们在六个广泛的任务类别中扩展了 RL 数据和奖励，构建了包含来自 59 个数据集的 60 万个样本的 Vero-600K 数据集，并设计了能够处理异构答案格式的任务路由奖励。Vero 实现了最先进的性能，在我们的 30 个具有挑战性的基准测试集 VeroEval 上，相比四个基础模型平均提升了 3.7-5.5 个百分点。以 Qwen3-VL-8B-Instruct 为起点，Vero 在无需额外私有思考数据的情况下，在 30 个基准测试中的 23 个上超越了 Qwen3-VL-8B-Thinking。当使用相同的基础模型进行训练时，Vero-600K 在各个任务类别上均优于现有的 RL 数据集。系统性的消融实验表明，不同的任务类别会诱发出性质截然不同的推理模式，这些模式在孤立状态下迁移效果较差，这表明广泛的数据覆盖是实现强大 RL 扩展的主要驱动力。所有数据、代码和模型均已发布。

## Abstract
What does it take to build a visual reasoner that works across charts, science, spatial understanding, and open-ended tasks? The strongest vision-language models (VLMs) show such broad visual reasoning is within reach, but the recipe behind them remains unclear, locked behind proprietary reinforcement learning (RL) pipelines with non-public data. We introduce Vero, a family of fully open VLMs that matches or exceeds existing open-weight models across diverse visual reasoning tasks. We scale RL data and rewards across six broad task categories, constructing Vero-600K, a 600K-sample dataset from 59 datasets, and designing task-routed rewards that handle heterogeneous answer formats. Vero achieves state-of-the-art performance, improving over four base models by 3.7-5.5 points on average across VeroEval, our suite of 30 challenging benchmarks. Starting from Qwen3-VL-8B-Instruct, Vero outperforms Qwen3-VL-8B-Thinking on 23 of 30 benchmarks without additional proprietary thinking data. When trained from the same base model, Vero-600K exceeds existing RL datasets across task categories. Systematic ablations reveal that different task categories elicit qualitatively distinct reasoning patterns that transfer poorly in isolation, suggesting that broad data coverage is the primary driver of strong RL scaling. All data, code, and models are released.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Vero**，一套用于提升视觉语言模型（VLM）通用视觉推理能力的开源强化学习（RL）方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何构建一个在图表分析、科学推理、空间理解及开放式任务中均具备强大能力的通用视觉推理器？
*   **研究动机**：
    *   **闭源垄断**：目前最强的视觉推理模型（如 GPT-5, Qwen3-VL）依赖于私有的 RL 流水线和非公开数据，其训练细节（配方）不透明。
    *   **开源局限**：现有的开源 RL 尝试（如 OpenMMReasoner）多局限于数学等狭窄领域，缺乏在多样化视觉任务上的通用性。
    *   **任务干扰**：在异构视觉任务上进行 RL 训练容易出现任务间的负迁移和优化不平衡。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：通过**大规模多样化数据覆盖**配合**任务路由奖励机制**，在单阶段 RL 训练中实现通用的视觉推理能力。
*   **关键技术细节**：
    *   **Vero-600K 数据集**：从 59 个原始数据集中筛选出 60 万个样本，划分为六大类别：STEM、空间与动作、知识与识别、图表与 OCR、定位/计数/搜索、描述与指令遵循。
    *   **数据清洗流水线**：包括启发式筛选、人工质量控制、基于大模型（Qwen3-235B）的问题过滤（剔除歧义和不可验证问题）以及答案标准化。
    *   **RL 算法 (GSPO)**：采用组序列策略优化（Group Sequence Policy Optimization），这是 GRPO 的变体，通过序列级重要性采样比率来提高训练稳定性。
    *   **任务路由奖励 (Task-routed Rewards)**：针对不同任务设计了 10 种奖励函数，包括字符串匹配、数学验证（Math-verify）、边界框 IoU（用于定位）、Web 动作匹配以及针对开放式任务的 **LLM-as-judge**。
    *   **格式与惩罚**：强制模型生成 `<think>...</think><answer>...</answer>` 结构，并引入长度惩罚以防止模型过度冗长。

### 3. 实验设计
*   **数据集与场景**：涵盖了从医学影像、几何证明到 UI 导航、航拍定位等 59 个数据集。
*   **Benchmark (VeroEval)**：构建了包含 30 个挑战性基准测试的评估套件，覆盖上述六大类别。
*   **对比方法**：
    *   **基础模型**：Qwen2.5-VL-7B, Qwen3-VL-8B-Instruct, Molmo2-O-7B。
    *   **推理模型**：Qwen3-VL-8B-Thinking, MiMo-VL-7B-RL。
    *   **开源 RL 方案**：LLaVA-OV-1.5-RL, VL-Rethinker。
    *   **闭源模型**：GPT-5 Nano。

### 4. 资源与算力
*   **硬件设备**：使用了 **8 张 NVIDIA H100 或 H200 GPU**。
*   **训练配置**：
    *   训练步数为 2000 步。
    *   上下文长度根据模型不同设定在 24k 到 36k 之间。
    *   使用了 FSDP2 分布式策略和 VeRL 框架。
*   **精度**：Qwen 系列使用 fp16 以保证稳定性，MiMo-VL 使用 bf16。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的跨模型验证（在 4 个基础模型上均有提升）和 30 个基准测试的详尽评估。
*   **消融实验**：非常充分。包括：
    *   **数据过滤影响**：验证了问题过滤和答案标准化的有效性。
    *   **混合策略**：对比了均匀分布、难度加权、长度加权等多种数据混合方案。
    *   **跨任务迁移**：研究了单任务训练对其他任务的负面影响，证明了多样性的必要性。
    *   **奖励设计**：对比了单一数学验证奖励与任务路由奖励的优劣。
*   **客观性**：通过在相同基础模型上运行不同 RL 数据集（如对比 OpenMMReasoner 数据集）进行公平竞争，结果具有说服力。

### 6. 主要结论与发现
*   **数据多样性是核心**：广泛的任务覆盖是 RL 扩展的主要驱动力。单领域训练（如只练数学）会导致模型在其他视觉任务上性能大幅下降。
*   **性能卓越**：Vero-Qwen3I-8B 在无需私有思考数据的情况下，在 23/30 个基准测试上超越了官方的 Qwen3-VL-Thinking。
*   **推理模式差异**：不同任务会诱发不同的“思考”行为。例如，STEM 任务会引发更多的“回溯（backtracking）”，而定位任务则倾向于抑制内省行为以进行直接视觉搜索。
*   **均匀混合最优**：在多任务 RL 中，简单的类别均匀采样比复杂的难度加权采样效果更好。

### 7. 优点与亮点
*   **完全开源**：提供了从原始数据处理、RL 代码到模型权重的全套方案，极具学术贡献。
*   **任务路由奖励**：解决了 VLM 在 RL 过程中处理异构答案格式（坐标、JSON、自由文本）的难题。
*   **深入的行为分析**：不仅关注分数，还利用认知框架分析了模型推理迹象（Reasoning Traces）中的高阶认知行为和技能分布。
*   **视觉聊天质量保护**：通过引入开放式指令遵循数据和 LLM 裁判，成功解决了 RL 训练容易导致模型回复变得生硬、简短的问题。

### 8. 不足与局限
*   **任务覆盖限制**：目前尚未包含视频理解和多轮对话任务。
*   **模型规模**：主要集中在 7B-9B 规模的模型，尚未在超大规模模型（如 70B+）上验证该配方的可扩展性。
*   **因果机制不明**：虽然观察到了推理行为的变化，但尚未从因果层面完全解释特定推理行为是如何直接导致准确率提升的。
*   **奖励黑盒风险**：尽管使用了 LLM 裁判，但仍存在模型针对裁判进行“奖励作弊（Reward Hacking）”的风险（虽然文中尝试通过 Prompt 缓解）。

（完）
