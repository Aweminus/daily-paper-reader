---
title: "V-Reflection: Transforming MLLMs from Passive Observers to Active Interrogators"
title_zh: V-Reflection：将多模态大语言模型从被动观察者转变为主动询问者
authors: "Jiazhou Zhou, Yucheng Chen, Hongyang Li, Qing Jiang, Hu Zhou, Ying-Cong Chen, Lei Zhang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.03307v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 用于视觉反思的主动询问智能体
tldr: 针对多模态大模型在细粒度任务中易产生感知幻觉的问题，本文提出 V-Reflection 框架。该框架通过“先思后看”机制，将模型从被动观察者转变为主动询问者，利用推理中的隐藏状态作为动态探针来提取视觉证据。通过两阶段蒸馏策略，模型在不增加推理负担的前提下，实现了对任务关键视觉信息的自主定位，显著提升了感知准确性。
selection_source: fresh_fetch
motivation: 现有模型将视觉输入视为静态信息，无法在推理过程中根据演变的状态动态地重新审视和验证视觉细节。
method: 采用两阶段蒸馏策略，通过框引导压缩模块建立空间基准，并利用动态自回归压缩模块将隐藏状态映射为主动询问视觉特征的探针。
result: 在六个感知密集型基准测试中显著提升了性能，有效减少了感知幻觉，且推理阶段保持了纯端到端的自回归效率。
conclusion: V-Reflection 证明了通过隐藏状态进行动态视觉反思能有效增强模型的细粒度感知能力，使模型具备了主动寻找关键证据的能力。
---

## 摘要
多模态大语言模型（MLLMs）已取得显著成功，但在细粒度任务中仍容易产生与感知相关的幻觉。这种脆弱性源于一个根本限制：它们的推理很大程度上局限于语言领域，将视觉输入视为静态的、与推理无关的前导内容，而非动态参与者。因此，目前的模型表现为被动观察者，无法重新审视视觉细节以将其不断演进的推理状态落地。为克服这一问题，我们提出了 V-Reflection 框架，通过一种“先思后看”的视觉反思机制，将 MLLM 转变为主动询问者。在推理过程中，潜在状态充当动态探针，主动询问视觉特征空间，为每个推理步骤寻找任务关键证据。我们的方法采用两阶段蒸馏策略。首先，框引导压缩（BCM）模块通过显式的空间落地建立稳定的像素到潜在目标的映射。接着，动态自回归压缩（DAC）模块将模型的隐藏状态映射为动态探针，以询问全局视觉特征图。通过将 BCM 教师模型的空间专业知识蒸馏到 DAC 学生模型中，V-Reflection 内化了定位任务关键证据的能力。在推理期间，这两个模块均保持完全不激活状态，在潜在空间中保持纯端到端的自回归解码，从而实现最优效率。大量实验证明了 V-Reflection 在六个感知密集型基准测试中的有效性，显著缩小了细粒度感知差距。可视化结果证实，潜在推理能够自主定位任务关键的视觉证据。

## Abstract
Multimodal Large Language Models (MLLMs) have achieved remarkable success, yet they remain prone to perception-related hallucinations in fine-grained tasks. This vulnerability arises from a fundamental limitation: their reasoning is largely restricted to the language domain, treating visual input as a static, reasoning-agnostic preamble rather than a dynamic participant. Consequently, current models act as passive observers, unable to re-examine visual details to ground their evolving reasoning states. To overcome this, we propose V-Reflection, a framework that transforms the MLLM into an active interrogator through a "think-then-look" visual reflection mechanism. During reasoning, latent states function as dynamic probes that actively interrogate the visual feature space, grounding each reasoning step for task-critical evidence. Our approach employs a two-stage distillation strategy. First, the Box-Guided Compression (BCM) module establishes stable pixel-to-latent targets through explicit spatial grounding. Next, a Dynamic Autoregressive Compression (DAC) module maps the model's hidden states into dynamic probes that interrogate the global visual feature map. By distilling the spatial expertise of the BCM teacher into the DAC student, V-Reflection internalizes the ability to localize task-critical evidence. During inference, both modules remain entirely inactive, maintaining a purely end-to-end autoregressive decoding in the latent space with optimal efficiency. Extensive experiments demonstrate the effectiveness of our V-Reflection across six perception-intensive benchmarks, significantly narrowing the fine-grained perception gap. Visualizations confirm that latent reasoning autonomously localizes task-critical visual evidence.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **V-Reflection** 的框架，旨在将多模态大语言模型（MLLM）从“被动观察者”转变为“主动询问者”，以解决细粒度感知中的幻觉问题。

### 1. 核心问题与整体含义（研究动机和背景）
当前 MLLM（如 LLaVA 风格模型）在处理细粒度视觉任务时容易产生感知幻觉。其根本原因在于：
*   **语言中心化推理：** 模型将视觉输入视为静态的、推理前置的背景，一旦进入自回归推理阶段，就无法根据当前的推理状态重新审视视觉细节。
*   **被动观察：** 模型推理主要在语言域进行，视觉特征是静态压缩的，导致模型倾向于依赖语言先验（如“手套”通常是“棉质”的）而非实际视觉证据（如图片中显示的“橡胶”材质）。
*   **研究动机：** 赋予模型“先思后看”（Think-then-look）的能力，让模型在推理过程中能根据演化的潜在状态（Latent States）主动去视觉特征空间中寻找关键证据。

### 2. 方法论：核心思想与技术细节
V-Reflection 引入了一种视觉自反思机制，通过两阶段蒸馏策略实现：

*   **核心思想：** 利用 LLM 的隐藏状态作为“动态探针”，在推理过程中主动询问全局视觉特征图。
*   **阶段 1：框引导压缩模块（BCM - 显式落地预热）：**
    *   作为“教师”模块，利用目标检测框（Bounding Box）先验，通过 RoI-Align 提取局部特征。
    *   使用可学习查询（Queries）将局部特征压缩为教师潜在标记（$Z_T$）。
    *   采用**随机解耦对齐策略**（Stochastic Decoupled Alignment）：通过伯努利分布随机切换梯度流，防止 LLM 隐藏状态与视觉潜在空间在对齐时发生表征坍缩。
*   **阶段 2：动态自回归压缩模块（DAC - 视觉潜在蒸馏）：**
    *   作为“学生”模块，将 LLM 的隐藏状态 $H$ 投影为动态探针 $Q_{dyn}$。
    *   这些探针在全局特征图上进行交叉注意力操作，生成学生潜在标记 $Z_S$。
    *   **双重蒸馏目标：** 最小化 $Z_S$ 与 $Z_T$ 之间的 MSE 损失（语义对齐），以及学生全局注意力图与教师局部注意力图之间的 KL 散度（空间先验转移）。
*   **推理阶段：** BCM 和 DAC 模块均不激活。模型通过内化的能力，在潜在空间中进行纯端到端的自回归解码，隐藏状态自动执行视觉搜索。

### 3. 实验设计
*   **数据集/场景：** 涵盖感知鲁棒性、细粒度视觉搜索、高分辨率理解和认知推理。
*   **Benchmark：** 
    *   感知与认知：MMVP, BLINK, V* Bench。
    *   高分辨率：HRBench-4K, HRBench-8K, MME-Real-Lite。
*   **对比方法：** 包括闭源模型 GPT-4o，以及开源模型 Qwen2.5-VL-7B（基座）、InternVL3、Deepeyes、LVR、Vision-R1 等。

### 4. 资源与算力
*   **硬件：** 使用了 8 张 NVIDIA A800 GPU 集群。
*   **训练时长：** 
    *   阶段 1：约 24 小时（2500 步）。
    *   阶段 2：约 12 小时（1250 步）。
*   **精度：** 采用 BF16 精度训练，使用了 DeepSpeed ZeRO-3 优化。

### 5. 实验数量与充分性
*   **实验规模：** 在 6 个主流感知密集型基准测试上进行了评估。
*   **消融实验：** 非常充分，涵盖了模块有效性（BCM vs DAC）、损失函数设计（LBCM, LDAC）、训练阶段必要性、动态探针来源（随机 vs 静态 vs 动态）以及潜在推理步数 $S$ 的影响。
*   **客观性：** 实验对比了同等参数规模（7B/8B）的最先进模型，并提供了详细的可视化分析（注意力图），直观展示了模型如何从局部关注扩展到全局上下文理解。

### 6. 主要结论与发现
*   **性能飞跃：** V-Reflection 在 MMVP 上达到 72.3%，显著超过 GPT-4o (58.33%) 和基座模型 Qwen2.5-VL (66.7%)。
*   **高分辨率优势：** 在 HRBench-4K 的跨实例感知（FCP）子集上提升了 6.7%，证明了其处理复杂空间布局的能力。
*   **内化能力：** 证明了模型可以不依赖外部工具（如裁剪、OCR API），仅通过内部潜在状态实现精准的视觉定位。
*   **效率保持：** 推理时无需额外架构开销，仅增加极少量的 KV 缓存延迟。

### 7. 优点
*   **机制创新：** 提出了“主动询问”范式，打破了 MLLM 传统的被动视觉处理模式。
*   **推理高效：** 训练时的复杂模块在推理时完全“隐身”，实现了零参数/计算量增加的视觉反思。
*   **即插即用：** 这种两阶段蒸馏框架可以推广到其他自回归多模态架构中。
*   **可视化支撑：** 提供了丰富的注意力图对比，清晰展示了模型如何摆脱 Bbox 限制并捕获全局相关性。

### 8. 不足与局限
*   **潜在步数限制：** 实验发现潜在推理步数 $S$ 在超过 8 步后收益递减甚至下降，说明模型在长时间潜在推理上的稳定性仍有待提升。
*   **训练依赖：** 第一阶段仍需要高质量的 Bbox 标注数据作为教师信号，这在某些极度稀缺的领域可能受限。
*   **动态场景：** 目前主要针对静态图像，尚未在视频或具身智能（Embodied AI）等更复杂的动态环境中验证。

（完）
