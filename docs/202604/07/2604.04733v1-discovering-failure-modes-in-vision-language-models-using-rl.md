---
title: Discovering Failure Modes in Vision-Language Models using RL
title_zh: 利用强化学习发现视觉语言模型中的失效模式
authors: "Kanishk Jain, Qian Yang, Shravan Nayak, Parisa Kordjamshidi, Nishanth Anand, Aishwarya Agrawal"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04733v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 基于强化学习的框架，用于发现视觉语言推理中的失效模式
tldr: 视觉语言模型（VLM）在计数和空间推理等简单任务上常犯错，而传统人工识别弱点的方法成本高且存在偏差。本文提出一种基于强化学习（RL）的自动化框架，通过训练提问者代理自适应生成查询，诱导候选模型产生错误回答。该方法能深入挖掘细粒度视觉细节，成功发现了36种新型失败模式，为全面理解和提升VLM的鲁棒性提供了高效、可扩展的工具。
selection_source: fresh_fetch
motivation: 传统人工识别VLM弱点的方法效率低下且受主观偏差影响，难以全面发现模型在细粒度视觉理解上的漏洞。
method: 提出一种强化学习框架，利用提问者代理根据候选模型的反馈自适应生成复杂查询，以自动诱导模型出错。
result: 该框架成功识别出36种VLM难以处理的新型失败模式，并证明了其在不同模型组合上的广泛适用性。
conclusion: 自动化RL框架能有效且无偏地发现VLM的视觉盲点，是评估和增强多模态模型性能的重要手段。
---

## 摘要
视觉语言模型（VLM）尽管在多模态基准测试中取得了强劲表现，但经常误解人类可以毫不费力识别的简单视觉概念，例如计数、空间推理和视角理解。先前的研究通过手动方式识别这些弱点，并发现它们通常源于特定技能的缺陷。然而，这种手动工作成本高、不可扩展，且容易受到人类偏见的影响，往往为了关注显著对象而忽略细微细节，导致对模型脆弱性的理解不完整。为了解决这些局限性，我们提出了一个基于强化学习（RL）的框架，在无需人工干预的情况下，自动发现给定数据分布上任何候选 VLM 的失效模式或盲点。我们的框架训练了一个提问者智能体，该智能体根据候选 VLM 的响应自适应地生成查询，以诱导其给出错误答案。随着训练的进行，我们的方法通过关注细粒度的视觉细节和不同的技能组合来增加问题的复杂性，从而识别出 VLM 难以应对的 36 种新型失效模式。我们通过展示该框架在各种模型组合中的泛化能力，证明了其广泛的适用性。

## Abstract
Vision-language Models (VLMs), despite achieving strong performance on multimodal benchmarks, often misinterpret straightforward visual concepts that humans identify effortlessly, such as counting, spatial reasoning, and viewpoint understanding. Previous studies manually identified these weaknesses and found that they often stem from deficits in specific skills. However, such manual efforts are costly, unscalable, and subject to human bias, which often overlooks subtle details in favor of salient objects, resulting in an incomplete understanding of a model's vulnerabilities. To address these limitations, we propose a Reinforcement Learning (RL)-based framework to automatically discover the failure modes or blind spots of any candidate VLM on a given data distribution without human intervention. Our framework trains a questioner agent that adaptively generates queries based on the candidate VLM's responses to elicit incorrect answers. Our approach increases question complexity by focusing on fine-grained visual details and distinct skill compositions as training progresses, consequently identifying 36 novel failure modes in which VLMs struggle. We demonstrate the broad applicability of our framework by showcasing its generalizability across various model combinations.

---

## 论文详细总结（自动生成）

### 视觉语言模型（VLM）失效模式自动发现框架：基于强化学习的研究总结

#### 1. 论文的核心问题与整体含义
尽管视觉语言模型（VLM）在多模态任务上表现优异，但在处理计数、空间推理和视角理解等基础视觉概念时，常会出现人类难以理解的低级错误。现有的失效模式（Failure Modes）识别主要依赖人工设计基准测试，这存在**成本高、不可扩展、且受人类认知偏差影响**（倾向于关注显著物体而忽略细微细节）的问题。本文旨在开发一种**全自动、自适应的强化学习（RL）框架**，通过训练一个“提问者”智能体，针对特定的候选模型自动挖掘其视觉盲点。

#### 2. 论文提出的方法论
论文提出了一个由三个核心组件构成的闭环 RL 框架：
*   **核心思想**：将失效模式发现建模为 RL 问题。提问者（Questioner）通过与候选模型（Answerer）交互，学习生成能诱导其出错的、高质量且多样化的问题。
*   **关键组件**：
    *   **提问者 (Questioner)**：基于 VLM 的 RL 智能体，使用 **GRPO（组相对策略优化）** 算法进行训练。它根据图像生成一系列问题。
    *   **回答者 (Answerer)**：目标候选 VLM，参数冻结，负责回答提问者的问题。
    *   **验证者 (Verifier)**：高性能推理 VLM（如 Qwen3-VL-30B-Thinking），负责评估问题的有效性（语法、视觉关联性等）和回答的正确性，并提供奖励信号。
*   **奖励函数 ($R_Q$)**：
    $$R_Q = \Delta_Q \cdot V_Q \cdot (1 - C_Q) - P_Q$$
    其中：$V_Q$ 为有效性（是否合法问题），$C_Q$ 为正确性（模型答对则奖励为0），$\Delta_Q$ 为多样性（包含语义和词法多样性），$P_Q$ 为对无效问题的惩罚。
*   **失效分类流水线**：提出了一种四阶段自动化分类法（原子技能识别 $\rightarrow$ 主题建模 $\rightarrow$ 技能提取 $\rightarrow$ 元技能识别），用于系统化梳理发现的失效模式。

#### 3. 实验设计
*   **数据集**：从 **COCO** 训练集中随机抽取 1000 张图像作为探测分布。
*   **模型设置**：
    *   提问者：Qwen2.5-VL (3B/7B)。
    *   回答者（目标模型）：Qwen2.5-VL (3B/7B)、Qwen3-VL-8B。
    *   验证者：Qwen3-VL-30B-Thinking。
*   **对比基线 (Baselines)**：
    1.  **Zero-Shot**：直接提示静态提问者。
    2.  **ConMe**：基于上下文学习的启发式提示方法。
    3.  **SFT**：在现有失效数据集上进行监督微调。
    4.  **Expert Iteration**：通过拒绝采样和微调进行迭代优化。
    5.  **RL+SFT**：结合 SFT 预训练和 RL 优化。
*   **评估指标**：失效发现率 (FDR)、问题有效率 (QVR)、语义/词法多样性 (SD/LD)、技能覆盖范围等。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。但考虑到使用了 GRPO 算法和 3B/7B 规模的模型进行 RL 训练，通常需要多张 A100 或 H100 级别的 GPU 支持。

#### 5. 实验数量与充分性
*   **实验规模**：在 1000 张图像上进行了大规模探测，生成了数万个问题。
*   **消融实验**：非常充分。作者对奖励函数的各个组件（语义多样性、词法多样性、惩罚项）、每张图的问题数量、前缀长度等关键超参数进行了详细消融。
*   **跨模型验证**：测试了不同规模（3B 到 8B）和不同家族模型作为提问者和回答者的组合，证明了框架的通用性。
*   **客观性**：引入了 **Gemini 1.5 Flash** 作为第三方独立评估器，并进行了 **Amazon Mechanical Turk 人类验证**（一致性达 75%-80%），确保了自动化评估的公正性。

#### 6. 论文的主要结论与发现
*   **RL 显著优于静态方法**：RL 框架的失效发现率（FDR）比最强基线高出约 13%，且生成的问题更具复杂性和细粒度。
*   **发现新型失效模式**：成功识别出 **36 种全新的失效模式**（如威胁评估、路径追踪、显著性检测等），这些是传统人工基准测试未覆盖的。
*   **自适应课程效应**：RL 智能体在训练过程中表现出从简单到复杂的演进，后期生成的问题涉及更多技能组合（Skill Composition）。
*   **模型特异性与普遍性**：某些错误（如否定处理）随模型规模增大而消失，而某些错误（如解剖学识别、排除推理）在不同规模模型中普遍存在。

#### 7. 优点
*   **自动化与可扩展性**：摆脱了人工标注的束缚，能够针对任何新模型快速生成定制化的“压力测试”集。
*   **多样性控制**：通过在奖励函数中引入词法和语义惩罚，有效防止了 RL 训练中常见的“奖励作弊”（即反复问同一个容易出错的问题）。
*   **细粒度分析**：提出的四阶段分类流水线为理解 VLM 的认知盲点提供了极具价值的工具。

#### 8. 不足与局限
*   **交互形式单一**：目前仅限于单图、单轮对话，尚未扩展到多轮对话或视频理解的失效发现。
*   **计算成本**：虽然节省了人力，但 RL 训练和高性能 VLM 验证者的调用需要显著的计算资源。
*   **闭环修复缺失**：论文重点在于“发现”失效，尚未深入探讨如何利用这些发现的失效数据来闭环“修复”或增强模型。
*   **潜在偏差**：验证者（Verifier）本身也是 VLM，可能存在自身的认知偏差，从而影响奖励信号的绝对准确性。

（完）
