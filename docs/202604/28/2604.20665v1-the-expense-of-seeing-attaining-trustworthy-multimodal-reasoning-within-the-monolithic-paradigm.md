---
title: "The Expense of Seeing: Attaining Trustworthy Multimodal Reasoning Within the Monolithic Paradigm"
title_zh: 视觉的代价：在单体范式下实现可信的多模态推理
authors: "Karan Goyal, Dikshant Kukreja"
date: 2026-04-22
pdf: "https://arxiv.org/pdf/2604.20665v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 视觉语言模型中可靠的多模态推理
tldr: 本研究挑战了当前视觉语言模型（VLM）能够忠实合成多模态数据的假设，指出模型存在“功能性盲视”，即过度依赖语言先验而非视觉输入。作者提出了一种基于信息论的“模态转换协议”，通过翻译语义负载而非消融数据，定义了ToS、CoS和FoS三项新指标及语义充分性准则（SSC）。研究揭示了多模态缩放的散度定律，即语言能力越强，视觉瓶颈惩罚越大，为构建真正具备多模态推理能力的AI系统提供了理论基础。
selection_source: fresh_fetch
motivation: 揭示当前主流VLM架构中存在的“功能性盲视”问题，即模型倾向于利用语言先验绕过视觉表征瓶颈，导致推理缺乏可信度。
method: 提出“模态转换协议”，通过量化语义负载的转换而非简单的数据消融，引入ToS、CoS、FoS指标及语义充分性准则（SSC）。
result: 发现了多模态缩放的散度定律，证明随着语言引擎推理能力的提升，视觉知识瓶颈带来的数学惩罚反而会矛盾地增加。
conclusion: 呼吁放弃对虚假“多模态增益”的追求，将SSC从被动诊断转为主动架构设计准则，以实现真正可信的多模态推理。
---

## 摘要
视觉语言模型（VLM）的迅速普及被广泛誉为统一多模态知识发现的曙光，但其基础建立在一个危险且未经质疑的公理之上：即当前的 VLM 能够忠实地合成多模态数据。我们认为事实并非如此。相反，在主流的“视觉编码器-投影器-大语言模型”（Vision Encoder-Projector-LLM）范式下，潜藏着深刻的可信度危机。最先进的模型并非从视觉输入中提取有根据的知识，而是经常表现出“功能性盲视”，即利用强大的语言先验来绕过严重的视觉表示瓶颈。在这项工作中，我们挑战了传统的多模态评估方法，这些方法依赖于数据消融或创建新数据集，因此致命地将数据集偏差与架构能力的不足混为一谈。我们提出了一种激进的、基于信息论的新方法：模态转换协议（Modality Translation Protocol），旨在量化揭示“视觉的代价”。通过转换语义负载而非消融它们，我们制定了三个新颖的指标——视觉的通行费（ToS）、视觉的诅咒（CoS）和视觉的谬误（FoS），并最终形成了语义充分性准则（SSC）。此外，我们提出了一个具有启发性的“多模态缩放发散定律”，假设随着底层语言引擎扩展到前所未有的推理能力，视觉知识瓶颈的数学惩罚反而会矛盾地增加。我们呼吁 KDD 社区放弃对“多模态增益”的虚幻追求。通过将 SSC 从被动的诊断约束提升为主动的架构蓝图，我们为迫使下一代 AI 系统真正“看见”数据并实现真正的多模态推理提供了严谨、可信的基础。

## Abstract
The rapid proliferation of Vision-Language Models (VLMs) is widely celebrated as the dawn of unified multimodal knowledge discovery but its foundation operates on a dangerous, unquestioned axiom: that current VLMs faithfully synthesise multimodal data. We argue they do not. Instead, a profound crisis of trustworthiness underlies the dominant Vision Encoder-Projector-LLM paradigm. Rather than extracting grounded knowledge from visual inputs, state-of-the-art models frequently exhibit functional blindness, i.e., exploiting strong language priors to bypass severe visual representation bottlenecks. In this work, we challenge the conventional methodology of multimodal evaluation, which relies on data ablation or new dataset creation and therefore fatally conflates dataset biases with architectural incapacity. We propose a radical, information-theoretic departure: the Modality Translation Protocol, designed to quantifiably unmask the Expense of Seeing. By translating semantic payloads rather than ablating them, we formulate three novel metrics -- the Toll (ToS), Curse (CoS), and Fallacy (FoS) of Seeing -- culminating in the Semantic Sufficiency Criterion (SSC). Furthermore, we posit a provocative Divergence Law of Multimodal Scaling, hypothesising that as the underlying language engines scale to unprecedented reasoning capabilities, the mathematical penalty of the visual knowledge bottleneck paradoxically increases. We challenge the KDD community to abandon the illusory pursuit of "multimodal gain". By elevating the SSC from a passive diagnostic constraint to an active architectural blueprint, we provide the rigorous, trustworthy foundation required to force the next generation of AI systems to truly see the data, achieving true multimodal reasoning.

---

## 论文详细总结（自动生成）

这篇论文对当前视觉语言模型（VLM）的评估范式提出了深刻挑战，并提出了一套全新的理论框架来衡量多模态推理的真实性。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉语言模型（VLM）是否存在“功能性盲视”？即模型是否真的在“看”图，还是仅仅利用强大的语言先验（LLM 背景知识）在进行统计学猜测（类似“聪明汉斯”效应）。
*   **研究动机**：作者认为现有的评估方法（如“多模态增益” $MG$）依赖于数据消融（删除图像看性能下降多少），这混淆了数据集偏差与模型架构本身的缺陷。
*   **背景**：随着 VLM 进入医疗、金融等高风险领域，如果模型只是在“猜”而不是在“看”，将导致严重的决策风险。

### 2. 论文提出的方法论：模态转换协议（Modality Translation Protocol）
作者提出不再通过“删除信息”来测试，而是通过“翻译模态”来保持语义等价，从而隔离架构瓶颈。
*   **核心思想**：定义三种输入状态来评估模型性能 $S$：
    1.  **$S_{Full}$ (标准 VLM)**：同时输入图像 $V$ 和文本 $T$。
    2.  **$S_{SymT}$ (符号化文本天花板)**：将图像内容转化为完美的文本描述，与原问题 $T$ 一起输入。这代表了模型推理能力的上限。
    3.  **$S_{SymV}$ (符号化视觉)**：将文本问题 $T$ 渲染进图像中，强迫模型仅通过视觉路径读取所有信息。
*   **关键指标**：
    *   **ToS (视觉的通行费)**：$S_{SymT} - S_{Full}$。衡量视觉模态相对于文本模态带来的性能损失。
    *   **CoS (视觉的诅咒)**：$S_{SymT} - S_{SymV}$。衡量模型在处理相同信息的不同模态时是否存在不对称偏见。
    *   **FoS (视觉的谬误)**：$S_{Full} - S_{SymV}$。正值代表视觉编码器（如 ViT）能力不足；负值代表跨模态投影头（Projector）融合失败。
*   **SSC (语义充分性准则)**：要求上述指标均趋近于 0，作为实现可信多模态推理的数学判定标准。

### 3. 实验设计
论文通过三个高风险的 KDD（知识发现与数据挖掘）案例研究来展示该协议的应用：
*   **案例 1：金融时间序列挖掘**：分析 K 线图预测突破。对比“看图预测”与“读取 OHLC 表格数据预测”。
*   **案例 2：医疗诊断**：分析胸部 X 光片。检测模型是基于视觉特征诊断，还是被“患者有 30 年烟龄”等文本先验误导。
*   **案例 3：药物研发的分子图挖掘**：分析 2D 分子结构拓扑。
*   **对比基准**：论文挑战了 MMStar、MATHVERSE 等现有基准，指出它们依赖消融实验的局限性。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该论文更偏向于**理论框架与方法论的提出**（Position Paper），旨在为社区提供评估准则和未来架构设计的蓝图，而非单纯的实验报告。

### 5. 实验数量与充分性
*   **实验规模**：论文主要通过逻辑推导和针对性案例（Case Studies）来论证其提出的“多模态缩放发散定律”（Divergence Law of Multimodal Scaling）。
*   **充分性评价**：从理论构建角度看，实验逻辑严密，成功揭示了模型规模扩大时，视觉瓶颈反而加剧的悖论。但由于缺乏在大规模开源模型（如 GPT-4o, Claude 3.5）上的广泛实测数据支撑，其普适性仍待社区进一步验证。

### 6. 论文的主要结论与发现
*   **多模态缩放发散定律**：随着 LLM 后端规模扩大，其推理能力（$S_{SymT}$）提升极快，但视觉投影头的带宽限制了其获取视觉信息的能力，导致“视觉通行费”（ToS）随模型规模增大而矛盾地增加。
*   **规模不是万能药**：单纯增加参数量无法解决多模态对齐问题，反而可能加剧模型对语言先验的依赖，掩盖视觉能力的缺失。
*   **架构转型**：呼吁将 SSC 从被动的诊断工具转变为主动的训练目标（如作为损失函数的一部分）。

### 7. 优点
*   **视角独特**：首次提出“视觉是一种代价”的观点，挑战了“多模态必然带来增益”的盲目乐观。
*   **诊断精准**：通过 FoS 指标的正负号，能够精确定位故障是发生在视觉编码阶段还是模态融合阶段。
*   **数据集无关性**：该评估协议不依赖特定的“视觉必需”数据集，可应用于任何常规数据集。

### 8. 不足与局限
*   **实施难度**：构建完美的“符号化文本天花板”（$S_{SymT}$）需要极高质量的图像标注或 Oracle 模型，在大规模自动化评估中存在挑战。
*   **实证数据较少**：论文以理论推导和案例分析为主，缺乏对当前主流开源 VLM 模型的横向大规模评测排名。
*   **应用限制**：对于某些难以用语言完全描述的视觉特征（如复杂的纹理、艺术风格），$S_{SymT}$ 的定义可能存在模糊地带。

（完）
