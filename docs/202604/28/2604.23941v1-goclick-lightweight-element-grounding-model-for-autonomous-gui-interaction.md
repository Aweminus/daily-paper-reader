---
title: "GoClick: Lightweight Element Grounding Model for Autonomous GUI Interaction"
authors: "Hongxin Li, Yuntao Chen, Zhaoxiang Zhang"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23941v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 用于非机器人领域的自主GUI交互智能体的轻量级VLM
tldr: 针对移动端GUI智能体对低延迟和轻量化的需求，本文提出GoClick，一个仅230M参数的轻量级GUI元素定位模型。现有大模型因计算资源限制难以在端侧部署，GoClick通过采用更适合小参数规模的Encoder-Decoder架构，并结合渐进式数据精炼流水线，从海量数据中提取高质量核心集进行训练。实验证明，GoClick在保持极高推理速度的同时，定位精度可媲美大型模型，显著提升了端云协作框架下GUI智能体的任务成功率。
selection_source: fresh_fetch
motivation: 现有的GUI元素定位模型参数量过大，难以在手机等资源受限的设备上实现低延迟的本地部署。
method: 采用Encoder-Decoder架构替代主流的Decoder-only架构，并利用渐进式数据精炼流水线筛选出3.8M高质量训练样本。
result: GoClick以230M的小规模参数实现了与大型模型相当的定位精度，并显著提升了推理速度和GUI智能体的任务成功率。
conclusion: GoClick证明了通过架构优化和高质量数据筛选，轻量级模型也能在GUI定位任务中表现卓越，为端侧智能体发展提供了新思路。
---

## Abstract
Graphical User Interface (GUI) element grounding (precisely locating elements on screenshots based on natural language instructions) is fundamental for agents interacting with GUIs. Deploying this capability directly on resource-constrained devices like mobile phones is increasingly critical for GUI agents requiring low latency. However, this goal faces a significant challenge, as current visual grounding methods typically employ large vision-language model (VLM) (more than 2.5B parameters), making them impractical for on-device execution due to memory and computational constraints. To address this, this paper introduces GoClick, a lightweight GUI element grounding VLM with only 230M parameters that achieves excellent visual grounding accuracy, even on par with significantly larger models. Simply downsizing existing decoder-only VLMs is a straightforward way to design a lightweight model, but our experiments reveal that this approach yields suboptimal results. Instead, we select an encoder-decoder architecture, which outperforms decoder-only alternatives at small parameter scales for GUI grounding tasks. Additionally, the limited capacity of small VLMs encourages us to develop a Progressive Data Refinement pipeline that utilizes task type filtering and data ratio adjustment to extract a high-quality 3.8M-sample core set from a 10.8M raw dataset. Training GoClick using this core set brings notable grounding accuracy gains. Our experiments show that GoClick excels on multiple GUI element grounding benchmarks while maintaining a small size and high inference speed. GoClick also enhances GUI agent performance when integrated into a device-cloud collaboration framework, where GoClick helps cloud-based task planners perform precise element localization and achieve higher success rates. We hope our method serves as a meaningful exploration within the GUI agent community.

---

## 论文详细总结（自动生成）

这是一份关于论文《GoClick: Lightweight Element Grounding Model for Autonomous GUI Interaction》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在资源受限的移动设备（如手机）上部署高性能、低延迟的图形用户界面（GUI）元素定位（Grounding）模型。
*   **研究背景**：GUI 智能体需要根据自然语言指令精准定位屏幕元素。目前的视觉定位方法通常依赖大型视觉语言模型（VLM，参数量 $\ge$ 2.5B），这导致在移动端部署时面临内存溢出和高延迟的挑战。
*   **整体含义**：本文旨在证明，通过优化的架构选择和高质量的数据精炼，极小规模的模型（如 230M 参数）也能在 GUI 定位任务上达到甚至超越大型模型的性能。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：放弃主流的大型 Decoder-only 架构，转而采用在小参数规模下空间感知能力更强的 **Encoder-Decoder 架构**，并配合一套**渐进式数据精炼（PDR）**流水线。
*   **关键技术细节**：
    *   **架构选择**：基于 Florence-2 架构，由视觉编码器（ViT）和多模态 Encoder-Decoder 组成。相比 Decoder-only 架构，它在处理坐标预测等窄输出空间任务时更具参数效率。
    *   **数据编译引擎**：收集了 10.8M 原始样本，涵盖四种指代描述（RE）：Alt-texts（文本）、Brief descriptions（视觉描述）、Action intent（操作意图）、Functionality descriptions（功能描述）。
    *   **渐进式数据精炼（PDR）流水线**：
        1.  **粗粒度精炼**：剔除过时的 GUI 模式（如 2011 年的 Android 4.0 数据）和对定位任务无助的指代生成（REG）任务。
        2.  **细粒度精炼**：通过消融实验调整不同数据源的比例，发现并非数据越多越好（例如某些网页数据全量加入反而导致过拟合）。最终精炼出 3.8M 的高质量核心集。
*   **算法流程**：输入 GUI 截图 $I$ 和指令 $L$，通过编码器提取特征，解码器输出归一化坐标 $c = [x, y]$。

### 3. 实验设计
*   **数据集/场景**：
    *   **定位基准测试**：FuncPred（功能定位）、ScreenSpot/ScreenSpot-v2（视觉描述定位）、MOTIF/RefExp（意图定位）、VisualWebBench（网页定位）。
    *   **智能体任务测试**：AITW、AndroidControl、GUIAct（移动端和网页端的连续操作任务）。
*   **对比方法**：
    *   **通用/大型模型**：GPT-4o、Qwen2VL-7B、CogAgent (18B)。
    *   **GUI 专家模型**：SeeClick (10B)、OS-ATLAS (8B/4B)、Aguvis (8B)、Ferret-UI (8B/3B)、ShowUI (2B)。
    *   **架构变体**：基于 InternVL-2、Qwen2-VL、SLiME 的同规模 Decoder-only 模型。

### 4. 资源与算力
*   **算力设备**：训练使用了 **8 张 NVIDIA L20 GPU**。
*   **训练时长**：
    *   GoClick-L (0.8B) 训练 2 个 Epoch 耗时约 **38 小时**。
    *   对比模型如 InternVL-2-1B 耗时 9 小时，Qwen2-VL-2B 耗时 24 小时。
*   **推理测试**：在单张 L20 GPU 上进行，模拟移动端环境计算首字延迟（TTFT）和每 Token 耗时（TPOT）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   在 **7 个** 定位基准数据集上进行精度对比。
    *   针对 **4 种** 不同架构的 VLM 在相同数据下进行 SFT 对比。
    *   针对数据精炼的 **2 个阶段** 进行了多组消融实验（图 6 展示了 6 个主要数据源的比例影响曲线）。
    *   在 **3 个** 智能体轨迹数据集上验证了“端云协作”模式的有效性。
*   **客观性与公平性**：通过在相同硬件、相同数据、相同 Epoch 下对比不同架构，以及使用标准化的 Step Success Rate (Step SR) 指标，实验设计显得非常客观且具有说服力。

### 6. 主要结论与发现
*   **架构优势**：在 1B 以下的小参数规模下，Encoder-Decoder 架构（Florence-2）在 GUI 定位任务上的表现显著优于 Decoder-only 架构（如 Qwen2-VL）。
*   **数据质量胜过数量**：通过 PDR 流水线将数据从 10.8M 压缩至 3.8M 后，模型平均精度反而提升了 4.0%，且训练成本大幅降低。
*   **性能卓越**：230M 参数的 GoClick-B 在多个指标上媲美 7B 甚至 10B 的模型，且推理速度快 3-5 倍。
*   **端云协作可行性**：将 GPT-4o 作为规划器（Planner），GoClick 作为端侧定位器（Grounder），其任务成功率远高于 GPT-4o 独立运行或使用 Set-of-Marks (SoM) 策略。

### 7. 优点（亮点）
*   **极高的参数效率**：用极小的模型实现了专家级的定位能力，非常适合端侧部署。
*   **数据精炼方法论**：系统性地研究了 GUI 数据的“负贡献”现象（如旧版系统截图和冗余网页数据），为后续研究提供了数据筛选的准则。
*   **端云协作模式**：提出了一种务实的工业界应用方案，解决了大模型“脑子好使但眼睛不好使（定位不准）”的问题。

### 8. 不足与局限
*   **任务局限性**：GoClick 专注于定位任务，缺乏自由文本对话和复杂逻辑推理能力，必须依赖外部规划器。
*   **硬件模拟偏差**：实验推理速度是在 L20 GPU 上测得的，虽然模拟了资源受限，但与真实的手机 ARM 架构芯片、统一内存架构下的表现可能存在差异。
*   **启发式风险**：数据精炼流水线中的某些比例调整仍带有一定的启发式（Heuristic）色彩，可能需要针对不同领域（如车载系统）重新调整。

（完）
