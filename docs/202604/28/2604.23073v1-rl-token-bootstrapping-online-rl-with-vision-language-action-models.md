---
title: "RL Token: Bootstrapping Online RL with Vision-Language-Action Models"
authors: "Charles Xu, Jost Tobias Springenberg, Michael Equi, Ali Amin, Adnan Esmail, Sergey Levine, Liyiming Ke"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.23073v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 支持对预训练的视觉-语言-动作模型进行在线强化学习微调
tldr: 引入RL Token，实现VLA模型的高效在线强化学习微调。
selection_source: fresh_fetch
motivation: 支持对预训练的视觉-语言-动作模型进行在线强化学习微调。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Vision-language-action (VLA) models can learn to perform diverse manipulation skills "out of the box," but achieving the precision and speed that real-world tasks demand requires further fine-tuning -- for example, via reinforcement learning (RL). We introduce a lightweight method that enables sample-efficient online RL fine-tuning of pretrained VLAs using just a few hours of real-world practice. We (1) adapt the VLA to expose an "RL token," a compact readout representation that preserves task-relevant pretrained knowledge while serving as an efficient interface for online RL, and (2) train a small actor-critic head on this RL token to refine the actions, while anchoring the learned policy to the VLA. Online RL with the RL token (RLT) makes it possible to fine-tune even large VLAs with RL quickly and efficiently. Across four real-robot tasks (screw installation, zip tie fastening, charger insertion, and Ethernet insertion), RLT improves the speed on the hardest part of the task by up to 3x and raises success rates significantly within minutes to a few hours of practice. It can even surpass the speed of human teleoperation on some of the tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《RL Token: Bootstrapping Online RL with Vision-Language-Action Models》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型虽然具备强大的通用性，但在处理需要亚毫米级精度的任务（如插拔、拧螺丝）时，往往表现得动作缓慢、成功率不稳定。
*   **核心问题**：如何利用预训练 VLA 模型的泛化能力，同时实现轻量级强化学习（RL）在真实机器人上的高效在线微调？传统的全量微调计算量巨大且样本效率低，而从头训练的小模型又缺乏 VLA 的感知先验。
*   **整体含义**：本文提出了一种名为 **RLT (RL Token)** 的方法，通过在 VLA 内部提取紧凑的表征作为接口，使机器人能在几小时甚至几分钟的练习内，显著提升复杂任务的精度和速度。

### 2. 论文提出的方法论
*   **核心思想**：将 VLA 视为冻结的特征提取器和行为先验，通过一个专门的“RL Token”接口连接轻量级的 Actor-Critic 结构。
*   **关键技术细节**：
    1.  **RL Token 提取**：在 VLA 的 Transformer 层后添加一个轻量级的编码器-解码器。编码器将 VLA 的高维特征压缩成一个紧凑的 RL Token；解码器通过重构原始特征来确保该 Token 保留了任务相关的关键信息。
    2.  **轻量级 Actor-Critic**：在 RL Token 之上训练小型 MLP 网络。Actor 不从零开始生成动作，而是以 VLA 预测的动作块（Action Chunk）为参考进行微调。
    3.  **动作块正则化**：在 RL 目标函数中加入正则项（BC Regularizer），约束 Actor 的输出不要偏离 VLA 的原始建议太远，从而将搜索空间限制在合理的局部范围内。
    4.  **参考动作丢弃（Dropout）**：在训练中随机屏蔽 VLA 的参考动作，强制 Actor 建立独立的改进路径，防止其只会简单模仿。
*   **算法流程**：先通过少量演示数据训练 RL Token 提取器；随后冻结 VLA，在真实机器人上进行在线 RL，利用人类提供的稀疏奖励（成功/失败）和必要的干预进行策略优化。

### 3. 实验设计
*   **实验场景**：选择了四种极具挑战性的高精度任务：
    *   **拧螺丝 (Screw installation)**：亚毫米级对齐。
    *   **扎带紧固 (Zip tie fastening)**：双臂协作处理柔性物体。
    *   **以太网线插入 (Ethernet insertion)**：狭窄空间对齐。
    *   **充电器插入 (Charger insertion)**：触觉敏感任务。
*   **Benchmark 与对比方法**：
    *   **Base VLA ($\pi_{0.6}$)**：未经过 RL 优化的基准模型。
    *   **HIL-SERL**：不使用 VLA 先验，直接从 ResNet 特征进行在线 RL。
    *   **PLD (Probe-Learn-Distill)**：学习残差动作的单步 RL 方法。
    *   **DSRL**：在扩散模型的潜空间进行引导的方法。
    *   **DAgger**：基于人类干预数据的模仿学习微调。

### 4. 资源与算力
*   **训练时长**：论文强调了“样本效率”，实际机器人练习时间在 **15 分钟到 5 小时** 之间（视任务难度而定）。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量。但提到 VLA 模型（$\pi_{0.6}$）是冻结的，RL 部分使用的是轻量级 MLP（隐藏层维度 256-512），且采用异步更新模式，暗示其对实时算力的要求在可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：每个任务评估了 50 个回合（Episodes），涵盖了“关键阶段评估”和“全任务评估”。
*   **消融实验**：非常充分。分别验证了 **RL Token 的必要性**（对比 ResNet）、**动作块（Chunking）的作用**、**正则化项的影响**以及**参考动作输入（Pass-Through）的贡献**。
*   **客观性**：通过“吞吐量”（每 10 分钟成功次数）这一指标综合衡量了成功率和执行速度，比单纯看成功率更客观。

### 6. 论文的主要结论与发现
*   **性能飞跃**：RLT 在关键阶段将任务执行速度提升了 **3 倍**。在拧螺丝任务中，成功率从 20% 提升至 65%。
*   **超越人类**：在以太网线插入任务中，RLT 优化后的策略速度甚至**超过了人类远程操作**的速度。
*   **涌现行为**：RL 发现了一些演示数据中没有的策略，例如通过轻微晃动（Wiggling）来利用物理顺应性完成插入，而不是像原始 VLA 那样反复尝试。

### 7. 优点
*   **实用性极强**：解决了大模型“最后 1 毫米”的精度难题，且微调成本极低（仅需数小时机器人时间）。
*   **架构优雅**：RL Token 的设计巧妙地平衡了“保留预训练知识”与“降低 RL 学习维度”之间的矛盾。
*   **动作块机制**：通过处理动作块而非单步动作，有效缓解了稀疏奖励下的信用分配（Credit Assignment）问题。

### 8. 不足与局限
*   **人类依赖**：训练过程仍需要人类提供成功/失败的标签，以及在必要时进行干预，尚未实现完全自主的闭环学习。
*   **阶段限制**：实验主要集中在任务的“关键阶段”（Critical Phase），虽然展示了全任务评估，但如何自动识别并触发 RL 介入仍有优化空间。
*   **泛化边界**：虽然利用了 VLA 的表征，但 RL 微调后的策略在面对环境剧烈变化时的鲁棒性尚未在文中进行深度探讨。

（完）
