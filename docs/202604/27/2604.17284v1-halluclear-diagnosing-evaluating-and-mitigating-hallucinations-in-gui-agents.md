---
title: "HalluClear: Diagnosing, Evaluating and Mitigating Hallucinations in GUI Agents"
title_zh: HalluClear：GUI 智能体幻觉的诊断、评估与缓解
authors: "Chao Jin, Wenkui Yang, Hao Sun, Yuqi Liao, Qianyi Jiang, Kai Zhou, Jie Cao, Ran He, Huaibo Huang"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17284v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: GUI智能体中的幻觉缓解
tldr: 针对GUI智能体在实际应用中因幻觉导致任务失败的问题，本文提出HalluClear框架。该框架包含GUI特有的幻觉分类体系、基于专家标注和集成信度估计的三阶段评估工作流，以及一种基于闭环结构化推理的轻量化后训练缓解方案。实验证明，仅需9k样本的微调即可显著降低幻觉并提升动作准确性，为实现鲁棒的GUI自动化提供了高效路径。
selection_source: fresh_fetch
motivation: GUI智能体在部署中常因幻觉引发连锁失败，且目前缺乏针对该领域的细粒度诊断、可靠评估和针对性缓解工具。
method: 构建了GUI幻觉分类法，开发了三阶段VLM评测流，并提出一种基于闭环结构化推理的轻量化持续后训练方案。
result: 在代表性智能体和公开基准上的实验表明，仅通过9000条样本的后训练即可显著减少幻觉并提高动作保真度。
conclusion: HalluClear为提升GUI智能体的鲁棒性提供了一种计算高效的方案，有效弥补了大规模缩放训练在处理幻觉方面的不足。
---

## 摘要
虽然 GUI 智能体的进展主要由工业规模的训练驱动，但无根据的幻觉往往会在实际部署中引发级联故障。与通用 VLM 领域不同，GUI 智能体领域缺乏一个专注于幻觉的套件，用于细粒度诊断、可靠评估和针对性缓解。为了填补这一空白，我们推出了 HalluClear，这是一个用于缓解 GUI 智能体幻觉的综合套件，作为计算密集型扩展（scaling）的补充。HalluClear 包含：(1) 一种源自经验性故障分析的 GUI 特定幻觉分类法；(2) 一种经过校准的三阶段评估工作流，通过专家标注的基准测试和集成可信度估计，增强了“VLM 作为裁判”（VLM-as-a-judge）的可靠性；(3) 一种基于闭环结构化推理的缓解方案，能够为通用型和 GUI 专用型智能体提供带有冷启动初始化的轻量化持续后训练。在代表性智能体和公共基准上的实验表明，仅使用我们套件中的 9K 个样本进行后训练，就能显著减少幻觉，从而提高接地（grounding）和动作保真度，为实现鲁棒的 GUI 自动化提供了一条计算高效的路径。

## Abstract
While progress in GUI agents has been largely driven by industrial-scale training, ungrounded hallucinations often trigger cascading failures in real-world deployments.Unlike general VLM domains, the GUI agent field lacks a hallucination-focused suite for fine-grained diagnosis, reliable evaluation, and targeted mitigation.To bridge this gap, we introduce HalluClear, a comprehensive suite for hallucination mitigation in GUI agents as a complement to computation-intensive scaling. HalluClear comprises: (1) a GUI-specific hallucination taxonomy derived from empirical failure analysis; (2) a calibrated three-stage evaluation workflow which enhances VLM-as-a-judge reliability via expert-annotated benchmarking and ensemble credibility estimation; and (3) a mitigation scheme based on closed-loop structured reasoning, enabling lightweight continual post-training with cold-start initialization for both generalist and GUI-specialist agents. Experiments across representative agents and public benchmarks demonstrate that post-training on only 9K samples within our suite can significantly reduce hallucinations, thereby improving grounding and action fidelity, offering a compute-efficient pathway to robust GUI automation.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **HalluClear**，一个专门针对图形用户界面（GUI）智能体幻觉问题的综合性框架。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管基于视觉语言模型（VLM）的 GUI 智能体在规模化训练下取得了进展，但其在实际应用中经常出现“幻觉”（即无根据的决策），导致任务级联失败。
*   **研究动机**：
    *   目前的 GUI 领域缺乏针对幻觉的细粒度诊断工具和可靠的评估标准。
    *   工业界倾向于通过增加计算量和数据规模来提升性能上限，但忽视了通过减少幻觉来提升性能“下限”的计算高效路径。
    *   现有的 VLM 评估套件多侧重于静态感知，难以反映 GUI 智能体动态、以动作为中心的特性。

### 2. 方法论
HalluClear 包含三个核心组件：
*   **GUI 特定幻觉分类法（Taxonomy）**：通过对失败案例的归纳，将幻觉分为两大类共 8 个子类：
    *   **感知幻觉 (PH)**：包括屏幕状态（PH.1）、元素存在（PH.2）、元素属性（PH.3）、元素关系（PH.4）。
    *   **推理幻觉 (RH)**：包括指令遵循（RH.1）、上下文一致性（RH.2）、逻辑链（RH.3）、外部事实（RH.4）。
*   **三阶段评估工作流**：
    1.  **JQ-Bench 构建**：由专家标注的高质量基准测试，包含黄金参考。
    2.  **裁判资格认证 (JQ)**：对候选 VLM 裁判（如 Gemini, Qwen 等）进行信度评估，只有通过高难度细粒度分类测试的裁判才能进入面板。
    3.  **幻觉率 (HR) 评估**：利用合格裁判在目标智能体输出上进行二元或细粒度检测，并结合信度分数进行校准。
*   **缓解方案 (OODA 闭环推理)**：
    *   **结构化推理**：将决策过程显式拆分为 **Observe**（感知）、**Orient**（对齐历史与目标）、**Decide**（逻辑推导）、**Act++**（执行、自我反思与总结）。
    *   **训练策略**：采用“冷启动 SFT（在 9K 结构化样本上微调）+ 强化学习（使用 GRPO 算法，针对格式和动作层次进行奖励设计）”的轻量化后训练路径。

### 3. 实验设计
*   **数据集/场景**：
    *   **感知接地（Grounding）**：ScreenSpot-V2, ScreenSpot-Pro。
    *   **动作执行（Action）**：AndroidControl, GUI-Odyssey（分为高层指令和底层指令两种难度）。
*   **对比方法**：
    *   **通用模型**：Qwen2.5-VL-7B。
    *   **GUI 专家模型**：UI-TARS-1.5-7B, GUI-Owl-7B。
*   **Benchmark**：使用 JQ-Bench 进行裁判校准，并在 HR-Pool 上测量幻觉率。

### 4. 资源与算力
*   **数据规模**：仅使用了 **9K** 条样本进行轻量化持续后训练（SFT 阶段和 RL 阶段各使用一部分）。
*   **算力细节**：文中提到了使用 SWIFT 框架进行 SFT，使用 VeRL 框架进行 RL（GRPO 算法）。虽然未详细列出具体的 GPU 型号和数量，但强调了该方法是“轻量化”且“计算高效”的，例如在 SFT 阶段冻结了视觉编码器并使用 LoRA 技术。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个代表性基准模型，在 4 个主流 GUI 公开数据集上进行了测试。
*   **充分性**：
    *   进行了详细的**消融实验**，验证了 SFT、RL 和 OODA 结构化推理各自的贡献。
    *   通过**多裁判集成**和**信度校准**来确保评估的客观性。
    *   对比了通用模型与专家模型在感知和推理幻觉上的不同表现，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **幻觉显著降低**：通过 HalluClear 后训练，所有测试智能体的幻觉率（HR）均大幅下降（例如 Qwen2.5-VL 从约 57% 降至 25% 左右）。
*   **性能全面提升**：减少幻觉直接带来了感知接地准确率和任务成功率（SR）的提升，尤其是在复杂软件环境（ScreenSpot-Pro）中表现突出。
*   **模型特性差异**：通用模型更易产生感知幻觉（空间关系理解弱），而专家模型在特定领域微调后更易产生推理幻觉（如过度自信导致忽视指令）。
*   **补齐短板**：该方法能有效修复专家模型在某些领域的“灾难性遗忘”或性能缺陷。

### 7. 优点与亮点
*   **系统性强**：从分类、评估到缓解，提供了一套完整的闭环工具链。
*   **评估可靠**：引入了“裁判资格认证”机制，解决了“VLM 裁判”本身可能存在的偏见和不稳定性问题。
*   **计算高效**：证明了不需要大规模重新训练，仅靠 9K 样本的结构化后训练即可显著提升智能体鲁棒性。
*   **闭环反思**：Act++ 步骤中的自我反思机制有效减少了推理与动作不一致的情况。

### 8. 不足与局限
*   **裁判泛化风险**：虽然裁判经过校准，但在面对从未见过的智能体输出分布时，仍可能存在判断偏差。
*   **最优策略的不可知性**：论文定义幻觉是相对于“当前信息状态下的最优策略”，但在部分可观测（POMDP）环境下，真正的最优策略难以确定，目前主要依赖人类专家作为代理。
*   **样本多样性**：虽然 9K 样本效果显著，但对于极度长尾的 GUI 场景，可能仍需要更广泛的数据覆盖。

（完）
