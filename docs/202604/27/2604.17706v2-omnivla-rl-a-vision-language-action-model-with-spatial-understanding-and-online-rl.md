---
title: "OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL"
title_zh: OmniVLA-RL：一种具有空间理解和在线强化学习的视觉-语言-动作模型
authors: "Haoxiang Jie, Yaoyuan Yan, Xiangyu Wei, Kailin Wang, Hongjie Yan, Zhiyou Heng, Daocheng Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17706v2"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 结合在线强化学习的视觉-语言-动作模型
tldr: OmniVLA-RL 针对具身智能中 VLA 模型空间感知不准、多模态融合欠佳及强化学习不稳定的挑战，提出了一种基于 Mix-of-Transformers (MoT) 的新架构，协同集成推理、空间和动作专家。此外，引入 Flow-GSPO 算法，通过将流匹配重构为随机微分方程并结合分组分段策略优化，显著提升了动作精度和训练鲁棒性，在 LIBERO 基准测试中表现卓越。
selection_source: fresh_fetch
motivation: 旨在解决现有 VLA 模型在空间感知精度、多模态融合效率以及强化学习训练稳定性方面的不足。
method: 采用 Mix-of-Transformers 架构整合多专家模块，并结合 Flow-GSPO 算法优化动作生成与策略训练过程。
result: 在 LIBERO 和 LIBERO-Plus 基准测试中，OmniVLA-RL 的整体性能超越了当前主流的 VLA 模型。
conclusion: 该研究通过创新的架构与优化策略，有效克服了 VLA 模型的局限性，为具身智能提供了更精准稳健的解决方案。
---

## 摘要
视觉-语言-动作（VLA）模型代表了具身智能（embodied AI）的一种范式转变，然而现有框架通常面临空间感知不精确、多模态融合欠佳以及强化学习不稳定的挑战。为了弥补这些差距，我们提出了 OmniVLA-RL，这是一种利用混合 Transformer（Mix-of-Transformers, MoT）设计来协同整合推理、空间和动作专家的创新架构。此外，我们引入了 Flow-GSPO，它将流匹配（flow matching）重新表述为随机微分方程（SDE）过程，并将其与分组分段策略优化（Group Segmented Policy Optimization, GSPO）相结合，以增强动作精度和训练鲁棒性。在 LIBERO 和 LIBERO-Plus 基准测试上的广泛评估表明，OmniVLA-RL 取得了出色的整体性能，并超越了现有的主流方法，有效克服了当前 VLA 模型的根本局限性。

## Abstract
Visual-Language-Action (VLA) models represent a paradigm shift in embodied AI, yet existing frameworks often struggle with imprecise spatial perception, suboptimal multimodal fusion, and instability in reinforcement learning. To bridge these gaps, we propose OmniVLA-RL, a novel architecture that leverages a Mix-of-Transformers (MoT) design to synergistically integrate reasoning, spatial, and action experts. Furthermore, we introduce Flow-GSPO, which reformulates flow matching as a Stochastic Differential Equation (SDE) process and integrates it with Group Segmented Policy Optimization (GSPO) to enhance action precision and training robustness. Extensive evaluations on the LIBERO and LIBERO-Plus benchmarks demonstrate that OmniVLA-RL achieves decent overall performance and surpasses mainstream existing methods, effectively overcoming the fundamental limitations of current VLA models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **OmniVLA-RL** 的新型视觉-语言-动作（VLA）模型，旨在提升具身智能机器人在复杂环境中的空间感知能力和强化学习训练的稳定性。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLA 模型（如 RT-2, OpenVLA）在处理具身任务时存在三大瓶颈：
    1.  **空间感知不精**：通用视觉语言模型（VLM）难以精确输出物体的 3D 位置和尺寸，导致抓取和避障精度不足。
    2.  **多模态融合效率低**：现有的早期或晚期融合方案未能深度整合语言指令、视觉语义和空间特征。
    3.  **强化学习（RL）不稳定**：传统的 PPO 算法过于复杂，而新兴的 GRPO 算法在处理动作序列采样时容易出现训练崩溃。
*   **研究背景**：随着生成式模型（如流匹配 Flow Matching）在动作生成中的应用，如何将这些生成模型与高效的在线强化学习算法结合，成为提升机器人泛化能力的关键。

### 2. 方法论
*   **核心思想**：提出一种基于 **Mix-of-Transformers (MoT)** 的架构，通过三个专家模块协同工作，并引入 **Flow-GSPO** 算法进行策略优化。
*   **关键技术细节**：
    *   **MoT 架构**：
        *   **推理专家 (Reasoning Expert)**：基于预训练 VLM（如 PaLiGemma），提取高层语义和指令信息。
        *   **空间专家 (Spatial Expert)**：利用 VGGT 编码器提取细粒度 3D 结构特征，并通过辅助头进行几何监督。
        *   **动作专家 (Action Expert)**：采用条件流匹配（CFM）生成高精度动作轨迹。
    *   **块级因果注意力 (Block-wise Causal Attention)**：将空间和语义 token 设为全可见前缀（Prefix），而动作 token 设为因果后缀（Suffix），防止随机噪声干扰场景理解。
    *   **Flow-GSPO 算法**：
        *   将流匹配的确定性 ODE 过程重构为基于 Fokker-Planck 方程的 **随机微分方程 (SDE)**，以满足 RL 的随机探索需求。
        *   结合 **分组分段策略优化 (GSPO)**，在动作块（Action Block）级别进行优势估计和策略更新，避免了 token 级别的偏差累积。
*   **训练流程**：分为三个阶段：1. 空间感知预训练；2. 动作生成预训练（基于 DROID 数据集）；3. 在线强化学习微调（Flow-GSPO）。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估知识迁移和基础操作能力。
    *   **LIBERO-Plus**：更具挑战性的长程任务基准，涉及多阶段操作序列。
*   **对比方法**：包括 Diffusion Policy, Octo, OpenVLA, SpatialVLA, CoT-VLA, $\pi_0$, $\pi_{0.5}$, F1 等主流 VLA 模型。
*   **RL 对比基准**：在在线微调阶段对比了 PPO 和 GRPO。

### 4. 资源与算力
*   **论文说明**：文中提到了使用 AdamW 优化器、特定的学习率（1e-5）和训练步数（200步 RL 更新），并指出模型可以在消费级 GPU 上运行（参考了 SmolVLA 的背景）。
*   **缺失信息**：论文**未明确说明**具体使用的 GPU 型号（如 A100 或 H100）以及具体的 GPU 数量和总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的四个维度上进行了详尽的排名对比。
    *   在 LIBERO-Plus 上展示了 RL 训练的收敛曲线。
    *   **消融实验**：针对有无空间专家、不同 RL 算法（PPO vs GRPO vs Flow-GSPO）进行了对比。
*   **充分性评价**：实验设计较为充分，涵盖了从基础模仿学习到高级强化学习的性能评估。通过对比多种 SOTA 模型，证明了其在长程任务和空间敏感任务中的优势。

### 6. 主要结论与发现
*   **性能领先**：OmniVLA-RL 在 LIBERO 基准上达到了 **97.6%** 的平均成功率，位居榜首。
*   **RL 稳定性**：Flow-GSPO 在收敛速度和最终成功率上显著优于 PPO 和 GRPO，尤其是在处理连续动作空间时表现出更强的鲁棒性。
*   **空间专家的必要性**：消融实验显示，移除空间专家会导致性能下降 8.3%，证明了独立空间表征对精确操作的关键作用。

### 7. 优点
*   **架构创新**：MoT 架构有效地解耦了感知、推理与执行，解决了多模态特征相互干扰的问题。
*   **算法融合**：成功将流匹配（Flow Matching）与分组策略优化（GSPO）结合，为生成式 VLA 模型的在线微调提供了新范式。
*   **空间增强**：通过引入专门的空间专家和几何监督，显著提升了模型对 3D 环境的理解精度。

### 8. 不足与局限
*   **Sim-to-Real 验证缺失**：实验主要在仿真环境（LIBERO）中完成，尚未在真实物理机器人硬件上进行大规模验证，存在“仿真到现实”的迁移风险。
*   **缺乏世界模型**：虽然动作连续性好，但架构中缺乏专门的“世界模型”来预测环境状态演变，对于极长程的逻辑规划可能仍有局限。
*   **算力细节不明**：未披露具体的训练资源消耗，难以评估其训练成本和复现难度。

（完）
