---
title: "ReCAPA: Hierarchical Predictive Correction to Mitigate Cascading Failures"
title_zh: ReCAPA：缓解级联故障的层级预测修正
authors: "Xiyin Zeng, Yuyu Sun, Haoyang Li, Shouqiang Liu, Hao Wang"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21232v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 多模态环境中用于多步任务的视觉-语言-动作系统
tldr: 针对视觉-语言-动作（VLA）系统在执行多步任务时易出现的级联故障问题，本文提出了ReCAPA框架。该框架通过在动作、子目标和轨迹三个层级引入预测性纠错与对比机制，利用Sinkhorn和Score-field模块强化语义对齐。此外，研究还提出了量化错误传播与恢复的新指标。实验证明，ReCAPA在多个具身智能基准测试中表现优异，有效缓解了复合误差并提升了任务成功率。
selection_source: fresh_fetch
motivation: 现有的VLA系统在中间步骤出错时，局部误差会随时间累积并导致级联故障，缺乏有效的动态纠偏机制。
method: 提出一种分层预测对齐与规划架构，在动作、子目标和轨迹层面利用Sinkhorn和Score-field模块进行预测性纠错与语义对齐。
result: 在VisualAgentBench、MineDojo和AI2-THOR等基准测试中，ReCAPA的性能显著超越了现有的闭源和开源大语言模型基准。
conclusion: 通过多层级的预测对齐，ReCAPA能有效抑制长程任务中的误差传播，显著提升了具身智能体在复杂环境下的鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）系统遵循指令在多模态环境中执行多步任务。最近的 VLA 方法通常依赖于事后修正机制，或者在固定的任务分解和对齐方案下运行。然而，一旦中间步骤被错误指定，局部误差就会通过后续步骤传播，并最终累积成级联故障。为了减轻这种复合效应，我们提出了预测对齐与规划架构（Predictive Alignment and Planning Architecture），这是一个利用预测和对比来调整动作、子目标和轨迹三个层级偏差的框架。通过使用基于 Sinkhorn 的模块和得分场（Score-field）模块，在所有层级上强制执行语义对齐。预测修正和对齐在训练期间共同更新动作生成器，使其能够调整细粒度步骤以保持与整体意图一致。我们还引入了两个新指标来量化任务中的误差传播和恢复过程，捕捉错误在长时程执行中是如何扩散和消退的。实验表明，ReCAPA 在 VisualAgentBench、MineDojo 和 AI2-THOR 等具身智能体基准测试中取得了具有竞争力的结果，优于强大的闭源和开源大语言模型基线。

## Abstract
Vision-Language-Action systems follow instructions to execute multi-step tasks in multimodal environments. Recent VLA approaches typically rely on post-hoc correction mechanisms or operate under fixed task decompositions and alignment schemes. However, once an intermediate step is mis-specified, local errors propagate through subsequent steps and eventually accumulate into cascading failures. To mitigate this compounding effect, we propose Predictive Alignment and Planning Architecture, a framework that uses prediction and contrast to adjust deviations across three levels: actions, subgoals, and trajectories. Semantic alignment is enforced at all levels using a Sinkhorn-based module and a Score-field module. The predictive correction and alignment jointly update the action generator during training, enabling it to adjust fine-grained steps to remain aligned with the overall intent. We further introduce two new metrics to quantify error propagation and recovery processes in tasks, capturing how mistakes spread and fade over long-horizon execution. Experiments show that ReCAPA achieves competitive results on embodied agent benchmarks such as VisualAgentBench, MineDojo, and AI2-THOR, outperforming strong proprietary and open-source Large Language Model baselines.

---

## 论文详细总结（自动生成）

### 论文总结：ReCAPA —— 缓解级联故障的层级预测修正框架

#### 1. 核心问题与整体含义（研究动机和背景）
在具身智能领域，视觉-语言-动作（VLA）系统在执行长程（Long-horizon）任务时，面临的核心挑战是**级联故障（Cascading Failures）**。现有的系统往往依赖于静态的任务分解或事后修正，一旦中间某个步骤出现微小偏差，误差会随时间迅速累积，导致后续步骤完全偏离原始意图（语义漂移）。论文的研究动机在于：如何通过一种层级的、具有前瞻性的预测机制，在误差尚未扩大前进行实时对齐和修正，从而增强智能体在复杂多步任务中的鲁棒性。

#### 2. 方法论：核心思想与关键技术
ReCAPA 提出了**层级预测修正（HPCC）**架构，将任务执行分为三个层级：**动作（Action）**、**子目标（Subgoal）**和**轨迹（Trajectory）**。

*   **核心思想**：通过低层级预测高层级的语义表示，并利用对齐信号进行自上而下的修正。
*   **关键技术细节**：
    *   **HPCC 模块**：在训练阶段，动作层预测子目标表示，子目标层预测整体轨迹表示。通过对比学习（InfoNCE 损失）确保预测值与真实目标一致。
    *   **Sinkhorn 对齐模块**：利用最优传输（Optimal Transport）理论，计算轨迹分布与指令提示（Prompt）分布之间的全局语义一致性，防止长程执行中的方向性偏差。
    *   **得分场（Score-field）模块**：通过向状态嵌入添加高斯噪声并学习去噪得分，为每个步骤提供细粒度的修正梯度，将偏离的动作“拉回”到高密度（符合意图）的区域。
    *   **推理机制**：在推理时，系统根据对齐分数动态重采样动作、调整子目标切换时机，并利用 Sinkhorn 分数筛选最佳执行路径。

#### 3. 实验设计：数据集、基准与对比方法
论文在多个极具挑战性的具身智能基准上进行了验证：
*   **数据集/场景**：
    *   **VisualAgentBench**：包含 OmniGibson（家庭任务）和 Minecraft（导航/合成）。
    *   **MineDojo**：复杂的开放世界 Minecraft 任务。
    *   **AI2-THOR**：室内交互式场景。
    *   **EmbodiedAgentInterface**（Behavior, VirtualHome）及 **AlfWorld**。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4V, Claude-3.5/4-Sonnet, Gemini 2.5 Flash。
    *   **开源模型**：CogVLM2, LLaVA-NeXT, InternVL-2, Qwen-VL 等。
    *   **专门智能体框架**：LLaMAR, ReAct, AdaPlanner, WALL-E 等。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA H20 GPU** 进行训练。
*   **训练细节**：总训练步数为 **200,000 步**。
*   **优化器**：使用 AdamW，学习率为 1e-4，配合余弦退火调度和 1000 步线性预热。
*   **编码器**：文本和视觉编码器采用了预训练的 Nomic 系列模型。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估**：除了传统的成功率（SR），还涵盖了传输率（TR）、覆盖率（Coverage）和平衡性（Balance）。
*   **消融实验**：针对层级结构（去掉子目标或轨迹层）、对齐策略（Sinkhorn vs. Score-field）以及预测/修正组件进行了详尽的对比。
*   **新指标引入**：提出了 **误差传播率（EPR）**和 **传播衰减系数（PAC）**，从动力学角度量化了错误是如何扩散和恢复的，这在以往研究中较为罕见。
*   **跨域验证**：在 Proc-THOR 和 Behavior1K 上预训练，并在其他基准上进行零样本或少样本迁移，验证了泛化能力。

#### 6. 主要结论与发现
*   **性能领先**：ReCAPA 在所有主要基准上均优于现有的 LLM 基线，例如在 VisualAgentBench 上比 GPT-4o-mini 高出 4.5%，在 MineDojo 上领先 9%。
*   **误差抑制**：EPR 指标显示，ReCAPA 能显著降低初始错误对后续步骤的影响；PAC 指标证明其恢复速度远快于其他模型。
*   **层级协同**：消融实验证明，轨迹级的全局对齐与动作级的局部修正缺一不可，两者结合能有效解决“局部合理但全局错误”的问题。

#### 7. 优点：亮点总结
*   **架构创新**：将预测机制与层级对齐结合，改变了以往“出错后再纠正”的被动模式，实现了“预见偏差并提前修正”。
*   **数学严谨性**：引入 Sinkhorn 散度和得分场理论，为语义对齐提供了坚实的数学基础。
*   **诊断工具**：提出的 EPR 和 PAC 指标为长程任务的鲁棒性评估提供了新的标准工具。
*   **泛化性强**：通过层级抽象，模型在未见过的任务和环境中表现出极强的迁移能力。

#### 8. 不足与局限
*   **反馈离散性**：目前的修正机制在层级间通过离散评分运行，虽然稳定，但无法提供连续的中间反馈，可能导致在评分间隔内积累微小偏差。
*   **确定性映射**：层级生成模块使用确定性映射计算下一层嵌入，在面对高度不确定的环境时，无法同时表示多种可能的合理后续路径。
*   **计算开销**：虽然推理时使用了轻量化模块，但层级预测和多次重采样可能会在极高性能要求的实时场景中产生延迟。

（完）
