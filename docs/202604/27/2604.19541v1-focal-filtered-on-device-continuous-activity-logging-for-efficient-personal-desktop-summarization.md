---
title: "FOCAL: Filtered On-device Continuous Activity Logging for Efficient Personal Desktop Summarization"
title_zh: FOCAL：用于高效个人桌面摘要的过滤式设备端持续活动日志记录
authors: "Haoran Yin, Zhiyuan Wen, Jiannong Cao, Bo Yuan, Ruosong Yang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19541v1"
tags: ["query:vlm-rl"]
score: 8.0
evidence: 使用视觉语言模型进行选择性视觉推理和任务归因的多智能体系统
tldr: FOCAL是一个隐私优先的端侧多智能体系统，旨在高效总结桌面交互流。它通过“过滤-规划-记录”架构，利用轻量级过滤、任务归因、选择性视觉推理和任务隔离记忆，解决了本地资源受限和跨任务上下文污染的问题。在DesktopBench测试中，FOCAL显著降低了计算开销，并在处理任务中断和关键信息召回方面表现优异，为端侧个人日志总结提供了高效方案。
selection_source: fresh_fetch
motivation: 桌面交互流的端侧总结面临视觉语言模型资源消耗大以及跨任务上下文污染的挑战。
method: 提出FOCAL系统，采用由过滤、大脑、记录和记忆四个智能体组成的统一架构，实现噪声抑制、任务归因和任务隔离的摘要生成。
result: "实验显示FOCAL减少了60.4%的Token消耗和72.3%的VLM调用，同时将关键信息召回率从0.38提升至0.61。"
conclusion: FOCAL证明了通过多智能体协作和任务隔离机制，可以在端侧高效且准确地将复杂的桌面活动流转化为结构化的个人日志。
---

## 摘要
桌面交互流提供了交织在一起的用户任务的持续且隐私敏感的记录。在设备端将这些流转换为按任务组织的个人日志面临两个主要挑战：详尽的视觉语言模型（VLM）处理会耗尽本地资源，而全局流处理会导致跨任务的上下文污染。我们提出了 FOCAL（过滤式设备端持续活动日志记录），这是一个采用统一“过滤-规划-记录”架构的隐私优先多智能体系统。它级联了一个用于噪声抑制的轻量级过滤智能体（Filter Agent）、一个用于任务归因的纯文本大脑智能体（Brain Agent）、一个用于选择性视觉推理的记录智能体（Record Agent），以及一个用于上下文连贯摘要的任务隔离记忆智能体（Memory Agent）。在 DesktopBench（包含 420 个复杂会话中的 2,572 张截图）上的实验表明，与基准模型相比，FOCAL 将总 Token 消耗降低了 60.4%，VLM 调用次数减少了 72.3%，同时将关键信息召回率（KIR）从 0.38 提升至 0.61。至关重要的是，在 A→B→A 任务中断情况下，FOCAL 保持了 0.81 的任务准确率（Task Acc）和 0.80 的 KIR，而基准模型的任务准确率则崩溃至 0.03。FOCAL 开创了将无指令桌面流高效、在设备端摘要为多视角个人日志的先河。

## Abstract
Desktop interaction streams provide a continuous, privacy-sensitive record of interleaved user tasks. Transforming these streams into task-organized personal logs on-device faces two main challenges: exhaustive Vision-Language Model (VLM) processing strains local resources, and global stream processing causes cross-task context pollution. We present FOCAL (Filtered On-device Continuous Activity Logging), a privacy-first multi-agent system utilizing a unified filter-plan-log architecture. It cascades a lightweight Filter Agent for noise suppression, a text-only Brain Agent for task attribution, a Record Agent for selective visual reasoning, and a task-isolated Memory Agent for context-coherent summarization. Experiments on DesktopBench (comprising 2,572 screenshots across 420 complex sessions) show FOCAL reduces total token consumption by 60.4% and VLM call count by 72.3% versus a baseline, while boosting Key Information Recall (KIR) from 0.38 to 0.61. Crucially, under $A{\to}B{\to}A$ task interruptions, FOCAL maintains Task Acc 0.81 and KIR 0.80, whereas the baseline collapses to Task Acc 0.03. FOCAL pioneers the efficient, on-device summarization of instruction-free desktop streams into multi-perspective personal logs.

---

## 论文详细总结（自动生成）

这是一份关于论文《FOCAL: Filtered On-device Continuous Activity Logging for Efficient Personal Desktop Summarization》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：现代桌面办公通常涉及多个任务交织（如写文档时查资料、回复邮件、调试代码）。将这些连续的屏幕交互流总结为按任务组织的个人日志，对于提高生产力和知识检索具有重要意义。
*   **核心挑战**：
    1.  **端侧资源受限**：详尽地调用视觉语言模型（VLM）处理每一帧截图会严重消耗本地 CPU/GPU 资源，导致系统卡顿。
    2.  **跨任务上下文污染**：用户频繁切换任务，传统的全局流处理会导致不同任务的上下文混淆，降低摘要质量。
    3.  **隐私敏感性**：桌面截图包含大量私密信息，必须在设备端（On-device）完成处理，不能依赖云端。
*   **整体含义**：FOCAL 旨在通过一种“隐私优先”的多智能体协作架构，在不依赖用户指令的情况下，高效、准确地将复杂的桌面活动流转化为结构化的任务日志。

### 2. 方法论
FOCAL 采用了统一的**“过滤-规划-日志”（Filter-Plan-Log）**架构，由五个专门的智能体组成：
*   **核心思想**：将昂贵的视觉推理（VLM）置于轻量级的元数据规划之后，并实现任务级别的内存隔离。
*   **关键技术细节**：
    1.  **Filter Agent（过滤智能体）**：在最前端抑制低信息量的事件（如静止屏幕），仅保留有效动作。
    2.  **Brain Agent（大脑智能体）**：**核心创新点**。仅利用 GUI 元数据（应用名、窗口标题）进行一次性规划，决定哪些帧需要 VLM 处理（自适应采样），并进行任务归因（Task Attribution）。
    3.  **Record Agent（记录智能体）**：仅对采样后的关键帧调用 VLM，结合当前任务的局部记忆生成语义描述。
    4.  **Memory Agent（记忆智能体）**：维护**任务隔离的存储空间**。每个任务拥有独立的上下文，防止 $A \to B \to A$ 模式下的信息干扰。
    5.  **Summary Agent（摘要智能体）**：在会话结束时，将各任务隔离的记忆整合为连贯的多视角摘要。

### 3. 实验设计
*   **数据集 (DesktopBench)**：基于 VideoGUI 重构，包含 2,572 张原始截图，分为两个子集：
    *   **Multi-task (320个会话)**：模拟复杂的跨应用工作流（如视频编辑+素材搜索）。
    *   **Interruption (100个会话)**：专门测试 $A \to B \to A$ 任务中断场景。
*   **对比方法 (Baselines)**：
    1.  **Naive LLM Agent**：全量 VLM 处理，无采样，无任务隔离记忆。
    2.  **FOCAL-GM**：保留 FOCAL 的采样机制，但使用全局共享记忆（Global Memory）。
*   **评估指标**：
    *   **效率**：VLM 调用次数 (VCC)、Token 消耗量 (TCS)。
    *   **质量**：关键信息召回率 (KIR)、任务计数准确率 (Task Acc)、BERTScore F1、G-Eval (GPT-5 评分)。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **Apple M4 芯片、16GB 统一内存**的 MacBook 上运行，模拟真实的端侧资源受限环境。
*   **模型选择**：使用本地部署的 **Qwen3:8B** 作为推理后端，证明了该系统在轻量级开源模型上的有效性。
*   **训练时长**：论文未提及训练时长，因为该系统主要基于预训练模型的推理和多智能体编排，不涉及大规模二次训练。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 420 组会话实验，涵盖了视频编辑、图像处理、AI 生成、幻灯片制作等多种真实办公场景。
*   **消融实验**：论文详细对比了“无大脑规划”、“全局记忆”与“任务隔离记忆”的不同组合，验证了每个模块的贡献。
*   **客观性与公平性**：通过引入 KIR（关键信息召回）和 G-Eval（多维度人工/强模型评分），弥补了 BERTScore 在多任务场景下无法识别上下文污染的缺陷，评估维度较为全面。

### 6. 主要结论与发现
*   **极高的效率提升**：相比 Naive 基准，FOCAL 减少了 **60.4% 的 Token 消耗**和 **72.3% 的 VLM 调用**。
*   **显著的质量改善**：关键信息召回率（KIR）从 0.38 提升至 **0.61**。
*   **强大的抗干扰能力**：在任务中断场景下，Naive 方法的任务准确率几乎崩溃（0.03），而 FOCAL 仍能保持 **0.81** 的准确率，证明了任务隔离记忆的必要性。

### 7. 优点
*   **架构优雅**：通过“先规划后推理”的逻辑，极大地缓解了端侧 VLM 的计算压力。
*   **任务隔离机制**：有效解决了长时交互流中常见的上下文污染问题，使摘要更具逻辑性。
*   **隐私保护**：全流程端侧化，符合个人助理类产品的核心安全需求。
*   **Benchmark 贡献**：构建了 DesktopBench，为无指令桌面日志研究提供了标准化的评估协议。

### 8. 不足与局限
*   **硬件单一性**：目前仅在 Apple M4 平台上测试，未覆盖 Android、Windows 或更低端的嵌入式设备。
*   **模型依赖**：虽然使用了 8B 模型，但系统性能仍受限于本地模型的指令遵循能力，更小的模型（如 1B-3B）可能无法胜任 Brain Agent 的复杂规划。
*   **长时会话挑战**：实验会话平均长度约为 17 个动作，对于持续数小时、包含数百个动作的超长工作流，其内存管理和延迟表现尚待验证。
*   **能耗监测缺失**：未提供具体的功耗（电池消耗）数据，这对于移动端/笔记本端部署至关重要。

（完）
