---
title: "MM-Zero: Self-Evolving Multi-Model Vision Language Models From Zero Data"
title_zh: MM-Zero：基于零数据的自我进化多模型视觉语言模型
authors: "Zongxia Li, Hongyang Du, Chengsong Huang, Xiyang Wu, Lantao Yu, Yicheng He, Jing Xie, Xiaomin Wu, Zhichao Liu, Jiarui Zhang, Fuxiao Liu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09206v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 基于强化学习的视觉语言模型推理零数据自我进化框架
tldr: MM-Zero是首个实现零数据自进化的多模态大模型（VLM）推理框架。它通过强化学习（GRPO）驱动，将单一基础模型划分为提议者、编码者和求解者三个角色，分别负责生成概念、编写代码渲染图像及进行多模态推理。该方法无需初始图像数据，通过自我博弈和反馈机制显著提升了VLM在各类基准测试中的推理能力，为多模态系统的自主进化开辟了新路径。
selection_source: fresh_fetch
motivation: 现有的多模态模型自进化通常需要初始图像数据作为种子，限制了其在完全无数据环境下的自主提升能力。
method: 提出一种多角色框架，利用GRPO算法训练同一基础模型扮演提议者、编码者（生成渲染代码）和求解者，实现从零数据开始的闭环自进化。
result: 实验证明MM-Zero在多个多模态推理基准测试中均取得了显著的性能提升，验证了零数据自进化的可行性。
conclusion: MM-Zero展示了通过多角色协作和强化学习，多模态模型可以在无需外部数据的情况下实现持续的自我改进。
---

## 摘要
自我进化已成为在极少人工干预下改进大语言模型（LLM）和视觉语言模型（VLM）等基础模型的关键范式。虽然最近的方法已经证明 LLM 智能体可以在极少甚至没有数据的情况下从零开始自我进化，但 VLM 引入了额外的视觉模态，通常需要至少一些种子数据（如图像）来引导自我进化过程。在这项工作中，我们提出了 Multi-model Multimodal Zero (MM-Zero)，这是第一个为 VLM 推理实现零数据自我进化的基于强化学习（RL）的框架。超越了先前的双角色（提议者 Proposer 和求解者 Solver）设置，MM-Zero 引入了一个多角色自我进化训练框架，包含三个专门的角色：生成抽象视觉概念并提出问题的提议者（Proposer）；将这些概念转换为可执行代码（如 Python、SVG）以渲染视觉图像的代码编写者（Coder）；以及对生成的视觉内容进行多模态推理的求解者（Solver）。所有三个角色均由同一个基础模型初始化，并使用群体相对策略优化（GRPO）进行训练，同时采用了精心设计的奖励机制，整合了执行反馈、视觉验证和难度平衡。我们的实验表明，MM-Zero 在广泛的多模态基准测试中提升了 VLM 的推理性能。MM-Zero 为多模态模型的自我进化多模型系统建立了一条可扩展的路径，将自我改进的前沿扩展到了传统的双模型范式之外。

## Abstract
Self-evolving has emerged as a key paradigm for improving foundational models such as Large Language Models (LLMs) and Vision Language Models (VLMs) with minimal human intervention. While recent approaches have demonstrated that LLM agents can self-evolve from scratch with little to no data, VLMs introduce an additional visual modality that typically requires at least some seed data, such as images, to bootstrap the self-evolution process. In this work, we present Multi-model Multimodal Zero (MM-Zero), the first RL-based framework to achieve zero-data self-evolution for VLM reasoning. Moving beyond prior dual-role (Proposer and Solver) setups, MM-Zero introduces a multi-role self-evolving training framework comprising three specialized roles: a Proposer that generates abstract visual concepts and formulates questions; a Coder that translates these concepts into executable code (e.g., Python, SVG) to render visual images; and a Solver that performs multimodal reasoning over the generated visual content. All three roles are initialized from the same base model and trained using Group Relative Policy Optimization (GRPO), with carefully designed reward mechanisms that integrate execution feedback, visual verification, and difficulty balancing. Our experiments show that MM-Zero improves VLM reasoning performance across a wide range of multimodal benchmarks. MM-Zero establishes a scalable path toward self-evolving multi-model systems for multimodal models, extending the frontier of self-improvement beyond the conventional two-model paradigm.

---

## 论文详细总结（自动生成）

### MM-Zero：基于零数据的自我进化多模型视觉语言模型总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在**完全不依赖外部数据**（无人类标注、无预存图像种子）的情况下，实现视觉语言模型（VLM）推理能力的自我进化？
*   **研究动机**：
    *   现有的 LLM 自进化（如 R-Zero）已实现零数据突破，但 VLM 通常仍需初始图像集作为“种子”，这构成了数据瓶颈。
    *   人工标注和图像过滤成本高昂，且静态数据集限制了模型进化的上限。
    *   通过程序化生成（代码渲染）合成视觉数据具有无限的可扩展性和多样性，类似于机器人仿真环境。

#### 2. 方法论：三角色协作框架
MM-Zero 将单一基础模型（Base Model）通过强化学习（GRPO）训练为三个专门角色，形成闭环：
*   **核心思想**：利用代码（SVG/Python）作为桥梁，将抽象的语言描述转化为具体的视觉图像，从而在无外部输入的情况下创造训练场景。
*   **关键角色**：
    1.  **提议者 (Proposer)**：生成视觉描述（Caption）、简单问题（用于验证渲染正确性）、简单答案及困难问题（用于训练推理）。
    2.  **编码者 (Coder)**：将提议者的描述转化为可执行代码（如 SVG），渲染成图像。
    3.  **求解者 (Solver)**：针对生成的图像回答困难问题，通过思维链（CoT）进行多模态推理。
*   **算法流程与奖励机制**：
    *   使用**群体相对策略优化 (GRPO)** 算法。
    *   **提议者奖励**：结合代码执行成功率、求解者对简单问题的准确率（验证图像质量）、困难问题的难度得分（基于求解者的一致性，遵循“金发姑娘原则”即难度适中）以及内容多样性惩罚。
    *   **编码者奖励**：侧重于代码的可执行性和渲染图像的语义准确性。
    *   **求解者奖励**：采用测试时强化学习（TTRL），通过多数投票生成伪标签，并强制执行 `<think>` 格式。

#### 3. 实验设计
*   **基础模型**：Qwen3-VL-4B-Instruct、Qwen3-VL-8B-Instruct、MiMo-VL-7B-SFT。
*   **评估基准 (Benchmarks)**：
    *   **通用视觉理解**：MMMU, MMMU-Pro, ChartQA, MM-Vet。
    *   **视觉数学推理**：MathVerse, MathVision, MathVista, VisNumBench。
    *   **幻觉检测**：HallusionBench, MMSI。
*   **对比与评估**：对比各模型的 Base 版本，观察随训练迭代（Iteration 1-5）性能的变化。使用 Qwen2.5-14B-Instruct 作为评判员（LLM-as-a-judge）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX 6000/Pro (96GB)** GPU。
*   **训练细节**：每个角色每轮训练 20 个 step，总计 60 个 step（前三轮迭代）。
*   **推理优化**：使用 vLLM 服务进行并行渲染和推理。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 8B 不同参数规模的模型，验证了框架的泛化性。
*   **迭代深度**：主实验展示了 3 轮迭代，补充实验扩展到了 5 轮，证明了性能尚未饱和。
*   **消融实验**：针对“难度平衡因子”和“内容多样性因子”进行了专门的消融研究，验证了各奖励组件的必要性。
*   **客观性**：实验设置了严格的过滤机制（如剔除太简单或太难的样本），并使用了多样性惩罚防止模型陷入奖励作弊（Reward Hacking）。

#### 6. 主要结论与发现
*   **零数据可行性**：MM-Zero 证明了 VLM 可以在完全没有外部图像的情况下，仅通过自我博弈提升推理能力。
*   **性能提升**：在 8B 模型上，视觉数学推理等任务平均提升了约 4 个百分点，且性能随迭代次数单调增长。
*   **能力涌现**：随着训练进行，编码者生成的图像布局更整洁，提议者提出的问题从简单的数值提取进化为复杂的多步组合推理。
*   **规模效应**：基础模型越强（如 8B vs 4B），自进化的效率和最终效果越好，因为更强的模型能生成更高质量的初始代码。

#### 7. 优点与亮点
*   **架构创新**：打破了传统的“提议者-求解者”双角色范式，引入“编码者”解决了视觉模态的生成难题。
*   **完全自主**：实现了真正的“零数据”启动，摆脱了对互联网图像数据的依赖。
*   **可验证性**：通过代码执行和简单问题校验，为 VLM 训练提供了客观的反馈信号。

#### 8. 不足与局限
*   **算力成本**：虽然不需要数据，但强化学习过程中的大规模采样和渲染对算力要求较高，难以在超大规模模型（如 38B+）上轻易复现。
*   **场景局限**：目前主要依赖代码渲染（SVG/Python），生成的图像多为图表、几何、流程图等，对于复杂的自然场景（如真实世界照片理解）的提升可能有限。
*   **模型偏差**：如果基础模型在代码生成上有严重缺陷，可能会导致自进化循环失效（如 4B 模型的提升幅度明显小于 8B）。

（完）
