---
title: "VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning"
title_zh: "VLA-Thinker: 通过“以图思考”推理增强视觉-语言-动作模型"
authors: "Chaoyang Wang, Wenrui Bao, Sicheng Gao, Bingxin Xu, Yu Tian, Yogesh S. Rawat, Yunhao Ge, Yuzhang Shang"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14523v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 基于 GRPO 的强化学习用于视觉-语言-动作推理
tldr: 本研究针对具身智能中的视觉-语言-动作（VLA）模型，提出了VLA-Thinker框架。该框架引入了“以图思考”推理机制，将感知建模为可动态调用的推理动作，解决了传统模型将视觉输入视为静态背景的局限。通过SFT冷启动和GRPO强化学习两阶段训练，模型在LIBERO和RoboTwin 2.0基准测试中表现卓越，显著提升了复杂长程任务的成功率。
selection_source: fresh_fetch
motivation: 现有VLA模型多依赖静态视觉输入的文本推理，限制了模型在长程任务中主动感知环境和消除歧义的能力。
method: 提出一种将感知作为动态推理动作的框架，结合视觉思维链SFT冷启动与GRPO强化学习来优化推理-动作轨迹。
result: "在LIBERO基准测试中实现了97.5%的成功率，并在RoboTwin 2.0等长程机器人任务中取得了显著的性能增益。"
conclusion: 通过将感知整合为动态推理过程并利用强化学习对齐，VLA-Thinker显著增强了具身智能模型在复杂环境下的操作性能。
---

## 摘要
视觉-语言-动作（VLA）模型在具身智能方面展现出巨大潜力，但现有大多数方法依赖于基于文本的思维链推理，其中视觉输入被视为静态上下文。这限制了模型在长程任务中主动重新审视环境并消除歧义的能力。我们提出了 VLA-Thinker，这是一个“以图思考”推理框架，它将感知建模为一种可动态调用的推理动作。为了训练该系统，我们引入了一个两阶段训练流水线，包括：(1) 使用精选视觉思维链数据的 SFT 冷启动阶段，以激活结构化推理和工具使用行为；(2) 基于 GRPO 的强化学习，使完整的推理-动作轨迹与任务级成功保持一致。在 LIBERO 和 RoboTwin 2.0 基准测试上的广泛实验表明，VLA-Thinker 显著提升了操作性能，在 LIBERO 上实现了 97.5% 的成功率，并在长程机器人任务中取得了显著提升。项目与代码：https://cywang735.github.io/VLA-Thinker/ 。

## Abstract
Vision-Language-Action (VLA) models have shown promising capabilities for embodied intelligence, but most existing approaches rely on text-based chain-of-thought reasoning where visual inputs are treated as static context. This limits the ability of the model to actively revisit the environment and resolve ambiguities during long-horizon tasks. We propose VLA-Thinker, a thinking-with-image reasoning framework that models perception as a dynamically invocable reasoning action. To train such a system, we introduce a two-stage training pipeline consisting of (1) an SFT cold-start phase with curated visual Chain-of-Thought data to activate structured reasoning and tool-use behaviors, and (2) GRPO-based reinforcement learning to align complete reasoning-action trajectories with task-level success. Extensive experiments on LIBERO and RoboTwin 2.0 benchmarks demonstrate that VLA-Thinker significantly improves manipulation performance, achieving 97.5% success rate on LIBERO and strong gains across long-horizon robotic tasks. Project and Codes: https://cywang735.github.io/VLA-Thinker/ .

---

## 论文详细总结（自动生成）

以下是对论文《VLA-Thinker: Boosting Vision-Language-Action Models through Thinking-with-Image Reasoning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在引入思维链（CoT）推理时，大多局限于“以文本为中心”的范式。视觉观察通常被一次性编码为静态上下文，而推理仅在语言空间展开。
*   **研究动机**：这种设计导致模型在面对长程（Long-horizon）操作任务时，无法主动重新审视环境以消除歧义或从中间错误中恢复。人类在决策时是主动、迭代且紧密耦合感知的，而现有模型感知是被动的。
*   **整体含义**：本研究旨在打破静态感知瓶颈，提出一种“以图思考”的推理框架，将视觉感知视为推理过程中的一个显式、可动态调用的“动作”，从而实现感知-推理-动作的深度协同。

### 2. 方法论
*   **核心思想**：将感知建模为一种显式的推理操作（Tool Invocation）。在生成最终动作前，模型可以根据需要调用视觉工具（如 `ZOOM-IN`）来获取更精细的局部图像，并将其作为新的证据反馈到推理链中。
*   **关键技术细节**：
    *   **多模态交织推理**：模型生成的轨迹包含：文本推理步骤（Thought）、感知调用（Tool Call）、返回的视觉证据（Visual Evidence）和最终环境动作（Action）。
    *   **两阶段训练流水线**：
        1.  **SFT 冷启动阶段**：利用 Qwen3-VL-30B 合成高质量的具身 CoT 数据（包含关键帧检测、工具调用逻辑和文本推理），通过监督微调激活模型的结构化推理和工具使用能力。
        2.  **RL 轨迹对齐阶段**：采用 **GRPO（Group Relative Policy Optimization）** 算法。该算法通过组内相对优势进行优化，无需显式的价值函数（Value Function），能够有效处理稀疏奖励下的长程推理-动作轨迹对齐。
*   **奖励函数**：$R(\tau) = \alpha_s \cdot I_{success} + \alpha_f \cdot I_{format}$。其中 $I_{success}$ 是任务成功信号，$I_{format}$ 是推理格式正则化项。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，用于评估长程操作和知识迁移。
    *   **RoboTwin 2.0**：双臂协作机器人基准，涵盖 12 个代表性任务，按步数分为短程、中程、长程和超长程（最高达 637 步）。
*   **对比方法**：
    *   **基准模型**：OpenVLA-OFT（本文的 Backbone）。
    *   **主流 VLA 模型**：π0, RT-1, Octo, DeepThinkVLA, RDT, CoT-VLA, UnifiedVLA 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练时长**：整个训练过程（SFT + RL）大约耗时 **3 天**。
*   **超参数**：SFT 阶段 Batch Size 为 64，学习率 $1 \times 10^{-5}$；RL 阶段 Batch Size 为 128，学习率 $2 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 5 个子集上进行了全面测试，每个任务评估 50 个随机初始场景。
    *   在 RoboTwin 2.0 的 12 个任务上进行了测试，每个任务评估 100 个随机初始场景。
    *   **消融实验**：对比了“仅 SFT”、“仅 RL”以及“完整 VLA-Thinker”的效果，验证了两阶段训练的必要性。
*   **充分性与客观性**：实验覆盖了从单臂到双臂、从短程到超长程的多种场景，并提供了详细的训练曲线（成功率与推理长度的变化），结果显示模型在 RL 阶段学会了“按需调用”工具，减少了冗余推理，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能突破**：VLA-Thinker 在 LIBERO 基准上达到了 **97.5%** 的平均成功率，比强基准 OpenVLA-OFT 提升了 **6.5%**。
*   **长程任务优势**：在 RoboTwin 2.0 的长程和超长程任务中，VLA-Thinker 的领先优势随任务复杂度增加而扩大，证明了动态感知对减少误差累积的关键作用。
*   **推理效率优化**：通过 GRPO 训练，模型不仅学会了如何推理，还学会了“何时不推理”，即在信息充足时直接输出动作，从而优化了推理成本。

### 7. 优点（亮点）
*   **范式创新**：首次在 VLA 模型中实现了“以图思考”，将感知从静态输入转变为动态推理动作。
*   **训练稳定性**：结合 SFT 冷启动和 GRPO 算法，成功解决了多模态推理在稀疏奖励下难以收敛的问题。
*   **泛化能力**：在多种不同形态的机器人和任务中均表现出极强的鲁棒性，尤其是在需要精细空间定位的任务中。

### 8. 不足与局限
*   **工具多样性有限**：目前仅验证了 `ZOOM-IN`（放大查看）这一种视觉工具，尚未探索物体分割、点云查询等更复杂的感知工具。
*   **推理开销**：由于引入了自回归推理过程，其推理时间比纯反应式模型（如 OpenVLA-OFT）增加了约 **19%**，在极高频控制场景下可能存在挑战。
*   **幻觉风险**：作为基于预训练多模态大模型（MLLM）的方法，仍可能继承 MLLM 的空间幻觉问题，导致错误的工具调用或推理。

（完）
