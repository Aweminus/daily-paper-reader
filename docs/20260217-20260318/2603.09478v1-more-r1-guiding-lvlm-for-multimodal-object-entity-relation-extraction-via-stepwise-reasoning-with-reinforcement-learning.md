---
title: "MORE-R1: Guiding LVLM for Multimodal Object-Entity Relation Extraction via Stepwise Reasoning with Reinforcement Learning"
title_zh: MORE-R1：通过强化学习的逐步推理引导大视觉语言模型进行多模态物体-实体关系抽取
authors: "Xiang Yuan, Xu Chu, Xinrong Chen, Haochen Li, Zonghong Dai, Hongcheng Fan, Xiaoyue Yuan, Weiping Li, Tong Mo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09478v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 为大视觉语言模型引入强化学习引导的分步推理
tldr: 针对多模态物体-实体关系抽取（MORE）任务中复杂推理不足的问题，本文提出MORE-R1模型。该模型通过引入显式分步推理和强化学习（RL），提升了大视觉语言模型（LVLM）的处理能力。研究设计了两阶段训练：首先利用自动构建的高质量分步推理数据集进行冷启动微调，随后采用GRPO算法及渐进式样本混合策略优化硬样本推理。实验证明，MORE-R1在MORE基准测试中显著超越现有基准，达到SOTA水平。
selection_source: fresh_fetch
motivation: 现有的多模态关系抽取方法缺乏中间推理透明度，且在处理复杂跨模态场景时表现不佳。
method: 提出一种结合冷启动监督微调与强化学习（GRPO）的两阶段训练框架，引导模型进行显式的分步推理。
result: 在MORE基准测试上取得了显著的性能提升，刷新了当前的最先进水平。
conclusion: 通过强化学习优化分步推理过程，能有效增强大视觉语言模型在复杂多模态关系抽取任务中的推理能力和准确性。
---

## 摘要
多模态物体-实体关系抽取（MORE）是信息抽取研究中一项具有挑战性的任务。它旨在识别视觉物体与文本实体之间的关系，需要复杂的多模态理解和跨模态推理能力。现有方法主要基于分类或不含推理的生成，难以处理 MORE 任务中的复杂抽取场景，且在可扩展性和中间推理透明度方面存在局限。为了应对这些挑战，我们提出了 MORE-R1，这是一种引入显式逐步推理与强化学习（RL）的新型模型，旨在使大视觉语言模型（LVLM）能够有效地处理 MORE 任务。MORE-R1 集成了一个两阶段训练过程，包括初始的有监督微调（SFT）冷启动训练阶段，以及随后的推理能力优化强化学习阶段。在初始阶段，我们设计了一种高效的方法来自动构建高质量的 SFT 数据集，其中包含针对 MORE 任务定制的细粒度逐步推理，使模型能够学习有效的推理范式。在随后的阶段，我们采用了群体相对策略优化（GRPO）强化学习算法，并结合渐进式样本混合策略，以稳定训练并进一步增强模型在困难样本上的推理能力。在 MORE 基准测试上的全面实验表明，MORE-R1 达到了最先进的性能，相比基准模型有显著提升。

## Abstract
Multimodal Object-Entity Relation Extraction (MORE) is a challenging task in information extraction research. It aims to identify relations between visual objects and textual entities, requiring complex multimodal understanding and cross-modal reasoning abilities. Existing methods, mainly classification-based or generation-based without reasoning, struggle to handle complex extraction scenarios in the MORE task and suffer from limited scalability and intermediate reasoning transparency. To address these challenges, we propose MORE-R1, a novel model that introduces explicit stepwise reasoning with Reinforcement Learning (RL) to enable Large Vision-Language Model (LVLM) to address the MORE task effectively. MORE-R1 integrates a two-stage training process, including an initial cold-start training stage with Supervised Fine-Tuning (SFT) and a subsequent RL stage for reasoning ability optimization. In the initial stage, we design an efficient way to automatically construct a high-quality SFT dataset containing fine-grained stepwise reasoning tailored to the MORE task, enabling the model to learn an effective reasoning paradigm. In the subsequent stage, we employ the Group Relative Policy Optimization (GRPO) RL algorithm with a Progressive Sample-Mixing Strategy to stabilize training and further enhance model's reasoning ability on hard samples. Comprehensive experiments on the MORE benchmark demonstrate that MORE-R1 achieves state-of-the-art performance with significant improvement over baselines.

---

## 论文详细总结（自动生成）

### MORE-R1：通过强化学习的逐步推理引导大视觉语言模型进行多模态关系抽取

#### 1. 核心问题与研究动机
*   **研究背景**：多模态物体-实体关系抽取（MORE）旨在识别图像中的视觉物体与文本中的实体之间的关系。这要求模型具备极强的跨模态对齐能力（如将图像红框中的球员与文本中的球队名关联）和复杂的逻辑推理能力。
*   **核心问题**：
    1.  **现有方法局限**：传统基于分类的方法可扩展性差（新增关系需重绘分类层），且缺乏推理透明度；简单的生成式方法（直接输出标签）在处理复杂语义（如区分“对手”与“队友”）时表现不佳。
    2.  **推理缺失**：现有大视觉语言模型（LVLM）在关系抽取任务上往往缺乏显式的推理链，导致决策过程不可见且准确率受限。

#### 2. 方法论：两阶段训练框架
MORE-R1 采用了“冷启动有监督微调（SFT）+ 强化学习（RL）优化”的两阶段方案：
*   **核心思想**：引导 LVLM（以 Qwen2.5-VL 为底座）生成显式的逐步推理（Chain-of-Thought），并通过强化学习探索更优的推理路径。
*   **阶段 1：冷启动 SFT**
    *   **自动数据构建**：利用 GPT-4o 作为专家模型，为 25% 的训练数据生成包含 6 个步骤的细粒度推理路径（图像分析、跨模态相关性、跨模态对齐、实体类型识别、关系过滤、最终判定）。
    *   **目标**：让模型初步习得 MORE 任务的推理范式。
*   **阶段 2：强化学习（RL）优化**
    *   **GRPO 算法**：采用群体相对策略优化（Group Relative Policy Optimization），通过组内相对得分计算优势函数，无需额外的价值模型（Value Model），计算效率高。
    *   **奖励函数设计**：由格式奖励（是否符合 `<think>` 标签）、长度奖励（鼓励深度思考）和答案奖励（标签是否正确）组成。
    *   **渐进式样本混合策略（关键创新）**：根据 SFT 模型的表现将样本分为“简单”和“困难”。在 RL 训练中，随 epoch 增加逐渐提高困难样本的比例，以稳定训练并攻克复杂场景。

#### 3. 实验设计
*   **数据集**：使用 **MORE 基准测试**，包含 20,264 个样本，涵盖 21 种关系类型（包括 `none`）。
*   **对比方法**：
    *   **分类模型**：MOREformer, HVFormer, MMIB, CGI-MRE, FocalMRE, REMOTE 等。
    *   **生成式模型**：LLaMA3.2-Vision (Zero-shot), Qwen2.5-VL (Zero-shot 及直接 SFT 版本)。
*   **评估指标**：准确率 (Acc)、精确率 (P)、召回率 (R) 和 F1 分数。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **模型底座**：Qwen2.5-VL-7B-Instruct。
*   **训练时长**：
    *   阶段 1 (SFT)：约 **2 小时**。
    *   阶段 2 (RL)：约 **24 小时**。
*   **推理效率**：在单张 A100 上，测试集（3,036 样本）推理约需 7 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了主流 SOTA 方法的对比、不同训练阶段的消融实验、以及针对 RL 采样策略的对比实验。
*   **消融实验**：详细对比了仅 SFT、不同样本混合比例（α 值）对结果的影响，验证了渐进式策略的必要性。
*   **客观性**：实验设置了多种 Baseline（包括同底座的直接微调版本），对比公平，且针对 MORE 任务的类别不平衡问题，重点分析了 F1 分数，评估较为客观。

#### 6. 主要结论与发现
*   **性能突破**：MORE-R1 在所有指标上均显著超越现有 SOTA。相比最强分类模型 REMOTE，F1 分数提升了 **6.1%**；相比直接微调标签的 LVLM，F1 提升了 **13.8%**。
*   **推理价值**：显式的逐步推理不仅提高了准确性，还显著提升了模型在复杂、易混淆关系上的辨析能力。
*   **RL 策略有效性**：渐进式样本混合策略能有效防止模型在 RL 过程中因直接挑战硬样本而导致的训练不稳定或性能退化。

#### 7. 优点与亮点
*   **透明度高**：通过 `<think>` 标签展示推理过程，使关系抽取过程具有可解释性。
*   **创新策略**：提出的“渐进式样本混合”解决了 RL 在处理多模态复杂任务时难以收敛的问题。
*   **可扩展性**：作为生成式模型，MORE-R1 无需修改架构即可适应新的关系类型，优于分类模型。

#### 8. 不足与局限
*   **数据依赖**：冷启动阶段高度依赖 GPT-4o 等闭源专家模型生成高质量推理链，成本较高且存在知识蒸馏的局限。
*   **任务覆盖**：目前仅在 MORE 一个基准数据集上验证，尚未在其他多模态关系抽取（如 MRE）或更广泛的 IE 任务上进行泛化测试。
*   **推理开销**：由于引入了长文本推理链，推理时的 Token 消耗和响应延迟高于传统的分类模型。

（完）
