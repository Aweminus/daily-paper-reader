---
title: "RetouchIQ: MLLM Agents for Instruction-Based Image Retouching with Generalist Reward"
title_zh: RetouchIQ：基于通用奖励的指令式图像修饰 MLLM 智能体
authors: "Qiucheng Wu, Jing Shi, Simon Jenni, Kushal Kafle, Tianyu Wang, Shiyu Chang, Handong Zhao"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17558v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用强化学习进行工具推理和执行的多模态大模型智能体
tldr: 本研究提出RetouchIQ框架，旨在利用多模态大模型（MLLM）智能体实现基于指令的专业图像修图。针对创意编辑中缺乏可靠奖励信号的挑战，该框架引入了一个通用的奖励模型，通过多模态推理为编辑结果提供个性化评分。研究团队构建了包含19万条指令推理对的数据集及新基准。实验证明，RetouchIQ在语义一致性和视觉质量上均优于现有系统，为可解释、可执行的专业图像编辑提供了新方案。
selection_source: fresh_fetch
motivation: 针对专业图像编辑中主观性强、缺乏可靠且可验证的强化学习奖励信号这一难题展开研究。
method: 开发了RetouchIQ框架，通过一个经强化学习微调的通用奖励模型为MLLM智能体提供基于多模态推理的标量反馈。
result: 实验结果显示，该方法在语义一致性和感知质量方面均显著超越了此前基于MLLM和扩散模型的编辑系统。
conclusion: 研究证明了通用奖励驱动的MLLM智能体能够作为灵活、可解释且具备执行能力的专业图像编辑助手。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展在将视觉语言推理扩展到基于专业工具的图像编辑方面展现了巨大潜力，实现了直观且具创造性的编辑。一个有前景的方向是利用强化学习（RL）使 MLLMs 能够在专业图像编辑软件中推理并执行最优的工具使用计划。然而，由于缺乏能够反映创意编辑本质上主观性的可靠、可验证的奖励信号，训练仍然具有挑战性。在这项工作中，我们介绍了 RetouchIQ，这是一个通过通用奖励模型引导的 MLLM 智能体来执行基于指令的可执行图像编辑的框架。RetouchIQ 解释用户指定的编辑意图并生成相应的、可执行的图像调整，将高层审美目标与精确的参数控制联系起来。为了超越传统的、基于规则的奖励（即使用手工指标计算与固定参考图像的相似度），我们提出了一个通用奖励模型，这是一个经过强化学习微调的 MLLM，它通过一组针对具体情况生成的指标来评估编辑结果。随后，该奖励模型通过多模态推理提供标量反馈，从而实现具有高质量、指令一致梯度的强化学习。我们策划了一个包含 19 万个指令-推理对的扩展数据集，并为基于指令的图像编辑建立了一个新的基准。实验表明，与之前的基于 MLLM 和基于扩散模型的编辑系统相比，RetouchIQ 在语义一致性和感知质量上都有显著提高。我们的研究结果证明了通用奖励驱动的 MLLM 智能体作为专业图像编辑的灵活、可解释且可执行助手的潜力。

## Abstract
Recent advances in multimodal large language models (MLLMs) have shown great potential for extending vision-language reasoning to professional tool-based image editing, enabling intuitive and creative editing. A promising direction is to use reinforcement learning (RL) to enable MLLMs to reason about and execute optimal tool-use plans within professional image-editing software. However, training remains challenging due to the lack of reliable, verifiable reward signals that can reflect the inherently subjective nature of creative editing. In this work, we introduce RetouchIQ, a framework that performs instruction-based executable image editing through MLLM agents guided by a generalist reward model. RetouchIQ interprets user-specified editing intentions and generates corresponding, executable image adjustments, bridging high-level aesthetic goals with precise parameter control. To move beyond conventional, rule-based rewards that compute similarity against a fixed reference image using handcrafted metrics, we propose a generalist reward model, an RL fine-tuned MLLM that evaluates edited results through a set of generated metrics on a case-by-case basis. Then, the reward model provides scalar feedback through multimodal reasoning, enabling reinforcement learning with high-quality, instruction-consistent gradients. We curate an extended dataset with 190k instruction-reasoning pairs and establish a new benchmark for instruction-based image editing. Experiments show that RetouchIQ substantially improves both semantic consistency and perceptual quality over previous MLLM-based and diffusion-based editing systems. Our findings demonstrate the potential of generalist reward-driven MLLM agents as flexible, explainable, and executable assistants for professional image editing.

---

## 论文详细总结（自动生成）

以下是对论文《RetouchIQ: MLLM Agents for Instruction-Based Image Retouching with Generalist Reward》的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：尽管多模态大语言模型（MLLM）在视觉推理上表现出色，但在调用专业图像编辑工具（如 Adobe Lightroom）进行修图时面临巨大挑战。
*   **核心难题**：图像修饰具有强烈的**主观性**。传统的强化学习（RL）依赖于“可验证奖励”（如像素级相似度），但同一条指令（如“让照片更有秋天的感觉”）可以有多种同样优秀的编辑结果。传统的规则奖励无法捕捉这种审美上的灵活性，导致模型训练效果受限。
*   **整体含义**：本文旨在开发一个能够理解人类自然语言指令、进行审美推理，并精确控制专业编辑参数的智能体，同时解决创意编辑中缺乏可靠奖励信号的问题。

### 2. 方法论：核心思想与技术细节
RetouchIQ 框架包含两个核心组件：**策略模型（Policy Model）**和**通用奖励模型（Generalist Reward Model, GRM）**。
*   **策略模型工作流**：
    1.  **推理链生成**：接收图像和指令，生成描述审美意图的推理过程。
    2.  **参数化执行**：输出具体的工具参数（如曝光度+0.9，对比度-30），并在专业软件中执行。
*   **通用奖励模型 (GRM)**：
    *   **动态指标生成**：GRM 不使用固定公式，而是针对每个案例生成一组评估指标（如“色彩平衡”、“艺术氛围”）。
    *   **多模态评分**：基于生成的指标对编辑后的图像进行推理并给出标量分数。
*   **关键技术：策略引导奖励训练 (PGRT)**：
    *   为了解决合成弱样本（通过随机扰动生成）与策略模型实际产生的弱样本之间的**分布偏移**问题，PGRT 使用策略模型自身的输出作为负样本来训练奖励模型，使奖励模型能更精准地分辨细微的感知差异。
*   **训练阶段**：分为监督微调（SFT）和强化学习（RL）两个阶段，采用交替训练策略优化策略模型和奖励模型。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：策划了 19 万条“指令-推理-编辑”对，涵盖全局和局部修图任务。
    *   **奖励模型数据集**：包含 1.5 万个对比样本（强编辑 vs 弱编辑）。
*   **基准测试 (Benchmark)**：
    *   **RetouchEval**：本文建立的新基准，包含 300 个真实用户编辑案例，分为质量增强、风格转换、局部修饰三个子集。
    *   **MIT-Adobe5K**：经典的公开图像增强数据集。
*   **对比方法**：
    *   **通用 MLLM**：GPT-5, Gemini-2.5。
    *   **MLLM 智能体**：JarvisArt, MonetGPT。
    *   **扩散模型**：Flux-Pro-1.1。

### 4. 资源与算力
*   **模型底座**：策略模型和奖励模型均基于 **Qwen2.5-VL-7B**；标注器和扰动器使用 **GLM-4.5V**。
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。这在学术论文中是一个小的信息缺失，但考虑到使用了 7B 规模的模型和 19 万条数据，预计需要相当规模的计算资源（如 A100/H100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的定量对比（RetouchEval 的三个维度）和定性分析。
*   **消融实验**：针对“规则奖励 vs 通用奖励”、“有无 PGRT 训练”进行了详细对比，验证了各组件的有效性。
*   **客观性**：使用了 L1/L2 距离、语义一致性（SC）、感知质量（PQ）等多种指标，并引入了第三方高性能模型（GLM-4.5V）作为自动评估员，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **性能领先**：RetouchIQ 在语义一致性和视觉质量上显著优于现有的 MLLM 智能体和扩散模型。
*   **奖励模型的重要性**：通用奖励模型（GRM）比基于像素规则的奖励更能提升模型的审美表现。
*   **分布对齐的关键**：PGRT 显著提升了奖励模型的准确性，进而通过更精准的梯度引导提升了策略模型的性能。
*   **工具化优势**：相比扩散模型，基于工具的 RetouchIQ 能完美保留原图结构，避免了扩散过程中的内容畸变。

### 7. 优点
*   **可解释性强**：模型不仅给出结果，还提供审美推理链和可调节的专业参数。
*   **解决主观性难题**：通过动态生成指标的 GRM，为创意类任务提供了一种处理“非唯一真值”奖励的新思路。
*   **专业级控制**：直接对接 Adobe Lightroom 等专业工具，生成的参数具有实际生产价值。

### 8. 不足与局限
*   **工具依赖**：其表现高度依赖于外部编辑软件的能力范围，无法处理超出参数调节范围的任务（如复杂的物体移除或重绘）。
*   **标注偏差风险**：训练数据和评估指标高度依赖于更高级别 MLLM（如 GLM-4.5V）的标注，可能存在模型偏见。
*   **实时性挑战**：由于涉及多步推理和外部工具调用，其处理速度可能慢于端到端的轻量级修图模型。

（完）
