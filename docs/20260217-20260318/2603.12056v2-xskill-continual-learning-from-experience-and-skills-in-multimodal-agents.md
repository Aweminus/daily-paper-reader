---
title: "XSkill: Continual Learning from Experience and Skills in Multimodal Agents"
title_zh: XSkill：多模态智能体中基于经验与技能的持续学习
authors: "Guanyu Jiang, Zhaochen Su, Xiaoye Qu, Yi R. Fung"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12056v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用视觉观察进行复杂推理任务的多模态智能体
tldr: 针对多模态智能体在开放场景中工具使用效率低和编排不灵活的问题，本文提出 XSkill 框架。该框架通过从过去轨迹中提取“经验”（动作级指导）和“技能”（任务级指导）实现无需参数更新的持续学习。XSkill 利用视觉观察进行知识提取与检索，在五个基准测试中显著优于现有基线，展现了强大的零样本泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决多模态智能体在复杂推理任务中工具使用效率低下且难以通过以往经验持续自我提升的挑战。
method: 提出双流框架 XSkill，通过视觉引导的总结和跨路径评估，从历史轨迹中提取动作级经验和任务级技能。
result: 在五个跨领域基准测试中，XSkill 配合四种骨干模型均显著超越了仅使用工具或基于学习的基线方法。
conclusion: 经验与技能双流机制在优化智能体推理行为方面具有互补作用，并能显著提升其在未知任务中的泛化性能。
---

## 摘要
多模态智能体现在能够利用多样化的工具处理复杂的推理任务，但在开放式场景中仍面临工具使用效率低下和编排不够灵活的问题。一个核心挑战是如何使此类智能体在不更新参数的情况下，通过学习过去的轨迹来实现持续改进。我们确定了实现这一目标必不可少的两种互补的可重用知识形式：经验（experiences），为工具选择和决策提供简明的基础动作级指导；技能（skills），为规划和工具使用提供结构化的任务级指导。为此，我们提出了 XSkill，这是一个用于多模态智能体从经验和技能中进行持续学习的双流框架。XSkill 将知识提取和检索都建立在视觉观察的基础上。在积累阶段，XSkill 通过基于视觉的总结和跨轨迹批判，从多路径试错（rollouts）中提取并巩固经验与技能。在推理阶段，它检索这些知识并将其应用于当前的视觉上下文，并将使用历史反馈到积累过程中，从而形成持续学习闭环。在跨越不同领域的五个基准测试和四个骨干模型上的评估结果表明，XSkill 始终且显著地优于仅使用工具的基准和基于学习的基准。进一步的分析表明，这两种知识流在影响智能体的推理行为方面发挥着互补作用，并展现出卓越的零样本泛化能力。

## Abstract
Multimodal agents can now tackle complex reasoning tasks with diverse tools, yet they still suffer from inefficient tool use and inflexible orchestration in open-ended settings. A central challenge is enabling such agents to continually improve without parameter updates by learning from past trajectories. We identify two complementary forms of reusable knowledge essential for this goal: experiences, providing concise action-level guidance for tool selection and decision making, and skills, providing structured task-level guidance for planning and tool use. To this end, we propose XSkill, a dual-stream framework for continual learning from experience and skills in multimodal agents. XSkill grounds both knowledge extraction and retrieval in visual observations. During accumulation, XSkill distills and consolidates experiences and skills from multi-path rollouts via visually grounded summarization and cross-rollout critique. During inference, it retrieves and adapts this knowledge to the current visual context and feeds usage history back into accumulation to form a continual learning loop. Evaluated on five benchmarks across diverse domains with four backbone models, XSkill consistently and substantially outperforms both tool-only and learning-based baselines. Further analysis reveals that the two knowledge streams play complementary roles in influencing the reasoning behaviors of agents and show superior zero-shot generalization.

---

## 论文详细总结（自动生成）

以下是对论文《XSkill: Continual Learning from Experience and Skills in Multimodal Agents》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管多模态大模型（MLLM）智能体在工具使用上取得了进展，但在开放场景中仍面临两大瓶颈：**工具使用效率低下**（在简单任务上浪费步骤，在复杂任务上缺乏深度探索）和**工具编排僵化**（难以跨任务灵活组合工具）。
*   **核心目标**：探索如何让智能体在**不更新参数（Training-free）**的情况下，通过从过去的执行轨迹中学习来持续提升性能。
*   **核心理念**：效仿人类学习方式，将知识分为两种互补形式：
    *   **经验（Experiences）**：动作级的简明指导，用于局部的工具选择、错误恢复和决策。
    *   **技能（Skills）**：任务级的结构化程序，用于高层规划和跨任务的工具编排。

### 2. 方法论：XSkill 双流框架
XSkill 包含两个主要阶段，其核心在于将知识提取与检索锚定在**视觉观察**上：
*   **阶段 I：知识积累（Accumulation）**
    *   **多路径演练（Rollouts）**：针对训练任务进行多次独立尝试。
    *   **视觉锚定总结**：MLLM 结合视觉图像和文本日志，分析哪些视觉特征触发了特定决策（如：图像倒置触发了旋转操作）。
    *   **跨路径批判（Cross-Rollout Critique）**：对比成功与失败的轨迹，识别导致结果差异的关键因素，生成“增加”或“修改”经验的操作。
    *   **层次化巩固**：通过语义相似度合并冗余经验，并对技能文档进行精简和去重，确保知识库的可扩展性。
*   **阶段 II：推理与应用（Inference）**
    *   **任务分解检索**：将复杂查询分解为多个抽象子任务，针对每个子任务检索最相关的经验。
    *   **上下文感知适配**：根据当前任务的视觉状态（如具体的图像内容）重写检索到的经验，并将通用技能模板实例化为具体的执行计划。
    *   **非强制性注入**：将适配后的知识注入系统提示词，作为智能体的参考而非硬性约束。

### 3. 实验设计
*   **数据集/场景**：涵盖三个领域共 5 个基准测试：
    *   **视觉工具使用**：VisualToolBench, TIR-Bench。
    *   **多模态搜索**：MMSearch-Plus, MMBrowseComp。
    *   **综合任务**：AgentVista（极具挑战性的真实视觉场景）。
*   **骨干模型**：Gemini-2.5-Pro, Gemini-3-Flash, GPT-5-mini, o4-mini，以及开源模型 Qwen3-VL 系列。
*   **对比方法（Baselines）**：
    *   **仅工具基线**（w/ Tools）。
    *   **学习型基线**：Agent Workflow Memory (AWM), Dynamic CheatSheet (DC), Agent-KB。
*   **评估指标**：Average@4（4次演练的平均成功率）和 Pass@4（4次中至少成功一次的比例）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或训练时长。
*   **原因分析**：由于 XSkill 是一个**无需训练（Training-free）**的框架，其核心操作（总结、批判、推理）主要依赖于调用 MLLM 的 API。实验中使用了 OpenAI 和 Google 的闭源模型 API，以及 Qwen3-VL 开源模型进行推理分析。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个数据集上进行了广泛测试，涵盖了从视觉推理到网页浏览的多种任务。
*   **充分性**：
    *   **多模型验证**：验证了框架在不同闭源和开源模型上的表现。
    *   **消融实验**：系统性地移除了经验流、技能流以及各个阶段组件，证明了双流机制的必要性。
    *   **鲁棒性分析**：分析了演练次数（N）对性能的影响。
    *   **泛化性测试**：进行了零样本跨任务转移实验（如用 VisualToolBench 的知识解决 TIR-Bench 任务）。
*   **客观性**：对比了当前最先进的同类学习框架，并使用了标准化的工具集和评估流程，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能提升**：XSkill 在所有模型和基准测试中均显著优于基线，Average@4 提升了 2.58 到 6.71 个百分点。
*   **互补作用**：
    *   **技能（Skills）**显著减少了执行错误（如语法错误、工具名称错误），提升了工具使用的**稳健性**。
    *   **经验（Experiences）**优化了工具选择策略（如在搜索任务中更多地调用图像搜索而非纯文本搜索），提升了**灵活性**。
*   **跨模型转移**：由一个模型（如 Gemini）积累的知识可以有效地转移给另一个模型（如 GPT-5-mini），证明了知识结构的通用性。

### 7. 优点与亮点
*   **视觉锚定（Visually Grounded）**：不同于以往仅依赖文本日志的框架，XSkill 强调视觉信息在知识提取和适配中的核心作用，更符合多模态任务的本质。
*   **双流设计**：巧妙地结合了“宏观流程（技能）”与“微观技巧（经验）”，兼顾了执行的规范性与决策的灵活性。
*   **无需训练**：通过非参数化的方式实现持续学习，极大地降低了智能体自我进化的成本。
*   **零样本泛化**：展现了极强的跨领域迁移能力，能够将学到的通用推理原则应用到未见过的任务中。

### 8. 不足与局限
*   **基础模型依赖**：实验发现，当基础模型能力较弱（如 Qwen3-VL-32B）时，外部知识的注入有时会干扰其原生行为，导致性能波动。
*   **成本开销**：虽然不需要训练，但多路径演练（Rollouts）和复杂的总结/批判过程会消耗大量的 API Token，在实际应用中可能存在成本压力。
*   **迭代深度**：目前的评估主要集中在“一次积累-一次测试”的循环，对于超长周期的终身学习（Lifelong Learning）中可能出现的知识遗忘或冲突问题探讨较少。

（完）
