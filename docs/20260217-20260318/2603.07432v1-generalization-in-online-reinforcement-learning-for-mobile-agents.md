---
title: Generalization in Online Reinforcement Learning for Mobile Agents
title_zh: 移动智能体在线强化学习中的泛化研究
authors: "Li Gu, Zihuan Jiang, Zhixiang Chi, Huan Liu, Ziqiang Wang, Yuanhao Yu, Glen Berseth, Yang Wang"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07432v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 在交互式移动环境中使用强化学习训练视觉语言模型智能体
tldr: 本研究针对移动代理在图形用户界面任务中的泛化难题，将其形式化为上下文马尔可夫决策过程，并推出了AndroidWorld-Generalization基准，涵盖未见实例、模板和应用三个维度。通过结合GRPO算法与可扩展的异步训练系统，研究展示了强化学习在提升视觉语言模型代理性能方面的潜力。实验表明，虽然RL在处理新实例时表现优异，但在跨应用泛化上仍面临巨大挑战，为未来研究提供了方向。
selection_source: fresh_fetch
motivation: 移动代理在GUI任务中的泛化能力研究因缺乏标准化基准和开源强化学习系统而受到限制。
method: 将问题形式化为上下文马尔可夫决策过程，提出AndroidWorld-Generalization基准，并结合GRPO算法构建了可扩展的强化学习训练系统。
result: "强化学习使7B参数的VLM代理在未见实例上的表现提升了26.1%，但在未见模板和应用上的提升相对有限。"
conclusion: 实验揭示了移动代理在复杂泛化场景下的挑战，并证明少样本测试时自适应是提升性能的有效方向。
---

## 摘要
基于图形用户界面（GUI）的移动智能体通过解析自然语言指令并与屏幕交互，在移动设备上自动执行数字任务。虽然最近的方法应用强化学习（RL）在交互式环境中训练视觉语言模型（VLM）智能体，并主要关注性能，但由于缺乏标准化的基准测试和开源 RL 系统，泛化性仍未得到充分探索。在这项工作中，我们将该问题形式化为上下文马尔可夫决策过程（CMDP），并引入了 AndroidWorld-Generalization，这是一个包含三个日益具有挑战性的评估方案的基准测试，用于评估对未见过的任务实例、模板和应用程序的零样本泛化能力。我们进一步提出了一种 RL 训练系统，该系统将群体相对策略优化（GRPO）与可扩展的 rollout 收集系统相结合，由容器化基础设施、异步执行和错误恢复组成，以支持可靠且高效的训练。在 AndroidWorld-Generalization 上的实验表明，RL 使 7B 参数的 VLM 智能体能够超越监督微调基准，在未见过的实例上实现了 26.1% 的提升，但在未见过的模板（15.7%）和应用（8.3%）上提升有限，这凸显了泛化的挑战。作为初步尝试，我们证明了测试时的少样本自适应可以提高在未见过应用上的性能，从而激励了该方向的未来研究。为了支持可复现性和公平比较，我们开源了完整的 RL 训练系统，包括环境、任务套件、模型、提示配置以及底层基础设施。

## Abstract
Graphical user interface (GUI)-based mobile agents automate digital tasks on mobile devices by interpreting natural-language instructions and interacting with the screen. While recent methods apply reinforcement learning (RL) to train vision-language-model(VLM) agents in interactive environments with a primary focus on performance, generalization remains underexplored due to the lack of standardized benchmarks and open-source RL systems. In this work, we formalize the problem as a Contextual Markov Decision Process (CMDP) and introduce \textbf{AndroidWorld-Generalization}, a benchmark with three increasingly challenging regimes for evaluating zero-shot generalization to unseen task instances, templates, and applications. We further propose an RL training system that integrates Group Relative Policy Optimization (GRPO) with a scalable rollout collection system, consisting of containerized infrastructure and asynchronous execution % , and error recovery to support reliable and efficient training. Experiments on AndroidWorld-Generalization show that RL enables a 7B-parameter VLM agent to surpass supervised fine-tuning baselines, yielding a 26.1\% improvement on unseen instances but only limited gains on unseen templates (15.7\%) and apps (8.3\%), underscoring the challenges of generalization. As a preliminary step, we demonstrate that few-shot adaptation at test-time improves performance on unseen apps, motivating future research in this direction. To support reproducibility and fair comparison, we open-source the full RL training system, including the environment, task suite, models, prompt configurations, and the underlying infrastructure \footnote{https://github.com/zihuanjiang/AndroidWorld-Generalization}.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalization in Online Reinforcement Learning for Mobile Agents》的结构化技术总结：

### 1. 核心问题与研究动机
*   **核心问题**：移动智能体（Mobile Agents）在处理图形用户界面（GUI）任务时，如何通过在线强化学习（RL）提高对**未见场景**（Unseen Scenarios）的泛化能力。
*   **研究动机**：
    *   **泛化性研究缺失**：现有研究多关注特定基准测试的性能提升，缺乏标准化的泛化评估体系。
    *   **缺乏开源系统**：移动环境复杂、易崩溃且延迟高，目前缺乏成熟、开源且可扩展的在线 RL 训练框架。
    *   **静态数据局限**：传统的监督微调（SFT）依赖静态轨迹，无法捕捉移动环境的动态交互，导致智能体在面对 UI 变化时鲁棒性差。

### 2. 方法论
*   **问题形式化**：将移动交互建模为**上下文马尔可夫决策过程（CMDP）**。通过定义不同的上下文（Context），将任务划分为训练集和测试集，评估零样本策略迁移（ZSPT）。
*   **算法选择**：采用 **GRPO（群体相对策略优化）**。
    *   针对移动任务的稀疏奖励特性，计算轨迹级的优势函数（Advantage），并将其广播至该轨迹的所有步骤。
    *   使用 KL 散度约束策略更新，确保训练稳定性。
*   **系统架构（核心贡献之一）**：
    *   **容器化基础设施**：利用 Docker 封装 Android 模拟器，实现资源隔离和故障容错。
    *   **异步 Rollout 收集**：打破同步瓶颈，环境独立运行，VLM 策略在任何环境准备就绪时立即生成动作，显著提升 GPU 利用率和吞吐量。
    *   **课程学习**：按照任务难度（简单 -> 中等 -> 困难）逐步引导智能体学习。

### 3. 实验设计
*   **基准测试：AndroidWorld-Generalization**。基于 AndroidWorld 扩展，构建了三个难度递增的泛化维度：
    1.  **未见实例（Unseen Instance）**：相同的任务模板，不同的参数（如联系人姓名、电话）。
    2.  **未见模板（Unseen Template）**：相同应用内的全新任务类型（如从“创建闹钟”迁移到“删除闹钟”）。
    3.  **未见应用（Unseen App）**：完全陌生的应用程序（如从“日历”迁移到“相机”）。
*   **对比方法**：
    *   **闭源模型 + Prompting**：GPT-4o, Claude Computer Use。
    *   **开源 SFT 模型**：UI-TARS (7B/72B), Qwen2.5-VL 等。
    *   **RL 基准**：PPO 算法。
*   **基础模型**：以 UI-TARS-7B-SFT 为起点进行在线 RL 训练。

### 4. 资源与算力
*   **硬件配置**：使用 2 台 **NVIDIA H100-80GB GPU** 进行动作生成和权重更新。
*   **环境配置**：16 个并行 Docker 容器运行 Android 13 模拟器。
*   **训练时长**：主实验运行 2000 个训练步，总计消耗约 **480 GPU 小时**。
*   **数据规模**：每个训练步采样 2 个任务实例，每个实例收集 8 条轨迹，轨迹步数上限为 20 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 20 个应用、116 个任务模板。
*   **充分性**：
    *   **多维度评估**：不仅对比了成功率，还分析了学习曲线、任务难度分布和任务类型（信息检索 vs. 任务完成）。
    *   **消融实验**：对比了异步与同步系统的效率差异，以及规则奖励与 LLM-as-a-judge 奖励的可靠性。
    *   **算法验证**：在 GRPO 之外也测试了 PPO，证明了结论在不同 RL 算法下的普适性。
*   **客观性**：采用了严格的训练/测试集划分，确保无数据泄露，并报告了多次运行的均值和标准差。

### 6. 主要结论与发现
*   **RL 的显著作用**：RL 训练后的 7B 模型在 AndroidWorld 上的表现（49.1%）远超其 SFT 基准（23.0%），甚至超过了 72B 的 SFT 模型和 GPT-4o。
*   **泛化瓶颈**：RL 在“未见实例”上提升巨大（+26.1%），但在“未见模板”（+15.7%）和“未见应用”（+8.3%）上提升较小，说明跨应用泛化仍是极大的挑战。
*   **测试时自适应（Few-shot Adaptation）**：实验证明，在部署阶段针对新应用进行极少量的（如 8 个实例）在线微调，可将性能进一步提升 10.4%。
*   **奖励函数可靠性**：规则脚本奖励比 LLM-as-a-judge 更可靠，后者容易产生误报（False Positives），误导优化方向。

### 7. 优点与亮点
*   **系统性**：首次将移动智能体的泛化问题形式化为 CMDP，并提供了清晰的分级评估框架。
*   **工程贡献**：开发并开源了首个全栈式移动智能体 RL 训练系统，解决了模拟器不稳定和同步效率低下的痛点。
*   **实用性**：证明了小参数模型（7B）通过在线 RL 可以达到甚至超越超大规模闭源模型的性能。

### 8. 不足与局限
*   **应用覆盖有限**：虽然涵盖了 20 个应用，但相比真实世界的应用商店仍是冰山一角，泛化性的上限尚未完全探明。
*   **算力门槛**：尽管进行了优化，但训练仍需高性能 GPU 和大量 CPU 资源来运行模拟器，对个人开发者仍有挑战。
*   **任务复杂度**：在“困难”级别的任务和复杂的“信息检索”任务上，RL 的提升依然有限，未能完全解决长程规划问题。

（完）
