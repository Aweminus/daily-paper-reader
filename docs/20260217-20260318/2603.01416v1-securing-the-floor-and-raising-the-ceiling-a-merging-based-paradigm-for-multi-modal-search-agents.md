---
title: "Securing the Floor and Raising the Ceiling: A Merging-based Paradigm for Multi-modal Search Agents"
title_zh: 保底与增益：一种基于合并的多模态搜索智能体范式
authors: "Zhixiang Wang, Jingxuan Xu, Dajun Chen, Yunfang Wu, Wei Jiang, Yong Li"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01416v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用VLM和多步推理的多模态搜索智能体
tldr: 针对多模态搜索智能体训练成本高、冷启动难的问题，本文提出一种基于模型合并的免训练范式。通过将文本搜索智能体与基础视觉语言模型（VLM）进行跨模态合并，无需额外多模态训练即可赋予模型自主搜索能力。引入显著性感知合并算法OBM以减少参数干扰，实验证明该方法在零样本场景下表现稳健，且作为预训练起点能显著提升收敛速度和最终精度。
selection_source: fresh_fetch
motivation: 现有多模态搜索智能体依赖大规模监督数据或强化学习，导致训练成本高昂且存在严重的冷启动问题。
method: 提出一种跨模态模型合并范式，并引入基于显著性感知的Optimal Brain Merging (OBM)算法，通过少量校准样本识别关键参数以实现高效融合。
result: 在InfoSeek和MMSearch等基准测试中，该方法在零样本阶段表现优异，且作为热启动策略时比标准VLM初始化具有更快的收敛速度和更高的峰值准确率。
conclusion: 模型合并是构建多模态搜索智能体的一种高效且稳健的方案，既能保障基础性能，又能提升微调后的性能上限。
---

## 摘要
视觉语言模型（VLM）的最新进展推动了多模态搜索智能体的发展，这些智能体能够主动调用外部搜索工具，并通过多步推理整合检索到的证据。尽管前景广阔，但现有方法通常依赖大规模监督轨迹或昂贵的强化学习（RL），导致训练成本高、不稳定，且标准 VLM 面临严重的冷启动问题。我们提出了一种无需训练的范式，通过跨模态模型合并赋予 VLM 自主搜索能力。通过将基于文本的搜索智能体与基础 VLM 融合，我们证明了在无需任何额外多模态训练数据的情况下，可以有效地构建多模态搜索能力。为了减轻跨模态整合过程中的参数干扰，我们引入了 Optimal Brain Merging (OBM)，这是一种显著性感知合并算法，仅需少量校准样本即可根据参数对模型损失的影响识别任务关键参数。在搜索密集型基准测试（如 InfoSeek、MMSearch）上的广泛实验表明：(1) 模型合并作为零样本智能体确保了合理的性能底线，其中 OBM 实现了卓越的搜索率；(2) OBM 作为热启动策略显著提升了性能上限，与标准 VLM 初始化相比，实现了更快的收敛速度和更高的峰值准确率。

## Abstract
Recent advances in Vision-Language Models (VLMs) have motivated the development of multi-modal search agents that can actively invoke external search tools and integrate retrieved evidence through multi-step reasoning. While promising, existing approaches typically rely on large-scale supervised trajectories or expensive reinforcement learning (RL), leading to high training cost, instability, and a severe cold-start problem for standard VLMs. We propose a training-free paradigm to empower VLMs with autonomous search capabilities via cross-modal model merging. By fusing a text-based search agent with a base VLM, we show that multi-modal search capabilities can be effectively composed without any additional multi-modal training data. To mitigate parameter interference during cross-modal integration, we introduce Optimal Brain Merging (OBM), a saliency-aware merging algorithm that identifies task-critical parameters based on their impact on model loss using only a small set of calibration samples. Extensive experiments on search-intensive benchmarks (e.g., InfoSeek, MMSearch) reveal that: (1) Model merging secures a reasonable performance floor as a zero-shot agent, with OBM achieving superior search rates; (2) OBM significantly raises the performance ceiling as a warm-start strategy, achieving faster convergence and higher peak accuracy than standard VLM initialization.

---

## 论文详细总结（自动生成）

这篇论文提出了一种通过模型合并（Model Merging）构建多模态搜索智能体的新范式，旨在解决现有方法在训练成本、稳定性和冷启动方面的挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何高效地赋予视觉语言模型（VLM）自主调用外部搜索工具（如图像/文本搜索）并进行多步推理的能力。
*   **研究背景**：现有的多模态搜索智能体主要依赖大规模监督微调（SFT）或强化学习（RL）。然而，获取高质量搜索推理轨迹成本极高，且标准 VLM 在 RL 训练初期缺乏搜索意识，存在严重的“冷启动”问题。
*   **核心思路**：研究者发现搜索逻辑（存在于 LLM 中）和视觉感知（存在于 VLM 中）可以互补。因此，论文提出通过**跨模态模型合并**，将预训练的文本搜索智能体与 VLM 在参数层面融合，实现能力的“无缝组合”。

### 2. 方法论：Optimal Brain Merging (OBM)
*   **核心思想**：在参数空间将文本搜索智能体（如 Search-R1）与 VLM（如 Qwen2.5-VL）的语言模块进行融合，同时保留 VLM 的视觉编码器和投影器。
*   **关键技术：OBM 算法**：
    *   **显著性驱动的稀疏化（Saliency-driven Sparsification）**：借鉴 Optimal Brain Damage 理论，利用二阶泰勒展开（Hessian 矩阵）评估参数对损失函数的影响。通过少量校准样本的单次前向传播，计算激活感知的参数显著性，识别出对跨模态推理至关重要的参数。
    *   **显著性加权符号共识（Saliency-weighted Sign Consensus）**：当不同模型的参数方向冲突时，不再仅凭数值大小决定，而是根据显著性权重进行投票，解决跨模态整合中的参数干扰。
*   **流程**：定义任务向量（Task Vectors） $\rightarrow$ 计算显著性得分 $\rightarrow$ 剪枝非关键参数 $\rightarrow$ 解决符号冲突 $\rightarrow$ 合并生成新模型。

### 3. 实验设计
*   **数据集**：使用了四个搜索密集型基准测试：**FVQA-test**（知识密集型）、**InfoSeek**（信息寻求型）、**MMSearch**（新闻与稀有知识）、**LiveVQA**（时效性新闻推理）。
*   **对比方法**：
    *   **基准线**：直接回答（DA）、提示词增强（Qwen-VL-R1）、固定 RAG 工作流。
    *   **训练基准**：经过强化学习训练的 MMSearch-R1。
    *   **合并算法对比**：Task Arithmetic (TA)、TIES-merging、DARE。
*   **评估指标**：准确率（Acc）、搜索率（SR，包括图像和文本搜索的调用频率）。

### 4. 资源与算力
*   **硬件设备**：论文提到在合并过程中使用了 **21张 H20 (96G) GPU**（主要用于处理大规模参数的加载与计算）。
*   **效率表现**：
    *   **合并耗时**：OBM 的显著性计算约需 2 分钟，整体合并过程在几分钟内完成。
    *   **训练加速**：作为 RL 的热启动策略，OBM 仅需 5 个训练步即可达到其他模型 1 个 epoch 的性能，显著降低了算力消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了零样本（Zero-shot）性能评估和强化学习训练动态分析两个阶段。
*   **消融实验**：针对校准数据类型（纯文本 vs. 多模态）进行了对比，验证了多模态校准对保持模态对齐的必要性。
*   **客观性**：实验对比了多种主流合并算法，并在多个不同侧重点的公开基准上进行测试，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **保底（Securing the Floor）**：模型合并在无需任何额外训练的情况下，建立了一个强大的零样本性能底线，使模型具备了初步的自主搜索本能。
*   **增益（Raising the Ceiling）**：将合并后的模型作为 RL 的起点（热启动），不仅收敛速度极快，且最终达到的峰值准确率显著高于从标准 VLM 开始训练的模型。
*   **OBM 的优越性**：相比传统的 TIES 或 DARE，OBM 能更好地处理跨模态参数干扰，避免了随机剪枝导致的视觉通路中断。

### 7. 优点
*   **高效免训练**：提供了一种无需昂贵多模态数据训练即可构建智能体的方案。
*   **解决冷启动**：通过参数注入直接赋予模型搜索逻辑，跳过了 RL 初期的盲目探索阶段。
*   **理论支撑强**：OBM 算法基于二阶优化理论，比单纯的数值平均或随机剪枝更具科学性。

### 8. 不足与局限
*   **模态偏好偏差**：零样本阶段的模型表现出“重文本、轻图像”的搜索倾向，图像搜索的调用效率仍有提升空间。
*   **血缘依赖**：该方法要求合并的模型具有相同的基座模型（如都源自 Qwen2.5），限制了在完全异构模型间的应用。
*   **校准依赖**：虽然校准集很小，但其分布仍可能影响最终合并模型的特定任务表现。

（完）
