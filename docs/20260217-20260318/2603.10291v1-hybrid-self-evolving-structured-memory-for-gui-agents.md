---
title: Hybrid Self-evolving Structured Memory for GUI Agents
title_zh: 面向 GUI 智能体的混合自演化结构化记忆
authors: "Sibo Zhu, Wenyi Wu, Kun Zhou, Stephen Wang, Biwei Huang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10291v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 具有结构化记忆的基于视觉语言模型的GUI智能体
tldr: 针对GUI智能体在处理长流程、复杂界面及错误恢复时的局限性，本文提出HyMEM，一种受人脑启发的混合自进化结构化记忆系统。该系统通过图结构耦合离散符号节点与连续轨迹嵌入，支持多跳检索、节点更新及动态工作记忆刷新。实验证明，HyMEM显著提升了开源模型性能，使其在多个基准测试中超越了GPT-4o等闭源模型。
selection_source: fresh_fetch
motivation: 现有的GUI智能体外部记忆多采用扁平化检索，缺乏人类记忆的结构化组织和自进化特性，难以应对复杂的真实世界任务。
method: 提出HyMEM图记忆框架，将高层符号节点与底层轨迹嵌入结合，通过图更新操作实现记忆的自我演化与多跳检索。
result: "HyMEM使Qwen2.5-VL-7B性能提升22.5%，在GUI任务上超越了Gemini-1.5-Pro和GPT-4o等顶尖闭源模型。"
conclusion: 结构化且可自进化的混合记忆机制是提升GUI智能体处理复杂、长程任务能力的关键路径。
---

## 摘要
视觉语言模型（VLM）的显著进展使得 GUI 智能体能够以类人的方式与计算机进行交互。然而，由于长程工作流、多样化的界面以及频繁的中间错误，现实世界的计算机使用任务仍然具有挑战性。先前的研究通过大量轨迹集合为智能体构建外部记忆，但依赖于对离散摘要或连续嵌入的扁平化检索，未能实现人类记忆的结构化组织和自演化特性。受大脑启发，我们提出了混合自演化结构化记忆（HyMEM），这是一种基于图的记忆，它将离散的高层符号节点与连续的轨迹嵌入相结合。HyMEM 维持一种图结构，以支持多跳检索、通过节点更新操作实现的自演化，以及推理过程中的即时工作记忆刷新。大量实验表明，HyMEM 持续提升了开源 GUI 智能体的性能，使 7B/8B 参数规模的主干网络能够媲美或超越强大的闭源模型；值得注意的是，它将 Qwen2.5-VL-7B 的性能提升了 22.5%，并优于 Gemini2.5-Pro-Vision 和 GPT-4o。

## Abstract
The remarkable progress of vision-language models (VLMs) has enabled GUI agents to interact with computers in a human-like manner. Yet real-world computer-use tasks remain difficult due to long-horizon workflows, diverse interfaces, and frequent intermediate errors. Prior work equips agents with external memory built from large collections of trajectories, but relies on flat retrieval over discrete summaries or continuous embeddings, falling short of the structured organization and self-evolving characteristics of human memory. Inspired by the brain, we propose Hybrid Self-evolving Structured Memory (HyMEM), a graph-based memory that couples discrete high-level symbolic nodes with continuous trajectory embeddings. HyMEM maintains a graph structure to support multi-hop retrieval, self-evolution via node update operations, and on-the-fly working-memory refreshing during inference. Extensive experiments show that HyMEM consistently improves open-source GUI agents, enabling 7B/8B backbones to match or surpass strong closed-source models; notably, it boosts Qwen2.5-VL-7B by +22.5% and outperforms Gemini2.5-Pro-Vision and GPT-4o.

---

## 论文详细总结（自动生成）

以下是对论文《Hybrid Self-evolving Structured Memory for GUI Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
GUI 智能体（如自动化操作电脑或手机的 AI）在处理长流程任务、应对多样化界面以及从中间错误中恢复时仍面临巨大挑战。现有的记忆机制通常采用“扁平化”检索，即简单地存储和检索离散的文本摘要或连续的向量嵌入。这种方式缺乏人类记忆的两个关键特性：**结构化组织**（将细节与高层概念关联）和**自演化能力**（根据新经验不断更新知识库）。论文旨在通过模仿人脑海马体（存储多模态细节）与新皮层（提取统计规律和符号）的协作机制，构建一种混合且可自我进化的图结构记忆系统。

### 2. 方法论：HyMEM 核心思想与技术细节
HyMEM（Hybrid Self-evolving Structured Memory）通过以下三个维度构建：

*   **混合图结构（Hybrid Graph Schema）**：
    *   **高层策略节点（Discrete Strategy）**：存储启发式策略（如“按价格从低到高过滤”）。
    *   **中层属性节点（Attributes）**：语义标签（如 #搜索、#价格），用于连接相关轨迹。
    *   **底层轨迹嵌入（Continuous Embeddings）**：利用轻量级 VLM 将整个交互序列压缩为连续向量，保留细粒度的视觉和动作细节。
*   **自演化构建机制（Self-Evolving Construction）**：
    *   当新轨迹产生时，系统通过 VLM 判定其与现有记忆的关系，执行三种操作：**ADD**（新增不同策略）、**MERGE**（合并相似策略但补充新细节）、**REPLACE**（用更高效、步骤更少的轨迹替换旧节点）。
*   **推理时的动态利用**：
    *   **结构化检索**：不仅基于相似度，还通过图结构进行“多跳”扩展，获取更具多样性的相关经验。
    *   **即时刷新（On-the-fly Refresh）**：在执行过程中，VLM 会检测任务是否进入新阶段（如从“搜索”转入“结账”），并实时触发记忆重检索和刷新，防止上下文漂移。

### 3. 实验设计
*   **数据集/场景**：
    *   **WebVoyager**：涵盖 Amazon、Google Maps 等真实网站的端到端任务。
    *   **Multimodal-Mind2Web**：评估跨域（OOD）泛化能力。
    *   **MMInA**：多步多模态互联网任务基准。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-2.5-Pro-Vision, Claude-4。
    *   **开源基座**：Qwen2.5-VL-7B, Qwen3-VL-8B, UI-TARS-1.5-7B 等。
    *   **记忆增强变体**：纯文本记忆（ReasoningBank, AWM）、纯连续记忆（CoMEM）、纯离散记忆。

### 4. 资源与算力
*   **训练细节**：使用了 **1 台 NVIDIA H100 GPU**。
*   **训练时长**：整个训练过程在 **20 小时内** 完成。
*   **参数效率**：采用 LoRA 结合 Q-Former 的微调方式，仅更新了总参数量的 **1.2%**。
*   **数据规模**：初始记忆库包含 2,883 条成功轨迹，后续通过自动数据飞轮扩展。

### 5. 实验数量与充分性
*   **实验规模**：在三个主流 Benchmark 上进行了全面测试，涵盖了购物、旅游、信息检索等多个领域。
*   **消融实验**：
    *   验证了**全局演化**（长期记忆整合）与**局部演化**（工作记忆刷新）的各自贡献。
    *   分析了**记忆库规模**（从 500 到 8000 条轨迹）对性能的缩放效应。
    *   探讨了检索策略中**相似度与多样性**的权衡。
*   **评价**：实验设计较为充分，通过跨模型（7B/8B）和跨基准的对比，证明了方法的普适性和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：HyMEM 显著提升了开源小模型的表现。例如，Qwen2.5-VL-7B 的成功率提升了 **22.5%**。
*   **超越闭源模型**：搭载 HyMEM 的 7B/8B 模型在平均性能上**超越了 GPT-4o 和 Gemini-2.5-Pro-Vision**，证明了结构化记忆在弥补模型参数规模劣势方面的巨大潜力。
*   **图结构的优势**：相比扁平检索，基于图的多跳检索能提供更具策略指导意义的上下文，而不仅仅是视觉相似的片段。
*   **自演化的必要性**：动态刷新机制解决了长程任务中“上下文失效”的问题。

### 7. 优点
*   **脑启发式架构**：巧妙结合了离散符号的逻辑性和连续嵌入的感知细节，平衡了抽象推理与精确操作。
*   **高效演化**：通过 ADD/MERGE/REPLACE 机制，实现了记忆的“去重”与“提纯”，避免了记忆库无限膨胀导致的检索效率下降。
*   **即插即用**：作为一种外部增强模块，可以适配多种不同的 VLM 主干网络。

### 8. 不足与局限
*   **更新策略依赖启发式**：目前的记忆更新决策（ADD/MERGE/REPLACE）主要依赖 VLM 的启发式判断，而非通过强化学习（RL）进行端到端优化。
*   **模型规模覆盖**：实验主要集中在 7B/8B 规模的模型，尚未在更大规模（如 32B 或 70B）的模型上验证其增益是否会边际递减。
*   **计算开销**：虽然训练高效，但在推理时进行多次 VLM 判定（如检测阶段切换）会增加额外的推理延迟和 Token 消耗。

（完）
