---
title: "NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models"
title_zh: NS-VLA：迈向神经符号视觉-语言-动作模型
authors: "Ziyue Zhu, Shangyang Wu, Shuai Zhao, Zhiqiu Zhao, Shengjie Li, Yi Wang, Fang Li, Haoran Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09542v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 通过在线强化学习构建的神经符号视觉-语言-动作框架
tldr: 针对视觉-语言-动作（VLA）模型在可重用原语学习、数据依赖及探索能力方面的局限，本文提出NS-VLA框架。该框架结合神经符号学习与在线强化学习，通过符号编码器提取结构化原语，利用符号求解器进行高效动作序列化，并借助强化学习优化生成。实验证明，NS-VLA在单样本训练、抗干扰能力及零样本泛化性上均优于现有方法，显著提升了机器人的操作效率与探索空间。
selection_source: fresh_fetch
motivation: 现有的VLA模型面临原语重用性差、对大规模数据依赖性强以及难以在演示之外进行自主探索等挑战。
method: 提出一种结合神经符号编码器、符号求解器和在线强化学习的框架，实现结构化特征提取与高效动作序列优化。
result: 在机器人操作基准测试中，该方法在单样本学习、鲁棒性和零样本泛化能力方面均表现出超越前代模型的性能。
conclusion: NS-VLA通过神经符号与强化学习的结合，为构建高数据效率、强泛化能力且具备自主探索能力的机器人控制模型提供了有效路径。
---

## 摘要
视觉-语言-动作（VLA）模型旨在将指令与视觉上下文关联，并为机器人操作生成动作序列。尽管近期取得了进展，VLA 模型在学习相关且可重用的原语（primitives）、减少对大规模数据和复杂架构的依赖，以及实现超越演示（demonstrations）的探索方面仍面临挑战。为了应对这些挑战，我们提出了一种通过在线强化学习（RL）实现的创新神经符号视觉-语言-动作（NS-VLA）框架。该框架引入了一个符号编码器来嵌入视觉和语言特征并提取结构化原语，利用符号求解器进行高效的数据动作排序，并利用在线强化学习通过广泛探索来优化生成。在机器人操作基准测试上的实验表明，NS-VLA 在单样本（one-shot）训练和数据扰动设置下均优于以往的方法，同时展现出卓越的零样本泛化能力、高数据效率和扩展的探索空间。我们的代码已开源。

## Abstract
Vision-Language-Action (VLA) models are formulated to ground instructions in visual context and generate action sequences for robotic manipulation. Despite recent progress, VLA models still face challenges in learning related and reusable primitives, reducing reliance on large-scale data and complex architectures, and enabling exploration beyond demonstrations. To address these challenges, we propose a novel Neuro-Symbolic Vision-Language-Action (NS-VLA) framework via online reinforcement learning (RL). It introduces a symbolic encoder to embedding vision and language features and extract structured primitives, utilizes a symbolic solver for data-efficient action sequencing, and leverages online RL to optimize generation via expansive exploration. Experiments on robotic manipulation benchmarks demonstrate that NS-VLA outperforms previous methods in both one-shot training and data-perturbed settings, while simultaneously exhibiting superior zero-shot generalizability, high data efficiency and expanded exploration space. Our code is available.

---

## 论文详细总结（自动生成）

这是一份关于论文《NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型主要采用端到端的黑盒架构，面临三大挑战：
    1.  **缺乏结构感知**：难以捕捉机器人操作中可重用的原语（Primitives）逻辑，导致泛化性差。
    2.  **数据依赖性强**：高度依赖大规模演示数据和复杂的模型架构，在小样本场景下表现不佳。
    3.  **探索能力有限**：大多基于行为克隆（BC），限制了模型在演示数据之外自主探索环境的能力。
*   **核心目标**：构建一个结合“神经感知”与“符号推理”的框架，通过在线强化学习（RL）提升模型的数据效率、鲁棒性和探索空间。

### 2. 方法论
NS-VLA 框架由三个紧密耦合的部分组成：
*   **神经符号编码器（Neuro-Symbolic Encoder）**：
    *   利用预训练 VLM（如 Qwen3-VL）提取视觉语言特征。
    *   **计划生成**：将自然语言指令转化为结构化的符号原语序列（如 `pick(mug) -> place_on(plate)`）。
    *   **符号分类器**：引入**单调约束（Monotone Constraint）**，确保计划指针只能停留在当前步骤或前进到下一步，有效防止了动作切换时的抖动。
*   **符号求解器（Symbolic Solver）**：
    *   **视觉令牌稀疏化（Visual Token Sparsification）**：通过查询驱动的 Top-K 注意力机制，仅保留与当前原语相关的视觉特征，大幅降低计算冗余。
    *   **动作生成器**：使用轻量级因果 Transformer，根据稀疏视觉上下文和符号状态生成**动作块（Action Chunks）**，实现高效的实时控制。
*   **在线强化学习优化（Online RL Optimization）**：
    *   采用 **GRPO（Group Relative Policy Optimization）** 算法。
    *   **原语分段奖励设计**：结合了任务成功奖励、分段里程碑奖励以及基于势能的进度塑造奖励（Progress Shaping），引导模型在稀疏奖励环境下进行有效探索。
    *   **KL 锚定**：在优化过程中加入 KL 散度约束，防止模型偏离行为克隆的参考策略，确保训练稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：标准的机器人操作基准测试。
    *   **LIBERO-Plus**：包含光照、纹理、布局等 7 种环境扰动的鲁棒性测试集。
    *   **CALVIN**：用于评估长程（Long-horizon）任务和零样本泛化能力。
*   **对比方法（Baselines）**：
    *   包括 OpenVLA, $\pi_0$, UniVLA, VLA-Adapter, ACT, Diffusion Policy 等主流 VLA 模型。
*   **实验设置**：
    *   **全量数据训练**：验证模型上限。
    *   **单样本（1-shot）训练**：每个任务仅提供一个演示视频，验证数据效率。
    *   **零样本泛化**：在 CALVIN 的 ABC→D 划分上测试。

### 4. 资源与算力
*   **模型规模**：实验中测试了基于 Qwen3-VL 的 2B、4B 和 8B 不同参数规模的版本。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但提到其符号求解器通过稀疏化技术提升了推理效率，且在线 RL 阶段仅更新轻量级模块（分类器和动作生成器），冻结了 VLM 主干。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三大基准测试，进行了全量数据、单样本、抗干扰、零样本泛化等多个维度的对比。
*   **消融实验**：针对原语分类器（P.C.）、视觉提取器（V.E.）、动作生成器（A.G.）和强化学习（R.L.）分别做了消融，验证了各组件的必要性。
*   **客观性**：实验采用了多随机种子取平均值的方法，并在 LIBERO-Plus 这种极具挑战性的扰动环境下进行测试，结果显示 NS-VLA 在性能衰减上远小于基准模型，体现了较强的客观性和说服力。

### 6. 主要结论与发现
*   **卓越的数据效率**：在 1-shot 设置下，NS-VLA 的成功率远超 OpenVLA 等模型，且性能较全量数据训练时下降最少。
*   **极强的鲁棒性**：在面对视觉和空间扰动时，NS-VLA 能够通过符号原语锁定关键目标，保持高成功率。
*   **更广的探索空间**：可视化结果表明，在线 RL 使得 NS-VLA 生成的轨迹覆盖范围比端到端方法更广，具备更强的环境适应性。
*   **零样本能力**：在 CALVIN 任务中，NS-VLA 的 5 连任务成功率比现有 SOTA 提升了 11.2%。

### 7. 优点
*   **架构创新**：成功将神经符号推理引入 VLA 领域，解决了端到端模型“知其然而不知其所以然”的问题。
*   **效率优化**：通过视觉令牌稀疏化和动作分块生成，显著降低了计算开销。
*   **训练稳定**：提出的 JGRPO 优化目标结合了组相对优势和 KL 约束，解决了 VLA 在线强化学习易崩溃的难题。

### 8. 不足与局限
*   **原语依赖**：目前仍需手动定义动作原语（如 pick, place），虽然论文提到了未来可自动发现原语，但当前版本对人工先验仍有一定依赖。
*   **Sim-to-Real 验证缺失**：实验全部在仿真环境（Simulation）中完成，尚未在真实物理机器人上进行大规模验证。
*   **复杂任务定义**：对于极其精细或非结构化的操作任务，如何定义合适的符号原语可能存在挑战。

（完）
