---
title: Specificity-aware reinforcement learning for fine-grained open-world classification
title_zh: 针对细粒度开放世界分类的特异性感知强化学习
authors: "Samuele Angheben, Davide Berasi, Alessandro Conti, Elisa Ricci, Yiming Wang"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03197v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 利用强化学习引导多模态大模型的细粒度分类推理
tldr: 针对开放世界细粒度图像分类中大模型预测过于泛化的问题，本文提出了SpeciaRL强化学习框架。该框架通过引入基于验证器的动态奖励信号，在保持预测准确性的同时，引导模型生成更具体、更细致的类别描述。实验证明，SpeciaRL在多个细粒度基准测试中实现了准确性与特异性的最佳平衡，显著提升了模型的细粒度理解能力。
selection_source: fresh_fetch
motivation: 现有的多模态大模型在细粒度分类时倾向于给出过于笼统的预测，尽管它们本身具备相关的领域知识。
method: 提出SpeciaRL框架，利用基于在线采样最佳预测的动态验证器奖励信号来微调模型，以平衡准确性与特异性。
result: 在广泛的细粒度基准测试中，SpeciaRL在准确性和特异性之间取得了最佳权衡，性能超越了现有方法。
conclusion: 通过强化学习引导模型追求特异性，可以有效解决开放世界细粒度分类中的预测泛化问题并提升模型性能。
---

## 摘要
在开放世界设定下（即没有预定义的标签集）对细粒度视觉概念进行分类，要求模型既要准确又要具有特异性。近期的推理型多模态大模型 (LMMs) 展现出强大的视觉理解能力，但在执行细粒度图像分类时往往会产生过于泛化的预测。我们的初步分析表明，模型确实具备内在的细粒度领域知识。然而，在不损害正确性 (correctness) 的前提下提升预测的特异性 (specificity) 仍然是一个具有挑战性且研究不足的问题。在这项工作中，我们研究了如何引导推理型 LMM 做出既正确又具有特异性的预测。我们提出了一种新颖的特异性感知强化学习框架 SpeciaRL，用于在开放世界设定下的细粒度图像分类任务中微调推理型 LMM。SpeciaRL 引入了一种动态的、基于验证器的奖励信号，该信号锚定在在线展开 (online rollouts) 中的最佳预测上，在提升特异性的同时尊重模型的能力边界，以防止产生错误的预测。我们的域外实验表明，SpeciaRL 在广泛的细粒度基准测试中实现了正确性与特异性之间的最佳权衡，超越了现有方法并推动了开放世界细粒度图像分类的发展。代码和模型已在 https://github.com/s-angheben/SpeciaRL 公开。

## Abstract
Classifying fine-grained visual concepts under open-world settings, i.e., without a predefined label set, demands models to be both accurate and specific. Recent reasoning Large Multimodal Models (LMMs) exhibit strong visual understanding capability but tend to produce overly generic predictions when performing fine-grained image classification. Our preliminary analysis reveals that models do possess the intrinsic fine-grained domain knowledge. However, promoting more specific predictions (specificity) without compromising correct ones (correctness) remains a non-trivial and understudied challenge. In this work, we investigate how to steer reasoning LMMs toward predictions that are both correct and specific. We propose a novel specificity-aware reinforcement learning framework, SpeciaRL, to fine-tune reasoning LMMs on fine-grained image classification under the open-world setting. SpeciaRL introduces a dynamic, verifier-based reward signal anchored to the best predictions within online rollouts, promoting specificity while respecting the model's capabilities to prevent incorrect predictions. Our out-of-domain experiments show that SpeciaRL delivers the best trade-off between correctness and specificity across extensive fine-grained benchmarks, surpassing existing methods and advancing open-world fine-grained image classification. Code and model are publicly available at https://github.com/s-angheben/SpeciaRL.

---

## 论文详细总结（自动生成）

### 针对细粒度开放世界分类的特异性感知强化学习 (SpeciaRL) 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：多模态大模型（LMMs）在开放世界细粒度图像分类中表现出“过于泛化”的倾向（例如，将“金翅莺”仅识别为“鸟”）。
*   **研究动机**：
    *   **知识潜能与表现的鸿沟**：初步分析发现，推理型 LMM（如 Qwen2.5VL）实际上具备细粒度领域知识（通过多次采样可以得到正确答案），但在单次推理时倾向于保守、通用的回答。
    *   **特异性与准确性的权衡**：简单地通过提示词要求模型“具体化”或使用标准监督微调（SFT），虽然提升了特异性（Specificity），但往往会导致错误率上升，损害了正确性（Correctness）。
    *   **缺乏动态反馈**：现有的强化学习方法通常采用静态奖励（对就给分，错就不给），无法处理开放世界中“正确但程度不同”的预测。

#### 2. 方法论：SpeciaRL 框架
*   **核心思想**：利用强化学习（RL）引导模型在自身能力范围内追求“最具体”的预测，通过动态调整奖励阈值来平衡特异性与正确性。
*   **关键技术细节**：
    *   **预测分类体系**：引入 LLM 验证器（LLM-as-a-judge），将预测结果与真值（GT）的关系分为六类：错误 (W)、弃权 (A)、泛化 (G)、较低特异性 (S-)、特异 (S)、更高特异性 (S+)。
    *   **特异性感知动态奖励**：
        *   在每次训练迭代中，模型对同一图像进行 $N$ 次采样（Rollouts）。
        *   **动态阈值 $c^*$**：根据这 $N$ 次采样中表现最好的类别来设定当前样本的奖励门槛。
        *   **奖励函数**：如果当前预测的特异性等级 $\succeq c^*$，则给予正奖励（1），否则为 0。这种设计确保了模型只在有能力做到更具体时才受到激励，避免了在知识盲区强行猜测导致的错误。
    *   **优化算法**：采用 **GRPO（Group Relative Policy Optimization）** 算法，这是一种无需评论员（Critic）模型、计算效率高的在线强化学习方法。

#### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：仅使用 CUB-200-2011（鸟类细粒度数据集）的 3000 个样本。
    *   **测试集（域外/跨领域评估）**：
        *   **细粒度组**：Flowers102（花）、Food101（食物）、OxfordPets（宠物）。
        *   **极细粒度组**：StanfordCars（汽车品牌/型号/年份）、FGVCAircraft（飞机型号）。
*   **对比方法**：
    *   **零样本方法**：CaSED（检索增强）、InternVL2.5、Qwen2.5VL。
    *   **提示词工程**：在 Prompt 中加入 "Be specific"。
    *   **微调基准**：SFT（监督微调）、RFT（带静态奖励的强化学习微调）。
*   **评估指标**：特异性得分、正确性得分，以及两者的调和平均值（HM）。

#### 4. 资源与算力
*   **硬件配置**：使用了 **6 台 NVIDIA A100 (80GB/64GB) GPU**。
    *   4 台用于执行训练循环（基于 Verl 框架）。
    *   2 台用于 LLM 验证器（LLM-as-a-judge）的推理。
*   **训练时长**：SpeciaRL 的训练过程大约耗时 **12 小时**。
*   **软件优化**：使用了 **vLLM** 推理引擎和 **前缀缓存（Prefix Caching）** 技术，将验证速度提升了数倍。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个代表性细粒度数据集上进行了详尽测试，并涵盖了从 3B 到 8B 不同规模的模型。
*   **消融实验**：非常充分。包括：
    *   不同奖励规则（静态 vs 动态）的对比。
    *   采样次数 $N$ 的影响。
    *   不同强化学习优化器（GRPO, Dr.GRPO, DAPO）的兼容性。
    *   训练数据规模和领域分布的影响。
    *   LLM 验证器的鲁棒性分析（注入噪声测试）。
*   **客观性**：采用“域外训练-域外测试”的设定，证明了模型学到的是一种“推理行为”而非简单的知识记忆，实验设计客观公平。

#### 6. 主要结论与发现
*   **最佳权衡**：SpeciaRL 在所有基准测试中均实现了特异性和正确性之间的最佳平衡（HM 指标最高）。
*   **推理链优化**：定性分析显示，SpeciaRL 不仅改变了最终答案，还显著改善了模型的思维链（CoT），使其推理过程更关注细粒度视觉特征。
*   **跨领域泛化**：尽管只在鸟类数据上训练，模型在飞机、汽车、食物等完全不同的领域也表现出更强的细粒度分类能力。

#### 7. 优点
*   **创新奖励机制**：动态奖励阈值解决了 RL 在开放式生成任务中难以定义“好坏”的痛点。
*   **高效性**：利用 GRPO 和 vLLM 优化，使得在有限算力下微调 7B 规模的多模态模型变得可行。
*   **泛化能力强**：通过强化学习引导模型“如何思考”而非“记住什么”，实现了极佳的跨领域迁移。

#### 8. 不足与局限
*   **验证器依赖**：高度依赖于一个更强大的 LLM（如 Llama3-72B）作为裁判，如果裁判本身在某些极细粒度领域存在偏见或知识盲区，会影响训练效果。
*   **科学名称偏好**：由于在 CUB（鸟类）上训练，模型有时会过度倾向于输出拉丁语科学名称（如 Felis Catus 而非 Cat），这在某些应用场景下可能不符合用户习惯。
*   **错误风险**：虽然缓解了正确性下降，但在追求极高特异性时，模型仍有小概率产生“自信的错误猜测”。

（完）
