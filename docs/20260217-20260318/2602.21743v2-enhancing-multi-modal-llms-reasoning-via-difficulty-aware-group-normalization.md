---
title: Enhancing Multi-Modal LLMs Reasoning via Difficulty-Aware Group Normalization
title_zh: 通过难度感知的组归一化增强多模态大语言模型的推理能力
authors: "Jinghan Li, Junfeng Fang, Jinda Lu, Yuan Wang, Xiaoyan Guo, Tianyu Zhang, Xiang Wang, Xiangnan He"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21743v2"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 使用 GRPO 进行多模态推理的强化学习
tldr: 针对多模态大模型在强化学习（GRPO）中因极端样本导致的标准差归一化不稳定问题，本文提出了Durian方法。该方法通过视觉熵和模型置信度定义样本难度，并基于难度级别对样本进行重新分组归一化。实验证明，该方法在保留组内差异的同时消除了极端样本的干扰，显著提升了多模态推理性能。
selection_source: fresh_fetch
motivation: 多模态模型在强化学习中易受感知和推理错误产生的极端样本影响，导致传统的基于标准差的归一化方法在训练时出现不稳定。
method: 提出难度感知分组归一化（Durian），利用视觉熵衡量感知复杂度并结合模型置信度量化推理难度，按难度重新分组共享标准差。
result: 在多个多模态推理基准测试中取得了显著的性能提升，证明了该方法能有效应对复杂视觉推理任务中的极端样本干扰。
conclusion: 通过引入难度感知的归一化策略，可以有效解决多模态强化学习的训练不稳定性，是增强多模态大模型推理能力的有效途径。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 和组相对策略优化 (GRPO) 显著提升了大语言模型的推理能力。然而，将这些方法扩展到多模态场景面临着一个关键挑战：基于标准差的归一化存在不稳定性，容易受到具有极高或极低奖励的极端样本的干扰。与纯文本大语言模型不同，多模态模型对这类干扰尤为敏感，因为感知误差和推理误差都会影响其响应。为解决这一问题，我们通过难度来表征每个样本，难度由感知复杂度（通过视觉熵衡量）和推理不确定性（通过模型置信度捕捉）定义。基于这一表征，我们提出了难度感知的组归一化 (Durian)，它根据难度等级对样本进行重新分组，并在每个组内共享标准差。我们的方法在消除对极端情况敏感性的同时，保留了 GRPO 的组内区分度，在多个多模态推理基准测试中取得了显著的性能提升。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) and Group Relative Policy Optimization (GRPO) have significantly advanced the reasoning capabilities of large language models. Extending these methods to multimodal settings, however, faces a critical challenge: the instability of std-based normalization, which is easily distorted by extreme samples with nearly positive or negative rewards. Unlike pure-text LLMs, multimodal models are particularly sensitive to such distortions, as both perceptual and reasoning errors influence their responses. To address this, we characterize each sample by its difficulty, defined through perceptual complexity (measured via visual entropy) and reasoning uncertainty (captured by model confidence). Building on this characterization, we propose difficulty-aware group normalization (Durian), which re-groups samples by difficulty levels and shares the std within each group. Our approach preserves GRPO's intra-group distinctions while eliminating sensitivity to extreme cases, yielding significant performance gains across multiple multimodal reasoning benchmarks.

---

## 论文详细总结（自动生成）

这篇论文提出了 **Durian**（Difficulty-Aware Group Normalization），旨在解决多模态大语言模型（MLLM）在强化学习训练中的不稳定性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在具有可验证奖励的强化学习（RLVR）中，**GRPO**（组相对策略优化）算法依赖于基于标准差（std）的奖励归一化。然而，这种机制对“极端样本”（即某一组内的回答几乎全部正确或全部错误）极其敏感。
*   **研究动机**：
    *   当奖励趋于一致（全0或全1）时，标准差极小，会导致优势函数（Advantage）被异常放大，从而使模型过度拟合这些极端样本。
    *   在多模态场景下，由于**感知复杂度**（看错图）和**推理不确定性**（想错逻辑）的双重影响，这种极端样本出现的频率远高于纯文本模型，导致训练极不稳定。

### 2. 方法论：Durian 难度感知分组归一化
Durian 的核心思想是：**不再仅在单个问题的采样组内计算标准差，而是根据样本难度重新分组，在难度相似的样本间共享标准差。**

*   **感知难度评估（数据中心视角）**：
    *   利用视觉编码器提取图像补丁（Patch）特征。
    *   计算特征的协方差矩阵并进行特征值分解。
    *   计算特征值分布的**香农熵（Entropy）**。熵值越高，代表图像视觉结构越复杂，感知难度越大。
*   **推理难度评估（模型中心视角）**：
    *   利用模型生成响应时的**Token 级对数概率（Log-probabilities）**。
    *   计算多次采样（Rollouts）的平均序列对数概率，以此作为**模型置信度**。置信度越低，推理难度越大。
*   **重新分组与归一化**：
    *   根据感知熵和推理置信度的分位数（如前 25%、后 25%）将 Batch 内的样本分为不同难度组。
    *   在同难度组内共享标准差计算优势函数，最后通过加权组合原始 GRPO 优势、感知难度优势和推理难度优势：
        $A_{Combined} = \alpha_{Ori} A_{GRPO} + \alpha_{Percep} A_{Perceptual} + \alpha_{Reason} A_{Reasoning}$

### 3. 实验设计
*   **基准测试（Benchmarks）**：涵盖了 5 个主流榜单，包括视觉推理（MathVerse, MathVision, MathVista, WeMath）和视觉感知（HallusionBench）。
*   **数据集**：主要在 **Geometry3K**（2.1K 样本）上训练，并在 **ViRL39k** 上进行了扩展验证。
*   **对比方法**：
    *   闭源模型：GPT-4o, Claude-3.5-Sonnet。
    *   开源模型：InternVL-2.5-8B, LLaVA-OneVision-7B 等。
    *   RLVR 相关模型：R1-VL, Vision-R1, OpenVLThinker 等。
    *   消融基准：Vanilla GRPO 和 DAPO。

### 4. 资源与算力
*   **硬件资源**：使用了 **8 台 NVIDIA H20 (96G) GPU**。
*   **训练框架**：基于 EasyR1 强化学习框架。
*   **超参数**：学习率 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 512，每个问题的采样数（Rollout Size）为 8。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了详尽的对比实验，涵盖了从 7B 规模模型（Qwen2.5-VL-7B）的性能提升到不同训练数据规模的验证。
*   **消融实验**：分别验证了“仅感知分组”、“仅推理分组”以及“两者结合”的效果，证明了双重维度的互补性。
*   **敏感性分析**：对分组数量（b）、加权系数（$\alpha$）以及采样数（Rollout Size）进行了参数扰动测试。
*   **充分性评价**：实验设计客观、公平，通过对错误样本的深入分析（验证熵与错误率的相关性），增强了方法论的可信度。

### 6. 主要结论与发现
*   **性能提升显著**：Durian 在 Qwen2.5-VL 基础上实现了平均 **11.3%** 的性能提升，尤其在 MathVision 上提升超过 16%。
*   **稳定性增强**：通过共享同难度样本的标准差，有效缓解了极端样本导致的梯度爆炸或训练偏移问题。
*   **小数据见大效**：仅使用 2.1K 的 Geometry3K 数据进行强化学习，其效果即可超越许多使用大规模蒸馏数据的模型。

### 7. 优点与亮点
*   **理论联系实际**：敏锐捕捉到了 GRPO 在多模态下的数学不稳定性，并给出了直观的解释。
*   **创新的难度度量**：引入谱分析（特征值熵）来量化图像感知难度，比单纯的像素统计更具语义代表性。
*   **即插即用**：该方法作为一种归一化策略，可以轻易集成到现有的 GRPO 或 PPO 变体中。

### 8. 不足与局限
*   **超参数依赖**：虽然实验证明其对参数不敏感，但引入了 3 个加权系数和分组阈值，增加了调参成本。
*   **计算开销**：虽然感知难度的特征值分解计算量相对较小，但在超大规模 Batch 下仍会增加额外的预处理时间。
*   **视觉编码器依赖**：感知难度的准确性高度依赖于预训练视觉编码器（如 Qwen2.5-VL 的视觉部分）提取特征的质量。

（完）
