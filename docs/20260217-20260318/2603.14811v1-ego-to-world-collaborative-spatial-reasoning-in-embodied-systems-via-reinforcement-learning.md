---
title: "Ego to World: Collaborative Spatial Reasoning in Embodied Systems via Reinforcement Learning"
title_zh: 从自我到世界：通过强化学习在具身系统中实现协作空间推理
authors: "Heng Zhou, Li Kang, Yiran Qin, Xiufeng Song, Ao Yu, Zilu Zhang, Haoming Song, Kaixin Xu, Yuchen Fan, Dongzhan Zhou, Xiaohong Liu, Ruimao Zhang, Philip Torr, Lei Bai, Zhenfei Yin"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14811v1"
tags: ["query:vlm-rl"]
score: 10.0
evidence: 用于视觉语言模型空间推理的强化学习
tldr: 本研究针对具身多智能体系统中局部视角受限的挑战，提出了Ego-to-World (E2W) 基准测试，涵盖全局计数、空间推理和动作抓取任务。为此开发了CoRL框架，结合思维链微调与强化学习，并引入跨视图空间奖励（CVSR）以提供密集反馈。实验证明CoRL在推理和感知指标上显著优于现有模型，并成功应用于真实世界多机器人协作场景，为分布式场景理解提供了新方案。
selection_source: fresh_fetch
motivation: 具身智能体常受限于局部视角的遮挡和歧义，亟需一种能融合多个异构视角以实现全局空间推理的能力。
method: 提出CoRL框架，通过思维链微调和基于GRPO的强化学习，利用跨视图空间奖励（CVSR）将推理步骤与视觉证据紧密关联。
result: CoRL在E2W基准测试中超越了强力基准模型，并在外部基准测试和真实多相机机器人操作中展现出优异的泛化与执行能力。
conclusion: E2W基准和CoRL框架为从分布式局部观察中学习全局场景理解奠定了基础，有效推动了协作式具身智能的发展。
---

## 摘要
从分布式的局部视角理解世界是具身多智能体系统面临的一项根本挑战。每个智能体通过自我中心视角感知环境，而这种视角通常受限于遮挡和歧义。为了研究这一问题，我们引入了 Ego-to-World (E2W) 基准测试，旨在评估视觉语言模型在三项任务中融合异构视角的能力：(i) 全局计数，(ii) 关系位置推理，以及 (iii) 需要预测特定视角图像坐标的面向动作的抓取。针对这一设定，我们提出了 CoRL，这是一个结合了思维链（Chain-of-Thought）有监督微调与使用组相对策略优化（Group-Relative Policy Optimization）的强化学习的两阶段框架。其核心组件跨视图空间奖励（CVSR）通过将推理步骤与视觉证据相联系，提供密集的任务对齐反馈，确保连贯的跨视图实体解析，并引导模型做出正确的最终预测。在 E2W 上的实验表明，CoRL 在推理和感知定位指标上均持续超越了强大的闭源和开源基线模型，消融实验进一步证实了 CVSR 各个组件的必要性。此外，CoRL 能够泛化到外部空间推理基准测试，并能利用校准后的多摄像机装置实现有效的真实世界多机器人操控，展示了跨视图定位以及成功的抓取与放置执行。总之，E2W 和 CoRL 为从分布式的自我中心观察中学习以世界为中心的场景理解提供了原则性基础，推动了协作具身智能的发展。

## Abstract
Understanding the world from distributed, partial viewpoints is a fundamental challenge for embodied multi-agent systems. Each agent perceives the environment through an ego-centric view that is often limited by occlusion and ambiguity. To study this problem, we introduce the Ego-to-World (E2W) benchmark, which evaluates a vision-language model's ability to fuse heterogeneous viewpoints across three tasks: (i) global counting, (ii) relational location reasoning, and (iii) action-oriented grasping that requires predicting view-specific image coordinates. To address this setting, we propose CoRL, a two-stage framework that combines Chain-of-Thought supervised fine-tuning with reinforcement learning using Group-Relative Policy Optimization. Its core component, the Cross-View Spatial Reward (CVSR), provides dense task-aligned feedback by linking reasoning steps to visual evidence, ensuring coherent cross-view entity resolution, and guiding the model toward correct final predictions. Experiments on E2W show that CoRL consistently surpasses strong proprietary and open-source baselines on both reasoning and perception-grounding metrics, while ablations further confirm the necessity of each CVSR component. Beyond that, CoRL generalizes to external spatial reasoning benchmarks and enables effective real-world multi-robot manipulation with calibrated multi-camera rigs, demonstrating cross-view localization and successful grasp-and-place execution. Together, E2W and CoRL provide a principled foundation for learning world-centric scene understanding from distributed, ego-centric observations, advancing collaborative embodied AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《Ego to World: Collaborative Spatial Reasoning in Embodied Systems via Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**具身多智能体系统中的协作空间推理**问题。
*   **研究动机**：在现实场景中，单个机器人（智能体）的视角（自我中心视角，Ego-centric view）往往受限于遮挡、视角狭窄或歧义，难以完全理解复杂环境。虽然全局摄像头可以提供全景，但其部署成本高且容易产生透视失真。
*   **核心问题**：如何让多个机器人通过融合各自局部的、异构的观测，构建出一致的“世界中心”（World-centric）场景理解，并据此进行复杂的空间推理（如计数、关系判断）和精确的动作执行（如抓取）。

### 2. 方法论：CoRL 框架
论文提出了 **CoRL (Collaborative Spatial Reasoning Reinforcement Learning)**，一个两阶段的训练框架：

*   **核心思想**：先通过有监督微调（SFT）赋予模型基础的推理逻辑，再通过强化学习（RL）利用密集的空间反馈信号进一步优化跨视图的对齐与定位能力。
*   **关键技术细节**：
    1.  **阶段一：CoT-SFT**：使用带有思维链（Chain-of-Thought）标注的数据进行微调，让模型学会“先思考、后回答”的模式。
    2.  **阶段二：强化学习（基于 GRPO）**：采用组相对策略优化（Group-Relative Policy Optimization），通过在同一提示词下生成多组候选响应并进行组内比较，降低方差并提高训练稳定性。
    3.  **跨视图空间奖励 (CVSR)**：这是本文的核心创新，包含四个子奖励：
        *   **格式奖励 ($R_{format}$)**：确保模型输出符合 `<think>` 标签和特定格式。
        *   **定位奖励 ($R_{ground}$)**：通过匈牙利算法匹配预测框与真实框的 IoU，强制模型在推理过程中定位关键物体。
        *   **重叠奖励 ($R_{overlap}$)**：要求模型识别在多个视图中共同出现的物体数量，以解决跨视图实体解析（Entity Resolution）问题。
        *   **答案正确性奖励 ($R_{ans}$)**：针对 QA 任务检查准确率，针对抓取任务计算预测坐标与真实坐标的欧氏距离。

### 3. 实验设计
*   **基准测试 (E2W-Bench)**：论文构建了一个包含 10 万个仿真样本和 6 万个真实世界样本的大规模基准，涵盖 50 多个物体类别。
    *   **E2W-1 (计数)**：跨视图统计物体总数。
    *   **E2W-2 (位置推理)**：推断未在同一视图中出现的物体间的空间关系。
    *   **E2W-3 (抓取)**：根据语言指令预测特定视图下的 2D 抓取坐标。
*   **对比方法**：
    *   **闭源模型**：GPT-5 (OpenAI 2025)、Gemini-2.5-Pro、Doubao-Seed-1.6。
    *   **开源模型**：GLM-4.5V、Qwen2.5-VL (3B/7B/32B)、LLaMA-3.2-11B、SpaceQwen。
    *   **消融变体**：SFT-only、RL-ZERO（无 SFT 直接 RL）以及移除 CVSR 各组件的对比。
*   **外部验证**：在 Where2Place 空间定位基准上进行泛化性测试。
*   **真实世界验证**：使用两台 Franka 机械臂和一台 Realman 移动底座进行实机协作抓取实验。

### 4. 资源与算力
*   **硬件设施**：实验使用了 **8 × NVIDIA H200 GPU**。
*   **训练细节**：
    *   SFT 阶段：训练 3 个 epoch，学习率 $2 \times 10^{-5}$。
    *   RL 阶段：运行 200 个 step，学习率 $1 \times 10^{-6}$，每组采样 $G=8$ 个响应。
*   **时长**：文中未明确给出总训练小时数，但考虑到 H200 的算力和 200 个 RL step 的规模，训练过程相对高效。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估（基于 16 万+样本），并在 3B 和 7B 两种参数规模的骨干网络上验证了有效性。
*   **消融实验**：非常充分。不仅验证了 CVSR 中每个奖励项（定位、重叠、答案、格式）的作用，还对比了“多视图协作”与“单全局视图”的效果。
*   **客观性**：对比了当前最顶尖的闭源模型（如 GPT-5），并使用了 scene-disjoint（场景不相交）的划分方式确保测试的严谨性。实机实验进一步增强了结果的说服力。

### 6. 主要结论与发现
1.  **CoRL 显著优于基线**：在 E2W 基准上，CoRL-7B 在推理任务上达到 82.67% 的准确率，远超 GPT-5 (54.50%) 和原始 Qwen2.5-VL。
2.  **RL 的必要性**：单纯的 SFT 虽然有提升，但 RL 阶段通过 CVSR 提供的密集反馈，显著增强了模型在复杂感知任务（如抓取坐标预测）中的表现。
3.  **多视图优于单视图**：实验证明，多个分布式的局部视角比单个全局视角能提供更丰富的几何细节，减少歧义。
4.  **泛化能力**：模型在未见过的真实世界场景和外部基准测试中均表现出良好的鲁棒性。

### 7. 优点
*   **任务定义新颖**：首次系统化地定义了具身智能中的“从局部到世界”的协作空间推理任务。
*   **奖励设计精巧**：CVSR 奖励机制成功地将抽象的逻辑推理与具体的视觉定位（Bounding Box）挂钩，解决了 VLM 在复杂空间任务中“幻觉”严重的问题。
*   **闭环验证**：从仿真训练到真实机器人部署的完整链路验证，具有很强的工程参考价值。

### 8. 不足与局限
*   **同步性假设**：目前框架处理的是静态、同步的多视图快照，未考虑真实系统中可能存在的相机延迟或异步观测问题。
*   **中心化瓶颈**：采用中心化 VLM 处理所有视图，当智能体数量大规模增加时，通信带宽和计算压力会成为瓶颈。
*   **维度限制**：目前的坐标预测仍基于 2D 图像平面，虽然结合深度图可以实现 3D 抓取，但 VLM 本身尚未直接进行端到端的 3D 空间表征学习。

（完）
