---
title: "MOSAIC: A Unified Platform for Cross-Paradigm Comparison and Evaluation of Homogeneous and Heterogeneous Multi-Agent RL, LLM, VLM, and Human Decision-Makers"
title_zh: MOSAIC：一个用于同质与异质多智能体强化学习（RL）、大语言模型（LLM）、视觉语言模型（VLM）及人类决策者跨范式比较与评估的统一平台
authors: "Abdulhamid M. Mousa, Yu Fu, Rakhmonberdi Khajiev, Jalaledin M. Azzabi, Abdulkarim M. Mousa, Peng Yang, Yunusa Haruna, Ming Liu"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.01260v1"
tags: ["query:vlm-rl"]
score: 9.0
evidence: 在决策环境中评估VLM和RL智能体的平台
tldr: MOSAIC是一个开源平台，旨在解决强化学习（RL）、大语言模型（LLM）、视觉语言模型（VLM）及人类决策者在多智能体环境中难以统一部署和公平比较的问题。它通过IPC协议和算子抽象，实现了不同范式智能体的无缝集成与协作。该平台支持确定性的跨范式评估，提供手动和脚本两种模式，为研究异构智能体交互和行为差异提供了可复现的视觉化工具。
selection_source: fresh_fetch
motivation: 现有基础设施缺乏在同一环境中部署和公平比较来自不同决策范式（如RL、LLM、人类）智能体的能力。
method: 引入基于IPC的工人协议隔离不同框架，通过算子抽象统一接口，并提供确定性的手动与脚本评估模式。
result: 成功构建了一个支持多种异构智能体在复杂环境中进行即时组队、行为对比和可复现实验的视觉化平台。
conclusion: MOSAIC为跨范式的多智能体研究提供了统一的评估标准和工具，促进了RL、LLM与人机交互领域的融合。
---

## 摘要
强化学习（RL）、大语言模型（LLM）和视觉语言模型（VLM）已被广泛地独立研究。然而，现有的基础设施缺乏在同一环境中部署来自不同决策范式的智能体的能力，这使得在混合多智能体设置中研究它们，或在相同条件下公平地比较它们的行为变得十分困难。我们提出了 MOSAIC，这是一个开源平台，通过整合多种现有的强化学习环境，并使异质智能体（RL 策略、LLM、VLM 和人类玩家）能够在即时（ad-hoc）团队设置中运行并获得可复现的结果，从而填补了这一空白。MOSAIC 提出了三项贡献：(i) 一种基于进程间通信（IPC）的工作节点协议，将原生和第三方框架封装为隔离的子进程工作节点，每个节点在不修改的情况下执行其原生的训练和推理逻辑，并通过版本化的进程间协议进行通信。(ii) 一种算子（operator）抽象，通过将工作节点映射到智能体来形成智能体级接口：无论是由 RL 策略、LLM 还是人类支持，每个算子都遵循一个最小化的统一接口。(iii) 一个确定性的跨范式评估框架，提供两种互补模式：一种是手动模式，在共享种子的条件下以步进同步（lock-step）方式推进多达 N 个并发算子，以便对行为差异进行细粒度的视觉检查；另一种是脚本模式，通过声明式 Python 脚本驱动自动化的长期运行评估，以实现可复现的实验。我们将 MOSAIC 作为一个开放的、视觉优先的平台发布，以促进 RL、LLM 和人机回环（human-in-the-loop）社区中可复现的跨范式研究。

## Abstract
Reinforcement learning (RL), large language models (LLMs), and vision-language models (VLMs) have been widely studied in isolation. However, existing infrastructure lacks the ability to deploy agents from different decision-making paradigms within the same environment, making it difficult to study them in hybrid multi-agent settings or to compare their behaviour fairly under identical conditions. We present MOSAIC, an open-source platform that bridges this gap by incorporating a diverse set of existing reinforcement learning environments and enabling heterogeneous agents (RL policies, LLMs, VLMs, and human players) to operate within them in ad-hoc team settings with reproducible results. MOSAIC introduces three contributions. (i) An IPC-based worker protocol that wraps both native and third-party frameworks as isolated subprocess workers, each executing its native training and inference logic unmodified, communicating through a versioned inter-process protocol. (ii) An operator abstraction that forms an agent-level interface by mapping workers to agents: each operator, regardless of whether it is backed by an RL policy, an LLM, or a human, conforms to a minimal unified interface. (iii) A deterministic cross-paradigm evaluation framework offering two complementary modes: a manual mode that advances up to N concurrent operators in lock-step under shared seeds for fine-grained visual inspection of behavioural differences, and a script mode that drives automated, long-running evaluation through declarative Python scripts, for reproducible experiments. We release MOSAIC as an open, visual-first platform to facilitate reproducible cross-paradigm research across the RL, LLM, and human-in-the-loop communities.

---

## 论文详细总结（自动生成）

### MOSAIC 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：强化学习（RL）、大语言模型（LLM）、视觉语言模型（VLM）和人类决策者在现有的研究中通常是孤立存在的。目前缺乏一个统一的基础设施，能够让这些来自不同决策范式的智能体在同一个环境中部署、交互并进行公平的性能比较。
*   **研究背景**：
    *   **接口碎片化**：RL 框架期望张量输入和整数动作；LLM 期望文本提示；人类需要交互界面。
    *   **协作复杂性**：现有的即时团队协作（Ad-hoc Teamwork）研究通常假设所有智能体共享相同的观测和动作表示，无法处理跨范式的异构团队。
    *   **评估不公**：缺乏在相同环境实例、共享随机种子下对不同类型智能体进行细粒度行为对比的工具。

#### 2. 方法论与关键技术
MOSAIC 采用了三层架构（编排层、通信层、执行层），核心技术包括：
*   **基于 IPC 的工人协议（Worker Protocol）**：
    *   使用进程间通信（IPC）将第三方框架（如 CleanRL, XuanCe, Ray RLlib）封装为独立的子进程。
    *   通过轻量级 JSON 协议（stdin/stdout）交换命令（reset, step, stop）和响应（观测、奖励、渲染数据）。
    *   **零侵入性**：无需修改原始库的源代码，仅需少量“胶水代码”（约 50-120 行）即可集成。
*   **算子抽象（Operator Abstraction）**：
    *   定义了统一的智能体级接口 `OperatorController`。
    *   无论底层是 RL 策略、LLM 还是人类，都通过 `select_action` 接口进行交互，实现了智能体与环境逻辑的解耦。
*   **确定性跨范式评估框架**：
    *   **手动模式**：支持 N 个算子在共享种子下步进同步运行，通过 Qt6 GUI 进行侧向对比和视觉检查。
    *   **脚本模式**：通过声明式 Python 脚本驱动自动化实验，生成统一的遥测数据（Telemetry）。

#### 3. 实验设计与基准测试
*   **环境支持**：集成了 26 个环境家族，涵盖 MiniGrid、MultiGrid（足球、推箱子）、Chess、Atari（通过文本/视觉）、Jumanji 等。
*   **智能体范式**：
    *   **RL**：支持 PPO, MAPPO, QMIX, DQN 等。
    *   **LLM/VLM**：集成 BALROG 框架，支持 GPT-4o, Claude, Gemini 等。
    *   **人类**：支持键盘输入的人机回环评估。
*   **对比基准**：论文将 MOSAIC 与 21 个现有框架（如 RLlib, CleanRL, AgentBench, BALROG 等）进行了功能对比，证明了其在多范式支持和跨范式评估上的唯一性。
*   **实验配置（附录 A）**：设计了对抗性匹配（如 RL vs LLM）和协作性异构团队（如 RL + LLM 协作）等多种实验矩阵。

#### 4. 资源与算力
*   **算力说明**：文中提到感谢北京航空航天大学和北京理工大学提供的计算资源。
*   **具体细节**：由于本文侧重于**平台开发与基础设施展示**，并未详细列出训练特定模型所需的 GPU 型号、数量或具体训练时长。
*   **集成成本**：论文给出了集成不同框架所需的代码行数（LOC），例如集成 CleanRL 仅需约 50 行代码，显示了平台的高效性。

#### 5. 实验数量与充分性
*   **测试覆盖**：包含 28 个以上的测试文件，涵盖种子复现性、训练/评估一致性、开销基准测试等。
*   **环境多样性**：支持 26 种环境，足以证明平台的通用性。
*   **局限性**：作为平台发布论文，本文主要展示了“如何做实验”和“实验配置的可能性”，而大规模的跨范式实证结果（如 GPT-4o 在所有环境下的具体得分）被安排在后续的伴随论文中。

#### 6. 主要结论与发现
*   **标准化意义**：MOSAIC 成功标准化了智能体侧的接口，补充了 Gymnasium 在环境侧的标准化工作。
*   **跨范式可行性**：证明了通过 IPC 隔离和统一算子协议，可以在不修改算法源码的情况下实现 RL、LLM 和人类的同场竞技。
*   **协作发现**：初步配置显示，LLM 在离散网格世界中具有较强的战略推理潜力，但在高频连续控制任务中仍面临挑战。

#### 7. 优点
*   **视觉优先**：提供强大的 GUI，允许研究者直观地观察不同范式智能体在相同局势下的决策差异。
*   **高度解耦**：通过进程隔离，避免了不同框架（如不同版本的 PyTorch 或 Ray）之间的依赖冲突。
*   **可复现性**：强调共享种子和确定性评估，为跨学科比较提供了严谨的基础。
*   **开源友好**：模块化安装，用户可根据需求仅安装特定的工人或环境。

#### 8. 不足与局限
*   **环境限制**：目前 LLM/VLM 智能体主要局限于离散网格世界，对于 MuJoCo 等连续控制环境，由于 LLM 的空间推理和反应延迟限制，尚未完全适配。
*   **性能开销**：基于 IPC 和 JSON 的通信虽然解耦性好，但在极高频率的交互任务中可能存在一定的通信延迟。
*   **实证数据缺失**：本文重点在于平台架构，缺乏大规模的跨范式性能基准数据（Benchmark Results），读者需等待后续研究以获取具体的模型表现对比。

（完）
